const aws = @import("aws");
const std = @import("std");

const Client = @import("client.zig").Client;
const CallOptions = @import("call_options.zig").CallOptions;
const ServiceError = @import("errors.zig").ServiceError;
const Operation = @import("operation.zig").Operation;

pub const UpdateRelationalDatabaseInput = struct {
    /// When `true`, applies changes immediately. When `false`, applies
    /// changes during the preferred maintenance window. Some changes may cause an
    /// outage.
    ///
    /// Default: `false`
    apply_immediately: ?bool = null,

    /// Indicates the certificate that needs to be associated with the database.
    ca_certificate_identifier: ?[]const u8 = null,

    /// When `true`, disables automated backup retention for your database.
    ///
    /// Disabling backup retention deletes all automated database backups. Before
    /// disabling this,
    /// you may want to create a snapshot of your database using the `create
    /// relational database
    /// snapshot` operation.
    ///
    /// Updates are applied during the next maintenance window because this can
    /// result in an
    /// outage.
    disable_backup_retention: ?bool = null,

    /// When `true`, enables automated backup retention for your database.
    ///
    /// Updates are applied during the next maintenance window because this can
    /// result in an
    /// outage.
    enable_backup_retention: ?bool = null,

    /// The password for the master user. The password can include any printable
    /// ASCII character
    /// except "/", """, or "@".
    ///
    /// My**SQL**
    ///
    /// Constraints: Must contain from 8 to 41 characters.
    ///
    /// **PostgreSQL**
    ///
    /// Constraints: Must contain from 8 to 128 characters.
    master_user_password: ?[]const u8 = null,

    /// The daily time range during which automated backups are created for your
    /// database if
    /// automated backups are enabled.
    ///
    /// Constraints:
    ///
    /// * Must be in the `hh24:mi-hh24:mi` format.
    ///
    /// Example: `16:00-16:30`
    ///
    /// * Specified in Coordinated Universal Time (UTC).
    ///
    /// * Must not conflict with the preferred maintenance window.
    ///
    /// * Must be at least 30 minutes.
    preferred_backup_window: ?[]const u8 = null,

    /// The weekly time range during which system maintenance can occur on your
    /// database.
    ///
    /// The default is a 30-minute window selected at random from an 8-hour block of
    /// time for each
    /// Amazon Web Services Region, occurring on a random day of the week.
    ///
    /// Constraints:
    ///
    /// * Must be in the `ddd:hh24:mi-ddd:hh24:mi` format.
    ///
    /// * Valid days: Mon, Tue, Wed, Thu, Fri, Sat, Sun.
    ///
    /// * Must be at least 30 minutes.
    ///
    /// * Specified in Coordinated Universal Time (UTC).
    ///
    /// * Example: `Tue:17:00-Tue:17:30`
    preferred_maintenance_window: ?[]const u8 = null,

    /// Specifies the accessibility options for your database. A value of `true`
    /// specifies a database that is available to resources outside of your
    /// Lightsail account. A
    /// value of `false` specifies a database that is available only to your
    /// Lightsail
    /// resources in the same region as your database.
    publicly_accessible: ?bool = null,

    /// This parameter is used to update the major version of the database. Enter
    /// the
    /// `blueprintId` for the major version that you want to update to.
    ///
    /// Use the
    /// [GetRelationalDatabaseBlueprints](https://docs.aws.amazon.com/lightsail/2016-11-28/api-reference/API_GetRelationalDatabaseBlueprints.html) action to get a list of available blueprint
    /// IDs.
    relational_database_blueprint_id: ?[]const u8 = null,

    /// The name of your Lightsail database resource to update.
    relational_database_name: []const u8,

    /// When `true`, the master user password is changed to a new strong password
    /// generated by Lightsail.
    ///
    /// Use the `get relational database master user password` operation to get the
    /// new
    /// password.
    rotate_master_user_password: ?bool = null,

    pub const json_field_names = .{
        .apply_immediately = "applyImmediately",
        .ca_certificate_identifier = "caCertificateIdentifier",
        .disable_backup_retention = "disableBackupRetention",
        .enable_backup_retention = "enableBackupRetention",
        .master_user_password = "masterUserPassword",
        .preferred_backup_window = "preferredBackupWindow",
        .preferred_maintenance_window = "preferredMaintenanceWindow",
        .publicly_accessible = "publiclyAccessible",
        .relational_database_blueprint_id = "relationalDatabaseBlueprintId",
        .relational_database_name = "relationalDatabaseName",
        .rotate_master_user_password = "rotateMasterUserPassword",
    };
};

pub const UpdateRelationalDatabaseOutput = struct {
    /// An array of objects that describe the result of the action, such as the
    /// status of the
    /// request, the timestamp of the request, and the resources affected by the
    /// request.
    operations: ?[]const Operation = null,

    pub const json_field_names = .{
        .operations = "operations",
    };
};

pub fn execute(client: *Client, allocator: std.mem.Allocator, input: UpdateRelationalDatabaseInput, options: CallOptions) !UpdateRelationalDatabaseOutput {
    var arena = std.heap.ArenaAllocator.init(client.allocator);
    defer arena.deinit();
    const alloc = arena.allocator();

    var request = try serializeRequest(alloc, input, client.config);
    defer request.deinit(alloc);

    const creds = try client.config.credentials.getCredentials(client.allocator);
    try aws.signing.signRequest(alloc, &request, creds, client.config.region, "lightsail");

    var response = try client.http_client.sendRequest(&request);
    defer response.deinit();

    if (!response.isSuccess()) {
        if (options.diagnostic) |d| {
            d.* = parseErrorResponse(client.allocator, response.body, response.status) catch .{ .kind = .{ .unknown = .{ .http_status = @intCast(response.status) } } };
        }
        return error.ServiceError;
    }

    const result = try deserializeResponse(allocator, response.body, response.status, response.headers);
    return result;
}

fn serializeRequest(allocator: std.mem.Allocator, input: UpdateRelationalDatabaseInput, config: *aws.Config) !aws.http.Request {
    const endpoint = try config.getEndpointForService("lightsail", "Lightsail", allocator);

    const host = aws.url.parseHost(endpoint);
    const tls = !std.mem.startsWith(u8, endpoint, "http://");
    const port = aws.url.parsePort(endpoint);

    const body = try aws.json.jsonStringify(input, allocator);

    var request = aws.http.Request.init(host);
    request.method = .POST;
    request.path = "/";
    request.tls = tls;
    request.port = port;
    request.body = body;
    try request.headers.put(allocator, "Content-Type", "application/x-amz-json-1.1");
    try request.headers.put(allocator, "X-Amz-Target", "Lightsail_20161128.UpdateRelationalDatabase");

    return request;
}

fn deserializeResponse(allocator: std.mem.Allocator, body: []const u8, status: u16, headers: anytype) !UpdateRelationalDatabaseOutput {
    _ = status;
    _ = headers;
    if (body.len == 0) return .{};
    return aws.json.parseJsonObject(UpdateRelationalDatabaseOutput, body, allocator);
}

fn parseErrorResponse(allocator: std.mem.Allocator, body: []const u8, status: u16) !ServiceError {
    const error_code = blk: {
        const type_str = aws.json.findJsonValue(body, "__type") orelse break :blk @as([]const u8, "Unknown");
        if (std.mem.lastIndexOfScalar(u8, type_str, '#')) |idx| {
            break :blk type_str[idx + 1 ..];
        }
        break :blk type_str;
    };
    const error_message = aws.json.findJsonValue(body, "message") orelse aws.json.findJsonValue(body, "Message") orelse "";
    var arena = std.heap.ArenaAllocator.init(allocator);
    errdefer arena.deinit();
    const arena_alloc = arena.allocator();
    const owned_message = try arena_alloc.dupe(u8, error_message);
    const owned_request_id = try arena_alloc.dupe(u8, "");

    if (std.mem.eql(u8, error_code, "AccessDeniedException")) {
        return .{ .arena = arena, .kind = .{ .access_denied_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "AccountSetupInProgressException")) {
        return .{ .arena = arena, .kind = .{ .account_setup_in_progress_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "InvalidInputException")) {
        return .{ .arena = arena, .kind = .{ .invalid_input_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "NotFoundException")) {
        return .{ .arena = arena, .kind = .{ .not_found_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "OperationFailureException")) {
        return .{ .arena = arena, .kind = .{ .operation_failure_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "RegionSetupInProgressException")) {
        return .{ .arena = arena, .kind = .{ .region_setup_in_progress_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ServiceException")) {
        return .{ .arena = arena, .kind = .{ .service_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "UnauthenticatedException")) {
        return .{ .arena = arena, .kind = .{ .unauthenticated_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }

    const owned_code = try arena_alloc.dupe(u8, error_code);
    return .{ .arena = arena, .kind = .{ .unknown = .{
        .code = owned_code,
        .message = owned_message,
        .request_id = owned_request_id,
        .http_status = status,
    } } };
}
