const aws = @import("aws");
const std = @import("std");

const Client = @import("client.zig").Client;
const CallOptions = @import("call_options.zig").CallOptions;
const ServiceError = @import("errors.zig").ServiceError;
const LoadBalancerMetricName = @import("load_balancer_metric_name.zig").LoadBalancerMetricName;
const MetricStatistic = @import("metric_statistic.zig").MetricStatistic;
const MetricUnit = @import("metric_unit.zig").MetricUnit;
const MetricDatapoint = @import("metric_datapoint.zig").MetricDatapoint;

pub const GetLoadBalancerMetricDataInput = struct {
    /// The end time of the period.
    end_time: i64,

    /// The name of the load balancer.
    load_balancer_name: []const u8,

    /// The metric for which you want to return information.
    ///
    /// Valid load balancer metric names are listed below, along with the most
    /// useful
    /// `statistics` to include in your request, and the published `unit`
    /// value.
    ///
    /// * **
    /// `ClientTLSNegotiationErrorCount`
    /// ** - The
    /// number of TLS connections initiated by the client that did not establish a
    /// session with
    /// the load balancer due to a TLS error generated by the load balancer.
    /// Possible causes
    /// include a mismatch of ciphers or protocols.
    ///
    /// `Statistics`: The most useful statistic is `Sum`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `HealthyHostCount`
    /// ** - The number of target
    /// instances that are considered healthy.
    ///
    /// `Statistics`: The most useful statistic are `Average`,
    /// `Minimum`, and `Maximum`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `HTTPCode_Instance_2XX_Count`
    /// ** - The number
    /// of HTTP 2XX response codes generated by the target instances. This does not
    /// include any
    /// response codes generated by the load balancer.
    ///
    /// `Statistics`: The most useful statistic is `Sum`. Note that
    /// `Minimum`, `Maximum`, and `Average` all return
    /// `1`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `HTTPCode_Instance_3XX_Count`
    /// ** - The number
    /// of HTTP 3XX response codes generated by the target instances. This does not
    /// include any
    /// response codes generated by the load balancer.
    ///
    /// `Statistics`: The most useful statistic is `Sum`. Note that
    /// `Minimum`, `Maximum`, and `Average` all return
    /// `1`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `HTTPCode_Instance_4XX_Count`
    /// ** - The number
    /// of HTTP 4XX response codes generated by the target instances. This does not
    /// include any
    /// response codes generated by the load balancer.
    ///
    /// `Statistics`: The most useful statistic is `Sum`. Note that
    /// `Minimum`, `Maximum`, and `Average` all return
    /// `1`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `HTTPCode_Instance_5XX_Count`
    /// ** - The number
    /// of HTTP 5XX response codes generated by the target instances. This does not
    /// include any
    /// response codes generated by the load balancer.
    ///
    /// `Statistics`: The most useful statistic is `Sum`. Note that
    /// `Minimum`, `Maximum`, and `Average` all return
    /// `1`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `HTTPCode_LB_4XX_Count`
    /// ** - The number of
    /// HTTP 4XX client error codes that originated from the load balancer. Client
    /// errors are
    /// generated when requests are malformed or incomplete. These requests were not
    /// received by
    /// the target instance. This count does not include response codes generated by
    /// the target
    /// instances.
    ///
    /// `Statistics`: The most useful statistic is `Sum`. Note that
    /// `Minimum`, `Maximum`, and `Average` all return
    /// `1`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `HTTPCode_LB_5XX_Count`
    /// ** - The number of
    /// HTTP 5XX server error codes that originated from the load balancer. This
    /// does not include
    /// any response codes generated by the target instance. This metric is reported
    /// if there are
    /// no healthy instances attached to the load balancer, or if the request rate
    /// exceeds the
    /// capacity of the instances (spillover) or the load balancer.
    ///
    /// `Statistics`: The most useful statistic is `Sum`. Note that
    /// `Minimum`, `Maximum`, and `Average` all return
    /// `1`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `InstanceResponseTime`
    /// ** - The time elapsed,
    /// in seconds, after the request leaves the load balancer until a response from
    /// the target
    /// instance is received.
    ///
    /// `Statistics`: The most useful statistic is `Average`.
    ///
    /// `Unit`: The published unit is `Seconds`.
    ///
    /// * **
    /// `RejectedConnectionCount`
    /// ** - The number of
    /// connections that were rejected because the load balancer had reached its
    /// maximum number of
    /// connections.
    ///
    /// `Statistics`: The most useful statistic is `Sum`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `RequestCount`
    /// ** - The number of requests
    /// processed over IPv4. This count includes only the requests with a response
    /// generated by a
    /// target instance of the load balancer.
    ///
    /// `Statistics`: The most useful statistic is `Sum`. Note that
    /// `Minimum`, `Maximum`, and `Average` all return
    /// `1`.
    ///
    /// `Unit`: The published unit is `Count`.
    ///
    /// * **
    /// `UnhealthyHostCount`
    /// ** - The number of
    /// target instances that are considered unhealthy.
    ///
    /// `Statistics`: The most useful statistic are `Average`,
    /// `Minimum`, and `Maximum`.
    ///
    /// `Unit`: The published unit is `Count`.
    metric_name: LoadBalancerMetricName,

    /// The granularity, in seconds, of the returned data points.
    period: i32,

    /// The start time of the period.
    start_time: i64,

    /// The statistic for the metric.
    ///
    /// The following statistics are available:
    ///
    /// * `Minimum` - The lowest value observed during the specified period. Use
    ///   this
    /// value to determine low volumes of activity for your application.
    ///
    /// * `Maximum` - The highest value observed during the specified period. Use
    /// this value to determine high volumes of activity for your application.
    ///
    /// * `Sum` - All values submitted for the matching metric added together. You
    /// can use this statistic to determine the total volume of a metric.
    ///
    /// * `Average` - The value of Sum / SampleCount during the specified period. By
    /// comparing this statistic with the Minimum and Maximum values, you can
    /// determine the full
    /// scope of a metric and how close the average use is to the Minimum and
    /// Maximum values. This
    /// comparison helps you to know when to increase or decrease your resources.
    ///
    /// * `SampleCount` - The count, or number, of data points used for the
    /// statistical calculation.
    statistics: []const MetricStatistic,

    /// The unit for the metric data request. Valid units depend on the metric data
    /// being
    /// requested. For the valid units with each available metric, see the
    /// `metricName`
    /// parameter.
    unit: MetricUnit,

    pub const json_field_names = .{
        .end_time = "endTime",
        .load_balancer_name = "loadBalancerName",
        .metric_name = "metricName",
        .period = "period",
        .start_time = "startTime",
        .statistics = "statistics",
        .unit = "unit",
    };
};

pub const GetLoadBalancerMetricDataOutput = struct {
    /// An array of objects that describe the metric data returned.
    metric_data: ?[]const MetricDatapoint = null,

    /// The name of the metric returned.
    metric_name: ?LoadBalancerMetricName = null,

    pub const json_field_names = .{
        .metric_data = "metricData",
        .metric_name = "metricName",
    };
};

pub fn execute(client: *Client, allocator: std.mem.Allocator, input: GetLoadBalancerMetricDataInput, options: CallOptions) !GetLoadBalancerMetricDataOutput {
    var arena = std.heap.ArenaAllocator.init(client.allocator);
    defer arena.deinit();
    const alloc = arena.allocator();

    var request = try serializeRequest(alloc, input, client.config);
    defer request.deinit(alloc);

    const creds = try client.config.credentials.getCredentials(alloc);
    try aws.signing.signRequest(alloc, &request, creds, client.config.region, "lightsail");

    var response = try client.http_client.sendRequest(&request);
    defer response.deinit();

    if (!response.isSuccess()) {
        if (options.diagnostic) |d| {
            d.* = parseErrorResponse(client.allocator, response.body, response.status) catch .{ .kind = .{ .unknown = .{ .http_status = @intCast(response.status) } } };
        }
        return error.ServiceError;
    }

    const result = try deserializeResponse(allocator, response.body, response.status, response.headers);
    return result;
}

fn serializeRequest(allocator: std.mem.Allocator, input: GetLoadBalancerMetricDataInput, config: *aws.Config) !aws.http.Request {
    const endpoint = try config.getEndpointForService("lightsail", "Lightsail", allocator);

    const host = aws.url.parseHost(endpoint);
    const tls = !std.mem.startsWith(u8, endpoint, "http://");
    const port = aws.url.parsePort(endpoint);

    const body = try aws.json.jsonStringify(input, allocator);

    var request = aws.http.Request.init(host);
    request.method = .POST;
    request.path = "/";
    request.tls = tls;
    request.port = port;
    request.body = body;
    try request.headers.put(allocator, "Content-Type", "application/x-amz-json-1.1");
    try request.headers.put(allocator, "X-Amz-Target", "Lightsail_20161128.GetLoadBalancerMetricData");

    return request;
}

fn deserializeResponse(allocator: std.mem.Allocator, body: []const u8, status: u16, headers: anytype) !GetLoadBalancerMetricDataOutput {
    _ = status;
    _ = headers;
    if (body.len == 0) return .{};
    return aws.json.parseJsonObject(GetLoadBalancerMetricDataOutput, body, allocator);
}

fn parseErrorResponse(allocator: std.mem.Allocator, body: []const u8, status: u16) !ServiceError {
    const error_code = blk: {
        const type_str = aws.json.findJsonValue(body, "__type") orelse break :blk @as([]const u8, "Unknown");
        if (std.mem.lastIndexOfScalar(u8, type_str, '#')) |idx| {
            break :blk type_str[idx + 1 ..];
        }
        break :blk type_str;
    };
    const error_message = aws.json.findJsonValue(body, "message") orelse aws.json.findJsonValue(body, "Message") orelse "";
    var arena = std.heap.ArenaAllocator.init(allocator);
    errdefer arena.deinit();
    const arena_alloc = arena.allocator();
    const owned_message = try arena_alloc.dupe(u8, error_message);
    const owned_request_id = try arena_alloc.dupe(u8, "");

    if (std.mem.eql(u8, error_code, "AccessDeniedException")) {
        return .{ .arena = arena, .kind = .{ .access_denied_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "AccountSetupInProgressException")) {
        return .{ .arena = arena, .kind = .{ .account_setup_in_progress_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "InvalidInputException")) {
        return .{ .arena = arena, .kind = .{ .invalid_input_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "NotFoundException")) {
        return .{ .arena = arena, .kind = .{ .not_found_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "OperationFailureException")) {
        return .{ .arena = arena, .kind = .{ .operation_failure_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "RegionSetupInProgressException")) {
        return .{ .arena = arena, .kind = .{ .region_setup_in_progress_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ServiceException")) {
        return .{ .arena = arena, .kind = .{ .service_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "UnauthenticatedException")) {
        return .{ .arena = arena, .kind = .{ .unauthenticated_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }

    const owned_code = try arena_alloc.dupe(u8, error_code);
    return .{ .arena = arena, .kind = .{ .unknown = .{
        .code = owned_code,
        .message = owned_message,
        .request_id = owned_request_id,
        .http_status = status,
    } } };
}
