pub const FormButtonsPosition = enum {
    top,
    bottom,
    top_and_bottom,

    pub const json_field_names = .{
        .top = "TOP",
        .bottom = "BOTTOM",
        .top_and_bottom = "TOP_AND_BOTTOM",
    };
};
