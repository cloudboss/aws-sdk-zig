const MetadataSchemaEntry = @import("metadata_schema_entry.zig").MetadataSchemaEntry;

/// Schema for metadata on memory records generated by a strategy.
pub const MemoryRecordSchema = struct {
    /// The metadata field definitions for this strategy.
    metadata_schema: ?[]const MetadataSchemaEntry = null,

    pub const json_field_names = .{
        .metadata_schema = "metadataSchema",
    };
};
