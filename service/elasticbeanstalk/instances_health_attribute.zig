pub const InstancesHealthAttribute = enum {
    health_status,
    color,
    causes,
    application_metrics,
    refreshed_at,
    launched_at,
    system,
    deployment,
    availability_zone,
    instance_type,
    all,
};
