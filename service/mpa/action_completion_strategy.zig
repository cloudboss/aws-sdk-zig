pub const ActionCompletionStrategy = enum {
    auto_completion_upon_approval,

    pub const json_field_names = .{
        .auto_completion_upon_approval = "AUTO_COMPLETION_UPON_APPROVAL",
    };
};
