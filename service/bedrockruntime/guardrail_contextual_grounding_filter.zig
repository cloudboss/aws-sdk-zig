const GuardrailContextualGroundingPolicyAction = @import("guardrail_contextual_grounding_policy_action.zig").GuardrailContextualGroundingPolicyAction;
const GuardrailContextualGroundingFilterType = @import("guardrail_contextual_grounding_filter_type.zig").GuardrailContextualGroundingFilterType;

/// The details for the guardrails contextual grounding filter.
pub const GuardrailContextualGroundingFilter = struct {
    /// The action performed by the guardrails contextual grounding filter.
    action: GuardrailContextualGroundingPolicyAction,

    /// Indicates whether content that fails the contextual grounding evaluation
    /// (grounding or relevance score less than the corresponding threshold) was
    /// detected.
    detected: ?bool = null,

    /// The score generated by contextual grounding filter.
    score: f64,

    /// The threshold used by contextual grounding filter to determine whether the
    /// content is grounded or not.
    threshold: f64,

    /// The contextual grounding filter type.
    @"type": GuardrailContextualGroundingFilterType,

    pub const json_field_names = .{
        .action = "action",
        .detected = "detected",
        .score = "score",
        .threshold = "threshold",
        .@"type" = "type",
    };
};
