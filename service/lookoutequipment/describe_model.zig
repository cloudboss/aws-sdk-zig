const aws = @import("aws");
const std = @import("std");

const Client = @import("client.zig").Client;
const ServiceError = @import("errors.zig").ServiceError;
const DataPreProcessingConfiguration = @import("data_pre_processing_configuration.zig").DataPreProcessingConfiguration;
const LabelsInputConfiguration = @import("labels_input_configuration.zig").LabelsInputConfiguration;
const ModelVersionStatus = @import("model_version_status.zig").ModelVersionStatus;
const ModelDiagnosticsOutputConfiguration = @import("model_diagnostics_output_configuration.zig").ModelDiagnosticsOutputConfiguration;
const ModelQuality = @import("model_quality.zig").ModelQuality;
const RetrainingSchedulerStatus = @import("retraining_scheduler_status.zig").RetrainingSchedulerStatus;
const ModelStatus = @import("model_status.zig").ModelStatus;

pub const DescribeModelInput = struct {
    /// The name of the machine learning model to be described.
    model_name: []const u8,

    pub const json_field_names = .{
        .model_name = "ModelName",
    };
};

pub const DescribeModelOutput = struct {
    /// Indicates the end time of the inference data that has been accumulated.
    accumulated_inference_data_end_time: ?i64 = null,

    /// Indicates the start time of the inference data that has been accumulated.
    accumulated_inference_data_start_time: ?i64 = null,

    /// The name of the model version used by the inference schedular when running a
    /// scheduled
    /// inference execution.
    active_model_version: ?i64 = null,

    /// The Amazon Resource Name (ARN) of the model version used by the inference
    /// scheduler when
    /// running a scheduled inference execution.
    active_model_version_arn: ?[]const u8 = null,

    /// Indicates the time and date at which the machine learning model was created.
    created_at: ?i64 = null,

    /// The configuration is the `TargetSamplingRate`, which is the sampling rate of
    /// the data after post processing by Amazon Lookout for Equipment. For example,
    /// if you provide data that has been
    /// collected at a 1 second level and you want the system to resample the data
    /// at a 1 minute
    /// rate before training, the `TargetSamplingRate` is 1 minute.
    ///
    /// When providing a value for the `TargetSamplingRate`, you must attach the
    /// prefix "PT" to the rate you want. The value for a 1 second rate is therefore
    /// *PT1S*, the value for a 15 minute rate is *PT15M*,
    /// and the value for a 1 hour rate is *PT1H*
    data_pre_processing_configuration: ?DataPreProcessingConfiguration = null,

    /// The Amazon Resouce Name (ARN) of the dataset used to create the machine
    /// learning model
    /// being described.
    dataset_arn: ?[]const u8 = null,

    /// The name of the dataset being used by the machine learning being described.
    dataset_name: ?[]const u8 = null,

    /// Indicates the time reference in the dataset that was used to end the subset
    /// of
    /// evaluation data for the machine learning model.
    evaluation_data_end_time: ?i64 = null,

    /// Indicates the time reference in the dataset that was used to begin the
    /// subset of
    /// evaluation data for the machine learning model.
    evaluation_data_start_time: ?i64 = null,

    /// If the training of the machine learning model failed, this indicates the
    /// reason for that
    /// failure.
    failed_reason: ?[]const u8 = null,

    /// The date and time when the import job was completed. This field appears if
    /// the active
    /// model version was imported.
    import_job_end_time: ?i64 = null,

    /// The date and time when the import job was started. This field appears if the
    /// active
    /// model version was imported.
    import_job_start_time: ?i64 = null,

    /// Specifies configuration information about the labels input, including its S3
    /// location.
    labels_input_configuration: ?LabelsInputConfiguration = null,

    /// Indicates the last time the machine learning model was updated. The type of
    /// update is
    /// not specified.
    last_updated_time: ?i64 = null,

    /// Indicates the number of days of data used in the most recent scheduled
    /// retraining run.
    latest_scheduled_retraining_available_data_in_days: ?i32 = null,

    /// If the model version was generated by retraining and the training failed,
    /// this indicates
    /// the reason for that failure.
    latest_scheduled_retraining_failed_reason: ?[]const u8 = null,

    /// Indicates the most recent model version that was generated by retraining.
    latest_scheduled_retraining_model_version: ?i64 = null,

    /// Indicates the start time of the most recent scheduled retraining run.
    latest_scheduled_retraining_start_time: ?i64 = null,

    /// Indicates the status of the most recent scheduled retraining run.
    latest_scheduled_retraining_status: ?ModelVersionStatus = null,

    /// The Amazon Resource Name (ARN) of the machine learning model being
    /// described.
    model_arn: ?[]const u8 = null,

    /// Configuration information for the model's pointwise model diagnostics.
    model_diagnostics_output_configuration: ?ModelDiagnosticsOutputConfiguration = null,

    /// The Model Metrics show an aggregated summary of the model's performance
    /// within the
    /// evaluation time range. This is the JSON content of the metrics created when
    /// evaluating the
    /// model.
    model_metrics: ?[]const u8 = null,

    /// The name of the machine learning model being described.
    model_name: ?[]const u8 = null,

    /// Provides a quality assessment for a model that uses labels. If Lookout for
    /// Equipment determines that the
    /// model quality is poor based on training metrics, the value is
    /// `POOR_QUALITY_DETECTED`. Otherwise, the value is
    /// `QUALITY_THRESHOLD_MET`.
    ///
    /// If the model is unlabeled, the model quality can't
    /// be assessed and the value of `ModelQuality` is
    /// `CANNOT_DETERMINE_QUALITY`. In this situation, you can get a model quality
    /// assessment by adding labels to the input dataset and retraining the model.
    ///
    /// For information about using labels with your models, see [Understanding
    /// labeling](https://docs.aws.amazon.com/lookout-for-equipment/latest/ug/understanding-labeling.html).
    ///
    /// For information about improving the quality of a model, see [Best practices
    /// with
    /// Amazon Lookout for
    /// Equipment](https://docs.aws.amazon.com/lookout-for-equipment/latest/ug/best-practices.html).
    model_quality: ?ModelQuality = null,

    /// The date the active model version was activated.
    model_version_activated_at: ?i64 = null,

    /// Indicates the date and time that the next scheduled retraining run will
    /// start on. Lookout for Equipment
    /// truncates the time you provide to the nearest UTC day.
    next_scheduled_retraining_start_date: ?i64 = null,

    /// Indicates that the asset associated with this sensor has been shut off. As
    /// long as this
    /// condition is met, Lookout for Equipment will not use data from this asset
    /// for training, evaluation, or
    /// inference.
    off_condition: ?[]const u8 = null,

    /// The model version that was set as the active model version prior to the
    /// current active
    /// model version.
    previous_active_model_version: ?i64 = null,

    /// The ARN of the model version that was set as the active model version prior
    /// to the
    /// current active model version.
    previous_active_model_version_arn: ?[]const u8 = null,

    /// The date and time when the previous active model version was activated.
    previous_model_version_activated_at: ?i64 = null,

    /// If the model version was retrained, this field shows a summary of the
    /// performance of the
    /// prior model on the new training range. You can use the information in this
    /// JSON-formatted
    /// object to compare the new model version and the prior model version.
    prior_model_metrics: ?[]const u8 = null,

    /// Indicates the status of the retraining scheduler.
    retraining_scheduler_status: ?RetrainingSchedulerStatus = null,

    /// The Amazon Resource Name (ARN) of a role with permission to access the data
    /// source for
    /// the machine learning model being described.
    role_arn: ?[]const u8 = null,

    /// A JSON description of the data that is in each time series dataset,
    /// including names,
    /// column names, and data types.
    schema: ?[]const u8 = null,

    /// Provides the identifier of the KMS key used to encrypt model data by Amazon
    /// Lookout
    /// for Equipment.
    server_side_kms_key_id: ?[]const u8 = null,

    /// The Amazon Resource Name (ARN) of the source model version. This field
    /// appears if the
    /// active model version was imported.
    source_model_version_arn: ?[]const u8 = null,

    /// Specifies the current status of the model being described. Status describes
    /// the status
    /// of the most recent action of the model.
    status: ?ModelStatus = null,

    /// Indicates the time reference in the dataset that was used to end the subset
    /// of training
    /// data for the machine learning model.
    training_data_end_time: ?i64 = null,

    /// Indicates the time reference in the dataset that was used to begin the
    /// subset of
    /// training data for the machine learning model.
    training_data_start_time: ?i64 = null,

    /// Indicates the time at which the training of the machine learning model was
    /// completed.
    training_execution_end_time: ?i64 = null,

    /// Indicates the time at which the training of the machine learning model
    /// began.
    training_execution_start_time: ?i64 = null,

    pub const json_field_names = .{
        .accumulated_inference_data_end_time = "AccumulatedInferenceDataEndTime",
        .accumulated_inference_data_start_time = "AccumulatedInferenceDataStartTime",
        .active_model_version = "ActiveModelVersion",
        .active_model_version_arn = "ActiveModelVersionArn",
        .created_at = "CreatedAt",
        .data_pre_processing_configuration = "DataPreProcessingConfiguration",
        .dataset_arn = "DatasetArn",
        .dataset_name = "DatasetName",
        .evaluation_data_end_time = "EvaluationDataEndTime",
        .evaluation_data_start_time = "EvaluationDataStartTime",
        .failed_reason = "FailedReason",
        .import_job_end_time = "ImportJobEndTime",
        .import_job_start_time = "ImportJobStartTime",
        .labels_input_configuration = "LabelsInputConfiguration",
        .last_updated_time = "LastUpdatedTime",
        .latest_scheduled_retraining_available_data_in_days = "LatestScheduledRetrainingAvailableDataInDays",
        .latest_scheduled_retraining_failed_reason = "LatestScheduledRetrainingFailedReason",
        .latest_scheduled_retraining_model_version = "LatestScheduledRetrainingModelVersion",
        .latest_scheduled_retraining_start_time = "LatestScheduledRetrainingStartTime",
        .latest_scheduled_retraining_status = "LatestScheduledRetrainingStatus",
        .model_arn = "ModelArn",
        .model_diagnostics_output_configuration = "ModelDiagnosticsOutputConfiguration",
        .model_metrics = "ModelMetrics",
        .model_name = "ModelName",
        .model_quality = "ModelQuality",
        .model_version_activated_at = "ModelVersionActivatedAt",
        .next_scheduled_retraining_start_date = "NextScheduledRetrainingStartDate",
        .off_condition = "OffCondition",
        .previous_active_model_version = "PreviousActiveModelVersion",
        .previous_active_model_version_arn = "PreviousActiveModelVersionArn",
        .previous_model_version_activated_at = "PreviousModelVersionActivatedAt",
        .prior_model_metrics = "PriorModelMetrics",
        .retraining_scheduler_status = "RetrainingSchedulerStatus",
        .role_arn = "RoleArn",
        .schema = "Schema",
        .server_side_kms_key_id = "ServerSideKmsKeyId",
        .source_model_version_arn = "SourceModelVersionArn",
        .status = "Status",
        .training_data_end_time = "TrainingDataEndTime",
        .training_data_start_time = "TrainingDataStartTime",
        .training_execution_end_time = "TrainingExecutionEndTime",
        .training_execution_start_time = "TrainingExecutionStartTime",
    };
};

pub const Options = struct {
    diagnostic: ?*ServiceError = null,
};

pub fn execute(client: *Client, allocator: std.mem.Allocator, input: DescribeModelInput, options: Options) !DescribeModelOutput {
    var arena = std.heap.ArenaAllocator.init(client.allocator);
    defer arena.deinit();
    const alloc = arena.allocator();

    var request = try serializeRequest(alloc, input, client.config);
    defer request.deinit(alloc);

    const creds = try client.config.credentials.getCredentials(alloc);
    try aws.signing.signRequest(alloc, &request, creds, client.config.region, "lookoutequipment");

    var response = try client.http_client.sendRequest(&request);
    defer response.deinit();

    if (!response.isSuccess()) {
        if (options.diagnostic) |d| {
            d.* = parseErrorResponse(client.allocator, response.body, response.status) catch .{ .kind = .{ .unknown = .{ .http_status = @intCast(response.status) } } };
        }
        return error.ServiceError;
    }

    const result = try deserializeResponse(allocator, response.body, response.status, response.headers);
    return result;
}

fn serializeRequest(allocator: std.mem.Allocator, input: DescribeModelInput, config: *aws.Config) !aws.http.Request {
    const endpoint = try config.getEndpointForService("lookoutequipment", "LookoutEquipment", allocator);

    const host = aws.url.parseHost(endpoint);
    const tls = !std.mem.startsWith(u8, endpoint, "http://");
    const port = aws.url.parsePort(endpoint);

    const body = try aws.json.jsonStringify(input, allocator);

    var request = aws.http.Request.init(host);
    request.method = .POST;
    request.path = "/";
    request.tls = tls;
    request.port = port;
    request.body = body;
    try request.headers.put(allocator, "Content-Type", "application/x-amz-json-1.0");
    try request.headers.put(allocator, "X-Amz-Target", "AWSLookoutEquipmentFrontendService.DescribeModel");

    return request;
}

fn deserializeResponse(allocator: std.mem.Allocator, body: []const u8, status: u16, headers: anytype) !DescribeModelOutput {
    _ = status;
    _ = headers;
    if (body.len == 0) return .{};
    return aws.json.parseJsonObject(DescribeModelOutput, body, allocator);
}

fn parseErrorResponse(allocator: std.mem.Allocator, body: []const u8, status: u16) !ServiceError {
    const error_code = blk: {
        const type_str = aws.json.findJsonValue(body, "__type") orelse break :blk @as([]const u8, "Unknown");
        if (std.mem.lastIndexOfScalar(u8, type_str, '#')) |idx| {
            break :blk type_str[idx + 1 ..];
        }
        break :blk type_str;
    };
    const error_message = aws.json.findJsonValue(body, "message") orelse aws.json.findJsonValue(body, "Message") orelse "";
    var arena = std.heap.ArenaAllocator.init(allocator);
    errdefer arena.deinit();
    const arena_alloc = arena.allocator();
    const owned_message = try arena_alloc.dupe(u8, error_message);
    const owned_request_id = try arena_alloc.dupe(u8, "");

    if (std.mem.eql(u8, error_code, "AccessDeniedException")) {
        return .{ .arena = arena, .kind = .{ .access_denied_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ConflictException")) {
        return .{ .arena = arena, .kind = .{ .conflict_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "InternalServerException")) {
        return .{ .arena = arena, .kind = .{ .internal_server_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ResourceNotFoundException")) {
        return .{ .arena = arena, .kind = .{ .resource_not_found_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ServiceQuotaExceededException")) {
        return .{ .arena = arena, .kind = .{ .service_quota_exceeded_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ThrottlingException")) {
        return .{ .arena = arena, .kind = .{ .throttling_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ValidationException")) {
        return .{ .arena = arena, .kind = .{ .validation_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }

    const owned_code = try arena_alloc.dupe(u8, error_code);
    return .{ .arena = arena, .kind = .{ .unknown = .{
        .code = owned_code,
        .message = owned_message,
        .request_id = owned_request_id,
        .http_status = status,
    } } };
}
