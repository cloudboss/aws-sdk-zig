const aws = @import("aws");
const std = @import("std");

const Client = @import("client.zig").Client;
const ServiceError = @import("errors.zig").ServiceError;
const BrandingConfigurationCreateInput = @import("branding_configuration_create_input.zig").BrandingConfigurationCreateInput;
const CookieSynchronizationConfiguration = @import("cookie_synchronization_configuration.zig").CookieSynchronizationConfiguration;
const EnabledType = @import("enabled_type.zig").EnabledType;
const Tag = @import("tag.zig").Tag;
const ToolbarConfiguration = @import("toolbar_configuration.zig").ToolbarConfiguration;

pub const CreateUserSettingsInput = struct {
    /// The additional encryption context of the user settings.
    additional_encryption_context: ?[]const aws.map.StringMapEntry = null,

    /// The branding configuration input that customizes the appearance of the web
    /// portal for end users. This includes a custom logo, favicon, wallpaper,
    /// localized strings, color theme, and an optional terms of service.
    branding_configuration_input: ?BrandingConfigurationCreateInput = null,

    /// A unique, case-sensitive identifier that you provide to ensure the
    /// idempotency of the request. Idempotency ensures that an API request
    /// completes only once. With an idempotent request, if the original request
    /// completes successfully, subsequent retries with the same client token
    /// returns the result from the original successful request.
    ///
    /// If you do not specify a client token, one is automatically generated by the
    /// Amazon Web Services SDK.
    client_token: ?[]const u8 = null,

    /// The configuration that specifies which cookies should be synchronized from
    /// the end user's local browser to the remote browser.
    cookie_synchronization_configuration: ?CookieSynchronizationConfiguration = null,

    /// Specifies whether the user can copy text from the streaming session to the
    /// local device.
    copy_allowed: EnabledType,

    /// The customer managed key used to encrypt sensitive information in the user
    /// settings.
    customer_managed_key: ?[]const u8 = null,

    /// Specifies whether the user can use deep links that open automatically when
    /// connecting to a session.
    deep_link_allowed: ?EnabledType = null,

    /// The amount of time that a streaming session remains active after users
    /// disconnect.
    disconnect_timeout_in_minutes: ?i32 = null,

    /// Specifies whether the user can download files from the streaming session to
    /// the local device.
    download_allowed: EnabledType,

    /// The amount of time that users can be idle (inactive) before they are
    /// disconnected from their streaming session and the disconnect timeout
    /// interval begins.
    idle_disconnect_timeout_in_minutes: ?i32 = null,

    /// Specifies whether the user can paste text from the local device to the
    /// streaming session.
    paste_allowed: EnabledType,

    /// Specifies whether the user can print to the local device.
    print_allowed: EnabledType,

    /// The tags to add to the user settings resource. A tag is a key-value pair.
    tags: ?[]const Tag = null,

    /// The configuration of the toolbar. This allows administrators to select the
    /// toolbar type and visual mode, set maximum display resolution for sessions,
    /// and choose which items are visible to end users during their sessions. If
    /// administrators do not modify these settings, end users retain control over
    /// their toolbar preferences.
    toolbar_configuration: ?ToolbarConfiguration = null,

    /// Specifies whether the user can upload files from the local device to the
    /// streaming session.
    upload_allowed: EnabledType,

    /// Specifies whether the user can use WebAuthn redirection for passwordless
    /// login to websites within the streaming session.
    web_authn_allowed: ?EnabledType = null,

    pub const json_field_names = .{
        .additional_encryption_context = "additionalEncryptionContext",
        .branding_configuration_input = "brandingConfigurationInput",
        .client_token = "clientToken",
        .cookie_synchronization_configuration = "cookieSynchronizationConfiguration",
        .copy_allowed = "copyAllowed",
        .customer_managed_key = "customerManagedKey",
        .deep_link_allowed = "deepLinkAllowed",
        .disconnect_timeout_in_minutes = "disconnectTimeoutInMinutes",
        .download_allowed = "downloadAllowed",
        .idle_disconnect_timeout_in_minutes = "idleDisconnectTimeoutInMinutes",
        .paste_allowed = "pasteAllowed",
        .print_allowed = "printAllowed",
        .tags = "tags",
        .toolbar_configuration = "toolbarConfiguration",
        .upload_allowed = "uploadAllowed",
        .web_authn_allowed = "webAuthnAllowed",
    };
};

pub const CreateUserSettingsOutput = struct {
    /// The ARN of the user settings.
    user_settings_arn: []const u8,

    pub const json_field_names = .{
        .user_settings_arn = "userSettingsArn",
    };
};

pub const Options = struct {
    diagnostic: ?*ServiceError = null,
};

pub fn execute(client: *Client, allocator: std.mem.Allocator, input: CreateUserSettingsInput, options: Options) !CreateUserSettingsOutput {
    var arena = std.heap.ArenaAllocator.init(client.allocator);
    defer arena.deinit();
    const alloc = arena.allocator();

    var request = try serializeRequest(alloc, input, client.config);
    defer request.deinit(alloc);

    const creds = try client.config.credentials.getCredentials(alloc);
    try aws.signing.signRequest(alloc, &request, creds, client.config.region, "workspacesweb");

    var response = try client.http_client.sendRequest(&request);
    defer response.deinit();

    if (!response.isSuccess()) {
        if (options.diagnostic) |d| {
            d.* = parseErrorResponse(client.allocator, response.body, response.status) catch .{ .kind = .{ .unknown = .{ .http_status = @intCast(response.status) } } };
        }
        return error.ServiceError;
    }

    const result = try deserializeResponse(allocator, response.body, response.status, response.headers);
    return result;
}

fn serializeRequest(allocator: std.mem.Allocator, input: CreateUserSettingsInput, config: *aws.Config) !aws.http.Request {
    const endpoint = try config.getEndpointForService("workspacesweb", "WorkSpaces Web", allocator);

    const host = aws.url.parseHost(endpoint);
    const tls = !std.mem.startsWith(u8, endpoint, "http://");
    const port = aws.url.parsePort(endpoint);

    const path = "/userSettings";

    var body_buf: std.ArrayList(u8) = .{};
    var has_prev = false;
    try body_buf.appendSlice(allocator, "{");

    if (input.additional_encryption_context) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"additionalEncryptionContext\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.branding_configuration_input) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"brandingConfigurationInput\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.client_token) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"clientToken\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.cookie_synchronization_configuration) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"cookieSynchronizationConfiguration\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (has_prev) try body_buf.appendSlice(allocator, ",");
    try body_buf.appendSlice(allocator, "\"copyAllowed\":");
    try aws.json.writeValue(@TypeOf(input.copy_allowed), input.copy_allowed, allocator, &body_buf);
    has_prev = true;
    if (input.customer_managed_key) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"customerManagedKey\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.deep_link_allowed) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"deepLinkAllowed\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.disconnect_timeout_in_minutes) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"disconnectTimeoutInMinutes\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (has_prev) try body_buf.appendSlice(allocator, ",");
    try body_buf.appendSlice(allocator, "\"downloadAllowed\":");
    try aws.json.writeValue(@TypeOf(input.download_allowed), input.download_allowed, allocator, &body_buf);
    has_prev = true;
    if (input.idle_disconnect_timeout_in_minutes) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"idleDisconnectTimeoutInMinutes\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (has_prev) try body_buf.appendSlice(allocator, ",");
    try body_buf.appendSlice(allocator, "\"pasteAllowed\":");
    try aws.json.writeValue(@TypeOf(input.paste_allowed), input.paste_allowed, allocator, &body_buf);
    has_prev = true;
    if (has_prev) try body_buf.appendSlice(allocator, ",");
    try body_buf.appendSlice(allocator, "\"printAllowed\":");
    try aws.json.writeValue(@TypeOf(input.print_allowed), input.print_allowed, allocator, &body_buf);
    has_prev = true;
    if (input.tags) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"tags\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.toolbar_configuration) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"toolbarConfiguration\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (has_prev) try body_buf.appendSlice(allocator, ",");
    try body_buf.appendSlice(allocator, "\"uploadAllowed\":");
    try aws.json.writeValue(@TypeOf(input.upload_allowed), input.upload_allowed, allocator, &body_buf);
    has_prev = true;
    if (input.web_authn_allowed) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"webAuthnAllowed\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }

    try body_buf.appendSlice(allocator, "}");
    const body = try body_buf.toOwnedSlice(allocator);

    var request = aws.http.Request.init(host);
    request.method = .POST;
    request.path = path;
    request.tls = tls;
    request.port = port;
    request.body = body;
    try request.headers.put(allocator, "Content-Type", "application/json");

    return request;
}

fn deserializeResponse(allocator: std.mem.Allocator, body: []const u8, status: u16, headers: anytype) !CreateUserSettingsOutput {
    var result: CreateUserSettingsOutput = .{};
    if (body.len > 0) {
        result = try aws.json.parseJsonObject(CreateUserSettingsOutput, body, allocator);
    }
    _ = status;
    _ = headers;

    return result;
}

fn parseErrorResponse(allocator: std.mem.Allocator, body: []const u8, status: u16) !ServiceError {
    const error_code = blk: {
        const type_str = aws.json.findJsonValue(body, "__type") orelse break :blk @as([]const u8, "Unknown");
        if (std.mem.lastIndexOfScalar(u8, type_str, '#')) |idx| {
            break :blk type_str[idx + 1 ..];
        }
        break :blk type_str;
    };
    const error_message = aws.json.findJsonValue(body, "message") orelse aws.json.findJsonValue(body, "Message") orelse "";
    var arena = std.heap.ArenaAllocator.init(allocator);
    errdefer arena.deinit();
    const arena_alloc = arena.allocator();
    const owned_message = try arena_alloc.dupe(u8, error_message);
    const owned_request_id = try arena_alloc.dupe(u8, "");

    if (std.mem.eql(u8, error_code, "AccessDeniedException")) {
        return .{ .arena = arena, .kind = .{ .access_denied_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ConflictException")) {
        return .{ .arena = arena, .kind = .{ .conflict_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "InternalServerException")) {
        return .{ .arena = arena, .kind = .{ .internal_server_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ResourceNotFoundException")) {
        return .{ .arena = arena, .kind = .{ .resource_not_found_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ServiceQuotaExceededException")) {
        return .{ .arena = arena, .kind = .{ .service_quota_exceeded_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ThrottlingException")) {
        return .{ .arena = arena, .kind = .{ .throttling_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "TooManyTagsException")) {
        return .{ .arena = arena, .kind = .{ .too_many_tags_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ValidationException")) {
        return .{ .arena = arena, .kind = .{ .validation_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }

    const owned_code = try arena_alloc.dupe(u8, error_code);
    return .{ .arena = arena, .kind = .{ .unknown = .{
        .code = owned_code,
        .message = owned_message,
        .request_id = owned_request_id,
        .http_status = status,
    } } };
}
