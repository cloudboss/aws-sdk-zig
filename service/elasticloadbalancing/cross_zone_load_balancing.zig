/// Information about the `CrossZoneLoadBalancing` attribute.
pub const CrossZoneLoadBalancing = struct {
    /// Specifies whether cross-zone load balancing is enabled for the load
    /// balancer.
    enabled: bool = false,
};
