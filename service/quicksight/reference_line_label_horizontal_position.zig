pub const ReferenceLineLabelHorizontalPosition = enum {
    left,
    center,
    right,

    pub const json_field_names = .{
        .left = "LEFT",
        .center = "CENTER",
        .right = "RIGHT",
    };
};
