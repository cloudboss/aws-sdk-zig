/// Describes the monitoring for the instance.
pub const LaunchTemplatesMonitoring = struct {
    /// Indicates whether detailed monitoring is enabled. Otherwise, basic
    /// monitoring is
    /// enabled.
    enabled: ?bool,
};
