const aws = @import("aws");
const std = @import("std");

const AcceleratorCount = @import("accelerator_count.zig").AcceleratorCount;
const AcceleratorCountRequest = @import("accelerator_count_request.zig").AcceleratorCountRequest;
const AcceleratorManufacturer = @import("accelerator_manufacturer.zig").AcceleratorManufacturer;
const AcceleratorName = @import("accelerator_name.zig").AcceleratorName;
const AcceleratorTotalMemoryMiB = @import("accelerator_total_memory_mi_b.zig").AcceleratorTotalMemoryMiB;
const AcceleratorTotalMemoryMiBRequest = @import("accelerator_total_memory_mi_b_request.zig").AcceleratorTotalMemoryMiBRequest;
const AcceleratorType = @import("accelerator_type.zig").AcceleratorType;
const AccessScopeAnalysisFinding = @import("access_scope_analysis_finding.zig").AccessScopeAnalysisFinding;
const AccessScopePath = @import("access_scope_path.zig").AccessScopePath;
const AccessScopePathRequest = @import("access_scope_path_request.zig").AccessScopePathRequest;
const AccountAttribute = @import("account_attribute.zig").AccountAttribute;
const AccountAttributeName = @import("account_attribute_name.zig").AccountAttributeName;
const AccountAttributeValue = @import("account_attribute_value.zig").AccountAttributeValue;
const ActiveInstance = @import("active_instance.zig").ActiveInstance;
const ActiveVpnTunnelStatus = @import("active_vpn_tunnel_status.zig").ActiveVpnTunnelStatus;
const ActivityStatus = @import("activity_status.zig").ActivityStatus;
const AddIpamOperatingRegion = @import("add_ipam_operating_region.zig").AddIpamOperatingRegion;
const AddIpamOrganizationalUnitExclusion = @import("add_ipam_organizational_unit_exclusion.zig").AddIpamOrganizationalUnitExclusion;
const AddPrefixListEntry = @import("add_prefix_list_entry.zig").AddPrefixListEntry;
const AddedPrincipal = @import("added_principal.zig").AddedPrincipal;
const AdditionalDetail = @import("additional_detail.zig").AdditionalDetail;
const Address = @import("address.zig").Address;
const AddressAttribute = @import("address_attribute.zig").AddressAttribute;
const AddressFamily = @import("address_family.zig").AddressFamily;
const AddressTransfer = @import("address_transfer.zig").AddressTransfer;
const AddressTransferStatus = @import("address_transfer_status.zig").AddressTransferStatus;
const AllocationState = @import("allocation_state.zig").AllocationState;
const AllocationStrategy = @import("allocation_strategy.zig").AllocationStrategy;
const AllocationType = @import("allocation_type.zig").AllocationType;
const AllowedPrincipal = @import("allowed_principal.zig").AllowedPrincipal;
const AllowsMultipleInstanceTypes = @import("allows_multiple_instance_types.zig").AllowsMultipleInstanceTypes;
const AlternatePathHint = @import("alternate_path_hint.zig").AlternatePathHint;
const AmdSevSnpSpecification = @import("amd_sev_snp_specification.zig").AmdSevSnpSpecification;
const AnalysisAclRule = @import("analysis_acl_rule.zig").AnalysisAclRule;
const AnalysisComponent = @import("analysis_component.zig").AnalysisComponent;
const AnalysisLoadBalancerListener = @import("analysis_load_balancer_listener.zig").AnalysisLoadBalancerListener;
const AnalysisLoadBalancerTarget = @import("analysis_load_balancer_target.zig").AnalysisLoadBalancerTarget;
const AnalysisPacketHeader = @import("analysis_packet_header.zig").AnalysisPacketHeader;
const AnalysisRouteTableRoute = @import("analysis_route_table_route.zig").AnalysisRouteTableRoute;
const AnalysisSecurityGroupRule = @import("analysis_security_group_rule.zig").AnalysisSecurityGroupRule;
const AnalysisStatus = @import("analysis_status.zig").AnalysisStatus;
const ApplianceModeSupportValue = @import("appliance_mode_support_value.zig").ApplianceModeSupportValue;
const ArchitectureType = @import("architecture_type.zig").ArchitectureType;
const ArchitectureValues = @import("architecture_values.zig").ArchitectureValues;
const AsnAssociation = @import("asn_association.zig").AsnAssociation;
const AsnAssociationState = @import("asn_association_state.zig").AsnAssociationState;
const AsnAuthorizationContext = @import("asn_authorization_context.zig").AsnAuthorizationContext;
const AsnState = @import("asn_state.zig").AsnState;
const AssignedPrivateIpAddress = @import("assigned_private_ip_address.zig").AssignedPrivateIpAddress;
const AssociatedNetworkType = @import("associated_network_type.zig").AssociatedNetworkType;
const AssociatedRole = @import("associated_role.zig").AssociatedRole;
const AssociatedTargetNetwork = @import("associated_target_network.zig").AssociatedTargetNetwork;
const AssociationStatus = @import("association_status.zig").AssociationStatus;
const AssociationStatusCode = @import("association_status_code.zig").AssociationStatusCode;
const AthenaIntegration = @import("athena_integration.zig").AthenaIntegration;
const AttachmentEnaSrdSpecification = @import("attachment_ena_srd_specification.zig").AttachmentEnaSrdSpecification;
const AttachmentEnaSrdUdpSpecification = @import("attachment_ena_srd_udp_specification.zig").AttachmentEnaSrdUdpSpecification;
const AttachmentLimitType = @import("attachment_limit_type.zig").AttachmentLimitType;
const AttachmentStatus = @import("attachment_status.zig").AttachmentStatus;
const AttributeBooleanValue = @import("attribute_boolean_value.zig").AttributeBooleanValue;
const AttributeSummary = @import("attribute_summary.zig").AttributeSummary;
const AttributeValue = @import("attribute_value.zig").AttributeValue;
const AuthorizationRule = @import("authorization_rule.zig").AuthorizationRule;
const AutoAcceptSharedAssociationsValue = @import("auto_accept_shared_associations_value.zig").AutoAcceptSharedAssociationsValue;
const AutoAcceptSharedAttachmentsValue = @import("auto_accept_shared_attachments_value.zig").AutoAcceptSharedAttachmentsValue;
const AutoPlacement = @import("auto_placement.zig").AutoPlacement;
const AutoProvisionZonesState = @import("auto_provision_zones_state.zig").AutoProvisionZonesState;
const AutoScalingIpsState = @import("auto_scaling_ips_state.zig").AutoScalingIpsState;
const AvailabilityMode = @import("availability_mode.zig").AvailabilityMode;
const AvailabilityZone = @import("availability_zone.zig").AvailabilityZone;
const AvailabilityZoneAddress = @import("availability_zone_address.zig").AvailabilityZoneAddress;
const AvailabilityZoneMessage = @import("availability_zone_message.zig").AvailabilityZoneMessage;
const AvailabilityZoneOptInStatus = @import("availability_zone_opt_in_status.zig").AvailabilityZoneOptInStatus;
const AvailabilityZoneState = @import("availability_zone_state.zig").AvailabilityZoneState;
const AvailableCapacity = @import("available_capacity.zig").AvailableCapacity;
const BandwidthWeightingType = @import("bandwidth_weighting_type.zig").BandwidthWeightingType;
const BareMetal = @import("bare_metal.zig").BareMetal;
const BaselineEbsBandwidthMbps = @import("baseline_ebs_bandwidth_mbps.zig").BaselineEbsBandwidthMbps;
const BaselineEbsBandwidthMbpsRequest = @import("baseline_ebs_bandwidth_mbps_request.zig").BaselineEbsBandwidthMbpsRequest;
const BaselinePerformanceFactors = @import("baseline_performance_factors.zig").BaselinePerformanceFactors;
const BaselinePerformanceFactorsRequest = @import("baseline_performance_factors_request.zig").BaselinePerformanceFactorsRequest;
const BatchState = @import("batch_state.zig").BatchState;
const BgpStatus = @import("bgp_status.zig").BgpStatus;
const BlobAttributeValue = @import("blob_attribute_value.zig").BlobAttributeValue;
const BlockDeviceMapping = @import("block_device_mapping.zig").BlockDeviceMapping;
const BlockDeviceMappingResponse = @import("block_device_mapping_response.zig").BlockDeviceMappingResponse;
const BlockPublicAccessMode = @import("block_public_access_mode.zig").BlockPublicAccessMode;
const BlockPublicAccessStates = @import("block_public_access_states.zig").BlockPublicAccessStates;
const BootModeType = @import("boot_mode_type.zig").BootModeType;
const BootModeValues = @import("boot_mode_values.zig").BootModeValues;
const BundleTask = @import("bundle_task.zig").BundleTask;
const BundleTaskError = @import("bundle_task_error.zig").BundleTaskError;
const BundleTaskState = @import("bundle_task_state.zig").BundleTaskState;
const BurstablePerformance = @import("burstable_performance.zig").BurstablePerformance;
const Byoasn = @import("byoasn.zig").Byoasn;
const ByoipCidr = @import("byoip_cidr.zig").ByoipCidr;
const ByoipCidrState = @import("byoip_cidr_state.zig").ByoipCidrState;
const CancelBatchErrorCode = @import("cancel_batch_error_code.zig").CancelBatchErrorCode;
const CancelCapacityReservationFleetError = @import("cancel_capacity_reservation_fleet_error.zig").CancelCapacityReservationFleetError;
const CancelSpotFleetRequestsError = @import("cancel_spot_fleet_requests_error.zig").CancelSpotFleetRequestsError;
const CancelSpotFleetRequestsErrorItem = @import("cancel_spot_fleet_requests_error_item.zig").CancelSpotFleetRequestsErrorItem;
const CancelSpotFleetRequestsSuccessItem = @import("cancel_spot_fleet_requests_success_item.zig").CancelSpotFleetRequestsSuccessItem;
const CancelSpotInstanceRequestState = @import("cancel_spot_instance_request_state.zig").CancelSpotInstanceRequestState;
const CancelledSpotInstanceRequest = @import("cancelled_spot_instance_request.zig").CancelledSpotInstanceRequest;
const CapacityAllocation = @import("capacity_allocation.zig").CapacityAllocation;
const CapacityBlock = @import("capacity_block.zig").CapacityBlock;
const CapacityBlockExtension = @import("capacity_block_extension.zig").CapacityBlockExtension;
const CapacityBlockExtensionOffering = @import("capacity_block_extension_offering.zig").CapacityBlockExtensionOffering;
const CapacityBlockExtensionStatus = @import("capacity_block_extension_status.zig").CapacityBlockExtensionStatus;
const CapacityBlockInterconnectStatus = @import("capacity_block_interconnect_status.zig").CapacityBlockInterconnectStatus;
const CapacityBlockOffering = @import("capacity_block_offering.zig").CapacityBlockOffering;
const CapacityBlockResourceState = @import("capacity_block_resource_state.zig").CapacityBlockResourceState;
const CapacityBlockStatus = @import("capacity_block_status.zig").CapacityBlockStatus;
const CapacityManagerCondition = @import("capacity_manager_condition.zig").CapacityManagerCondition;
const CapacityManagerDataExportResponse = @import("capacity_manager_data_export_response.zig").CapacityManagerDataExportResponse;
const CapacityManagerDataExportStatus = @import("capacity_manager_data_export_status.zig").CapacityManagerDataExportStatus;
const CapacityManagerDimension = @import("capacity_manager_dimension.zig").CapacityManagerDimension;
const CapacityReservation = @import("capacity_reservation.zig").CapacityReservation;
const CapacityReservationBillingRequest = @import("capacity_reservation_billing_request.zig").CapacityReservationBillingRequest;
const CapacityReservationBillingRequestStatus = @import("capacity_reservation_billing_request_status.zig").CapacityReservationBillingRequestStatus;
const CapacityReservationCommitmentInfo = @import("capacity_reservation_commitment_info.zig").CapacityReservationCommitmentInfo;
const CapacityReservationDeliveryPreference = @import("capacity_reservation_delivery_preference.zig").CapacityReservationDeliveryPreference;
const CapacityReservationFleet = @import("capacity_reservation_fleet.zig").CapacityReservationFleet;
const CapacityReservationFleetCancellationState = @import("capacity_reservation_fleet_cancellation_state.zig").CapacityReservationFleetCancellationState;
const CapacityReservationFleetState = @import("capacity_reservation_fleet_state.zig").CapacityReservationFleetState;
const CapacityReservationGroup = @import("capacity_reservation_group.zig").CapacityReservationGroup;
const CapacityReservationInfo = @import("capacity_reservation_info.zig").CapacityReservationInfo;
const CapacityReservationInstancePlatform = @import("capacity_reservation_instance_platform.zig").CapacityReservationInstancePlatform;
const CapacityReservationOptions = @import("capacity_reservation_options.zig").CapacityReservationOptions;
const CapacityReservationOptionsRequest = @import("capacity_reservation_options_request.zig").CapacityReservationOptionsRequest;
const CapacityReservationPreference = @import("capacity_reservation_preference.zig").CapacityReservationPreference;
const CapacityReservationSpecification = @import("capacity_reservation_specification.zig").CapacityReservationSpecification;
const CapacityReservationSpecificationResponse = @import("capacity_reservation_specification_response.zig").CapacityReservationSpecificationResponse;
const CapacityReservationState = @import("capacity_reservation_state.zig").CapacityReservationState;
const CapacityReservationStatus = @import("capacity_reservation_status.zig").CapacityReservationStatus;
const CapacityReservationTarget = @import("capacity_reservation_target.zig").CapacityReservationTarget;
const CapacityReservationTargetResponse = @import("capacity_reservation_target_response.zig").CapacityReservationTargetResponse;
const CapacityReservationTenancy = @import("capacity_reservation_tenancy.zig").CapacityReservationTenancy;
const CapacityReservationTopology = @import("capacity_reservation_topology.zig").CapacityReservationTopology;
const CapacityReservationType = @import("capacity_reservation_type.zig").CapacityReservationType;
const CapacityTenancy = @import("capacity_tenancy.zig").CapacityTenancy;
const CarrierGateway = @import("carrier_gateway.zig").CarrierGateway;
const CarrierGatewayState = @import("carrier_gateway_state.zig").CarrierGatewayState;
const CertificateAuthentication = @import("certificate_authentication.zig").CertificateAuthentication;
const CertificateAuthenticationRequest = @import("certificate_authentication_request.zig").CertificateAuthenticationRequest;
const CidrAuthorizationContext = @import("cidr_authorization_context.zig").CidrAuthorizationContext;
const CidrBlock = @import("cidr_block.zig").CidrBlock;
const ClassicLinkDnsSupport = @import("classic_link_dns_support.zig").ClassicLinkDnsSupport;
const ClassicLinkInstance = @import("classic_link_instance.zig").ClassicLinkInstance;
const ClassicLoadBalancer = @import("classic_load_balancer.zig").ClassicLoadBalancer;
const ClassicLoadBalancersConfig = @import("classic_load_balancers_config.zig").ClassicLoadBalancersConfig;
const ClientCertificateRevocationListStatus = @import("client_certificate_revocation_list_status.zig").ClientCertificateRevocationListStatus;
const ClientCertificateRevocationListStatusCode = @import("client_certificate_revocation_list_status_code.zig").ClientCertificateRevocationListStatusCode;
const ClientConnectOptions = @import("client_connect_options.zig").ClientConnectOptions;
const ClientConnectResponseOptions = @import("client_connect_response_options.zig").ClientConnectResponseOptions;
const ClientData = @import("client_data.zig").ClientData;
const ClientLoginBannerOptions = @import("client_login_banner_options.zig").ClientLoginBannerOptions;
const ClientLoginBannerResponseOptions = @import("client_login_banner_response_options.zig").ClientLoginBannerResponseOptions;
const ClientRouteEnforcementOptions = @import("client_route_enforcement_options.zig").ClientRouteEnforcementOptions;
const ClientRouteEnforcementResponseOptions = @import("client_route_enforcement_response_options.zig").ClientRouteEnforcementResponseOptions;
const ClientVpnAuthentication = @import("client_vpn_authentication.zig").ClientVpnAuthentication;
const ClientVpnAuthenticationRequest = @import("client_vpn_authentication_request.zig").ClientVpnAuthenticationRequest;
const ClientVpnAuthenticationType = @import("client_vpn_authentication_type.zig").ClientVpnAuthenticationType;
const ClientVpnAuthorizationRuleStatus = @import("client_vpn_authorization_rule_status.zig").ClientVpnAuthorizationRuleStatus;
const ClientVpnAuthorizationRuleStatusCode = @import("client_vpn_authorization_rule_status_code.zig").ClientVpnAuthorizationRuleStatusCode;
const ClientVpnConnection = @import("client_vpn_connection.zig").ClientVpnConnection;
const ClientVpnConnectionStatus = @import("client_vpn_connection_status.zig").ClientVpnConnectionStatus;
const ClientVpnConnectionStatusCode = @import("client_vpn_connection_status_code.zig").ClientVpnConnectionStatusCode;
const ClientVpnEndpoint = @import("client_vpn_endpoint.zig").ClientVpnEndpoint;
const ClientVpnEndpointAttributeStatus = @import("client_vpn_endpoint_attribute_status.zig").ClientVpnEndpointAttributeStatus;
const ClientVpnEndpointAttributeStatusCode = @import("client_vpn_endpoint_attribute_status_code.zig").ClientVpnEndpointAttributeStatusCode;
const ClientVpnEndpointStatus = @import("client_vpn_endpoint_status.zig").ClientVpnEndpointStatus;
const ClientVpnEndpointStatusCode = @import("client_vpn_endpoint_status_code.zig").ClientVpnEndpointStatusCode;
const ClientVpnRoute = @import("client_vpn_route.zig").ClientVpnRoute;
const ClientVpnRouteStatus = @import("client_vpn_route_status.zig").ClientVpnRouteStatus;
const ClientVpnRouteStatusCode = @import("client_vpn_route_status_code.zig").ClientVpnRouteStatusCode;
const CloudWatchLogOptions = @import("cloud_watch_log_options.zig").CloudWatchLogOptions;
const CloudWatchLogOptionsSpecification = @import("cloud_watch_log_options_specification.zig").CloudWatchLogOptionsSpecification;
const CoipAddressUsage = @import("coip_address_usage.zig").CoipAddressUsage;
const CoipCidr = @import("coip_cidr.zig").CoipCidr;
const CoipPool = @import("coip_pool.zig").CoipPool;
const Comparison = @import("comparison.zig").Comparison;
const ConnectionLogOptions = @import("connection_log_options.zig").ConnectionLogOptions;
const ConnectionLogResponseOptions = @import("connection_log_response_options.zig").ConnectionLogResponseOptions;
const ConnectionNotification = @import("connection_notification.zig").ConnectionNotification;
const ConnectionNotificationState = @import("connection_notification_state.zig").ConnectionNotificationState;
const ConnectionNotificationType = @import("connection_notification_type.zig").ConnectionNotificationType;
const ConnectionTrackingConfiguration = @import("connection_tracking_configuration.zig").ConnectionTrackingConfiguration;
const ConnectionTrackingSpecification = @import("connection_tracking_specification.zig").ConnectionTrackingSpecification;
const ConnectionTrackingSpecificationRequest = @import("connection_tracking_specification_request.zig").ConnectionTrackingSpecificationRequest;
const ConnectionTrackingSpecificationResponse = @import("connection_tracking_specification_response.zig").ConnectionTrackingSpecificationResponse;
const ConnectivityType = @import("connectivity_type.zig").ConnectivityType;
const ContainerFormat = @import("container_format.zig").ContainerFormat;
const ConversionTask = @import("conversion_task.zig").ConversionTask;
const ConversionTaskState = @import("conversion_task_state.zig").ConversionTaskState;
const CpuManufacturer = @import("cpu_manufacturer.zig").CpuManufacturer;
const CpuOptions = @import("cpu_options.zig").CpuOptions;
const CpuOptionsRequest = @import("cpu_options_request.zig").CpuOptionsRequest;
const CpuPerformanceFactor = @import("cpu_performance_factor.zig").CpuPerformanceFactor;
const CpuPerformanceFactorRequest = @import("cpu_performance_factor_request.zig").CpuPerformanceFactorRequest;
const CreateFleetError = @import("create_fleet_error.zig").CreateFleetError;
const CreateFleetInstance = @import("create_fleet_instance.zig").CreateFleetInstance;
const CreateTransitGatewayConnectRequestOptions = @import("create_transit_gateway_connect_request_options.zig").CreateTransitGatewayConnectRequestOptions;
const CreateTransitGatewayMulticastDomainRequestOptions = @import("create_transit_gateway_multicast_domain_request_options.zig").CreateTransitGatewayMulticastDomainRequestOptions;
const CreateTransitGatewayPeeringAttachmentRequestOptions = @import("create_transit_gateway_peering_attachment_request_options.zig").CreateTransitGatewayPeeringAttachmentRequestOptions;
const CreateTransitGatewayVpcAttachmentRequestOptions = @import("create_transit_gateway_vpc_attachment_request_options.zig").CreateTransitGatewayVpcAttachmentRequestOptions;
const CreateVerifiedAccessEndpointCidrOptions = @import("create_verified_access_endpoint_cidr_options.zig").CreateVerifiedAccessEndpointCidrOptions;
const CreateVerifiedAccessEndpointEniOptions = @import("create_verified_access_endpoint_eni_options.zig").CreateVerifiedAccessEndpointEniOptions;
const CreateVerifiedAccessEndpointLoadBalancerOptions = @import("create_verified_access_endpoint_load_balancer_options.zig").CreateVerifiedAccessEndpointLoadBalancerOptions;
const CreateVerifiedAccessEndpointPortRange = @import("create_verified_access_endpoint_port_range.zig").CreateVerifiedAccessEndpointPortRange;
const CreateVerifiedAccessEndpointRdsOptions = @import("create_verified_access_endpoint_rds_options.zig").CreateVerifiedAccessEndpointRdsOptions;
const CreateVerifiedAccessNativeApplicationOidcOptions = @import("create_verified_access_native_application_oidc_options.zig").CreateVerifiedAccessNativeApplicationOidcOptions;
const CreateVerifiedAccessTrustProviderDeviceOptions = @import("create_verified_access_trust_provider_device_options.zig").CreateVerifiedAccessTrustProviderDeviceOptions;
const CreateVerifiedAccessTrustProviderOidcOptions = @import("create_verified_access_trust_provider_oidc_options.zig").CreateVerifiedAccessTrustProviderOidcOptions;
const CreateVolumePermission = @import("create_volume_permission.zig").CreateVolumePermission;
const CreateVolumePermissionModifications = @import("create_volume_permission_modifications.zig").CreateVolumePermissionModifications;
const CreationDateCondition = @import("creation_date_condition.zig").CreationDateCondition;
const CreationDateConditionRequest = @import("creation_date_condition_request.zig").CreationDateConditionRequest;
const CreditSpecification = @import("credit_specification.zig").CreditSpecification;
const CreditSpecificationRequest = @import("credit_specification_request.zig").CreditSpecificationRequest;
const CurrencyCodeValues = @import("currency_code_values.zig").CurrencyCodeValues;
const CustomerGateway = @import("customer_gateway.zig").CustomerGateway;
const DataQuery = @import("data_query.zig").DataQuery;
const DataResponse = @import("data_response.zig").DataResponse;
const DatafeedSubscriptionState = @import("datafeed_subscription_state.zig").DatafeedSubscriptionState;
const DeclarativePoliciesReport = @import("declarative_policies_report.zig").DeclarativePoliciesReport;
const DefaultRouteTableAssociationValue = @import("default_route_table_association_value.zig").DefaultRouteTableAssociationValue;
const DefaultRouteTablePropagationValue = @import("default_route_table_propagation_value.zig").DefaultRouteTablePropagationValue;
const DefaultTargetCapacityType = @import("default_target_capacity_type.zig").DefaultTargetCapacityType;
const DeleteFleetError = @import("delete_fleet_error.zig").DeleteFleetError;
const DeleteFleetErrorCode = @import("delete_fleet_error_code.zig").DeleteFleetErrorCode;
const DeleteFleetErrorItem = @import("delete_fleet_error_item.zig").DeleteFleetErrorItem;
const DeleteFleetSuccessItem = @import("delete_fleet_success_item.zig").DeleteFleetSuccessItem;
const DeleteLaunchTemplateVersionsResponseErrorItem = @import("delete_launch_template_versions_response_error_item.zig").DeleteLaunchTemplateVersionsResponseErrorItem;
const DeleteLaunchTemplateVersionsResponseSuccessItem = @import("delete_launch_template_versions_response_success_item.zig").DeleteLaunchTemplateVersionsResponseSuccessItem;
const DeleteQueuedReservedInstancesError = @import("delete_queued_reserved_instances_error.zig").DeleteQueuedReservedInstancesError;
const DeleteQueuedReservedInstancesErrorCode = @import("delete_queued_reserved_instances_error_code.zig").DeleteQueuedReservedInstancesErrorCode;
const DeleteSnapshotReturnCode = @import("delete_snapshot_return_code.zig").DeleteSnapshotReturnCode;
const DeprecationTimeCondition = @import("deprecation_time_condition.zig").DeprecationTimeCondition;
const DeprecationTimeConditionRequest = @import("deprecation_time_condition_request.zig").DeprecationTimeConditionRequest;
const DeregisterInstanceTagAttributeRequest = @import("deregister_instance_tag_attribute_request.zig").DeregisterInstanceTagAttributeRequest;
const DescribeFastLaunchImagesSuccessItem = @import("describe_fast_launch_images_success_item.zig").DescribeFastLaunchImagesSuccessItem;
const DescribeFastSnapshotRestoreSuccessItem = @import("describe_fast_snapshot_restore_success_item.zig").DescribeFastSnapshotRestoreSuccessItem;
const DescribeFleetError = @import("describe_fleet_error.zig").DescribeFleetError;
const DescribeFleetsInstances = @import("describe_fleets_instances.zig").DescribeFleetsInstances;
const DestinationFileFormat = @import("destination_file_format.zig").DestinationFileFormat;
const DestinationOptionsRequest = @import("destination_options_request.zig").DestinationOptionsRequest;
const DestinationOptionsResponse = @import("destination_options_response.zig").DestinationOptionsResponse;
const DeviceOptions = @import("device_options.zig").DeviceOptions;
const DeviceTrustProviderType = @import("device_trust_provider_type.zig").DeviceTrustProviderType;
const DeviceType = @import("device_type.zig").DeviceType;
const DhcpConfiguration = @import("dhcp_configuration.zig").DhcpConfiguration;
const DhcpOptions = @import("dhcp_options.zig").DhcpOptions;
const DimensionCondition = @import("dimension_condition.zig").DimensionCondition;
const DirectoryServiceAuthentication = @import("directory_service_authentication.zig").DirectoryServiceAuthentication;
const DirectoryServiceAuthenticationRequest = @import("directory_service_authentication_request.zig").DirectoryServiceAuthenticationRequest;
const DisableFastSnapshotRestoreErrorItem = @import("disable_fast_snapshot_restore_error_item.zig").DisableFastSnapshotRestoreErrorItem;
const DisableFastSnapshotRestoreStateError = @import("disable_fast_snapshot_restore_state_error.zig").DisableFastSnapshotRestoreStateError;
const DisableFastSnapshotRestoreStateErrorItem = @import("disable_fast_snapshot_restore_state_error_item.zig").DisableFastSnapshotRestoreStateErrorItem;
const DisableFastSnapshotRestoreSuccessItem = @import("disable_fast_snapshot_restore_success_item.zig").DisableFastSnapshotRestoreSuccessItem;
const DiskImage = @import("disk_image.zig").DiskImage;
const DiskImageDescription = @import("disk_image_description.zig").DiskImageDescription;
const DiskImageDetail = @import("disk_image_detail.zig").DiskImageDetail;
const DiskImageFormat = @import("disk_image_format.zig").DiskImageFormat;
const DiskImageVolumeDescription = @import("disk_image_volume_description.zig").DiskImageVolumeDescription;
const DiskInfo = @import("disk_info.zig").DiskInfo;
const DiskType = @import("disk_type.zig").DiskType;
const DnsEntry = @import("dns_entry.zig").DnsEntry;
const DnsNameState = @import("dns_name_state.zig").DnsNameState;
const DnsOptions = @import("dns_options.zig").DnsOptions;
const DnsOptionsSpecification = @import("dns_options_specification.zig").DnsOptionsSpecification;
const DnsRecordIpType = @import("dns_record_ip_type.zig").DnsRecordIpType;
const DnsServersOptionsModifyStructure = @import("dns_servers_options_modify_structure.zig").DnsServersOptionsModifyStructure;
const DnsSupportValue = @import("dns_support_value.zig").DnsSupportValue;
const DomainType = @import("domain_type.zig").DomainType;
const DynamicRoutingValue = @import("dynamic_routing_value.zig").DynamicRoutingValue;
const EbsBlockDevice = @import("ebs_block_device.zig").EbsBlockDevice;
const EbsBlockDeviceResponse = @import("ebs_block_device_response.zig").EbsBlockDeviceResponse;
const EbsCardInfo = @import("ebs_card_info.zig").EbsCardInfo;
const EbsEncryptionSupport = @import("ebs_encryption_support.zig").EbsEncryptionSupport;
const EbsInfo = @import("ebs_info.zig").EbsInfo;
const EbsInstanceBlockDevice = @import("ebs_instance_block_device.zig").EbsInstanceBlockDevice;
const EbsInstanceBlockDeviceSpecification = @import("ebs_instance_block_device_specification.zig").EbsInstanceBlockDeviceSpecification;
const EbsNvmeSupport = @import("ebs_nvme_support.zig").EbsNvmeSupport;
const EbsOptimizedInfo = @import("ebs_optimized_info.zig").EbsOptimizedInfo;
const EbsOptimizedSupport = @import("ebs_optimized_support.zig").EbsOptimizedSupport;
const EbsStatusDetails = @import("ebs_status_details.zig").EbsStatusDetails;
const EbsStatusSummary = @import("ebs_status_summary.zig").EbsStatusSummary;
const Ec2InstanceConnectEndpoint = @import("ec_2_instance_connect_endpoint.zig").Ec2InstanceConnectEndpoint;
const Ec2InstanceConnectEndpointState = @import("ec_2_instance_connect_endpoint_state.zig").Ec2InstanceConnectEndpointState;
const EfaInfo = @import("efa_info.zig").EfaInfo;
const EgressOnlyInternetGateway = @import("egress_only_internet_gateway.zig").EgressOnlyInternetGateway;
const ElasticGpuAssociation = @import("elastic_gpu_association.zig").ElasticGpuAssociation;
const ElasticGpuHealth = @import("elastic_gpu_health.zig").ElasticGpuHealth;
const ElasticGpuSpecification = @import("elastic_gpu_specification.zig").ElasticGpuSpecification;
const ElasticGpuSpecificationResponse = @import("elastic_gpu_specification_response.zig").ElasticGpuSpecificationResponse;
const ElasticGpuState = @import("elastic_gpu_state.zig").ElasticGpuState;
const ElasticGpuStatus = @import("elastic_gpu_status.zig").ElasticGpuStatus;
const ElasticGpus = @import("elastic_gpus.zig").ElasticGpus;
const ElasticInferenceAccelerator = @import("elastic_inference_accelerator.zig").ElasticInferenceAccelerator;
const ElasticInferenceAcceleratorAssociation = @import("elastic_inference_accelerator_association.zig").ElasticInferenceAcceleratorAssociation;
const EnaSrdSpecification = @import("ena_srd_specification.zig").EnaSrdSpecification;
const EnaSrdSpecificationRequest = @import("ena_srd_specification_request.zig").EnaSrdSpecificationRequest;
const EnaSrdUdpSpecification = @import("ena_srd_udp_specification.zig").EnaSrdUdpSpecification;
const EnaSrdUdpSpecificationRequest = @import("ena_srd_udp_specification_request.zig").EnaSrdUdpSpecificationRequest;
const EnaSupport = @import("ena_support.zig").EnaSupport;
const EnableFastSnapshotRestoreErrorItem = @import("enable_fast_snapshot_restore_error_item.zig").EnableFastSnapshotRestoreErrorItem;
const EnableFastSnapshotRestoreStateError = @import("enable_fast_snapshot_restore_state_error.zig").EnableFastSnapshotRestoreStateError;
const EnableFastSnapshotRestoreStateErrorItem = @import("enable_fast_snapshot_restore_state_error_item.zig").EnableFastSnapshotRestoreStateErrorItem;
const EnableFastSnapshotRestoreSuccessItem = @import("enable_fast_snapshot_restore_success_item.zig").EnableFastSnapshotRestoreSuccessItem;
const EnclaveOptions = @import("enclave_options.zig").EnclaveOptions;
const EnclaveOptionsRequest = @import("enclave_options_request.zig").EnclaveOptionsRequest;
const EncryptionStateValue = @import("encryption_state_value.zig").EncryptionStateValue;
const EncryptionSupport = @import("encryption_support.zig").EncryptionSupport;
const EncryptionSupportOptionValue = @import("encryption_support_option_value.zig").EncryptionSupportOptionValue;
const EndDateType = @import("end_date_type.zig").EndDateType;
const EndpointIpAddressType = @import("endpoint_ip_address_type.zig").EndpointIpAddressType;
const EphemeralNvmeSupport = @import("ephemeral_nvme_support.zig").EphemeralNvmeSupport;
const EventCode = @import("event_code.zig").EventCode;
const EventInformation = @import("event_information.zig").EventInformation;
const EventType = @import("event_type.zig").EventType;
const ExcessCapacityTerminationPolicy = @import("excess_capacity_termination_policy.zig").ExcessCapacityTerminationPolicy;
const Explanation = @import("explanation.zig").Explanation;
const ExportEnvironment = @import("export_environment.zig").ExportEnvironment;
const ExportImageTask = @import("export_image_task.zig").ExportImageTask;
const ExportTask = @import("export_task.zig").ExportTask;
const ExportTaskS3Location = @import("export_task_s3_location.zig").ExportTaskS3Location;
const ExportTaskS3LocationRequest = @import("export_task_s3_location_request.zig").ExportTaskS3LocationRequest;
const ExportTaskState = @import("export_task_state.zig").ExportTaskState;
const ExportToS3Task = @import("export_to_s3_task.zig").ExportToS3Task;
const ExportToS3TaskSpecification = @import("export_to_s3_task_specification.zig").ExportToS3TaskSpecification;
const ExternalAuthorityConfiguration = @import("external_authority_configuration.zig").ExternalAuthorityConfiguration;
const FailedCapacityReservationFleetCancellationResult = @import("failed_capacity_reservation_fleet_cancellation_result.zig").FailedCapacityReservationFleetCancellationResult;
const FailedQueuedPurchaseDeletion = @import("failed_queued_purchase_deletion.zig").FailedQueuedPurchaseDeletion;
const FastLaunchLaunchTemplateSpecificationRequest = @import("fast_launch_launch_template_specification_request.zig").FastLaunchLaunchTemplateSpecificationRequest;
const FastLaunchLaunchTemplateSpecificationResponse = @import("fast_launch_launch_template_specification_response.zig").FastLaunchLaunchTemplateSpecificationResponse;
const FastLaunchResourceType = @import("fast_launch_resource_type.zig").FastLaunchResourceType;
const FastLaunchSnapshotConfigurationRequest = @import("fast_launch_snapshot_configuration_request.zig").FastLaunchSnapshotConfigurationRequest;
const FastLaunchSnapshotConfigurationResponse = @import("fast_launch_snapshot_configuration_response.zig").FastLaunchSnapshotConfigurationResponse;
const FastLaunchStateCode = @import("fast_launch_state_code.zig").FastLaunchStateCode;
const FastSnapshotRestoreStateCode = @import("fast_snapshot_restore_state_code.zig").FastSnapshotRestoreStateCode;
const FederatedAuthentication = @import("federated_authentication.zig").FederatedAuthentication;
const FederatedAuthenticationRequest = @import("federated_authentication_request.zig").FederatedAuthenticationRequest;
const Filter = @import("filter.zig").Filter;
const FilterByDimension = @import("filter_by_dimension.zig").FilterByDimension;
const FilterPortRange = @import("filter_port_range.zig").FilterPortRange;
const FindingsFound = @import("findings_found.zig").FindingsFound;
const FirewallStatefulRule = @import("firewall_stateful_rule.zig").FirewallStatefulRule;
const FirewallStatelessRule = @import("firewall_stateless_rule.zig").FirewallStatelessRule;
const FleetActivityStatus = @import("fleet_activity_status.zig").FleetActivityStatus;
const FleetBlockDeviceMappingRequest = @import("fleet_block_device_mapping_request.zig").FleetBlockDeviceMappingRequest;
const FleetCapacityReservation = @import("fleet_capacity_reservation.zig").FleetCapacityReservation;
const FleetCapacityReservationTenancy = @import("fleet_capacity_reservation_tenancy.zig").FleetCapacityReservationTenancy;
const FleetCapacityReservationUsageStrategy = @import("fleet_capacity_reservation_usage_strategy.zig").FleetCapacityReservationUsageStrategy;
const FleetData = @import("fleet_data.zig").FleetData;
const FleetEbsBlockDeviceRequest = @import("fleet_ebs_block_device_request.zig").FleetEbsBlockDeviceRequest;
const FleetEventType = @import("fleet_event_type.zig").FleetEventType;
const FleetExcessCapacityTerminationPolicy = @import("fleet_excess_capacity_termination_policy.zig").FleetExcessCapacityTerminationPolicy;
const FleetInstanceMatchCriteria = @import("fleet_instance_match_criteria.zig").FleetInstanceMatchCriteria;
const FleetLaunchTemplateConfig = @import("fleet_launch_template_config.zig").FleetLaunchTemplateConfig;
const FleetLaunchTemplateConfigRequest = @import("fleet_launch_template_config_request.zig").FleetLaunchTemplateConfigRequest;
const FleetLaunchTemplateOverrides = @import("fleet_launch_template_overrides.zig").FleetLaunchTemplateOverrides;
const FleetLaunchTemplateOverridesRequest = @import("fleet_launch_template_overrides_request.zig").FleetLaunchTemplateOverridesRequest;
const FleetLaunchTemplateSpecification = @import("fleet_launch_template_specification.zig").FleetLaunchTemplateSpecification;
const FleetLaunchTemplateSpecificationRequest = @import("fleet_launch_template_specification_request.zig").FleetLaunchTemplateSpecificationRequest;
const FleetOnDemandAllocationStrategy = @import("fleet_on_demand_allocation_strategy.zig").FleetOnDemandAllocationStrategy;
const FleetReplacementStrategy = @import("fleet_replacement_strategy.zig").FleetReplacementStrategy;
const FleetSpotCapacityRebalance = @import("fleet_spot_capacity_rebalance.zig").FleetSpotCapacityRebalance;
const FleetSpotCapacityRebalanceRequest = @import("fleet_spot_capacity_rebalance_request.zig").FleetSpotCapacityRebalanceRequest;
const FleetSpotMaintenanceStrategies = @import("fleet_spot_maintenance_strategies.zig").FleetSpotMaintenanceStrategies;
const FleetSpotMaintenanceStrategiesRequest = @import("fleet_spot_maintenance_strategies_request.zig").FleetSpotMaintenanceStrategiesRequest;
const FleetStateCode = @import("fleet_state_code.zig").FleetStateCode;
const FleetType = @import("fleet_type.zig").FleetType;
const FlexibleEnaQueuesSupport = @import("flexible_ena_queues_support.zig").FlexibleEnaQueuesSupport;
const FlowLog = @import("flow_log.zig").FlowLog;
const FpgaDeviceInfo = @import("fpga_device_info.zig").FpgaDeviceInfo;
const FpgaDeviceMemoryInfo = @import("fpga_device_memory_info.zig").FpgaDeviceMemoryInfo;
const FpgaImage = @import("fpga_image.zig").FpgaImage;
const FpgaImageAttribute = @import("fpga_image_attribute.zig").FpgaImageAttribute;
const FpgaImageState = @import("fpga_image_state.zig").FpgaImageState;
const FpgaImageStateCode = @import("fpga_image_state_code.zig").FpgaImageStateCode;
const FpgaInfo = @import("fpga_info.zig").FpgaInfo;
const GatewayAssociationState = @import("gateway_association_state.zig").GatewayAssociationState;
const GatewayType = @import("gateway_type.zig").GatewayType;
const GpuDeviceInfo = @import("gpu_device_info.zig").GpuDeviceInfo;
const GpuDeviceMemoryInfo = @import("gpu_device_memory_info.zig").GpuDeviceMemoryInfo;
const GpuInfo = @import("gpu_info.zig").GpuInfo;
const GroupBy = @import("group_by.zig").GroupBy;
const GroupIdentifier = @import("group_identifier.zig").GroupIdentifier;
const HaStatus = @import("ha_status.zig").HaStatus;
const HibernationOptions = @import("hibernation_options.zig").HibernationOptions;
const HibernationOptionsRequest = @import("hibernation_options_request.zig").HibernationOptionsRequest;
const HistoryRecord = @import("history_record.zig").HistoryRecord;
const HistoryRecordEntry = @import("history_record_entry.zig").HistoryRecordEntry;
const Host = @import("host.zig").Host;
const HostInstance = @import("host_instance.zig").HostInstance;
const HostMaintenance = @import("host_maintenance.zig").HostMaintenance;
const HostOffering = @import("host_offering.zig").HostOffering;
const HostProperties = @import("host_properties.zig").HostProperties;
const HostRecovery = @import("host_recovery.zig").HostRecovery;
const HostReservation = @import("host_reservation.zig").HostReservation;
const HostnameType = @import("hostname_type.zig").HostnameType;
const HttpTokensState = @import("http_tokens_state.zig").HttpTokensState;
const HypervisorType = @import("hypervisor_type.zig").HypervisorType;
const IKEVersionsListValue = @import("ike_versions_list_value.zig").IKEVersionsListValue;
const IKEVersionsRequestListValue = @import("ike_versions_request_list_value.zig").IKEVersionsRequestListValue;
const IamInstanceProfile = @import("iam_instance_profile.zig").IamInstanceProfile;
const IamInstanceProfileAssociation = @import("iam_instance_profile_association.zig").IamInstanceProfileAssociation;
const IamInstanceProfileAssociationState = @import("iam_instance_profile_association_state.zig").IamInstanceProfileAssociationState;
const IamInstanceProfileSpecification = @import("iam_instance_profile_specification.zig").IamInstanceProfileSpecification;
const IcmpTypeCode = @import("icmp_type_code.zig").IcmpTypeCode;
const IdFormat = @import("id_format.zig").IdFormat;
const Igmpv2SupportValue = @import("igmpv_2_support_value.zig").Igmpv2SupportValue;
const Image = @import("image.zig").Image;
const ImageAncestryEntry = @import("image_ancestry_entry.zig").ImageAncestryEntry;
const ImageCriterion = @import("image_criterion.zig").ImageCriterion;
const ImageCriterionRequest = @import("image_criterion_request.zig").ImageCriterionRequest;
const ImageDiskContainer = @import("image_disk_container.zig").ImageDiskContainer;
const ImageMetadata = @import("image_metadata.zig").ImageMetadata;
const ImageRecycleBinInfo = @import("image_recycle_bin_info.zig").ImageRecycleBinInfo;
const ImageReference = @import("image_reference.zig").ImageReference;
const ImageReferenceOptionName = @import("image_reference_option_name.zig").ImageReferenceOptionName;
const ImageReferenceResourceType = @import("image_reference_resource_type.zig").ImageReferenceResourceType;
const ImageState = @import("image_state.zig").ImageState;
const ImageTypeValues = @import("image_type_values.zig").ImageTypeValues;
const ImageUsageReport = @import("image_usage_report.zig").ImageUsageReport;
const ImageUsageReportEntry = @import("image_usage_report_entry.zig").ImageUsageReportEntry;
const ImageUsageResourceType = @import("image_usage_resource_type.zig").ImageUsageResourceType;
const ImageUsageResourceTypeOption = @import("image_usage_resource_type_option.zig").ImageUsageResourceTypeOption;
const ImageUsageResourceTypeOptionRequest = @import("image_usage_resource_type_option_request.zig").ImageUsageResourceTypeOptionRequest;
const ImageUsageResourceTypeRequest = @import("image_usage_resource_type_request.zig").ImageUsageResourceTypeRequest;
const ImdsSupportValues = @import("imds_support_values.zig").ImdsSupportValues;
const ImportImageLicenseConfigurationRequest = @import("import_image_license_configuration_request.zig").ImportImageLicenseConfigurationRequest;
const ImportImageLicenseConfigurationResponse = @import("import_image_license_configuration_response.zig").ImportImageLicenseConfigurationResponse;
const ImportImageTask = @import("import_image_task.zig").ImportImageTask;
const ImportInstanceLaunchSpecification = @import("import_instance_launch_specification.zig").ImportInstanceLaunchSpecification;
const ImportInstanceTaskDetails = @import("import_instance_task_details.zig").ImportInstanceTaskDetails;
const ImportInstanceVolumeDetailItem = @import("import_instance_volume_detail_item.zig").ImportInstanceVolumeDetailItem;
const ImportSnapshotTask = @import("import_snapshot_task.zig").ImportSnapshotTask;
const ImportVolumeTaskDetails = @import("import_volume_task_details.zig").ImportVolumeTaskDetails;
const InferenceAcceleratorInfo = @import("inference_accelerator_info.zig").InferenceAcceleratorInfo;
const InferenceDeviceInfo = @import("inference_device_info.zig").InferenceDeviceInfo;
const InferenceDeviceMemoryInfo = @import("inference_device_memory_info.zig").InferenceDeviceMemoryInfo;
const InitializationStatusDetails = @import("initialization_status_details.zig").InitializationStatusDetails;
const InitializationType = @import("initialization_type.zig").InitializationType;
const Instance = @import("instance.zig").Instance;
const InstanceAttachmentEnaSrdSpecification = @import("instance_attachment_ena_srd_specification.zig").InstanceAttachmentEnaSrdSpecification;
const InstanceAttachmentEnaSrdUdpSpecification = @import("instance_attachment_ena_srd_udp_specification.zig").InstanceAttachmentEnaSrdUdpSpecification;
const InstanceAutoRecoveryState = @import("instance_auto_recovery_state.zig").InstanceAutoRecoveryState;
const InstanceBandwidthWeighting = @import("instance_bandwidth_weighting.zig").InstanceBandwidthWeighting;
const InstanceBlockDeviceMapping = @import("instance_block_device_mapping.zig").InstanceBlockDeviceMapping;
const InstanceBlockDeviceMappingSpecification = @import("instance_block_device_mapping_specification.zig").InstanceBlockDeviceMappingSpecification;
const InstanceBootModeValues = @import("instance_boot_mode_values.zig").InstanceBootModeValues;
const InstanceCapacity = @import("instance_capacity.zig").InstanceCapacity;
const InstanceConnectEndpointDnsNames = @import("instance_connect_endpoint_dns_names.zig").InstanceConnectEndpointDnsNames;
const InstanceConnectEndpointPublicDnsNames = @import("instance_connect_endpoint_public_dns_names.zig").InstanceConnectEndpointPublicDnsNames;
const InstanceCount = @import("instance_count.zig").InstanceCount;
const InstanceCreditSpecification = @import("instance_credit_specification.zig").InstanceCreditSpecification;
const InstanceCreditSpecificationRequest = @import("instance_credit_specification_request.zig").InstanceCreditSpecificationRequest;
const InstanceEventWindow = @import("instance_event_window.zig").InstanceEventWindow;
const InstanceEventWindowAssociationRequest = @import("instance_event_window_association_request.zig").InstanceEventWindowAssociationRequest;
const InstanceEventWindowAssociationTarget = @import("instance_event_window_association_target.zig").InstanceEventWindowAssociationTarget;
const InstanceEventWindowDisassociationRequest = @import("instance_event_window_disassociation_request.zig").InstanceEventWindowDisassociationRequest;
const InstanceEventWindowState = @import("instance_event_window_state.zig").InstanceEventWindowState;
const InstanceEventWindowStateChange = @import("instance_event_window_state_change.zig").InstanceEventWindowStateChange;
const InstanceEventWindowTimeRange = @import("instance_event_window_time_range.zig").InstanceEventWindowTimeRange;
const InstanceEventWindowTimeRangeRequest = @import("instance_event_window_time_range_request.zig").InstanceEventWindowTimeRangeRequest;
const InstanceExportDetails = @import("instance_export_details.zig").InstanceExportDetails;
const InstanceFamilyCreditSpecification = @import("instance_family_credit_specification.zig").InstanceFamilyCreditSpecification;
const InstanceGeneration = @import("instance_generation.zig").InstanceGeneration;
const InstanceHealthStatus = @import("instance_health_status.zig").InstanceHealthStatus;
const InstanceImageMetadata = @import("instance_image_metadata.zig").InstanceImageMetadata;
const InstanceInterruptionBehavior = @import("instance_interruption_behavior.zig").InstanceInterruptionBehavior;
const InstanceIpv4Prefix = @import("instance_ipv_4_prefix.zig").InstanceIpv4Prefix;
const InstanceIpv6Address = @import("instance_ipv_6_address.zig").InstanceIpv6Address;
const InstanceIpv6AddressRequest = @import("instance_ipv_6_address_request.zig").InstanceIpv6AddressRequest;
const InstanceIpv6Prefix = @import("instance_ipv_6_prefix.zig").InstanceIpv6Prefix;
const InstanceLifecycle = @import("instance_lifecycle.zig").InstanceLifecycle;
const InstanceLifecycleType = @import("instance_lifecycle_type.zig").InstanceLifecycleType;
const InstanceMaintenanceOptions = @import("instance_maintenance_options.zig").InstanceMaintenanceOptions;
const InstanceMaintenanceOptionsRequest = @import("instance_maintenance_options_request.zig").InstanceMaintenanceOptionsRequest;
const InstanceMarketOptionsRequest = @import("instance_market_options_request.zig").InstanceMarketOptionsRequest;
const InstanceMatchCriteria = @import("instance_match_criteria.zig").InstanceMatchCriteria;
const InstanceMetadataDefaultsResponse = @import("instance_metadata_defaults_response.zig").InstanceMetadataDefaultsResponse;
const InstanceMetadataEndpointState = @import("instance_metadata_endpoint_state.zig").InstanceMetadataEndpointState;
const InstanceMetadataOptionsRequest = @import("instance_metadata_options_request.zig").InstanceMetadataOptionsRequest;
const InstanceMetadataOptionsResponse = @import("instance_metadata_options_response.zig").InstanceMetadataOptionsResponse;
const InstanceMetadataOptionsState = @import("instance_metadata_options_state.zig").InstanceMetadataOptionsState;
const InstanceMetadataProtocolState = @import("instance_metadata_protocol_state.zig").InstanceMetadataProtocolState;
const InstanceMetadataTagsState = @import("instance_metadata_tags_state.zig").InstanceMetadataTagsState;
const InstanceMonitoring = @import("instance_monitoring.zig").InstanceMonitoring;
const InstanceNetworkInterface = @import("instance_network_interface.zig").InstanceNetworkInterface;
const InstanceNetworkInterfaceAssociation = @import("instance_network_interface_association.zig").InstanceNetworkInterfaceAssociation;
const InstanceNetworkInterfaceAttachment = @import("instance_network_interface_attachment.zig").InstanceNetworkInterfaceAttachment;
const InstanceNetworkInterfaceSpecification = @import("instance_network_interface_specification.zig").InstanceNetworkInterfaceSpecification;
const InstanceNetworkPerformanceOptions = @import("instance_network_performance_options.zig").InstanceNetworkPerformanceOptions;
const InstanceNetworkPerformanceOptionsRequest = @import("instance_network_performance_options_request.zig").InstanceNetworkPerformanceOptionsRequest;
const InstancePrivateIpAddress = @import("instance_private_ip_address.zig").InstancePrivateIpAddress;
const InstanceRebootMigrationState = @import("instance_reboot_migration_state.zig").InstanceRebootMigrationState;
const InstanceRequirements = @import("instance_requirements.zig").InstanceRequirements;
const InstanceRequirementsRequest = @import("instance_requirements_request.zig").InstanceRequirementsRequest;
const InstanceRequirementsWithMetadataRequest = @import("instance_requirements_with_metadata_request.zig").InstanceRequirementsWithMetadataRequest;
const InstanceSpecification = @import("instance_specification.zig").InstanceSpecification;
const InstanceState = @import("instance_state.zig").InstanceState;
const InstanceStateChange = @import("instance_state_change.zig").InstanceStateChange;
const InstanceStateName = @import("instance_state_name.zig").InstanceStateName;
const InstanceStatus = @import("instance_status.zig").InstanceStatus;
const InstanceStatusDetails = @import("instance_status_details.zig").InstanceStatusDetails;
const InstanceStatusEvent = @import("instance_status_event.zig").InstanceStatusEvent;
const InstanceStatusSummary = @import("instance_status_summary.zig").InstanceStatusSummary;
const InstanceStorageEncryptionSupport = @import("instance_storage_encryption_support.zig").InstanceStorageEncryptionSupport;
const InstanceStorageInfo = @import("instance_storage_info.zig").InstanceStorageInfo;
const InstanceTagNotificationAttribute = @import("instance_tag_notification_attribute.zig").InstanceTagNotificationAttribute;
const InstanceTopology = @import("instance_topology.zig").InstanceTopology;
const InstanceType = @import("instance_type.zig").InstanceType;
const InstanceTypeHypervisor = @import("instance_type_hypervisor.zig").InstanceTypeHypervisor;
const InstanceTypeInfo = @import("instance_type_info.zig").InstanceTypeInfo;
const InstanceTypeInfoFromInstanceRequirements = @import("instance_type_info_from_instance_requirements.zig").InstanceTypeInfoFromInstanceRequirements;
const InstanceTypeOffering = @import("instance_type_offering.zig").InstanceTypeOffering;
const InstanceUsage = @import("instance_usage.zig").InstanceUsage;
const IntegrateServices = @import("integrate_services.zig").IntegrateServices;
const InterfacePermissionType = @import("interface_permission_type.zig").InterfacePermissionType;
const InterfaceProtocolType = @import("interface_protocol_type.zig").InterfaceProtocolType;
const InternetGateway = @import("internet_gateway.zig").InternetGateway;
const InternetGatewayAttachment = @import("internet_gateway_attachment.zig").InternetGatewayAttachment;
const InternetGatewayBlockMode = @import("internet_gateway_block_mode.zig").InternetGatewayBlockMode;
const InternetGatewayExclusionMode = @import("internet_gateway_exclusion_mode.zig").InternetGatewayExclusionMode;
const InterruptibleCapacityAllocation = @import("interruptible_capacity_allocation.zig").InterruptibleCapacityAllocation;
const InterruptibleCapacityReservationAllocationStatus = @import("interruptible_capacity_reservation_allocation_status.zig").InterruptibleCapacityReservationAllocationStatus;
const InterruptionInfo = @import("interruption_info.zig").InterruptionInfo;
const InterruptionType = @import("interruption_type.zig").InterruptionType;
const IpAddressType = @import("ip_address_type.zig").IpAddressType;
const IpPermission = @import("ip_permission.zig").IpPermission;
const IpRange = @import("ip_range.zig").IpRange;
const IpSource = @import("ip_source.zig").IpSource;
const Ipam = @import("ipam.zig").Ipam;
const IpamAddressHistoryRecord = @import("ipam_address_history_record.zig").IpamAddressHistoryRecord;
const IpamAddressHistoryResourceType = @import("ipam_address_history_resource_type.zig").IpamAddressHistoryResourceType;
const IpamAssociatedResourceDiscoveryStatus = @import("ipam_associated_resource_discovery_status.zig").IpamAssociatedResourceDiscoveryStatus;
const IpamCidrAuthorizationContext = @import("ipam_cidr_authorization_context.zig").IpamCidrAuthorizationContext;
const IpamComplianceStatus = @import("ipam_compliance_status.zig").IpamComplianceStatus;
const IpamDiscoveredAccount = @import("ipam_discovered_account.zig").IpamDiscoveredAccount;
const IpamDiscoveredPublicAddress = @import("ipam_discovered_public_address.zig").IpamDiscoveredPublicAddress;
const IpamDiscoveredResourceCidr = @import("ipam_discovered_resource_cidr.zig").IpamDiscoveredResourceCidr;
const IpamDiscoveryFailureCode = @import("ipam_discovery_failure_code.zig").IpamDiscoveryFailureCode;
const IpamDiscoveryFailureReason = @import("ipam_discovery_failure_reason.zig").IpamDiscoveryFailureReason;
const IpamExternalResourceVerificationToken = @import("ipam_external_resource_verification_token.zig").IpamExternalResourceVerificationToken;
const IpamExternalResourceVerificationTokenState = @import("ipam_external_resource_verification_token_state.zig").IpamExternalResourceVerificationTokenState;
const IpamManagementState = @import("ipam_management_state.zig").IpamManagementState;
const IpamMeteredAccount = @import("ipam_metered_account.zig").IpamMeteredAccount;
const IpamNetworkInterfaceAttachmentStatus = @import("ipam_network_interface_attachment_status.zig").IpamNetworkInterfaceAttachmentStatus;
const IpamOperatingRegion = @import("ipam_operating_region.zig").IpamOperatingRegion;
const IpamOrganizationalUnitExclusion = @import("ipam_organizational_unit_exclusion.zig").IpamOrganizationalUnitExclusion;
const IpamOverlapStatus = @import("ipam_overlap_status.zig").IpamOverlapStatus;
const IpamPolicy = @import("ipam_policy.zig").IpamPolicy;
const IpamPolicyAllocationRule = @import("ipam_policy_allocation_rule.zig").IpamPolicyAllocationRule;
const IpamPolicyAllocationRuleRequest = @import("ipam_policy_allocation_rule_request.zig").IpamPolicyAllocationRuleRequest;
const IpamPolicyDocument = @import("ipam_policy_document.zig").IpamPolicyDocument;
const IpamPolicyOrganizationTarget = @import("ipam_policy_organization_target.zig").IpamPolicyOrganizationTarget;
const IpamPolicyResourceType = @import("ipam_policy_resource_type.zig").IpamPolicyResourceType;
const IpamPolicyState = @import("ipam_policy_state.zig").IpamPolicyState;
const IpamPool = @import("ipam_pool.zig").IpamPool;
const IpamPoolAllocation = @import("ipam_pool_allocation.zig").IpamPoolAllocation;
const IpamPoolAllocationResourceType = @import("ipam_pool_allocation_resource_type.zig").IpamPoolAllocationResourceType;
const IpamPoolAwsService = @import("ipam_pool_aws_service.zig").IpamPoolAwsService;
const IpamPoolCidr = @import("ipam_pool_cidr.zig").IpamPoolCidr;
const IpamPoolCidrFailureCode = @import("ipam_pool_cidr_failure_code.zig").IpamPoolCidrFailureCode;
const IpamPoolCidrFailureReason = @import("ipam_pool_cidr_failure_reason.zig").IpamPoolCidrFailureReason;
const IpamPoolCidrState = @import("ipam_pool_cidr_state.zig").IpamPoolCidrState;
const IpamPoolPublicIpSource = @import("ipam_pool_public_ip_source.zig").IpamPoolPublicIpSource;
const IpamPoolSourceResource = @import("ipam_pool_source_resource.zig").IpamPoolSourceResource;
const IpamPoolSourceResourceRequest = @import("ipam_pool_source_resource_request.zig").IpamPoolSourceResourceRequest;
const IpamPoolSourceResourceType = @import("ipam_pool_source_resource_type.zig").IpamPoolSourceResourceType;
const IpamPoolState = @import("ipam_pool_state.zig").IpamPoolState;
const IpamPrefixListResolver = @import("ipam_prefix_list_resolver.zig").IpamPrefixListResolver;
const IpamPrefixListResolverRule = @import("ipam_prefix_list_resolver_rule.zig").IpamPrefixListResolverRule;
const IpamPrefixListResolverRuleCondition = @import("ipam_prefix_list_resolver_rule_condition.zig").IpamPrefixListResolverRuleCondition;
const IpamPrefixListResolverRuleConditionOperation = @import("ipam_prefix_list_resolver_rule_condition_operation.zig").IpamPrefixListResolverRuleConditionOperation;
const IpamPrefixListResolverRuleConditionRequest = @import("ipam_prefix_list_resolver_rule_condition_request.zig").IpamPrefixListResolverRuleConditionRequest;
const IpamPrefixListResolverRuleRequest = @import("ipam_prefix_list_resolver_rule_request.zig").IpamPrefixListResolverRuleRequest;
const IpamPrefixListResolverRuleType = @import("ipam_prefix_list_resolver_rule_type.zig").IpamPrefixListResolverRuleType;
const IpamPrefixListResolverState = @import("ipam_prefix_list_resolver_state.zig").IpamPrefixListResolverState;
const IpamPrefixListResolverTarget = @import("ipam_prefix_list_resolver_target.zig").IpamPrefixListResolverTarget;
const IpamPrefixListResolverTargetState = @import("ipam_prefix_list_resolver_target_state.zig").IpamPrefixListResolverTargetState;
const IpamPrefixListResolverVersion = @import("ipam_prefix_list_resolver_version.zig").IpamPrefixListResolverVersion;
const IpamPrefixListResolverVersionCreationStatus = @import("ipam_prefix_list_resolver_version_creation_status.zig").IpamPrefixListResolverVersionCreationStatus;
const IpamPrefixListResolverVersionEntry = @import("ipam_prefix_list_resolver_version_entry.zig").IpamPrefixListResolverVersionEntry;
const IpamPublicAddressAssociationStatus = @import("ipam_public_address_association_status.zig").IpamPublicAddressAssociationStatus;
const IpamPublicAddressAwsService = @import("ipam_public_address_aws_service.zig").IpamPublicAddressAwsService;
const IpamPublicAddressSecurityGroup = @import("ipam_public_address_security_group.zig").IpamPublicAddressSecurityGroup;
const IpamPublicAddressTag = @import("ipam_public_address_tag.zig").IpamPublicAddressTag;
const IpamPublicAddressTags = @import("ipam_public_address_tags.zig").IpamPublicAddressTags;
const IpamPublicAddressType = @import("ipam_public_address_type.zig").IpamPublicAddressType;
const IpamResourceCidr = @import("ipam_resource_cidr.zig").IpamResourceCidr;
const IpamResourceCidrIpSource = @import("ipam_resource_cidr_ip_source.zig").IpamResourceCidrIpSource;
const IpamResourceDiscovery = @import("ipam_resource_discovery.zig").IpamResourceDiscovery;
const IpamResourceDiscoveryAssociation = @import("ipam_resource_discovery_association.zig").IpamResourceDiscoveryAssociation;
const IpamResourceDiscoveryAssociationState = @import("ipam_resource_discovery_association_state.zig").IpamResourceDiscoveryAssociationState;
const IpamResourceDiscoveryState = @import("ipam_resource_discovery_state.zig").IpamResourceDiscoveryState;
const IpamResourceTag = @import("ipam_resource_tag.zig").IpamResourceTag;
const IpamResourceType = @import("ipam_resource_type.zig").IpamResourceType;
const IpamScope = @import("ipam_scope.zig").IpamScope;
const IpamScopeExternalAuthorityConfiguration = @import("ipam_scope_external_authority_configuration.zig").IpamScopeExternalAuthorityConfiguration;
const IpamScopeExternalAuthorityType = @import("ipam_scope_external_authority_type.zig").IpamScopeExternalAuthorityType;
const IpamScopeState = @import("ipam_scope_state.zig").IpamScopeState;
const IpamScopeType = @import("ipam_scope_type.zig").IpamScopeType;
const IpamState = @import("ipam_state.zig").IpamState;
const IpamTier = @import("ipam_tier.zig").IpamTier;
const Ipv4PrefixSpecification = @import("ipv_4_prefix_specification.zig").Ipv4PrefixSpecification;
const Ipv4PrefixSpecificationRequest = @import("ipv_4_prefix_specification_request.zig").Ipv4PrefixSpecificationRequest;
const Ipv4PrefixSpecificationResponse = @import("ipv_4_prefix_specification_response.zig").Ipv4PrefixSpecificationResponse;
const Ipv6AddressAttribute = @import("ipv_6_address_attribute.zig").Ipv6AddressAttribute;
const Ipv6CidrAssociation = @import("ipv_6_cidr_association.zig").Ipv6CidrAssociation;
const Ipv6CidrBlock = @import("ipv_6_cidr_block.zig").Ipv6CidrBlock;
const Ipv6Pool = @import("ipv_6_pool.zig").Ipv6Pool;
const Ipv6PrefixSpecification = @import("ipv_6_prefix_specification.zig").Ipv6PrefixSpecification;
const Ipv6PrefixSpecificationRequest = @import("ipv_6_prefix_specification_request.zig").Ipv6PrefixSpecificationRequest;
const Ipv6PrefixSpecificationResponse = @import("ipv_6_prefix_specification_response.zig").Ipv6PrefixSpecificationResponse;
const Ipv6Range = @import("ipv_6_range.zig").Ipv6Range;
const Ipv6SupportValue = @import("ipv_6_support_value.zig").Ipv6SupportValue;
const KeyPairInfo = @import("key_pair_info.zig").KeyPairInfo;
const KeyType = @import("key_type.zig").KeyType;
const LastError = @import("last_error.zig").LastError;
const LaunchPermission = @import("launch_permission.zig").LaunchPermission;
const LaunchPermissionModifications = @import("launch_permission_modifications.zig").LaunchPermissionModifications;
const LaunchSpecification = @import("launch_specification.zig").LaunchSpecification;
const LaunchTemplate = @import("launch_template.zig").LaunchTemplate;
const LaunchTemplateAndOverridesResponse = @import("launch_template_and_overrides_response.zig").LaunchTemplateAndOverridesResponse;
const LaunchTemplateAutoRecoveryState = @import("launch_template_auto_recovery_state.zig").LaunchTemplateAutoRecoveryState;
const LaunchTemplateBlockDeviceMapping = @import("launch_template_block_device_mapping.zig").LaunchTemplateBlockDeviceMapping;
const LaunchTemplateBlockDeviceMappingRequest = @import("launch_template_block_device_mapping_request.zig").LaunchTemplateBlockDeviceMappingRequest;
const LaunchTemplateCapacityReservationSpecificationRequest = @import("launch_template_capacity_reservation_specification_request.zig").LaunchTemplateCapacityReservationSpecificationRequest;
const LaunchTemplateCapacityReservationSpecificationResponse = @import("launch_template_capacity_reservation_specification_response.zig").LaunchTemplateCapacityReservationSpecificationResponse;
const LaunchTemplateConfig = @import("launch_template_config.zig").LaunchTemplateConfig;
const LaunchTemplateCpuOptions = @import("launch_template_cpu_options.zig").LaunchTemplateCpuOptions;
const LaunchTemplateCpuOptionsRequest = @import("launch_template_cpu_options_request.zig").LaunchTemplateCpuOptionsRequest;
const LaunchTemplateEbsBlockDevice = @import("launch_template_ebs_block_device.zig").LaunchTemplateEbsBlockDevice;
const LaunchTemplateEbsBlockDeviceRequest = @import("launch_template_ebs_block_device_request.zig").LaunchTemplateEbsBlockDeviceRequest;
const LaunchTemplateElasticInferenceAccelerator = @import("launch_template_elastic_inference_accelerator.zig").LaunchTemplateElasticInferenceAccelerator;
const LaunchTemplateElasticInferenceAcceleratorResponse = @import("launch_template_elastic_inference_accelerator_response.zig").LaunchTemplateElasticInferenceAcceleratorResponse;
const LaunchTemplateEnaSrdSpecification = @import("launch_template_ena_srd_specification.zig").LaunchTemplateEnaSrdSpecification;
const LaunchTemplateEnaSrdUdpSpecification = @import("launch_template_ena_srd_udp_specification.zig").LaunchTemplateEnaSrdUdpSpecification;
const LaunchTemplateEnclaveOptions = @import("launch_template_enclave_options.zig").LaunchTemplateEnclaveOptions;
const LaunchTemplateEnclaveOptionsRequest = @import("launch_template_enclave_options_request.zig").LaunchTemplateEnclaveOptionsRequest;
const LaunchTemplateErrorCode = @import("launch_template_error_code.zig").LaunchTemplateErrorCode;
const LaunchTemplateHibernationOptions = @import("launch_template_hibernation_options.zig").LaunchTemplateHibernationOptions;
const LaunchTemplateHibernationOptionsRequest = @import("launch_template_hibernation_options_request.zig").LaunchTemplateHibernationOptionsRequest;
const LaunchTemplateHttpTokensState = @import("launch_template_http_tokens_state.zig").LaunchTemplateHttpTokensState;
const LaunchTemplateIamInstanceProfileSpecification = @import("launch_template_iam_instance_profile_specification.zig").LaunchTemplateIamInstanceProfileSpecification;
const LaunchTemplateIamInstanceProfileSpecificationRequest = @import("launch_template_iam_instance_profile_specification_request.zig").LaunchTemplateIamInstanceProfileSpecificationRequest;
const LaunchTemplateInstanceMaintenanceOptions = @import("launch_template_instance_maintenance_options.zig").LaunchTemplateInstanceMaintenanceOptions;
const LaunchTemplateInstanceMaintenanceOptionsRequest = @import("launch_template_instance_maintenance_options_request.zig").LaunchTemplateInstanceMaintenanceOptionsRequest;
const LaunchTemplateInstanceMarketOptions = @import("launch_template_instance_market_options.zig").LaunchTemplateInstanceMarketOptions;
const LaunchTemplateInstanceMarketOptionsRequest = @import("launch_template_instance_market_options_request.zig").LaunchTemplateInstanceMarketOptionsRequest;
const LaunchTemplateInstanceMetadataEndpointState = @import("launch_template_instance_metadata_endpoint_state.zig").LaunchTemplateInstanceMetadataEndpointState;
const LaunchTemplateInstanceMetadataOptions = @import("launch_template_instance_metadata_options.zig").LaunchTemplateInstanceMetadataOptions;
const LaunchTemplateInstanceMetadataOptionsRequest = @import("launch_template_instance_metadata_options_request.zig").LaunchTemplateInstanceMetadataOptionsRequest;
const LaunchTemplateInstanceMetadataOptionsState = @import("launch_template_instance_metadata_options_state.zig").LaunchTemplateInstanceMetadataOptionsState;
const LaunchTemplateInstanceMetadataProtocolIpv6 = @import("launch_template_instance_metadata_protocol_ipv_6.zig").LaunchTemplateInstanceMetadataProtocolIpv6;
const LaunchTemplateInstanceMetadataTagsState = @import("launch_template_instance_metadata_tags_state.zig").LaunchTemplateInstanceMetadataTagsState;
const LaunchTemplateInstanceNetworkInterfaceSpecification = @import("launch_template_instance_network_interface_specification.zig").LaunchTemplateInstanceNetworkInterfaceSpecification;
const LaunchTemplateInstanceNetworkInterfaceSpecificationRequest = @import("launch_template_instance_network_interface_specification_request.zig").LaunchTemplateInstanceNetworkInterfaceSpecificationRequest;
const LaunchTemplateLicenseConfiguration = @import("launch_template_license_configuration.zig").LaunchTemplateLicenseConfiguration;
const LaunchTemplateLicenseConfigurationRequest = @import("launch_template_license_configuration_request.zig").LaunchTemplateLicenseConfigurationRequest;
const LaunchTemplateNetworkPerformanceOptions = @import("launch_template_network_performance_options.zig").LaunchTemplateNetworkPerformanceOptions;
const LaunchTemplateNetworkPerformanceOptionsRequest = @import("launch_template_network_performance_options_request.zig").LaunchTemplateNetworkPerformanceOptionsRequest;
const LaunchTemplateOverrides = @import("launch_template_overrides.zig").LaunchTemplateOverrides;
const LaunchTemplatePlacement = @import("launch_template_placement.zig").LaunchTemplatePlacement;
const LaunchTemplatePlacementRequest = @import("launch_template_placement_request.zig").LaunchTemplatePlacementRequest;
const LaunchTemplatePrivateDnsNameOptions = @import("launch_template_private_dns_name_options.zig").LaunchTemplatePrivateDnsNameOptions;
const LaunchTemplatePrivateDnsNameOptionsRequest = @import("launch_template_private_dns_name_options_request.zig").LaunchTemplatePrivateDnsNameOptionsRequest;
const LaunchTemplateSpecification = @import("launch_template_specification.zig").LaunchTemplateSpecification;
const LaunchTemplateSpotMarketOptions = @import("launch_template_spot_market_options.zig").LaunchTemplateSpotMarketOptions;
const LaunchTemplateSpotMarketOptionsRequest = @import("launch_template_spot_market_options_request.zig").LaunchTemplateSpotMarketOptionsRequest;
const LaunchTemplateTagSpecification = @import("launch_template_tag_specification.zig").LaunchTemplateTagSpecification;
const LaunchTemplateTagSpecificationRequest = @import("launch_template_tag_specification_request.zig").LaunchTemplateTagSpecificationRequest;
const LaunchTemplateVersion = @import("launch_template_version.zig").LaunchTemplateVersion;
const LaunchTemplatesMonitoring = @import("launch_templates_monitoring.zig").LaunchTemplatesMonitoring;
const LaunchTemplatesMonitoringRequest = @import("launch_templates_monitoring_request.zig").LaunchTemplatesMonitoringRequest;
const LicenseConfiguration = @import("license_configuration.zig").LicenseConfiguration;
const LicenseConfigurationRequest = @import("license_configuration_request.zig").LicenseConfigurationRequest;
const ListingState = @import("listing_state.zig").ListingState;
const ListingStatus = @import("listing_status.zig").ListingStatus;
const LoadBalancersConfig = @import("load_balancers_config.zig").LoadBalancersConfig;
const LoadPermission = @import("load_permission.zig").LoadPermission;
const LoadPermissionModifications = @import("load_permission_modifications.zig").LoadPermissionModifications;
const LoadPermissionRequest = @import("load_permission_request.zig").LoadPermissionRequest;
const LocalGateway = @import("local_gateway.zig").LocalGateway;
const LocalGatewayRoute = @import("local_gateway_route.zig").LocalGatewayRoute;
const LocalGatewayRouteState = @import("local_gateway_route_state.zig").LocalGatewayRouteState;
const LocalGatewayRouteTable = @import("local_gateway_route_table.zig").LocalGatewayRouteTable;
const LocalGatewayRouteTableMode = @import("local_gateway_route_table_mode.zig").LocalGatewayRouteTableMode;
const LocalGatewayRouteTableVirtualInterfaceGroupAssociation = @import("local_gateway_route_table_virtual_interface_group_association.zig").LocalGatewayRouteTableVirtualInterfaceGroupAssociation;
const LocalGatewayRouteTableVpcAssociation = @import("local_gateway_route_table_vpc_association.zig").LocalGatewayRouteTableVpcAssociation;
const LocalGatewayRouteType = @import("local_gateway_route_type.zig").LocalGatewayRouteType;
const LocalGatewayVirtualInterface = @import("local_gateway_virtual_interface.zig").LocalGatewayVirtualInterface;
const LocalGatewayVirtualInterfaceConfigurationState = @import("local_gateway_virtual_interface_configuration_state.zig").LocalGatewayVirtualInterfaceConfigurationState;
const LocalGatewayVirtualInterfaceGroup = @import("local_gateway_virtual_interface_group.zig").LocalGatewayVirtualInterfaceGroup;
const LocalGatewayVirtualInterfaceGroupConfigurationState = @import("local_gateway_virtual_interface_group_configuration_state.zig").LocalGatewayVirtualInterfaceGroupConfigurationState;
const LocalStorage = @import("local_storage.zig").LocalStorage;
const LocalStorageType = @import("local_storage_type.zig").LocalStorageType;
const LocationType = @import("location_type.zig").LocationType;
const LockState = @import("lock_state.zig").LockState;
const LockedSnapshotsInfo = @import("locked_snapshots_info.zig").LockedSnapshotsInfo;
const LogDestinationType = @import("log_destination_type.zig").LogDestinationType;
const MacHost = @import("mac_host.zig").MacHost;
const MacModificationTask = @import("mac_modification_task.zig").MacModificationTask;
const MacModificationTaskState = @import("mac_modification_task_state.zig").MacModificationTaskState;
const MacModificationTaskType = @import("mac_modification_task_type.zig").MacModificationTaskType;
const MacSystemIntegrityProtectionConfiguration = @import("mac_system_integrity_protection_configuration.zig").MacSystemIntegrityProtectionConfiguration;
const MacSystemIntegrityProtectionConfigurationRequest = @import("mac_system_integrity_protection_configuration_request.zig").MacSystemIntegrityProtectionConfigurationRequest;
const MacSystemIntegrityProtectionSettingStatus = @import("mac_system_integrity_protection_setting_status.zig").MacSystemIntegrityProtectionSettingStatus;
const MaintenanceDetails = @import("maintenance_details.zig").MaintenanceDetails;
const ManagedBy = @import("managed_by.zig").ManagedBy;
const ManagedPrefixList = @import("managed_prefix_list.zig").ManagedPrefixList;
const MarketType = @import("market_type.zig").MarketType;
const MediaAcceleratorInfo = @import("media_accelerator_info.zig").MediaAcceleratorInfo;
const MediaDeviceInfo = @import("media_device_info.zig").MediaDeviceInfo;
const MediaDeviceMemoryInfo = @import("media_device_memory_info.zig").MediaDeviceMemoryInfo;
const MembershipType = @import("membership_type.zig").MembershipType;
const MemoryGiBPerVCpu = @import("memory_gi_b_per_v_cpu.zig").MemoryGiBPerVCpu;
const MemoryGiBPerVCpuRequest = @import("memory_gi_b_per_v_cpu_request.zig").MemoryGiBPerVCpuRequest;
const MemoryInfo = @import("memory_info.zig").MemoryInfo;
const MemoryMiB = @import("memory_mi_b.zig").MemoryMiB;
const MemoryMiBRequest = @import("memory_mi_b_request.zig").MemoryMiBRequest;
const Metric = @import("metric.zig").Metric;
const MetricDataResult = @import("metric_data_result.zig").MetricDataResult;
const MetricPoint = @import("metric_point.zig").MetricPoint;
const MetricType = @import("metric_type.zig").MetricType;
const MetricValue = @import("metric_value.zig").MetricValue;
const ModifyTransitGatewayOptions = @import("modify_transit_gateway_options.zig").ModifyTransitGatewayOptions;
const ModifyTransitGatewayVpcAttachmentRequestOptions = @import("modify_transit_gateway_vpc_attachment_request_options.zig").ModifyTransitGatewayVpcAttachmentRequestOptions;
const ModifyVerifiedAccessEndpointCidrOptions = @import("modify_verified_access_endpoint_cidr_options.zig").ModifyVerifiedAccessEndpointCidrOptions;
const ModifyVerifiedAccessEndpointEniOptions = @import("modify_verified_access_endpoint_eni_options.zig").ModifyVerifiedAccessEndpointEniOptions;
const ModifyVerifiedAccessEndpointLoadBalancerOptions = @import("modify_verified_access_endpoint_load_balancer_options.zig").ModifyVerifiedAccessEndpointLoadBalancerOptions;
const ModifyVerifiedAccessEndpointPortRange = @import("modify_verified_access_endpoint_port_range.zig").ModifyVerifiedAccessEndpointPortRange;
const ModifyVerifiedAccessEndpointRdsOptions = @import("modify_verified_access_endpoint_rds_options.zig").ModifyVerifiedAccessEndpointRdsOptions;
const ModifyVerifiedAccessNativeApplicationOidcOptions = @import("modify_verified_access_native_application_oidc_options.zig").ModifyVerifiedAccessNativeApplicationOidcOptions;
const ModifyVerifiedAccessTrustProviderDeviceOptions = @import("modify_verified_access_trust_provider_device_options.zig").ModifyVerifiedAccessTrustProviderDeviceOptions;
const ModifyVerifiedAccessTrustProviderOidcOptions = @import("modify_verified_access_trust_provider_oidc_options.zig").ModifyVerifiedAccessTrustProviderOidcOptions;
const ModifyVpnTunnelOptionsSpecification = @import("modify_vpn_tunnel_options_specification.zig").ModifyVpnTunnelOptionsSpecification;
const Monitoring = @import("monitoring.zig").Monitoring;
const MonitoringState = @import("monitoring_state.zig").MonitoringState;
const MoveStatus = @import("move_status.zig").MoveStatus;
const MovingAddressStatus = @import("moving_address_status.zig").MovingAddressStatus;
const MulticastSupportValue = @import("multicast_support_value.zig").MulticastSupportValue;
const NatGateway = @import("nat_gateway.zig").NatGateway;
const NatGatewayAddress = @import("nat_gateway_address.zig").NatGatewayAddress;
const NatGatewayAddressStatus = @import("nat_gateway_address_status.zig").NatGatewayAddressStatus;
const NatGatewayApplianceModifyState = @import("nat_gateway_appliance_modify_state.zig").NatGatewayApplianceModifyState;
const NatGatewayApplianceState = @import("nat_gateway_appliance_state.zig").NatGatewayApplianceState;
const NatGatewayApplianceType = @import("nat_gateway_appliance_type.zig").NatGatewayApplianceType;
const NatGatewayAttachedAppliance = @import("nat_gateway_attached_appliance.zig").NatGatewayAttachedAppliance;
const NatGatewayState = @import("nat_gateway_state.zig").NatGatewayState;
const NativeApplicationOidcOptions = @import("native_application_oidc_options.zig").NativeApplicationOidcOptions;
const NetworkAcl = @import("network_acl.zig").NetworkAcl;
const NetworkAclAssociation = @import("network_acl_association.zig").NetworkAclAssociation;
const NetworkAclEntry = @import("network_acl_entry.zig").NetworkAclEntry;
const NetworkBandwidthGbps = @import("network_bandwidth_gbps.zig").NetworkBandwidthGbps;
const NetworkBandwidthGbpsRequest = @import("network_bandwidth_gbps_request.zig").NetworkBandwidthGbpsRequest;
const NetworkCardInfo = @import("network_card_info.zig").NetworkCardInfo;
const NetworkInfo = @import("network_info.zig").NetworkInfo;
const NetworkInsightsAccessScope = @import("network_insights_access_scope.zig").NetworkInsightsAccessScope;
const NetworkInsightsAccessScopeAnalysis = @import("network_insights_access_scope_analysis.zig").NetworkInsightsAccessScopeAnalysis;
const NetworkInsightsAccessScopeContent = @import("network_insights_access_scope_content.zig").NetworkInsightsAccessScopeContent;
const NetworkInsightsAnalysis = @import("network_insights_analysis.zig").NetworkInsightsAnalysis;
const NetworkInsightsPath = @import("network_insights_path.zig").NetworkInsightsPath;
const NetworkInterface = @import("network_interface.zig").NetworkInterface;
const NetworkInterfaceAssociation = @import("network_interface_association.zig").NetworkInterfaceAssociation;
const NetworkInterfaceAttachment = @import("network_interface_attachment.zig").NetworkInterfaceAttachment;
const NetworkInterfaceAttachmentChanges = @import("network_interface_attachment_changes.zig").NetworkInterfaceAttachmentChanges;
const NetworkInterfaceCount = @import("network_interface_count.zig").NetworkInterfaceCount;
const NetworkInterfaceCountRequest = @import("network_interface_count_request.zig").NetworkInterfaceCountRequest;
const NetworkInterfaceIpv6Address = @import("network_interface_ipv_6_address.zig").NetworkInterfaceIpv6Address;
const NetworkInterfacePermission = @import("network_interface_permission.zig").NetworkInterfacePermission;
const NetworkInterfacePermissionState = @import("network_interface_permission_state.zig").NetworkInterfacePermissionState;
const NetworkInterfacePermissionStateCode = @import("network_interface_permission_state_code.zig").NetworkInterfacePermissionStateCode;
const NetworkInterfacePrivateIpAddress = @import("network_interface_private_ip_address.zig").NetworkInterfacePrivateIpAddress;
const NetworkInterfaceStatus = @import("network_interface_status.zig").NetworkInterfaceStatus;
const NetworkInterfaceType = @import("network_interface_type.zig").NetworkInterfaceType;
const NeuronDeviceCoreInfo = @import("neuron_device_core_info.zig").NeuronDeviceCoreInfo;
const NeuronDeviceInfo = @import("neuron_device_info.zig").NeuronDeviceInfo;
const NeuronDeviceMemoryInfo = @import("neuron_device_memory_info.zig").NeuronDeviceMemoryInfo;
const NeuronInfo = @import("neuron_info.zig").NeuronInfo;
const NewDhcpConfiguration = @import("new_dhcp_configuration.zig").NewDhcpConfiguration;
const NitroEnclavesSupport = @import("nitro_enclaves_support.zig").NitroEnclavesSupport;
const NitroTpmInfo = @import("nitro_tpm_info.zig").NitroTpmInfo;
const NitroTpmSupport = @import("nitro_tpm_support.zig").NitroTpmSupport;
const OfferingClassType = @import("offering_class_type.zig").OfferingClassType;
const OfferingTypeValues = @import("offering_type_values.zig").OfferingTypeValues;
const OidcOptions = @import("oidc_options.zig").OidcOptions;
const OnDemandAllocationStrategy = @import("on_demand_allocation_strategy.zig").OnDemandAllocationStrategy;
const OnDemandOptions = @import("on_demand_options.zig").OnDemandOptions;
const OnDemandOptionsRequest = @import("on_demand_options_request.zig").OnDemandOptionsRequest;
const OperatorRequest = @import("operator_request.zig").OperatorRequest;
const OperatorResponse = @import("operator_response.zig").OperatorResponse;
const OutpostLag = @import("outpost_lag.zig").OutpostLag;
const OutputFormat = @import("output_format.zig").OutputFormat;
const PacketHeaderStatement = @import("packet_header_statement.zig").PacketHeaderStatement;
const PacketHeaderStatementRequest = @import("packet_header_statement_request.zig").PacketHeaderStatementRequest;
const PartitionLoadFrequency = @import("partition_load_frequency.zig").PartitionLoadFrequency;
const PathComponent = @import("path_component.zig").PathComponent;
const PathFilter = @import("path_filter.zig").PathFilter;
const PathRequestFilter = @import("path_request_filter.zig").PathRequestFilter;
const PathStatement = @import("path_statement.zig").PathStatement;
const PathStatementRequest = @import("path_statement_request.zig").PathStatementRequest;
const PayerResponsibility = @import("payer_responsibility.zig").PayerResponsibility;
const PaymentOption = @import("payment_option.zig").PaymentOption;
const PciId = @import("pci_id.zig").PciId;
const PeeringAttachmentStatus = @import("peering_attachment_status.zig").PeeringAttachmentStatus;
const PeeringConnectionOptions = @import("peering_connection_options.zig").PeeringConnectionOptions;
const PeeringConnectionOptionsRequest = @import("peering_connection_options_request.zig").PeeringConnectionOptionsRequest;
const PeeringTgwInfo = @import("peering_tgw_info.zig").PeeringTgwInfo;
const PerformanceFactorReference = @import("performance_factor_reference.zig").PerformanceFactorReference;
const PerformanceFactorReferenceRequest = @import("performance_factor_reference_request.zig").PerformanceFactorReferenceRequest;
const PeriodType = @import("period_type.zig").PeriodType;
const PermissionGroup = @import("permission_group.zig").PermissionGroup;
const Phase1DHGroupNumbersListValue = @import("phase_1_dh_group_numbers_list_value.zig").Phase1DHGroupNumbersListValue;
const Phase1DHGroupNumbersRequestListValue = @import("phase_1_dh_group_numbers_request_list_value.zig").Phase1DHGroupNumbersRequestListValue;
const Phase1EncryptionAlgorithmsListValue = @import("phase_1_encryption_algorithms_list_value.zig").Phase1EncryptionAlgorithmsListValue;
const Phase1EncryptionAlgorithmsRequestListValue = @import("phase_1_encryption_algorithms_request_list_value.zig").Phase1EncryptionAlgorithmsRequestListValue;
const Phase1IntegrityAlgorithmsListValue = @import("phase_1_integrity_algorithms_list_value.zig").Phase1IntegrityAlgorithmsListValue;
const Phase1IntegrityAlgorithmsRequestListValue = @import("phase_1_integrity_algorithms_request_list_value.zig").Phase1IntegrityAlgorithmsRequestListValue;
const Phase2DHGroupNumbersListValue = @import("phase_2_dh_group_numbers_list_value.zig").Phase2DHGroupNumbersListValue;
const Phase2DHGroupNumbersRequestListValue = @import("phase_2_dh_group_numbers_request_list_value.zig").Phase2DHGroupNumbersRequestListValue;
const Phase2EncryptionAlgorithmsListValue = @import("phase_2_encryption_algorithms_list_value.zig").Phase2EncryptionAlgorithmsListValue;
const Phase2EncryptionAlgorithmsRequestListValue = @import("phase_2_encryption_algorithms_request_list_value.zig").Phase2EncryptionAlgorithmsRequestListValue;
const Phase2IntegrityAlgorithmsListValue = @import("phase_2_integrity_algorithms_list_value.zig").Phase2IntegrityAlgorithmsListValue;
const Phase2IntegrityAlgorithmsRequestListValue = @import("phase_2_integrity_algorithms_request_list_value.zig").Phase2IntegrityAlgorithmsRequestListValue;
const PhcSupport = @import("phc_support.zig").PhcSupport;
const Placement = @import("placement.zig").Placement;
const PlacementGroup = @import("placement_group.zig").PlacementGroup;
const PlacementGroupInfo = @import("placement_group_info.zig").PlacementGroupInfo;
const PlacementGroupState = @import("placement_group_state.zig").PlacementGroupState;
const PlacementGroupStrategy = @import("placement_group_strategy.zig").PlacementGroupStrategy;
const PlacementResponse = @import("placement_response.zig").PlacementResponse;
const PlacementStrategy = @import("placement_strategy.zig").PlacementStrategy;
const PlatformValues = @import("platform_values.zig").PlatformValues;
const PoolCidrBlock = @import("pool_cidr_block.zig").PoolCidrBlock;
const PortRange = @import("port_range.zig").PortRange;
const PrefixList = @import("prefix_list.zig").PrefixList;
const PrefixListAssociation = @import("prefix_list_association.zig").PrefixListAssociation;
const PrefixListEntry = @import("prefix_list_entry.zig").PrefixListEntry;
const PrefixListId = @import("prefix_list_id.zig").PrefixListId;
const PrefixListState = @import("prefix_list_state.zig").PrefixListState;
const PriceSchedule = @import("price_schedule.zig").PriceSchedule;
const PriceScheduleSpecification = @import("price_schedule_specification.zig").PriceScheduleSpecification;
const PricingDetail = @import("pricing_detail.zig").PricingDetail;
const PrincipalIdFormat = @import("principal_id_format.zig").PrincipalIdFormat;
const PrincipalType = @import("principal_type.zig").PrincipalType;
const PrivateDnsDetails = @import("private_dns_details.zig").PrivateDnsDetails;
const PrivateDnsNameConfiguration = @import("private_dns_name_configuration.zig").PrivateDnsNameConfiguration;
const PrivateDnsNameOptionsOnLaunch = @import("private_dns_name_options_on_launch.zig").PrivateDnsNameOptionsOnLaunch;
const PrivateDnsNameOptionsRequest = @import("private_dns_name_options_request.zig").PrivateDnsNameOptionsRequest;
const PrivateDnsNameOptionsResponse = @import("private_dns_name_options_response.zig").PrivateDnsNameOptionsResponse;
const PrivateIpAddressSpecification = @import("private_ip_address_specification.zig").PrivateIpAddressSpecification;
const ProcessorInfo = @import("processor_info.zig").ProcessorInfo;
const ProductCode = @import("product_code.zig").ProductCode;
const ProductCodeValues = @import("product_code_values.zig").ProductCodeValues;
const PropagatingVgw = @import("propagating_vgw.zig").PropagatingVgw;
const Protocol = @import("protocol.zig").Protocol;
const ProtocolValue = @import("protocol_value.zig").ProtocolValue;
const ProvisionedBandwidth = @import("provisioned_bandwidth.zig").ProvisionedBandwidth;
const PtrUpdateStatus = @import("ptr_update_status.zig").PtrUpdateStatus;
const PublicIpDnsNameOptions = @import("public_ip_dns_name_options.zig").PublicIpDnsNameOptions;
const PublicIpv4Pool = @import("public_ipv_4_pool.zig").PublicIpv4Pool;
const PublicIpv4PoolRange = @import("public_ipv_4_pool_range.zig").PublicIpv4PoolRange;
const Purchase = @import("purchase.zig").Purchase;
const PurchaseRequest = @import("purchase_request.zig").PurchaseRequest;
const RIProductDescription = @import("ri_product_description.zig").RIProductDescription;
const RebootMigrationSupport = @import("reboot_migration_support.zig").RebootMigrationSupport;
const RecurringCharge = @import("recurring_charge.zig").RecurringCharge;
const RecurringChargeFrequency = @import("recurring_charge_frequency.zig").RecurringChargeFrequency;
const ReferencedSecurityGroup = @import("referenced_security_group.zig").ReferencedSecurityGroup;
const Region = @import("region.zig").Region;
const RegionalSummary = @import("regional_summary.zig").RegionalSummary;
const RegisterInstanceTagAttributeRequest = @import("register_instance_tag_attribute_request.zig").RegisterInstanceTagAttributeRequest;
const RegisteredInstance = @import("registered_instance.zig").RegisteredInstance;
const RemoveIpamOperatingRegion = @import("remove_ipam_operating_region.zig").RemoveIpamOperatingRegion;
const RemoveIpamOrganizationalUnitExclusion = @import("remove_ipam_organizational_unit_exclusion.zig").RemoveIpamOrganizationalUnitExclusion;
const RemovePrefixListEntry = @import("remove_prefix_list_entry.zig").RemovePrefixListEntry;
const ReplaceRootVolumeTask = @import("replace_root_volume_task.zig").ReplaceRootVolumeTask;
const ReplaceRootVolumeTaskState = @import("replace_root_volume_task_state.zig").ReplaceRootVolumeTaskState;
const ReplacementStrategy = @import("replacement_strategy.zig").ReplacementStrategy;
const ReportInstanceReasonCodes = @import("report_instance_reason_codes.zig").ReportInstanceReasonCodes;
const ReportState = @import("report_state.zig").ReportState;
const RequestFilterPortRange = @import("request_filter_port_range.zig").RequestFilterPortRange;
const RequestIpamResourceTag = @import("request_ipam_resource_tag.zig").RequestIpamResourceTag;
const RequestLaunchTemplateData = @import("request_launch_template_data.zig").RequestLaunchTemplateData;
const RequestSpotLaunchSpecification = @import("request_spot_launch_specification.zig").RequestSpotLaunchSpecification;
const Reservation = @import("reservation.zig").Reservation;
const ReservationEndDateType = @import("reservation_end_date_type.zig").ReservationEndDateType;
const ReservationFleetInstanceSpecification = @import("reservation_fleet_instance_specification.zig").ReservationFleetInstanceSpecification;
const ReservationState = @import("reservation_state.zig").ReservationState;
const ReservationType = @import("reservation_type.zig").ReservationType;
const ReservationValue = @import("reservation_value.zig").ReservationValue;
const ReservedInstanceLimitPrice = @import("reserved_instance_limit_price.zig").ReservedInstanceLimitPrice;
const ReservedInstanceReservationValue = @import("reserved_instance_reservation_value.zig").ReservedInstanceReservationValue;
const ReservedInstanceState = @import("reserved_instance_state.zig").ReservedInstanceState;
const ReservedInstances = @import("reserved_instances.zig").ReservedInstances;
const ReservedInstancesConfiguration = @import("reserved_instances_configuration.zig").ReservedInstancesConfiguration;
const ReservedInstancesId = @import("reserved_instances_id.zig").ReservedInstancesId;
const ReservedInstancesListing = @import("reserved_instances_listing.zig").ReservedInstancesListing;
const ReservedInstancesModification = @import("reserved_instances_modification.zig").ReservedInstancesModification;
const ReservedInstancesModificationResult = @import("reserved_instances_modification_result.zig").ReservedInstancesModificationResult;
const ReservedInstancesOffering = @import("reserved_instances_offering.zig").ReservedInstancesOffering;
const ResourceStatement = @import("resource_statement.zig").ResourceStatement;
const ResourceStatementRequest = @import("resource_statement_request.zig").ResourceStatementRequest;
const ResourceType = @import("resource_type.zig").ResourceType;
const ResourceTypeOption = @import("resource_type_option.zig").ResourceTypeOption;
const ResourceTypeRequest = @import("resource_type_request.zig").ResourceTypeRequest;
const ResponseError = @import("response_error.zig").ResponseError;
const ResponseLaunchTemplateData = @import("response_launch_template_data.zig").ResponseLaunchTemplateData;
const RevokedSecurityGroupRule = @import("revoked_security_group_rule.zig").RevokedSecurityGroupRule;
const RootDeviceType = @import("root_device_type.zig").RootDeviceType;
const Route = @import("route.zig").Route;
const RouteOrigin = @import("route_origin.zig").RouteOrigin;
const RouteServer = @import("route_server.zig").RouteServer;
const RouteServerAssociation = @import("route_server_association.zig").RouteServerAssociation;
const RouteServerAssociationState = @import("route_server_association_state.zig").RouteServerAssociationState;
const RouteServerBfdState = @import("route_server_bfd_state.zig").RouteServerBfdState;
const RouteServerBfdStatus = @import("route_server_bfd_status.zig").RouteServerBfdStatus;
const RouteServerBgpOptions = @import("route_server_bgp_options.zig").RouteServerBgpOptions;
const RouteServerBgpOptionsRequest = @import("route_server_bgp_options_request.zig").RouteServerBgpOptionsRequest;
const RouteServerBgpState = @import("route_server_bgp_state.zig").RouteServerBgpState;
const RouteServerBgpStatus = @import("route_server_bgp_status.zig").RouteServerBgpStatus;
const RouteServerEndpoint = @import("route_server_endpoint.zig").RouteServerEndpoint;
const RouteServerEndpointState = @import("route_server_endpoint_state.zig").RouteServerEndpointState;
const RouteServerPeer = @import("route_server_peer.zig").RouteServerPeer;
const RouteServerPeerLivenessMode = @import("route_server_peer_liveness_mode.zig").RouteServerPeerLivenessMode;
const RouteServerPeerState = @import("route_server_peer_state.zig").RouteServerPeerState;
const RouteServerPersistRoutesState = @import("route_server_persist_routes_state.zig").RouteServerPersistRoutesState;
const RouteServerPropagation = @import("route_server_propagation.zig").RouteServerPropagation;
const RouteServerPropagationState = @import("route_server_propagation_state.zig").RouteServerPropagationState;
const RouteServerRoute = @import("route_server_route.zig").RouteServerRoute;
const RouteServerRouteInstallationDetail = @import("route_server_route_installation_detail.zig").RouteServerRouteInstallationDetail;
const RouteServerRouteInstallationStatus = @import("route_server_route_installation_status.zig").RouteServerRouteInstallationStatus;
const RouteServerRouteStatus = @import("route_server_route_status.zig").RouteServerRouteStatus;
const RouteServerState = @import("route_server_state.zig").RouteServerState;
const RouteState = @import("route_state.zig").RouteState;
const RouteTable = @import("route_table.zig").RouteTable;
const RouteTableAssociation = @import("route_table_association.zig").RouteTableAssociation;
const RouteTableAssociationState = @import("route_table_association_state.zig").RouteTableAssociationState;
const RouteTableAssociationStateCode = @import("route_table_association_state_code.zig").RouteTableAssociationStateCode;
const RuleAction = @import("rule_action.zig").RuleAction;
const RuleGroupRuleOptionsPair = @import("rule_group_rule_options_pair.zig").RuleGroupRuleOptionsPair;
const RuleGroupTypePair = @import("rule_group_type_pair.zig").RuleGroupTypePair;
const RuleOption = @import("rule_option.zig").RuleOption;
const RunInstancesMonitoringEnabled = @import("run_instances_monitoring_enabled.zig").RunInstancesMonitoringEnabled;
const S3ObjectTag = @import("s3_object_tag.zig").S3ObjectTag;
const S3Storage = @import("s3_storage.zig").S3Storage;
const SSEType = @import("sse_type.zig").SSEType;
const Schedule = @import("schedule.zig").Schedule;
const ScheduledInstance = @import("scheduled_instance.zig").ScheduledInstance;
const ScheduledInstanceAvailability = @import("scheduled_instance_availability.zig").ScheduledInstanceAvailability;
const ScheduledInstanceRecurrence = @import("scheduled_instance_recurrence.zig").ScheduledInstanceRecurrence;
const ScheduledInstanceRecurrenceRequest = @import("scheduled_instance_recurrence_request.zig").ScheduledInstanceRecurrenceRequest;
const ScheduledInstancesBlockDeviceMapping = @import("scheduled_instances_block_device_mapping.zig").ScheduledInstancesBlockDeviceMapping;
const ScheduledInstancesEbs = @import("scheduled_instances_ebs.zig").ScheduledInstancesEbs;
const ScheduledInstancesIamInstanceProfile = @import("scheduled_instances_iam_instance_profile.zig").ScheduledInstancesIamInstanceProfile;
const ScheduledInstancesIpv6Address = @import("scheduled_instances_ipv_6_address.zig").ScheduledInstancesIpv6Address;
const ScheduledInstancesLaunchSpecification = @import("scheduled_instances_launch_specification.zig").ScheduledInstancesLaunchSpecification;
const ScheduledInstancesMonitoring = @import("scheduled_instances_monitoring.zig").ScheduledInstancesMonitoring;
const ScheduledInstancesNetworkInterface = @import("scheduled_instances_network_interface.zig").ScheduledInstancesNetworkInterface;
const ScheduledInstancesPlacement = @import("scheduled_instances_placement.zig").ScheduledInstancesPlacement;
const ScheduledInstancesPrivateIpAddressConfig = @import("scheduled_instances_private_ip_address_config.zig").ScheduledInstancesPrivateIpAddressConfig;
const SecurityGroup = @import("security_group.zig").SecurityGroup;
const SecurityGroupForVpc = @import("security_group_for_vpc.zig").SecurityGroupForVpc;
const SecurityGroupIdentifier = @import("security_group_identifier.zig").SecurityGroupIdentifier;
const SecurityGroupReference = @import("security_group_reference.zig").SecurityGroupReference;
const SecurityGroupReferencingSupportValue = @import("security_group_referencing_support_value.zig").SecurityGroupReferencingSupportValue;
const SecurityGroupRule = @import("security_group_rule.zig").SecurityGroupRule;
const SecurityGroupRuleDescription = @import("security_group_rule_description.zig").SecurityGroupRuleDescription;
const SecurityGroupRuleRequest = @import("security_group_rule_request.zig").SecurityGroupRuleRequest;
const SecurityGroupRuleUpdate = @import("security_group_rule_update.zig").SecurityGroupRuleUpdate;
const SecurityGroupVpcAssociation = @import("security_group_vpc_association.zig").SecurityGroupVpcAssociation;
const SecurityGroupVpcAssociationState = @import("security_group_vpc_association_state.zig").SecurityGroupVpcAssociationState;
const ServiceConfiguration = @import("service_configuration.zig").ServiceConfiguration;
const ServiceConnectivityType = @import("service_connectivity_type.zig").ServiceConnectivityType;
const ServiceDetail = @import("service_detail.zig").ServiceDetail;
const ServiceLinkVirtualInterface = @import("service_link_virtual_interface.zig").ServiceLinkVirtualInterface;
const ServiceLinkVirtualInterfaceConfigurationState = @import("service_link_virtual_interface_configuration_state.zig").ServiceLinkVirtualInterfaceConfigurationState;
const ServiceManaged = @import("service_managed.zig").ServiceManaged;
const ServiceState = @import("service_state.zig").ServiceState;
const ServiceType = @import("service_type.zig").ServiceType;
const ServiceTypeDetail = @import("service_type_detail.zig").ServiceTypeDetail;
const ShutdownBehavior = @import("shutdown_behavior.zig").ShutdownBehavior;
const SlotDateTimeRangeRequest = @import("slot_date_time_range_request.zig").SlotDateTimeRangeRequest;
const SlotStartTimeRangeRequest = @import("slot_start_time_range_request.zig").SlotStartTimeRangeRequest;
const Snapshot = @import("snapshot.zig").Snapshot;
const SnapshotDetail = @import("snapshot_detail.zig").SnapshotDetail;
const SnapshotDiskContainer = @import("snapshot_disk_container.zig").SnapshotDiskContainer;
const SnapshotInfo = @import("snapshot_info.zig").SnapshotInfo;
const SnapshotRecycleBinInfo = @import("snapshot_recycle_bin_info.zig").SnapshotRecycleBinInfo;
const SnapshotReturnCodes = @import("snapshot_return_codes.zig").SnapshotReturnCodes;
const SnapshotState = @import("snapshot_state.zig").SnapshotState;
const SnapshotTaskDetail = @import("snapshot_task_detail.zig").SnapshotTaskDetail;
const SnapshotTierStatus = @import("snapshot_tier_status.zig").SnapshotTierStatus;
const SpotAllocationStrategy = @import("spot_allocation_strategy.zig").SpotAllocationStrategy;
const SpotCapacityRebalance = @import("spot_capacity_rebalance.zig").SpotCapacityRebalance;
const SpotDatafeedSubscription = @import("spot_datafeed_subscription.zig").SpotDatafeedSubscription;
const SpotFleetLaunchSpecification = @import("spot_fleet_launch_specification.zig").SpotFleetLaunchSpecification;
const SpotFleetMonitoring = @import("spot_fleet_monitoring.zig").SpotFleetMonitoring;
const SpotFleetRequestConfig = @import("spot_fleet_request_config.zig").SpotFleetRequestConfig;
const SpotFleetRequestConfigData = @import("spot_fleet_request_config_data.zig").SpotFleetRequestConfigData;
const SpotFleetTagSpecification = @import("spot_fleet_tag_specification.zig").SpotFleetTagSpecification;
const SpotInstanceInterruptionBehavior = @import("spot_instance_interruption_behavior.zig").SpotInstanceInterruptionBehavior;
const SpotInstanceRequest = @import("spot_instance_request.zig").SpotInstanceRequest;
const SpotInstanceState = @import("spot_instance_state.zig").SpotInstanceState;
const SpotInstanceStateFault = @import("spot_instance_state_fault.zig").SpotInstanceStateFault;
const SpotInstanceStatus = @import("spot_instance_status.zig").SpotInstanceStatus;
const SpotInstanceType = @import("spot_instance_type.zig").SpotInstanceType;
const SpotMaintenanceStrategies = @import("spot_maintenance_strategies.zig").SpotMaintenanceStrategies;
const SpotMarketOptions = @import("spot_market_options.zig").SpotMarketOptions;
const SpotOptions = @import("spot_options.zig").SpotOptions;
const SpotOptionsRequest = @import("spot_options_request.zig").SpotOptionsRequest;
const SpotPlacement = @import("spot_placement.zig").SpotPlacement;
const SpotPlacementScore = @import("spot_placement_score.zig").SpotPlacementScore;
const SpotPrice = @import("spot_price.zig").SpotPrice;
const SpreadLevel = @import("spread_level.zig").SpreadLevel;
const SqlServerLicenseUsage = @import("sql_server_license_usage.zig").SqlServerLicenseUsage;
const StaleIpPermission = @import("stale_ip_permission.zig").StaleIpPermission;
const StaleSecurityGroup = @import("stale_security_group.zig").StaleSecurityGroup;
const State = @import("state.zig").State;
const StateReason = @import("state_reason.zig").StateReason;
const StaticSourcesSupportValue = @import("static_sources_support_value.zig").StaticSourcesSupportValue;
const StatisticType = @import("statistic_type.zig").StatisticType;
const StatusName = @import("status_name.zig").StatusName;
const StatusType = @import("status_type.zig").StatusType;
const Storage = @import("storage.zig").Storage;
const StorageLocation = @import("storage_location.zig").StorageLocation;
const StorageTier = @import("storage_tier.zig").StorageTier;
const StoreImageTaskResult = @import("store_image_task_result.zig").StoreImageTaskResult;
const Subnet = @import("subnet.zig").Subnet;
const SubnetAssociation = @import("subnet_association.zig").SubnetAssociation;
const SubnetCidrBlockState = @import("subnet_cidr_block_state.zig").SubnetCidrBlockState;
const SubnetCidrBlockStateCode = @import("subnet_cidr_block_state_code.zig").SubnetCidrBlockStateCode;
const SubnetCidrReservation = @import("subnet_cidr_reservation.zig").SubnetCidrReservation;
const SubnetCidrReservationType = @import("subnet_cidr_reservation_type.zig").SubnetCidrReservationType;
const SubnetConfiguration = @import("subnet_configuration.zig").SubnetConfiguration;
const SubnetIpPrefixes = @import("subnet_ip_prefixes.zig").SubnetIpPrefixes;
const SubnetIpv6CidrBlockAssociation = @import("subnet_ipv_6_cidr_block_association.zig").SubnetIpv6CidrBlockAssociation;
const SubnetState = @import("subnet_state.zig").SubnetState;
const Subscription = @import("subscription.zig").Subscription;
const SuccessfulInstanceCreditSpecificationItem = @import("successful_instance_credit_specification_item.zig").SuccessfulInstanceCreditSpecificationItem;
const SuccessfulQueuedPurchaseDeletion = @import("successful_queued_purchase_deletion.zig").SuccessfulQueuedPurchaseDeletion;
const SummaryStatus = @import("summary_status.zig").SummaryStatus;
const SupportedAdditionalProcessorFeature = @import("supported_additional_processor_feature.zig").SupportedAdditionalProcessorFeature;
const SupportedRegionDetail = @import("supported_region_detail.zig").SupportedRegionDetail;
const Tag = @import("tag.zig").Tag;
const TagDescription = @import("tag_description.zig").TagDescription;
const TagSpecification = @import("tag_specification.zig").TagSpecification;
const TargetCapacitySpecification = @import("target_capacity_specification.zig").TargetCapacitySpecification;
const TargetCapacitySpecificationRequest = @import("target_capacity_specification_request.zig").TargetCapacitySpecificationRequest;
const TargetCapacityUnitType = @import("target_capacity_unit_type.zig").TargetCapacityUnitType;
const TargetConfiguration = @import("target_configuration.zig").TargetConfiguration;
const TargetConfigurationRequest = @import("target_configuration_request.zig").TargetConfigurationRequest;
const TargetGroup = @import("target_group.zig").TargetGroup;
const TargetGroupsConfig = @import("target_groups_config.zig").TargetGroupsConfig;
const TargetNetwork = @import("target_network.zig").TargetNetwork;
const TargetReservationValue = @import("target_reservation_value.zig").TargetReservationValue;
const TelemetryStatus = @import("telemetry_status.zig").TelemetryStatus;
const Tenancy = @import("tenancy.zig").Tenancy;
const TerminateConnectionStatus = @import("terminate_connection_status.zig").TerminateConnectionStatus;
const ThroughResourcesStatement = @import("through_resources_statement.zig").ThroughResourcesStatement;
const ThroughResourcesStatementRequest = @import("through_resources_statement_request.zig").ThroughResourcesStatementRequest;
const TieringOperationStatus = @import("tiering_operation_status.zig").TieringOperationStatus;
const TokenState = @import("token_state.zig").TokenState;
const TotalLocalStorageGB = @import("total_local_storage_gb.zig").TotalLocalStorageGB;
const TotalLocalStorageGBRequest = @import("total_local_storage_gb_request.zig").TotalLocalStorageGBRequest;
const TpmSupportValues = @import("tpm_support_values.zig").TpmSupportValues;
const TrafficDirection = @import("traffic_direction.zig").TrafficDirection;
const TrafficIpAddressType = @import("traffic_ip_address_type.zig").TrafficIpAddressType;
const TrafficMirrorFilter = @import("traffic_mirror_filter.zig").TrafficMirrorFilter;
const TrafficMirrorFilterRule = @import("traffic_mirror_filter_rule.zig").TrafficMirrorFilterRule;
const TrafficMirrorFilterRuleField = @import("traffic_mirror_filter_rule_field.zig").TrafficMirrorFilterRuleField;
const TrafficMirrorNetworkService = @import("traffic_mirror_network_service.zig").TrafficMirrorNetworkService;
const TrafficMirrorPortRange = @import("traffic_mirror_port_range.zig").TrafficMirrorPortRange;
const TrafficMirrorPortRangeRequest = @import("traffic_mirror_port_range_request.zig").TrafficMirrorPortRangeRequest;
const TrafficMirrorRuleAction = @import("traffic_mirror_rule_action.zig").TrafficMirrorRuleAction;
const TrafficMirrorSession = @import("traffic_mirror_session.zig").TrafficMirrorSession;
const TrafficMirrorSessionField = @import("traffic_mirror_session_field.zig").TrafficMirrorSessionField;
const TrafficMirrorTarget = @import("traffic_mirror_target.zig").TrafficMirrorTarget;
const TrafficMirrorTargetType = @import("traffic_mirror_target_type.zig").TrafficMirrorTargetType;
const TrafficType = @import("traffic_type.zig").TrafficType;
const TransferType = @import("transfer_type.zig").TransferType;
const TransitGateway = @import("transit_gateway.zig").TransitGateway;
const TransitGatewayAssociation = @import("transit_gateway_association.zig").TransitGatewayAssociation;
const TransitGatewayAssociationState = @import("transit_gateway_association_state.zig").TransitGatewayAssociationState;
const TransitGatewayAttachment = @import("transit_gateway_attachment.zig").TransitGatewayAttachment;
const TransitGatewayAttachmentAssociation = @import("transit_gateway_attachment_association.zig").TransitGatewayAttachmentAssociation;
const TransitGatewayAttachmentBgpConfiguration = @import("transit_gateway_attachment_bgp_configuration.zig").TransitGatewayAttachmentBgpConfiguration;
const TransitGatewayAttachmentPropagation = @import("transit_gateway_attachment_propagation.zig").TransitGatewayAttachmentPropagation;
const TransitGatewayAttachmentResourceType = @import("transit_gateway_attachment_resource_type.zig").TransitGatewayAttachmentResourceType;
const TransitGatewayAttachmentState = @import("transit_gateway_attachment_state.zig").TransitGatewayAttachmentState;
const TransitGatewayConnect = @import("transit_gateway_connect.zig").TransitGatewayConnect;
const TransitGatewayConnectOptions = @import("transit_gateway_connect_options.zig").TransitGatewayConnectOptions;
const TransitGatewayConnectPeer = @import("transit_gateway_connect_peer.zig").TransitGatewayConnectPeer;
const TransitGatewayConnectPeerConfiguration = @import("transit_gateway_connect_peer_configuration.zig").TransitGatewayConnectPeerConfiguration;
const TransitGatewayConnectPeerState = @import("transit_gateway_connect_peer_state.zig").TransitGatewayConnectPeerState;
const TransitGatewayConnectRequestBgpOptions = @import("transit_gateway_connect_request_bgp_options.zig").TransitGatewayConnectRequestBgpOptions;
const TransitGatewayMeteringPayerType = @import("transit_gateway_metering_payer_type.zig").TransitGatewayMeteringPayerType;
const TransitGatewayMeteringPolicy = @import("transit_gateway_metering_policy.zig").TransitGatewayMeteringPolicy;
const TransitGatewayMeteringPolicyEntry = @import("transit_gateway_metering_policy_entry.zig").TransitGatewayMeteringPolicyEntry;
const TransitGatewayMeteringPolicyEntryState = @import("transit_gateway_metering_policy_entry_state.zig").TransitGatewayMeteringPolicyEntryState;
const TransitGatewayMeteringPolicyRule = @import("transit_gateway_metering_policy_rule.zig").TransitGatewayMeteringPolicyRule;
const TransitGatewayMeteringPolicyState = @import("transit_gateway_metering_policy_state.zig").TransitGatewayMeteringPolicyState;
const TransitGatewayMulitcastDomainAssociationState = @import("transit_gateway_mulitcast_domain_association_state.zig").TransitGatewayMulitcastDomainAssociationState;
const TransitGatewayMulticastDeregisteredGroupMembers = @import("transit_gateway_multicast_deregistered_group_members.zig").TransitGatewayMulticastDeregisteredGroupMembers;
const TransitGatewayMulticastDeregisteredGroupSources = @import("transit_gateway_multicast_deregistered_group_sources.zig").TransitGatewayMulticastDeregisteredGroupSources;
const TransitGatewayMulticastDomain = @import("transit_gateway_multicast_domain.zig").TransitGatewayMulticastDomain;
const TransitGatewayMulticastDomainAssociation = @import("transit_gateway_multicast_domain_association.zig").TransitGatewayMulticastDomainAssociation;
const TransitGatewayMulticastDomainAssociations = @import("transit_gateway_multicast_domain_associations.zig").TransitGatewayMulticastDomainAssociations;
const TransitGatewayMulticastDomainOptions = @import("transit_gateway_multicast_domain_options.zig").TransitGatewayMulticastDomainOptions;
const TransitGatewayMulticastDomainState = @import("transit_gateway_multicast_domain_state.zig").TransitGatewayMulticastDomainState;
const TransitGatewayMulticastGroup = @import("transit_gateway_multicast_group.zig").TransitGatewayMulticastGroup;
const TransitGatewayMulticastRegisteredGroupMembers = @import("transit_gateway_multicast_registered_group_members.zig").TransitGatewayMulticastRegisteredGroupMembers;
const TransitGatewayMulticastRegisteredGroupSources = @import("transit_gateway_multicast_registered_group_sources.zig").TransitGatewayMulticastRegisteredGroupSources;
const TransitGatewayOptions = @import("transit_gateway_options.zig").TransitGatewayOptions;
const TransitGatewayPeeringAttachment = @import("transit_gateway_peering_attachment.zig").TransitGatewayPeeringAttachment;
const TransitGatewayPeeringAttachmentOptions = @import("transit_gateway_peering_attachment_options.zig").TransitGatewayPeeringAttachmentOptions;
const TransitGatewayPolicyRule = @import("transit_gateway_policy_rule.zig").TransitGatewayPolicyRule;
const TransitGatewayPolicyRuleMetaData = @import("transit_gateway_policy_rule_meta_data.zig").TransitGatewayPolicyRuleMetaData;
const TransitGatewayPolicyTable = @import("transit_gateway_policy_table.zig").TransitGatewayPolicyTable;
const TransitGatewayPolicyTableAssociation = @import("transit_gateway_policy_table_association.zig").TransitGatewayPolicyTableAssociation;
const TransitGatewayPolicyTableEntry = @import("transit_gateway_policy_table_entry.zig").TransitGatewayPolicyTableEntry;
const TransitGatewayPolicyTableState = @import("transit_gateway_policy_table_state.zig").TransitGatewayPolicyTableState;
const TransitGatewayPrefixListAttachment = @import("transit_gateway_prefix_list_attachment.zig").TransitGatewayPrefixListAttachment;
const TransitGatewayPrefixListReference = @import("transit_gateway_prefix_list_reference.zig").TransitGatewayPrefixListReference;
const TransitGatewayPrefixListReferenceState = @import("transit_gateway_prefix_list_reference_state.zig").TransitGatewayPrefixListReferenceState;
const TransitGatewayPropagation = @import("transit_gateway_propagation.zig").TransitGatewayPropagation;
const TransitGatewayPropagationState = @import("transit_gateway_propagation_state.zig").TransitGatewayPropagationState;
const TransitGatewayRequestOptions = @import("transit_gateway_request_options.zig").TransitGatewayRequestOptions;
const TransitGatewayRoute = @import("transit_gateway_route.zig").TransitGatewayRoute;
const TransitGatewayRouteAttachment = @import("transit_gateway_route_attachment.zig").TransitGatewayRouteAttachment;
const TransitGatewayRouteState = @import("transit_gateway_route_state.zig").TransitGatewayRouteState;
const TransitGatewayRouteTable = @import("transit_gateway_route_table.zig").TransitGatewayRouteTable;
const TransitGatewayRouteTableAnnouncement = @import("transit_gateway_route_table_announcement.zig").TransitGatewayRouteTableAnnouncement;
const TransitGatewayRouteTableAnnouncementDirection = @import("transit_gateway_route_table_announcement_direction.zig").TransitGatewayRouteTableAnnouncementDirection;
const TransitGatewayRouteTableAnnouncementState = @import("transit_gateway_route_table_announcement_state.zig").TransitGatewayRouteTableAnnouncementState;
const TransitGatewayRouteTableAssociation = @import("transit_gateway_route_table_association.zig").TransitGatewayRouteTableAssociation;
const TransitGatewayRouteTablePropagation = @import("transit_gateway_route_table_propagation.zig").TransitGatewayRouteTablePropagation;
const TransitGatewayRouteTableRoute = @import("transit_gateway_route_table_route.zig").TransitGatewayRouteTableRoute;
const TransitGatewayRouteTableState = @import("transit_gateway_route_table_state.zig").TransitGatewayRouteTableState;
const TransitGatewayRouteType = @import("transit_gateway_route_type.zig").TransitGatewayRouteType;
const TransitGatewayState = @import("transit_gateway_state.zig").TransitGatewayState;
const TransitGatewayVpcAttachment = @import("transit_gateway_vpc_attachment.zig").TransitGatewayVpcAttachment;
const TransitGatewayVpcAttachmentOptions = @import("transit_gateway_vpc_attachment_options.zig").TransitGatewayVpcAttachmentOptions;
const TransportProtocol = @import("transport_protocol.zig").TransportProtocol;
const TrunkInterfaceAssociation = @import("trunk_interface_association.zig").TrunkInterfaceAssociation;
const TrustProviderType = @import("trust_provider_type.zig").TrustProviderType;
const TunnelInsideIpVersion = @import("tunnel_inside_ip_version.zig").TunnelInsideIpVersion;
const TunnelOption = @import("tunnel_option.zig").TunnelOption;
const UnlimitedSupportedInstanceFamily = @import("unlimited_supported_instance_family.zig").UnlimitedSupportedInstanceFamily;
const UnsuccessfulInstanceCreditSpecificationErrorCode = @import("unsuccessful_instance_credit_specification_error_code.zig").UnsuccessfulInstanceCreditSpecificationErrorCode;
const UnsuccessfulInstanceCreditSpecificationItem = @import("unsuccessful_instance_credit_specification_item.zig").UnsuccessfulInstanceCreditSpecificationItem;
const UnsuccessfulInstanceCreditSpecificationItemError = @import("unsuccessful_instance_credit_specification_item_error.zig").UnsuccessfulInstanceCreditSpecificationItemError;
const UnsuccessfulItem = @import("unsuccessful_item.zig").UnsuccessfulItem;
const UnsuccessfulItemError = @import("unsuccessful_item_error.zig").UnsuccessfulItemError;
const UsageClassType = @import("usage_class_type.zig").UsageClassType;
const UserBucket = @import("user_bucket.zig").UserBucket;
const UserBucketDetails = @import("user_bucket_details.zig").UserBucketDetails;
const UserData = @import("user_data.zig").UserData;
const UserIdGroupPair = @import("user_id_group_pair.zig").UserIdGroupPair;
const UserTrustProviderType = @import("user_trust_provider_type.zig").UserTrustProviderType;
const VCpuCountRange = @import("v_cpu_count_range.zig").VCpuCountRange;
const VCpuCountRangeRequest = @import("v_cpu_count_range_request.zig").VCpuCountRangeRequest;
const VCpuInfo = @import("v_cpu_info.zig").VCpuInfo;
const ValidationError = @import("validation_error.zig").ValidationError;
const ValidationWarning = @import("validation_warning.zig").ValidationWarning;
const VerifiedAccessEndpoint = @import("verified_access_endpoint.zig").VerifiedAccessEndpoint;
const VerifiedAccessEndpointAttachmentType = @import("verified_access_endpoint_attachment_type.zig").VerifiedAccessEndpointAttachmentType;
const VerifiedAccessEndpointCidrOptions = @import("verified_access_endpoint_cidr_options.zig").VerifiedAccessEndpointCidrOptions;
const VerifiedAccessEndpointEniOptions = @import("verified_access_endpoint_eni_options.zig").VerifiedAccessEndpointEniOptions;
const VerifiedAccessEndpointLoadBalancerOptions = @import("verified_access_endpoint_load_balancer_options.zig").VerifiedAccessEndpointLoadBalancerOptions;
const VerifiedAccessEndpointPortRange = @import("verified_access_endpoint_port_range.zig").VerifiedAccessEndpointPortRange;
const VerifiedAccessEndpointProtocol = @import("verified_access_endpoint_protocol.zig").VerifiedAccessEndpointProtocol;
const VerifiedAccessEndpointRdsOptions = @import("verified_access_endpoint_rds_options.zig").VerifiedAccessEndpointRdsOptions;
const VerifiedAccessEndpointStatus = @import("verified_access_endpoint_status.zig").VerifiedAccessEndpointStatus;
const VerifiedAccessEndpointStatusCode = @import("verified_access_endpoint_status_code.zig").VerifiedAccessEndpointStatusCode;
const VerifiedAccessEndpointTarget = @import("verified_access_endpoint_target.zig").VerifiedAccessEndpointTarget;
const VerifiedAccessEndpointType = @import("verified_access_endpoint_type.zig").VerifiedAccessEndpointType;
const VerifiedAccessGroup = @import("verified_access_group.zig").VerifiedAccessGroup;
const VerifiedAccessInstance = @import("verified_access_instance.zig").VerifiedAccessInstance;
const VerifiedAccessInstanceCustomSubDomain = @import("verified_access_instance_custom_sub_domain.zig").VerifiedAccessInstanceCustomSubDomain;
const VerifiedAccessInstanceLoggingConfiguration = @import("verified_access_instance_logging_configuration.zig").VerifiedAccessInstanceLoggingConfiguration;
const VerifiedAccessInstanceOpenVpnClientConfiguration = @import("verified_access_instance_open_vpn_client_configuration.zig").VerifiedAccessInstanceOpenVpnClientConfiguration;
const VerifiedAccessInstanceOpenVpnClientConfigurationRoute = @import("verified_access_instance_open_vpn_client_configuration_route.zig").VerifiedAccessInstanceOpenVpnClientConfigurationRoute;
const VerifiedAccessInstanceUserTrustProviderClientConfiguration = @import("verified_access_instance_user_trust_provider_client_configuration.zig").VerifiedAccessInstanceUserTrustProviderClientConfiguration;
const VerifiedAccessLogCloudWatchLogsDestination = @import("verified_access_log_cloud_watch_logs_destination.zig").VerifiedAccessLogCloudWatchLogsDestination;
const VerifiedAccessLogCloudWatchLogsDestinationOptions = @import("verified_access_log_cloud_watch_logs_destination_options.zig").VerifiedAccessLogCloudWatchLogsDestinationOptions;
const VerifiedAccessLogDeliveryStatus = @import("verified_access_log_delivery_status.zig").VerifiedAccessLogDeliveryStatus;
const VerifiedAccessLogDeliveryStatusCode = @import("verified_access_log_delivery_status_code.zig").VerifiedAccessLogDeliveryStatusCode;
const VerifiedAccessLogKinesisDataFirehoseDestination = @import("verified_access_log_kinesis_data_firehose_destination.zig").VerifiedAccessLogKinesisDataFirehoseDestination;
const VerifiedAccessLogKinesisDataFirehoseDestinationOptions = @import("verified_access_log_kinesis_data_firehose_destination_options.zig").VerifiedAccessLogKinesisDataFirehoseDestinationOptions;
const VerifiedAccessLogOptions = @import("verified_access_log_options.zig").VerifiedAccessLogOptions;
const VerifiedAccessLogS3Destination = @import("verified_access_log_s3_destination.zig").VerifiedAccessLogS3Destination;
const VerifiedAccessLogS3DestinationOptions = @import("verified_access_log_s3_destination_options.zig").VerifiedAccessLogS3DestinationOptions;
const VerifiedAccessLogs = @import("verified_access_logs.zig").VerifiedAccessLogs;
const VerifiedAccessSseSpecificationRequest = @import("verified_access_sse_specification_request.zig").VerifiedAccessSseSpecificationRequest;
const VerifiedAccessSseSpecificationResponse = @import("verified_access_sse_specification_response.zig").VerifiedAccessSseSpecificationResponse;
const VerifiedAccessTrustProvider = @import("verified_access_trust_provider.zig").VerifiedAccessTrustProvider;
const VerifiedAccessTrustProviderCondensed = @import("verified_access_trust_provider_condensed.zig").VerifiedAccessTrustProviderCondensed;
const VgwTelemetry = @import("vgw_telemetry.zig").VgwTelemetry;
const VirtualizationType = @import("virtualization_type.zig").VirtualizationType;
const Volume = @import("volume.zig").Volume;
const VolumeAttachment = @import("volume_attachment.zig").VolumeAttachment;
const VolumeAttachmentState = @import("volume_attachment_state.zig").VolumeAttachmentState;
const VolumeDetail = @import("volume_detail.zig").VolumeDetail;
const VolumeModification = @import("volume_modification.zig").VolumeModification;
const VolumeModificationState = @import("volume_modification_state.zig").VolumeModificationState;
const VolumeRecycleBinInfo = @import("volume_recycle_bin_info.zig").VolumeRecycleBinInfo;
const VolumeState = @import("volume_state.zig").VolumeState;
const VolumeStatusAction = @import("volume_status_action.zig").VolumeStatusAction;
const VolumeStatusAttachmentStatus = @import("volume_status_attachment_status.zig").VolumeStatusAttachmentStatus;
const VolumeStatusDetails = @import("volume_status_details.zig").VolumeStatusDetails;
const VolumeStatusEvent = @import("volume_status_event.zig").VolumeStatusEvent;
const VolumeStatusInfo = @import("volume_status_info.zig").VolumeStatusInfo;
const VolumeStatusInfoStatus = @import("volume_status_info_status.zig").VolumeStatusInfoStatus;
const VolumeStatusItem = @import("volume_status_item.zig").VolumeStatusItem;
const VolumeStatusName = @import("volume_status_name.zig").VolumeStatusName;
const VolumeType = @import("volume_type.zig").VolumeType;
const Vpc = @import("vpc.zig").Vpc;
const VpcAttachment = @import("vpc_attachment.zig").VpcAttachment;
const VpcBlockPublicAccessExclusion = @import("vpc_block_public_access_exclusion.zig").VpcBlockPublicAccessExclusion;
const VpcBlockPublicAccessExclusionState = @import("vpc_block_public_access_exclusion_state.zig").VpcBlockPublicAccessExclusionState;
const VpcBlockPublicAccessExclusionsAllowed = @import("vpc_block_public_access_exclusions_allowed.zig").VpcBlockPublicAccessExclusionsAllowed;
const VpcBlockPublicAccessOptions = @import("vpc_block_public_access_options.zig").VpcBlockPublicAccessOptions;
const VpcBlockPublicAccessState = @import("vpc_block_public_access_state.zig").VpcBlockPublicAccessState;
const VpcCidrBlockAssociation = @import("vpc_cidr_block_association.zig").VpcCidrBlockAssociation;
const VpcCidrBlockState = @import("vpc_cidr_block_state.zig").VpcCidrBlockState;
const VpcCidrBlockStateCode = @import("vpc_cidr_block_state_code.zig").VpcCidrBlockStateCode;
const VpcClassicLink = @import("vpc_classic_link.zig").VpcClassicLink;
const VpcEncryptionControl = @import("vpc_encryption_control.zig").VpcEncryptionControl;
const VpcEncryptionControlConfiguration = @import("vpc_encryption_control_configuration.zig").VpcEncryptionControlConfiguration;
const VpcEncryptionControlExclusion = @import("vpc_encryption_control_exclusion.zig").VpcEncryptionControlExclusion;
const VpcEncryptionControlExclusionState = @import("vpc_encryption_control_exclusion_state.zig").VpcEncryptionControlExclusionState;
const VpcEncryptionControlExclusionStateInput = @import("vpc_encryption_control_exclusion_state_input.zig").VpcEncryptionControlExclusionStateInput;
const VpcEncryptionControlExclusions = @import("vpc_encryption_control_exclusions.zig").VpcEncryptionControlExclusions;
const VpcEncryptionControlMode = @import("vpc_encryption_control_mode.zig").VpcEncryptionControlMode;
const VpcEncryptionControlState = @import("vpc_encryption_control_state.zig").VpcEncryptionControlState;
const VpcEncryptionNonCompliantResource = @import("vpc_encryption_non_compliant_resource.zig").VpcEncryptionNonCompliantResource;
const VpcEndpoint = @import("vpc_endpoint.zig").VpcEndpoint;
const VpcEndpointAssociation = @import("vpc_endpoint_association.zig").VpcEndpointAssociation;
const VpcEndpointConnection = @import("vpc_endpoint_connection.zig").VpcEndpointConnection;
const VpcEndpointType = @import("vpc_endpoint_type.zig").VpcEndpointType;
const VpcIpv6CidrBlockAssociation = @import("vpc_ipv_6_cidr_block_association.zig").VpcIpv6CidrBlockAssociation;
const VpcPeeringConnection = @import("vpc_peering_connection.zig").VpcPeeringConnection;
const VpcPeeringConnectionOptionsDescription = @import("vpc_peering_connection_options_description.zig").VpcPeeringConnectionOptionsDescription;
const VpcPeeringConnectionStateReason = @import("vpc_peering_connection_state_reason.zig").VpcPeeringConnectionStateReason;
const VpcPeeringConnectionStateReasonCode = @import("vpc_peering_connection_state_reason_code.zig").VpcPeeringConnectionStateReasonCode;
const VpcPeeringConnectionVpcInfo = @import("vpc_peering_connection_vpc_info.zig").VpcPeeringConnectionVpcInfo;
const VpcState = @import("vpc_state.zig").VpcState;
const VpnConcentrator = @import("vpn_concentrator.zig").VpnConcentrator;
const VpnConnection = @import("vpn_connection.zig").VpnConnection;
const VpnConnectionDeviceType = @import("vpn_connection_device_type.zig").VpnConnectionDeviceType;
const VpnConnectionOptions = @import("vpn_connection_options.zig").VpnConnectionOptions;
const VpnConnectionOptionsSpecification = @import("vpn_connection_options_specification.zig").VpnConnectionOptionsSpecification;
const VpnEcmpSupportValue = @import("vpn_ecmp_support_value.zig").VpnEcmpSupportValue;
const VpnGateway = @import("vpn_gateway.zig").VpnGateway;
const VpnProtocol = @import("vpn_protocol.zig").VpnProtocol;
const VpnState = @import("vpn_state.zig").VpnState;
const VpnStaticRoute = @import("vpn_static_route.zig").VpnStaticRoute;
const VpnStaticRouteSource = @import("vpn_static_route_source.zig").VpnStaticRouteSource;
const VpnTunnelBandwidth = @import("vpn_tunnel_bandwidth.zig").VpnTunnelBandwidth;
const VpnTunnelLogOptions = @import("vpn_tunnel_log_options.zig").VpnTunnelLogOptions;
const VpnTunnelLogOptionsSpecification = @import("vpn_tunnel_log_options_specification.zig").VpnTunnelLogOptionsSpecification;
const VpnTunnelOptionsSpecification = @import("vpn_tunnel_options_specification.zig").VpnTunnelOptionsSpecification;
const VpnTunnelProvisioningStatus = @import("vpn_tunnel_provisioning_status.zig").VpnTunnelProvisioningStatus;
const WeekDay = @import("week_day.zig").WeekDay;
const scope = @import("scope.zig").scope;

pub fn deserializeAcceleratorManufacturerSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AcceleratorManufacturer {
    var list: std.ArrayList(AcceleratorManufacturer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (AcceleratorManufacturer.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAcceleratorNameSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AcceleratorName {
    var list: std.ArrayList(AcceleratorName) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (AcceleratorName.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAcceleratorTypeSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AcceleratorType {
    var list: std.ArrayList(AcceleratorType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (AcceleratorType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAccessScopeAnalysisFindingList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AccessScopeAnalysisFinding {
    var list: std.ArrayList(AccessScopeAnalysisFinding) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAccessScopeAnalysisFinding(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAccessScopePathList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AccessScopePath {
    var list: std.ArrayList(AccessScopePath) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAccessScopePath(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAccountAttributeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AccountAttribute {
    var list: std.ArrayList(AccountAttribute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAccountAttribute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAccountAttributeValueList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AccountAttributeValue {
    var list: std.ArrayList(AccountAttributeValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAccountAttributeValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeActiveInstanceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ActiveInstance {
    var list: std.ArrayList(ActiveInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeActiveInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAddedPrincipalSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AddedPrincipal {
    var list: std.ArrayList(AddedPrincipal) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAddedPrincipal(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAdditionalDetailList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AdditionalDetail {
    var list: std.ArrayList(AdditionalDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAdditionalDetail(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Address {
    var list: std.ArrayList(Address) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAddress(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAddressSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AddressAttribute {
    var list: std.ArrayList(AddressAttribute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAddressAttribute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAddressTransferList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AddressTransfer {
    var list: std.ArrayList(AddressTransfer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAddressTransfer(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAllowedInstanceTypeSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAllowedPrincipalSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AllowedPrincipal {
    var list: std.ArrayList(AllowedPrincipal) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAllowedPrincipal(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAlternatePathHintList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AlternatePathHint {
    var list: std.ArrayList(AlternatePathHint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAlternatePathHint(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAnalysisComponentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AnalysisComponent {
    var list: std.ArrayList(AnalysisComponent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAnalysisComponent(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeArchitectureTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ArchitectureType {
    var list: std.ArrayList(ArchitectureType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (ArchitectureType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeArnList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAsPath(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAsnAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AsnAssociation {
    var list: std.ArrayList(AsnAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAsnAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAssignedPrivateIpAddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AssignedPrivateIpAddress {
    var list: std.ArrayList(AssignedPrivateIpAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAssignedPrivateIpAddress(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAssociatedRolesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AssociatedRole {
    var list: std.ArrayList(AssociatedRole) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAssociatedRole(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAssociatedSubnetList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAssociatedTargetNetworkSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AssociatedTargetNetwork {
    var list: std.ArrayList(AssociatedTargetNetwork) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAssociatedTargetNetwork(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAttributeSummaryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AttributeSummary {
    var list: std.ArrayList(AttributeSummary) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAttributeSummary(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAuthorizationRuleSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AuthorizationRule {
    var list: std.ArrayList(AuthorizationRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAuthorizationRule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAvailabilityZoneList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AvailabilityZone {
    var list: std.ArrayList(AvailabilityZone) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAvailabilityZone(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAvailabilityZoneMessageList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AvailabilityZoneMessage {
    var list: std.ArrayList(AvailabilityZoneMessage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAvailabilityZoneMessage(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAvailableInstanceCapacityList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceCapacity {
    var list: std.ArrayList(InstanceCapacity) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceCapacity(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeBandwidthWeightingTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const BandwidthWeightingType {
    var list: std.ArrayList(BandwidthWeightingType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (BandwidthWeightingType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeBlockDeviceMappingList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const BlockDeviceMapping {
    var list: std.ArrayList(BlockDeviceMapping) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeBlockDeviceMapping(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeBlockDeviceMappingResponseList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const BlockDeviceMappingResponse {
    var list: std.ArrayList(BlockDeviceMappingResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeBlockDeviceMappingResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeBootModeTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const BootModeType {
    var list: std.ArrayList(BootModeType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (BootModeType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeBundleTaskList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const BundleTask {
    var list: std.ArrayList(BundleTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeBundleTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeByoasnSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Byoasn {
    var list: std.ArrayList(Byoasn) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeByoasn(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeByoipCidrSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ByoipCidr {
    var list: std.ArrayList(ByoipCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeByoipCidr(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCancelSpotFleetRequestsErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CancelSpotFleetRequestsErrorItem {
    var list: std.ArrayList(CancelSpotFleetRequestsErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCancelSpotFleetRequestsErrorItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCancelSpotFleetRequestsSuccessSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CancelSpotFleetRequestsSuccessItem {
    var list: std.ArrayList(CancelSpotFleetRequestsSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCancelSpotFleetRequestsSuccessItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCancelledSpotInstanceRequestList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CancelledSpotInstanceRequest {
    var list: std.ArrayList(CancelledSpotInstanceRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCancelledSpotInstanceRequest(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityAllocations(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityAllocation {
    var list: std.ArrayList(CapacityAllocation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityAllocation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityBlockExtensionOfferingSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityBlockExtensionOffering {
    var list: std.ArrayList(CapacityBlockExtensionOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityBlockExtensionOffering(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityBlockExtensionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityBlockExtension {
    var list: std.ArrayList(CapacityBlockExtension) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityBlockExtension(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityBlockOfferingSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityBlockOffering {
    var list: std.ArrayList(CapacityBlockOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityBlockOffering(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityBlockSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityBlock {
    var list: std.ArrayList(CapacityBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityBlock(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityBlockStatusSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityBlockStatus {
    var list: std.ArrayList(CapacityBlockStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityBlockStatus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityManagerDataExportResponseSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityManagerDataExportResponse {
    var list: std.ArrayList(CapacityManagerDataExportResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityManagerDataExportResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationBillingRequestSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityReservationBillingRequest {
    var list: std.ArrayList(CapacityReservationBillingRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityReservationBillingRequest(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationFleetCancellationStateSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityReservationFleetCancellationState {
    var list: std.ArrayList(CapacityReservationFleetCancellationState) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityReservationFleetCancellationState(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationFleetSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityReservationFleet {
    var list: std.ArrayList(CapacityReservationFleet) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityReservationFleet(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationGroupSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityReservationGroup {
    var list: std.ArrayList(CapacityReservationGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityReservationGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityReservation {
    var list: std.ArrayList(CapacityReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityReservation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationStatusSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityReservationStatus {
    var list: std.ArrayList(CapacityReservationStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityReservationStatus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCapacityReservationTopologySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityReservationTopology {
    var list: std.ArrayList(CapacityReservationTopology) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityReservationTopology(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCarrierGatewaySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CarrierGateway {
    var list: std.ArrayList(CarrierGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCarrierGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCidrBlockSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CidrBlock {
    var list: std.ArrayList(CidrBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCidrBlock(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeClassicLinkDnsSupportList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ClassicLinkDnsSupport {
    var list: std.ArrayList(ClassicLinkDnsSupport) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeClassicLinkDnsSupport(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeClassicLinkInstanceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ClassicLinkInstance {
    var list: std.ArrayList(ClassicLinkInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeClassicLinkInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeClassicLoadBalancers(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ClassicLoadBalancer {
    var list: std.ArrayList(ClassicLoadBalancer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeClassicLoadBalancer(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeClientVpnAuthenticationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ClientVpnAuthentication {
    var list: std.ArrayList(ClientVpnAuthentication) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeClientVpnAuthentication(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeClientVpnConnectionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ClientVpnConnection {
    var list: std.ArrayList(ClientVpnConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeClientVpnConnection(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeClientVpnRouteSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ClientVpnRoute {
    var list: std.ArrayList(ClientVpnRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeClientVpnRoute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeClientVpnSecurityGroupIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCoipAddressUsageSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CoipAddressUsage {
    var list: std.ArrayList(CoipAddressUsage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCoipAddressUsage(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCoipPoolSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CoipPool {
    var list: std.ArrayList(CoipPool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCoipPool(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeConnectionNotificationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ConnectionNotification {
    var list: std.ArrayList(ConnectionNotification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeConnectionNotification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCoreCountList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCpuManufacturerSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CpuManufacturer {
    var list: std.ArrayList(CpuManufacturer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (CpuManufacturer.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCreateFleetErrorsSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CreateFleetError {
    var list: std.ArrayList(CreateFleetError) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCreateFleetError(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCreateFleetInstancesSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CreateFleetInstance {
    var list: std.ArrayList(CreateFleetInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCreateFleetInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCreateVolumePermissionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CreateVolumePermission {
    var list: std.ArrayList(CreateVolumePermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCreateVolumePermission(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeCustomerGatewayList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CustomerGateway {
    var list: std.ArrayList(CustomerGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCustomerGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDataResponses(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DataResponse {
    var list: std.ArrayList(DataResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDataResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeclarativePoliciesReportList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DeclarativePoliciesReport {
    var list: std.ArrayList(DeclarativePoliciesReport) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDeclarativePoliciesReport(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDedicatedHostIdList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeleteFleetErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DeleteFleetErrorItem {
    var list: std.ArrayList(DeleteFleetErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDeleteFleetErrorItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeleteFleetSuccessSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DeleteFleetSuccessItem {
    var list: std.ArrayList(DeleteFleetSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDeleteFleetSuccessItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DeleteLaunchTemplateVersionsResponseErrorItem {
    var list: std.ArrayList(DeleteLaunchTemplateVersionsResponseErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDeleteLaunchTemplateVersionsResponseErrorItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseSuccessSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DeleteLaunchTemplateVersionsResponseSuccessItem {
    var list: std.ArrayList(DeleteLaunchTemplateVersionsResponseSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDeleteLaunchTemplateVersionsResponseSuccessItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeleteSnapshotResultSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DeleteSnapshotReturnCode {
    var list: std.ArrayList(DeleteSnapshotReturnCode) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDeleteSnapshotReturnCode(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeprovisionedAddressSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDescribeConversionTaskList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ConversionTask {
    var list: std.ArrayList(ConversionTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeConversionTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDescribeFastLaunchImagesSuccessSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DescribeFastLaunchImagesSuccessItem {
    var list: std.ArrayList(DescribeFastLaunchImagesSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDescribeFastLaunchImagesSuccessItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDescribeFastSnapshotRestoreSuccessSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DescribeFastSnapshotRestoreSuccessItem {
    var list: std.ArrayList(DescribeFastSnapshotRestoreSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDescribeFastSnapshotRestoreSuccessItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDescribeFleetsErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DescribeFleetError {
    var list: std.ArrayList(DescribeFleetError) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDescribeFleetError(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDescribeFleetsInstancesSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DescribeFleetsInstances {
    var list: std.ArrayList(DescribeFleetsInstances) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDescribeFleetsInstances(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDeviceTrustProviderTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DeviceTrustProviderType {
    var list: std.ArrayList(DeviceTrustProviderType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (DeviceTrustProviderType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDhcpConfigurationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DhcpConfiguration {
    var list: std.ArrayList(DhcpConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDhcpConfiguration(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDhcpConfigurationValueList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const AttributeValue {
    var list: std.ArrayList(AttributeValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeAttributeValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDhcpOptionsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DhcpOptions {
    var list: std.ArrayList(DhcpOptions) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDhcpOptions(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDisableFastSnapshotRestoreErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DisableFastSnapshotRestoreErrorItem {
    var list: std.ArrayList(DisableFastSnapshotRestoreErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDisableFastSnapshotRestoreErrorItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDisableFastSnapshotRestoreStateErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DisableFastSnapshotRestoreStateErrorItem {
    var list: std.ArrayList(DisableFastSnapshotRestoreStateErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDisableFastSnapshotRestoreStateErrorItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDisableFastSnapshotRestoreSuccessSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DisableFastSnapshotRestoreSuccessItem {
    var list: std.ArrayList(DisableFastSnapshotRestoreSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDisableFastSnapshotRestoreSuccessItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDiskInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DiskInfo {
    var list: std.ArrayList(DiskInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDiskInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeDnsEntrySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const DnsEntry {
    var list: std.ArrayList(DnsEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeDnsEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeEbsCardInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const EbsCardInfo {
    var list: std.ArrayList(EbsCardInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeEbsCardInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeEbsStatusDetailsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const EbsStatusDetails {
    var list: std.ArrayList(EbsStatusDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeEbsStatusDetails(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeEgressOnlyInternetGatewayList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const EgressOnlyInternetGateway {
    var list: std.ArrayList(EgressOnlyInternetGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeEgressOnlyInternetGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeElasticGpuAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ElasticGpuAssociation {
    var list: std.ArrayList(ElasticGpuAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeElasticGpuAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeElasticGpuSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ElasticGpus {
    var list: std.ArrayList(ElasticGpus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeElasticGpus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeElasticGpuSpecificationResponseList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ElasticGpuSpecificationResponse {
    var list: std.ArrayList(ElasticGpuSpecificationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeElasticGpuSpecificationResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeElasticInferenceAcceleratorAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ElasticInferenceAcceleratorAssociation {
    var list: std.ArrayList(ElasticInferenceAcceleratorAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeElasticInferenceAcceleratorAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeEnableFastSnapshotRestoreErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const EnableFastSnapshotRestoreErrorItem {
    var list: std.ArrayList(EnableFastSnapshotRestoreErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeEnableFastSnapshotRestoreErrorItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeEnableFastSnapshotRestoreStateErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const EnableFastSnapshotRestoreStateErrorItem {
    var list: std.ArrayList(EnableFastSnapshotRestoreStateErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeEnableFastSnapshotRestoreStateErrorItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeEnableFastSnapshotRestoreSuccessSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const EnableFastSnapshotRestoreSuccessItem {
    var list: std.ArrayList(EnableFastSnapshotRestoreSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeEnableFastSnapshotRestoreSuccessItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeEndpointSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ClientVpnEndpoint {
    var list: std.ArrayList(ClientVpnEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeClientVpnEndpoint(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeErrorSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ValidationError {
    var list: std.ArrayList(ValidationError) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeValidationError(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeExcludedInstanceTypeSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeExplanationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Explanation {
    var list: std.ArrayList(Explanation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeExplanation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeExportImageTaskList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ExportImageTask {
    var list: std.ArrayList(ExportImageTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeExportImageTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeExportTaskList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ExportTask {
    var list: std.ArrayList(ExportTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeExportTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFailedCapacityReservationFleetCancellationResultSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FailedCapacityReservationFleetCancellationResult {
    var list: std.ArrayList(FailedCapacityReservationFleetCancellationResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFailedCapacityReservationFleetCancellationResult(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFailedQueuedPurchaseDeletionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FailedQueuedPurchaseDeletion {
    var list: std.ArrayList(FailedQueuedPurchaseDeletion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFailedQueuedPurchaseDeletion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFleetCapacityReservationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FleetCapacityReservation {
    var list: std.ArrayList(FleetCapacityReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFleetCapacityReservation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFleetLaunchTemplateConfigList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FleetLaunchTemplateConfig {
    var list: std.ArrayList(FleetLaunchTemplateConfig) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFleetLaunchTemplateConfig(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFleetLaunchTemplateOverridesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FleetLaunchTemplateOverrides {
    var list: std.ArrayList(FleetLaunchTemplateOverrides) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFleetLaunchTemplateOverrides(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFleetSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FleetData {
    var list: std.ArrayList(FleetData) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFleetData(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFlowLogSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FlowLog {
    var list: std.ArrayList(FlowLog) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFlowLog(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFpgaDeviceInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FpgaDeviceInfo {
    var list: std.ArrayList(FpgaDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFpgaDeviceInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeFpgaImageList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const FpgaImage {
    var list: std.ArrayList(FpgaImage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeFpgaImage(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeGpuDeviceInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const GpuDeviceInfo {
    var list: std.ArrayList(GpuDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeGpuDeviceInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeGroupIdStringList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeGroupIdentifierList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const GroupIdentifier {
    var list: std.ArrayList(GroupIdentifier) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeGroupIdentifier(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeGroupIdentifierSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SecurityGroupIdentifier {
    var list: std.ArrayList(SecurityGroupIdentifier) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSecurityGroupIdentifier(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeHistoryRecordSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const HistoryRecordEntry {
    var list: std.ArrayList(HistoryRecordEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeHistoryRecordEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeHistoryRecords(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const HistoryRecord {
    var list: std.ArrayList(HistoryRecord) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeHistoryRecord(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeHostInstanceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const HostInstance {
    var list: std.ArrayList(HostInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeHostInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeHostList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Host {
    var list: std.ArrayList(Host) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeHost(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeHostOfferingSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const HostOffering {
    var list: std.ArrayList(HostOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeHostOffering(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeHostReservationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const HostReservation {
    var list: std.ArrayList(HostReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeHostReservation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIKEVersionsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IKEVersionsListValue {
    var list: std.ArrayList(IKEVersionsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIKEVersionsListValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIamInstanceProfileAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IamInstanceProfileAssociation {
    var list: std.ArrayList(IamInstanceProfileAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIamInstanceProfileAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIdFormatList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IdFormat {
    var list: std.ArrayList(IdFormat) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIdFormat(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageAncestryEntryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageAncestryEntry {
    var list: std.ArrayList(ImageAncestryEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageAncestryEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageCriterionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageCriterion {
    var list: std.ArrayList(ImageCriterion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageCriterion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Image {
    var list: std.ArrayList(Image) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImage(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageNameList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageProviderList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageRecycleBinInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageRecycleBinInfo {
    var list: std.ArrayList(ImageRecycleBinInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageRecycleBinInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageReferenceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageReference {
    var list: std.ArrayList(ImageReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageReference(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageUsageReportEntryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageUsageReportEntry {
    var list: std.ArrayList(ImageUsageReportEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageUsageReportEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageUsageReportList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageUsageReport {
    var list: std.ArrayList(ImageUsageReport) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageUsageReport(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageUsageResourceTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageUsageResourceType {
    var list: std.ArrayList(ImageUsageResourceType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageUsageResourceType(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageUsageResourceTypeOptionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImageUsageResourceTypeOption {
    var list: std.ArrayList(ImageUsageResourceTypeOption) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImageUsageResourceTypeOption(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImageUsageResourceTypeOptionValuesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImportImageLicenseSpecificationListResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImportImageLicenseConfigurationResponse {
    var list: std.ArrayList(ImportImageLicenseConfigurationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImportImageLicenseConfigurationResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImportImageTaskList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImportImageTask {
    var list: std.ArrayList(ImportImageTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImportImageTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImportInstanceVolumeDetailSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImportInstanceVolumeDetailItem {
    var list: std.ArrayList(ImportInstanceVolumeDetailItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImportInstanceVolumeDetailItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeImportSnapshotTaskList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ImportSnapshotTask {
    var list: std.ArrayList(ImportSnapshotTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeImportSnapshotTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInferenceDeviceInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InferenceDeviceInfo {
    var list: std.ArrayList(InferenceDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInferenceDeviceInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInsideCidrBlocksStringList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceBlockDeviceMappingList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceBlockDeviceMapping {
    var list: std.ArrayList(InstanceBlockDeviceMapping) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceBlockDeviceMapping(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceConnectEndpointSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ec2InstanceConnectEndpoint {
    var list: std.ArrayList(Ec2InstanceConnectEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeEc2InstanceConnectEndpoint(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceCountList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceCount {
    var list: std.ArrayList(InstanceCount) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceCount(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceCreditSpecificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceCreditSpecification {
    var list: std.ArrayList(InstanceCreditSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceCreditSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceEventWindowSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceEventWindow {
    var list: std.ArrayList(InstanceEventWindow) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceEventWindow(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceEventWindowTimeRangeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceEventWindowTimeRange {
    var list: std.ArrayList(InstanceEventWindowTimeRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceEventWindowTimeRange(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceGenerationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceGeneration {
    var list: std.ArrayList(InstanceGeneration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (InstanceGeneration.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceIdList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceIdsSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceImageMetadataList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceImageMetadata {
    var list: std.ArrayList(InstanceImageMetadata) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceImageMetadata(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceIpv4PrefixList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceIpv4Prefix {
    var list: std.ArrayList(InstanceIpv4Prefix) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceIpv4Prefix(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceIpv6AddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceIpv6Address {
    var list: std.ArrayList(InstanceIpv6Address) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceIpv6Address(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceIpv6PrefixList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceIpv6Prefix {
    var list: std.ArrayList(InstanceIpv6Prefix) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceIpv6Prefix(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Instance {
    var list: std.ArrayList(Instance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceMonitoringList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceMonitoring {
    var list: std.ArrayList(InstanceMonitoring) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceMonitoring(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceNetworkInterfaceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceNetworkInterface {
    var list: std.ArrayList(InstanceNetworkInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceNetworkInterface(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceNetworkInterfaceSpecificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceNetworkInterfaceSpecification {
    var list: std.ArrayList(InstanceNetworkInterfaceSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceNetworkInterfaceSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstancePrivateIpAddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstancePrivateIpAddress {
    var list: std.ArrayList(InstancePrivateIpAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstancePrivateIpAddress(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceTopology {
    var list: std.ArrayList(InstanceTopology) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceTopology(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceStateChangeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceStateChange {
    var list: std.ArrayList(InstanceStateChange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceStateChange(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceStatusDetailsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceStatusDetails {
    var list: std.ArrayList(InstanceStatusDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceStatusDetails(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceStatusEventList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceStatusEvent {
    var list: std.ArrayList(InstanceStatusEvent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceStatusEvent(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceStatusList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceStatus {
    var list: std.ArrayList(InstanceStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceStatus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceTagKeySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceTypeInfoFromInstanceRequirementsSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceTypeInfoFromInstanceRequirements {
    var list: std.ArrayList(InstanceTypeInfoFromInstanceRequirements) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceTypeInfoFromInstanceRequirements(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceTypeInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceTypeInfo {
    var list: std.ArrayList(InstanceTypeInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceTypeInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceTypeOfferingsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceTypeOffering {
    var list: std.ArrayList(InstanceTypeOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceTypeOffering(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceTypesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInstanceUsageSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InstanceUsage {
    var list: std.ArrayList(InstanceUsage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInstanceUsage(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInternetGatewayAttachmentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InternetGatewayAttachment {
    var list: std.ArrayList(InternetGatewayAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInternetGatewayAttachment(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeInternetGatewayList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const InternetGateway {
    var list: std.ArrayList(InternetGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeInternetGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpAddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpPermissionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpPermission {
    var list: std.ArrayList(IpPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpPermission(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpPrefixList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpRangeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpRange {
    var list: std.ArrayList(IpRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpRange(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpRanges(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamAddressHistoryRecordSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamAddressHistoryRecord {
    var list: std.ArrayList(IpamAddressHistoryRecord) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamAddressHistoryRecord(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamDiscoveredAccountSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamDiscoveredAccount {
    var list: std.ArrayList(IpamDiscoveredAccount) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamDiscoveredAccount(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamDiscoveredPublicAddressSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamDiscoveredPublicAddress {
    var list: std.ArrayList(IpamDiscoveredPublicAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamDiscoveredPublicAddress(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamDiscoveredResourceCidrSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamDiscoveredResourceCidr {
    var list: std.ArrayList(IpamDiscoveredResourceCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamDiscoveredResourceCidr(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamExternalResourceVerificationTokenSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamExternalResourceVerificationToken {
    var list: std.ArrayList(IpamExternalResourceVerificationToken) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamExternalResourceVerificationToken(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamOperatingRegionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamOperatingRegion {
    var list: std.ArrayList(IpamOperatingRegion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamOperatingRegion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamOrganizationalUnitExclusionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamOrganizationalUnitExclusion {
    var list: std.ArrayList(IpamOrganizationalUnitExclusion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamOrganizationalUnitExclusion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPolicyAllocationRuleList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPolicyAllocationRule {
    var list: std.ArrayList(IpamPolicyAllocationRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPolicyAllocationRule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPolicyDocumentSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPolicyDocument {
    var list: std.ArrayList(IpamPolicyDocument) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPolicyDocument(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPolicyOrganizationTargetSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPolicyOrganizationTarget {
    var list: std.ArrayList(IpamPolicyOrganizationTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPolicyOrganizationTarget(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPolicySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPolicy {
    var list: std.ArrayList(IpamPolicy) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPolicy(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPoolAllocationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPoolAllocation {
    var list: std.ArrayList(IpamPoolAllocation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPoolAllocation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPoolCidrSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPoolCidr {
    var list: std.ArrayList(IpamPoolCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPoolCidr(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPoolSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPool {
    var list: std.ArrayList(IpamPool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPool(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPrefixListResolverRuleConditionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPrefixListResolverRuleCondition {
    var list: std.ArrayList(IpamPrefixListResolverRuleCondition) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPrefixListResolverRuleCondition(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPrefixListResolverRuleSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPrefixListResolverRule {
    var list: std.ArrayList(IpamPrefixListResolverRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPrefixListResolverRule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPrefixListResolverSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPrefixListResolver {
    var list: std.ArrayList(IpamPrefixListResolver) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPrefixListResolver(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPrefixListResolverTargetSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPrefixListResolverTarget {
    var list: std.ArrayList(IpamPrefixListResolverTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPrefixListResolverTarget(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPrefixListResolverVersionEntrySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPrefixListResolverVersionEntry {
    var list: std.ArrayList(IpamPrefixListResolverVersionEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPrefixListResolverVersionEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPrefixListResolverVersionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPrefixListResolverVersion {
    var list: std.ArrayList(IpamPrefixListResolverVersion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPrefixListResolverVersion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPublicAddressSecurityGroupList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPublicAddressSecurityGroup {
    var list: std.ArrayList(IpamPublicAddressSecurityGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPublicAddressSecurityGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamPublicAddressTagList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamPublicAddressTag {
    var list: std.ArrayList(IpamPublicAddressTag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamPublicAddressTag(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamResourceCidrSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamResourceCidr {
    var list: std.ArrayList(IpamResourceCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamResourceCidr(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamResourceDiscoveryAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamResourceDiscoveryAssociation {
    var list: std.ArrayList(IpamResourceDiscoveryAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamResourceDiscoveryAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamResourceDiscoverySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamResourceDiscovery {
    var list: std.ArrayList(IpamResourceDiscovery) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamResourceDiscovery(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamResourceTagList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamResourceTag {
    var list: std.ArrayList(IpamResourceTag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamResourceTag(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamScopeSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const IpamScope {
    var list: std.ArrayList(IpamScope) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpamScope(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpamSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipam {
    var list: std.ArrayList(Ipam) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpam(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv4PrefixList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv4PrefixSpecificationRequest {
    var list: std.ArrayList(Ipv4PrefixSpecificationRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv4PrefixSpecificationRequest(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv4PrefixListResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv4PrefixSpecificationResponse {
    var list: std.ArrayList(Ipv4PrefixSpecificationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv4PrefixSpecificationResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv4PrefixesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv4PrefixSpecification {
    var list: std.ArrayList(Ipv4PrefixSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv4PrefixSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6AddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6CidrAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv6CidrAssociation {
    var list: std.ArrayList(Ipv6CidrAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv6CidrAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6CidrBlockSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv6CidrBlock {
    var list: std.ArrayList(Ipv6CidrBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv6CidrBlock(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6PoolSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv6Pool {
    var list: std.ArrayList(Ipv6Pool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv6Pool(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6PrefixList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv6PrefixSpecificationRequest {
    var list: std.ArrayList(Ipv6PrefixSpecificationRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv6PrefixSpecificationRequest(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6PrefixListResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv6PrefixSpecificationResponse {
    var list: std.ArrayList(Ipv6PrefixSpecificationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv6PrefixSpecificationResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6PrefixesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv6PrefixSpecification {
    var list: std.ArrayList(Ipv6PrefixSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv6PrefixSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeIpv6RangeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Ipv6Range {
    var list: std.ArrayList(Ipv6Range) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeIpv6Range(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeKeyPairList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const KeyPairInfo {
    var list: std.ArrayList(KeyPairInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeKeyPairInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchPermissionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchPermission {
    var list: std.ArrayList(LaunchPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchPermission(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchSpecsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SpotFleetLaunchSpecification {
    var list: std.ArrayList(SpotFleetLaunchSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSpotFleetLaunchSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateBlockDeviceMappingList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateBlockDeviceMapping {
    var list: std.ArrayList(LaunchTemplateBlockDeviceMapping) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateBlockDeviceMapping(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateConfigList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateConfig {
    var list: std.ArrayList(LaunchTemplateConfig) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateConfig(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateElasticInferenceAcceleratorResponseList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateElasticInferenceAcceleratorResponse {
    var list: std.ArrayList(LaunchTemplateElasticInferenceAcceleratorResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateElasticInferenceAcceleratorResponse(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateInstanceNetworkInterfaceSpecificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateInstanceNetworkInterfaceSpecification {
    var list: std.ArrayList(LaunchTemplateInstanceNetworkInterfaceSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateInstanceNetworkInterfaceSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateLicenseList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateLicenseConfiguration {
    var list: std.ArrayList(LaunchTemplateLicenseConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateLicenseConfiguration(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateOverridesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateOverrides {
    var list: std.ArrayList(LaunchTemplateOverrides) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateOverrides(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplate {
    var list: std.ArrayList(LaunchTemplate) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplate(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateTagSpecificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateTagSpecification {
    var list: std.ArrayList(LaunchTemplateTagSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateTagSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLaunchTemplateVersionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LaunchTemplateVersion {
    var list: std.ArrayList(LaunchTemplateVersion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLaunchTemplateVersion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLicenseList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LicenseConfiguration {
    var list: std.ArrayList(LicenseConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLicenseConfiguration(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLoadPermissionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LoadPermission {
    var list: std.ArrayList(LoadPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLoadPermission(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewayRouteList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalGatewayRoute {
    var list: std.ArrayList(LocalGatewayRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLocalGatewayRoute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewayRouteTableSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalGatewayRouteTable {
    var list: std.ArrayList(LocalGatewayRouteTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLocalGatewayRouteTable(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewayRouteTableVirtualInterfaceGroupAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalGatewayRouteTableVirtualInterfaceGroupAssociation {
    var list: std.ArrayList(LocalGatewayRouteTableVirtualInterfaceGroupAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLocalGatewayRouteTableVirtualInterfaceGroupAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewayRouteTableVpcAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalGatewayRouteTableVpcAssociation {
    var list: std.ArrayList(LocalGatewayRouteTableVpcAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLocalGatewayRouteTableVpcAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewaySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalGateway {
    var list: std.ArrayList(LocalGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLocalGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewayVirtualInterfaceGroupSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalGatewayVirtualInterfaceGroup {
    var list: std.ArrayList(LocalGatewayVirtualInterfaceGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLocalGatewayVirtualInterfaceGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewayVirtualInterfaceIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalGatewayVirtualInterfaceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalGatewayVirtualInterface {
    var list: std.ArrayList(LocalGatewayVirtualInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLocalGatewayVirtualInterface(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLocalStorageTypeSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LocalStorageType {
    var list: std.ArrayList(LocalStorageType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (LocalStorageType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeLockedSnapshotsInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const LockedSnapshotsInfo {
    var list: std.ArrayList(LockedSnapshotsInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeLockedSnapshotsInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMacHostList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const MacHost {
    var list: std.ArrayList(MacHost) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeMacHost(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMacModificationTaskList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const MacModificationTask {
    var list: std.ArrayList(MacModificationTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeMacModificationTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMacOSVersionStringList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeManagedPrefixListSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ManagedPrefixList {
    var list: std.ArrayList(ManagedPrefixList) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeManagedPrefixList(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMarketplaceProductCodeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMediaDeviceInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const MediaDeviceInfo {
    var list: std.ArrayList(MediaDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeMediaDeviceInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMetricDataResultSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const MetricDataResult {
    var list: std.ArrayList(MetricDataResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeMetricDataResult(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMetricDimensionResultSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const CapacityManagerDimension {
    var list: std.ArrayList(CapacityManagerDimension) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeCapacityManagerDimension(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMetricPoints(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const MetricPoint {
    var list: std.ArrayList(MetricPoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeMetricPoint(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMetricValueSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const MetricValue {
    var list: std.ArrayList(MetricValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeMetricValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeMovingAddressStatusSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const MovingAddressStatus {
    var list: std.ArrayList(MovingAddressStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeMovingAddressStatus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNatGatewayAddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NatGatewayAddress {
    var list: std.ArrayList(NatGatewayAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNatGatewayAddress(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNatGatewayAttachedApplianceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NatGatewayAttachedAppliance {
    var list: std.ArrayList(NatGatewayAttachedAppliance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNatGatewayAttachedAppliance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNatGatewayList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NatGateway {
    var list: std.ArrayList(NatGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNatGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkAclAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkAclAssociation {
    var list: std.ArrayList(NetworkAclAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkAclAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkAclEntryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkAclEntry {
    var list: std.ArrayList(NetworkAclEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkAclEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkAclList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkAcl {
    var list: std.ArrayList(NetworkAcl) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkAcl(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkCardInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkCardInfo {
    var list: std.ArrayList(NetworkCardInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkCardInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInsightsAccessScopeAnalysisList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInsightsAccessScopeAnalysis {
    var list: std.ArrayList(NetworkInsightsAccessScopeAnalysis) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInsightsAccessScopeAnalysis(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInsightsAccessScopeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInsightsAccessScope {
    var list: std.ArrayList(NetworkInsightsAccessScope) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInsightsAccessScope(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInsightsAnalysisList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInsightsAnalysis {
    var list: std.ArrayList(NetworkInsightsAnalysis) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInsightsAnalysis(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInsightsPathList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInsightsPath {
    var list: std.ArrayList(NetworkInsightsPath) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInsightsPath(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInterfaceIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInterfaceIpv6AddressesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInterfaceIpv6Address {
    var list: std.ArrayList(NetworkInterfaceIpv6Address) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInterfaceIpv6Address(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInterfaceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInterface {
    var list: std.ArrayList(NetworkInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInterface(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInterfacePermissionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInterfacePermission {
    var list: std.ArrayList(NetworkInterfacePermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInterfacePermission(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkInterfacePrivateIpAddressList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NetworkInterfacePrivateIpAddress {
    var list: std.ArrayList(NetworkInterfacePrivateIpAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNetworkInterfacePrivateIpAddress(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkNodeSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNetworkNodesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNeuronDeviceInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const NeuronDeviceInfo {
    var list: std.ArrayList(NeuronDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeNeuronDeviceInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeNitroTpmSupportedVersionsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeOccurrenceDaySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeOutpostLagSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const OutpostLag {
    var list: std.ArrayList(OutpostLag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeOutpostLag(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePathComponentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PathComponent {
    var list: std.ArrayList(PathComponent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePathComponent(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePerformanceFactorReferenceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PerformanceFactorReference {
    var list: std.ArrayList(PerformanceFactorReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePerformanceFactorReference(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePhase1DHGroupNumbersList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Phase1DHGroupNumbersListValue {
    var list: std.ArrayList(Phase1DHGroupNumbersListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePhase1DHGroupNumbersListValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePhase1EncryptionAlgorithmsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Phase1EncryptionAlgorithmsListValue {
    var list: std.ArrayList(Phase1EncryptionAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePhase1EncryptionAlgorithmsListValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePhase1IntegrityAlgorithmsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Phase1IntegrityAlgorithmsListValue {
    var list: std.ArrayList(Phase1IntegrityAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePhase1IntegrityAlgorithmsListValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePhase2DHGroupNumbersList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Phase2DHGroupNumbersListValue {
    var list: std.ArrayList(Phase2DHGroupNumbersListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePhase2DHGroupNumbersListValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePhase2EncryptionAlgorithmsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Phase2EncryptionAlgorithmsListValue {
    var list: std.ArrayList(Phase2EncryptionAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePhase2EncryptionAlgorithmsListValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePhase2IntegrityAlgorithmsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Phase2IntegrityAlgorithmsListValue {
    var list: std.ArrayList(Phase2IntegrityAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePhase2IntegrityAlgorithmsListValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePlacementGroupList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PlacementGroup {
    var list: std.ArrayList(PlacementGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePlacementGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePlacementGroupStrategyList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PlacementGroupStrategy {
    var list: std.ArrayList(PlacementGroupStrategy) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (PlacementGroupStrategy.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePoolCidrBlocksSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PoolCidrBlock {
    var list: std.ArrayList(PoolCidrBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePoolCidrBlock(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePortRangeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PortRange {
    var list: std.ArrayList(PortRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePortRange(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrefixListAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PrefixListAssociation {
    var list: std.ArrayList(PrefixListAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePrefixListAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrefixListEntrySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PrefixListEntry {
    var list: std.ArrayList(PrefixListEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePrefixListEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrefixListIdList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PrefixListId {
    var list: std.ArrayList(PrefixListId) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePrefixListId(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrefixListIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrefixListSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PrefixList {
    var list: std.ArrayList(PrefixList) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePrefixList(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePriceScheduleList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PriceSchedule {
    var list: std.ArrayList(PriceSchedule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePriceSchedule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePricingDetailsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PricingDetail {
    var list: std.ArrayList(PricingDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePricingDetail(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrincipalIdFormatList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PrincipalIdFormat {
    var list: std.ArrayList(PrincipalIdFormat) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePrincipalIdFormat(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrivateDnsDetailsSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PrivateDnsDetails {
    var list: std.ArrayList(PrivateDnsDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePrivateDnsDetails(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrivateDnsSpecifiedDomainSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePrivateIpAddressSpecificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PrivateIpAddressSpecification {
    var list: std.ArrayList(PrivateIpAddressSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePrivateIpAddressSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeProductCodeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ProductCode {
    var list: std.ArrayList(ProductCode) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeProductCode(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePropagatingVgwList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PropagatingVgw {
    var list: std.ArrayList(PropagatingVgw) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePropagatingVgw(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeProtocolIntList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeProtocolList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Protocol {
    var list: std.ArrayList(Protocol) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (Protocol.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePublicIpv4PoolRangeSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PublicIpv4PoolRange {
    var list: std.ArrayList(PublicIpv4PoolRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePublicIpv4PoolRange(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePublicIpv4PoolSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const PublicIpv4Pool {
    var list: std.ArrayList(PublicIpv4Pool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePublicIpv4Pool(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePurchaseSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Purchase {
    var list: std.ArrayList(Purchase) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializePurchase(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializePurchasedScheduledInstanceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ScheduledInstance {
    var list: std.ArrayList(ScheduledInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeScheduledInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRecurringChargesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RecurringCharge {
    var list: std.ArrayList(RecurringCharge) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRecurringCharge(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRegionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Region {
    var list: std.ArrayList(Region) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRegion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRegionalSummaryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RegionalSummary {
    var list: std.ArrayList(RegionalSummary) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRegionalSummary(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRegisteredInstanceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RegisteredInstance {
    var list: std.ArrayList(RegisteredInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRegisteredInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReplaceRootVolumeTasks(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReplaceRootVolumeTask {
    var list: std.ArrayList(ReplaceRootVolumeTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReplaceRootVolumeTask(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Reservation {
    var list: std.ArrayList(Reservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservedInstanceReservationValueSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReservedInstanceReservationValue {
    var list: std.ArrayList(ReservedInstanceReservationValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservedInstanceReservationValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservedInstancesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReservedInstances {
    var list: std.ArrayList(ReservedInstances) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservedInstances(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservedInstancesListingList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReservedInstancesListing {
    var list: std.ArrayList(ReservedInstancesListing) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservedInstancesListing(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservedInstancesModificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReservedInstancesModification {
    var list: std.ArrayList(ReservedInstancesModification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservedInstancesModification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservedInstancesModificationResultList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReservedInstancesModificationResult {
    var list: std.ArrayList(ReservedInstancesModificationResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservedInstancesModificationResult(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservedInstancesOfferingList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReservedInstancesOffering {
    var list: std.ArrayList(ReservedInstancesOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservedInstancesOffering(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeReservedIntancesIds(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ReservedInstancesId {
    var list: std.ArrayList(ReservedInstancesId) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeReservedInstancesId(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeResponseHostIdList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeResponseHostIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRevokedSecurityGroupRuleList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RevokedSecurityGroupRule {
    var list: std.ArrayList(RevokedSecurityGroupRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRevokedSecurityGroupRule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRootDeviceTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RootDeviceType {
    var list: std.ArrayList(RootDeviceType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (RootDeviceType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Route {
    var list: std.ArrayList(Route) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRoute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteServerAssociationsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteServerAssociation {
    var list: std.ArrayList(RouteServerAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteServerAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteServerEndpointsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteServerEndpoint {
    var list: std.ArrayList(RouteServerEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteServerEndpoint(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteServerPeersList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteServerPeer {
    var list: std.ArrayList(RouteServerPeer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteServerPeer(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteServerPropagationsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteServerPropagation {
    var list: std.ArrayList(RouteServerPropagation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteServerPropagation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteServerRouteInstallationDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteServerRouteInstallationDetail {
    var list: std.ArrayList(RouteServerRouteInstallationDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteServerRouteInstallationDetail(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteServerRouteList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteServerRoute {
    var list: std.ArrayList(RouteServerRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteServerRoute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteServersList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteServer {
    var list: std.ArrayList(RouteServer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteServer(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteTableAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteTableAssociation {
    var list: std.ArrayList(RouteTableAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteTableAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRouteTableList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RouteTable {
    var list: std.ArrayList(RouteTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRouteTable(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRuleGroupRuleOptionsPairList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RuleGroupRuleOptionsPair {
    var list: std.ArrayList(RuleGroupRuleOptionsPair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRuleGroupRuleOptionsPair(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRuleGroupTypePairList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RuleGroupTypePair {
    var list: std.ArrayList(RuleGroupTypePair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRuleGroupTypePair(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeRuleOptionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const RuleOption {
    var list: std.ArrayList(RuleOption) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeRuleOption(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeScheduledInstanceAvailabilitySet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ScheduledInstanceAvailability {
    var list: std.ArrayList(ScheduledInstanceAvailability) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeScheduledInstanceAvailability(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeScheduledInstanceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ScheduledInstance {
    var list: std.ArrayList(ScheduledInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeScheduledInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupForVpcList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SecurityGroupForVpc {
    var list: std.ArrayList(SecurityGroupForVpc) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSecurityGroupForVpc(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupIdList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupIdStringList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SecurityGroup {
    var list: std.ArrayList(SecurityGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSecurityGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupReferences(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SecurityGroupReference {
    var list: std.ArrayList(SecurityGroupReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSecurityGroupReference(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupRuleList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SecurityGroupRule {
    var list: std.ArrayList(SecurityGroupRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSecurityGroupRule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSecurityGroupVpcAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SecurityGroupVpcAssociation {
    var list: std.ArrayList(SecurityGroupVpcAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSecurityGroupVpcAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeServiceConfigurationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ServiceConfiguration {
    var list: std.ArrayList(ServiceConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeServiceConfiguration(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeServiceDetailSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ServiceDetail {
    var list: std.ArrayList(ServiceDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeServiceDetail(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeServiceLinkVirtualInterfaceIdSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeServiceLinkVirtualInterfaceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ServiceLinkVirtualInterface {
    var list: std.ArrayList(ServiceLinkVirtualInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeServiceLinkVirtualInterface(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeServiceTypeDetailSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ServiceTypeDetail {
    var list: std.ArrayList(ServiceTypeDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeServiceTypeDetail(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSnapshotDetailList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SnapshotDetail {
    var list: std.ArrayList(SnapshotDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSnapshotDetail(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSnapshotList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Snapshot {
    var list: std.ArrayList(Snapshot) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSnapshot(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSnapshotRecycleBinInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SnapshotRecycleBinInfo {
    var list: std.ArrayList(SnapshotRecycleBinInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSnapshotRecycleBinInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSnapshotSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SnapshotInfo {
    var list: std.ArrayList(SnapshotInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSnapshotInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSpotFleetRequestConfigSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SpotFleetRequestConfig {
    var list: std.ArrayList(SpotFleetRequestConfig) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSpotFleetRequestConfig(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSpotFleetTagSpecificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SpotFleetTagSpecification {
    var list: std.ArrayList(SpotFleetTagSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSpotFleetTagSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSpotInstanceRequestList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SpotInstanceRequest {
    var list: std.ArrayList(SpotInstanceRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSpotInstanceRequest(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSpotPlacementScores(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SpotPlacementScore {
    var list: std.ArrayList(SpotPlacementScore) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSpotPlacementScore(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSpotPriceHistoryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SpotPrice {
    var list: std.ArrayList(SpotPrice) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSpotPrice(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeStaleIpPermissionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const StaleIpPermission {
    var list: std.ArrayList(StaleIpPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeStaleIpPermission(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeStaleSecurityGroupSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const StaleSecurityGroup {
    var list: std.ArrayList(StaleSecurityGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeStaleSecurityGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeStoreImageTaskResultSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const StoreImageTaskResult {
    var list: std.ArrayList(StoreImageTaskResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeStoreImageTaskResult(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeStringList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSubnetAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SubnetAssociation {
    var list: std.ArrayList(SubnetAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSubnetAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSubnetCidrReservationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SubnetCidrReservation {
    var list: std.ArrayList(SubnetCidrReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSubnetCidrReservation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSubnetIpPrefixesList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SubnetIpPrefixes {
    var list: std.ArrayList(SubnetIpPrefixes) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSubnetIpPrefixes(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSubnetIpv6CidrBlockAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SubnetIpv6CidrBlockAssociation {
    var list: std.ArrayList(SubnetIpv6CidrBlockAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSubnetIpv6CidrBlockAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSubnetList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Subnet {
    var list: std.ArrayList(Subnet) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSubnet(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSubscriptionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Subscription {
    var list: std.ArrayList(Subscription) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSubscription(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSuccessfulInstanceCreditSpecificationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SuccessfulInstanceCreditSpecificationItem {
    var list: std.ArrayList(SuccessfulInstanceCreditSpecificationItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSuccessfulInstanceCreditSpecificationItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSuccessfulQueuedPurchaseDeletionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SuccessfulQueuedPurchaseDeletion {
    var list: std.ArrayList(SuccessfulQueuedPurchaseDeletion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSuccessfulQueuedPurchaseDeletion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSupportedAdditionalProcessorFeatureList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SupportedAdditionalProcessorFeature {
    var list: std.ArrayList(SupportedAdditionalProcessorFeature) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (SupportedAdditionalProcessorFeature.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSupportedIpAddressTypes(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ServiceConnectivityType {
    var list: std.ArrayList(ServiceConnectivityType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (ServiceConnectivityType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeSupportedRegionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SupportedRegionDetail {
    var list: std.ArrayList(SupportedRegionDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSupportedRegionDetail(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTagDescriptionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TagDescription {
    var list: std.ArrayList(TagDescription) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTagDescription(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTagList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Tag {
    var list: std.ArrayList(Tag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTag(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTagSpecificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TagSpecification {
    var list: std.ArrayList(TagSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTagSpecification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTargetGroups(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TargetGroup {
    var list: std.ArrayList(TargetGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTargetGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTargetNetworkSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TargetNetwork {
    var list: std.ArrayList(TargetNetwork) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTargetNetwork(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTargetReservationValueSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TargetReservationValue {
    var list: std.ArrayList(TargetReservationValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTargetReservationValue(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTerminateConnectionStatusSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TerminateConnectionStatus {
    var list: std.ArrayList(TerminateConnectionStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTerminateConnectionStatus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeThreadsPerCoreList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeThroughResourcesStatementList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const ThroughResourcesStatement {
    var list: std.ArrayList(ThroughResourcesStatement) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeThroughResourcesStatement(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTrafficMirrorFilterRuleList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TrafficMirrorFilterRule {
    var list: std.ArrayList(TrafficMirrorFilterRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTrafficMirrorFilterRule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTrafficMirrorFilterRuleSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TrafficMirrorFilterRule {
    var list: std.ArrayList(TrafficMirrorFilterRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTrafficMirrorFilterRule(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTrafficMirrorFilterSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TrafficMirrorFilter {
    var list: std.ArrayList(TrafficMirrorFilter) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTrafficMirrorFilter(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTrafficMirrorNetworkServiceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TrafficMirrorNetworkService {
    var list: std.ArrayList(TrafficMirrorNetworkService) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (TrafficMirrorNetworkService.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTrafficMirrorSessionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TrafficMirrorSession {
    var list: std.ArrayList(TrafficMirrorSession) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTrafficMirrorSession(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTrafficMirrorTargetSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TrafficMirrorTarget {
    var list: std.ArrayList(TrafficMirrorTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTrafficMirrorTarget(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayAttachmentBgpConfigurationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayAttachmentBgpConfiguration {
    var list: std.ArrayList(TransitGatewayAttachmentBgpConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayAttachmentBgpConfiguration(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayAttachmentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayAttachment {
    var list: std.ArrayList(TransitGatewayAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayAttachment(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayAttachmentPropagationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayAttachmentPropagation {
    var list: std.ArrayList(TransitGatewayAttachmentPropagation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayAttachmentPropagation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayConnectList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayConnect {
    var list: std.ArrayList(TransitGatewayConnect) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayConnect(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayConnectPeerList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayConnectPeer {
    var list: std.ArrayList(TransitGatewayConnectPeer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayConnectPeer(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGateway {
    var list: std.ArrayList(TransitGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayMeteringPolicyEntryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayMeteringPolicyEntry {
    var list: std.ArrayList(TransitGatewayMeteringPolicyEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayMeteringPolicyEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayMeteringPolicyList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayMeteringPolicy {
    var list: std.ArrayList(TransitGatewayMeteringPolicy) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayMeteringPolicy(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayMulticastDomainAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayMulticastDomainAssociation {
    var list: std.ArrayList(TransitGatewayMulticastDomainAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayMulticastDomainAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayMulticastDomainList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayMulticastDomain {
    var list: std.ArrayList(TransitGatewayMulticastDomain) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayMulticastDomain(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayMulticastGroupList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayMulticastGroup {
    var list: std.ArrayList(TransitGatewayMulticastGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayMulticastGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayPeeringAttachmentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayPeeringAttachment {
    var list: std.ArrayList(TransitGatewayPeeringAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayPeeringAttachment(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayPolicyTableAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayPolicyTableAssociation {
    var list: std.ArrayList(TransitGatewayPolicyTableAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayPolicyTableAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayPolicyTableEntryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayPolicyTableEntry {
    var list: std.ArrayList(TransitGatewayPolicyTableEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayPolicyTableEntry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayPolicyTableList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayPolicyTable {
    var list: std.ArrayList(TransitGatewayPolicyTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayPolicyTable(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayPrefixListReferenceSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayPrefixListReference {
    var list: std.ArrayList(TransitGatewayPrefixListReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayPrefixListReference(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayRouteAttachmentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayRouteAttachment {
    var list: std.ArrayList(TransitGatewayRouteAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayRouteAttachment(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayRouteList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayRoute {
    var list: std.ArrayList(TransitGatewayRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayRoute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayRouteTableAnnouncementList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayRouteTableAnnouncement {
    var list: std.ArrayList(TransitGatewayRouteTableAnnouncement) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayRouteTableAnnouncement(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayRouteTableAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayRouteTableAssociation {
    var list: std.ArrayList(TransitGatewayRouteTableAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayRouteTableAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayRouteTableList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayRouteTable {
    var list: std.ArrayList(TransitGatewayRouteTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayRouteTable(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayRouteTablePropagationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayRouteTablePropagation {
    var list: std.ArrayList(TransitGatewayRouteTablePropagation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayRouteTablePropagation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTransitGatewayVpcAttachmentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TransitGatewayVpcAttachment {
    var list: std.ArrayList(TransitGatewayVpcAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTransitGatewayVpcAttachment(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTrunkInterfaceAssociationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TrunkInterfaceAssociation {
    var list: std.ArrayList(TrunkInterfaceAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTrunkInterfaceAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeTunnelOptionsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const TunnelOption {
    var list: std.ArrayList(TunnelOption) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeTunnelOption(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeUnsuccessfulInstanceCreditSpecificationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const UnsuccessfulInstanceCreditSpecificationItem {
    var list: std.ArrayList(UnsuccessfulInstanceCreditSpecificationItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeUnsuccessfulInstanceCreditSpecificationItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeUnsuccessfulItemList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const UnsuccessfulItem {
    var list: std.ArrayList(UnsuccessfulItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeUnsuccessfulItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeUnsuccessfulItemSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const UnsuccessfulItem {
    var list: std.ArrayList(UnsuccessfulItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeUnsuccessfulItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeUsageClassTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const UsageClassType {
    var list: std.ArrayList(UsageClassType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (UsageClassType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeUserIdGroupPairList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const UserIdGroupPair {
    var list: std.ArrayList(UserIdGroupPair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeUserIdGroupPair(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeUserIdGroupPairSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const UserIdGroupPair {
    var list: std.ArrayList(UserIdGroupPair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeUserIdGroupPair(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeUserIdList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeValueStringList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessEndpointList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessEndpoint {
    var list: std.ArrayList(VerifiedAccessEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessEndpoint(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessEndpointPortRangeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessEndpointPortRange {
    var list: std.ArrayList(VerifiedAccessEndpointPortRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessEndpointPortRange(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessEndpointSubnetIdList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessEndpointTargetList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessEndpointTarget {
    var list: std.ArrayList(VerifiedAccessEndpointTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessEndpointTarget(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessGroupList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessGroup {
    var list: std.ArrayList(VerifiedAccessGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessGroup(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessInstanceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessInstance {
    var list: std.ArrayList(VerifiedAccessInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessInstance(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessInstanceLoggingConfigurationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessInstanceLoggingConfiguration {
    var list: std.ArrayList(VerifiedAccessInstanceLoggingConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessInstanceLoggingConfiguration(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfigurationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessInstanceOpenVpnClientConfiguration {
    var list: std.ArrayList(VerifiedAccessInstanceOpenVpnClientConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessInstanceOpenVpnClientConfiguration(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRouteList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessInstanceOpenVpnClientConfigurationRoute {
    var list: std.ArrayList(VerifiedAccessInstanceOpenVpnClientConfigurationRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRoute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessTrustProviderCondensedList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessTrustProviderCondensed {
    var list: std.ArrayList(VerifiedAccessTrustProviderCondensed) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessTrustProviderCondensed(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVerifiedAccessTrustProviderList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VerifiedAccessTrustProvider {
    var list: std.ArrayList(VerifiedAccessTrustProvider) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVerifiedAccessTrustProvider(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVgwTelemetryList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VgwTelemetry {
    var list: std.ArrayList(VgwTelemetry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVgwTelemetry(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVirtualizationTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VirtualizationType {
    var list: std.ArrayList(VirtualizationType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (VirtualizationType.fromWireName(try reader.readElementText())) |v| try list.append(allocator, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeAttachmentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeAttachment {
    var list: std.ArrayList(VolumeAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeAttachment(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Volume {
    var list: std.ArrayList(Volume) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolume(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeModificationList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeModification {
    var list: std.ArrayList(VolumeModification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeModification(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeRecycleBinInfoList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeRecycleBinInfo {
    var list: std.ArrayList(VolumeRecycleBinInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeRecycleBinInfo(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeStatusActionsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeStatusAction {
    var list: std.ArrayList(VolumeStatusAction) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeStatusAction(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeStatusAttachmentStatusList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeStatusAttachmentStatus {
    var list: std.ArrayList(VolumeStatusAttachmentStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeStatusAttachmentStatus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeStatusDetailsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeStatusDetails {
    var list: std.ArrayList(VolumeStatusDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeStatusDetails(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeStatusEventsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeStatusEvent {
    var list: std.ArrayList(VolumeStatusEvent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeStatusEvent(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVolumeStatusList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VolumeStatusItem {
    var list: std.ArrayList(VolumeStatusItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVolumeStatusItem(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcAttachmentList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcAttachment {
    var list: std.ArrayList(VpcAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcAttachment(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcBlockPublicAccessExclusionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcBlockPublicAccessExclusion {
    var list: std.ArrayList(VpcBlockPublicAccessExclusion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcBlockPublicAccessExclusion(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcCidrBlockAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcCidrBlockAssociation {
    var list: std.ArrayList(VpcCidrBlockAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcCidrBlockAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcClassicLinkList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcClassicLink {
    var list: std.ArrayList(VpcClassicLink) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcClassicLink(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcEncryptionControlList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcEncryptionControl {
    var list: std.ArrayList(VpcEncryptionControl) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcEncryptionControl(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcEncryptionNonCompliantResourceList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcEncryptionNonCompliantResource {
    var list: std.ArrayList(VpcEncryptionNonCompliantResource) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcEncryptionNonCompliantResource(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcEndpointAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcEndpointAssociation {
    var list: std.ArrayList(VpcEndpointAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcEndpointAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcEndpointConnectionSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcEndpointConnection {
    var list: std.ArrayList(VpcEndpointConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcEndpointConnection(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcEndpointSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcEndpoint {
    var list: std.ArrayList(VpcEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcEndpoint(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcIpv6CidrBlockAssociationSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcIpv6CidrBlockAssociation {
    var list: std.ArrayList(VpcIpv6CidrBlockAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcIpv6CidrBlockAssociation(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const Vpc {
    var list: std.ArrayList(Vpc) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpc(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpcPeeringConnectionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpcPeeringConnection {
    var list: std.ArrayList(VpcPeeringConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpcPeeringConnection(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpnConcentratorList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpnConcentrator {
    var list: std.ArrayList(VpnConcentrator) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpnConcentrator(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpnConnectionDeviceTypeList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpnConnectionDeviceType {
    var list: std.ArrayList(VpnConnectionDeviceType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpnConnectionDeviceType(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpnConnectionList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpnConnection {
    var list: std.ArrayList(VpnConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpnConnection(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpnGatewayList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpnGateway {
    var list: std.ArrayList(VpnGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpnGateway(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeVpnStaticRouteList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const VpnStaticRoute {
    var list: std.ArrayList(VpnStaticRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeVpnStaticRoute(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeWorkloadsList(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try allocator.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializesnapshotTierStatusSet(allocator: std.mem.Allocator, reader: *aws.xml.Reader, comptime item_tag: []const u8) ![]const SnapshotTierStatus {
    var list: std.ArrayList(SnapshotTierStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(allocator, try deserializeSnapshotTierStatus(allocator, reader));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(allocator);
}

pub fn deserializeAcceleratorCount(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AcceleratorCount {
    _ = allocator;
    var result: AcceleratorCount = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAcceleratorTotalMemoryMiB(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AcceleratorTotalMemoryMiB {
    _ = allocator;
    var result: AcceleratorTotalMemoryMiB = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccessScopeAnalysisFinding(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AccessScopeAnalysisFinding {
    var result: AccessScopeAnalysisFinding = undefined;
    result.finding_components = null;
    result.finding_id = null;
    result.network_insights_access_scope_analysis_id = null;
    result.network_insights_access_scope_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "findingComponentSet")) {
                    result.finding_components = try deserializePathComponentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "findingId")) {
                    result.finding_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeAnalysisId")) {
                    result.network_insights_access_scope_analysis_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccessScopePath(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AccessScopePath {
    var result: AccessScopePath = undefined;
    result.destination = null;
    result.source = null;
    result.through_resources = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try deserializePathStatement(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try deserializePathStatement(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "throughResourceSet")) {
                    result.through_resources = try deserializeThroughResourcesStatementList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccountAttribute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AccountAttribute {
    var result: AccountAttribute = undefined;
    result.attribute_name = null;
    result.attribute_values = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attributeName")) {
                    result.attribute_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attributeValueSet")) {
                    result.attribute_values = try deserializeAccountAttributeValueList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccountAttributeValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AccountAttributeValue {
    var result: AccountAttributeValue = undefined;
    result.attribute_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attributeValue")) {
                    result.attribute_value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeActiveInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ActiveInstance {
    var result: ActiveInstance = undefined;
    result.instance_health = null;
    result.instance_id = null;
    result.instance_type = null;
    result.spot_instance_request_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceHealth")) {
                    result.instance_health = InstanceHealthStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeActiveVpnTunnelStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ActiveVpnTunnelStatus {
    var result: ActiveVpnTunnelStatus = undefined;
    result.ike_version = null;
    result.phase_1_dh_group = null;
    result.phase_1_encryption_algorithm = null;
    result.phase_1_integrity_algorithm = null;
    result.phase_2_dh_group = null;
    result.phase_2_encryption_algorithm = null;
    result.phase_2_integrity_algorithm = null;
    result.provisioning_status = null;
    result.provisioning_status_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ikeVersion")) {
                    result.ike_version = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase1DHGroup")) {
                    result.phase_1_dh_group = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "phase1EncryptionAlgorithm")) {
                    result.phase_1_encryption_algorithm = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase1IntegrityAlgorithm")) {
                    result.phase_1_integrity_algorithm = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase2DHGroup")) {
                    result.phase_2_dh_group = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "phase2EncryptionAlgorithm")) {
                    result.phase_2_encryption_algorithm = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase2IntegrityAlgorithm")) {
                    result.phase_2_integrity_algorithm = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisioningStatus")) {
                    result.provisioning_status = VpnTunnelProvisioningStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisioningStatusReason")) {
                    result.provisioning_status_reason = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddedPrincipal(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AddedPrincipal {
    var result: AddedPrincipal = undefined;
    result.principal = null;
    result.principal_type = null;
    result.service_id = null;
    result.service_permission_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "principal")) {
                    result.principal = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "principalType")) {
                    result.principal_type = PrincipalType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "servicePermissionId")) {
                    result.service_permission_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAdditionalDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AdditionalDetail {
    var result: AdditionalDetail = undefined;
    result.additional_detail_type = null;
    result.component = null;
    result.load_balancers = null;
    result.rule_group_rule_options_pairs = null;
    result.rule_group_type_pairs = null;
    result.rule_options = null;
    result.service_name = null;
    result.vpc_endpoint_service = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "additionalDetailType")) {
                    result.additional_detail_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "component")) {
                    result.component = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "loadBalancerSet")) {
                    result.load_balancers = try deserializeAnalysisComponentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ruleGroupRuleOptionsPairSet")) {
                    result.rule_group_rule_options_pairs = try deserializeRuleGroupRuleOptionsPairList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ruleGroupTypePairSet")) {
                    result.rule_group_type_pairs = try deserializeRuleGroupTypePairList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ruleOptionSet")) {
                    result.rule_options = try deserializeRuleOptionList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointService")) {
                    result.vpc_endpoint_service = try deserializeAnalysisComponent(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddress(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Address {
    var result: Address = undefined;
    result.allocation_id = null;
    result.association_id = null;
    result.carrier_ip = null;
    result.customer_owned_ip = null;
    result.customer_owned_ipv_4_pool = null;
    result.domain = null;
    result.instance_id = null;
    result.network_border_group = null;
    result.network_interface_id = null;
    result.network_interface_owner_id = null;
    result.private_ip_address = null;
    result.public_ip = null;
    result.public_ipv_4_pool = null;
    result.service_managed = null;
    result.subnet_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "carrierIp")) {
                    result.carrier_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIp")) {
                    result.customer_owned_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIpv4Pool")) {
                    result.customer_owned_ipv_4_pool = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "domain")) {
                    result.domain = DomainType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceOwnerId")) {
                    result.network_interface_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv4Pool")) {
                    result.public_ipv_4_pool = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceManaged")) {
                    result.service_managed = ServiceManaged.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddressAttribute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AddressAttribute {
    var result: AddressAttribute = undefined;
    result.allocation_id = null;
    result.ptr_record = null;
    result.ptr_record_update = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ptrRecord")) {
                    result.ptr_record = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ptrRecordUpdate")) {
                    result.ptr_record_update = try deserializePtrUpdateStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddressTransfer(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AddressTransfer {
    var result: AddressTransfer = undefined;
    result.address_transfer_status = null;
    result.allocation_id = null;
    result.public_ip = null;
    result.transfer_account_id = null;
    result.transfer_offer_accepted_timestamp = null;
    result.transfer_offer_expiration_timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressTransferStatus")) {
                    result.address_transfer_status = AddressTransferStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transferAccountId")) {
                    result.transfer_account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transferOfferAcceptedTimestamp")) {
                    result.transfer_offer_accepted_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "transferOfferExpirationTimestamp")) {
                    result.transfer_offer_expiration_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAllowedPrincipal(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AllowedPrincipal {
    var result: AllowedPrincipal = undefined;
    result.principal = null;
    result.principal_type = null;
    result.service_id = null;
    result.service_permission_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "principal")) {
                    result.principal = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "principalType")) {
                    result.principal_type = PrincipalType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "servicePermissionId")) {
                    result.service_permission_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAlternatePathHint(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AlternatePathHint {
    var result: AlternatePathHint = undefined;
    result.component_arn = null;
    result.component_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "componentArn")) {
                    result.component_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "componentId")) {
                    result.component_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisAclRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AnalysisAclRule {
    var result: AnalysisAclRule = undefined;
    result.cidr = null;
    result.egress = null;
    result.port_range = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egress")) {
                    result.egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "portRange")) {
                    result.port_range = try deserializePortRange(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleNumber")) {
                    result.rule_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisComponent(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AnalysisComponent {
    var result: AnalysisComponent = undefined;
    result.arn = null;
    result.id = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisLoadBalancerListener(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AnalysisLoadBalancerListener {
    _ = allocator;
    var result: AnalysisLoadBalancerListener = undefined;
    result.instance_port = null;
    result.load_balancer_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instancePort")) {
                    result.instance_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "loadBalancerPort")) {
                    result.load_balancer_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisLoadBalancerTarget(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AnalysisLoadBalancerTarget {
    var result: AnalysisLoadBalancerTarget = undefined;
    result.address = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance = null;
    result.port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "address")) {
                    result.address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instance")) {
                    result.instance = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisPacketHeader(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AnalysisPacketHeader {
    var result: AnalysisPacketHeader = undefined;
    result.destination_addresses = null;
    result.destination_port_ranges = null;
    result.protocol = null;
    result.source_addresses = null;
    result.source_port_ranges = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationAddressSet")) {
                    result.destination_addresses = try deserializeIpAddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "destinationPortRangeSet")) {
                    result.destination_port_ranges = try deserializePortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceAddressSet")) {
                    result.source_addresses = try deserializeIpAddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourcePortRangeSet")) {
                    result.source_port_ranges = try deserializePortRangeList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisRouteTableRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AnalysisRouteTableRoute {
    var result: AnalysisRouteTableRoute = undefined;
    result.carrier_gateway_id = null;
    result.core_network_arn = null;
    result.destination_cidr = null;
    result.destination_prefix_list_id = null;
    result.egress_only_internet_gateway_id = null;
    result.gateway_id = null;
    result.instance_id = null;
    result.local_gateway_id = null;
    result.nat_gateway_id = null;
    result.network_interface_id = null;
    result.origin = null;
    result.state = null;
    result.transit_gateway_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierGatewayId")) {
                    result.carrier_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkArn")) {
                    result.core_network_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListId")) {
                    result.destination_prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressOnlyInternetGatewayId")) {
                    result.egress_only_internet_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGatewayId")) {
                    result.nat_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "origin")) {
                    result.origin = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisSecurityGroupRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AnalysisSecurityGroupRule {
    var result: AnalysisSecurityGroupRule = undefined;
    result.cidr = null;
    result.direction = null;
    result.port_range = null;
    result.prefix_list_id = null;
    result.protocol = null;
    result.security_group_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "direction")) {
                    result.direction = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "portRange")) {
                    result.port_range = try deserializePortRange(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupId")) {
                    result.security_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAsnAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AsnAssociation {
    var result: AsnAssociation = undefined;
    result.asn = null;
    result.cidr = null;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "asn")) {
                    result.asn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = AsnAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssignedPrivateIpAddress(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AssignedPrivateIpAddress {
    var result: AssignedPrivateIpAddress = undefined;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssociatedRole(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AssociatedRole {
    var result: AssociatedRole = undefined;
    result.associated_role_arn = null;
    result.certificate_s3_bucket_name = null;
    result.certificate_s3_object_key = null;
    result.encryption_kms_key_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedRoleArn")) {
                    result.associated_role_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "certificateS3BucketName")) {
                    result.certificate_s3_bucket_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "certificateS3ObjectKey")) {
                    result.certificate_s3_object_key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionKmsKeyId")) {
                    result.encryption_kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssociatedTargetNetwork(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AssociatedTargetNetwork {
    var result: AssociatedTargetNetwork = undefined;
    result.network_id = null;
    result.network_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "networkId")) {
                    result.network_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkType")) {
                    result.network_type = AssociatedNetworkType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssociationStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AssociationStatus {
    var result: AssociationStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = AssociationStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttachmentEnaSrdSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AttachmentEnaSrdSpecification {
    var result: AttachmentEnaSrdSpecification = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeAttachmentEnaSrdUdpSpecification(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttachmentEnaSrdUdpSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AttachmentEnaSrdUdpSpecification {
    _ = allocator;
    var result: AttachmentEnaSrdUdpSpecification = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttributeBooleanValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AttributeBooleanValue {
    _ = allocator;
    var result: AttributeBooleanValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttributeSummary(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AttributeSummary {
    var result: AttributeSummary = undefined;
    result.attribute_name = null;
    result.most_frequent_value = null;
    result.number_of_matched_accounts = null;
    result.number_of_unmatched_accounts = null;
    result.regional_summaries = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attributeName")) {
                    result.attribute_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "mostFrequentValue")) {
                    result.most_frequent_value = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "numberOfMatchedAccounts")) {
                    result.number_of_matched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "numberOfUnmatchedAccounts")) {
                    result.number_of_unmatched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "regionalSummarySet")) {
                    result.regional_summaries = try deserializeRegionalSummaryList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttributeValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AttributeValue {
    var result: AttributeValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAuthorizationRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AuthorizationRule {
    var result: AuthorizationRule = undefined;
    result.access_all = null;
    result.client_vpn_endpoint_id = null;
    result.description = null;
    result.destination_cidr = null;
    result.group_id = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accessAll")) {
                    result.access_all = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnAuthorizationRuleStatus(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAvailabilityZone(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AvailabilityZone {
    var result: AvailabilityZone = undefined;
    result.group_long_name = null;
    result.group_name = null;
    result.messages = null;
    result.network_border_group = null;
    result.opt_in_status = null;
    result.parent_zone_id = null;
    result.parent_zone_name = null;
    result.region_name = null;
    result.state = null;
    result.zone_id = null;
    result.zone_name = null;
    result.zone_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupLongName")) {
                    result.group_long_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "messageSet")) {
                    result.messages = try deserializeAvailabilityZoneMessageList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "optInStatus")) {
                    result.opt_in_status = AvailabilityZoneOptInStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "parentZoneId")) {
                    result.parent_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "parentZoneName")) {
                    result.parent_zone_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneState")) {
                    result.state = AvailabilityZoneState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneId")) {
                    result.zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneName")) {
                    result.zone_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneType")) {
                    result.zone_type = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAvailabilityZoneMessage(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AvailabilityZoneMessage {
    var result: AvailabilityZoneMessage = undefined;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAvailableCapacity(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !AvailableCapacity {
    var result: AvailableCapacity = undefined;
    result.available_instance_capacity = null;
    result.available_v_cpus = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availableInstanceCapacity")) {
                    result.available_instance_capacity = try deserializeAvailableInstanceCapacityList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "availableVCpus")) {
                    result.available_v_cpus = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBaselineEbsBandwidthMbps(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !BaselineEbsBandwidthMbps {
    _ = allocator;
    var result: BaselineEbsBandwidthMbps = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBaselinePerformanceFactors(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !BaselinePerformanceFactors {
    var result: BaselinePerformanceFactors = undefined;
    result.cpu = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpu")) {
                    result.cpu = try deserializeCpuPerformanceFactor(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBlockDeviceMapping(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !BlockDeviceMapping {
    var result: BlockDeviceMapping = undefined;
    result.device_name = null;
    result.ebs = null;
    result.no_device = null;
    result.virtual_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeEbsBlockDevice(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "noDevice")) {
                    result.no_device = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualName")) {
                    result.virtual_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBlockDeviceMappingResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !BlockDeviceMappingResponse {
    var result: BlockDeviceMappingResponse = undefined;
    result.device_name = null;
    result.ebs = null;
    result.no_device = null;
    result.virtual_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeEbsBlockDeviceResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "noDevice")) {
                    result.no_device = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualName")) {
                    result.virtual_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBlockPublicAccessStates(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !BlockPublicAccessStates {
    _ = allocator;
    var result: BlockPublicAccessStates = undefined;
    result.internet_gateway_block_mode = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "internetGatewayBlockMode")) {
                    result.internet_gateway_block_mode = BlockPublicAccessMode.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBundleTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !BundleTask {
    var result: BundleTask = undefined;
    result.bundle_id = null;
    result.bundle_task_error = null;
    result.instance_id = null;
    result.progress = null;
    result.start_time = null;
    result.state = null;
    result.storage = null;
    result.update_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bundleId")) {
                    result.bundle_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "error")) {
                    result.bundle_task_error = try deserializeBundleTaskError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = BundleTaskState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storage")) {
                    result.storage = try deserializeStorage(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "updateTime")) {
                    result.update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBundleTaskError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !BundleTaskError {
    var result: BundleTaskError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeByoasn(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Byoasn {
    var result: Byoasn = undefined;
    result.asn = null;
    result.ipam_id = null;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "asn")) {
                    result.asn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = AsnState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeByoipCidr(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ByoipCidr {
    var result: ByoipCidr = undefined;
    result.advertisement_type = null;
    result.asn_associations = null;
    result.cidr = null;
    result.description = null;
    result.network_border_group = null;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "advertisementType")) {
                    result.advertisement_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "asnAssociationSet")) {
                    result.asn_associations = try deserializeAsnAssociationSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = ByoipCidrState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelCapacityReservationFleetError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CancelCapacityReservationFleetError {
    var result: CancelCapacityReservationFleetError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelSpotFleetRequestsError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CancelSpotFleetRequestsError {
    var result: CancelSpotFleetRequestsError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = CancelBatchErrorCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelSpotFleetRequestsErrorItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CancelSpotFleetRequestsErrorItem {
    var result: CancelSpotFleetRequestsErrorItem = undefined;
    result.@"error" = null;
    result.spot_fleet_request_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeCancelSpotFleetRequestsError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestId")) {
                    result.spot_fleet_request_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelSpotFleetRequestsSuccessItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CancelSpotFleetRequestsSuccessItem {
    var result: CancelSpotFleetRequestsSuccessItem = undefined;
    result.current_spot_fleet_request_state = null;
    result.previous_spot_fleet_request_state = null;
    result.spot_fleet_request_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currentSpotFleetRequestState")) {
                    result.current_spot_fleet_request_state = BatchState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousSpotFleetRequestState")) {
                    result.previous_spot_fleet_request_state = BatchState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestId")) {
                    result.spot_fleet_request_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelledSpotInstanceRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CancelledSpotInstanceRequest {
    var result: CancelledSpotInstanceRequest = undefined;
    result.spot_instance_request_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = CancelSpotInstanceRequestState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityAllocation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityAllocation {
    _ = allocator;
    var result: CapacityAllocation = undefined;
    result.allocation_type = null;
    result.count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationType")) {
                    result.allocation_type = AllocationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlock(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityBlock {
    var result: CapacityBlock = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_id = null;
    result.capacity_reservation_ids = null;
    result.create_date = null;
    result.end_date = null;
    result.start_date = null;
    result.state = null;
    result.tags = null;
    result.ultraserver_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationIdSet")) {
                    result.capacity_reservation_ids = try deserializeCapacityReservationIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = CapacityBlockResourceState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ultraserverType")) {
                    result.ultraserver_type = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockExtension(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityBlockExtension {
    var result: CapacityBlockExtension = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_extension_duration_hours = null;
    result.capacity_block_extension_end_date = null;
    result.capacity_block_extension_offering_id = null;
    result.capacity_block_extension_purchase_date = null;
    result.capacity_block_extension_start_date = null;
    result.capacity_block_extension_status = null;
    result.capacity_reservation_id = null;
    result.currency_code = null;
    result.instance_count = null;
    result.instance_type = null;
    result.upfront_fee = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionDurationHours")) {
                    result.capacity_block_extension_duration_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionEndDate")) {
                    result.capacity_block_extension_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionOfferingId")) {
                    result.capacity_block_extension_offering_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionPurchaseDate")) {
                    result.capacity_block_extension_purchase_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionStartDate")) {
                    result.capacity_block_extension_start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionStatus")) {
                    result.capacity_block_extension_status = CapacityBlockExtensionStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontFee")) {
                    result.upfront_fee = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockExtensionOffering(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityBlockExtensionOffering {
    var result: CapacityBlockExtensionOffering = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_extension_duration_hours = null;
    result.capacity_block_extension_end_date = null;
    result.capacity_block_extension_offering_id = null;
    result.capacity_block_extension_start_date = null;
    result.currency_code = null;
    result.instance_count = null;
    result.instance_type = null;
    result.start_date = null;
    result.tenancy = null;
    result.upfront_fee = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionDurationHours")) {
                    result.capacity_block_extension_duration_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionEndDate")) {
                    result.capacity_block_extension_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionOfferingId")) {
                    result.capacity_block_extension_offering_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionStartDate")) {
                    result.capacity_block_extension_start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = CapacityReservationTenancy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontFee")) {
                    result.upfront_fee = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockOffering(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityBlockOffering {
    var result: CapacityBlockOffering = undefined;
    result.availability_zone = null;
    result.capacity_block_duration_hours = null;
    result.capacity_block_duration_minutes = null;
    result.capacity_block_offering_id = null;
    result.currency_code = null;
    result.end_date = null;
    result.instance_count = null;
    result.instance_type = null;
    result.start_date = null;
    result.tenancy = null;
    result.ultraserver_count = null;
    result.ultraserver_type = null;
    result.upfront_fee = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockDurationHours")) {
                    result.capacity_block_duration_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockDurationMinutes")) {
                    result.capacity_block_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockOfferingId")) {
                    result.capacity_block_offering_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = CapacityReservationTenancy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ultraserverCount")) {
                    result.ultraserver_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ultraserverType")) {
                    result.ultraserver_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontFee")) {
                    result.upfront_fee = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityBlockStatus {
    var result: CapacityBlockStatus = undefined;
    result.capacity_block_id = null;
    result.capacity_reservation_statuses = null;
    result.interconnect_status = null;
    result.total_available_capacity = null;
    result.total_capacity = null;
    result.total_unavailable_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationStatusSet")) {
                    result.capacity_reservation_statuses = try deserializeCapacityReservationStatusSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "interconnectStatus")) {
                    result.interconnect_status = CapacityBlockInterconnectStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalAvailableCapacity")) {
                    result.total_available_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalCapacity")) {
                    result.total_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalUnavailableCapacity")) {
                    result.total_unavailable_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityManagerDataExportResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityManagerDataExportResponse {
    var result: CapacityManagerDataExportResponse = undefined;
    result.capacity_manager_data_export_id = null;
    result.create_time = null;
    result.latest_delivery_s3_location_uri = null;
    result.latest_delivery_status = null;
    result.latest_delivery_status_message = null;
    result.latest_delivery_time = null;
    result.output_format = null;
    result.s3_bucket_name = null;
    result.s3_bucket_prefix = null;
    result.schedule = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityManagerDataExportId")) {
                    result.capacity_manager_data_export_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "latestDeliveryS3LocationUri")) {
                    result.latest_delivery_s3_location_uri = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "latestDeliveryStatus")) {
                    result.latest_delivery_status = CapacityManagerDataExportStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "latestDeliveryStatusMessage")) {
                    result.latest_delivery_status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "latestDeliveryTime")) {
                    result.latest_delivery_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "outputFormat")) {
                    result.output_format = OutputFormat.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3BucketName")) {
                    result.s3_bucket_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3BucketPrefix")) {
                    result.s3_bucket_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "schedule")) {
                    result.schedule = Schedule.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityManagerDimension(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityManagerDimension {
    var result: CapacityManagerDimension = undefined;
    result.account_id = null;
    result.availability_zone_id = null;
    result.instance_family = null;
    result.instance_platform = null;
    result.instance_type = null;
    result.reservation_arn = null;
    result.reservation_create_timestamp = null;
    result.reservation_end_date_type = null;
    result.reservation_end_timestamp = null;
    result.reservation_id = null;
    result.reservation_instance_match_criteria = null;
    result.reservation_start_timestamp = null;
    result.reservation_state = null;
    result.reservation_type = null;
    result.reservation_unused_financial_owner = null;
    result.resource_region = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePlatform")) {
                    result.instance_platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationArn")) {
                    result.reservation_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationCreateTimestamp")) {
                    result.reservation_create_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reservationEndDateType")) {
                    result.reservation_end_date_type = ReservationEndDateType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationEndTimestamp")) {
                    result.reservation_end_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reservationId")) {
                    result.reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationInstanceMatchCriteria")) {
                    result.reservation_instance_match_criteria = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationStartTimestamp")) {
                    result.reservation_start_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reservationState")) {
                    result.reservation_state = ReservationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationType")) {
                    result.reservation_type = ReservationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationUnusedFinancialOwner")) {
                    result.reservation_unused_financial_owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = CapacityTenancy.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservation {
    var result: CapacityReservation = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.available_instance_count = null;
    result.capacity_allocations = null;
    result.capacity_block_id = null;
    result.capacity_reservation_arn = null;
    result.capacity_reservation_fleet_id = null;
    result.capacity_reservation_id = null;
    result.commitment_info = null;
    result.create_date = null;
    result.delivery_preference = null;
    result.ebs_optimized = null;
    result.end_date = null;
    result.end_date_type = null;
    result.ephemeral_storage = null;
    result.instance_match_criteria = null;
    result.instance_platform = null;
    result.instance_type = null;
    result.interruptible = null;
    result.interruptible_capacity_allocation = null;
    result.interruption_info = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.placement_group_arn = null;
    result.reservation_type = null;
    result.start_date = null;
    result.state = null;
    result.tags = null;
    result.tenancy = null;
    result.total_instance_count = null;
    result.unused_reservation_billing_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableInstanceCount")) {
                    result.available_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityAllocationSet")) {
                    result.capacity_allocations = try deserializeCapacityAllocations(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationArn")) {
                    result.capacity_reservation_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "commitmentInfo")) {
                    result.commitment_info = try deserializeCapacityReservationCommitmentInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deliveryPreference")) {
                    result.delivery_preference = CapacityReservationDeliveryPreference.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "endDateType")) {
                    result.end_date_type = EndDateType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ephemeralStorage")) {
                    result.ephemeral_storage = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "instanceMatchCriteria")) {
                    result.instance_match_criteria = InstanceMatchCriteria.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePlatform")) {
                    result.instance_platform = CapacityReservationInstancePlatform.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "interruptible")) {
                    result.interruptible = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "interruptibleCapacityAllocation")) {
                    result.interruptible_capacity_allocation = try deserializeInterruptibleCapacityAllocation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "interruptionInfo")) {
                    result.interruption_info = try deserializeInterruptionInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placementGroupArn")) {
                    result.placement_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationType")) {
                    result.reservation_type = CapacityReservationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = CapacityReservationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = CapacityReservationTenancy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalInstanceCount")) {
                    result.total_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "unusedReservationBillingOwnerId")) {
                    result.unused_reservation_billing_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationBillingRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationBillingRequest {
    var result: CapacityReservationBillingRequest = undefined;
    result.capacity_reservation_id = null;
    result.capacity_reservation_info = null;
    result.last_update_time = null;
    result.requested_by = null;
    result.status = null;
    result.status_message = null;
    result.unused_reservation_billing_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationInfo")) {
                    result.capacity_reservation_info = try deserializeCapacityReservationInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "lastUpdateTime")) {
                    result.last_update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "requestedBy")) {
                    result.requested_by = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = CapacityReservationBillingRequestStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "unusedReservationBillingOwnerId")) {
                    result.unused_reservation_billing_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationCommitmentInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationCommitmentInfo {
    _ = allocator;
    var result: CapacityReservationCommitmentInfo = undefined;
    result.commitment_end_date = null;
    result.committed_instance_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "commitmentEndDate")) {
                    result.commitment_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "committedInstanceCount")) {
                    result.committed_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationFleet(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationFleet {
    var result: CapacityReservationFleet = undefined;
    result.allocation_strategy = null;
    result.capacity_reservation_fleet_arn = null;
    result.capacity_reservation_fleet_id = null;
    result.create_time = null;
    result.end_date = null;
    result.instance_match_criteria = null;
    result.instance_type_specifications = null;
    result.state = null;
    result.tags = null;
    result.tenancy = null;
    result.total_fulfilled_capacity = null;
    result.total_target_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetArn")) {
                    result.capacity_reservation_fleet_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceMatchCriteria")) {
                    result.instance_match_criteria = FleetInstanceMatchCriteria.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceTypeSpecificationSet")) {
                    result.instance_type_specifications = try deserializeFleetCapacityReservationSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = CapacityReservationFleetState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = FleetCapacityReservationTenancy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalFulfilledCapacity")) {
                    result.total_fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "totalTargetCapacity")) {
                    result.total_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationFleetCancellationState(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationFleetCancellationState {
    var result: CapacityReservationFleetCancellationState = undefined;
    result.capacity_reservation_fleet_id = null;
    result.current_fleet_state = null;
    result.previous_fleet_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currentFleetState")) {
                    result.current_fleet_state = CapacityReservationFleetState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousFleetState")) {
                    result.previous_fleet_state = CapacityReservationFleetState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationGroup {
    var result: CapacityReservationGroup = undefined;
    result.group_arn = null;
    result.owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupArn")) {
                    result.group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationInfo {
    var result: CapacityReservationInfo = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_type = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = CapacityReservationTenancy.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationOptions {
    _ = allocator;
    var result: CapacityReservationOptions = undefined;
    result.usage_strategy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "usageStrategy")) {
                    result.usage_strategy = FleetCapacityReservationUsageStrategy.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationSpecificationResponse {
    var result: CapacityReservationSpecificationResponse = undefined;
    result.capacity_reservation_preference = null;
    result.capacity_reservation_target = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationPreference")) {
                    result.capacity_reservation_preference = CapacityReservationPreference.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationTarget")) {
                    result.capacity_reservation_target = try deserializeCapacityReservationTargetResponse(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationStatus {
    var result: CapacityReservationStatus = undefined;
    result.capacity_reservation_id = null;
    result.total_available_capacity = null;
    result.total_capacity = null;
    result.total_unavailable_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalAvailableCapacity")) {
                    result.total_available_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalCapacity")) {
                    result.total_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalUnavailableCapacity")) {
                    result.total_unavailable_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationTargetResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationTargetResponse {
    var result: CapacityReservationTargetResponse = undefined;
    result.capacity_reservation_id = null;
    result.capacity_reservation_resource_group_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationResourceGroupArn")) {
                    result.capacity_reservation_resource_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationTopology(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CapacityReservationTopology {
    var result: CapacityReservationTopology = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_id = null;
    result.capacity_reservation_id = null;
    result.group_name = null;
    result.instance_type = null;
    result.network_nodes = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkNodeSet")) {
                    result.network_nodes = try deserializeNetworkNodeSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCarrierGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CarrierGateway {
    var result: CarrierGateway = undefined;
    result.carrier_gateway_id = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierGatewayId")) {
                    result.carrier_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = CarrierGatewayState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCertificateAuthentication(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CertificateAuthentication {
    var result: CertificateAuthentication = undefined;
    result.client_root_certificate_chain = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientRootCertificateChain")) {
                    result.client_root_certificate_chain = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCidrBlock(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CidrBlock {
    var result: CidrBlock = undefined;
    result.cidr_block = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLinkDnsSupport(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClassicLinkDnsSupport {
    var result: ClassicLinkDnsSupport = undefined;
    result.classic_link_dns_supported = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLinkDnsSupported")) {
                    result.classic_link_dns_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLinkInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClassicLinkInstance {
    var result: ClassicLinkInstance = undefined;
    result.groups = null;
    result.instance_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLoadBalancer(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClassicLoadBalancer {
    var result: ClassicLoadBalancer = undefined;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLoadBalancersConfig(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClassicLoadBalancersConfig {
    var result: ClassicLoadBalancersConfig = undefined;
    result.classic_load_balancers = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLoadBalancers")) {
                    result.classic_load_balancers = try deserializeClassicLoadBalancers(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientCertificateRevocationListStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientCertificateRevocationListStatus {
    var result: ClientCertificateRevocationListStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = ClientCertificateRevocationListStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientConnectResponseOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientConnectResponseOptions {
    var result: ClientConnectResponseOptions = undefined;
    result.enabled = null;
    result.lambda_function_arn = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "lambdaFunctionArn")) {
                    result.lambda_function_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnEndpointAttributeStatus(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientLoginBannerResponseOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientLoginBannerResponseOptions {
    var result: ClientLoginBannerResponseOptions = undefined;
    result.banner_text = null;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bannerText")) {
                    result.banner_text = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientRouteEnforcementResponseOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientRouteEnforcementResponseOptions {
    _ = allocator;
    var result: ClientRouteEnforcementResponseOptions = undefined;
    result.enforced = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enforced")) {
                    result.enforced = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnAuthentication(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnAuthentication {
    var result: ClientVpnAuthentication = undefined;
    result.active_directory = null;
    result.federated_authentication = null;
    result.mutual_authentication = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "activeDirectory")) {
                    result.active_directory = try deserializeDirectoryServiceAuthentication(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "federatedAuthentication")) {
                    result.federated_authentication = try deserializeFederatedAuthentication(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "mutualAuthentication")) {
                    result.mutual_authentication = try deserializeCertificateAuthentication(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = ClientVpnAuthenticationType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnAuthorizationRuleStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnAuthorizationRuleStatus {
    var result: ClientVpnAuthorizationRuleStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = ClientVpnAuthorizationRuleStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnConnection(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnConnection {
    var result: ClientVpnConnection = undefined;
    result.client_ip = null;
    result.client_ipv_6_address = null;
    result.client_vpn_endpoint_id = null;
    result.common_name = null;
    result.connection_end_time = null;
    result.connection_established_time = null;
    result.connection_id = null;
    result.egress_bytes = null;
    result.egress_packets = null;
    result.ingress_bytes = null;
    result.ingress_packets = null;
    result.posture_compliance_statuses = null;
    result.status = null;
    result.timestamp = null;
    result.username = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientIp")) {
                    result.client_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientIpv6Address")) {
                    result.client_ipv_6_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "commonName")) {
                    result.common_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionEndTime")) {
                    result.connection_end_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionEstablishedTime")) {
                    result.connection_established_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionId")) {
                    result.connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressBytes")) {
                    result.egress_bytes = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressPackets")) {
                    result.egress_packets = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ingressBytes")) {
                    result.ingress_bytes = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ingressPackets")) {
                    result.ingress_packets = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "postureComplianceStatusSet")) {
                    result.posture_compliance_statuses = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnConnectionStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "username")) {
                    result.username = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnConnectionStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnConnectionStatus {
    var result: ClientVpnConnectionStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = ClientVpnConnectionStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnEndpoint(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnEndpoint {
    var result: ClientVpnEndpoint = undefined;
    result.associated_target_networks = null;
    result.authentication_options = null;
    result.client_cidr_block = null;
    result.client_connect_options = null;
    result.client_login_banner_options = null;
    result.client_route_enforcement_options = null;
    result.client_vpn_endpoint_id = null;
    result.connection_log_options = null;
    result.creation_time = null;
    result.deletion_time = null;
    result.description = null;
    result.disconnect_on_session_timeout = null;
    result.dns_name = null;
    result.dns_servers = null;
    result.endpoint_ip_address_type = null;
    result.security_group_ids = null;
    result.self_service_portal_url = null;
    result.server_certificate_arn = null;
    result.session_timeout_hours = null;
    result.split_tunnel = null;
    result.status = null;
    result.tags = null;
    result.traffic_ip_address_type = null;
    result.transport_protocol = null;
    result.vpc_id = null;
    result.vpn_port = null;
    result.vpn_protocol = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedTargetNetwork")) {
                    result.associated_target_networks = try deserializeAssociatedTargetNetworkSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "authenticationOptions")) {
                    result.authentication_options = try deserializeClientVpnAuthenticationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "clientCidrBlock")) {
                    result.client_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientConnectOptions")) {
                    result.client_connect_options = try deserializeClientConnectResponseOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "clientLoginBannerOptions")) {
                    result.client_login_banner_options = try deserializeClientLoginBannerResponseOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "clientRouteEnforcementOptions")) {
                    result.client_route_enforcement_options = try deserializeClientRouteEnforcementResponseOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionLogOptions")) {
                    result.connection_log_options = try deserializeConnectionLogResponseOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deletionTime")) {
                    result.deletion_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disconnectOnSessionTimeout")) {
                    result.disconnect_on_session_timeout = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsServer")) {
                    result.dns_servers = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "endpointIpAddressType")) {
                    result.endpoint_ip_address_type = EndpointIpAddressType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeClientVpnSecurityGroupIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "selfServicePortalUrl")) {
                    result.self_service_portal_url = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serverCertificateArn")) {
                    result.server_certificate_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sessionTimeoutHours")) {
                    result.session_timeout_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "splitTunnel")) {
                    result.split_tunnel = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnEndpointStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trafficIpAddressType")) {
                    result.traffic_ip_address_type = TrafficIpAddressType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transportProtocol")) {
                    result.transport_protocol = TransportProtocol.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnPort")) {
                    result.vpn_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "vpnProtocol")) {
                    result.vpn_protocol = VpnProtocol.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnEndpointAttributeStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnEndpointAttributeStatus {
    var result: ClientVpnEndpointAttributeStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = ClientVpnEndpointAttributeStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnEndpointStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnEndpointStatus {
    var result: ClientVpnEndpointStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = ClientVpnEndpointStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnRoute {
    var result: ClientVpnRoute = undefined;
    result.client_vpn_endpoint_id = null;
    result.description = null;
    result.destination_cidr = null;
    result.origin = null;
    result.status = null;
    result.target_subnet = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "origin")) {
                    result.origin = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnRouteStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "targetSubnet")) {
                    result.target_subnet = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnRouteStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ClientVpnRouteStatus {
    var result: ClientVpnRouteStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = ClientVpnRouteStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCloudWatchLogOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CloudWatchLogOptions {
    var result: CloudWatchLogOptions = undefined;
    result.bgp_log_enabled = null;
    result.bgp_log_group_arn = null;
    result.bgp_log_output_format = null;
    result.log_enabled = null;
    result.log_group_arn = null;
    result.log_output_format = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpLogEnabled")) {
                    result.bgp_log_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "bgpLogGroupArn")) {
                    result.bgp_log_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bgpLogOutputFormat")) {
                    result.bgp_log_output_format = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logEnabled")) {
                    result.log_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "logGroupArn")) {
                    result.log_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logOutputFormat")) {
                    result.log_output_format = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCoipAddressUsage(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CoipAddressUsage {
    var result: CoipAddressUsage = undefined;
    result.allocation_id = null;
    result.aws_account_id = null;
    result.aws_service = null;
    result.co_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsAccountId")) {
                    result.aws_account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsService")) {
                    result.aws_service = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coIp")) {
                    result.co_ip = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCoipCidr(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CoipCidr {
    var result: CoipCidr = undefined;
    result.cidr = null;
    result.coip_pool_id = null;
    result.local_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coipPoolId")) {
                    result.coip_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCoipPool(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CoipPool {
    var result: CoipPool = undefined;
    result.local_gateway_route_table_id = null;
    result.pool_arn = null;
    result.pool_cidrs = null;
    result.pool_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolArn")) {
                    result.pool_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolCidrSet")) {
                    result.pool_cidrs = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "poolId")) {
                    result.pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionLogResponseOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ConnectionLogResponseOptions {
    var result: ConnectionLogResponseOptions = undefined;
    result.cloudwatch_log_group = null;
    result.cloudwatch_log_stream = null;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "CloudwatchLogGroup")) {
                    result.cloudwatch_log_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "CloudwatchLogStream")) {
                    result.cloudwatch_log_stream = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "Enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionNotification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ConnectionNotification {
    var result: ConnectionNotification = undefined;
    result.connection_events = null;
    result.connection_notification_arn = null;
    result.connection_notification_id = null;
    result.connection_notification_state = null;
    result.connection_notification_type = null;
    result.service_id = null;
    result.service_region = null;
    result.vpc_endpoint_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "connectionEvents")) {
                    result.connection_events = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "connectionNotificationArn")) {
                    result.connection_notification_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionNotificationId")) {
                    result.connection_notification_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionNotificationState")) {
                    result.connection_notification_state = ConnectionNotificationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionNotificationType")) {
                    result.connection_notification_type = ConnectionNotificationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceRegion")) {
                    result.service_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ConnectionTrackingConfiguration {
    _ = allocator;
    var result: ConnectionTrackingConfiguration = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "tcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ConnectionTrackingSpecification {
    _ = allocator;
    var result: ConnectionTrackingSpecification = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "tcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingSpecificationRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ConnectionTrackingSpecificationRequest {
    _ = allocator;
    var result: ConnectionTrackingSpecificationRequest = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "TcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "UdpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "UdpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ConnectionTrackingSpecificationResponse {
    _ = allocator;
    var result: ConnectionTrackingSpecificationResponse = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "tcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConversionTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ConversionTask {
    var result: ConversionTask = undefined;
    result.conversion_task_id = null;
    result.expiration_time = null;
    result.import_instance = null;
    result.import_volume = null;
    result.state = null;
    result.status_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "conversionTaskId")) {
                    result.conversion_task_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "expirationTime")) {
                    result.expiration_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importInstance")) {
                    result.import_instance = try deserializeImportInstanceTaskDetails(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "importVolume")) {
                    result.import_volume = try deserializeImportVolumeTaskDetails(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = ConversionTaskState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCpuOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CpuOptions {
    _ = allocator;
    var result: CpuOptions = undefined;
    result.amd_sev_snp = null;
    result.core_count = null;
    result.threads_per_core = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amdSevSnp")) {
                    result.amd_sev_snp = AmdSevSnpSpecification.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreCount")) {
                    result.core_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "threadsPerCore")) {
                    result.threads_per_core = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCpuPerformanceFactor(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CpuPerformanceFactor {
    var result: CpuPerformanceFactor = undefined;
    result.references = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "referenceSet")) {
                    result.references = try deserializePerformanceFactorReferenceSet(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreateFleetError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CreateFleetError {
    var result: CreateFleetError = undefined;
    result.error_code = null;
    result.error_message = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "errorCode")) {
                    result.error_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "errorMessage")) {
                    result.error_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = InstanceLifecycle.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreateFleetInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CreateFleetInstance {
    @setEvalBranchQuota(2000);
    var result: CreateFleetInstance = undefined;
    result.instance_ids = null;
    result.instance_type = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    result.platform = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceIds")) {
                    result.instance_ids = try deserializeInstanceIdsSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = InstanceLifecycle.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = PlatformValues.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreateVolumePermission(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CreateVolumePermission {
    var result: CreateVolumePermission = undefined;
    result.group = null;
    result.user_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "group")) {
                    result.group = PermissionGroup.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreationDateCondition(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CreationDateCondition {
    _ = allocator;
    var result: CreationDateCondition = undefined;
    result.maximum_days_since_created = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "maximumDaysSinceCreated")) {
                    result.maximum_days_since_created = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreditSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CreditSpecification {
    var result: CreditSpecification = undefined;
    result.cpu_credits = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpuCredits")) {
                    result.cpu_credits = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCustomerGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !CustomerGateway {
    var result: CustomerGateway = undefined;
    result.bgp_asn = null;
    result.bgp_asn_extended = null;
    result.certificate_arn = null;
    result.customer_gateway_id = null;
    result.device_name = null;
    result.ip_address = null;
    result.state = null;
    result.tags = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpAsn")) {
                    result.bgp_asn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bgpAsnExtended")) {
                    result.bgp_asn_extended = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "certificateArn")) {
                    result.certificate_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGatewayId")) {
                    result.customer_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddress")) {
                    result.ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDataResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DataResponse {
    var result: DataResponse = undefined;
    result.destination = null;
    result.id = null;
    result.metric = null;
    result.metric_points = null;
    result.period = null;
    result.source = null;
    result.statistic = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metric")) {
                    result.metric = MetricType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metricPointSet")) {
                    result.metric_points = try deserializeMetricPoints(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "period")) {
                    result.period = PeriodType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statistic")) {
                    result.statistic = StatisticType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeclarativePoliciesReport(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeclarativePoliciesReport {
    var result: DeclarativePoliciesReport = undefined;
    result.end_time = null;
    result.report_id = null;
    result.s3_bucket = null;
    result.s3_prefix = null;
    result.start_time = null;
    result.status = null;
    result.tags = null;
    result.target_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endTime")) {
                    result.end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reportId")) {
                    result.report_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Prefix")) {
                    result.s3_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = ReportState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "targetId")) {
                    result.target_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteFleetError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeleteFleetError {
    var result: DeleteFleetError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = DeleteFleetErrorCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteFleetErrorItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeleteFleetErrorItem {
    var result: DeleteFleetErrorItem = undefined;
    result.@"error" = null;
    result.fleet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeDeleteFleetError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "fleetId")) {
                    result.fleet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteFleetSuccessItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeleteFleetSuccessItem {
    var result: DeleteFleetSuccessItem = undefined;
    result.current_fleet_state = null;
    result.fleet_id = null;
    result.previous_fleet_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currentFleetState")) {
                    result.current_fleet_state = FleetStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fleetId")) {
                    result.fleet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousFleetState")) {
                    result.previous_fleet_state = FleetStateCode.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseErrorItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeleteLaunchTemplateVersionsResponseErrorItem {
    var result: DeleteLaunchTemplateVersionsResponseErrorItem = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.response_error = null;
    result.version_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "responseError")) {
                    result.response_error = try deserializeResponseError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "versionNumber")) {
                    result.version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseSuccessItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeleteLaunchTemplateVersionsResponseSuccessItem {
    var result: DeleteLaunchTemplateVersionsResponseSuccessItem = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.version_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "versionNumber")) {
                    result.version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteQueuedReservedInstancesError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeleteQueuedReservedInstancesError {
    var result: DeleteQueuedReservedInstancesError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = DeleteQueuedReservedInstancesErrorCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteSnapshotReturnCode(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeleteSnapshotReturnCode {
    var result: DeleteSnapshotReturnCode = undefined;
    result.return_code = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "returnCode")) {
                    result.return_code = SnapshotReturnCodes.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeprecationTimeCondition(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeprecationTimeCondition {
    _ = allocator;
    var result: DeprecationTimeCondition = undefined;
    result.maximum_days_since_deprecated = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "maximumDaysSinceDeprecated")) {
                    result.maximum_days_since_deprecated = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFastLaunchImagesSuccessItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DescribeFastLaunchImagesSuccessItem {
    var result: DescribeFastLaunchImagesSuccessItem = undefined;
    result.image_id = null;
    result.launch_template = null;
    result.max_parallel_launches = null;
    result.owner_id = null;
    result.resource_type = null;
    result.snapshot_configuration = null;
    result.state = null;
    result.state_transition_reason = null;
    result.state_transition_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplate")) {
                    result.launch_template = try deserializeFastLaunchLaunchTemplateSpecificationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "maxParallelLaunches")) {
                    result.max_parallel_launches = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = FastLaunchResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotConfiguration")) {
                    result.snapshot_configuration = try deserializeFastLaunchSnapshotConfigurationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = FastLaunchStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionTime")) {
                    result.state_transition_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFastSnapshotRestoreSuccessItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DescribeFastSnapshotRestoreSuccessItem {
    var result: DescribeFastSnapshotRestoreSuccessItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.disabled_time = null;
    result.disabling_time = null;
    result.enabled_time = null;
    result.enabling_time = null;
    result.optimizing_time = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.snapshot_id = null;
    result.state = null;
    result.state_transition_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disabledTime")) {
                    result.disabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "disablingTime")) {
                    result.disabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enabledTime")) {
                    result.enabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enablingTime")) {
                    result.enabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "optimizingTime")) {
                    result.optimizing_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = FastSnapshotRestoreStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFleetError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DescribeFleetError {
    var result: DescribeFleetError = undefined;
    result.error_code = null;
    result.error_message = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "errorCode")) {
                    result.error_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "errorMessage")) {
                    result.error_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = InstanceLifecycle.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFleetsInstances(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DescribeFleetsInstances {
    @setEvalBranchQuota(2000);
    var result: DescribeFleetsInstances = undefined;
    result.instance_ids = null;
    result.instance_type = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    result.platform = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceIds")) {
                    result.instance_ids = try deserializeInstanceIdsSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = InstanceLifecycle.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = PlatformValues.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDestinationOptionsResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DestinationOptionsResponse {
    _ = allocator;
    var result: DestinationOptionsResponse = undefined;
    result.file_format = null;
    result.hive_compatible_partitions = null;
    result.per_hour_partition = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fileFormat")) {
                    result.file_format = DestinationFileFormat.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hiveCompatiblePartitions")) {
                    result.hive_compatible_partitions = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "perHourPartition")) {
                    result.per_hour_partition = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeviceOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DeviceOptions {
    var result: DeviceOptions = undefined;
    result.public_signing_key_url = null;
    result.tenant_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "publicSigningKeyUrl")) {
                    result.public_signing_key_url = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenantId")) {
                    result.tenant_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDhcpConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DhcpConfiguration {
    var result: DhcpConfiguration = undefined;
    result.key = null;
    result.values = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "valueSet")) {
                    result.values = try deserializeDhcpConfigurationValueList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDhcpOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DhcpOptions {
    var result: DhcpOptions = undefined;
    result.dhcp_configurations = null;
    result.dhcp_options_id = null;
    result.owner_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dhcpConfigurationSet")) {
                    result.dhcp_configurations = try deserializeDhcpConfigurationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "dhcpOptionsId")) {
                    result.dhcp_options_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDirectoryServiceAuthentication(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DirectoryServiceAuthentication {
    var result: DirectoryServiceAuthentication = undefined;
    result.directory_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "directoryId")) {
                    result.directory_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreErrorItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DisableFastSnapshotRestoreErrorItem {
    var result: DisableFastSnapshotRestoreErrorItem = undefined;
    result.fast_snapshot_restore_state_errors = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fastSnapshotRestoreStateErrorSet")) {
                    result.fast_snapshot_restore_state_errors = try deserializeDisableFastSnapshotRestoreStateErrorSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreStateError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DisableFastSnapshotRestoreStateError {
    var result: DisableFastSnapshotRestoreStateError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreStateErrorItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DisableFastSnapshotRestoreStateErrorItem {
    var result: DisableFastSnapshotRestoreStateErrorItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.@"error" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeDisableFastSnapshotRestoreStateError(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreSuccessItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DisableFastSnapshotRestoreSuccessItem {
    var result: DisableFastSnapshotRestoreSuccessItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.disabled_time = null;
    result.disabling_time = null;
    result.enabled_time = null;
    result.enabling_time = null;
    result.optimizing_time = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.snapshot_id = null;
    result.state = null;
    result.state_transition_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disabledTime")) {
                    result.disabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "disablingTime")) {
                    result.disabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enabledTime")) {
                    result.enabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enablingTime")) {
                    result.enabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "optimizingTime")) {
                    result.optimizing_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = FastSnapshotRestoreStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDiskImageDescription(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DiskImageDescription {
    var result: DiskImageDescription = undefined;
    result.checksum = null;
    result.format = null;
    result.import_manifest_url = null;
    result.size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "checksum")) {
                    result.checksum = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "format")) {
                    result.format = DiskImageFormat.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importManifestUrl")) {
                    result.import_manifest_url = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDiskImageVolumeDescription(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DiskImageVolumeDescription {
    var result: DiskImageVolumeDescription = undefined;
    result.id = null;
    result.size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDiskInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DiskInfo {
    _ = allocator;
    var result: DiskInfo = undefined;
    result.count = null;
    result.size_in_gb = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "sizeInGB")) {
                    result.size_in_gb = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = DiskType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDnsEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DnsEntry {
    var result: DnsEntry = undefined;
    result.dns_name = null;
    result.hosted_zone_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostedZoneId")) {
                    result.hosted_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDnsOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !DnsOptions {
    var result: DnsOptions = undefined;
    result.dns_record_ip_type = null;
    result.private_dns_only_for_inbound_resolver_endpoint = null;
    result.private_dns_preference = null;
    result.private_dns_specified_domains = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsRecordIpType")) {
                    result.dns_record_ip_type = DnsRecordIpType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsOnlyForInboundResolverEndpoint")) {
                    result.private_dns_only_for_inbound_resolver_endpoint = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateDnsPreference")) {
                    result.private_dns_preference = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsSpecifiedDomainSet")) {
                    result.private_dns_specified_domains = try deserializePrivateDnsSpecifiedDomainSet(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsBlockDevice(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsBlockDevice {
    var result: EbsBlockDevice = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.delete_on_termination = null;
    result.ebs_card_index = null;
    result.encrypted = null;
    result.iops = null;
    result.kms_key_id = null;
    result.outpost_arn = null;
    result.snapshot_id = null;
    result.throughput = null;
    result.volume_initialization_rate = null;
    result.volume_size = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "AvailabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "EbsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "VolumeInitializationRate")) {
                    result.volume_initialization_rate = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = VolumeType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsBlockDeviceResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsBlockDeviceResponse {
    var result: EbsBlockDeviceResponse = undefined;
    result.delete_on_termination = null;
    result.encrypted = null;
    result.iops = null;
    result.kms_key_id = null;
    result.snapshot_id = null;
    result.throughput = null;
    result.volume_size = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = VolumeType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsCardInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsCardInfo {
    _ = allocator;
    var result: EbsCardInfo = undefined;
    result.baseline_bandwidth_in_mbps = null;
    result.baseline_iops = null;
    result.baseline_throughput_in_m_bps = null;
    result.ebs_card_index = null;
    result.maximum_bandwidth_in_mbps = null;
    result.maximum_iops = null;
    result.maximum_throughput_in_m_bps = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "baselineBandwidthInMbps")) {
                    result.baseline_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineIops")) {
                    result.baseline_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineThroughputInMBps")) {
                    result.baseline_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumBandwidthInMbps")) {
                    result.maximum_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumIops")) {
                    result.maximum_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumThroughputInMBps")) {
                    result.maximum_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsInfo {
    var result: EbsInfo = undefined;
    result.attachment_limit_type = null;
    result.ebs_cards = null;
    result.ebs_optimized_info = null;
    result.ebs_optimized_support = null;
    result.encryption_support = null;
    result.maximum_ebs_attachments = null;
    result.maximum_ebs_cards = null;
    result.nvme_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentLimitType")) {
                    result.attachment_limit_type = AttachmentLimitType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsCardSet")) {
                    result.ebs_cards = try deserializeEbsCardInfoList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ebsOptimizedInfo")) {
                    result.ebs_optimized_info = try deserializeEbsOptimizedInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ebsOptimizedSupport")) {
                    result.ebs_optimized_support = EbsOptimizedSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionSupport")) {
                    result.encryption_support = EbsEncryptionSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maximumEbsAttachments")) {
                    result.maximum_ebs_attachments = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumEbsCards")) {
                    result.maximum_ebs_cards = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "nvmeSupport")) {
                    result.nvme_support = EbsNvmeSupport.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsInstanceBlockDevice(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsInstanceBlockDevice {
    var result: EbsInstanceBlockDevice = undefined;
    result.associated_resource = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.ebs_card_index = null;
    result.operator = null;
    result.status = null;
    result.volume_id = null;
    result.volume_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResource")) {
                    result.associated_resource = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = AttachmentStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeOwnerId")) {
                    result.volume_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsOptimizedInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsOptimizedInfo {
    _ = allocator;
    var result: EbsOptimizedInfo = undefined;
    result.baseline_bandwidth_in_mbps = null;
    result.baseline_iops = null;
    result.baseline_throughput_in_m_bps = null;
    result.maximum_bandwidth_in_mbps = null;
    result.maximum_iops = null;
    result.maximum_throughput_in_m_bps = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "baselineBandwidthInMbps")) {
                    result.baseline_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineIops")) {
                    result.baseline_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineThroughputInMBps")) {
                    result.baseline_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumBandwidthInMbps")) {
                    result.maximum_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumIops")) {
                    result.maximum_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumThroughputInMBps")) {
                    result.maximum_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsStatusDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsStatusDetails {
    _ = allocator;
    var result: EbsStatusDetails = undefined;
    result.impaired_since = null;
    result.name = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "impairedSince")) {
                    result.impaired_since = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = StatusName.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = StatusType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsStatusSummary(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EbsStatusSummary {
    var result: EbsStatusSummary = undefined;
    result.details = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "details")) {
                    result.details = try deserializeEbsStatusDetailsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = SummaryStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEc2InstanceConnectEndpoint(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ec2InstanceConnectEndpoint {
    var result: Ec2InstanceConnectEndpoint = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.created_at = null;
    result.dns_name = null;
    result.fips_dns_name = null;
    result.instance_connect_endpoint_arn = null;
    result.instance_connect_endpoint_id = null;
    result.ip_address_type = null;
    result.network_interface_ids = null;
    result.owner_id = null;
    result.preserve_client_ip = null;
    result.public_dns_names = null;
    result.security_group_ids = null;
    result.state = null;
    result.state_message = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createdAt")) {
                    result.created_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fipsDnsName")) {
                    result.fips_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceConnectEndpointArn")) {
                    result.instance_connect_endpoint_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceConnectEndpointId")) {
                    result.instance_connect_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddressType")) {
                    result.ip_address_type = IpAddressType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceIdSet")) {
                    result.network_interface_ids = try deserializeNetworkInterfaceIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "preserveClientIp")) {
                    result.preserve_client_ip = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicDnsNames")) {
                    result.public_dns_names = try deserializeInstanceConnectEndpointPublicDnsNames(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeSecurityGroupIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = Ec2InstanceConnectEndpointState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEfaInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EfaInfo {
    _ = allocator;
    var result: EfaInfo = undefined;
    result.maximum_efa_interfaces = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "maximumEfaInterfaces")) {
                    result.maximum_efa_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEgressOnlyInternetGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EgressOnlyInternetGateway {
    var result: EgressOnlyInternetGateway = undefined;
    result.attachments = null;
    result.egress_only_internet_gateway_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentSet")) {
                    result.attachments = try deserializeInternetGatewayAttachmentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "egressOnlyInternetGatewayId")) {
                    result.egress_only_internet_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpuAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ElasticGpuAssociation {
    var result: ElasticGpuAssociation = undefined;
    result.elastic_gpu_association_id = null;
    result.elastic_gpu_association_state = null;
    result.elastic_gpu_association_time = null;
    result.elastic_gpu_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "elasticGpuAssociationId")) {
                    result.elastic_gpu_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuAssociationState")) {
                    result.elastic_gpu_association_state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuAssociationTime")) {
                    result.elastic_gpu_association_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuId")) {
                    result.elastic_gpu_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpuHealth(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ElasticGpuHealth {
    _ = allocator;
    var result: ElasticGpuHealth = undefined;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "status")) {
                    result.status = ElasticGpuStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpuSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ElasticGpuSpecificationResponse {
    var result: ElasticGpuSpecificationResponse = undefined;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ElasticGpus {
    var result: ElasticGpus = undefined;
    result.availability_zone = null;
    result.elastic_gpu_health = null;
    result.elastic_gpu_id = null;
    result.elastic_gpu_state = null;
    result.elastic_gpu_type = null;
    result.instance_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuHealth")) {
                    result.elastic_gpu_health = try deserializeElasticGpuHealth(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "elasticGpuId")) {
                    result.elastic_gpu_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuState")) {
                    result.elastic_gpu_state = ElasticGpuState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuType")) {
                    result.elastic_gpu_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticInferenceAcceleratorAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ElasticInferenceAcceleratorAssociation {
    var result: ElasticInferenceAcceleratorAssociation = undefined;
    result.elastic_inference_accelerator_arn = null;
    result.elastic_inference_accelerator_association_id = null;
    result.elastic_inference_accelerator_association_state = null;
    result.elastic_inference_accelerator_association_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorArn")) {
                    result.elastic_inference_accelerator_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationId")) {
                    result.elastic_inference_accelerator_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationState")) {
                    result.elastic_inference_accelerator_association_state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationTime")) {
                    result.elastic_inference_accelerator_association_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnaSrdSpecificationRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EnaSrdSpecificationRequest {
    var result: EnaSrdSpecificationRequest = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "EnaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "EnaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeEnaSrdUdpSpecificationRequest(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnaSrdUdpSpecificationRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EnaSrdUdpSpecificationRequest {
    _ = allocator;
    var result: EnaSrdUdpSpecificationRequest = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "EnaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreErrorItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EnableFastSnapshotRestoreErrorItem {
    var result: EnableFastSnapshotRestoreErrorItem = undefined;
    result.fast_snapshot_restore_state_errors = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fastSnapshotRestoreStateErrorSet")) {
                    result.fast_snapshot_restore_state_errors = try deserializeEnableFastSnapshotRestoreStateErrorSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreStateError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EnableFastSnapshotRestoreStateError {
    var result: EnableFastSnapshotRestoreStateError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreStateErrorItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EnableFastSnapshotRestoreStateErrorItem {
    var result: EnableFastSnapshotRestoreStateErrorItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.@"error" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeEnableFastSnapshotRestoreStateError(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreSuccessItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EnableFastSnapshotRestoreSuccessItem {
    var result: EnableFastSnapshotRestoreSuccessItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.disabled_time = null;
    result.disabling_time = null;
    result.enabled_time = null;
    result.enabling_time = null;
    result.optimizing_time = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.snapshot_id = null;
    result.state = null;
    result.state_transition_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disabledTime")) {
                    result.disabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "disablingTime")) {
                    result.disabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enabledTime")) {
                    result.enabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enablingTime")) {
                    result.enabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "optimizingTime")) {
                    result.optimizing_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = FastSnapshotRestoreStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnclaveOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EnclaveOptions {
    _ = allocator;
    var result: EnclaveOptions = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEncryptionSupport(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EncryptionSupport {
    var result: EncryptionSupport = undefined;
    result.encryption_state = null;
    result.state_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "encryptionState")) {
                    result.encryption_state = EncryptionStateValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEventInformation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !EventInformation {
    var result: EventInformation = undefined;
    result.event_description = null;
    result.event_sub_type = null;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eventDescription")) {
                    result.event_description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventSubType")) {
                    result.event_sub_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExplanation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Explanation {
    var result: Explanation = undefined;
    result.acl = null;
    result.acl_rule = null;
    result.address = null;
    result.addresses = null;
    result.attached_to = null;
    result.availability_zone_ids = null;
    result.availability_zones = null;
    result.cidrs = null;
    result.classic_load_balancer_listener = null;
    result.component = null;
    result.component_account = null;
    result.component_region = null;
    result.customer_gateway = null;
    result.destination = null;
    result.destination_vpc = null;
    result.direction = null;
    result.elastic_load_balancer_listener = null;
    result.explanation_code = null;
    result.firewall_stateful_rule = null;
    result.firewall_stateless_rule = null;
    result.ingress_route_table = null;
    result.internet_gateway = null;
    result.load_balancer_arn = null;
    result.load_balancer_listener_port = null;
    result.load_balancer_target = null;
    result.load_balancer_target_group = null;
    result.load_balancer_target_groups = null;
    result.load_balancer_target_port = null;
    result.missing_component = null;
    result.nat_gateway = null;
    result.network_interface = null;
    result.packet_field = null;
    result.port = null;
    result.port_ranges = null;
    result.prefix_list = null;
    result.protocols = null;
    result.route_table = null;
    result.route_table_route = null;
    result.security_group = null;
    result.security_group_rule = null;
    result.security_groups = null;
    result.source_vpc = null;
    result.state = null;
    result.subnet = null;
    result.subnet_route_table = null;
    result.transit_gateway = null;
    result.transit_gateway_attachment = null;
    result.transit_gateway_route_table = null;
    result.transit_gateway_route_table_route = null;
    result.vpc = null;
    result.vpc_endpoint = null;
    result.vpc_peering_connection = null;
    result.vpn_connection = null;
    result.vpn_gateway = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acl")) {
                    result.acl = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "aclRule")) {
                    result.acl_rule = try deserializeAnalysisAclRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "address")) {
                    result.address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressSet")) {
                    result.addresses = try deserializeIpAddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "attachedTo")) {
                    result.attached_to = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "availabilityZoneIdSet")) {
                    result.availability_zone_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneSet")) {
                    result.availability_zones = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "cidrSet")) {
                    result.cidrs = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "classicLoadBalancerListener")) {
                    result.classic_load_balancer_listener = try deserializeAnalysisLoadBalancerListener(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "component")) {
                    result.component = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "componentAccount")) {
                    result.component_account = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "componentRegion")) {
                    result.component_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGateway")) {
                    result.customer_gateway = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "destinationVpc")) {
                    result.destination_vpc = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "direction")) {
                    result.direction = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticLoadBalancerListener")) {
                    result.elastic_load_balancer_listener = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "explanationCode")) {
                    result.explanation_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "firewallStatefulRule")) {
                    result.firewall_stateful_rule = try deserializeFirewallStatefulRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "firewallStatelessRule")) {
                    result.firewall_stateless_rule = try deserializeFirewallStatelessRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ingressRouteTable")) {
                    result.ingress_route_table = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "internetGateway")) {
                    result.internet_gateway = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "loadBalancerArn")) {
                    result.load_balancer_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "loadBalancerListenerPort")) {
                    result.load_balancer_listener_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "loadBalancerTarget")) {
                    result.load_balancer_target = try deserializeAnalysisLoadBalancerTarget(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "loadBalancerTargetGroup")) {
                    result.load_balancer_target_group = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "loadBalancerTargetGroupSet")) {
                    result.load_balancer_target_groups = try deserializeAnalysisComponentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "loadBalancerTargetPort")) {
                    result.load_balancer_target_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "missingComponent")) {
                    result.missing_component = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGateway")) {
                    result.nat_gateway = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterface")) {
                    result.network_interface = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "packetField")) {
                    result.packet_field = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializePortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "prefixList")) {
                    result.prefix_list = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "protocolSet")) {
                    result.protocols = try deserializeStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "routeTable")) {
                    result.route_table = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "routeTableRoute")) {
                    result.route_table_route = try deserializeAnalysisRouteTableRoute(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "securityGroup")) {
                    result.security_group = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "securityGroupRule")) {
                    result.security_group_rule = try deserializeAnalysisSecurityGroupRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "securityGroupSet")) {
                    result.security_groups = try deserializeAnalysisComponentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourceVpc")) {
                    result.source_vpc = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnet")) {
                    result.subnet = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "subnetRouteTable")) {
                    result.subnet_route_table = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGateway")) {
                    result.transit_gateway = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachment")) {
                    result.transit_gateway_attachment = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTable")) {
                    result.transit_gateway_route_table = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableRoute")) {
                    result.transit_gateway_route_table_route = try deserializeTransitGatewayRouteTableRoute(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpc")) {
                    result.vpc = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpcEndpoint")) {
                    result.vpc_endpoint = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnection")) {
                    result.vpc_peering_connection = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpnConnection")) {
                    result.vpn_connection = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpnGateway")) {
                    result.vpn_gateway = try deserializeAnalysisComponent(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportImageTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ExportImageTask {
    var result: ExportImageTask = undefined;
    result.description = null;
    result.export_image_task_id = null;
    result.image_id = null;
    result.progress = null;
    result.s3_export_location = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exportImageTaskId")) {
                    result.export_image_task_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3ExportLocation")) {
                    result.s3_export_location = try deserializeExportTaskS3Location(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ExportTask {
    var result: ExportTask = undefined;
    result.description = null;
    result.export_task_id = null;
    result.export_to_s3_task = null;
    result.instance_export_details = null;
    result.state = null;
    result.status_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exportTaskId")) {
                    result.export_task_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exportToS3")) {
                    result.export_to_s3_task = try deserializeExportToS3Task(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceExport")) {
                    result.instance_export_details = try deserializeInstanceExportDetails(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = ExportTaskState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportTaskS3Location(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ExportTaskS3Location {
    var result: ExportTaskS3Location = undefined;
    result.s3_bucket = null;
    result.s3_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Prefix")) {
                    result.s3_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportToS3Task(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ExportToS3Task {
    var result: ExportToS3Task = undefined;
    result.container_format = null;
    result.disk_image_format = null;
    result.s3_bucket = null;
    result.s3_key = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "containerFormat")) {
                    result.container_format = ContainerFormat.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "diskImageFormat")) {
                    result.disk_image_format = DiskImageFormat.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Key")) {
                    result.s3_key = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFailedCapacityReservationFleetCancellationResult(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FailedCapacityReservationFleetCancellationResult {
    var result: FailedCapacityReservationFleetCancellationResult = undefined;
    result.cancel_capacity_reservation_fleet_error = null;
    result.capacity_reservation_fleet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cancelCapacityReservationFleetError")) {
                    result.cancel_capacity_reservation_fleet_error = try deserializeCancelCapacityReservationFleetError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFailedQueuedPurchaseDeletion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FailedQueuedPurchaseDeletion {
    var result: FailedQueuedPurchaseDeletion = undefined;
    result.@"error" = null;
    result.reserved_instances_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeDeleteQueuedReservedInstancesError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFastLaunchLaunchTemplateSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FastLaunchLaunchTemplateSpecificationResponse {
    var result: FastLaunchLaunchTemplateSpecificationResponse = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFastLaunchSnapshotConfigurationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FastLaunchSnapshotConfigurationResponse {
    _ = allocator;
    var result: FastLaunchSnapshotConfigurationResponse = undefined;
    result.target_resource_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "targetResourceCount")) {
                    result.target_resource_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFederatedAuthentication(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FederatedAuthentication {
    var result: FederatedAuthentication = undefined;
    result.saml_provider_arn = null;
    result.self_service_saml_provider_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "samlProviderArn")) {
                    result.saml_provider_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "selfServiceSamlProviderArn")) {
                    result.self_service_saml_provider_arn = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFilterPortRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FilterPortRange {
    _ = allocator;
    var result: FilterPortRange = undefined;
    result.from_port = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFirewallStatefulRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FirewallStatefulRule {
    var result: FirewallStatefulRule = undefined;
    result.destination_ports = null;
    result.destinations = null;
    result.direction = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_group_arn = null;
    result.source_ports = null;
    result.sources = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationPortSet")) {
                    result.destination_ports = try deserializePortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "destinationSet")) {
                    result.destinations = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "direction")) {
                    result.direction = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortSet")) {
                    result.source_ports = try deserializePortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourceSet")) {
                    result.sources = try deserializeValueStringList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFirewallStatelessRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FirewallStatelessRule {
    var result: FirewallStatelessRule = undefined;
    result.destination_ports = null;
    result.destinations = null;
    result.priority = null;
    result.protocols = null;
    result.rule_action = null;
    result.rule_group_arn = null;
    result.source_ports = null;
    result.sources = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationPortSet")) {
                    result.destination_ports = try deserializePortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "destinationSet")) {
                    result.destinations = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "protocolSet")) {
                    result.protocols = try deserializeProtocolIntList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortSet")) {
                    result.source_ports = try deserializePortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourceSet")) {
                    result.sources = try deserializeValueStringList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetCapacityReservation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FleetCapacityReservation {
    @setEvalBranchQuota(2000);
    var result: FleetCapacityReservation = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_reservation_id = null;
    result.create_date = null;
    result.ebs_optimized = null;
    result.fulfilled_capacity = null;
    result.instance_platform = null;
    result.instance_type = null;
    result.priority = null;
    result.total_instance_count = null;
    result.weight = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "fulfilledCapacity")) {
                    result.fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instancePlatform")) {
                    result.instance_platform = CapacityReservationInstancePlatform.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalInstanceCount")) {
                    result.total_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "weight")) {
                    result.weight = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetData(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FleetData {
    var result: FleetData = undefined;
    result.activity_status = null;
    result.client_token = null;
    result.context = null;
    result.create_time = null;
    result.errors = null;
    result.excess_capacity_termination_policy = null;
    result.fleet_id = null;
    result.fleet_state = null;
    result.fulfilled_capacity = null;
    result.fulfilled_on_demand_capacity = null;
    result.instances = null;
    result.launch_template_configs = null;
    result.on_demand_options = null;
    result.replace_unhealthy_instances = null;
    result.spot_options = null;
    result.tags = null;
    result.target_capacity_specification = null;
    result.terminate_instances_with_expiration = null;
    result.@"type" = null;
    result.valid_from = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "activityStatus")) {
                    result.activity_status = FleetActivityStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "context")) {
                    result.context = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "errorSet")) {
                    result.errors = try deserializeDescribeFleetsErrorSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "excessCapacityTerminationPolicy")) {
                    result.excess_capacity_termination_policy = FleetExcessCapacityTerminationPolicy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fleetId")) {
                    result.fleet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fleetState")) {
                    result.fleet_state = FleetStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fulfilledCapacity")) {
                    result.fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fulfilledOnDemandCapacity")) {
                    result.fulfilled_on_demand_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fleetInstanceSet")) {
                    result.instances = try deserializeDescribeFleetsInstancesSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "launchTemplateConfigs")) {
                    result.launch_template_configs = try deserializeFleetLaunchTemplateConfigList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "onDemandOptions")) {
                    result.on_demand_options = try deserializeOnDemandOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "replaceUnhealthyInstances")) {
                    result.replace_unhealthy_instances = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotOptions")) {
                    result.spot_options = try deserializeSpotOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "targetCapacitySpecification")) {
                    result.target_capacity_specification = try deserializeTargetCapacitySpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "terminateInstancesWithExpiration")) {
                    result.terminate_instances_with_expiration = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = FleetType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validFrom")) {
                    result.valid_from = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetLaunchTemplateConfig(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FleetLaunchTemplateConfig {
    var result: FleetLaunchTemplateConfig = undefined;
    result.launch_template_specification = null;
    result.overrides = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateSpecification")) {
                    result.launch_template_specification = try deserializeFleetLaunchTemplateSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "overrides")) {
                    result.overrides = try deserializeFleetLaunchTemplateOverridesList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetLaunchTemplateOverrides(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FleetLaunchTemplateOverrides {
    @setEvalBranchQuota(2000);
    var result: FleetLaunchTemplateOverrides = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.block_device_mappings = null;
    result.image_id = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.max_price = null;
    result.placement = null;
    result.priority = null;
    result.subnet_id = null;
    result.weighted_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMappingSet")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingResponseList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxPrice")) {
                    result.max_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializePlacementResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "weightedCapacity")) {
                    result.weighted_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetLaunchTemplateSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FleetLaunchTemplateSpecification {
    var result: FleetLaunchTemplateSpecification = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetSpotCapacityRebalance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FleetSpotCapacityRebalance {
    _ = allocator;
    var result: FleetSpotCapacityRebalance = undefined;
    result.replacement_strategy = null;
    result.termination_delay = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "replacementStrategy")) {
                    result.replacement_strategy = FleetReplacementStrategy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "terminationDelay")) {
                    result.termination_delay = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetSpotMaintenanceStrategies(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FleetSpotMaintenanceStrategies {
    var result: FleetSpotMaintenanceStrategies = undefined;
    result.capacity_rebalance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityRebalance")) {
                    result.capacity_rebalance = try deserializeFleetSpotCapacityRebalance(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFlowLog(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FlowLog {
    var result: FlowLog = undefined;
    result.creation_time = null;
    result.deliver_cross_account_role = null;
    result.deliver_logs_error_message = null;
    result.deliver_logs_permission_arn = null;
    result.deliver_logs_status = null;
    result.destination_options = null;
    result.flow_log_id = null;
    result.flow_log_status = null;
    result.log_destination = null;
    result.log_destination_type = null;
    result.log_format = null;
    result.log_group_name = null;
    result.max_aggregation_interval = null;
    result.resource_id = null;
    result.tags = null;
    result.traffic_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deliverCrossAccountRole")) {
                    result.deliver_cross_account_role = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliverLogsErrorMessage")) {
                    result.deliver_logs_error_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliverLogsPermissionArn")) {
                    result.deliver_logs_permission_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliverLogsStatus")) {
                    result.deliver_logs_status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationOptions")) {
                    result.destination_options = try deserializeDestinationOptionsResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "flowLogId")) {
                    result.flow_log_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "flowLogStatus")) {
                    result.flow_log_status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logDestination")) {
                    result.log_destination = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logDestinationType")) {
                    result.log_destination_type = LogDestinationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logFormat")) {
                    result.log_format = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logGroupName")) {
                    result.log_group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxAggregationInterval")) {
                    result.max_aggregation_interval = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trafficType")) {
                    result.traffic_type = TrafficType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaDeviceInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FpgaDeviceInfo {
    var result: FpgaDeviceInfo = undefined;
    result.count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeFpgaDeviceMemoryInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaDeviceMemoryInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FpgaDeviceMemoryInfo {
    _ = allocator;
    var result: FpgaDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaImage(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FpgaImage {
    var result: FpgaImage = undefined;
    result.create_time = null;
    result.data_retention_support = null;
    result.description = null;
    result.fpga_image_global_id = null;
    result.fpga_image_id = null;
    result.instance_types = null;
    result.name = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.pci_id = null;
    result.product_codes = null;
    result.public = null;
    result.shell_version = null;
    result.state = null;
    result.tags = null;
    result.update_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dataRetentionSupport")) {
                    result.data_retention_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fpgaImageGlobalId")) {
                    result.fpga_image_global_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fpgaImageId")) {
                    result.fpga_image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceTypes")) {
                    result.instance_types = try deserializeInstanceTypesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "pciId")) {
                    result.pci_id = try deserializePciId(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "public")) {
                    result.public = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "shellVersion")) {
                    result.shell_version = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try deserializeFpgaImageState(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tags")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "updateTime")) {
                    result.update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaImageAttribute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FpgaImageAttribute {
    var result: FpgaImageAttribute = undefined;
    result.description = null;
    result.fpga_image_id = null;
    result.load_permissions = null;
    result.name = null;
    result.product_codes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fpgaImageId")) {
                    result.fpga_image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "loadPermissions")) {
                    result.load_permissions = try deserializeLoadPermissionList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaImageState(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FpgaImageState {
    var result: FpgaImageState = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = FpgaImageStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !FpgaInfo {
    var result: FpgaInfo = undefined;
    result.fpgas = null;
    result.total_fpga_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fpgas")) {
                    result.fpgas = try deserializeFpgaDeviceInfoList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "totalFpgaMemoryInMiB")) {
                    result.total_fpga_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGpuDeviceInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !GpuDeviceInfo {
    var result: GpuDeviceInfo = undefined;
    result.count = null;
    result.gpu_partition_size = null;
    result.logical_gpu_count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    result.workloads = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "gpuPartitionSize")) {
                    result.gpu_partition_size = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "logicalGpuCount")) {
                    result.logical_gpu_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeGpuDeviceMemoryInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "workloadSet")) {
                    result.workloads = try deserializeWorkloadsList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGpuDeviceMemoryInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !GpuDeviceMemoryInfo {
    _ = allocator;
    var result: GpuDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGpuInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !GpuInfo {
    var result: GpuInfo = undefined;
    result.gpus = null;
    result.total_gpu_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "gpus")) {
                    result.gpus = try deserializeGpuDeviceInfoList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "totalGpuMemoryInMiB")) {
                    result.total_gpu_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGroupIdentifier(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !GroupIdentifier {
    var result: GroupIdentifier = undefined;
    result.group_id = null;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHibernationOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !HibernationOptions {
    _ = allocator;
    var result: HibernationOptions = undefined;
    result.configured = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configured")) {
                    result.configured = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHistoryRecord(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !HistoryRecord {
    var result: HistoryRecord = undefined;
    result.event_information = null;
    result.event_type = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eventInformation")) {
                    result.event_information = try deserializeEventInformation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = EventType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHistoryRecordEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !HistoryRecordEntry {
    var result: HistoryRecordEntry = undefined;
    result.event_information = null;
    result.event_type = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eventInformation")) {
                    result.event_information = try deserializeEventInformation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = FleetEventType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHost(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Host {
    var result: Host = undefined;
    result.allocation_time = null;
    result.allows_multiple_instance_types = null;
    result.asset_id = null;
    result.auto_placement = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.available_capacity = null;
    result.client_token = null;
    result.host_id = null;
    result.host_maintenance = null;
    result.host_properties = null;
    result.host_recovery = null;
    result.host_reservation_id = null;
    result.instances = null;
    result.member_of_service_linked_resource_group = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.release_time = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationTime")) {
                    result.allocation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "allowsMultipleInstanceTypes")) {
                    result.allows_multiple_instance_types = AllowsMultipleInstanceTypes.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "assetId")) {
                    result.asset_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "autoPlacement")) {
                    result.auto_placement = AutoPlacement.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableCapacity")) {
                    result.available_capacity = try deserializeAvailableCapacity(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostMaintenance")) {
                    result.host_maintenance = HostMaintenance.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostProperties")) {
                    result.host_properties = try deserializeHostProperties(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "hostRecovery")) {
                    result.host_recovery = HostRecovery.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostReservationId")) {
                    result.host_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instances")) {
                    result.instances = try deserializeHostInstanceList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "memberOfServiceLinkedResourceGroup")) {
                    result.member_of_service_linked_resource_group = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "releaseTime")) {
                    result.release_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = AllocationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !HostInstance {
    var result: HostInstance = undefined;
    result.instance_id = null;
    result.instance_type = null;
    result.owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostOffering(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !HostOffering {
    var result: HostOffering = undefined;
    result.currency_code = null;
    result.duration = null;
    result.hourly_price = null;
    result.instance_family = null;
    result.offering_id = null;
    result.payment_option = null;
    result.upfront_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = CurrencyCodeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringId")) {
                    result.offering_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "paymentOption")) {
                    result.payment_option = PaymentOption.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontPrice")) {
                    result.upfront_price = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostProperties(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !HostProperties {
    var result: HostProperties = undefined;
    result.cores = null;
    result.instance_family = null;
    result.instance_type = null;
    result.sockets = null;
    result.total_v_cpus = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cores")) {
                    result.cores = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sockets")) {
                    result.sockets = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalVCpus")) {
                    result.total_v_cpus = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostReservation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !HostReservation {
    var result: HostReservation = undefined;
    result.count = null;
    result.currency_code = null;
    result.duration = null;
    result.end = null;
    result.host_id_set = null;
    result.host_reservation_id = null;
    result.hourly_price = null;
    result.instance_family = null;
    result.offering_id = null;
    result.payment_option = null;
    result.start = null;
    result.state = null;
    result.tags = null;
    result.upfront_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = CurrencyCodeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "end")) {
                    result.end = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "hostIdSet")) {
                    result.host_id_set = try deserializeResponseHostIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "hostReservationId")) {
                    result.host_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringId")) {
                    result.offering_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "paymentOption")) {
                    result.payment_option = PaymentOption.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "start")) {
                    result.start = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = ReservationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "upfrontPrice")) {
                    result.upfront_price = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIKEVersionsListValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IKEVersionsListValue {
    var result: IKEVersionsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIamInstanceProfile(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IamInstanceProfile {
    var result: IamInstanceProfile = undefined;
    result.arn = null;
    result.id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIamInstanceProfileAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IamInstanceProfileAssociation {
    var result: IamInstanceProfileAssociation = undefined;
    result.association_id = null;
    result.iam_instance_profile = null;
    result.instance_id = null;
    result.state = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfile(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IamInstanceProfileAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIamInstanceProfileSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IamInstanceProfileSpecification {
    var result: IamInstanceProfileSpecification = undefined;
    result.arn = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIcmpTypeCode(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IcmpTypeCode {
    _ = allocator;
    var result: IcmpTypeCode = undefined;
    result.code = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIdFormat(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IdFormat {
    var result: IdFormat = undefined;
    result.deadline = null;
    result.resource = null;
    result.use_long_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deadline")) {
                    result.deadline = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "resource")) {
                    result.resource = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "useLongIds")) {
                    result.use_long_ids = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImage(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Image {
    var result: Image = undefined;
    result.architecture = null;
    result.block_device_mappings = null;
    result.boot_mode = null;
    result.creation_date = null;
    result.deprecation_time = null;
    result.deregistration_protection = null;
    result.description = null;
    result.ena_support = null;
    result.free_tier_eligible = null;
    result.hypervisor = null;
    result.image_allowed = null;
    result.image_id = null;
    result.image_location = null;
    result.image_owner_alias = null;
    result.image_type = null;
    result.imds_support = null;
    result.kernel_id = null;
    result.last_launched_time = null;
    result.name = null;
    result.owner_id = null;
    result.platform = null;
    result.platform_details = null;
    result.product_codes = null;
    result.public = null;
    result.ramdisk_id = null;
    result.root_device_name = null;
    result.root_device_type = null;
    result.source_image_id = null;
    result.source_image_region = null;
    result.source_instance_id = null;
    result.sriov_net_support = null;
    result.state = null;
    result.state_reason = null;
    result.tags = null;
    result.tpm_support = null;
    result.usage_operation = null;
    result.virtualization_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "architecture")) {
                    result.architecture = ArchitectureValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "bootMode")) {
                    result.boot_mode = BootModeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "creationDate")) {
                    result.creation_date = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deprecationTime")) {
                    result.deprecation_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deregistrationProtection")) {
                    result.deregistration_protection = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enaSupport")) {
                    result.ena_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "freeTierEligible")) {
                    result.free_tier_eligible = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = HypervisorType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageAllowed")) {
                    result.image_allowed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageLocation")) {
                    result.image_location = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerAlias")) {
                    result.image_owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageType")) {
                    result.image_type = ImageTypeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imdsSupport")) {
                    result.imds_support = ImdsSupportValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastLaunchedTime")) {
                    result.last_launched_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = PlatformValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platformDetails")) {
                    result.platform_details = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "isPublic")) {
                    result.public = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceName")) {
                    result.root_device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceType")) {
                    result.root_device_type = DeviceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageId")) {
                    result.source_image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageRegion")) {
                    result.source_image_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceInstanceId")) {
                    result.source_instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sriovNetSupport")) {
                    result.sriov_net_support = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageState")) {
                    result.state = ImageState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try deserializeStateReason(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tpmSupport")) {
                    result.tpm_support = TpmSupportValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageOperation")) {
                    result.usage_operation = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualizationType")) {
                    result.virtualization_type = VirtualizationType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageAncestryEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageAncestryEntry {
    var result: ImageAncestryEntry = undefined;
    result.creation_date = null;
    result.image_id = null;
    result.image_owner_alias = null;
    result.source_image_id = null;
    result.source_image_region = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationDate")) {
                    result.creation_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerAlias")) {
                    result.image_owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageId")) {
                    result.source_image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageRegion")) {
                    result.source_image_region = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageCriterion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageCriterion {
    var result: ImageCriterion = undefined;
    result.creation_date_condition = null;
    result.deprecation_time_condition = null;
    result.image_names = null;
    result.image_providers = null;
    result.marketplace_product_codes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationDateCondition")) {
                    result.creation_date_condition = try deserializeCreationDateCondition(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "deprecationTimeCondition")) {
                    result.deprecation_time_condition = try deserializeDeprecationTimeCondition(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "imageNameSet")) {
                    result.image_names = try deserializeImageNameList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "imageProviderSet")) {
                    result.image_providers = try deserializeImageProviderList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "marketplaceProductCodeSet")) {
                    result.marketplace_product_codes = try deserializeMarketplaceProductCodeList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageMetadata(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageMetadata {
    var result: ImageMetadata = undefined;
    result.creation_date = null;
    result.deprecation_time = null;
    result.image_allowed = null;
    result.image_id = null;
    result.image_owner_alias = null;
    result.is_public = null;
    result.name = null;
    result.owner_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationDate")) {
                    result.creation_date = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deprecationTime")) {
                    result.deprecation_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageAllowed")) {
                    result.image_allowed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerAlias")) {
                    result.image_owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPublic")) {
                    result.is_public = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageState")) {
                    result.state = ImageState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageRecycleBinInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageRecycleBinInfo {
    var result: ImageRecycleBinInfo = undefined;
    result.description = null;
    result.image_id = null;
    result.name = null;
    result.recycle_bin_enter_time = null;
    result.recycle_bin_exit_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recycleBinEnterTime")) {
                    result.recycle_bin_enter_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recycleBinExitTime")) {
                    result.recycle_bin_exit_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageReference(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageReference {
    var result: ImageReference = undefined;
    result.arn = null;
    result.image_id = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = ImageReferenceResourceType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageReport(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageUsageReport {
    var result: ImageUsageReport = undefined;
    result.account_ids = null;
    result.creation_time = null;
    result.expiration_time = null;
    result.image_id = null;
    result.report_id = null;
    result.resource_types = null;
    result.state = null;
    result.state_reason = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountIdSet")) {
                    result.account_ids = try deserializeUserIdList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "expirationTime")) {
                    result.expiration_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reportId")) {
                    result.report_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTypeSet")) {
                    result.resource_types = try deserializeImageUsageResourceTypeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageReportEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageUsageReportEntry {
    var result: ImageUsageReportEntry = undefined;
    result.account_id = null;
    result.image_id = null;
    result.report_creation_time = null;
    result.report_id = null;
    result.resource_type = null;
    result.usage_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reportCreationTime")) {
                    result.report_creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reportId")) {
                    result.report_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageCount")) {
                    result.usage_count = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageResourceType(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageUsageResourceType {
    var result: ImageUsageResourceType = undefined;
    result.resource_type = null;
    result.resource_type_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTypeOptionSet")) {
                    result.resource_type_options = try deserializeImageUsageResourceTypeOptionList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageResourceTypeOption(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImageUsageResourceTypeOption {
    var result: ImageUsageResourceTypeOption = undefined;
    result.option_name = null;
    result.option_values = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "optionName")) {
                    result.option_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "optionValueSet")) {
                    result.option_values = try deserializeImageUsageResourceTypeOptionValuesList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportImageLicenseConfigurationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImportImageLicenseConfigurationResponse {
    var result: ImportImageLicenseConfigurationResponse = undefined;
    result.license_configuration_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "licenseConfigurationArn")) {
                    result.license_configuration_arn = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportImageTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImportImageTask {
    var result: ImportImageTask = undefined;
    result.architecture = null;
    result.boot_mode = null;
    result.description = null;
    result.encrypted = null;
    result.hypervisor = null;
    result.image_id = null;
    result.import_task_id = null;
    result.kms_key_id = null;
    result.license_specifications = null;
    result.license_type = null;
    result.platform = null;
    result.progress = null;
    result.snapshot_details = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    result.usage_operation = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "architecture")) {
                    result.architecture = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bootMode")) {
                    result.boot_mode = BootModeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importTaskId")) {
                    result.import_task_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "licenseSpecifications")) {
                    result.license_specifications = try deserializeImportImageLicenseSpecificationListResponse(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "licenseType")) {
                    result.license_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotDetailSet")) {
                    result.snapshot_details = try deserializeSnapshotDetailList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "usageOperation")) {
                    result.usage_operation = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportInstanceTaskDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImportInstanceTaskDetails {
    var result: ImportInstanceTaskDetails = undefined;
    result.description = null;
    result.instance_id = null;
    result.platform = null;
    result.volumes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = PlatformValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumes")) {
                    result.volumes = try deserializeImportInstanceVolumeDetailSet(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportInstanceVolumeDetailItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImportInstanceVolumeDetailItem {
    var result: ImportInstanceVolumeDetailItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.bytes_converted = null;
    result.description = null;
    result.image = null;
    result.status = null;
    result.status_message = null;
    result.volume = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bytesConverted")) {
                    result.bytes_converted = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "image")) {
                    result.image = try deserializeDiskImageDescription(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volume")) {
                    result.volume = try deserializeDiskImageVolumeDescription(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportSnapshotTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImportSnapshotTask {
    var result: ImportSnapshotTask = undefined;
    result.description = null;
    result.import_task_id = null;
    result.snapshot_task_detail = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importTaskId")) {
                    result.import_task_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotTaskDetail")) {
                    result.snapshot_task_detail = try deserializeSnapshotTaskDetail(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportVolumeTaskDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ImportVolumeTaskDetails {
    var result: ImportVolumeTaskDetails = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.bytes_converted = null;
    result.description = null;
    result.image = null;
    result.volume = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bytesConverted")) {
                    result.bytes_converted = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "image")) {
                    result.image = try deserializeDiskImageDescription(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "volume")) {
                    result.volume = try deserializeDiskImageVolumeDescription(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInferenceAcceleratorInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InferenceAcceleratorInfo {
    var result: InferenceAcceleratorInfo = undefined;
    result.accelerators = null;
    result.total_inference_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accelerators")) {
                    result.accelerators = try deserializeInferenceDeviceInfoList(allocator, reader, "member");
                } else if (std.mem.eql(u8, e.local, "totalInferenceMemoryInMiB")) {
                    result.total_inference_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInferenceDeviceInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InferenceDeviceInfo {
    var result: InferenceDeviceInfo = undefined;
    result.count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeInferenceDeviceMemoryInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInferenceDeviceMemoryInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InferenceDeviceMemoryInfo {
    _ = allocator;
    var result: InferenceDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInitializationStatusDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InitializationStatusDetails {
    _ = allocator;
    var result: InitializationStatusDetails = undefined;
    result.estimated_time_to_complete_in_seconds = null;
    result.initialization_type = null;
    result.progress = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "estimatedTimeToCompleteInSeconds")) {
                    result.estimated_time_to_complete_in_seconds = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "initializationType")) {
                    result.initialization_type = InitializationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Instance {
    @setEvalBranchQuota(2000);
    var result: Instance = undefined;
    result.ami_launch_index = null;
    result.architecture = null;
    result.block_device_mappings = null;
    result.boot_mode = null;
    result.capacity_block_id = null;
    result.capacity_reservation_id = null;
    result.capacity_reservation_specification = null;
    result.client_token = null;
    result.cpu_options = null;
    result.current_instance_boot_mode = null;
    result.ebs_optimized = null;
    result.elastic_gpu_associations = null;
    result.elastic_inference_accelerator_associations = null;
    result.ena_support = null;
    result.enclave_options = null;
    result.hibernation_options = null;
    result.hypervisor = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_id = null;
    result.instance_lifecycle = null;
    result.instance_type = null;
    result.ipv_6_address = null;
    result.kernel_id = null;
    result.key_name = null;
    result.launch_time = null;
    result.licenses = null;
    result.maintenance_options = null;
    result.metadata_options = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.network_performance_options = null;
    result.operator = null;
    result.outpost_arn = null;
    result.placement = null;
    result.platform = null;
    result.platform_details = null;
    result.private_dns_name = null;
    result.private_dns_name_options = null;
    result.private_ip_address = null;
    result.product_codes = null;
    result.public_dns_name = null;
    result.public_ip_address = null;
    result.ramdisk_id = null;
    result.root_device_name = null;
    result.root_device_type = null;
    result.security_groups = null;
    result.source_dest_check = null;
    result.spot_instance_request_id = null;
    result.sriov_net_support = null;
    result.state = null;
    result.state_reason = null;
    result.state_transition_reason = null;
    result.subnet_id = null;
    result.tags = null;
    result.tpm_support = null;
    result.usage_operation = null;
    result.usage_operation_update_time = null;
    result.virtualization_type = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amiLaunchIndex")) {
                    result.ami_launch_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "architecture")) {
                    result.architecture = ArchitectureValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeInstanceBlockDeviceMappingList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "bootMode")) {
                    result.boot_mode = BootModeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationSpecification")) {
                    result.capacity_reservation_specification = try deserializeCapacityReservationSpecificationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cpuOptions")) {
                    result.cpu_options = try deserializeCpuOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "currentInstanceBootMode")) {
                    result.current_instance_boot_mode = InstanceBootModeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "elasticGpuAssociationSet")) {
                    result.elastic_gpu_associations = try deserializeElasticGpuAssociationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationSet")) {
                    result.elastic_inference_accelerator_associations = try deserializeElasticInferenceAcceleratorAssociationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "enaSupport")) {
                    result.ena_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enclaveOptions")) {
                    result.enclave_options = try deserializeEnclaveOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "hibernationOptions")) {
                    result.hibernation_options = try deserializeHibernationOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = HypervisorType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfile(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceLifecycle")) {
                    result.instance_lifecycle = InstanceLifecycleType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTime")) {
                    result.launch_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "licenseSet")) {
                    result.licenses = try deserializeLicenseList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "maintenanceOptions")) {
                    result.maintenance_options = try deserializeInstanceMaintenanceOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "metadataOptions")) {
                    result.metadata_options = try deserializeInstanceMetadataOptionsResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeMonitoring(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeInstanceNetworkInterfaceList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkPerformanceOptions")) {
                    result.network_performance_options = try deserializeInstanceNetworkPerformanceOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializePlacement(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = PlatformValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platformDetails")) {
                    result.platform_details = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameOptions")) {
                    result.private_dns_name_options = try deserializePrivateDnsNameOptionsResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.public_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddress")) {
                    result.public_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceName")) {
                    result.root_device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceType")) {
                    result.root_device_type = DeviceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.security_groups = try deserializeGroupIdentifierList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourceDestCheck")) {
                    result.source_dest_check = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sriovNetSupport")) {
                    result.sriov_net_support = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceState")) {
                    result.state = try deserializeInstanceState(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try deserializeStateReason(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "reason")) {
                    result.state_transition_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tpmSupport")) {
                    result.tpm_support = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageOperation")) {
                    result.usage_operation = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageOperationUpdateTime")) {
                    result.usage_operation_update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "virtualizationType")) {
                    result.virtualization_type = VirtualizationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceAttachmentEnaSrdSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceAttachmentEnaSrdSpecification {
    var result: InstanceAttachmentEnaSrdSpecification = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeInstanceAttachmentEnaSrdUdpSpecification(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceAttachmentEnaSrdUdpSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceAttachmentEnaSrdUdpSpecification {
    _ = allocator;
    var result: InstanceAttachmentEnaSrdUdpSpecification = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceBlockDeviceMapping(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceBlockDeviceMapping {
    var result: InstanceBlockDeviceMapping = undefined;
    result.device_name = null;
    result.ebs = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeEbsInstanceBlockDevice(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceCapacity(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceCapacity {
    var result: InstanceCapacity = undefined;
    result.available_capacity = null;
    result.instance_type = null;
    result.total_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availableCapacity")) {
                    result.available_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalCapacity")) {
                    result.total_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceConnectEndpointDnsNames(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceConnectEndpointDnsNames {
    var result: InstanceConnectEndpointDnsNames = undefined;
    result.dns_name = null;
    result.fips_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fipsDnsName")) {
                    result.fips_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceConnectEndpointPublicDnsNames(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceConnectEndpointPublicDnsNames {
    var result: InstanceConnectEndpointPublicDnsNames = undefined;
    result.dualstack = null;
    result.ipv_4 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dualstack")) {
                    result.dualstack = try deserializeInstanceConnectEndpointDnsNames(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ipv4")) {
                    result.ipv_4 = try deserializeInstanceConnectEndpointDnsNames(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceCount(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceCount {
    _ = allocator;
    var result: InstanceCount = undefined;
    result.instance_count = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = ListingState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceCreditSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceCreditSpecification {
    var result: InstanceCreditSpecification = undefined;
    result.cpu_credits = null;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpuCredits")) {
                    result.cpu_credits = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindow(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceEventWindow {
    var result: InstanceEventWindow = undefined;
    result.association_target = null;
    result.cron_expression = null;
    result.instance_event_window_id = null;
    result.name = null;
    result.state = null;
    result.tags = null;
    result.time_ranges = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationTarget")) {
                    result.association_target = try deserializeInstanceEventWindowAssociationTarget(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "cronExpression")) {
                    result.cron_expression = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceEventWindowId")) {
                    result.instance_event_window_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = InstanceEventWindowState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "timeRangeSet")) {
                    result.time_ranges = try deserializeInstanceEventWindowTimeRangeList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindowAssociationTarget(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceEventWindowAssociationTarget {
    var result: InstanceEventWindowAssociationTarget = undefined;
    result.dedicated_host_ids = null;
    result.instance_ids = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dedicatedHostIdSet")) {
                    result.dedicated_host_ids = try deserializeDedicatedHostIdList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "instanceIdSet")) {
                    result.instance_ids = try deserializeInstanceIdList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindowStateChange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceEventWindowStateChange {
    var result: InstanceEventWindowStateChange = undefined;
    result.instance_event_window_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceEventWindowId")) {
                    result.instance_event_window_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = InstanceEventWindowState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindowTimeRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceEventWindowTimeRange {
    _ = allocator;
    var result: InstanceEventWindowTimeRange = undefined;
    result.end_hour = null;
    result.end_week_day = null;
    result.start_hour = null;
    result.start_week_day = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endHour")) {
                    result.end_hour = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "endWeekDay")) {
                    result.end_week_day = WeekDay.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startHour")) {
                    result.start_hour = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "startWeekDay")) {
                    result.start_week_day = WeekDay.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceExportDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceExportDetails {
    var result: InstanceExportDetails = undefined;
    result.instance_id = null;
    result.target_environment = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetEnvironment")) {
                    result.target_environment = ExportEnvironment.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceFamilyCreditSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceFamilyCreditSpecification {
    var result: InstanceFamilyCreditSpecification = undefined;
    result.cpu_credits = null;
    result.instance_family = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpuCredits")) {
                    result.cpu_credits = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = UnlimitedSupportedInstanceFamily.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceImageMetadata(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceImageMetadata {
    @setEvalBranchQuota(2000);
    var result: InstanceImageMetadata = undefined;
    result.availability_zone = null;
    result.image_metadata = null;
    result.instance_id = null;
    result.instance_type = null;
    result.launch_time = null;
    result.operator = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.zone_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageMetadata")) {
                    result.image_metadata = try deserializeImageMetadata(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTime")) {
                    result.launch_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceOwnerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceState")) {
                    result.state = try deserializeInstanceState(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "zoneId")) {
                    result.zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceIpv4Prefix(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceIpv4Prefix {
    var result: InstanceIpv4Prefix = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv4Prefix")) {
                    result.ipv_4_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceIpv6Address(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceIpv6Address {
    var result: InstanceIpv6Address = undefined;
    result.ipv_6_address = null;
    result.is_primary_ipv_6 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPrimaryIpv6")) {
                    result.is_primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceIpv6Prefix(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceIpv6Prefix {
    var result: InstanceIpv6Prefix = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Prefix")) {
                    result.ipv_6_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMaintenanceOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceMaintenanceOptions {
    _ = allocator;
    var result: InstanceMaintenanceOptions = undefined;
    result.auto_recovery = null;
    result.reboot_migration = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoRecovery")) {
                    result.auto_recovery = InstanceAutoRecoveryState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rebootMigration")) {
                    result.reboot_migration = InstanceRebootMigrationState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMetadataDefaultsResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceMetadataDefaultsResponse {
    var result: InstanceMetadataDefaultsResponse = undefined;
    result.http_endpoint = null;
    result.http_put_response_hop_limit = null;
    result.http_tokens = null;
    result.instance_metadata_tags = null;
    result.managed_by = null;
    result.managed_exception_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "httpEndpoint")) {
                    result.http_endpoint = InstanceMetadataEndpointState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpPutResponseHopLimit")) {
                    result.http_put_response_hop_limit = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "httpTokens")) {
                    result.http_tokens = HttpTokensState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMetadataTags")) {
                    result.instance_metadata_tags = InstanceMetadataTagsState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "managedBy")) {
                    result.managed_by = ManagedBy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "managedExceptionMessage")) {
                    result.managed_exception_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMetadataOptionsResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceMetadataOptionsResponse {
    _ = allocator;
    var result: InstanceMetadataOptionsResponse = undefined;
    result.http_endpoint = null;
    result.http_protocol_ipv_6 = null;
    result.http_put_response_hop_limit = null;
    result.http_tokens = null;
    result.instance_metadata_tags = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "httpEndpoint")) {
                    result.http_endpoint = InstanceMetadataEndpointState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpProtocolIpv6")) {
                    result.http_protocol_ipv_6 = InstanceMetadataProtocolState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpPutResponseHopLimit")) {
                    result.http_put_response_hop_limit = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "httpTokens")) {
                    result.http_tokens = HttpTokensState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMetadataTags")) {
                    result.instance_metadata_tags = InstanceMetadataTagsState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = InstanceMetadataOptionsState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMonitoring(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceMonitoring {
    var result: InstanceMonitoring = undefined;
    result.instance_id = null;
    result.monitoring = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeMonitoring(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterface(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceNetworkInterface {
    var result: InstanceNetworkInterface = undefined;
    result.association = null;
    result.attachment = null;
    result.connection_tracking_configuration = null;
    result.description = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_addresses = null;
    result.ipv_6_prefixes = null;
    result.mac_address = null;
    result.network_interface_id = null;
    result.operator = null;
    result.owner_id = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.source_dest_check = null;
    result.status = null;
    result.subnet_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeInstanceNetworkInterfaceAssociation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "attachment")) {
                    result.attachment = try deserializeInstanceNetworkInterfaceAttachment(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "connectionTrackingConfiguration")) {
                    result.connection_tracking_configuration = try deserializeConnectionTrackingSpecificationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "interfaceType")) {
                    result.interface_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeInstanceIpv4PrefixList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeInstanceIpv6AddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeInstanceIpv6PrefixList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "macAddress")) {
                    result.mac_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializeInstancePrivateIpAddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourceDestCheck")) {
                    result.source_dest_check = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = NetworkInterfaceStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterfaceAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceNetworkInterfaceAssociation {
    var result: InstanceNetworkInterfaceAssociation = undefined;
    result.carrier_ip = null;
    result.customer_owned_ip = null;
    result.ip_owner_id = null;
    result.public_dns_name = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierIp")) {
                    result.carrier_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIp")) {
                    result.customer_owned_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipOwnerId")) {
                    result.ip_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDnsName")) {
                    result.public_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterfaceAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceNetworkInterfaceAttachment {
    var result: InstanceNetworkInterfaceAttachment = undefined;
    result.attachment_id = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.network_card_index = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentId")) {
                    result.attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeInstanceAttachmentEnaSrdSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = AttachmentStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterfaceSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceNetworkInterfaceSpecification {
    var result: InstanceNetworkInterfaceSpecification = undefined;
    result.associate_carrier_ip_address = null;
    result.associate_public_ip_address = null;
    result.connection_tracking_specification = null;
    result.delete_on_termination = null;
    result.description = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefix_count = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_address_count = null;
    result.ipv_6_addresses = null;
    result.ipv_6_prefix_count = null;
    result.ipv_6_prefixes = null;
    result.network_card_index = null;
    result.network_interface_id = null;
    result.primary_ipv_6 = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.secondary_private_ip_address_count = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "AssociateCarrierIpAddress")) {
                    result.associate_carrier_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "associatePublicIpAddress")) {
                    result.associate_public_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ConnectionTrackingSpecification")) {
                    result.connection_tracking_specification = try deserializeConnectionTrackingSpecificationRequest(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "EnaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "EnaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeEnaSrdSpecificationRequest(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "SecurityGroupId")) {
                    result.groups = try deserializeSecurityGroupIdStringList(allocator, reader, "SecurityGroupId");
                } else if (std.mem.eql(u8, e.local, "InterfaceType")) {
                    result.interface_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "Ipv4PrefixCount")) {
                    result.ipv_4_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "Ipv4Prefix")) {
                    result.ipv_4_prefixes = try deserializeIpv4PrefixList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6AddressCount")) {
                    result.ipv_6_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeInstanceIpv6AddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "Ipv6PrefixCount")) {
                    result.ipv_6_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "Ipv6Prefix")) {
                    result.ipv_6_prefixes = try deserializeIpv6PrefixList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "NetworkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "PrimaryIpv6")) {
                    result.primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializePrivateIpAddressSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "secondaryPrivateIpAddressCount")) {
                    result.secondary_private_ip_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkPerformanceOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceNetworkPerformanceOptions {
    _ = allocator;
    var result: InstanceNetworkPerformanceOptions = undefined;
    result.bandwidth_weighting = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bandwidthWeighting")) {
                    result.bandwidth_weighting = InstanceBandwidthWeighting.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstancePrivateIpAddress(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstancePrivateIpAddress {
    var result: InstancePrivateIpAddress = undefined;
    result.association = null;
    result.primary = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeInstanceNetworkInterfaceAssociation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "primary")) {
                    result.primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceRequirements(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceRequirements {
    var result: InstanceRequirements = undefined;
    result.accelerator_count = null;
    result.accelerator_manufacturers = null;
    result.accelerator_names = null;
    result.accelerator_total_memory_mi_b = null;
    result.accelerator_types = null;
    result.allowed_instance_types = null;
    result.bare_metal = null;
    result.baseline_ebs_bandwidth_mbps = null;
    result.baseline_performance_factors = null;
    result.burstable_performance = null;
    result.cpu_manufacturers = null;
    result.excluded_instance_types = null;
    result.instance_generations = null;
    result.local_storage = null;
    result.local_storage_types = null;
    result.max_spot_price_as_percentage_of_optimal_on_demand_price = null;
    result.memory_gi_b_per_v_cpu = null;
    result.memory_mi_b = null;
    result.network_bandwidth_gbps = null;
    result.network_interface_count = null;
    result.on_demand_max_price_percentage_over_lowest_price = null;
    result.require_encryption_in_transit = null;
    result.require_hibernate_support = null;
    result.spot_max_price_percentage_over_lowest_price = null;
    result.total_local_storage_gb = null;
    result.v_cpu_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceleratorCount")) {
                    result.accelerator_count = try deserializeAcceleratorCount(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "acceleratorManufacturerSet")) {
                    result.accelerator_manufacturers = try deserializeAcceleratorManufacturerSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "acceleratorNameSet")) {
                    result.accelerator_names = try deserializeAcceleratorNameSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "acceleratorTotalMemoryMiB")) {
                    result.accelerator_total_memory_mi_b = try deserializeAcceleratorTotalMemoryMiB(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "acceleratorTypeSet")) {
                    result.accelerator_types = try deserializeAcceleratorTypeSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "allowedInstanceTypeSet")) {
                    result.allowed_instance_types = try deserializeAllowedInstanceTypeSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "bareMetal")) {
                    result.bare_metal = BareMetal.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "baselineEbsBandwidthMbps")) {
                    result.baseline_ebs_bandwidth_mbps = try deserializeBaselineEbsBandwidthMbps(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "baselinePerformanceFactors")) {
                    result.baseline_performance_factors = try deserializeBaselinePerformanceFactors(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "burstablePerformance")) {
                    result.burstable_performance = BurstablePerformance.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cpuManufacturerSet")) {
                    result.cpu_manufacturers = try deserializeCpuManufacturerSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "excludedInstanceTypeSet")) {
                    result.excluded_instance_types = try deserializeExcludedInstanceTypeSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "instanceGenerationSet")) {
                    result.instance_generations = try deserializeInstanceGenerationSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "localStorage")) {
                    result.local_storage = LocalStorage.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localStorageTypeSet")) {
                    result.local_storage_types = try deserializeLocalStorageTypeSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "maxSpotPriceAsPercentageOfOptimalOnDemandPrice")) {
                    result.max_spot_price_as_percentage_of_optimal_on_demand_price = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "memoryGiBPerVCpu")) {
                    result.memory_gi_b_per_v_cpu = try deserializeMemoryGiBPerVCpu(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "memoryMiB")) {
                    result.memory_mi_b = try deserializeMemoryMiB(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkBandwidthGbps")) {
                    result.network_bandwidth_gbps = try deserializeNetworkBandwidthGbps(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceCount")) {
                    result.network_interface_count = try deserializeNetworkInterfaceCount(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "onDemandMaxPricePercentageOverLowestPrice")) {
                    result.on_demand_max_price_percentage_over_lowest_price = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "requireEncryptionInTransit")) {
                    result.require_encryption_in_transit = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "requireHibernateSupport")) {
                    result.require_hibernate_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotMaxPricePercentageOverLowestPrice")) {
                    result.spot_max_price_percentage_over_lowest_price = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalLocalStorageGB")) {
                    result.total_local_storage_gb = try deserializeTotalLocalStorageGB(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vCpuCount")) {
                    result.v_cpu_count = try deserializeVCpuCountRange(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceState(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceState {
    _ = allocator;
    var result: InstanceState = undefined;
    result.code = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = InstanceStateName.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStateChange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceStateChange {
    var result: InstanceStateChange = undefined;
    result.current_state = null;
    result.instance_id = null;
    result.previous_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currentState")) {
                    result.current_state = try deserializeInstanceState(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousState")) {
                    result.previous_state = try deserializeInstanceState(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceStatus {
    var result: InstanceStatus = undefined;
    result.attached_ebs_status = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.events = null;
    result.instance_id = null;
    result.instance_state = null;
    result.instance_status = null;
    result.operator = null;
    result.outpost_arn = null;
    result.system_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachedEbsStatus")) {
                    result.attached_ebs_status = try deserializeEbsStatusSummary(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventsSet")) {
                    result.events = try deserializeInstanceStatusEventList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceState")) {
                    result.instance_state = try deserializeInstanceState(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceStatus")) {
                    result.instance_status = try deserializeInstanceStatusSummary(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "systemStatus")) {
                    result.system_status = try deserializeInstanceStatusSummary(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatusDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceStatusDetails {
    _ = allocator;
    var result: InstanceStatusDetails = undefined;
    result.impaired_since = null;
    result.name = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "impairedSince")) {
                    result.impaired_since = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = StatusName.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = StatusType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatusEvent(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceStatusEvent {
    var result: InstanceStatusEvent = undefined;
    result.code = null;
    result.description = null;
    result.instance_event_id = null;
    result.not_after = null;
    result.not_before = null;
    result.not_before_deadline = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = EventCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceEventId")) {
                    result.instance_event_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "notAfter")) {
                    result.not_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "notBefore")) {
                    result.not_before = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "notBeforeDeadline")) {
                    result.not_before_deadline = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatusSummary(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceStatusSummary {
    var result: InstanceStatusSummary = undefined;
    result.details = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "details")) {
                    result.details = try deserializeInstanceStatusDetailsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = SummaryStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStorageInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceStorageInfo {
    var result: InstanceStorageInfo = undefined;
    result.disks = null;
    result.encryption_support = null;
    result.nvme_support = null;
    result.total_size_in_gb = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "disks")) {
                    result.disks = try deserializeDiskInfoList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "encryptionSupport")) {
                    result.encryption_support = InstanceStorageEncryptionSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nvmeSupport")) {
                    result.nvme_support = EphemeralNvmeSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalSizeInGB")) {
                    result.total_size_in_gb = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTagNotificationAttribute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceTagNotificationAttribute {
    var result: InstanceTagNotificationAttribute = undefined;
    result.include_all_tags_of_instance = null;
    result.instance_tag_keys = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "includeAllTagsOfInstance")) {
                    result.include_all_tags_of_instance = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "instanceTagKeySet")) {
                    result.instance_tag_keys = try deserializeInstanceTagKeySet(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTopology(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceTopology {
    var result: InstanceTopology = undefined;
    result.availability_zone = null;
    result.capacity_block_id = null;
    result.group_name = null;
    result.instance_id = null;
    result.instance_type = null;
    result.network_nodes = null;
    result.zone_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkNodeSet")) {
                    result.network_nodes = try deserializeNetworkNodesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "zoneId")) {
                    result.zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTypeInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceTypeInfo {
    @setEvalBranchQuota(2000);
    var result: InstanceTypeInfo = undefined;
    result.auto_recovery_supported = null;
    result.bare_metal = null;
    result.burstable_performance_supported = null;
    result.current_generation = null;
    result.dedicated_hosts_supported = null;
    result.ebs_info = null;
    result.fpga_info = null;
    result.free_tier_eligible = null;
    result.gpu_info = null;
    result.hibernation_supported = null;
    result.hypervisor = null;
    result.inference_accelerator_info = null;
    result.instance_storage_info = null;
    result.instance_storage_supported = null;
    result.instance_type = null;
    result.media_accelerator_info = null;
    result.memory_info = null;
    result.network_info = null;
    result.neuron_info = null;
    result.nitro_enclaves_support = null;
    result.nitro_tpm_info = null;
    result.nitro_tpm_support = null;
    result.phc_support = null;
    result.placement_group_info = null;
    result.processor_info = null;
    result.reboot_migration_support = null;
    result.supported_boot_modes = null;
    result.supported_root_device_types = null;
    result.supported_usage_classes = null;
    result.supported_virtualization_types = null;
    result.v_cpu_info = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoRecoverySupported")) {
                    result.auto_recovery_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "bareMetal")) {
                    result.bare_metal = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "burstablePerformanceSupported")) {
                    result.burstable_performance_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "currentGeneration")) {
                    result.current_generation = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "dedicatedHostsSupported")) {
                    result.dedicated_hosts_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsInfo")) {
                    result.ebs_info = try deserializeEbsInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "fpgaInfo")) {
                    result.fpga_info = try deserializeFpgaInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "freeTierEligible")) {
                    result.free_tier_eligible = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "gpuInfo")) {
                    result.gpu_info = try deserializeGpuInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "hibernationSupported")) {
                    result.hibernation_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = InstanceTypeHypervisor.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "inferenceAcceleratorInfo")) {
                    result.inference_accelerator_info = try deserializeInferenceAcceleratorInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceStorageInfo")) {
                    result.instance_storage_info = try deserializeInstanceStorageInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceStorageSupported")) {
                    result.instance_storage_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "mediaAcceleratorInfo")) {
                    result.media_accelerator_info = try deserializeMediaAcceleratorInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeMemoryInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInfo")) {
                    result.network_info = try deserializeNetworkInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "neuronInfo")) {
                    result.neuron_info = try deserializeNeuronInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "nitroEnclavesSupport")) {
                    result.nitro_enclaves_support = NitroEnclavesSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nitroTpmInfo")) {
                    result.nitro_tpm_info = try deserializeNitroTpmInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "nitroTpmSupport")) {
                    result.nitro_tpm_support = NitroTpmSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phcSupport")) {
                    result.phc_support = PhcSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placementGroupInfo")) {
                    result.placement_group_info = try deserializePlacementGroupInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "processorInfo")) {
                    result.processor_info = try deserializeProcessorInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "rebootMigrationSupport")) {
                    result.reboot_migration_support = RebootMigrationSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "supportedBootModes")) {
                    result.supported_boot_modes = try deserializeBootModeTypeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "supportedRootDeviceTypes")) {
                    result.supported_root_device_types = try deserializeRootDeviceTypeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "supportedUsageClasses")) {
                    result.supported_usage_classes = try deserializeUsageClassTypeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "supportedVirtualizationTypes")) {
                    result.supported_virtualization_types = try deserializeVirtualizationTypeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vCpuInfo")) {
                    result.v_cpu_info = try deserializeVCpuInfo(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTypeInfoFromInstanceRequirements(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceTypeInfoFromInstanceRequirements {
    var result: InstanceTypeInfoFromInstanceRequirements = undefined;
    result.instance_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTypeOffering(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceTypeOffering {
    @setEvalBranchQuota(2000);
    var result: InstanceTypeOffering = undefined;
    result.instance_type = null;
    result.location = null;
    result.location_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "location")) {
                    result.location = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "locationType")) {
                    result.location_type = LocationType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceUsage(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InstanceUsage {
    var result: InstanceUsage = undefined;
    result.account_id = null;
    result.used_instance_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usedInstanceCount")) {
                    result.used_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInternetGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InternetGateway {
    var result: InternetGateway = undefined;
    result.attachments = null;
    result.internet_gateway_id = null;
    result.owner_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentSet")) {
                    result.attachments = try deserializeInternetGatewayAttachmentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "internetGatewayId")) {
                    result.internet_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInternetGatewayAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InternetGatewayAttachment {
    var result: InternetGatewayAttachment = undefined;
    result.state = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = AttachmentStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInterruptibleCapacityAllocation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InterruptibleCapacityAllocation {
    var result: InterruptibleCapacityAllocation = undefined;
    result.instance_count = null;
    result.interruptible_capacity_reservation_id = null;
    result.interruption_type = null;
    result.status = null;
    result.target_instance_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "interruptibleCapacityReservationId")) {
                    result.interruptible_capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "interruptionType")) {
                    result.interruption_type = InterruptionType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = InterruptibleCapacityReservationAllocationStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetInstanceCount")) {
                    result.target_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInterruptionInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !InterruptionInfo {
    var result: InterruptionInfo = undefined;
    result.interruption_type = null;
    result.source_capacity_reservation_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "interruptionType")) {
                    result.interruption_type = InterruptionType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceCapacityReservationId")) {
                    result.source_capacity_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpPermission(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpPermission {
    var result: IpPermission = undefined;
    result.from_port = null;
    result.ip_protocol = null;
    result.ip_ranges = null;
    result.ipv_6_ranges = null;
    result.prefix_list_ids = null;
    result.to_port = null;
    result.user_id_group_pairs = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipRanges")) {
                    result.ip_ranges = try deserializeIpRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Ranges")) {
                    result.ipv_6_ranges = try deserializeIpv6RangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "prefixListIds")) {
                    result.prefix_list_ids = try deserializePrefixListIdList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groups")) {
                    result.user_id_group_pairs = try deserializeUserIdGroupPairList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpRange {
    var result: IpRange = undefined;
    result.cidr_ip = null;
    result.description = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIp")) {
                    result.cidr_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpam(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipam {
    var result: Ipam = undefined;
    result.default_resource_discovery_association_id = null;
    result.default_resource_discovery_id = null;
    result.description = null;
    result.enable_private_gua = null;
    result.ipam_arn = null;
    result.ipam_id = null;
    result.ipam_region = null;
    result.metered_account = null;
    result.operating_regions = null;
    result.owner_id = null;
    result.private_default_scope_id = null;
    result.public_default_scope_id = null;
    result.resource_discovery_association_count = null;
    result.scope_count = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.tier = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "defaultResourceDiscoveryAssociationId")) {
                    result.default_resource_discovery_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultResourceDiscoveryId")) {
                    result.default_resource_discovery_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enablePrivateGua")) {
                    result.enable_private_gua = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "meteredAccount")) {
                    result.metered_account = IpamMeteredAccount.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operatingRegionSet")) {
                    result.operating_regions = try deserializeIpamOperatingRegionSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDefaultScopeId")) {
                    result.private_default_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDefaultScopeId")) {
                    result.public_default_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceDiscoveryAssociationCount")) {
                    result.resource_discovery_association_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "scopeCount")) {
                    result.scope_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tier")) {
                    result.tier = IpamTier.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamAddressHistoryRecord(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamAddressHistoryRecord {
    var result: IpamAddressHistoryRecord = undefined;
    result.resource_cidr = null;
    result.resource_compliance_status = null;
    result.resource_id = null;
    result.resource_name = null;
    result.resource_overlap_status = null;
    result.resource_owner_id = null;
    result.resource_region = null;
    result.resource_type = null;
    result.sampled_end_time = null;
    result.sampled_start_time = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceCidr")) {
                    result.resource_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceComplianceStatus")) {
                    result.resource_compliance_status = IpamComplianceStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceName")) {
                    result.resource_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOverlapStatus")) {
                    result.resource_overlap_status = IpamOverlapStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = IpamAddressHistoryResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sampledEndTime")) {
                    result.sampled_end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "sampledStartTime")) {
                    result.sampled_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveredAccount(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamDiscoveredAccount {
    var result: IpamDiscoveredAccount = undefined;
    result.account_id = null;
    result.discovery_region = null;
    result.failure_reason = null;
    result.last_attempted_discovery_time = null;
    result.last_successful_discovery_time = null;
    result.organizational_unit_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "discoveryRegion")) {
                    result.discovery_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try deserializeIpamDiscoveryFailureReason(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "lastAttemptedDiscoveryTime")) {
                    result.last_attempted_discovery_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lastSuccessfulDiscoveryTime")) {
                    result.last_successful_discovery_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "organizationalUnitId")) {
                    result.organizational_unit_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveredPublicAddress(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamDiscoveredPublicAddress {
    var result: IpamDiscoveredPublicAddress = undefined;
    result.address = null;
    result.address_allocation_id = null;
    result.address_owner_id = null;
    result.address_region = null;
    result.address_type = null;
    result.association_status = null;
    result.instance_id = null;
    result.ipam_resource_discovery_id = null;
    result.network_border_group = null;
    result.network_interface_description = null;
    result.network_interface_id = null;
    result.public_ipv_4_pool_id = null;
    result.sample_time = null;
    result.security_groups = null;
    result.service = null;
    result.service_resource = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "address")) {
                    result.address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressAllocationId")) {
                    result.address_allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressOwnerId")) {
                    result.address_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressRegion")) {
                    result.address_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressType")) {
                    result.address_type = IpamPublicAddressType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationStatus")) {
                    result.association_status = IpamPublicAddressAssociationStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceDescription")) {
                    result.network_interface_description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv4PoolId")) {
                    result.public_ipv_4_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sampleTime")) {
                    result.sample_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "securityGroupSet")) {
                    result.security_groups = try deserializeIpamPublicAddressSecurityGroupList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "service")) {
                    result.service = IpamPublicAddressAwsService.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceResource")) {
                    result.service_resource = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tags")) {
                    result.tags = try deserializeIpamPublicAddressTags(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveredResourceCidr(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamDiscoveredResourceCidr {
    var result: IpamDiscoveredResourceCidr = undefined;
    result.availability_zone_id = null;
    result.ipam_resource_discovery_id = null;
    result.ip_source = null;
    result.ip_usage = null;
    result.network_interface_attachment_status = null;
    result.resource_cidr = null;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_region = null;
    result.resource_tags = null;
    result.resource_type = null;
    result.sample_time = null;
    result.subnet_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipSource")) {
                    result.ip_source = IpamResourceCidrIpSource.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipUsage")) {
                    result.ip_usage = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInterfaceAttachmentStatus")) {
                    result.network_interface_attachment_status = IpamNetworkInterfaceAttachmentStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceCidr")) {
                    result.resource_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTagSet")) {
                    result.resource_tags = try deserializeIpamResourceTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = IpamResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sampleTime")) {
                    result.sample_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveryFailureReason(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamDiscoveryFailureReason {
    var result: IpamDiscoveryFailureReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = IpamDiscoveryFailureCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamExternalResourceVerificationToken(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamExternalResourceVerificationToken {
    var result: IpamExternalResourceVerificationToken = undefined;
    result.ipam_arn = null;
    result.ipam_external_resource_verification_token_arn = null;
    result.ipam_external_resource_verification_token_id = null;
    result.ipam_id = null;
    result.ipam_region = null;
    result.not_after = null;
    result.state = null;
    result.status = null;
    result.tags = null;
    result.token_name = null;
    result.token_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamExternalResourceVerificationTokenArn")) {
                    result.ipam_external_resource_verification_token_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamExternalResourceVerificationTokenId")) {
                    result.ipam_external_resource_verification_token_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "notAfter")) {
                    result.not_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamExternalResourceVerificationTokenState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = TokenState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tokenName")) {
                    result.token_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenValue")) {
                    result.token_value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamOperatingRegion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamOperatingRegion {
    var result: IpamOperatingRegion = undefined;
    result.region_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamOrganizationalUnitExclusion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamOrganizationalUnitExclusion {
    var result: IpamOrganizationalUnitExclusion = undefined;
    result.organizations_entity_path = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "organizationsEntityPath")) {
                    result.organizations_entity_path = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicy(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPolicy {
    var result: IpamPolicy = undefined;
    result.ipam_id = null;
    result.ipam_policy_arn = null;
    result.ipam_policy_id = null;
    result.ipam_policy_region = null;
    result.owner_id = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPolicyArn")) {
                    result.ipam_policy_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPolicyId")) {
                    result.ipam_policy_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPolicyRegion")) {
                    result.ipam_policy_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamPolicyState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicyAllocationRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPolicyAllocationRule {
    var result: IpamPolicyAllocationRule = undefined;
    result.source_ipam_pool_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sourceIpamPoolId")) {
                    result.source_ipam_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicyDocument(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPolicyDocument {
    var result: IpamPolicyDocument = undefined;
    result.allocation_rules = null;
    result.ipam_policy_id = null;
    result.locale = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationRuleSet")) {
                    result.allocation_rules = try deserializeIpamPolicyAllocationRuleList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipamPolicyId")) {
                    result.ipam_policy_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "locale")) {
                    result.locale = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = IpamPolicyResourceType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicyOrganizationTarget(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPolicyOrganizationTarget {
    var result: IpamPolicyOrganizationTarget = undefined;
    result.organization_target_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "organizationTargetId")) {
                    result.organization_target_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPool(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPool {
    var result: IpamPool = undefined;
    result.address_family = null;
    result.allocation_default_netmask_length = null;
    result.allocation_max_netmask_length = null;
    result.allocation_min_netmask_length = null;
    result.allocation_resource_tags = null;
    result.auto_import = null;
    result.aws_service = null;
    result.description = null;
    result.ipam_arn = null;
    result.ipam_pool_arn = null;
    result.ipam_pool_id = null;
    result.ipam_region = null;
    result.ipam_scope_arn = null;
    result.ipam_scope_type = null;
    result.locale = null;
    result.owner_id = null;
    result.pool_depth = null;
    result.public_ip_source = null;
    result.publicly_advertisable = null;
    result.source_ipam_pool_id = null;
    result.source_resource = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressFamily")) {
                    result.address_family = AddressFamily.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "allocationDefaultNetmaskLength")) {
                    result.allocation_default_netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "allocationMaxNetmaskLength")) {
                    result.allocation_max_netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "allocationMinNetmaskLength")) {
                    result.allocation_min_netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "allocationResourceTagSet")) {
                    result.allocation_resource_tags = try deserializeIpamResourceTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "autoImport")) {
                    result.auto_import = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "awsService")) {
                    result.aws_service = IpamPoolAwsService.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolArn")) {
                    result.ipam_pool_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolId")) {
                    result.ipam_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeArn")) {
                    result.ipam_scope_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeType")) {
                    result.ipam_scope_type = IpamScopeType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "locale")) {
                    result.locale = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolDepth")) {
                    result.pool_depth = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "publicIpSource")) {
                    result.public_ip_source = IpamPoolPublicIpSource.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publiclyAdvertisable")) {
                    result.publicly_advertisable = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "sourceIpamPoolId")) {
                    result.source_ipam_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceResource")) {
                    result.source_resource = try deserializeIpamPoolSourceResource(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamPoolState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolAllocation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPoolAllocation {
    var result: IpamPoolAllocation = undefined;
    result.cidr = null;
    result.description = null;
    result.ipam_pool_allocation_id = null;
    result.resource_id = null;
    result.resource_owner = null;
    result.resource_region = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolAllocationId")) {
                    result.ipam_pool_allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = IpamPoolAllocationResourceType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolCidr(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPoolCidr {
    var result: IpamPoolCidr = undefined;
    result.cidr = null;
    result.failure_reason = null;
    result.ipam_pool_cidr_id = null;
    result.netmask_length = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try deserializeIpamPoolCidrFailureReason(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ipamPoolCidrId")) {
                    result.ipam_pool_cidr_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "netmaskLength")) {
                    result.netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamPoolCidrState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolCidrFailureReason(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPoolCidrFailureReason {
    var result: IpamPoolCidrFailureReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = IpamPoolCidrFailureCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolSourceResource(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPoolSourceResource {
    var result: IpamPoolSourceResource = undefined;
    result.resource_id = null;
    result.resource_owner = null;
    result.resource_region = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = IpamPoolSourceResourceType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolver(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPrefixListResolver {
    var result: IpamPrefixListResolver = undefined;
    result.address_family = null;
    result.description = null;
    result.ipam_arn = null;
    result.ipam_prefix_list_resolver_arn = null;
    result.ipam_prefix_list_resolver_id = null;
    result.ipam_region = null;
    result.last_version_creation_status = null;
    result.last_version_creation_status_message = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressFamily")) {
                    result.address_family = AddressFamily.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverArn")) {
                    result.ipam_prefix_list_resolver_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverId")) {
                    result.ipam_prefix_list_resolver_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastVersionCreationStatus")) {
                    result.last_version_creation_status = IpamPrefixListResolverVersionCreationStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastVersionCreationStatusMessage")) {
                    result.last_version_creation_status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamPrefixListResolverState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPrefixListResolverRule {
    var result: IpamPrefixListResolverRule = undefined;
    result.conditions = null;
    result.ipam_scope_id = null;
    result.resource_type = null;
    result.rule_type = null;
    result.static_cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "conditionSet")) {
                    result.conditions = try deserializeIpamPrefixListResolverRuleConditionSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipamScopeId")) {
                    result.ipam_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = IpamResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleType")) {
                    result.rule_type = IpamPrefixListResolverRuleType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staticCidr")) {
                    result.static_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverRuleCondition(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPrefixListResolverRuleCondition {
    var result: IpamPrefixListResolverRuleCondition = undefined;
    result.cidr = null;
    result.ipam_pool_id = null;
    result.operation = null;
    result.resource_id = null;
    result.resource_owner = null;
    result.resource_region = null;
    result.resource_tag = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolId")) {
                    result.ipam_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operation")) {
                    result.operation = IpamPrefixListResolverRuleConditionOperation.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTag")) {
                    result.resource_tag = try deserializeIpamResourceTag(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverTarget(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPrefixListResolverTarget {
    var result: IpamPrefixListResolverTarget = undefined;
    result.desired_version = null;
    result.ipam_prefix_list_resolver_id = null;
    result.ipam_prefix_list_resolver_target_arn = null;
    result.ipam_prefix_list_resolver_target_id = null;
    result.last_synced_version = null;
    result.owner_id = null;
    result.prefix_list_id = null;
    result.prefix_list_region = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.track_latest_version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "desiredVersion")) {
                    result.desired_version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverId")) {
                    result.ipam_prefix_list_resolver_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverTargetArn")) {
                    result.ipam_prefix_list_resolver_target_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverTargetId")) {
                    result.ipam_prefix_list_resolver_target_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastSyncedVersion")) {
                    result.last_synced_version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListRegion")) {
                    result.prefix_list_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamPrefixListResolverTargetState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trackLatestVersion")) {
                    result.track_latest_version = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverVersion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPrefixListResolverVersion {
    _ = allocator;
    var result: IpamPrefixListResolverVersion = undefined;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "version")) {
                    result.version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverVersionEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPrefixListResolverVersionEntry {
    var result: IpamPrefixListResolverVersionEntry = undefined;
    result.cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPublicAddressSecurityGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPublicAddressSecurityGroup {
    var result: IpamPublicAddressSecurityGroup = undefined;
    result.group_id = null;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPublicAddressTag(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPublicAddressTag {
    var result: IpamPublicAddressTag = undefined;
    result.key = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPublicAddressTags(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamPublicAddressTags {
    var result: IpamPublicAddressTags = undefined;
    result.eip_tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eipTagSet")) {
                    result.eip_tags = try deserializeIpamPublicAddressTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceCidr(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamResourceCidr {
    var result: IpamResourceCidr = undefined;
    result.availability_zone_id = null;
    result.compliance_status = null;
    result.ipam_id = null;
    result.ipam_pool_id = null;
    result.ipam_scope_id = null;
    result.ip_usage = null;
    result.management_state = null;
    result.overlap_status = null;
    result.resource_cidr = null;
    result.resource_id = null;
    result.resource_name = null;
    result.resource_owner_id = null;
    result.resource_region = null;
    result.resource_tags = null;
    result.resource_type = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "complianceStatus")) {
                    result.compliance_status = IpamComplianceStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolId")) {
                    result.ipam_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeId")) {
                    result.ipam_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipUsage")) {
                    result.ip_usage = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "managementState")) {
                    result.management_state = IpamManagementState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "overlapStatus")) {
                    result.overlap_status = IpamOverlapStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceCidr")) {
                    result.resource_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceName")) {
                    result.resource_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTagSet")) {
                    result.resource_tags = try deserializeIpamResourceTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = IpamResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceDiscovery(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamResourceDiscovery {
    var result: IpamResourceDiscovery = undefined;
    result.description = null;
    result.ipam_resource_discovery_arn = null;
    result.ipam_resource_discovery_id = null;
    result.ipam_resource_discovery_region = null;
    result.is_default = null;
    result.operating_regions = null;
    result.organizational_unit_exclusions = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryArn")) {
                    result.ipam_resource_discovery_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryRegion")) {
                    result.ipam_resource_discovery_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "operatingRegionSet")) {
                    result.operating_regions = try deserializeIpamOperatingRegionSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "organizationalUnitExclusionSet")) {
                    result.organizational_unit_exclusions = try deserializeIpamOrganizationalUnitExclusionSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamResourceDiscoveryState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceDiscoveryAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamResourceDiscoveryAssociation {
    var result: IpamResourceDiscoveryAssociation = undefined;
    result.ipam_arn = null;
    result.ipam_id = null;
    result.ipam_region = null;
    result.ipam_resource_discovery_association_arn = null;
    result.ipam_resource_discovery_association_id = null;
    result.ipam_resource_discovery_id = null;
    result.is_default = null;
    result.owner_id = null;
    result.resource_discovery_status = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryAssociationArn")) {
                    result.ipam_resource_discovery_association_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryAssociationId")) {
                    result.ipam_resource_discovery_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceDiscoveryStatus")) {
                    result.resource_discovery_status = IpamAssociatedResourceDiscoveryStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamResourceDiscoveryAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceTag(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamResourceTag {
    var result: IpamResourceTag = undefined;
    result.key = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamScope(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamScope {
    var result: IpamScope = undefined;
    result.description = null;
    result.external_authority_configuration = null;
    result.ipam_arn = null;
    result.ipam_region = null;
    result.ipam_scope_arn = null;
    result.ipam_scope_id = null;
    result.ipam_scope_type = null;
    result.is_default = null;
    result.owner_id = null;
    result.pool_count = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "externalAuthorityConfiguration")) {
                    result.external_authority_configuration = try deserializeIpamScopeExternalAuthorityConfiguration(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeArn")) {
                    result.ipam_scope_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeId")) {
                    result.ipam_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeType")) {
                    result.ipam_scope_type = IpamScopeType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolCount")) {
                    result.pool_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = IpamScopeState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamScopeExternalAuthorityConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !IpamScopeExternalAuthorityConfiguration {
    var result: IpamScopeExternalAuthorityConfiguration = undefined;
    result.external_resource_identifier = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "externalResourceIdentifier")) {
                    result.external_resource_identifier = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = IpamScopeExternalAuthorityType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv4PrefixSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv4PrefixSpecification {
    var result: Ipv4PrefixSpecification = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv4Prefix")) {
                    result.ipv_4_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv4PrefixSpecificationRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv4PrefixSpecificationRequest {
    var result: Ipv4PrefixSpecificationRequest = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "Ipv4Prefix")) {
                    result.ipv_4_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv4PrefixSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv4PrefixSpecificationResponse {
    var result: Ipv4PrefixSpecificationResponse = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv4Prefix")) {
                    result.ipv_4_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6CidrAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv6CidrAssociation {
    var result: Ipv6CidrAssociation = undefined;
    result.associated_resource = null;
    result.ipv_6_cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResource")) {
                    result.associated_resource = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6Cidr")) {
                    result.ipv_6_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6CidrBlock(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv6CidrBlock {
    var result: Ipv6CidrBlock = undefined;
    result.ipv_6_cidr_block = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6Pool(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv6Pool {
    var result: Ipv6Pool = undefined;
    result.description = null;
    result.pool_cidr_blocks = null;
    result.pool_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolCidrBlockSet")) {
                    result.pool_cidr_blocks = try deserializePoolCidrBlocksSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "poolId")) {
                    result.pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6PrefixSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv6PrefixSpecification {
    var result: Ipv6PrefixSpecification = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Prefix")) {
                    result.ipv_6_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6PrefixSpecificationRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv6PrefixSpecificationRequest {
    var result: Ipv6PrefixSpecificationRequest = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "Ipv6Prefix")) {
                    result.ipv_6_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6PrefixSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv6PrefixSpecificationResponse {
    var result: Ipv6PrefixSpecificationResponse = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Prefix")) {
                    result.ipv_6_prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6Range(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Ipv6Range {
    var result: Ipv6Range = undefined;
    result.cidr_ipv_6 = null;
    result.description = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIpv6")) {
                    result.cidr_ipv_6 = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeKeyPairInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !KeyPairInfo {
    var result: KeyPairInfo = undefined;
    result.create_time = null;
    result.key_fingerprint = null;
    result.key_name = null;
    result.key_pair_id = null;
    result.key_type = null;
    result.public_key = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "keyFingerprint")) {
                    result.key_fingerprint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyPairId")) {
                    result.key_pair_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyType")) {
                    result.key_type = KeyType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicKey")) {
                    result.public_key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLastError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LastError {
    var result: LastError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchPermission(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchPermission {
    var result: LaunchPermission = undefined;
    result.group = null;
    result.organizational_unit_arn = null;
    result.organization_arn = null;
    result.user_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "group")) {
                    result.group = PermissionGroup.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "organizationalUnitArn")) {
                    result.organizational_unit_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "organizationArn")) {
                    result.organization_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchSpecification {
    @setEvalBranchQuota(2000);
    var result: LaunchSpecification = undefined;
    result.addressing_type = null;
    result.block_device_mappings = null;
    result.ebs_optimized = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_type = null;
    result.kernel_id = null;
    result.key_name = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.placement = null;
    result.ramdisk_id = null;
    result.security_groups = null;
    result.subnet_id = null;
    result.user_data = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressingType")) {
                    result.addressing_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfileSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeRunInstancesMonitoringEnabled(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeInstanceNetworkInterfaceSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializeSpotPlacement(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.security_groups = try deserializeGroupIdentifierList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userData")) {
                    result.user_data = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplate(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplate {
    var result: LaunchTemplate = undefined;
    result.created_by = null;
    result.create_time = null;
    result.default_version_number = null;
    result.latest_version_number = null;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.operator = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdBy")) {
                    result.created_by = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultVersionNumber")) {
                    result.default_version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "latestVersionNumber")) {
                    result.latest_version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateAndOverridesResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateAndOverridesResponse {
    var result: LaunchTemplateAndOverridesResponse = undefined;
    result.launch_template_specification = null;
    result.overrides = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateSpecification")) {
                    result.launch_template_specification = try deserializeFleetLaunchTemplateSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "overrides")) {
                    result.overrides = try deserializeFleetLaunchTemplateOverrides(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateBlockDeviceMapping(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateBlockDeviceMapping {
    var result: LaunchTemplateBlockDeviceMapping = undefined;
    result.device_name = null;
    result.ebs = null;
    result.no_device = null;
    result.virtual_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeLaunchTemplateEbsBlockDevice(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "noDevice")) {
                    result.no_device = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualName")) {
                    result.virtual_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateCapacityReservationSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateCapacityReservationSpecificationResponse {
    var result: LaunchTemplateCapacityReservationSpecificationResponse = undefined;
    result.capacity_reservation_preference = null;
    result.capacity_reservation_target = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationPreference")) {
                    result.capacity_reservation_preference = CapacityReservationPreference.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationTarget")) {
                    result.capacity_reservation_target = try deserializeCapacityReservationTargetResponse(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateConfig(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateConfig {
    var result: LaunchTemplateConfig = undefined;
    result.launch_template_specification = null;
    result.overrides = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateSpecification")) {
                    result.launch_template_specification = try deserializeFleetLaunchTemplateSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "overrides")) {
                    result.overrides = try deserializeLaunchTemplateOverridesList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateCpuOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateCpuOptions {
    _ = allocator;
    var result: LaunchTemplateCpuOptions = undefined;
    result.amd_sev_snp = null;
    result.core_count = null;
    result.threads_per_core = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amdSevSnp")) {
                    result.amd_sev_snp = AmdSevSnpSpecification.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreCount")) {
                    result.core_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "threadsPerCore")) {
                    result.threads_per_core = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEbsBlockDevice(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateEbsBlockDevice {
    var result: LaunchTemplateEbsBlockDevice = undefined;
    result.delete_on_termination = null;
    result.ebs_card_index = null;
    result.encrypted = null;
    result.iops = null;
    result.kms_key_id = null;
    result.snapshot_id = null;
    result.throughput = null;
    result.volume_initialization_rate = null;
    result.volume_size = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeInitializationRate")) {
                    result.volume_initialization_rate = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = VolumeType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateElasticInferenceAcceleratorResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateElasticInferenceAcceleratorResponse {
    var result: LaunchTemplateElasticInferenceAcceleratorResponse = undefined;
    result.count = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEnaSrdSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateEnaSrdSpecification {
    var result: LaunchTemplateEnaSrdSpecification = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeLaunchTemplateEnaSrdUdpSpecification(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEnaSrdUdpSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateEnaSrdUdpSpecification {
    _ = allocator;
    var result: LaunchTemplateEnaSrdUdpSpecification = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEnclaveOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateEnclaveOptions {
    _ = allocator;
    var result: LaunchTemplateEnclaveOptions = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateHibernationOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateHibernationOptions {
    _ = allocator;
    var result: LaunchTemplateHibernationOptions = undefined;
    result.configured = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configured")) {
                    result.configured = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateIamInstanceProfileSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateIamInstanceProfileSpecification {
    var result: LaunchTemplateIamInstanceProfileSpecification = undefined;
    result.arn = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceMaintenanceOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateInstanceMaintenanceOptions {
    _ = allocator;
    var result: LaunchTemplateInstanceMaintenanceOptions = undefined;
    result.auto_recovery = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoRecovery")) {
                    result.auto_recovery = LaunchTemplateAutoRecoveryState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceMarketOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateInstanceMarketOptions {
    var result: LaunchTemplateInstanceMarketOptions = undefined;
    result.market_type = null;
    result.spot_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "marketType")) {
                    result.market_type = MarketType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotOptions")) {
                    result.spot_options = try deserializeLaunchTemplateSpotMarketOptions(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceMetadataOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateInstanceMetadataOptions {
    _ = allocator;
    var result: LaunchTemplateInstanceMetadataOptions = undefined;
    result.http_endpoint = null;
    result.http_protocol_ipv_6 = null;
    result.http_put_response_hop_limit = null;
    result.http_tokens = null;
    result.instance_metadata_tags = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "httpEndpoint")) {
                    result.http_endpoint = LaunchTemplateInstanceMetadataEndpointState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpProtocolIpv6")) {
                    result.http_protocol_ipv_6 = LaunchTemplateInstanceMetadataProtocolIpv6.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpPutResponseHopLimit")) {
                    result.http_put_response_hop_limit = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "httpTokens")) {
                    result.http_tokens = LaunchTemplateHttpTokensState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMetadataTags")) {
                    result.instance_metadata_tags = LaunchTemplateInstanceMetadataTagsState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = LaunchTemplateInstanceMetadataOptionsState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceNetworkInterfaceSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateInstanceNetworkInterfaceSpecification {
    var result: LaunchTemplateInstanceNetworkInterfaceSpecification = undefined;
    result.associate_carrier_ip_address = null;
    result.associate_public_ip_address = null;
    result.connection_tracking_specification = null;
    result.delete_on_termination = null;
    result.description = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefix_count = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_address_count = null;
    result.ipv_6_addresses = null;
    result.ipv_6_prefix_count = null;
    result.ipv_6_prefixes = null;
    result.network_card_index = null;
    result.network_interface_id = null;
    result.primary_ipv_6 = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.secondary_private_ip_address_count = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associateCarrierIpAddress")) {
                    result.associate_carrier_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "associatePublicIpAddress")) {
                    result.associate_public_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "connectionTrackingSpecification")) {
                    result.connection_tracking_specification = try deserializeConnectionTrackingSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeLaunchTemplateEnaSrdSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdStringList(allocator, reader, "groupId");
                } else if (std.mem.eql(u8, e.local, "interfaceType")) {
                    result.interface_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixCount")) {
                    result.ipv_4_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeIpv4PrefixListResponse(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6AddressCount")) {
                    result.ipv_6_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeInstanceIpv6AddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixCount")) {
                    result.ipv_6_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeIpv6PrefixListResponse(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "primaryIpv6")) {
                    result.primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializePrivateIpAddressSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "secondaryPrivateIpAddressCount")) {
                    result.secondary_private_ip_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateLicenseConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateLicenseConfiguration {
    var result: LaunchTemplateLicenseConfiguration = undefined;
    result.license_configuration_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "licenseConfigurationArn")) {
                    result.license_configuration_arn = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateNetworkPerformanceOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateNetworkPerformanceOptions {
    _ = allocator;
    var result: LaunchTemplateNetworkPerformanceOptions = undefined;
    result.bandwidth_weighting = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bandwidthWeighting")) {
                    result.bandwidth_weighting = InstanceBandwidthWeighting.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateOverrides(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateOverrides {
    @setEvalBranchQuota(2000);
    var result: LaunchTemplateOverrides = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.priority = null;
    result.spot_price = null;
    result.subnet_id = null;
    result.weighted_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "weightedCapacity")) {
                    result.weighted_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplatePlacement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplatePlacement {
    var result: LaunchTemplatePlacement = undefined;
    result.affinity = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.group_id = null;
    result.group_name = null;
    result.host_id = null;
    result.host_resource_group_arn = null;
    result.partition_number = null;
    result.spread_domain = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "affinity")) {
                    result.affinity = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostResourceGroupArn")) {
                    result.host_resource_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "partitionNumber")) {
                    result.partition_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spreadDomain")) {
                    result.spread_domain = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = Tenancy.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplatePrivateDnsNameOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplatePrivateDnsNameOptions {
    _ = allocator;
    var result: LaunchTemplatePrivateDnsNameOptions = undefined;
    result.enable_resource_name_dns_aaaa_record = null;
    result.enable_resource_name_dns_a_record = null;
    result.hostname_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableResourceNameDnsAAAARecord")) {
                    result.enable_resource_name_dns_aaaa_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableResourceNameDnsARecord")) {
                    result.enable_resource_name_dns_a_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hostnameType")) {
                    result.hostname_type = HostnameType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateSpotMarketOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateSpotMarketOptions {
    var result: LaunchTemplateSpotMarketOptions = undefined;
    result.block_duration_minutes = null;
    result.instance_interruption_behavior = null;
    result.max_price = null;
    result.spot_instance_type = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blockDurationMinutes")) {
                    result.block_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = InstanceInterruptionBehavior.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxPrice")) {
                    result.max_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotInstanceType")) {
                    result.spot_instance_type = SpotInstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateTagSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateTagSpecification {
    var result: LaunchTemplateTagSpecification = undefined;
    result.resource_type = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = ResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateVersion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplateVersion {
    var result: LaunchTemplateVersion = undefined;
    result.created_by = null;
    result.create_time = null;
    result.default_version = null;
    result.launch_template_data = null;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.operator = null;
    result.version_description = null;
    result.version_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdBy")) {
                    result.created_by = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultVersion")) {
                    result.default_version = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "launchTemplateData")) {
                    result.launch_template_data = try deserializeResponseLaunchTemplateData(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "versionDescription")) {
                    result.version_description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "versionNumber")) {
                    result.version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplatesMonitoring(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LaunchTemplatesMonitoring {
    _ = allocator;
    var result: LaunchTemplatesMonitoring = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLicenseConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LicenseConfiguration {
    var result: LicenseConfiguration = undefined;
    result.license_configuration_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "licenseConfigurationArn")) {
                    result.license_configuration_arn = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLoadBalancersConfig(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LoadBalancersConfig {
    var result: LoadBalancersConfig = undefined;
    result.classic_load_balancers_config = null;
    result.target_groups_config = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLoadBalancersConfig")) {
                    result.classic_load_balancers_config = try deserializeClassicLoadBalancersConfig(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "targetGroupsConfig")) {
                    result.target_groups_config = try deserializeTargetGroupsConfig(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLoadPermission(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LoadPermission {
    var result: LoadPermission = undefined;
    result.group = null;
    result.user_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "group")) {
                    result.group = PermissionGroup.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LocalGateway {
    var result: LocalGateway = undefined;
    result.local_gateway_id = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LocalGatewayRoute {
    var result: LocalGatewayRoute = undefined;
    result.coip_pool_id = null;
    result.destination_cidr_block = null;
    result.destination_prefix_list_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.network_interface_id = null;
    result.owner_id = null;
    result.state = null;
    result.subnet_id = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coipPoolId")) {
                    result.coip_pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListId")) {
                    result.destination_prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = LocalGatewayRouteState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = LocalGatewayRouteType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRouteTable(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LocalGatewayRouteTable {
    var result: LocalGatewayRouteTable = undefined;
    result.local_gateway_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.mode = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.state = null;
    result.state_reason = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "mode")) {
                    result.mode = LocalGatewayRouteTableMode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try deserializeStateReason(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRouteTableVirtualInterfaceGroupAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LocalGatewayRouteTableVirtualInterfaceGroupAssociation {
    var result: LocalGatewayRouteTableVirtualInterfaceGroupAssociation = undefined;
    result.local_gateway_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.local_gateway_route_table_virtual_interface_group_association_id = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableVirtualInterfaceGroupAssociationId")) {
                    result.local_gateway_route_table_virtual_interface_group_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRouteTableVpcAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LocalGatewayRouteTableVpcAssociation {
    var result: LocalGatewayRouteTableVpcAssociation = undefined;
    result.local_gateway_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.local_gateway_route_table_vpc_association_id = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableVpcAssociationId")) {
                    result.local_gateway_route_table_vpc_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayVirtualInterface(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LocalGatewayVirtualInterface {
    var result: LocalGatewayVirtualInterface = undefined;
    result.configuration_state = null;
    result.local_address = null;
    result.local_bgp_asn = null;
    result.local_gateway_id = null;
    result.local_gateway_virtual_interface_arn = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.local_gateway_virtual_interface_id = null;
    result.outpost_lag_id = null;
    result.owner_id = null;
    result.peer_address = null;
    result.peer_bgp_asn = null;
    result.peer_bgp_asn_extended = null;
    result.tags = null;
    result.vlan = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configurationState")) {
                    result.configuration_state = LocalGatewayVirtualInterfaceConfigurationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localAddress")) {
                    result.local_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localBgpAsn")) {
                    result.local_bgp_asn = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceArn")) {
                    result.local_gateway_virtual_interface_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceId")) {
                    result.local_gateway_virtual_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostLagId")) {
                    result.outpost_lag_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerBgpAsn")) {
                    result.peer_bgp_asn = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "peerBgpAsnExtended")) {
                    result.peer_bgp_asn_extended = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vlan")) {
                    result.vlan = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayVirtualInterfaceGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LocalGatewayVirtualInterfaceGroup {
    var result: LocalGatewayVirtualInterfaceGroup = undefined;
    result.configuration_state = null;
    result.local_bgp_asn = null;
    result.local_bgp_asn_extended = null;
    result.local_gateway_id = null;
    result.local_gateway_virtual_interface_group_arn = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.local_gateway_virtual_interface_ids = null;
    result.owner_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configurationState")) {
                    result.configuration_state = LocalGatewayVirtualInterfaceGroupConfigurationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localBgpAsn")) {
                    result.local_bgp_asn = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "localBgpAsnExtended")) {
                    result.local_bgp_asn_extended = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupArn")) {
                    result.local_gateway_virtual_interface_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceIdSet")) {
                    result.local_gateway_virtual_interface_ids = try deserializeLocalGatewayVirtualInterfaceIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLockedSnapshotsInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !LockedSnapshotsInfo {
    var result: LockedSnapshotsInfo = undefined;
    result.cool_off_period = null;
    result.cool_off_period_expires_on = null;
    result.lock_created_on = null;
    result.lock_duration = null;
    result.lock_duration_start_time = null;
    result.lock_expires_on = null;
    result.lock_state = null;
    result.owner_id = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coolOffPeriod")) {
                    result.cool_off_period = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "coolOffPeriodExpiresOn")) {
                    result.cool_off_period_expires_on = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockCreatedOn")) {
                    result.lock_created_on = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockDuration")) {
                    result.lock_duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "lockDurationStartTime")) {
                    result.lock_duration_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockExpiresOn")) {
                    result.lock_expires_on = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockState")) {
                    result.lock_state = LockState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMacHost(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MacHost {
    var result: MacHost = undefined;
    result.host_id = null;
    result.mac_os_latest_supported_versions = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "macOSLatestSupportedVersionSet")) {
                    result.mac_os_latest_supported_versions = try deserializeMacOSVersionStringList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMacModificationTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MacModificationTask {
    var result: MacModificationTask = undefined;
    result.instance_id = null;
    result.mac_modification_task_id = null;
    result.mac_system_integrity_protection_config = null;
    result.start_time = null;
    result.tags = null;
    result.task_state = null;
    result.task_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "macModificationTaskId")) {
                    result.mac_modification_task_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "macSystemIntegrityProtectionConfig")) {
                    result.mac_system_integrity_protection_config = try deserializeMacSystemIntegrityProtectionConfiguration(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "taskState")) {
                    result.task_state = MacModificationTaskState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "taskType")) {
                    result.task_type = MacModificationTaskType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMacSystemIntegrityProtectionConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MacSystemIntegrityProtectionConfiguration {
    _ = allocator;
    var result: MacSystemIntegrityProtectionConfiguration = undefined;
    result.apple_internal = null;
    result.base_system = null;
    result.debugging_restrictions = null;
    result.d_trace_restrictions = null;
    result.filesystem_protections = null;
    result.kext_signing = null;
    result.nvram_protections = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "appleInternal")) {
                    result.apple_internal = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "baseSystem")) {
                    result.base_system = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "debuggingRestrictions")) {
                    result.debugging_restrictions = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dTraceRestrictions")) {
                    result.d_trace_restrictions = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "filesystemProtections")) {
                    result.filesystem_protections = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kextSigning")) {
                    result.kext_signing = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nvramProtections")) {
                    result.nvram_protections = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = MacSystemIntegrityProtectionSettingStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMaintenanceDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MaintenanceDetails {
    var result: MaintenanceDetails = undefined;
    result.last_maintenance_applied = null;
    result.maintenance_auto_applied_after = null;
    result.pending_maintenance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "lastMaintenanceApplied")) {
                    result.last_maintenance_applied = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "maintenanceAutoAppliedAfter")) {
                    result.maintenance_auto_applied_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "pendingMaintenance")) {
                    result.pending_maintenance = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeManagedPrefixList(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ManagedPrefixList {
    var result: ManagedPrefixList = undefined;
    result.address_family = null;
    result.ipam_prefix_list_resolver_sync_enabled = null;
    result.ipam_prefix_list_resolver_target_id = null;
    result.max_entries = null;
    result.owner_id = null;
    result.prefix_list_arn = null;
    result.prefix_list_id = null;
    result.prefix_list_name = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressFamily")) {
                    result.address_family = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverSyncEnabled")) {
                    result.ipam_prefix_list_resolver_sync_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverTargetId")) {
                    result.ipam_prefix_list_resolver_target_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxEntries")) {
                    result.max_entries = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListArn")) {
                    result.prefix_list_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListName")) {
                    result.prefix_list_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = PrefixListState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMediaAcceleratorInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MediaAcceleratorInfo {
    var result: MediaAcceleratorInfo = undefined;
    result.accelerators = null;
    result.total_media_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accelerators")) {
                    result.accelerators = try deserializeMediaDeviceInfoList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "totalMediaMemoryInMiB")) {
                    result.total_media_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMediaDeviceInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MediaDeviceInfo {
    var result: MediaDeviceInfo = undefined;
    result.count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeMediaDeviceMemoryInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMediaDeviceMemoryInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MediaDeviceMemoryInfo {
    _ = allocator;
    var result: MediaDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMemoryGiBPerVCpu(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MemoryGiBPerVCpu {
    _ = allocator;
    var result: MemoryGiBPerVCpu = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMemoryInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MemoryInfo {
    _ = allocator;
    var result: MemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMemoryMiB(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MemoryMiB {
    _ = allocator;
    var result: MemoryMiB = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMetricDataResult(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MetricDataResult {
    var result: MetricDataResult = undefined;
    result.dimension = null;
    result.metric_values = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dimension")) {
                    result.dimension = try deserializeCapacityManagerDimension(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "metricValueSet")) {
                    result.metric_values = try deserializeMetricValueSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMetricPoint(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MetricPoint {
    var result: MetricPoint = undefined;
    result.end_date = null;
    result.start_date = null;
    result.status = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMetricValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MetricValue {
    _ = allocator;
    var result: MetricValue = undefined;
    result.metric = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "metric")) {
                    result.metric = Metric.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMonitoring(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Monitoring {
    _ = allocator;
    var result: Monitoring = undefined;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = MonitoringState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMovingAddressStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !MovingAddressStatus {
    var result: MovingAddressStatus = undefined;
    result.move_status = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "moveStatus")) {
                    result.move_status = MoveStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNatGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NatGateway {
    var result: NatGateway = undefined;
    result.attached_appliances = null;
    result.auto_provision_zones = null;
    result.auto_scaling_ips = null;
    result.availability_mode = null;
    result.connectivity_type = null;
    result.create_time = null;
    result.delete_time = null;
    result.failure_code = null;
    result.failure_message = null;
    result.nat_gateway_addresses = null;
    result.nat_gateway_id = null;
    result.provisioned_bandwidth = null;
    result.route_table_id = null;
    result.state = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachedApplianceSet")) {
                    result.attached_appliances = try deserializeNatGatewayAttachedApplianceList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "autoProvisionZones")) {
                    result.auto_provision_zones = AutoProvisionZonesState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "autoScalingIps")) {
                    result.auto_scaling_ips = AutoScalingIpsState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityMode")) {
                    result.availability_mode = AvailabilityMode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectivityType")) {
                    result.connectivity_type = ConnectivityType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteTime")) {
                    result.delete_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "failureCode")) {
                    result.failure_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureMessage")) {
                    result.failure_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGatewayAddressSet")) {
                    result.nat_gateway_addresses = try deserializeNatGatewayAddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "natGatewayId")) {
                    result.nat_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisionedBandwidth")) {
                    result.provisioned_bandwidth = try deserializeProvisionedBandwidth(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = NatGatewayState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNatGatewayAddress(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NatGatewayAddress {
    var result: NatGatewayAddress = undefined;
    result.allocation_id = null;
    result.association_id = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.failure_message = null;
    result.is_primary = null;
    result.network_interface_id = null;
    result.private_ip = null;
    result.public_ip = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureMessage")) {
                    result.failure_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPrimary")) {
                    result.is_primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIp")) {
                    result.private_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = NatGatewayAddressStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNatGatewayAttachedAppliance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NatGatewayAttachedAppliance {
    var result: NatGatewayAttachedAppliance = undefined;
    result.appliance_arn = null;
    result.attachment_state = null;
    result.failure_code = null;
    result.failure_message = null;
    result.modification_state = null;
    result.@"type" = null;
    result.vpc_endpoint_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "applianceArn")) {
                    result.appliance_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachmentState")) {
                    result.attachment_state = NatGatewayApplianceState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureCode")) {
                    result.failure_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureMessage")) {
                    result.failure_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "modificationState")) {
                    result.modification_state = NatGatewayApplianceModifyState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = NatGatewayApplianceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNativeApplicationOidcOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NativeApplicationOidcOptions {
    var result: NativeApplicationOidcOptions = undefined;
    result.authorization_endpoint = null;
    result.client_id = null;
    result.issuer = null;
    result.public_signing_key_endpoint = null;
    result.scope = null;
    result.token_endpoint = null;
    result.user_info_endpoint = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "authorizationEndpoint")) {
                    result.authorization_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientId")) {
                    result.client_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "issuer")) {
                    result.issuer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicSigningKeyEndpoint")) {
                    result.public_signing_key_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenEndpoint")) {
                    result.token_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userInfoEndpoint")) {
                    result.user_info_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkAcl(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkAcl {
    var result: NetworkAcl = undefined;
    result.associations = null;
    result.entries = null;
    result.is_default = null;
    result.network_acl_id = null;
    result.owner_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationSet")) {
                    result.associations = try deserializeNetworkAclAssociationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "entrySet")) {
                    result.entries = try deserializeNetworkAclEntryList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "default")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "networkAclId")) {
                    result.network_acl_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkAclAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkAclAssociation {
    var result: NetworkAclAssociation = undefined;
    result.network_acl_association_id = null;
    result.network_acl_id = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "networkAclAssociationId")) {
                    result.network_acl_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkAclId")) {
                    result.network_acl_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkAclEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkAclEntry {
    var result: NetworkAclEntry = undefined;
    result.cidr_block = null;
    result.egress = null;
    result.icmp_type_code = null;
    result.ipv_6_cidr_block = null;
    result.port_range = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egress")) {
                    result.egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "icmpTypeCode")) {
                    result.icmp_type_code = try deserializeIcmpTypeCode(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "portRange")) {
                    result.port_range = try deserializePortRange(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = RuleAction.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleNumber")) {
                    result.rule_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkBandwidthGbps(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkBandwidthGbps {
    _ = allocator;
    var result: NetworkBandwidthGbps = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkCardInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkCardInfo {
    var result: NetworkCardInfo = undefined;
    result.additional_flexible_network_interfaces = null;
    result.baseline_bandwidth_in_gbps = null;
    result.default_ena_queue_count_per_interface = null;
    result.maximum_ena_queue_count = null;
    result.maximum_ena_queue_count_per_interface = null;
    result.maximum_network_interfaces = null;
    result.network_card_index = null;
    result.network_performance = null;
    result.peak_bandwidth_in_gbps = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "additionalFlexibleNetworkInterfaces")) {
                    result.additional_flexible_network_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineBandwidthInGbps")) {
                    result.baseline_bandwidth_in_gbps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultEnaQueueCountPerInterface")) {
                    result.default_ena_queue_count_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumEnaQueueCount")) {
                    result.maximum_ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumEnaQueueCountPerInterface")) {
                    result.maximum_ena_queue_count_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumNetworkInterfaces")) {
                    result.maximum_network_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkPerformance")) {
                    result.network_performance = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peakBandwidthInGbps")) {
                    result.peak_bandwidth_in_gbps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInfo {
    var result: NetworkInfo = undefined;
    result.bandwidth_weightings = null;
    result.default_network_card_index = null;
    result.efa_info = null;
    result.efa_supported = null;
    result.ena_srd_supported = null;
    result.ena_support = null;
    result.encryption_in_transit_supported = null;
    result.flexible_ena_queues_support = null;
    result.ipv_4_addresses_per_interface = null;
    result.ipv_6_addresses_per_interface = null;
    result.ipv_6_supported = null;
    result.maximum_network_cards = null;
    result.maximum_network_interfaces = null;
    result.network_cards = null;
    result.network_performance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bandwidthWeightings")) {
                    result.bandwidth_weightings = try deserializeBandwidthWeightingTypeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "defaultNetworkCardIndex")) {
                    result.default_network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "efaInfo")) {
                    result.efa_info = try deserializeEfaInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "efaSupported")) {
                    result.efa_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdSupported")) {
                    result.ena_srd_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSupport")) {
                    result.ena_support = EnaSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionInTransitSupported")) {
                    result.encryption_in_transit_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "flexibleEnaQueuesSupport")) {
                    result.flexible_ena_queues_support = FlexibleEnaQueuesSupport.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4AddressesPerInterface")) {
                    result.ipv_4_addresses_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesPerInterface")) {
                    result.ipv_6_addresses_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6Supported")) {
                    result.ipv_6_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "maximumNetworkCards")) {
                    result.maximum_network_cards = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumNetworkInterfaces")) {
                    result.maximum_network_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkCards")) {
                    result.network_cards = try deserializeNetworkCardInfoList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkPerformance")) {
                    result.network_performance = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAccessScope(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInsightsAccessScope {
    var result: NetworkInsightsAccessScope = undefined;
    result.created_date = null;
    result.network_insights_access_scope_arn = null;
    result.network_insights_access_scope_id = null;
    result.tags = null;
    result.updated_date = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdDate")) {
                    result.created_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeArn")) {
                    result.network_insights_access_scope_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "updatedDate")) {
                    result.updated_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAccessScopeAnalysis(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInsightsAccessScopeAnalysis {
    var result: NetworkInsightsAccessScopeAnalysis = undefined;
    result.analyzed_eni_count = null;
    result.end_date = null;
    result.findings_found = null;
    result.network_insights_access_scope_analysis_arn = null;
    result.network_insights_access_scope_analysis_id = null;
    result.network_insights_access_scope_id = null;
    result.start_date = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    result.warning_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "analyzedEniCount")) {
                    result.analyzed_eni_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "findingsFound")) {
                    result.findings_found = FindingsFound.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeAnalysisArn")) {
                    result.network_insights_access_scope_analysis_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeAnalysisId")) {
                    result.network_insights_access_scope_analysis_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = AnalysisStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "warningMessage")) {
                    result.warning_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAccessScopeContent(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInsightsAccessScopeContent {
    var result: NetworkInsightsAccessScopeContent = undefined;
    result.exclude_paths = null;
    result.match_paths = null;
    result.network_insights_access_scope_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "excludePathSet")) {
                    result.exclude_paths = try deserializeAccessScopePathList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "matchPathSet")) {
                    result.match_paths = try deserializeAccessScopePathList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAnalysis(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInsightsAnalysis {
    var result: NetworkInsightsAnalysis = undefined;
    result.additional_accounts = null;
    result.alternate_path_hints = null;
    result.explanations = null;
    result.filter_in_arns = null;
    result.filter_out_arns = null;
    result.forward_path_components = null;
    result.network_insights_analysis_arn = null;
    result.network_insights_analysis_id = null;
    result.network_insights_path_id = null;
    result.network_path_found = null;
    result.return_path_components = null;
    result.start_date = null;
    result.status = null;
    result.status_message = null;
    result.suggested_accounts = null;
    result.tags = null;
    result.warning_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "additionalAccountSet")) {
                    result.additional_accounts = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "alternatePathHintSet")) {
                    result.alternate_path_hints = try deserializeAlternatePathHintList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "explanationSet")) {
                    result.explanations = try deserializeExplanationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "filterInArnSet")) {
                    result.filter_in_arns = try deserializeArnList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "filterOutArnSet")) {
                    result.filter_out_arns = try deserializeArnList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "forwardPathComponentSet")) {
                    result.forward_path_components = try deserializePathComponentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkInsightsAnalysisArn")) {
                    result.network_insights_analysis_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAnalysisId")) {
                    result.network_insights_analysis_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsPathId")) {
                    result.network_insights_path_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkPathFound")) {
                    result.network_path_found = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "returnPathComponentSet")) {
                    result.return_path_components = try deserializePathComponentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = AnalysisStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "suggestedAccountSet")) {
                    result.suggested_accounts = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "warningMessage")) {
                    result.warning_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsPath(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInsightsPath {
    var result: NetworkInsightsPath = undefined;
    result.created_date = null;
    result.destination = null;
    result.destination_arn = null;
    result.destination_ip = null;
    result.destination_port = null;
    result.filter_at_destination = null;
    result.filter_at_source = null;
    result.network_insights_path_arn = null;
    result.network_insights_path_id = null;
    result.protocol = null;
    result.source = null;
    result.source_arn = null;
    result.source_ip = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdDate")) {
                    result.created_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationArn")) {
                    result.destination_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationIp")) {
                    result.destination_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPort")) {
                    result.destination_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "filterAtDestination")) {
                    result.filter_at_destination = try deserializePathFilter(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "filterAtSource")) {
                    result.filter_at_source = try deserializePathFilter(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInsightsPathArn")) {
                    result.network_insights_path_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsPathId")) {
                    result.network_insights_path_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = Protocol.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceArn")) {
                    result.source_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceIp")) {
                    result.source_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterface(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterface {
    var result: NetworkInterface = undefined;
    result.associated_subnets = null;
    result.association = null;
    result.attachment = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.connection_tracking_configuration = null;
    result.deny_all_igw_traffic = null;
    result.description = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_address = null;
    result.ipv_6_addresses = null;
    result.ipv_6_native = null;
    result.ipv_6_prefixes = null;
    result.mac_address = null;
    result.network_interface_id = null;
    result.operator = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.public_dns_name = null;
    result.public_ip_dns_name_options = null;
    result.requester_id = null;
    result.requester_managed = null;
    result.source_dest_check = null;
    result.status = null;
    result.subnet_id = null;
    result.tag_set = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedSubnetSet")) {
                    result.associated_subnets = try deserializeAssociatedSubnetList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeNetworkInterfaceAssociation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "attachment")) {
                    result.attachment = try deserializeNetworkInterfaceAttachment(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionTrackingConfiguration")) {
                    result.connection_tracking_configuration = try deserializeConnectionTrackingConfiguration(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "denyAllIgwTraffic")) {
                    result.deny_all_igw_traffic = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "interfaceType")) {
                    result.interface_type = NetworkInterfaceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeIpv4PrefixesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeNetworkInterfaceIpv6AddressesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Native")) {
                    result.ipv_6_native = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeIpv6PrefixesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "macAddress")) {
                    result.mac_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializeNetworkInterfacePrivateIpAddressList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "publicDnsName")) {
                    result.public_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpDnsNameOptions")) {
                    result.public_ip_dns_name_options = try deserializePublicIpDnsNameOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "requesterId")) {
                    result.requester_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "requesterManaged")) {
                    result.requester_managed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "sourceDestCheck")) {
                    result.source_dest_check = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = NetworkInterfaceStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tag_set = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterfaceAssociation {
    var result: NetworkInterfaceAssociation = undefined;
    result.allocation_id = null;
    result.association_id = null;
    result.carrier_ip = null;
    result.customer_owned_ip = null;
    result.ip_owner_id = null;
    result.public_dns_name = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "carrierIp")) {
                    result.carrier_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIp")) {
                    result.customer_owned_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipOwnerId")) {
                    result.ip_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDnsName")) {
                    result.public_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterfaceAttachment {
    var result: NetworkInterfaceAttachment = undefined;
    result.attachment_id = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.instance_id = null;
    result.instance_owner_id = null;
    result.network_card_index = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentId")) {
                    result.attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeAttachmentEnaSrdSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceOwnerId")) {
                    result.instance_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = AttachmentStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceCount(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterfaceCount {
    _ = allocator;
    var result: NetworkInterfaceCount = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceIpv6Address(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterfaceIpv6Address {
    var result: NetworkInterfaceIpv6Address = undefined;
    result.ipv_6_address = null;
    result.is_primary_ipv_6 = null;
    result.public_ipv_6_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPrimaryIpv6")) {
                    result.is_primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicIpv6DnsName")) {
                    result.public_ipv_6_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfacePermission(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterfacePermission {
    var result: NetworkInterfacePermission = undefined;
    result.aws_account_id = null;
    result.aws_service = null;
    result.network_interface_id = null;
    result.network_interface_permission_id = null;
    result.permission = null;
    result.permission_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "awsAccountId")) {
                    result.aws_account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsService")) {
                    result.aws_service = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfacePermissionId")) {
                    result.network_interface_permission_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "permission")) {
                    result.permission = InterfacePermissionType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "permissionState")) {
                    result.permission_state = try deserializeNetworkInterfacePermissionState(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfacePermissionState(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterfacePermissionState {
    var result: NetworkInterfacePermissionState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = NetworkInterfacePermissionStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfacePrivateIpAddress(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NetworkInterfacePrivateIpAddress {
    var result: NetworkInterfacePrivateIpAddress = undefined;
    result.association = null;
    result.primary = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeNetworkInterfaceAssociation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "primary")) {
                    result.primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronDeviceCoreInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NeuronDeviceCoreInfo {
    _ = allocator;
    var result: NeuronDeviceCoreInfo = undefined;
    result.count = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronDeviceInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NeuronDeviceInfo {
    var result: NeuronDeviceInfo = undefined;
    result.core_info = null;
    result.count = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coreInfo")) {
                    result.core_info = try deserializeNeuronDeviceCoreInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeNeuronDeviceMemoryInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronDeviceMemoryInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NeuronDeviceMemoryInfo {
    _ = allocator;
    var result: NeuronDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NeuronInfo {
    var result: NeuronInfo = undefined;
    result.neuron_devices = null;
    result.total_neuron_device_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "neuronDevices")) {
                    result.neuron_devices = try deserializeNeuronDeviceInfoList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "totalNeuronDeviceMemoryInMiB")) {
                    result.total_neuron_device_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNitroTpmInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !NitroTpmInfo {
    var result: NitroTpmInfo = undefined;
    result.supported_versions = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "supportedVersions")) {
                    result.supported_versions = try deserializeNitroTpmSupportedVersionsList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOidcOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !OidcOptions {
    var result: OidcOptions = undefined;
    result.authorization_endpoint = null;
    result.client_id = null;
    result.client_secret = null;
    result.issuer = null;
    result.scope = null;
    result.token_endpoint = null;
    result.user_info_endpoint = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "authorizationEndpoint")) {
                    result.authorization_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientId")) {
                    result.client_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientSecret")) {
                    result.client_secret = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "issuer")) {
                    result.issuer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenEndpoint")) {
                    result.token_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userInfoEndpoint")) {
                    result.user_info_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOnDemandOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !OnDemandOptions {
    var result: OnDemandOptions = undefined;
    result.allocation_strategy = null;
    result.capacity_reservation_options = null;
    result.max_total_price = null;
    result.min_target_capacity = null;
    result.single_availability_zone = null;
    result.single_instance_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = FleetOnDemandAllocationStrategy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationOptions")) {
                    result.capacity_reservation_options = try deserializeCapacityReservationOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "maxTotalPrice")) {
                    result.max_total_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "minTargetCapacity")) {
                    result.min_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "singleAvailabilityZone")) {
                    result.single_availability_zone = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "singleInstanceType")) {
                    result.single_instance_type = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOperatorResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !OperatorResponse {
    var result: OperatorResponse = undefined;
    result.managed = null;
    result.principal = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "managed")) {
                    result.managed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "principal")) {
                    result.principal = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOutpostLag(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !OutpostLag {
    var result: OutpostLag = undefined;
    result.local_gateway_virtual_interface_ids = null;
    result.outpost_arn = null;
    result.outpost_lag_id = null;
    result.owner_id = null;
    result.service_link_virtual_interface_ids = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceIdSet")) {
                    result.local_gateway_virtual_interface_ids = try deserializeLocalGatewayVirtualInterfaceIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostLagId")) {
                    result.outpost_lag_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceLinkVirtualInterfaceIdSet")) {
                    result.service_link_virtual_interface_ids = try deserializeServiceLinkVirtualInterfaceIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePacketHeaderStatement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PacketHeaderStatement {
    var result: PacketHeaderStatement = undefined;
    result.destination_addresses = null;
    result.destination_ports = null;
    result.destination_prefix_lists = null;
    result.protocols = null;
    result.source_addresses = null;
    result.source_ports = null;
    result.source_prefix_lists = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationAddressSet")) {
                    result.destination_addresses = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "destinationPortSet")) {
                    result.destination_ports = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListSet")) {
                    result.destination_prefix_lists = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "protocolSet")) {
                    result.protocols = try deserializeProtocolList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourceAddressSet")) {
                    result.source_addresses = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourcePortSet")) {
                    result.source_ports = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sourcePrefixListSet")) {
                    result.source_prefix_lists = try deserializeValueStringList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePathComponent(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PathComponent {
    var result: PathComponent = undefined;
    result.acl_rule = null;
    result.additional_details = null;
    result.attached_to = null;
    result.component = null;
    result.destination_vpc = null;
    result.elastic_load_balancer_listener = null;
    result.explanations = null;
    result.firewall_stateful_rule = null;
    result.firewall_stateless_rule = null;
    result.inbound_header = null;
    result.outbound_header = null;
    result.route_table_route = null;
    result.security_group_rule = null;
    result.sequence_number = null;
    result.service_name = null;
    result.source_vpc = null;
    result.subnet = null;
    result.transit_gateway = null;
    result.transit_gateway_route_table_route = null;
    result.vpc = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "aclRule")) {
                    result.acl_rule = try deserializeAnalysisAclRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "additionalDetailSet")) {
                    result.additional_details = try deserializeAdditionalDetailList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "attachedTo")) {
                    result.attached_to = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "component")) {
                    result.component = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "destinationVpc")) {
                    result.destination_vpc = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "elasticLoadBalancerListener")) {
                    result.elastic_load_balancer_listener = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "explanationSet")) {
                    result.explanations = try deserializeExplanationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "firewallStatefulRule")) {
                    result.firewall_stateful_rule = try deserializeFirewallStatefulRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "firewallStatelessRule")) {
                    result.firewall_stateless_rule = try deserializeFirewallStatelessRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "inboundHeader")) {
                    result.inbound_header = try deserializeAnalysisPacketHeader(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outboundHeader")) {
                    result.outbound_header = try deserializeAnalysisPacketHeader(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "routeTableRoute")) {
                    result.route_table_route = try deserializeAnalysisRouteTableRoute(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "securityGroupRule")) {
                    result.security_group_rule = try deserializeAnalysisSecurityGroupRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "sequenceNumber")) {
                    result.sequence_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceVpc")) {
                    result.source_vpc = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "subnet")) {
                    result.subnet = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGateway")) {
                    result.transit_gateway = try deserializeAnalysisComponent(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableRoute")) {
                    result.transit_gateway_route_table_route = try deserializeTransitGatewayRouteTableRoute(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpc")) {
                    result.vpc = try deserializeAnalysisComponent(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePathFilter(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PathFilter {
    var result: PathFilter = undefined;
    result.destination_address = null;
    result.destination_port_range = null;
    result.source_address = null;
    result.source_port_range = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationAddress")) {
                    result.destination_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try deserializeFilterPortRange(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "sourceAddress")) {
                    result.source_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try deserializeFilterPortRange(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePathStatement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PathStatement {
    var result: PathStatement = undefined;
    result.packet_header_statement = null;
    result.resource_statement = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "packetHeaderStatement")) {
                    result.packet_header_statement = try deserializePacketHeaderStatement(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "resourceStatement")) {
                    result.resource_statement = try deserializeResourceStatement(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePciId(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PciId {
    var result: PciId = undefined;
    result.device_id = null;
    result.subsystem_id = null;
    result.subsystem_vendor_id = null;
    result.vendor_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "DeviceId")) {
                    result.device_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "SubsystemId")) {
                    result.subsystem_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "SubsystemVendorId")) {
                    result.subsystem_vendor_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "VendorId")) {
                    result.vendor_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePeeringAttachmentStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PeeringAttachmentStatus {
    var result: PeeringAttachmentStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePeeringConnectionOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PeeringConnectionOptions {
    _ = allocator;
    var result: PeeringConnectionOptions = undefined;
    result.allow_dns_resolution_from_remote_vpc = null;
    result.allow_egress_from_local_classic_link_to_remote_vpc = null;
    result.allow_egress_from_local_vpc_to_remote_classic_link = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allowDnsResolutionFromRemoteVpc")) {
                    result.allow_dns_resolution_from_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalClassicLinkToRemoteVpc")) {
                    result.allow_egress_from_local_classic_link_to_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalVpcToRemoteClassicLink")) {
                    result.allow_egress_from_local_vpc_to_remote_classic_link = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePeeringTgwInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PeeringTgwInfo {
    var result: PeeringTgwInfo = undefined;
    result.core_network_id = null;
    result.owner_id = null;
    result.region = null;
    result.transit_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coreNetworkId")) {
                    result.core_network_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePerformanceFactorReference(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PerformanceFactorReference {
    var result: PerformanceFactorReference = undefined;
    result.instance_family = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase1DHGroupNumbersListValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Phase1DHGroupNumbersListValue {
    _ = allocator;
    var result: Phase1DHGroupNumbersListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase1EncryptionAlgorithmsListValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Phase1EncryptionAlgorithmsListValue {
    var result: Phase1EncryptionAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase1IntegrityAlgorithmsListValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Phase1IntegrityAlgorithmsListValue {
    var result: Phase1IntegrityAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase2DHGroupNumbersListValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Phase2DHGroupNumbersListValue {
    _ = allocator;
    var result: Phase2DHGroupNumbersListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase2EncryptionAlgorithmsListValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Phase2EncryptionAlgorithmsListValue {
    var result: Phase2EncryptionAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase2IntegrityAlgorithmsListValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Phase2IntegrityAlgorithmsListValue {
    var result: Phase2IntegrityAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Placement {
    var result: Placement = undefined;
    result.affinity = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.group_id = null;
    result.group_name = null;
    result.host_id = null;
    result.host_resource_group_arn = null;
    result.partition_number = null;
    result.spread_domain = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "affinity")) {
                    result.affinity = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostResourceGroupArn")) {
                    result.host_resource_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "partitionNumber")) {
                    result.partition_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spreadDomain")) {
                    result.spread_domain = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = Tenancy.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacementGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PlacementGroup {
    var result: PlacementGroup = undefined;
    result.group_arn = null;
    result.group_id = null;
    result.group_name = null;
    result.linked_group_id = null;
    result.partition_count = null;
    result.spread_level = null;
    result.state = null;
    result.strategy = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupArn")) {
                    result.group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "linkedGroupId")) {
                    result.linked_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "partitionCount")) {
                    result.partition_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spreadLevel")) {
                    result.spread_level = SpreadLevel.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = PlacementGroupState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "strategy")) {
                    result.strategy = PlacementStrategy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacementGroupInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PlacementGroupInfo {
    var result: PlacementGroupInfo = undefined;
    result.supported_strategies = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "supportedStrategies")) {
                    result.supported_strategies = try deserializePlacementGroupStrategyList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacementResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PlacementResponse {
    var result: PlacementResponse = undefined;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePoolCidrBlock(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PoolCidrBlock {
    var result: PoolCidrBlock = undefined;
    result.cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "poolCidrBlock")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePortRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PortRange {
    _ = allocator;
    var result: PortRange = undefined;
    result.from = null;
    result.to = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "from")) {
                    result.from = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "to")) {
                    result.to = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixList(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrefixList {
    var result: PrefixList = undefined;
    result.cidrs = null;
    result.prefix_list_id = null;
    result.prefix_list_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrSet")) {
                    result.cidrs = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListName")) {
                    result.prefix_list_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixListAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrefixListAssociation {
    var result: PrefixListAssociation = undefined;
    result.resource_id = null;
    result.resource_owner = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixListEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrefixListEntry {
    var result: PrefixListEntry = undefined;
    result.cidr = null;
    result.description = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixListId(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrefixListId {
    var result: PrefixListId = undefined;
    result.description = null;
    result.prefix_list_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePriceSchedule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PriceSchedule {
    _ = allocator;
    var result: PriceSchedule = undefined;
    result.active = null;
    result.currency_code = null;
    result.price = null;
    result.term = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "active")) {
                    result.active = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = CurrencyCodeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "price")) {
                    result.price = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "term")) {
                    result.term = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePricingDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PricingDetail {
    _ = allocator;
    var result: PricingDetail = undefined;
    result.count = null;
    result.price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "price")) {
                    result.price = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrincipalIdFormat(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrincipalIdFormat {
    var result: PrincipalIdFormat = undefined;
    result.arn = null;
    result.statuses = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusSet")) {
                    result.statuses = try deserializeIdFormatList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrivateDnsDetails {
    var result: PrivateDnsDetails = undefined;
    result.private_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsNameConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrivateDnsNameConfiguration {
    var result: PrivateDnsNameConfiguration = undefined;
    result.name = null;
    result.state = null;
    result.@"type" = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = DnsNameState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsNameOptionsOnLaunch(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrivateDnsNameOptionsOnLaunch {
    _ = allocator;
    var result: PrivateDnsNameOptionsOnLaunch = undefined;
    result.enable_resource_name_dns_aaaa_record = null;
    result.enable_resource_name_dns_a_record = null;
    result.hostname_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableResourceNameDnsAAAARecord")) {
                    result.enable_resource_name_dns_aaaa_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableResourceNameDnsARecord")) {
                    result.enable_resource_name_dns_a_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hostnameType")) {
                    result.hostname_type = HostnameType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsNameOptionsResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrivateDnsNameOptionsResponse {
    _ = allocator;
    var result: PrivateDnsNameOptionsResponse = undefined;
    result.enable_resource_name_dns_aaaa_record = null;
    result.enable_resource_name_dns_a_record = null;
    result.hostname_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableResourceNameDnsAAAARecord")) {
                    result.enable_resource_name_dns_aaaa_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableResourceNameDnsARecord")) {
                    result.enable_resource_name_dns_a_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hostnameType")) {
                    result.hostname_type = HostnameType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateIpAddressSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PrivateIpAddressSpecification {
    var result: PrivateIpAddressSpecification = undefined;
    result.primary = null;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "primary")) {
                    result.primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeProcessorInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ProcessorInfo {
    var result: ProcessorInfo = undefined;
    result.manufacturer = null;
    result.supported_architectures = null;
    result.supported_features = null;
    result.sustained_clock_speed_in_ghz = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "supportedArchitectures")) {
                    result.supported_architectures = try deserializeArchitectureTypeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "supportedFeatures")) {
                    result.supported_features = try deserializeSupportedAdditionalProcessorFeatureList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sustainedClockSpeedInGhz")) {
                    result.sustained_clock_speed_in_ghz = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeProductCode(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ProductCode {
    var result: ProductCode = undefined;
    result.product_code_id = null;
    result.product_code_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "productCode")) {
                    result.product_code_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.product_code_type = ProductCodeValues.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePropagatingVgw(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PropagatingVgw {
    var result: PropagatingVgw = undefined;
    result.gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeProvisionedBandwidth(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ProvisionedBandwidth {
    var result: ProvisionedBandwidth = undefined;
    result.provisioned = null;
    result.provision_time = null;
    result.requested = null;
    result.request_time = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "provisioned")) {
                    result.provisioned = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisionTime")) {
                    result.provision_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "requested")) {
                    result.requested = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "requestTime")) {
                    result.request_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePtrUpdateStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PtrUpdateStatus {
    var result: PtrUpdateStatus = undefined;
    result.reason = null;
    result.status = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reason")) {
                    result.reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePublicIpDnsNameOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PublicIpDnsNameOptions {
    var result: PublicIpDnsNameOptions = undefined;
    result.dns_hostname_type = null;
    result.public_dual_stack_dns_name = null;
    result.public_ipv_4_dns_name = null;
    result.public_ipv_6_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsHostnameType")) {
                    result.dns_hostname_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDualStackDnsName")) {
                    result.public_dual_stack_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv4DnsName")) {
                    result.public_ipv_4_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv6DnsName")) {
                    result.public_ipv_6_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePublicIpv4Pool(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PublicIpv4Pool {
    var result: PublicIpv4Pool = undefined;
    result.description = null;
    result.network_border_group = null;
    result.pool_address_ranges = null;
    result.pool_id = null;
    result.tags = null;
    result.total_address_count = null;
    result.total_available_address_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolAddressRangeSet")) {
                    result.pool_address_ranges = try deserializePublicIpv4PoolRangeSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "poolId")) {
                    result.pool_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "totalAddressCount")) {
                    result.total_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalAvailableAddressCount")) {
                    result.total_available_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePublicIpv4PoolRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !PublicIpv4PoolRange {
    var result: PublicIpv4PoolRange = undefined;
    result.address_count = null;
    result.available_address_count = null;
    result.first_address = null;
    result.last_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressCount")) {
                    result.address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "availableAddressCount")) {
                    result.available_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "firstAddress")) {
                    result.first_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastAddress")) {
                    result.last_address = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePurchase(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Purchase {
    var result: Purchase = undefined;
    result.currency_code = null;
    result.duration = null;
    result.host_id_set = null;
    result.host_reservation_id = null;
    result.hourly_price = null;
    result.instance_family = null;
    result.payment_option = null;
    result.upfront_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = CurrencyCodeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "hostIdSet")) {
                    result.host_id_set = try deserializeResponseHostIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "hostReservationId")) {
                    result.host_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "paymentOption")) {
                    result.payment_option = PaymentOption.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontPrice")) {
                    result.upfront_price = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRecurringCharge(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RecurringCharge {
    _ = allocator;
    var result: RecurringCharge = undefined;
    result.amount = null;
    result.frequency = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amount")) {
                    result.amount = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "frequency")) {
                    result.frequency = RecurringChargeFrequency.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReferencedSecurityGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReferencedSecurityGroup {
    var result: ReferencedSecurityGroup = undefined;
    result.group_id = null;
    result.peering_status = null;
    result.user_id = null;
    result.vpc_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringStatus")) {
                    result.peering_status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRegion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Region {
    var result: Region = undefined;
    result.endpoint = null;
    result.opt_in_status = null;
    result.region_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "regionEndpoint")) {
                    result.endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "optInStatus")) {
                    result.opt_in_status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRegionalSummary(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RegionalSummary {
    var result: RegionalSummary = undefined;
    result.number_of_matched_accounts = null;
    result.number_of_unmatched_accounts = null;
    result.region_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "numberOfMatchedAccounts")) {
                    result.number_of_matched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "numberOfUnmatchedAccounts")) {
                    result.number_of_unmatched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRegisteredInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RegisteredInstance {
    var result: RegisteredInstance = undefined;
    result.ha_status = null;
    result.instance_id = null;
    result.last_updated_time = null;
    result.processing_status = null;
    result.sql_server_credentials = null;
    result.sql_server_license_usage = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "haStatus")) {
                    result.ha_status = HaStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "processingStatus")) {
                    result.processing_status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sqlServerCredentials")) {
                    result.sql_server_credentials = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sqlServerLicenseUsage")) {
                    result.sql_server_license_usage = SqlServerLicenseUsage.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReplaceRootVolumeTask(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReplaceRootVolumeTask {
    var result: ReplaceRootVolumeTask = undefined;
    result.complete_time = null;
    result.delete_replaced_root_volume = null;
    result.image_id = null;
    result.instance_id = null;
    result.replace_root_volume_task_id = null;
    result.snapshot_id = null;
    result.start_time = null;
    result.tags = null;
    result.task_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "completeTime")) {
                    result.complete_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deleteReplacedRootVolume")) {
                    result.delete_replaced_root_volume = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "replaceRootVolumeTaskId")) {
                    result.replace_root_volume_task_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "taskState")) {
                    result.task_state = ReplaceRootVolumeTaskState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Reservation {
    var result: Reservation = undefined;
    result.groups = null;
    result.instances = null;
    result.owner_id = null;
    result.requester_id = null;
    result.reservation_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "instancesSet")) {
                    result.instances = try deserializeInstanceList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "requesterId")) {
                    result.requester_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationId")) {
                    result.reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservationValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservationValue {
    var result: ReservationValue = undefined;
    result.hourly_price = null;
    result.remaining_total_value = null;
    result.remaining_upfront_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remainingTotalValue")) {
                    result.remaining_total_value = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remainingUpfrontValue")) {
                    result.remaining_upfront_value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstanceReservationValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstanceReservationValue {
    var result: ReservedInstanceReservationValue = undefined;
    result.reservation_value = null;
    result.reserved_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservationValue")) {
                    result.reservation_value = try deserializeReservationValue(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "reservedInstanceId")) {
                    result.reserved_instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstances(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstances {
    @setEvalBranchQuota(2000);
    var result: ReservedInstances = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.currency_code = null;
    result.duration = null;
    result.end = null;
    result.fixed_price = null;
    result.instance_count = null;
    result.instance_tenancy = null;
    result.instance_type = null;
    result.offering_class = null;
    result.offering_type = null;
    result.product_description = null;
    result.recurring_charges = null;
    result.reserved_instances_id = null;
    result.scope = null;
    result.start = null;
    result.state = null;
    result.tags = null;
    result.usage_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = CurrencyCodeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "end")) {
                    result.end = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fixedPrice")) {
                    result.fixed_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceTenancy")) {
                    result.instance_tenancy = Tenancy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringClass")) {
                    result.offering_class = OfferingClassType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringType")) {
                    result.offering_type = OfferingTypeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = RIProductDescription.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recurringCharges")) {
                    result.recurring_charges = try deserializeRecurringChargesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = scope.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "start")) {
                    result.start = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = ReservedInstanceState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "usagePrice")) {
                    result.usage_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstancesConfiguration {
    @setEvalBranchQuota(2000);
    var result: ReservedInstancesConfiguration = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_count = null;
    result.instance_type = null;
    result.platform = null;
    result.scope = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = scope.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesId(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstancesId {
    var result: ReservedInstancesId = undefined;
    result.reserved_instances_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesListing(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstancesListing {
    var result: ReservedInstancesListing = undefined;
    result.client_token = null;
    result.create_date = null;
    result.instance_counts = null;
    result.price_schedules = null;
    result.reserved_instances_id = null;
    result.reserved_instances_listing_id = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    result.update_date = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceCounts")) {
                    result.instance_counts = try deserializeInstanceCountList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "priceSchedules")) {
                    result.price_schedules = try deserializePriceScheduleList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservedInstancesListingId")) {
                    result.reserved_instances_listing_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = ListingStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "updateDate")) {
                    result.update_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesModification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstancesModification {
    var result: ReservedInstancesModification = undefined;
    result.client_token = null;
    result.create_date = null;
    result.effective_date = null;
    result.modification_results = null;
    result.reserved_instances_ids = null;
    result.reserved_instances_modification_id = null;
    result.status = null;
    result.status_message = null;
    result.update_date = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "effectiveDate")) {
                    result.effective_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "modificationResultSet")) {
                    result.modification_results = try deserializeReservedInstancesModificationResultList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesSet")) {
                    result.reserved_instances_ids = try deserializeReservedIntancesIds(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesModificationId")) {
                    result.reserved_instances_modification_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updateDate")) {
                    result.update_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesModificationResult(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstancesModificationResult {
    var result: ReservedInstancesModificationResult = undefined;
    result.reserved_instances_id = null;
    result.target_configuration = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetConfiguration")) {
                    result.target_configuration = try deserializeReservedInstancesConfiguration(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesOffering(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ReservedInstancesOffering {
    @setEvalBranchQuota(2000);
    var result: ReservedInstancesOffering = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.currency_code = null;
    result.duration = null;
    result.fixed_price = null;
    result.instance_tenancy = null;
    result.instance_type = null;
    result.marketplace = null;
    result.offering_class = null;
    result.offering_type = null;
    result.pricing_details = null;
    result.product_description = null;
    result.recurring_charges = null;
    result.reserved_instances_offering_id = null;
    result.scope = null;
    result.usage_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = CurrencyCodeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "fixedPrice")) {
                    result.fixed_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceTenancy")) {
                    result.instance_tenancy = Tenancy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "marketplace")) {
                    result.marketplace = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "offeringClass")) {
                    result.offering_class = OfferingClassType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringType")) {
                    result.offering_type = OfferingTypeValues.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "pricingDetailsSet")) {
                    result.pricing_details = try deserializePricingDetailsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = RIProductDescription.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recurringCharges")) {
                    result.recurring_charges = try deserializeRecurringChargesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesOfferingId")) {
                    result.reserved_instances_offering_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = scope.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usagePrice")) {
                    result.usage_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeResourceStatement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ResourceStatement {
    var result: ResourceStatement = undefined;
    result.resources = null;
    result.resource_types = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceSet")) {
                    result.resources = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "resourceTypeSet")) {
                    result.resource_types = try deserializeValueStringList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeResponseError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ResponseError {
    var result: ResponseError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = LaunchTemplateErrorCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeResponseLaunchTemplateData(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ResponseLaunchTemplateData {
    @setEvalBranchQuota(2000);
    var result: ResponseLaunchTemplateData = undefined;
    result.block_device_mappings = null;
    result.capacity_reservation_specification = null;
    result.cpu_options = null;
    result.credit_specification = null;
    result.disable_api_stop = null;
    result.disable_api_termination = null;
    result.ebs_optimized = null;
    result.elastic_gpu_specifications = null;
    result.elastic_inference_accelerators = null;
    result.enclave_options = null;
    result.hibernation_options = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_initiated_shutdown_behavior = null;
    result.instance_market_options = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.kernel_id = null;
    result.key_name = null;
    result.license_specifications = null;
    result.maintenance_options = null;
    result.metadata_options = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.network_performance_options = null;
    result.operator = null;
    result.placement = null;
    result.private_dns_name_options = null;
    result.ram_disk_id = null;
    result.security_group_ids = null;
    result.security_groups = null;
    result.tag_specifications = null;
    result.user_data = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blockDeviceMappingSet")) {
                    result.block_device_mappings = try deserializeLaunchTemplateBlockDeviceMappingList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "capacityReservationSpecification")) {
                    result.capacity_reservation_specification = try deserializeLaunchTemplateCapacityReservationSpecificationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "cpuOptions")) {
                    result.cpu_options = try deserializeLaunchTemplateCpuOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "creditSpecification")) {
                    result.credit_specification = try deserializeCreditSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "disableApiStop")) {
                    result.disable_api_stop = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "disableApiTermination")) {
                    result.disable_api_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "elasticGpuSpecificationSet")) {
                    result.elastic_gpu_specifications = try deserializeElasticGpuSpecificationResponseList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorSet")) {
                    result.elastic_inference_accelerators = try deserializeLaunchTemplateElasticInferenceAcceleratorResponseList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "enclaveOptions")) {
                    result.enclave_options = try deserializeLaunchTemplateEnclaveOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "hibernationOptions")) {
                    result.hibernation_options = try deserializeLaunchTemplateHibernationOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeLaunchTemplateIamInstanceProfileSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInitiatedShutdownBehavior")) {
                    result.instance_initiated_shutdown_behavior = ShutdownBehavior.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMarketOptions")) {
                    result.instance_market_options = try deserializeLaunchTemplateInstanceMarketOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "licenseSet")) {
                    result.license_specifications = try deserializeLaunchTemplateLicenseList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "maintenanceOptions")) {
                    result.maintenance_options = try deserializeLaunchTemplateInstanceMaintenanceOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "metadataOptions")) {
                    result.metadata_options = try deserializeLaunchTemplateInstanceMetadataOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeLaunchTemplatesMonitoring(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeLaunchTemplateInstanceNetworkInterfaceSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkPerformanceOptions")) {
                    result.network_performance_options = try deserializeLaunchTemplateNetworkPerformanceOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializeLaunchTemplatePlacement(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "privateDnsNameOptions")) {
                    result.private_dns_name_options = try deserializeLaunchTemplatePrivateDnsNameOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ramDiskId")) {
                    result.ram_disk_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "securityGroupSet")) {
                    result.security_groups = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSpecificationSet")) {
                    result.tag_specifications = try deserializeLaunchTemplateTagSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "userData")) {
                    result.user_data = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRevokedSecurityGroupRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RevokedSecurityGroupRule {
    var result: RevokedSecurityGroupRule = undefined;
    result.cidr_ipv_4 = null;
    result.cidr_ipv_6 = null;
    result.description = null;
    result.from_port = null;
    result.group_id = null;
    result.ip_protocol = null;
    result.is_egress = null;
    result.prefix_list_id = null;
    result.referenced_group_id = null;
    result.security_group_rule_id = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIpv4")) {
                    result.cidr_ipv_4 = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrIpv6")) {
                    result.cidr_ipv_6 = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isEgress")) {
                    result.is_egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "referencedGroupId")) {
                    result.referenced_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupRuleId")) {
                    result.security_group_rule_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Route {
    var result: Route = undefined;
    result.carrier_gateway_id = null;
    result.core_network_arn = null;
    result.destination_cidr_block = null;
    result.destination_ipv_6_cidr_block = null;
    result.destination_prefix_list_id = null;
    result.egress_only_internet_gateway_id = null;
    result.gateway_id = null;
    result.instance_id = null;
    result.instance_owner_id = null;
    result.ip_address = null;
    result.local_gateway_id = null;
    result.nat_gateway_id = null;
    result.network_interface_id = null;
    result.odb_network_arn = null;
    result.origin = null;
    result.state = null;
    result.transit_gateway_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierGatewayId")) {
                    result.carrier_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkArn")) {
                    result.core_network_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationIpv6CidrBlock")) {
                    result.destination_ipv_6_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListId")) {
                    result.destination_prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressOnlyInternetGatewayId")) {
                    result.egress_only_internet_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceOwnerId")) {
                    result.instance_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddress")) {
                    result.ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGatewayId")) {
                    result.nat_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "odbNetworkArn")) {
                    result.odb_network_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "origin")) {
                    result.origin = RouteOrigin.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = RouteState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServer(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServer {
    var result: RouteServer = undefined;
    result.amazon_side_asn = null;
    result.persist_routes_duration = null;
    result.persist_routes_state = null;
    result.route_server_id = null;
    result.sns_notifications_enabled = null;
    result.sns_topic_arn = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amazonSideAsn")) {
                    result.amazon_side_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "persistRoutesDuration")) {
                    result.persist_routes_duration = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "persistRoutesState")) {
                    result.persist_routes_state = RouteServerPersistRoutesState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snsNotificationsEnabled")) {
                    result.sns_notifications_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "snsTopicArn")) {
                    result.sns_topic_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = RouteServerState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerAssociation {
    var result: RouteServerAssociation = undefined;
    result.route_server_id = null;
    result.state = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = RouteServerAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerBfdStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerBfdStatus {
    _ = allocator;
    var result: RouteServerBfdStatus = undefined;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "status")) {
                    result.status = RouteServerBfdState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerBgpOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerBgpOptions {
    _ = allocator;
    var result: RouteServerBgpOptions = undefined;
    result.peer_asn = null;
    result.peer_liveness_detection = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "peerAsn")) {
                    result.peer_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "peerLivenessDetection")) {
                    result.peer_liveness_detection = RouteServerPeerLivenessMode.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerBgpStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerBgpStatus {
    _ = allocator;
    var result: RouteServerBgpStatus = undefined;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "status")) {
                    result.status = RouteServerBgpState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerEndpoint(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerEndpoint {
    var result: RouteServerEndpoint = undefined;
    result.eni_address = null;
    result.eni_id = null;
    result.failure_reason = null;
    result.route_server_endpoint_id = null;
    result.route_server_id = null;
    result.state = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eniAddress")) {
                    result.eni_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eniId")) {
                    result.eni_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerEndpointId")) {
                    result.route_server_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = RouteServerEndpointState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerPeer(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerPeer {
    var result: RouteServerPeer = undefined;
    result.bfd_status = null;
    result.bgp_options = null;
    result.bgp_status = null;
    result.endpoint_eni_address = null;
    result.endpoint_eni_id = null;
    result.failure_reason = null;
    result.peer_address = null;
    result.route_server_endpoint_id = null;
    result.route_server_id = null;
    result.route_server_peer_id = null;
    result.state = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bfdStatus")) {
                    result.bfd_status = try deserializeRouteServerBfdStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "bgpOptions")) {
                    result.bgp_options = try deserializeRouteServerBgpOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "bgpStatus")) {
                    result.bgp_status = try deserializeRouteServerBgpStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "endpointEniAddress")) {
                    result.endpoint_eni_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endpointEniId")) {
                    result.endpoint_eni_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerEndpointId")) {
                    result.route_server_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerPeerId")) {
                    result.route_server_peer_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = RouteServerPeerState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerPropagation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerPropagation {
    var result: RouteServerPropagation = undefined;
    result.route_server_id = null;
    result.route_table_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = RouteServerPropagationState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerRoute {
    var result: RouteServerRoute = undefined;
    result.as_paths = null;
    result.med = null;
    result.next_hop_ip = null;
    result.prefix = null;
    result.route_installation_details = null;
    result.route_server_endpoint_id = null;
    result.route_server_peer_id = null;
    result.route_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "asPathSet")) {
                    result.as_paths = try deserializeAsPath(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "med")) {
                    result.med = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "nextHopIp")) {
                    result.next_hop_ip = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeInstallationDetailSet")) {
                    result.route_installation_details = try deserializeRouteServerRouteInstallationDetails(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "routeServerEndpointId")) {
                    result.route_server_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerPeerId")) {
                    result.route_server_peer_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeStatus")) {
                    result.route_status = RouteServerRouteStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerRouteInstallationDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteServerRouteInstallationDetail {
    var result: RouteServerRouteInstallationDetail = undefined;
    result.route_installation_status = null;
    result.route_installation_status_reason = null;
    result.route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "routeInstallationStatus")) {
                    result.route_installation_status = RouteServerRouteInstallationStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeInstallationStatusReason")) {
                    result.route_installation_status_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteTable(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteTable {
    var result: RouteTable = undefined;
    result.associations = null;
    result.owner_id = null;
    result.propagating_vgws = null;
    result.routes = null;
    result.route_table_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationSet")) {
                    result.associations = try deserializeRouteTableAssociationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "propagatingVgwSet")) {
                    result.propagating_vgws = try deserializePropagatingVgwList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "routeSet")) {
                    result.routes = try deserializeRouteList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteTableAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteTableAssociation {
    var result: RouteTableAssociation = undefined;
    result.association_state = null;
    result.gateway_id = null;
    result.main = null;
    result.public_ipv_4_pool = null;
    result.route_table_association_id = null;
    result.route_table_id = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationState")) {
                    result.association_state = try deserializeRouteTableAssociationState(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "main")) {
                    result.main = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicIpv4Pool")) {
                    result.public_ipv_4_pool = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableAssociationId")) {
                    result.route_table_association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteTableAssociationState(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RouteTableAssociationState {
    var result: RouteTableAssociationState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = RouteTableAssociationStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRuleGroupRuleOptionsPair(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RuleGroupRuleOptionsPair {
    var result: RuleGroupRuleOptionsPair = undefined;
    result.rule_group_arn = null;
    result.rule_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleOptionSet")) {
                    result.rule_options = try deserializeRuleOptionList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRuleGroupTypePair(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RuleGroupTypePair {
    var result: RuleGroupTypePair = undefined;
    result.rule_group_arn = null;
    result.rule_group_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleGroupType")) {
                    result.rule_group_type = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRuleOption(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RuleOption {
    var result: RuleOption = undefined;
    result.keyword = null;
    result.settings = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "keyword")) {
                    result.keyword = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "settingSet")) {
                    result.settings = try deserializeStringList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRunInstancesMonitoringEnabled(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !RunInstancesMonitoringEnabled {
    _ = allocator;
    var result: RunInstancesMonitoringEnabled = undefined;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeS3Storage(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !S3Storage {
    var result: S3Storage = undefined;
    result.aws_access_key_id = null;
    result.bucket = null;
    result.prefix = null;
    result.upload_policy = null;
    result.upload_policy_signature = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "AWSAccessKeyId")) {
                    result.aws_access_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bucket")) {
                    result.bucket = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "uploadPolicy")) {
                    result.upload_policy = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "uploadPolicySignature")) {
                    result.upload_policy_signature = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeScheduledInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ScheduledInstance {
    var result: ScheduledInstance = undefined;
    result.availability_zone = null;
    result.create_date = null;
    result.hourly_price = null;
    result.instance_count = null;
    result.instance_type = null;
    result.network_platform = null;
    result.next_slot_start_time = null;
    result.platform = null;
    result.previous_slot_end_time = null;
    result.recurrence = null;
    result.scheduled_instance_id = null;
    result.slot_duration_in_hours = null;
    result.term_end_date = null;
    result.term_start_date = null;
    result.total_scheduled_instance_hours = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkPlatform")) {
                    result.network_platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nextSlotStartTime")) {
                    result.next_slot_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousSlotEndTime")) {
                    result.previous_slot_end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recurrence")) {
                    result.recurrence = try deserializeScheduledInstanceRecurrence(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "scheduledInstanceId")) {
                    result.scheduled_instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "slotDurationInHours")) {
                    result.slot_duration_in_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "termEndDate")) {
                    result.term_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "termStartDate")) {
                    result.term_start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "totalScheduledInstanceHours")) {
                    result.total_scheduled_instance_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeScheduledInstanceAvailability(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ScheduledInstanceAvailability {
    var result: ScheduledInstanceAvailability = undefined;
    result.availability_zone = null;
    result.available_instance_count = null;
    result.first_slot_start_time = null;
    result.hourly_price = null;
    result.instance_type = null;
    result.max_term_duration_in_days = null;
    result.min_term_duration_in_days = null;
    result.network_platform = null;
    result.platform = null;
    result.purchase_token = null;
    result.recurrence = null;
    result.slot_duration_in_hours = null;
    result.total_scheduled_instance_hours = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableInstanceCount")) {
                    result.available_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "firstSlotStartTime")) {
                    result.first_slot_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxTermDurationInDays")) {
                    result.max_term_duration_in_days = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "minTermDurationInDays")) {
                    result.min_term_duration_in_days = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkPlatform")) {
                    result.network_platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "purchaseToken")) {
                    result.purchase_token = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recurrence")) {
                    result.recurrence = try deserializeScheduledInstanceRecurrence(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "slotDurationInHours")) {
                    result.slot_duration_in_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalScheduledInstanceHours")) {
                    result.total_scheduled_instance_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeScheduledInstanceRecurrence(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ScheduledInstanceRecurrence {
    var result: ScheduledInstanceRecurrence = undefined;
    result.frequency = null;
    result.interval = null;
    result.occurrence_day_set = null;
    result.occurrence_relative_to_end = null;
    result.occurrence_unit = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "frequency")) {
                    result.frequency = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "interval")) {
                    result.interval = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "occurrenceDaySet")) {
                    result.occurrence_day_set = try deserializeOccurrenceDaySet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "occurrenceRelativeToEnd")) {
                    result.occurrence_relative_to_end = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "occurrenceUnit")) {
                    result.occurrence_unit = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SecurityGroup {
    var result: SecurityGroup = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.ip_permissions = null;
    result.ip_permissions_egress = null;
    result.owner_id = null;
    result.security_group_arn = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupDescription")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipPermissions")) {
                    result.ip_permissions = try deserializeIpPermissionList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipPermissionsEgress")) {
                    result.ip_permissions_egress = try deserializeIpPermissionList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupArn")) {
                    result.security_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupForVpc(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SecurityGroupForVpc {
    var result: SecurityGroupForVpc = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.owner_id = null;
    result.primary_vpc_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "primaryVpcId")) {
                    result.primary_vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupIdentifier(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SecurityGroupIdentifier {
    var result: SecurityGroupIdentifier = undefined;
    result.group_id = null;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupReference(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SecurityGroupReference {
    var result: SecurityGroupReference = undefined;
    result.group_id = null;
    result.referencing_vpc_id = null;
    result.transit_gateway_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "referencingVpcId")) {
                    result.referencing_vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SecurityGroupRule {
    var result: SecurityGroupRule = undefined;
    result.cidr_ipv_4 = null;
    result.cidr_ipv_6 = null;
    result.description = null;
    result.from_port = null;
    result.group_id = null;
    result.group_owner_id = null;
    result.ip_protocol = null;
    result.is_egress = null;
    result.prefix_list_id = null;
    result.referenced_group_info = null;
    result.security_group_rule_arn = null;
    result.security_group_rule_id = null;
    result.tags = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIpv4")) {
                    result.cidr_ipv_4 = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrIpv6")) {
                    result.cidr_ipv_6 = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupOwnerId")) {
                    result.group_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isEgress")) {
                    result.is_egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "referencedGroupInfo")) {
                    result.referenced_group_info = try deserializeReferencedSecurityGroup(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "securityGroupRuleArn")) {
                    result.security_group_rule_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupRuleId")) {
                    result.security_group_rule_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupVpcAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SecurityGroupVpcAssociation {
    var result: SecurityGroupVpcAssociation = undefined;
    result.group_id = null;
    result.group_owner_id = null;
    result.state = null;
    result.state_reason = null;
    result.vpc_id = null;
    result.vpc_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupOwnerId")) {
                    result.group_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = SecurityGroupVpcAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcOwnerId")) {
                    result.vpc_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ServiceConfiguration {
    var result: ServiceConfiguration = undefined;
    result.acceptance_required = null;
    result.availability_zone_ids = null;
    result.availability_zones = null;
    result.base_endpoint_dns_names = null;
    result.gateway_load_balancer_arns = null;
    result.manages_vpc_endpoints = null;
    result.network_load_balancer_arns = null;
    result.payer_responsibility = null;
    result.private_dns_name = null;
    result.private_dns_name_configuration = null;
    result.remote_access_enabled = null;
    result.service_id = null;
    result.service_name = null;
    result.service_state = null;
    result.service_type = null;
    result.supported_ip_address_types = null;
    result.supported_regions = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceptanceRequired")) {
                    result.acceptance_required = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneIdSet")) {
                    result.availability_zone_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneSet")) {
                    result.availability_zones = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "baseEndpointDnsNameSet")) {
                    result.base_endpoint_dns_names = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "gatewayLoadBalancerArnSet")) {
                    result.gateway_load_balancer_arns = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "managesVpcEndpoints")) {
                    result.manages_vpc_endpoints = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "networkLoadBalancerArnSet")) {
                    result.network_load_balancer_arns = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "payerResponsibility")) {
                    result.payer_responsibility = PayerResponsibility.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameConfiguration")) {
                    result.private_dns_name_configuration = try deserializePrivateDnsNameConfiguration(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "remoteAccessEnabled")) {
                    result.remote_access_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceState")) {
                    result.service_state = ServiceState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceType")) {
                    result.service_type = try deserializeServiceTypeDetailSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "supportedIpAddressTypeSet")) {
                    result.supported_ip_address_types = try deserializeSupportedIpAddressTypes(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "supportedRegionSet")) {
                    result.supported_regions = try deserializeSupportedRegionSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ServiceDetail {
    var result: ServiceDetail = undefined;
    result.acceptance_required = null;
    result.availability_zone_ids = null;
    result.availability_zones = null;
    result.base_endpoint_dns_names = null;
    result.manages_vpc_endpoints = null;
    result.owner = null;
    result.payer_responsibility = null;
    result.private_dns_name = null;
    result.private_dns_names = null;
    result.private_dns_name_verification_state = null;
    result.service_id = null;
    result.service_name = null;
    result.service_region = null;
    result.service_type = null;
    result.supported_ip_address_types = null;
    result.tags = null;
    result.vpc_endpoint_policy_supported = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceptanceRequired")) {
                    result.acceptance_required = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneIdSet")) {
                    result.availability_zone_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneSet")) {
                    result.availability_zones = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "baseEndpointDnsNameSet")) {
                    result.base_endpoint_dns_names = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "managesVpcEndpoints")) {
                    result.manages_vpc_endpoints = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "owner")) {
                    result.owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "payerResponsibility")) {
                    result.payer_responsibility = PayerResponsibility.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameSet")) {
                    result.private_dns_names = try deserializePrivateDnsDetailsSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "privateDnsNameVerificationState")) {
                    result.private_dns_name_verification_state = DnsNameState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceRegion")) {
                    result.service_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceType")) {
                    result.service_type = try deserializeServiceTypeDetailSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "supportedIpAddressTypeSet")) {
                    result.supported_ip_address_types = try deserializeSupportedIpAddressTypes(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointPolicySupported")) {
                    result.vpc_endpoint_policy_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceLinkVirtualInterface(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ServiceLinkVirtualInterface {
    var result: ServiceLinkVirtualInterface = undefined;
    result.configuration_state = null;
    result.local_address = null;
    result.outpost_arn = null;
    result.outpost_id = null;
    result.outpost_lag_id = null;
    result.owner_id = null;
    result.peer_address = null;
    result.peer_bgp_asn = null;
    result.service_link_virtual_interface_arn = null;
    result.service_link_virtual_interface_id = null;
    result.tags = null;
    result.vlan = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configurationState")) {
                    result.configuration_state = ServiceLinkVirtualInterfaceConfigurationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localAddress")) {
                    result.local_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostId")) {
                    result.outpost_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostLagId")) {
                    result.outpost_lag_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerBgpAsn")) {
                    result.peer_bgp_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "serviceLinkVirtualInterfaceArn")) {
                    result.service_link_virtual_interface_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceLinkVirtualInterfaceId")) {
                    result.service_link_virtual_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vlan")) {
                    result.vlan = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceTypeDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ServiceTypeDetail {
    _ = allocator;
    var result: ServiceTypeDetail = undefined;
    result.service_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "serviceType")) {
                    result.service_type = ServiceType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshot(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Snapshot {
    var result: Snapshot = undefined;
    result.availability_zone = null;
    result.completion_duration_minutes = null;
    result.completion_time = null;
    result.data_encryption_key_id = null;
    result.description = null;
    result.encrypted = null;
    result.full_snapshot_size_in_bytes = null;
    result.kms_key_id = null;
    result.outpost_arn = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.progress = null;
    result.restore_expiry_time = null;
    result.snapshot_id = null;
    result.sse_type = null;
    result.start_time = null;
    result.state = null;
    result.state_message = null;
    result.storage_tier = null;
    result.tags = null;
    result.transfer_type = null;
    result.volume_id = null;
    result.volume_size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "completionDurationMinutes")) {
                    result.completion_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "completionTime")) {
                    result.completion_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dataEncryptionKeyId")) {
                    result.data_encryption_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "fullSnapshotSizeInBytes")) {
                    result.full_snapshot_size_in_bytes = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "restoreExpiryTime")) {
                    result.restore_expiry_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseType")) {
                    result.sse_type = SSEType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.state = SnapshotState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storageTier")) {
                    result.storage_tier = StorageTier.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transferType")) {
                    result.transfer_type = TransferType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SnapshotDetail {
    var result: SnapshotDetail = undefined;
    result.description = null;
    result.device_name = null;
    result.disk_image_size = null;
    result.format = null;
    result.progress = null;
    result.snapshot_id = null;
    result.status = null;
    result.status_message = null;
    result.url = null;
    result.user_bucket = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "diskImageSize")) {
                    result.disk_image_size = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "format")) {
                    result.format = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "url")) {
                    result.url = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userBucket")) {
                    result.user_bucket = try deserializeUserBucketDetails(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SnapshotInfo {
    var result: SnapshotInfo = undefined;
    result.availability_zone = null;
    result.description = null;
    result.encrypted = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.progress = null;
    result.snapshot_id = null;
    result.sse_type = null;
    result.start_time = null;
    result.state = null;
    result.tags = null;
    result.volume_id = null;
    result.volume_size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseType")) {
                    result.sse_type = SSEType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = SnapshotState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotRecycleBinInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SnapshotRecycleBinInfo {
    var result: SnapshotRecycleBinInfo = undefined;
    result.description = null;
    result.recycle_bin_enter_time = null;
    result.recycle_bin_exit_time = null;
    result.snapshot_id = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recycleBinEnterTime")) {
                    result.recycle_bin_enter_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recycleBinExitTime")) {
                    result.recycle_bin_exit_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotTaskDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SnapshotTaskDetail {
    var result: SnapshotTaskDetail = undefined;
    result.description = null;
    result.disk_image_size = null;
    result.encrypted = null;
    result.format = null;
    result.kms_key_id = null;
    result.progress = null;
    result.snapshot_id = null;
    result.status = null;
    result.status_message = null;
    result.url = null;
    result.user_bucket = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "diskImageSize")) {
                    result.disk_image_size = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "format")) {
                    result.format = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "url")) {
                    result.url = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userBucket")) {
                    result.user_bucket = try deserializeUserBucketDetails(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotTierStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SnapshotTierStatus {
    var result: SnapshotTierStatus = undefined;
    result.archival_complete_time = null;
    result.last_tiering_operation_status = null;
    result.last_tiering_operation_status_detail = null;
    result.last_tiering_progress = null;
    result.last_tiering_start_time = null;
    result.owner_id = null;
    result.restore_expiry_time = null;
    result.snapshot_id = null;
    result.status = null;
    result.storage_tier = null;
    result.tags = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "archivalCompleteTime")) {
                    result.archival_complete_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lastTieringOperationStatus")) {
                    result.last_tiering_operation_status = TieringOperationStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastTieringOperationStatusDetail")) {
                    result.last_tiering_operation_status_detail = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastTieringProgress")) {
                    result.last_tiering_progress = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "lastTieringStartTime")) {
                    result.last_tiering_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "restoreExpiryTime")) {
                    result.restore_expiry_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = SnapshotState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storageTier")) {
                    result.storage_tier = StorageTier.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotCapacityRebalance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotCapacityRebalance {
    _ = allocator;
    var result: SpotCapacityRebalance = undefined;
    result.replacement_strategy = null;
    result.termination_delay = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "replacementStrategy")) {
                    result.replacement_strategy = ReplacementStrategy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "terminationDelay")) {
                    result.termination_delay = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotDatafeedSubscription(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotDatafeedSubscription {
    var result: SpotDatafeedSubscription = undefined;
    result.bucket = null;
    result.fault = null;
    result.owner_id = null;
    result.prefix = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bucket")) {
                    result.bucket = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fault")) {
                    result.fault = try deserializeSpotInstanceStateFault(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = DatafeedSubscriptionState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetLaunchSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotFleetLaunchSpecification {
    @setEvalBranchQuota(2000);
    var result: SpotFleetLaunchSpecification = undefined;
    result.addressing_type = null;
    result.block_device_mappings = null;
    result.ebs_optimized = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.kernel_id = null;
    result.key_name = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.placement = null;
    result.ramdisk_id = null;
    result.security_groups = null;
    result.spot_price = null;
    result.subnet_id = null;
    result.tag_specifications = null;
    result.user_data = null;
    result.weighted_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressingType")) {
                    result.addressing_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfileSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeSpotFleetMonitoring(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeInstanceNetworkInterfaceSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializeSpotPlacement(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.security_groups = try deserializeGroupIdentifierList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSpecificationSet")) {
                    result.tag_specifications = try deserializeSpotFleetTagSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "userData")) {
                    result.user_data = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "weightedCapacity")) {
                    result.weighted_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetMonitoring(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotFleetMonitoring {
    _ = allocator;
    var result: SpotFleetMonitoring = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetRequestConfig(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotFleetRequestConfig {
    var result: SpotFleetRequestConfig = undefined;
    result.activity_status = null;
    result.create_time = null;
    result.spot_fleet_request_config = null;
    result.spot_fleet_request_id = null;
    result.spot_fleet_request_state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "activityStatus")) {
                    result.activity_status = ActivityStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestConfig")) {
                    result.spot_fleet_request_config = try deserializeSpotFleetRequestConfigData(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestId")) {
                    result.spot_fleet_request_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestState")) {
                    result.spot_fleet_request_state = BatchState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetRequestConfigData(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotFleetRequestConfigData {
    var result: SpotFleetRequestConfigData = undefined;
    result.allocation_strategy = null;
    result.client_token = null;
    result.context = null;
    result.excess_capacity_termination_policy = null;
    result.fulfilled_capacity = null;
    result.instance_interruption_behavior = null;
    result.instance_pools_to_use_count = null;
    result.launch_specifications = null;
    result.launch_template_configs = null;
    result.load_balancers_config = null;
    result.on_demand_allocation_strategy = null;
    result.on_demand_fulfilled_capacity = null;
    result.on_demand_max_total_price = null;
    result.on_demand_target_capacity = null;
    result.replace_unhealthy_instances = null;
    result.spot_maintenance_strategies = null;
    result.spot_max_total_price = null;
    result.spot_price = null;
    result.tag_specifications = null;
    result.target_capacity_unit_type = null;
    result.terminate_instances_with_expiration = null;
    result.@"type" = null;
    result.valid_from = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = AllocationStrategy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "context")) {
                    result.context = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "excessCapacityTerminationPolicy")) {
                    result.excess_capacity_termination_policy = ExcessCapacityTerminationPolicy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fulfilledCapacity")) {
                    result.fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "iamFleetRole")) {
                    result.iam_fleet_role = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = InstanceInterruptionBehavior.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePoolsToUseCount")) {
                    result.instance_pools_to_use_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "launchSpecifications")) {
                    result.launch_specifications = try deserializeLaunchSpecsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "launchTemplateConfigs")) {
                    result.launch_template_configs = try deserializeLaunchTemplateConfigList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "loadBalancersConfig")) {
                    result.load_balancers_config = try deserializeLoadBalancersConfig(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "onDemandAllocationStrategy")) {
                    result.on_demand_allocation_strategy = OnDemandAllocationStrategy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "onDemandFulfilledCapacity")) {
                    result.on_demand_fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "onDemandMaxTotalPrice")) {
                    result.on_demand_max_total_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "onDemandTargetCapacity")) {
                    result.on_demand_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "replaceUnhealthyInstances")) {
                    result.replace_unhealthy_instances = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotMaintenanceStrategies")) {
                    result.spot_maintenance_strategies = try deserializeSpotMaintenanceStrategies(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "spotMaxTotalPrice")) {
                    result.spot_max_total_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "TagSpecification")) {
                    result.tag_specifications = try deserializeTagSpecificationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "targetCapacity")) {
                    result.target_capacity = try std.fmt.parseInt(i32, try reader.readElementText(), 10);
                } else if (std.mem.eql(u8, e.local, "targetCapacityUnitType")) {
                    result.target_capacity_unit_type = TargetCapacityUnitType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "terminateInstancesWithExpiration")) {
                    result.terminate_instances_with_expiration = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = FleetType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validFrom")) {
                    result.valid_from = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetTagSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotFleetTagSpecification {
    var result: SpotFleetTagSpecification = undefined;
    result.resource_type = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = ResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tag")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotInstanceRequest(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotInstanceRequest {
    var result: SpotInstanceRequest = undefined;
    result.actual_block_hourly_price = null;
    result.availability_zone_group = null;
    result.block_duration_minutes = null;
    result.create_time = null;
    result.fault = null;
    result.instance_id = null;
    result.instance_interruption_behavior = null;
    result.launched_availability_zone = null;
    result.launched_availability_zone_id = null;
    result.launch_group = null;
    result.launch_specification = null;
    result.product_description = null;
    result.spot_instance_request_id = null;
    result.spot_price = null;
    result.state = null;
    result.status = null;
    result.tags = null;
    result.@"type" = null;
    result.valid_from = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "actualBlockHourlyPrice")) {
                    result.actual_block_hourly_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneGroup")) {
                    result.availability_zone_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDurationMinutes")) {
                    result.block_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fault")) {
                    result.fault = try deserializeSpotInstanceStateFault(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = InstanceInterruptionBehavior.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchedAvailabilityZone")) {
                    result.launched_availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchedAvailabilityZoneId")) {
                    result.launched_availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchGroup")) {
                    result.launch_group = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchSpecification")) {
                    result.launch_specification = try deserializeLaunchSpecification(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = RIProductDescription.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = SpotInstanceState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeSpotInstanceStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = SpotInstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validFrom")) {
                    result.valid_from = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotInstanceStateFault(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotInstanceStateFault {
    var result: SpotInstanceStateFault = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotInstanceStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotInstanceStatus {
    var result: SpotInstanceStatus = undefined;
    result.code = null;
    result.message = null;
    result.update_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updateTime")) {
                    result.update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotMaintenanceStrategies(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotMaintenanceStrategies {
    var result: SpotMaintenanceStrategies = undefined;
    result.capacity_rebalance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityRebalance")) {
                    result.capacity_rebalance = try deserializeSpotCapacityRebalance(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotOptions {
    var result: SpotOptions = undefined;
    result.allocation_strategy = null;
    result.instance_interruption_behavior = null;
    result.instance_pools_to_use_count = null;
    result.maintenance_strategies = null;
    result.max_total_price = null;
    result.min_target_capacity = null;
    result.single_availability_zone = null;
    result.single_instance_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = SpotAllocationStrategy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = SpotInstanceInterruptionBehavior.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePoolsToUseCount")) {
                    result.instance_pools_to_use_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maintenanceStrategies")) {
                    result.maintenance_strategies = try deserializeFleetSpotMaintenanceStrategies(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "maxTotalPrice")) {
                    result.max_total_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "minTargetCapacity")) {
                    result.min_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "singleAvailabilityZone")) {
                    result.single_availability_zone = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "singleInstanceType")) {
                    result.single_instance_type = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotPlacement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotPlacement {
    var result: SpotPlacement = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.group_name = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = Tenancy.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotPlacementScore(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotPlacementScore {
    var result: SpotPlacementScore = undefined;
    result.availability_zone_id = null;
    result.region = null;
    result.score = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "score")) {
                    result.score = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotPrice(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SpotPrice {
    @setEvalBranchQuota(2000);
    var result: SpotPrice = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_type = null;
    result.product_description = null;
    result.spot_price = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = InstanceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = RIProductDescription.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStaleIpPermission(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !StaleIpPermission {
    var result: StaleIpPermission = undefined;
    result.from_port = null;
    result.ip_protocol = null;
    result.ip_ranges = null;
    result.prefix_list_ids = null;
    result.to_port = null;
    result.user_id_group_pairs = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipRanges")) {
                    result.ip_ranges = try deserializeIpRanges(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "prefixListIds")) {
                    result.prefix_list_ids = try deserializePrefixListIdSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groups")) {
                    result.user_id_group_pairs = try deserializeUserIdGroupPairSet(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStaleSecurityGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !StaleSecurityGroup {
    var result: StaleSecurityGroup = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.stale_ip_permissions = null;
    result.stale_ip_permissions_egress = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staleIpPermissions")) {
                    result.stale_ip_permissions = try deserializeStaleIpPermissionSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "staleIpPermissionsEgress")) {
                    result.stale_ip_permissions_egress = try deserializeStaleIpPermissionSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStateReason(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !StateReason {
    var result: StateReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStorage(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Storage {
    var result: Storage = undefined;
    result.s3 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "S3")) {
                    result.s3 = try deserializeS3Storage(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStoreImageTaskResult(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !StoreImageTaskResult {
    var result: StoreImageTaskResult = undefined;
    result.ami_id = null;
    result.bucket = null;
    result.progress_percentage = null;
    result.s3_object_key = null;
    result.store_task_failure_reason = null;
    result.store_task_state = null;
    result.task_start_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amiId")) {
                    result.ami_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bucket")) {
                    result.bucket = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progressPercentage")) {
                    result.progress_percentage = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "s3objectKey")) {
                    result.s3_object_key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storeTaskFailureReason")) {
                    result.store_task_failure_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storeTaskState")) {
                    result.store_task_state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "taskStartTime")) {
                    result.task_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnet(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Subnet {
    var result: Subnet = undefined;
    result.assign_ipv_6_address_on_creation = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.available_ip_address_count = null;
    result.block_public_access_states = null;
    result.cidr_block = null;
    result.customer_owned_ipv_4_pool = null;
    result.default_for_az = null;
    result.enable_dns_64 = null;
    result.enable_lni_at_device_index = null;
    result.ipv_6_cidr_block_association_set = null;
    result.ipv_6_native = null;
    result.map_customer_owned_ip_on_launch = null;
    result.map_public_ip_on_launch = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.private_dns_name_options_on_launch = null;
    result.state = null;
    result.subnet_arn = null;
    result.subnet_id = null;
    result.tags = null;
    result.@"type" = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "assignIpv6AddressOnCreation")) {
                    result.assign_ipv_6_address_on_creation = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableIpAddressCount")) {
                    result.available_ip_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "blockPublicAccessStates")) {
                    result.block_public_access_states = try deserializeBlockPublicAccessStates(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIpv4Pool")) {
                    result.customer_owned_ipv_4_pool = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultForAz")) {
                    result.default_for_az = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableDns64")) {
                    result.enable_dns_64 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableLniAtDeviceIndex")) {
                    result.enable_lni_at_device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockAssociationSet")) {
                    result.ipv_6_cidr_block_association_set = try deserializeSubnetIpv6CidrBlockAssociationSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Native")) {
                    result.ipv_6_native = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "mapCustomerOwnedIpOnLaunch")) {
                    result.map_customer_owned_ip_on_launch = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "mapPublicIpOnLaunch")) {
                    result.map_public_ip_on_launch = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameOptionsOnLaunch")) {
                    result.private_dns_name_options_on_launch = try deserializePrivateDnsNameOptionsOnLaunch(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = SubnetState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetArn")) {
                    result.subnet_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SubnetAssociation {
    var result: SubnetAssociation = undefined;
    result.state = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayMulitcastDomainAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetCidrBlockState(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SubnetCidrBlockState {
    var result: SubnetCidrBlockState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = SubnetCidrBlockStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetCidrReservation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SubnetCidrReservation {
    var result: SubnetCidrReservation = undefined;
    result.cidr = null;
    result.description = null;
    result.owner_id = null;
    result.reservation_type = null;
    result.subnet_cidr_reservation_id = null;
    result.subnet_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationType")) {
                    result.reservation_type = SubnetCidrReservationType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetCidrReservationId")) {
                    result.subnet_cidr_reservation_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetIpPrefixes(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SubnetIpPrefixes {
    var result: SubnetIpPrefixes = undefined;
    result.ip_prefixes = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipPrefixSet")) {
                    result.ip_prefixes = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetIpv6CidrBlockAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SubnetIpv6CidrBlockAssociation {
    var result: SubnetIpv6CidrBlockAssociation = undefined;
    result.association_id = null;
    result.ip_source = null;
    result.ipv_6_address_attribute = null;
    result.ipv_6_cidr_block = null;
    result.ipv_6_cidr_block_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipSource")) {
                    result.ip_source = IpSource.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6AddressAttribute")) {
                    result.ipv_6_address_attribute = Ipv6AddressAttribute.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockState")) {
                    result.ipv_6_cidr_block_state = try deserializeSubnetCidrBlockState(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubscription(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Subscription {
    var result: Subscription = undefined;
    result.destination = null;
    result.metric = null;
    result.period = null;
    result.source = null;
    result.statistic = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metric")) {
                    result.metric = MetricType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "period")) {
                    result.period = PeriodType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statistic")) {
                    result.statistic = StatisticType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSuccessfulInstanceCreditSpecificationItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SuccessfulInstanceCreditSpecificationItem {
    var result: SuccessfulInstanceCreditSpecificationItem = undefined;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSuccessfulQueuedPurchaseDeletion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SuccessfulQueuedPurchaseDeletion {
    var result: SuccessfulQueuedPurchaseDeletion = undefined;
    result.reserved_instances_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSupportedRegionDetail(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !SupportedRegionDetail {
    var result: SupportedRegionDetail = undefined;
    result.region = null;
    result.service_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceState")) {
                    result.service_state = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTag(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Tag {
    var result: Tag = undefined;
    result.key = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTagDescription(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TagDescription {
    var result: TagDescription = undefined;
    result.key = null;
    result.resource_id = null;
    result.resource_type = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = ResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTagSpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TagSpecification {
    var result: TagSpecification = undefined;
    result.resource_type = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = ResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "Tag")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetCapacitySpecification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TargetCapacitySpecification {
    _ = allocator;
    var result: TargetCapacitySpecification = undefined;
    result.default_target_capacity_type = null;
    result.on_demand_target_capacity = null;
    result.spot_target_capacity = null;
    result.target_capacity_unit_type = null;
    result.total_target_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "defaultTargetCapacityType")) {
                    result.default_target_capacity_type = DefaultTargetCapacityType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "onDemandTargetCapacity")) {
                    result.on_demand_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spotTargetCapacity")) {
                    result.spot_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetCapacityUnitType")) {
                    result.target_capacity_unit_type = TargetCapacityUnitType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalTargetCapacity")) {
                    result.total_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TargetConfiguration {
    var result: TargetConfiguration = undefined;
    result.instance_count = null;
    result.offering_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "offeringId")) {
                    result.offering_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TargetGroup {
    var result: TargetGroup = undefined;
    result.arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetGroupsConfig(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TargetGroupsConfig {
    var result: TargetGroupsConfig = undefined;
    result.target_groups = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "targetGroups")) {
                    result.target_groups = try deserializeTargetGroups(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetNetwork(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TargetNetwork {
    var result: TargetNetwork = undefined;
    result.association_id = null;
    result.client_vpn_endpoint_id = null;
    result.security_groups = null;
    result.status = null;
    result.target_network_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroups")) {
                    result.security_groups = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeAssociationStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "targetNetworkId")) {
                    result.target_network_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetReservationValue(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TargetReservationValue {
    var result: TargetReservationValue = undefined;
    result.reservation_value = null;
    result.target_configuration = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservationValue")) {
                    result.reservation_value = try deserializeReservationValue(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "targetConfiguration")) {
                    result.target_configuration = try deserializeTargetConfiguration(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTerminateConnectionStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TerminateConnectionStatus {
    var result: TerminateConnectionStatus = undefined;
    result.connection_id = null;
    result.current_status = null;
    result.previous_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "connectionId")) {
                    result.connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currentStatus")) {
                    result.current_status = try deserializeClientVpnConnectionStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "previousStatus")) {
                    result.previous_status = try deserializeClientVpnConnectionStatus(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeThroughResourcesStatement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ThroughResourcesStatement {
    var result: ThroughResourcesStatement = undefined;
    result.resource_statement = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceStatement")) {
                    result.resource_statement = try deserializeResourceStatement(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTotalLocalStorageGB(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TotalLocalStorageGB {
    _ = allocator;
    var result: TotalLocalStorageGB = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorFilter(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TrafficMirrorFilter {
    var result: TrafficMirrorFilter = undefined;
    result.description = null;
    result.egress_filter_rules = null;
    result.ingress_filter_rules = null;
    result.network_services = null;
    result.tags = null;
    result.traffic_mirror_filter_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressFilterRuleSet")) {
                    result.egress_filter_rules = try deserializeTrafficMirrorFilterRuleList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ingressFilterRuleSet")) {
                    result.ingress_filter_rules = try deserializeTrafficMirrorFilterRuleList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "networkServiceSet")) {
                    result.network_services = try deserializeTrafficMirrorNetworkServiceList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterId")) {
                    result.traffic_mirror_filter_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorFilterRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TrafficMirrorFilterRule {
    var result: TrafficMirrorFilterRule = undefined;
    result.description = null;
    result.destination_cidr_block = null;
    result.destination_port_range = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_number = null;
    result.source_cidr_block = null;
    result.source_port_range = null;
    result.tags = null;
    result.traffic_direction = null;
    result.traffic_mirror_filter_id = null;
    result.traffic_mirror_filter_rule_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try deserializeTrafficMirrorPortRange(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = TrafficMirrorRuleAction.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleNumber")) {
                    result.rule_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "sourceCidrBlock")) {
                    result.source_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try deserializeTrafficMirrorPortRange(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trafficDirection")) {
                    result.traffic_direction = TrafficDirection.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterId")) {
                    result.traffic_mirror_filter_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterRuleId")) {
                    result.traffic_mirror_filter_rule_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorPortRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TrafficMirrorPortRange {
    _ = allocator;
    var result: TrafficMirrorPortRange = undefined;
    result.from_port = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorSession(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TrafficMirrorSession {
    var result: TrafficMirrorSession = undefined;
    result.description = null;
    result.network_interface_id = null;
    result.owner_id = null;
    result.packet_length = null;
    result.session_number = null;
    result.tags = null;
    result.traffic_mirror_filter_id = null;
    result.traffic_mirror_session_id = null;
    result.traffic_mirror_target_id = null;
    result.virtual_network_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "packetLength")) {
                    result.packet_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "sessionNumber")) {
                    result.session_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterId")) {
                    result.traffic_mirror_filter_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorSessionId")) {
                    result.traffic_mirror_session_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorTargetId")) {
                    result.traffic_mirror_target_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualNetworkId")) {
                    result.virtual_network_id = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorTarget(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TrafficMirrorTarget {
    var result: TrafficMirrorTarget = undefined;
    result.description = null;
    result.gateway_load_balancer_endpoint_id = null;
    result.network_interface_id = null;
    result.network_load_balancer_arn = null;
    result.owner_id = null;
    result.tags = null;
    result.traffic_mirror_target_id = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayLoadBalancerEndpointId")) {
                    result.gateway_load_balancer_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkLoadBalancerArn")) {
                    result.network_load_balancer_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trafficMirrorTargetId")) {
                    result.traffic_mirror_target_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = TrafficMirrorTargetType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGateway {
    var result: TransitGateway = undefined;
    result.creation_time = null;
    result.description = null;
    result.options = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_arn = null;
    result.transit_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayArn")) {
                    result.transit_gateway_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayAssociation {
    var result: TransitGatewayAssociation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayAttachment {
    var result: TransitGatewayAttachment = undefined;
    result.association = null;
    result.creation_time = null;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.transit_gateway_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeTransitGatewayAttachmentAssociation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAttachmentState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayOwnerId")) {
                    result.transit_gateway_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachmentAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayAttachmentAssociation {
    var result: TransitGatewayAttachmentAssociation = undefined;
    result.state = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachmentBgpConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayAttachmentBgpConfiguration {
    var result: TransitGatewayAttachmentBgpConfiguration = undefined;
    result.bgp_status = null;
    result.peer_address = null;
    result.peer_asn = null;
    result.transit_gateway_address = null;
    result.transit_gateway_asn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpStatus")) {
                    result.bgp_status = BgpStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAsn")) {
                    result.peer_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "transitGatewayAddress")) {
                    result.transit_gateway_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAsn")) {
                    result.transit_gateway_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachmentPropagation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayAttachmentPropagation {
    var result: TransitGatewayAttachmentPropagation = undefined;
    result.state = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayPropagationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnect(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayConnect {
    var result: TransitGatewayConnect = undefined;
    result.creation_time = null;
    result.options = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.transport_transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayConnectOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAttachmentState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transportTransitGatewayAttachmentId")) {
                    result.transport_transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnectOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayConnectOptions {
    _ = allocator;
    var result: TransitGatewayConnectOptions = undefined;
    result.protocol = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = ProtocolValue.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnectPeer(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayConnectPeer {
    var result: TransitGatewayConnectPeer = undefined;
    result.connect_peer_configuration = null;
    result.creation_time = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_connect_peer_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "connectPeerConfiguration")) {
                    result.connect_peer_configuration = try deserializeTransitGatewayConnectPeerConfiguration(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayConnectPeerState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayConnectPeerId")) {
                    result.transit_gateway_connect_peer_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnectPeerConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayConnectPeerConfiguration {
    var result: TransitGatewayConnectPeerConfiguration = undefined;
    result.bgp_configurations = null;
    result.inside_cidr_blocks = null;
    result.peer_address = null;
    result.protocol = null;
    result.transit_gateway_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpConfigurations")) {
                    result.bgp_configurations = try deserializeTransitGatewayAttachmentBgpConfigurationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "insideCidrBlocks")) {
                    result.inside_cidr_blocks = try deserializeInsideCidrBlocksStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = ProtocolValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAddress")) {
                    result.transit_gateway_address = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMeteringPolicy(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMeteringPolicy {
    var result: TransitGatewayMeteringPolicy = undefined;
    result.middlebox_attachment_ids = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_metering_policy_id = null;
    result.update_effective_at = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "middleboxAttachmentIdSet")) {
                    result.middlebox_attachment_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayMeteringPolicyState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMeteringPolicyId")) {
                    result.transit_gateway_metering_policy_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updateEffectiveAt")) {
                    result.update_effective_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMeteringPolicyEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMeteringPolicyEntry {
    var result: TransitGatewayMeteringPolicyEntry = undefined;
    result.metered_account = null;
    result.metering_policy_rule = null;
    result.policy_rule_number = null;
    result.state = null;
    result.updated_at = null;
    result.update_effective_at = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "meteredAccount")) {
                    result.metered_account = TransitGatewayMeteringPayerType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "meteringPolicyRule")) {
                    result.metering_policy_rule = try deserializeTransitGatewayMeteringPolicyRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "policyRuleNumber")) {
                    result.policy_rule_number = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayMeteringPolicyEntryState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updatedAt")) {
                    result.updated_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "updateEffectiveAt")) {
                    result.update_effective_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMeteringPolicyRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMeteringPolicyRule {
    var result: TransitGatewayMeteringPolicyRule = undefined;
    result.destination_cidr_block = null;
    result.destination_port_range = null;
    result.destination_transit_gateway_attachment_id = null;
    result.destination_transit_gateway_attachment_type = null;
    result.protocol = null;
    result.source_cidr_block = null;
    result.source_port_range = null;
    result.source_transit_gateway_attachment_id = null;
    result.source_transit_gateway_attachment_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationTransitGatewayAttachmentId")) {
                    result.destination_transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationTransitGatewayAttachmentType")) {
                    result.destination_transit_gateway_attachment_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceCidrBlock")) {
                    result.source_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceTransitGatewayAttachmentId")) {
                    result.source_transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceTransitGatewayAttachmentType")) {
                    result.source_transit_gateway_attachment_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDeregisteredGroupMembers(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastDeregisteredGroupMembers {
    var result: TransitGatewayMulticastDeregisteredGroupMembers = undefined;
    result.deregistered_network_interface_ids = null;
    result.group_ip_address = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deregisteredNetworkInterfaceIds")) {
                    result.deregistered_network_interface_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDeregisteredGroupSources(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastDeregisteredGroupSources {
    var result: TransitGatewayMulticastDeregisteredGroupSources = undefined;
    result.deregistered_network_interface_ids = null;
    result.group_ip_address = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deregisteredNetworkInterfaceIds")) {
                    result.deregistered_network_interface_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomain(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastDomain {
    var result: TransitGatewayMulticastDomain = undefined;
    result.creation_time = null;
    result.options = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_multicast_domain_arn = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayMulticastDomainOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayMulticastDomainState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainArn")) {
                    result.transit_gateway_multicast_domain_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomainAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastDomainAssociation {
    var result: TransitGatewayMulticastDomainAssociation = undefined;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.subnet = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnet")) {
                    result.subnet = try deserializeSubnetAssociation(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomainAssociations(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastDomainAssociations {
    var result: TransitGatewayMulticastDomainAssociations = undefined;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.subnets = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnets")) {
                    result.subnets = try deserializeSubnetAssociationList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomainOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastDomainOptions {
    _ = allocator;
    var result: TransitGatewayMulticastDomainOptions = undefined;
    result.auto_accept_shared_associations = null;
    result.igmpv_2_support = null;
    result.static_sources_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoAcceptSharedAssociations")) {
                    result.auto_accept_shared_associations = AutoAcceptSharedAssociationsValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "igmpv2Support")) {
                    result.igmpv_2_support = Igmpv2SupportValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staticSourcesSupport")) {
                    result.static_sources_support = StaticSourcesSupportValue.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastGroup {
    var result: TransitGatewayMulticastGroup = undefined;
    result.group_ip_address = null;
    result.group_member = null;
    result.group_source = null;
    result.member_type = null;
    result.network_interface_id = null;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.source_type = null;
    result.subnet_id = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupMember")) {
                    result.group_member = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "groupSource")) {
                    result.group_source = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "memberType")) {
                    result.member_type = MembershipType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceType")) {
                    result.source_type = MembershipType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastRegisteredGroupMembers(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastRegisteredGroupMembers {
    var result: TransitGatewayMulticastRegisteredGroupMembers = undefined;
    result.group_ip_address = null;
    result.registered_network_interface_ids = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "registeredNetworkInterfaceIds")) {
                    result.registered_network_interface_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastRegisteredGroupSources(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayMulticastRegisteredGroupSources {
    var result: TransitGatewayMulticastRegisteredGroupSources = undefined;
    result.group_ip_address = null;
    result.registered_network_interface_ids = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "registeredNetworkInterfaceIds")) {
                    result.registered_network_interface_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayOptions {
    var result: TransitGatewayOptions = undefined;
    result.amazon_side_asn = null;
    result.association_default_route_table_id = null;
    result.auto_accept_shared_attachments = null;
    result.default_route_table_association = null;
    result.default_route_table_propagation = null;
    result.dns_support = null;
    result.encryption_support = null;
    result.multicast_support = null;
    result.propagation_default_route_table_id = null;
    result.security_group_referencing_support = null;
    result.transit_gateway_cidr_blocks = null;
    result.vpn_ecmp_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amazonSideAsn")) {
                    result.amazon_side_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "associationDefaultRouteTableId")) {
                    result.association_default_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "autoAcceptSharedAttachments")) {
                    result.auto_accept_shared_attachments = AutoAcceptSharedAttachmentsValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultRouteTableAssociation")) {
                    result.default_route_table_association = DefaultRouteTableAssociationValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultRouteTablePropagation")) {
                    result.default_route_table_propagation = DefaultRouteTablePropagationValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsSupport")) {
                    result.dns_support = DnsSupportValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionSupport")) {
                    result.encryption_support = try deserializeEncryptionSupport(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "multicastSupport")) {
                    result.multicast_support = MulticastSupportValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "propagationDefaultRouteTableId")) {
                    result.propagation_default_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupReferencingSupport")) {
                    result.security_group_referencing_support = SecurityGroupReferencingSupportValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayCidrBlocks")) {
                    result.transit_gateway_cidr_blocks = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpnEcmpSupport")) {
                    result.vpn_ecmp_support = VpnEcmpSupportValue.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPeeringAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPeeringAttachment {
    var result: TransitGatewayPeeringAttachment = undefined;
    result.accepter_tgw_info = null;
    result.accepter_transit_gateway_attachment_id = null;
    result.creation_time = null;
    result.options = null;
    result.requester_tgw_info = null;
    result.state = null;
    result.status = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accepterTgwInfo")) {
                    result.accepter_tgw_info = try deserializePeeringTgwInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "accepterTransitGatewayAttachmentId")) {
                    result.accepter_transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayPeeringAttachmentOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "requesterTgwInfo")) {
                    result.requester_tgw_info = try deserializePeeringTgwInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAttachmentState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializePeeringAttachmentStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPeeringAttachmentOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPeeringAttachmentOptions {
    _ = allocator;
    var result: TransitGatewayPeeringAttachmentOptions = undefined;
    result.dynamic_routing = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dynamicRouting")) {
                    result.dynamic_routing = DynamicRoutingValue.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyRule(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPolicyRule {
    var result: TransitGatewayPolicyRule = undefined;
    result.destination_cidr_block = null;
    result.destination_port_range = null;
    result.meta_data = null;
    result.protocol = null;
    result.source_cidr_block = null;
    result.source_port_range = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metaData")) {
                    result.meta_data = try deserializeTransitGatewayPolicyRuleMetaData(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceCidrBlock")) {
                    result.source_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyRuleMetaData(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPolicyRuleMetaData {
    var result: TransitGatewayPolicyRuleMetaData = undefined;
    result.meta_data_key = null;
    result.meta_data_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "metaDataKey")) {
                    result.meta_data_key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metaDataValue")) {
                    result.meta_data_value = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyTable(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPolicyTable {
    var result: TransitGatewayPolicyTable = undefined;
    result.creation_time = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_policy_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayPolicyTableState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayPolicyTableId")) {
                    result.transit_gateway_policy_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyTableAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPolicyTableAssociation {
    var result: TransitGatewayPolicyTableAssociation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_policy_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayPolicyTableId")) {
                    result.transit_gateway_policy_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyTableEntry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPolicyTableEntry {
    var result: TransitGatewayPolicyTableEntry = undefined;
    result.policy_rule = null;
    result.policy_rule_number = null;
    result.target_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "policyRule")) {
                    result.policy_rule = try deserializeTransitGatewayPolicyRule(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "policyRuleNumber")) {
                    result.policy_rule_number = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetRouteTableId")) {
                    result.target_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPrefixListAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPrefixListAttachment {
    var result: TransitGatewayPrefixListAttachment = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPrefixListReference(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPrefixListReference {
    var result: TransitGatewayPrefixListReference = undefined;
    result.blackhole = null;
    result.prefix_list_id = null;
    result.prefix_list_owner_id = null;
    result.state = null;
    result.transit_gateway_attachment = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blackhole")) {
                    result.blackhole = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListOwnerId")) {
                    result.prefix_list_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayPrefixListReferenceState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachment")) {
                    result.transit_gateway_attachment = try deserializeTransitGatewayPrefixListAttachment(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPropagation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayPropagation {
    var result: TransitGatewayPropagation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_route_table_announcement_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayPropagationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayRoute {
    var result: TransitGatewayRoute = undefined;
    result.destination_cidr_block = null;
    result.prefix_list_id = null;
    result.state = null;
    result.transit_gateway_attachments = null;
    result.transit_gateway_route_table_announcement_id = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayRouteState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachments")) {
                    result.transit_gateway_attachments = try deserializeTransitGatewayRouteAttachmentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = TransitGatewayRouteType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayRouteAttachment {
    var result: TransitGatewayRouteAttachment = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTable(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayRouteTable {
    var result: TransitGatewayRouteTable = undefined;
    result.creation_time = null;
    result.default_association_route_table = null;
    result.default_propagation_route_table = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultAssociationRouteTable")) {
                    result.default_association_route_table = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "defaultPropagationRouteTable")) {
                    result.default_propagation_route_table = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayRouteTableState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTableAnnouncement(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayRouteTableAnnouncement {
    var result: TransitGatewayRouteTableAnnouncement = undefined;
    result.announcement_direction = null;
    result.core_network_id = null;
    result.creation_time = null;
    result.peer_core_network_id = null;
    result.peering_attachment_id = null;
    result.peer_transit_gateway_id = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_route_table_announcement_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "announcementDirection")) {
                    result.announcement_direction = TransitGatewayRouteTableAnnouncementDirection.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkId")) {
                    result.core_network_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "peerCoreNetworkId")) {
                    result.peer_core_network_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringAttachmentId")) {
                    result.peering_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerTransitGatewayId")) {
                    result.peer_transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayRouteTableAnnouncementState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTableAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayRouteTableAssociation {
    var result: TransitGatewayRouteTableAssociation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTablePropagation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayRouteTablePropagation {
    var result: TransitGatewayRouteTablePropagation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_route_table_announcement_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = TransitGatewayAttachmentResourceType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayPropagationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTableRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayRouteTableRoute {
    var result: TransitGatewayRouteTableRoute = undefined;
    result.attachment_id = null;
    result.destination_cidr = null;
    result.prefix_list_id = null;
    result.resource_id = null;
    result.resource_type = null;
    result.route_origin = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentId")) {
                    result.attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeOrigin")) {
                    result.route_origin = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayVpcAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayVpcAttachment {
    var result: TransitGatewayVpcAttachment = undefined;
    result.creation_time = null;
    result.options = null;
    result.state = null;
    result.subnet_ids = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.vpc_id = null;
    result.vpc_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayVpcAttachmentOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = TransitGatewayAttachmentState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIds")) {
                    result.subnet_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcOwnerId")) {
                    result.vpc_owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayVpcAttachmentOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TransitGatewayVpcAttachmentOptions {
    _ = allocator;
    var result: TransitGatewayVpcAttachmentOptions = undefined;
    result.appliance_mode_support = null;
    result.dns_support = null;
    result.ipv_6_support = null;
    result.security_group_referencing_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "applianceModeSupport")) {
                    result.appliance_mode_support = ApplianceModeSupportValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsSupport")) {
                    result.dns_support = DnsSupportValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6Support")) {
                    result.ipv_6_support = Ipv6SupportValue.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupReferencingSupport")) {
                    result.security_group_referencing_support = SecurityGroupReferencingSupportValue.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrunkInterfaceAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TrunkInterfaceAssociation {
    var result: TrunkInterfaceAssociation = undefined;
    result.association_id = null;
    result.branch_interface_id = null;
    result.gre_key = null;
    result.interface_protocol = null;
    result.tags = null;
    result.trunk_interface_id = null;
    result.vlan_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "branchInterfaceId")) {
                    result.branch_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "greKey")) {
                    result.gre_key = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "interfaceProtocol")) {
                    result.interface_protocol = InterfaceProtocolType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trunkInterfaceId")) {
                    result.trunk_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vlanId")) {
                    result.vlan_id = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTunnelOption(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !TunnelOption {
    var result: TunnelOption = undefined;
    result.dpd_timeout_action = null;
    result.dpd_timeout_seconds = null;
    result.enable_tunnel_lifecycle_control = null;
    result.ike_versions = null;
    result.log_options = null;
    result.outside_ip_address = null;
    result.phase_1_dh_group_numbers = null;
    result.phase_1_encryption_algorithms = null;
    result.phase_1_integrity_algorithms = null;
    result.phase_1_lifetime_seconds = null;
    result.phase_2_dh_group_numbers = null;
    result.phase_2_encryption_algorithms = null;
    result.phase_2_integrity_algorithms = null;
    result.phase_2_lifetime_seconds = null;
    result.pre_shared_key = null;
    result.rekey_fuzz_percentage = null;
    result.rekey_margin_time_seconds = null;
    result.replay_window_size = null;
    result.startup_action = null;
    result.tunnel_inside_cidr = null;
    result.tunnel_inside_ipv_6_cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dpdTimeoutAction")) {
                    result.dpd_timeout_action = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dpdTimeoutSeconds")) {
                    result.dpd_timeout_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enableTunnelLifecycleControl")) {
                    result.enable_tunnel_lifecycle_control = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ikeVersionSet")) {
                    result.ike_versions = try deserializeIKEVersionsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "logOptions")) {
                    result.log_options = try deserializeVpnTunnelLogOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outsideIpAddress")) {
                    result.outside_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase1DHGroupNumberSet")) {
                    result.phase_1_dh_group_numbers = try deserializePhase1DHGroupNumbersList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "phase1EncryptionAlgorithmSet")) {
                    result.phase_1_encryption_algorithms = try deserializePhase1EncryptionAlgorithmsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "phase1IntegrityAlgorithmSet")) {
                    result.phase_1_integrity_algorithms = try deserializePhase1IntegrityAlgorithmsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "phase1LifetimeSeconds")) {
                    result.phase_1_lifetime_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "phase2DHGroupNumberSet")) {
                    result.phase_2_dh_group_numbers = try deserializePhase2DHGroupNumbersList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "phase2EncryptionAlgorithmSet")) {
                    result.phase_2_encryption_algorithms = try deserializePhase2EncryptionAlgorithmsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "phase2IntegrityAlgorithmSet")) {
                    result.phase_2_integrity_algorithms = try deserializePhase2IntegrityAlgorithmsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "phase2LifetimeSeconds")) {
                    result.phase_2_lifetime_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "preSharedKey")) {
                    result.pre_shared_key = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rekeyFuzzPercentage")) {
                    result.rekey_fuzz_percentage = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "rekeyMarginTimeSeconds")) {
                    result.rekey_margin_time_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "replayWindowSize")) {
                    result.replay_window_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "startupAction")) {
                    result.startup_action = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelInsideCidr")) {
                    result.tunnel_inside_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelInsideIpv6Cidr")) {
                    result.tunnel_inside_ipv_6_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulInstanceCreditSpecificationItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !UnsuccessfulInstanceCreditSpecificationItem {
    var result: UnsuccessfulInstanceCreditSpecificationItem = undefined;
    result.@"error" = null;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeUnsuccessfulInstanceCreditSpecificationItemError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulInstanceCreditSpecificationItemError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !UnsuccessfulInstanceCreditSpecificationItemError {
    var result: UnsuccessfulInstanceCreditSpecificationItemError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = UnsuccessfulInstanceCreditSpecificationErrorCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !UnsuccessfulItem {
    var result: UnsuccessfulItem = undefined;
    result.@"error" = null;
    result.resource_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeUnsuccessfulItemError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulItemError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !UnsuccessfulItemError {
    var result: UnsuccessfulItemError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUserBucketDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !UserBucketDetails {
    var result: UserBucketDetails = undefined;
    result.s3_bucket = null;
    result.s3_key = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Key")) {
                    result.s3_key = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUserIdGroupPair(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !UserIdGroupPair {
    var result: UserIdGroupPair = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.peering_status = null;
    result.user_id = null;
    result.vpc_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringStatus")) {
                    result.peering_status = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVCpuCountRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VCpuCountRange {
    _ = allocator;
    var result: VCpuCountRange = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVCpuInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VCpuInfo {
    var result: VCpuInfo = undefined;
    result.default_cores = null;
    result.default_threads_per_core = null;
    result.default_v_cpus = null;
    result.valid_cores = null;
    result.valid_threads_per_core = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "defaultCores")) {
                    result.default_cores = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultThreadsPerCore")) {
                    result.default_threads_per_core = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultVCpus")) {
                    result.default_v_cpus = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "validCores")) {
                    result.valid_cores = try deserializeCoreCountList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "validThreadsPerCore")) {
                    result.valid_threads_per_core = try deserializeThreadsPerCoreList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeValidationError(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ValidationError {
    var result: ValidationError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeValidationWarning(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !ValidationWarning {
    var result: ValidationWarning = undefined;
    result.errors = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "errorSet")) {
                    result.errors = try deserializeErrorSet(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpoint(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpoint {
    var result: VerifiedAccessEndpoint = undefined;
    result.application_domain = null;
    result.attachment_type = null;
    result.cidr_options = null;
    result.creation_time = null;
    result.deletion_time = null;
    result.description = null;
    result.device_validation_domain = null;
    result.domain_certificate_arn = null;
    result.endpoint_domain = null;
    result.endpoint_type = null;
    result.last_updated_time = null;
    result.load_balancer_options = null;
    result.network_interface_options = null;
    result.rds_options = null;
    result.security_group_ids = null;
    result.sse_specification = null;
    result.status = null;
    result.tags = null;
    result.verified_access_endpoint_id = null;
    result.verified_access_group_id = null;
    result.verified_access_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "applicationDomain")) {
                    result.application_domain = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachmentType")) {
                    result.attachment_type = VerifiedAccessEndpointAttachmentType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrOptions")) {
                    result.cidr_options = try deserializeVerifiedAccessEndpointCidrOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deletionTime")) {
                    result.deletion_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceValidationDomain")) {
                    result.device_validation_domain = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "domainCertificateArn")) {
                    result.domain_certificate_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endpointDomain")) {
                    result.endpoint_domain = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endpointType")) {
                    result.endpoint_type = VerifiedAccessEndpointType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "loadBalancerOptions")) {
                    result.load_balancer_options = try deserializeVerifiedAccessEndpointLoadBalancerOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceOptions")) {
                    result.network_interface_options = try deserializeVerifiedAccessEndpointEniOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "rdsOptions")) {
                    result.rds_options = try deserializeVerifiedAccessEndpointRdsOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeSecurityGroupIdList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "sseSpecification")) {
                    result.sse_specification = try deserializeVerifiedAccessSseSpecificationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeVerifiedAccessEndpointStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "verifiedAccessEndpointId")) {
                    result.verified_access_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessGroupId")) {
                    result.verified_access_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointCidrOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpointCidrOptions {
    var result: VerifiedAccessEndpointCidrOptions = undefined;
    result.cidr = null;
    result.port_ranges = null;
    result.protocol = null;
    result.subnet_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializeVerifiedAccessEndpointPortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = VerifiedAccessEndpointProtocol.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeVerifiedAccessEndpointSubnetIdList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointEniOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpointEniOptions {
    var result: VerifiedAccessEndpointEniOptions = undefined;
    result.network_interface_id = null;
    result.port = null;
    result.port_ranges = null;
    result.protocol = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializeVerifiedAccessEndpointPortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = VerifiedAccessEndpointProtocol.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointLoadBalancerOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpointLoadBalancerOptions {
    var result: VerifiedAccessEndpointLoadBalancerOptions = undefined;
    result.load_balancer_arn = null;
    result.port = null;
    result.port_ranges = null;
    result.protocol = null;
    result.subnet_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "loadBalancerArn")) {
                    result.load_balancer_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializeVerifiedAccessEndpointPortRangeList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = VerifiedAccessEndpointProtocol.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeVerifiedAccessEndpointSubnetIdList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointPortRange(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpointPortRange {
    _ = allocator;
    var result: VerifiedAccessEndpointPortRange = undefined;
    result.from_port = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointRdsOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpointRdsOptions {
    var result: VerifiedAccessEndpointRdsOptions = undefined;
    result.port = null;
    result.protocol = null;
    result.rds_db_cluster_arn = null;
    result.rds_db_instance_arn = null;
    result.rds_db_proxy_arn = null;
    result.rds_endpoint = null;
    result.subnet_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = VerifiedAccessEndpointProtocol.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsDbClusterArn")) {
                    result.rds_db_cluster_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsDbInstanceArn")) {
                    result.rds_db_instance_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsDbProxyArn")) {
                    result.rds_db_proxy_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsEndpoint")) {
                    result.rds_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeVerifiedAccessEndpointSubnetIdList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpointStatus {
    var result: VerifiedAccessEndpointStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = VerifiedAccessEndpointStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointTarget(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessEndpointTarget {
    var result: VerifiedAccessEndpointTarget = undefined;
    result.verified_access_endpoint_id = null;
    result.verified_access_endpoint_target_dns = null;
    result.verified_access_endpoint_target_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "verifiedAccessEndpointId")) {
                    result.verified_access_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessEndpointTargetDns")) {
                    result.verified_access_endpoint_target_dns = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessEndpointTargetIpAddress")) {
                    result.verified_access_endpoint_target_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessGroup(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessGroup {
    var result: VerifiedAccessGroup = undefined;
    result.creation_time = null;
    result.deletion_time = null;
    result.description = null;
    result.last_updated_time = null;
    result.owner = null;
    result.sse_specification = null;
    result.tags = null;
    result.verified_access_group_arn = null;
    result.verified_access_group_id = null;
    result.verified_access_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deletionTime")) {
                    result.deletion_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "owner")) {
                    result.owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseSpecification")) {
                    result.sse_specification = try deserializeVerifiedAccessSseSpecificationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "verifiedAccessGroupArn")) {
                    result.verified_access_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessGroupId")) {
                    result.verified_access_group_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstance(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessInstance {
    var result: VerifiedAccessInstance = undefined;
    result.cidr_endpoints_custom_sub_domain = null;
    result.creation_time = null;
    result.description = null;
    result.fips_enabled = null;
    result.last_updated_time = null;
    result.tags = null;
    result.verified_access_instance_id = null;
    result.verified_access_trust_providers = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrEndpointsCustomSubDomain")) {
                    result.cidr_endpoints_custom_sub_domain = try deserializeVerifiedAccessInstanceCustomSubDomain(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fipsEnabled")) {
                    result.fips_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessTrustProviderSet")) {
                    result.verified_access_trust_providers = try deserializeVerifiedAccessTrustProviderCondensedList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceCustomSubDomain(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessInstanceCustomSubDomain {
    var result: VerifiedAccessInstanceCustomSubDomain = undefined;
    result.nameservers = null;
    result.sub_domain = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "nameserverSet")) {
                    result.nameservers = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "subDomain")) {
                    result.sub_domain = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceLoggingConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessInstanceLoggingConfiguration {
    var result: VerifiedAccessInstanceLoggingConfiguration = undefined;
    result.access_logs = null;
    result.verified_access_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accessLogs")) {
                    result.access_logs = try deserializeVerifiedAccessLogs(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessInstanceOpenVpnClientConfiguration {
    var result: VerifiedAccessInstanceOpenVpnClientConfiguration = undefined;
    result.config = null;
    result.routes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "config")) {
                    result.config = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeSet")) {
                    result.routes = try deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRouteList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessInstanceOpenVpnClientConfigurationRoute {
    var result: VerifiedAccessInstanceOpenVpnClientConfigurationRoute = undefined;
    result.cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceUserTrustProviderClientConfiguration(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessInstanceUserTrustProviderClientConfiguration {
    var result: VerifiedAccessInstanceUserTrustProviderClientConfiguration = undefined;
    result.authorization_endpoint = null;
    result.client_id = null;
    result.client_secret = null;
    result.issuer = null;
    result.pkce_enabled = null;
    result.public_signing_key_endpoint = null;
    result.scopes = null;
    result.token_endpoint = null;
    result.@"type" = null;
    result.user_info_endpoint = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "authorizationEndpoint")) {
                    result.authorization_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientId")) {
                    result.client_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientSecret")) {
                    result.client_secret = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "issuer")) {
                    result.issuer = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "pkceEnabled")) {
                    result.pkce_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicSigningKeyEndpoint")) {
                    result.public_signing_key_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scopes")) {
                    result.scopes = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenEndpoint")) {
                    result.token_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = UserTrustProviderType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userInfoEndpoint")) {
                    result.user_info_endpoint = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogCloudWatchLogsDestination(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessLogCloudWatchLogsDestination {
    var result: VerifiedAccessLogCloudWatchLogsDestination = undefined;
    result.delivery_status = null;
    result.enabled = null;
    result.log_group = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deliveryStatus")) {
                    result.delivery_status = try deserializeVerifiedAccessLogDeliveryStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "logGroup")) {
                    result.log_group = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogDeliveryStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessLogDeliveryStatus {
    var result: VerifiedAccessLogDeliveryStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = VerifiedAccessLogDeliveryStatusCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogKinesisDataFirehoseDestination(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessLogKinesisDataFirehoseDestination {
    var result: VerifiedAccessLogKinesisDataFirehoseDestination = undefined;
    result.delivery_status = null;
    result.delivery_stream = null;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deliveryStatus")) {
                    result.delivery_status = try deserializeVerifiedAccessLogDeliveryStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "deliveryStream")) {
                    result.delivery_stream = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogS3Destination(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessLogS3Destination {
    var result: VerifiedAccessLogS3Destination = undefined;
    result.bucket_name = null;
    result.bucket_owner = null;
    result.delivery_status = null;
    result.enabled = null;
    result.prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bucketName")) {
                    result.bucket_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bucketOwner")) {
                    result.bucket_owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliveryStatus")) {
                    result.delivery_status = try deserializeVerifiedAccessLogDeliveryStatus(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogs(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessLogs {
    var result: VerifiedAccessLogs = undefined;
    result.cloud_watch_logs = null;
    result.include_trust_context = null;
    result.kinesis_data_firehose = null;
    result.log_version = null;
    result.s3 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cloudWatchLogs")) {
                    result.cloud_watch_logs = try deserializeVerifiedAccessLogCloudWatchLogsDestination(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "includeTrustContext")) {
                    result.include_trust_context = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "kinesisDataFirehose")) {
                    result.kinesis_data_firehose = try deserializeVerifiedAccessLogKinesisDataFirehoseDestination(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "logVersion")) {
                    result.log_version = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3")) {
                    result.s3 = try deserializeVerifiedAccessLogS3Destination(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessSseSpecificationResponse(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessSseSpecificationResponse {
    var result: VerifiedAccessSseSpecificationResponse = undefined;
    result.customer_managed_key_enabled = null;
    result.kms_key_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "customerManagedKeyEnabled")) {
                    result.customer_managed_key_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "kmsKeyArn")) {
                    result.kms_key_arn = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessTrustProvider(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessTrustProvider {
    var result: VerifiedAccessTrustProvider = undefined;
    result.creation_time = null;
    result.description = null;
    result.device_options = null;
    result.device_trust_provider_type = null;
    result.last_updated_time = null;
    result.native_application_oidc_options = null;
    result.oidc_options = null;
    result.policy_reference_name = null;
    result.sse_specification = null;
    result.tags = null;
    result.trust_provider_type = null;
    result.user_trust_provider_type = null;
    result.verified_access_trust_provider_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceOptions")) {
                    result.device_options = try deserializeDeviceOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "deviceTrustProviderType")) {
                    result.device_trust_provider_type = DeviceTrustProviderType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nativeApplicationOidcOptions")) {
                    result.native_application_oidc_options = try deserializeNativeApplicationOidcOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "oidcOptions")) {
                    result.oidc_options = try deserializeOidcOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "policyReferenceName")) {
                    result.policy_reference_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseSpecification")) {
                    result.sse_specification = try deserializeVerifiedAccessSseSpecificationResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "trustProviderType")) {
                    result.trust_provider_type = TrustProviderType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userTrustProviderType")) {
                    result.user_trust_provider_type = UserTrustProviderType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessTrustProviderId")) {
                    result.verified_access_trust_provider_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessTrustProviderCondensed(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VerifiedAccessTrustProviderCondensed {
    var result: VerifiedAccessTrustProviderCondensed = undefined;
    result.description = null;
    result.device_trust_provider_type = null;
    result.trust_provider_type = null;
    result.user_trust_provider_type = null;
    result.verified_access_trust_provider_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceTrustProviderType")) {
                    result.device_trust_provider_type = DeviceTrustProviderType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trustProviderType")) {
                    result.trust_provider_type = TrustProviderType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userTrustProviderType")) {
                    result.user_trust_provider_type = UserTrustProviderType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessTrustProviderId")) {
                    result.verified_access_trust_provider_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVgwTelemetry(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VgwTelemetry {
    var result: VgwTelemetry = undefined;
    result.accepted_route_count = null;
    result.certificate_arn = null;
    result.last_status_change = null;
    result.outside_ip_address = null;
    result.status = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceptedRouteCount")) {
                    result.accepted_route_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "certificateArn")) {
                    result.certificate_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastStatusChange")) {
                    result.last_status_change = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "outsideIpAddress")) {
                    result.outside_ip_address = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = TelemetryStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolume(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Volume {
    var result: Volume = undefined;
    result.attachments = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.create_time = null;
    result.encrypted = null;
    result.fast_restored = null;
    result.iops = null;
    result.kms_key_id = null;
    result.multi_attach_enabled = null;
    result.operator = null;
    result.outpost_arn = null;
    result.size = null;
    result.snapshot_id = null;
    result.source_volume_id = null;
    result.sse_type = null;
    result.state = null;
    result.tags = null;
    result.throughput = null;
    result.volume_id = null;
    result.volume_initialization_rate = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentSet")) {
                    result.attachments = try deserializeVolumeAttachmentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "fastRestored")) {
                    result.fast_restored = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "multiAttachEnabled")) {
                    result.multi_attach_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceVolumeId")) {
                    result.source_volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseType")) {
                    result.sse_type = SSEType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.state = VolumeState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeInitializationRate")) {
                    result.volume_initialization_rate = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = VolumeType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeAttachment {
    var result: VolumeAttachment = undefined;
    result.associated_resource = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.device = null;
    result.ebs_card_index = null;
    result.instance_id = null;
    result.instance_owning_service = null;
    result.state = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResource")) {
                    result.associated_resource = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "device")) {
                    result.device = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceOwningService")) {
                    result.instance_owning_service = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.state = VolumeAttachmentState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeModification(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeModification {
    var result: VolumeModification = undefined;
    result.end_time = null;
    result.modification_state = null;
    result.original_iops = null;
    result.original_multi_attach_enabled = null;
    result.original_size = null;
    result.original_throughput = null;
    result.original_volume_type = null;
    result.progress = null;
    result.start_time = null;
    result.status_message = null;
    result.target_iops = null;
    result.target_multi_attach_enabled = null;
    result.target_size = null;
    result.target_throughput = null;
    result.target_volume_type = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endTime")) {
                    result.end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "modificationState")) {
                    result.modification_state = VolumeModificationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "originalIops")) {
                    result.original_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "originalMultiAttachEnabled")) {
                    result.original_multi_attach_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "originalSize")) {
                    result.original_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "originalThroughput")) {
                    result.original_throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "originalVolumeType")) {
                    result.original_volume_type = VolumeType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetIops")) {
                    result.target_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetMultiAttachEnabled")) {
                    result.target_multi_attach_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "targetSize")) {
                    result.target_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetThroughput")) {
                    result.target_throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetVolumeType")) {
                    result.target_volume_type = VolumeType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeRecycleBinInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeRecycleBinInfo {
    var result: VolumeRecycleBinInfo = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.create_time = null;
    result.iops = null;
    result.operator = null;
    result.outpost_arn = null;
    result.recycle_bin_enter_time = null;
    result.recycle_bin_exit_time = null;
    result.size = null;
    result.snapshot_id = null;
    result.source_volume_id = null;
    result.state = null;
    result.throughput = null;
    result.volume_id = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recycleBinEnterTime")) {
                    result.recycle_bin_enter_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recycleBinExitTime")) {
                    result.recycle_bin_exit_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceVolumeId")) {
                    result.source_volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VolumeState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = VolumeType.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusAction(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeStatusAction {
    var result: VolumeStatusAction = undefined;
    result.code = null;
    result.description = null;
    result.event_id = null;
    result.event_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventId")) {
                    result.event_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusAttachmentStatus(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeStatusAttachmentStatus {
    var result: VolumeStatusAttachmentStatus = undefined;
    result.instance_id = null;
    result.io_performance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ioPerformance")) {
                    result.io_performance = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusDetails(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeStatusDetails {
    var result: VolumeStatusDetails = undefined;
    result.name = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "name")) {
                    result.name = VolumeStatusName.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusEvent(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeStatusEvent {
    var result: VolumeStatusEvent = undefined;
    result.description = null;
    result.event_id = null;
    result.event_type = null;
    result.instance_id = null;
    result.not_after = null;
    result.not_before = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventId")) {
                    result.event_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "notAfter")) {
                    result.not_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "notBefore")) {
                    result.not_before = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeStatusInfo {
    var result: VolumeStatusInfo = undefined;
    result.details = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "details")) {
                    result.details = try deserializeVolumeStatusDetailsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = VolumeStatusInfoStatus.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusItem(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VolumeStatusItem {
    var result: VolumeStatusItem = undefined;
    result.actions = null;
    result.attachment_statuses = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.events = null;
    result.initialization_status_details = null;
    result.outpost_arn = null;
    result.volume_id = null;
    result.volume_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "actionsSet")) {
                    result.actions = try deserializeVolumeStatusActionsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "attachmentStatuses")) {
                    result.attachment_statuses = try deserializeVolumeStatusAttachmentStatusList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventsSet")) {
                    result.events = try deserializeVolumeStatusEventsList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "initializationStatusDetails")) {
                    result.initialization_status_details = try deserializeInitializationStatusDetails(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeStatus")) {
                    result.volume_status = try deserializeVolumeStatusInfo(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpc(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !Vpc {
    var result: Vpc = undefined;
    result.block_public_access_states = null;
    result.cidr_block = null;
    result.cidr_block_association_set = null;
    result.dhcp_options_id = null;
    result.encryption_control = null;
    result.instance_tenancy = null;
    result.ipv_6_cidr_block_association_set = null;
    result.is_default = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blockPublicAccessStates")) {
                    result.block_public_access_states = try deserializeBlockPublicAccessStates(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlockAssociationSet")) {
                    result.cidr_block_association_set = try deserializeVpcCidrBlockAssociationSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "dhcpOptionsId")) {
                    result.dhcp_options_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionControl")) {
                    result.encryption_control = try deserializeVpcEncryptionControl(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "instanceTenancy")) {
                    result.instance_tenancy = Tenancy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockAssociationSet")) {
                    result.ipv_6_cidr_block_association_set = try deserializeVpcIpv6CidrBlockAssociationSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpcState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcAttachment(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcAttachment {
    var result: VpcAttachment = undefined;
    result.state = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = AttachmentStatus.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcBlockPublicAccessExclusion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcBlockPublicAccessExclusion {
    var result: VpcBlockPublicAccessExclusion = undefined;
    result.creation_timestamp = null;
    result.deletion_timestamp = null;
    result.exclusion_id = null;
    result.internet_gateway_exclusion_mode = null;
    result.last_update_timestamp = null;
    result.reason = null;
    result.resource_arn = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTimestamp")) {
                    result.creation_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deletionTimestamp")) {
                    result.deletion_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "exclusionId")) {
                    result.exclusion_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "internetGatewayExclusionMode")) {
                    result.internet_gateway_exclusion_mode = InternetGatewayExclusionMode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdateTimestamp")) {
                    result.last_update_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reason")) {
                    result.reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceArn")) {
                    result.resource_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpcBlockPublicAccessExclusionState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcBlockPublicAccessOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcBlockPublicAccessOptions {
    var result: VpcBlockPublicAccessOptions = undefined;
    result.aws_account_id = null;
    result.aws_region = null;
    result.exclusions_allowed = null;
    result.internet_gateway_block_mode = null;
    result.last_update_timestamp = null;
    result.managed_by = null;
    result.reason = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "awsAccountId")) {
                    result.aws_account_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsRegion")) {
                    result.aws_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exclusionsAllowed")) {
                    result.exclusions_allowed = VpcBlockPublicAccessExclusionsAllowed.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "internetGatewayBlockMode")) {
                    result.internet_gateway_block_mode = InternetGatewayBlockMode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdateTimestamp")) {
                    result.last_update_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "managedBy")) {
                    result.managed_by = ManagedBy.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reason")) {
                    result.reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpcBlockPublicAccessState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcCidrBlockAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcCidrBlockAssociation {
    var result: VpcCidrBlockAssociation = undefined;
    result.association_id = null;
    result.cidr_block = null;
    result.cidr_block_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlockState")) {
                    result.cidr_block_state = try deserializeVpcCidrBlockState(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcCidrBlockState(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcCidrBlockState {
    var result: VpcCidrBlockState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpcCidrBlockStateCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcClassicLink(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcClassicLink {
    var result: VpcClassicLink = undefined;
    result.classic_link_enabled = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLinkEnabled")) {
                    result.classic_link_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionControl(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcEncryptionControl {
    var result: VpcEncryptionControl = undefined;
    result.mode = null;
    result.resource_exclusions = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.vpc_encryption_control_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "mode")) {
                    result.mode = VpcEncryptionControlMode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceExclusions")) {
                    result.resource_exclusions = try deserializeVpcEncryptionControlExclusions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpcEncryptionControlState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEncryptionControlId")) {
                    result.vpc_encryption_control_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionControlExclusion(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcEncryptionControlExclusion {
    var result: VpcEncryptionControlExclusion = undefined;
    result.state = null;
    result.state_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpcEncryptionControlExclusionState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionControlExclusions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcEncryptionControlExclusions {
    var result: VpcEncryptionControlExclusions = undefined;
    result.egress_only_internet_gateway = null;
    result.elastic_file_system = null;
    result.internet_gateway = null;
    result.lambda = null;
    result.nat_gateway = null;
    result.virtual_private_gateway = null;
    result.vpc_lattice = null;
    result.vpc_peering = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "egressOnlyInternetGateway")) {
                    result.egress_only_internet_gateway = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "elasticFileSystem")) {
                    result.elastic_file_system = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "internetGateway")) {
                    result.internet_gateway = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "lambda")) {
                    result.lambda = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "natGateway")) {
                    result.nat_gateway = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "virtualPrivateGateway")) {
                    result.virtual_private_gateway = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpcLattice")) {
                    result.vpc_lattice = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "vpcPeering")) {
                    result.vpc_peering = try deserializeVpcEncryptionControlExclusion(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionNonCompliantResource(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcEncryptionNonCompliantResource {
    var result: VpcEncryptionNonCompliantResource = undefined;
    result.description = null;
    result.id = null;
    result.is_excludable = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isExcludable")) {
                    result.is_excludable = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEndpoint(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcEndpoint {
    var result: VpcEndpoint = undefined;
    result.creation_timestamp = null;
    result.dns_entries = null;
    result.dns_options = null;
    result.failure_reason = null;
    result.groups = null;
    result.ip_address_type = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_prefixes = null;
    result.last_error = null;
    result.network_interface_ids = null;
    result.owner_id = null;
    result.policy_document = null;
    result.private_dns_enabled = null;
    result.requester_managed = null;
    result.resource_configuration_arn = null;
    result.route_table_ids = null;
    result.service_name = null;
    result.service_network_arn = null;
    result.service_region = null;
    result.state = null;
    result.subnet_ids = null;
    result.tags = null;
    result.vpc_endpoint_id = null;
    result.vpc_endpoint_type = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTimestamp")) {
                    result.creation_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dnsEntrySet")) {
                    result.dns_entries = try deserializeDnsEntrySet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "dnsOptions")) {
                    result.dns_options = try deserializeDnsOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipAddressType")) {
                    result.ip_address_type = IpAddressType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeSubnetIpPrefixesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeSubnetIpPrefixesList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "lastError")) {
                    result.last_error = try deserializeLastError(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceIdSet")) {
                    result.network_interface_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "policyDocument")) {
                    result.policy_document = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsEnabled")) {
                    result.private_dns_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "requesterManaged")) {
                    result.requester_managed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "resourceConfigurationArn")) {
                    result.resource_configuration_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableIdSet")) {
                    result.route_table_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceNetworkArn")) {
                    result.service_network_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceRegion")) {
                    result.service_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = State.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointType")) {
                    result.vpc_endpoint_type = VpcEndpointType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEndpointAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcEndpointAssociation {
    var result: VpcEndpointAssociation = undefined;
    result.associated_resource_accessibility = null;
    result.associated_resource_arn = null;
    result.dns_entry = null;
    result.failure_code = null;
    result.failure_reason = null;
    result.id = null;
    result.private_dns_entry = null;
    result.resource_configuration_group_arn = null;
    result.service_network_arn = null;
    result.service_network_name = null;
    result.tags = null;
    result.vpc_endpoint_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResourceAccessibility")) {
                    result.associated_resource_accessibility = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associatedResourceArn")) {
                    result.associated_resource_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsEntry")) {
                    result.dns_entry = try deserializeDnsEntry(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "failureCode")) {
                    result.failure_code = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsEntry")) {
                    result.private_dns_entry = try deserializeDnsEntry(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "resourceConfigurationGroupArn")) {
                    result.resource_configuration_group_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceNetworkArn")) {
                    result.service_network_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceNetworkName")) {
                    result.service_network_name = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEndpointConnection(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcEndpointConnection {
    var result: VpcEndpointConnection = undefined;
    result.creation_timestamp = null;
    result.dns_entries = null;
    result.gateway_load_balancer_arns = null;
    result.ip_address_type = null;
    result.network_load_balancer_arns = null;
    result.service_id = null;
    result.tags = null;
    result.vpc_endpoint_connection_id = null;
    result.vpc_endpoint_id = null;
    result.vpc_endpoint_owner = null;
    result.vpc_endpoint_region = null;
    result.vpc_endpoint_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTimestamp")) {
                    result.creation_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dnsEntrySet")) {
                    result.dns_entries = try deserializeDnsEntrySet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "gatewayLoadBalancerArnSet")) {
                    result.gateway_load_balancer_arns = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipAddressType")) {
                    result.ip_address_type = IpAddressType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkLoadBalancerArnSet")) {
                    result.network_load_balancer_arns = try deserializeValueStringList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointConnectionId")) {
                    result.vpc_endpoint_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointOwner")) {
                    result.vpc_endpoint_owner = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointRegion")) {
                    result.vpc_endpoint_region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointState")) {
                    result.vpc_endpoint_state = State.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcIpv6CidrBlockAssociation(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcIpv6CidrBlockAssociation {
    var result: VpcIpv6CidrBlockAssociation = undefined;
    result.association_id = null;
    result.ip_source = null;
    result.ipv_6_address_attribute = null;
    result.ipv_6_cidr_block = null;
    result.ipv_6_cidr_block_state = null;
    result.ipv_6_pool = null;
    result.network_border_group = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipSource")) {
                    result.ip_source = IpSource.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6AddressAttribute")) {
                    result.ipv_6_address_attribute = Ipv6AddressAttribute.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockState")) {
                    result.ipv_6_cidr_block_state = try deserializeVpcCidrBlockState(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "ipv6Pool")) {
                    result.ipv_6_pool = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnection(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcPeeringConnection {
    var result: VpcPeeringConnection = undefined;
    result.accepter_vpc_info = null;
    result.expiration_time = null;
    result.requester_vpc_info = null;
    result.status = null;
    result.tags = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accepterVpcInfo")) {
                    result.accepter_vpc_info = try deserializeVpcPeeringConnectionVpcInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "expirationTime")) {
                    result.expiration_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "requesterVpcInfo")) {
                    result.requester_vpc_info = try deserializeVpcPeeringConnectionVpcInfo(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeVpcPeeringConnectionStateReason(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnectionOptionsDescription(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcPeeringConnectionOptionsDescription {
    _ = allocator;
    var result: VpcPeeringConnectionOptionsDescription = undefined;
    result.allow_dns_resolution_from_remote_vpc = null;
    result.allow_egress_from_local_classic_link_to_remote_vpc = null;
    result.allow_egress_from_local_vpc_to_remote_classic_link = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allowDnsResolutionFromRemoteVpc")) {
                    result.allow_dns_resolution_from_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalClassicLinkToRemoteVpc")) {
                    result.allow_egress_from_local_classic_link_to_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalVpcToRemoteClassicLink")) {
                    result.allow_egress_from_local_vpc_to_remote_classic_link = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnectionStateReason(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcPeeringConnectionStateReason {
    var result: VpcPeeringConnectionStateReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = VpcPeeringConnectionStateReasonCode.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnectionVpcInfo(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpcPeeringConnectionVpcInfo {
    var result: VpcPeeringConnectionVpcInfo = undefined;
    result.cidr_block = null;
    result.cidr_block_set = null;
    result.ipv_6_cidr_block_set = null;
    result.owner_id = null;
    result.peering_options = null;
    result.region = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlockSet")) {
                    result.cidr_block_set = try deserializeCidrBlockSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockSet")) {
                    result.ipv_6_cidr_block_set = try deserializeIpv6CidrBlockSet(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringOptions")) {
                    result.peering_options = try deserializeVpcPeeringConnectionOptionsDescription(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConcentrator(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpnConcentrator {
    var result: VpnConcentrator = undefined;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.@"type" = null;
    result.vpn_concentrator_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnConcentratorId")) {
                    result.vpn_concentrator_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConnection(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpnConnection {
    var result: VpnConnection = undefined;
    result.category = null;
    result.core_network_arn = null;
    result.core_network_attachment_arn = null;
    result.customer_gateway_configuration = null;
    result.customer_gateway_id = null;
    result.gateway_association_state = null;
    result.options = null;
    result.pre_shared_key_arn = null;
    result.routes = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.@"type" = null;
    result.vgw_telemetry = null;
    result.vpn_concentrator_id = null;
    result.vpn_connection_id = null;
    result.vpn_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "category")) {
                    result.category = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkArn")) {
                    result.core_network_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkAttachmentArn")) {
                    result.core_network_attachment_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGatewayConfiguration")) {
                    result.customer_gateway_configuration = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGatewayId")) {
                    result.customer_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayAssociationState")) {
                    result.gateway_association_state = GatewayAssociationState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeVpnConnectionOptions(allocator, reader);
                } else if (std.mem.eql(u8, e.local, "preSharedKeyArn")) {
                    result.pre_shared_key_arn = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routes")) {
                    result.routes = try deserializeVpnStaticRouteList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpnState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = GatewayType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vgwTelemetry")) {
                    result.vgw_telemetry = try deserializeVgwTelemetryList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpnConcentratorId")) {
                    result.vpn_concentrator_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnConnectionId")) {
                    result.vpn_connection_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnGatewayId")) {
                    result.vpn_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConnectionDeviceType(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpnConnectionDeviceType {
    var result: VpnConnectionDeviceType = undefined;
    result.platform = null;
    result.software = null;
    result.vendor = null;
    result.vpn_connection_device_type_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "software")) {
                    result.software = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vendor")) {
                    result.vendor = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnConnectionDeviceTypeId")) {
                    result.vpn_connection_device_type_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConnectionOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpnConnectionOptions {
    var result: VpnConnectionOptions = undefined;
    result.enable_acceleration = null;
    result.local_ipv_4_network_cidr = null;
    result.local_ipv_6_network_cidr = null;
    result.outside_ip_address_type = null;
    result.remote_ipv_4_network_cidr = null;
    result.remote_ipv_6_network_cidr = null;
    result.static_routes_only = null;
    result.transport_transit_gateway_attachment_id = null;
    result.tunnel_bandwidth = null;
    result.tunnel_inside_ip_version = null;
    result.tunnel_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableAcceleration")) {
                    result.enable_acceleration = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "localIpv4NetworkCidr")) {
                    result.local_ipv_4_network_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localIpv6NetworkCidr")) {
                    result.local_ipv_6_network_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outsideIpAddressType")) {
                    result.outside_ip_address_type = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remoteIpv4NetworkCidr")) {
                    result.remote_ipv_4_network_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remoteIpv6NetworkCidr")) {
                    result.remote_ipv_6_network_cidr = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staticRoutesOnly")) {
                    result.static_routes_only = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "transportTransitGatewayAttachmentId")) {
                    result.transport_transit_gateway_attachment_id = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelBandwidth")) {
                    result.tunnel_bandwidth = VpnTunnelBandwidth.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelInsideIpVersion")) {
                    result.tunnel_inside_ip_version = TunnelInsideIpVersion.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelOptionSet")) {
                    result.tunnel_options = try deserializeTunnelOptionsList(allocator, reader, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnGateway(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpnGateway {
    var result: VpnGateway = undefined;
    result.amazon_side_asn = null;
    result.availability_zone = null;
    result.state = null;
    result.tags = null;
    result.@"type" = null;
    result.vpc_attachments = null;
    result.vpn_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amazonSideAsn")) {
                    result.amazon_side_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpnState.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = GatewayType.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachments")) {
                    result.vpc_attachments = try deserializeVpcAttachmentList(allocator, reader, "item");
                } else if (std.mem.eql(u8, e.local, "vpnGatewayId")) {
                    result.vpn_gateway_id = try allocator.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnStaticRoute(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpnStaticRoute {
    var result: VpnStaticRoute = undefined;
    result.destination_cidr_block = null;
    result.source = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try allocator.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = VpnStaticRouteSource.fromWireName(try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = VpnState.fromWireName(try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnTunnelLogOptions(allocator: std.mem.Allocator, reader: *aws.xml.Reader) !VpnTunnelLogOptions {
    var result: VpnTunnelLogOptions = undefined;
    result.cloud_watch_log_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cloudWatchLogOptions")) {
                    result.cloud_watch_log_options = try deserializeCloudWatchLogOptions(allocator, reader);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn serializeAcceleratorManufacturerSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AcceleratorManufacturer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAcceleratorNameSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AcceleratorName, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAcceleratorTypeSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AcceleratorType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAccessScopePathListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AccessScopePathRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeAccessScopePathRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAccountAttributeNameStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AccountAttributeName, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAddIpamOperatingRegionSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AddIpamOperatingRegion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeAddIpamOperatingRegion(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAddIpamOrganizationalUnitExclusionSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AddIpamOrganizationalUnitExclusion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeAddIpamOrganizationalUnitExclusion(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAddPrefixListEntries(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AddPrefixListEntry, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeAddPrefixListEntry(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAllocationIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAllocationIds(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAllowedInstanceTypeSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeArchitectureTypeSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ArchitectureType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeArnList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAssetIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAssociationIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAthenaIntegrationsSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AthenaIntegration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeAthenaIntegration(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAvailabilityZoneAddresses(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AvailabilityZoneAddress, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeAvailabilityZoneAddress(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAvailabilityZoneIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAvailabilityZoneStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeBillingProductList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeBlockDeviceMappingList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const BlockDeviceMapping, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeBlockDeviceMapping(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeBlockDeviceMappingRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const BlockDeviceMapping, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeBlockDeviceMapping(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeBundleIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCapacityBlockIds(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCapacityManagerConditionSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CapacityManagerCondition, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeCapacityManagerCondition(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCapacityManagerDataExportIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCapacityReservationFleetIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCapacityReservationIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCarrierGatewayIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeClassicLoadBalancers(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ClassicLoadBalancer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeClassicLoadBalancer(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeClientVpnAuthenticationRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ClientVpnAuthenticationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeClientVpnAuthenticationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeClientVpnEndpointIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeClientVpnSecurityGroupIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCoipPoolIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeConditionValueList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeConnectionNotificationIdsList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeConversionIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCpuManufacturerSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CpuManufacturer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCreateVerifiedAccessEndpointPortRangeList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CreateVerifiedAccessEndpointPortRange, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeCreateVerifiedAccessEndpointPortRange(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCreateVerifiedAccessEndpointSubnetIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCreateVolumePermissionList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CreateVolumePermission, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeCreateVolumePermission(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeCustomerGatewayIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDataQueries(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const DataQuery, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeDataQuery(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDedicatedHostIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDeleteQueuedReservedInstancesIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDescribeImageReferencesImageIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDescribeImageUsageReportsImageIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDescribeInstanceTopologyGroupNameSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDescribeInstanceTopologyInstanceIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDhcpOptionsIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeDiskImageList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const DiskImage, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeDiskImage(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeEgressOnlyInternetGatewayIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeEipAssociationIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeElasticGpuIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeElasticGpuSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ElasticGpuSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeElasticGpuSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeElasticGpuSpecifications(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ElasticGpuSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeElasticGpuSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeElasticInferenceAccelerators(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ElasticInferenceAccelerator, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeElasticInferenceAccelerator(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeExcludedInstanceTypeSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeExecutableByStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeExportImageTaskIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeExportTaskIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFastLaunchImageIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFilterList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Filter, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeFilter(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFleetBlockDeviceMappingRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const FleetBlockDeviceMappingRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeFleetBlockDeviceMappingRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFleetIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFleetLaunchTemplateConfigListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const FleetLaunchTemplateConfigRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeFleetLaunchTemplateConfigRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFleetLaunchTemplateOverridesListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const FleetLaunchTemplateOverridesRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeFleetLaunchTemplateOverridesRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFlowLogIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFlowLogResourceIds(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeFpgaImageIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeGroupBySet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const GroupBy, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeGroupIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeGroupIdentifierList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const GroupIdentifier, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeGroupIdentifier(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeGroupIds(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeGroupNameStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeHostReservationIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIKEVersionsRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IKEVersionsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIKEVersionsRequestListValue(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageCriterionRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageCriterionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeImageCriterionRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageDiskContainerList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageDiskContainer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeImageDiskContainer(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageNameRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageProviderRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageUsageReportIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageUsageReportUserIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageUsageResourceTypeOptionRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageUsageResourceTypeOptionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeImageUsageResourceTypeOptionRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageUsageResourceTypeOptionValuesList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImageUsageResourceTypeRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageUsageResourceTypeRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeImageUsageResourceTypeRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImportImageLicenseSpecificationListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImportImageLicenseConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeImportImageLicenseConfigurationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImportSnapshotTaskIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeImportTaskIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInsideCidrBlocksStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceBlockDeviceMappingSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceBlockDeviceMappingSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeInstanceBlockDeviceMappingSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceCreditSpecificationListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceCreditSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeInstanceCreditSpecificationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceEventWindowIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceEventWindowTimeRangeRequestSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceEventWindowTimeRangeRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeInstanceEventWindowTimeRangeRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceGenerationSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceGeneration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceIdUpdateStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceIpv6AddressList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceIpv6Address, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeInstanceIpv6Address(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceIpv6AddressListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceIpv6AddressRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeInstanceIpv6AddressRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceNetworkInterfaceSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceNetworkInterfaceSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeInstanceNetworkInterfaceSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceTagKeySet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceTypeList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInstanceTypes(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeInternetGatewayIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpPermissionList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpPermission, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpPermission(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpPrefixList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpRangeList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpRange, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpRange(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpamPolicyAllocationRuleListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpamPolicyAllocationRuleRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpamPolicyAllocationRuleRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpamPoolAllocationAllowedCidrs(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpamPoolAllocationDisallowedCidrs(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpamPrefixListResolverRuleConditionRequestSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpamPrefixListResolverRuleConditionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpamPrefixListResolverRuleConditionRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpamPrefixListResolverRuleRequestSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpamPrefixListResolverRuleRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpamPrefixListResolverRuleRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpamPrefixListResolverVersionNumberSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const i64, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{item}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpv4PrefixList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Ipv4PrefixSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpv4PrefixSpecificationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpv6AddressList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpv6PoolIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpv6PrefixList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Ipv6PrefixSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpv6PrefixSpecificationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeIpv6RangeList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Ipv6Range, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeIpv6Range(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeKeyNameStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeKeyPairIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchPermissionList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchPermission, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchPermission(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchSpecsList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SpotFleetLaunchSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeSpotFleetLaunchSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateBlockDeviceMappingRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateBlockDeviceMappingRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchTemplateBlockDeviceMappingRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateConfigList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateConfig, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchTemplateConfig(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateElasticInferenceAcceleratorList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateElasticInferenceAccelerator, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchTemplateElasticInferenceAccelerator(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateInstanceNetworkInterfaceSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateLicenseSpecificationListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateLicenseConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchTemplateLicenseConfigurationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateNameStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateOverridesList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateOverrides, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchTemplateOverrides(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLaunchTemplateTagSpecificationRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateTagSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLaunchTemplateTagSpecificationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLicenseSpecificationListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LicenseConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLicenseConfigurationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLoadPermissionListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LoadPermissionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeLoadPermissionRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLocalGatewayIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLocalGatewayRouteTableIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLocalGatewayRouteTableVpcAssociationIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLocalGatewayVirtualInterfaceGroupIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLocalGatewayVirtualInterfaceIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeLocalStorageTypeSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LocalStorageType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeMacModificationTaskIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeMarketplaceProductCodeRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeMetricSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Metric, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeModifyVerifiedAccessEndpointPortRangeList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ModifyVerifiedAccessEndpointPortRange, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeModifyVerifiedAccessEndpointPortRange(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeModifyVerifiedAccessEndpointSubnetIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNatGatewayIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNetworkAclIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNetworkInsightsAccessScopeAnalysisIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNetworkInsightsAccessScopeIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNetworkInsightsAnalysisIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNetworkInsightsPathIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNetworkInterfaceIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNetworkInterfacePermissionIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeNewDhcpConfigurationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const NewDhcpConfiguration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeNewDhcpConfiguration(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeOccurrenceDayRequestSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const i32, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{item}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeOrganizationArnStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeOrganizationalUnitArnStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeOutpostLagIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeOwnerStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePerformanceFactorReferenceSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PerformanceFactorReference, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePerformanceFactorReference(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePerformanceFactorReferenceSetRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PerformanceFactorReferenceRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePerformanceFactorReferenceRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePhase1DHGroupNumbersRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase1DHGroupNumbersRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePhase1DHGroupNumbersRequestListValue(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePhase1EncryptionAlgorithmsRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase1EncryptionAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePhase1EncryptionAlgorithmsRequestListValue(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePhase1IntegrityAlgorithmsRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase1IntegrityAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePhase1IntegrityAlgorithmsRequestListValue(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePhase2DHGroupNumbersRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase2DHGroupNumbersRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePhase2DHGroupNumbersRequestListValue(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePhase2EncryptionAlgorithmsRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase2EncryptionAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePhase2EncryptionAlgorithmsRequestListValue(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePhase2IntegrityAlgorithmsRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase2IntegrityAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePhase2IntegrityAlgorithmsRequestListValue(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePlacementGroupIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePlacementGroupStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePrefixListIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PrefixListId, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePrefixListId(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePrefixListResourceIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePriceScheduleSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PriceScheduleSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePriceScheduleSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePrivateDnsSpecifiedDomainSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePrivateIpAddressConfigSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesPrivateIpAddressConfig, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeScheduledInstancesPrivateIpAddressConfig(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePrivateIpAddressSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PrivateIpAddressSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePrivateIpAddressSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePrivateIpAddressStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeProductCodeStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeProductDescriptionList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeProtocolList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Protocol, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePublicIpStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePublicIpv4PoolIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializePurchaseRequestSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PurchaseRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializePurchaseRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReasonCodesList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ReportInstanceReasonCodes, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRegionNameStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRegionNames(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRemoveIpamOperatingRegionSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RemoveIpamOperatingRegion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeRemoveIpamOperatingRegion(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRemoveIpamOrganizationalUnitExclusionSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RemoveIpamOrganizationalUnitExclusion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeRemoveIpamOrganizationalUnitExclusion(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRemovePrefixListEntries(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RemovePrefixListEntry, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeRemovePrefixListEntry(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReplaceRootVolumeTaskIds(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRequestHostIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRequestHostIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRequestInstanceTypeList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRequestIpamResourceTagList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RequestIpamResourceTag, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeRequestIpamResourceTag(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRequestSpotLaunchSpecificationSecurityGroupIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRequestSpotLaunchSpecificationSecurityGroupList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReservationFleetInstanceSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ReservationFleetInstanceSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeReservationFleetInstanceSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReservedInstanceIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReservedInstancesConfigurationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ReservedInstancesConfiguration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeReservedInstancesConfiguration(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReservedInstancesIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReservedInstancesModificationIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeReservedInstancesOfferingIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeResourceIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeResourceList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeResourceTypeOptionList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ResourceTypeOption, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeResourceTypeOption(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeResourceTypeOptionValuesList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeResourceTypeRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ResourceTypeRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeResourceTypeRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRestorableByStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRouteServerEndpointIdsList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRouteServerIdsList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRouteServerPeerIdsList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeRouteTableIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeS3ObjectTagList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const S3ObjectTag, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeS3ObjectTag(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeScheduledInstanceIdRequestSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeScheduledInstancesBlockDeviceMappingSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesBlockDeviceMapping, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeScheduledInstancesBlockDeviceMapping(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeScheduledInstancesIpv6AddressList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesIpv6Address, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeScheduledInstancesIpv6Address(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeScheduledInstancesNetworkInterfaceSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesNetworkInterface, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeScheduledInstancesNetworkInterface(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeScheduledInstancesSecurityGroupIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSecurityGroupIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSecurityGroupIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSecurityGroupIdStringListRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSecurityGroupRuleDescriptionList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SecurityGroupRuleDescription, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeSecurityGroupRuleDescription(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSecurityGroupRuleIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSecurityGroupRuleUpdateList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SecurityGroupRuleUpdate, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeSecurityGroupRuleUpdate(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSecurityGroupStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeServiceLinkVirtualInterfaceIdSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSnapshotIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSpotFleetRequestIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSpotFleetTagSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SpotFleetTagSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeSpotFleetTagSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSpotInstanceRequestIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSubnetConfigurationsList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SubnetConfiguration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeSubnetConfiguration(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSubnetIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeSubnetIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTagList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Tag, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeTag(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTagSpecificationList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TagSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeTagSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTargetConfigurationRequestSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TargetConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeTargetConfigurationRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTargetGroups(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TargetGroup, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeTargetGroup(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeThroughResourcesStatementRequestList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ThroughResourcesStatementRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeThroughResourcesStatementRequest(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrafficMirrorFilterIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrafficMirrorFilterRuleFieldList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TrafficMirrorFilterRuleField, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrafficMirrorFilterRuleIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrafficMirrorNetworkServiceList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TrafficMirrorNetworkService, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrafficMirrorSessionFieldList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TrafficMirrorSessionField, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrafficMirrorSessionIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrafficMirrorTargetIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayAttachmentIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayCidrBlockStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayConnectPeerIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayMeteringPolicyIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayMulticastDomainIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayNetworkInterfaceIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayPolicyTableIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayRouteTableAnnouncementIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewayRouteTableIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTransitGatewaySubnetIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeTrunkInterfaceAssociationIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeUserGroupStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeUserIdGroupPairList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const UserIdGroupPair, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeUserIdGroupPair(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeUserIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeValueStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVerifiedAccessEndpointIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVerifiedAccessGroupIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVerifiedAccessInstanceIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVerifiedAccessTrustProviderIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVersionStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVirtualizationTypeSet(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const VirtualizationType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try buf.appendSlice(allocator, item.wireName());
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVolumeIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcBlockPublicAccessExclusionIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcClassicLinkIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcEncryptionControlIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcEndpointIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcEndpointRouteTableIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcEndpointSecurityGroupIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcEndpointServiceIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcEndpointSubnetIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpcPeeringConnectionIdList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpnConcentratorIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpnConnectionIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpnGatewayIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeVpnTunnelOptionsSpecificationsList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const VpnTunnelOptionsSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try serializeVpnTunnelOptionsSpecification(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeZoneIdStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeZoneNameStringList(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(allocator, "<");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
        try aws.xml.appendXmlEscaped(allocator, buf, item);
        try buf.appendSlice(allocator, "</");
        try buf.appendSlice(allocator, item_tag);
        try buf.appendSlice(allocator, ">");
    }
}

pub fn serializeAcceleratorCount(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorCount) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeAcceleratorCountRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorCountRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<Min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Min>");
    }
}

pub fn serializeAcceleratorTotalMemoryMiB(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorTotalMemoryMiB) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeAcceleratorTotalMemoryMiBRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorTotalMemoryMiBRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<Min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Min>");
    }
}

pub fn serializeAccessScopePathRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AccessScopePathRequest) !void {
    if (value.destination) |v| {
        try buf.appendSlice(allocator, "<Destination>");
        try serializePathStatementRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Destination>");
    }
    if (value.source) |v| {
        try buf.appendSlice(allocator, "<Source>");
        try serializePathStatementRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Source>");
    }
    if (value.through_resources) |v| {
        try buf.appendSlice(allocator, "<ThroughResource>");
        try serializeThroughResourcesStatementRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ThroughResource>");
    }
}

pub fn serializeAddIpamOperatingRegion(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AddIpamOperatingRegion) !void {
    if (value.region_name) |v| {
        try buf.appendSlice(allocator, "<RegionName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RegionName>");
    }
}

pub fn serializeAddIpamOrganizationalUnitExclusion(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AddIpamOrganizationalUnitExclusion) !void {
    if (value.organizations_entity_path) |v| {
        try buf.appendSlice(allocator, "<OrganizationsEntityPath>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</OrganizationsEntityPath>");
    }
}

pub fn serializeAddPrefixListEntry(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AddPrefixListEntry) !void {
    try buf.appendSlice(allocator, "<Cidr>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.cidr);
    try buf.appendSlice(allocator, "</Cidr>");
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
}

pub fn serializeAsnAuthorizationContext(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AsnAuthorizationContext) !void {
    try buf.appendSlice(allocator, "<Message>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.message);
    try buf.appendSlice(allocator, "</Message>");
    try buf.appendSlice(allocator, "<Signature>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.signature);
    try buf.appendSlice(allocator, "</Signature>");
}

pub fn serializeAthenaIntegration(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AthenaIntegration) !void {
    try buf.appendSlice(allocator, "<IntegrationResultS3DestinationArn>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.integration_result_s3_destination_arn);
    try buf.appendSlice(allocator, "</IntegrationResultS3DestinationArn>");
    if (value.partition_end_date) |v| {
        try buf.appendSlice(allocator, "<PartitionEndDate>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</PartitionEndDate>");
    }
    try buf.appendSlice(allocator, "<PartitionLoadFrequency>");
    try buf.appendSlice(allocator, value.partition_load_frequency.wireName());
    try buf.appendSlice(allocator, "</PartitionLoadFrequency>");
    if (value.partition_start_date) |v| {
        try buf.appendSlice(allocator, "<PartitionStartDate>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</PartitionStartDate>");
    }
}

pub fn serializeAttributeBooleanValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AttributeBooleanValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<value>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</value>");
    }
}

pub fn serializeAttributeValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AttributeValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</value>");
    }
}

pub fn serializeAvailabilityZoneAddress(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: AvailabilityZoneAddress) !void {
    if (value.allocation_ids) |v| {
        try buf.appendSlice(allocator, "<AllocationId>");
        try serializeAllocationIdList(allocator, buf, v, "AllocationId");
        try buf.appendSlice(allocator, "</AllocationId>");
    }
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZoneId>");
    }
}

pub fn serializeBaselineEbsBandwidthMbps(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselineEbsBandwidthMbps) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeBaselineEbsBandwidthMbpsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselineEbsBandwidthMbpsRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<Min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Min>");
    }
}

pub fn serializeBaselinePerformanceFactors(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselinePerformanceFactors) !void {
    if (value.cpu) |v| {
        try buf.appendSlice(allocator, "<cpu>");
        try serializeCpuPerformanceFactor(allocator, buf, v);
        try buf.appendSlice(allocator, "</cpu>");
    }
}

pub fn serializeBaselinePerformanceFactorsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselinePerformanceFactorsRequest) !void {
    if (value.cpu) |v| {
        try buf.appendSlice(allocator, "<Cpu>");
        try serializeCpuPerformanceFactorRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Cpu>");
    }
}

pub fn serializeBlobAttributeValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: BlobAttributeValue) !void {
    _ = allocator;
    _ = buf;
    _ = value;
}

pub fn serializeBlockDeviceMapping(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: BlockDeviceMapping) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(allocator, "<deviceName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</deviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(allocator, "<ebs>");
        try serializeEbsBlockDevice(allocator, buf, v);
        try buf.appendSlice(allocator, "</ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(allocator, "<noDevice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</noDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(allocator, "<virtualName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</virtualName>");
    }
}

pub fn serializeCapacityManagerCondition(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityManagerCondition) !void {
    if (value.dimension_condition) |v| {
        try buf.appendSlice(allocator, "<DimensionCondition>");
        try serializeDimensionCondition(allocator, buf, v);
        try buf.appendSlice(allocator, "</DimensionCondition>");
    }
}

pub fn serializeCapacityReservationOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityReservationOptionsRequest) !void {
    if (value.usage_strategy) |v| {
        try buf.appendSlice(allocator, "<UsageStrategy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</UsageStrategy>");
    }
}

pub fn serializeCapacityReservationSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityReservationSpecification) !void {
    if (value.capacity_reservation_preference) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationPreference>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</CapacityReservationPreference>");
    }
    if (value.capacity_reservation_target) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationTarget>");
        try serializeCapacityReservationTarget(allocator, buf, v);
        try buf.appendSlice(allocator, "</CapacityReservationTarget>");
    }
}

pub fn serializeCapacityReservationTarget(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityReservationTarget) !void {
    if (value.capacity_reservation_id) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</CapacityReservationId>");
    }
    if (value.capacity_reservation_resource_group_arn) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationResourceGroupArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</CapacityReservationResourceGroupArn>");
    }
}

pub fn serializeCertificateAuthenticationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CertificateAuthenticationRequest) !void {
    if (value.client_root_certificate_chain_arn) |v| {
        try buf.appendSlice(allocator, "<ClientRootCertificateChainArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientRootCertificateChainArn>");
    }
}

pub fn serializeCidrAuthorizationContext(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CidrAuthorizationContext) !void {
    try buf.appendSlice(allocator, "<Message>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.message);
    try buf.appendSlice(allocator, "</Message>");
    try buf.appendSlice(allocator, "<Signature>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.signature);
    try buf.appendSlice(allocator, "</Signature>");
}

pub fn serializeClassicLoadBalancer(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClassicLoadBalancer) !void {
    if (value.name) |v| {
        try buf.appendSlice(allocator, "<name>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</name>");
    }
}

pub fn serializeClassicLoadBalancersConfig(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClassicLoadBalancersConfig) !void {
    if (value.classic_load_balancers) |v| {
        try buf.appendSlice(allocator, "<classicLoadBalancers>");
        try serializeClassicLoadBalancers(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</classicLoadBalancers>");
    }
}

pub fn serializeClientConnectOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientConnectOptions) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
    if (value.lambda_function_arn) |v| {
        try buf.appendSlice(allocator, "<LambdaFunctionArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LambdaFunctionArn>");
    }
}

pub fn serializeClientData(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientData) !void {
    if (value.comment) |v| {
        try buf.appendSlice(allocator, "<Comment>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Comment>");
    }
    if (value.upload_end) |v| {
        try buf.appendSlice(allocator, "<UploadEnd>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</UploadEnd>");
    }
    if (value.upload_size) |v| {
        try buf.appendSlice(allocator, "<UploadSize>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</UploadSize>");
    }
    if (value.upload_start) |v| {
        try buf.appendSlice(allocator, "<UploadStart>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</UploadStart>");
    }
}

pub fn serializeClientLoginBannerOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientLoginBannerOptions) !void {
    if (value.banner_text) |v| {
        try buf.appendSlice(allocator, "<BannerText>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</BannerText>");
    }
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
}

pub fn serializeClientRouteEnforcementOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientRouteEnforcementOptions) !void {
    if (value.enforced) |v| {
        try buf.appendSlice(allocator, "<Enforced>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enforced>");
    }
}

pub fn serializeClientVpnAuthenticationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientVpnAuthenticationRequest) !void {
    if (value.active_directory) |v| {
        try buf.appendSlice(allocator, "<ActiveDirectory>");
        try serializeDirectoryServiceAuthenticationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</ActiveDirectory>");
    }
    if (value.federated_authentication) |v| {
        try buf.appendSlice(allocator, "<FederatedAuthentication>");
        try serializeFederatedAuthenticationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</FederatedAuthentication>");
    }
    if (value.mutual_authentication) |v| {
        try buf.appendSlice(allocator, "<MutualAuthentication>");
        try serializeCertificateAuthenticationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</MutualAuthentication>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(allocator, "<Type>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Type>");
    }
}

pub fn serializeCloudWatchLogOptionsSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CloudWatchLogOptionsSpecification) !void {
    if (value.bgp_log_enabled) |v| {
        try buf.appendSlice(allocator, "<BgpLogEnabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</BgpLogEnabled>");
    }
    if (value.bgp_log_group_arn) |v| {
        try buf.appendSlice(allocator, "<BgpLogGroupArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</BgpLogGroupArn>");
    }
    if (value.bgp_log_output_format) |v| {
        try buf.appendSlice(allocator, "<BgpLogOutputFormat>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</BgpLogOutputFormat>");
    }
    if (value.log_enabled) |v| {
        try buf.appendSlice(allocator, "<LogEnabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</LogEnabled>");
    }
    if (value.log_group_arn) |v| {
        try buf.appendSlice(allocator, "<LogGroupArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LogGroupArn>");
    }
    if (value.log_output_format) |v| {
        try buf.appendSlice(allocator, "<LogOutputFormat>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LogOutputFormat>");
    }
}

pub fn serializeConnectionLogOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ConnectionLogOptions) !void {
    if (value.cloudwatch_log_group) |v| {
        try buf.appendSlice(allocator, "<CloudwatchLogGroup>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</CloudwatchLogGroup>");
    }
    if (value.cloudwatch_log_stream) |v| {
        try buf.appendSlice(allocator, "<CloudwatchLogStream>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</CloudwatchLogStream>");
    }
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
}

pub fn serializeConnectionTrackingSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ConnectionTrackingSpecificationRequest) !void {
    if (value.tcp_established_timeout) |v| {
        try buf.appendSlice(allocator, "<TcpEstablishedTimeout>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</TcpEstablishedTimeout>");
    }
    if (value.udp_stream_timeout) |v| {
        try buf.appendSlice(allocator, "<UdpStreamTimeout>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</UdpStreamTimeout>");
    }
    if (value.udp_timeout) |v| {
        try buf.appendSlice(allocator, "<UdpTimeout>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</UdpTimeout>");
    }
}

pub fn serializeCpuOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CpuOptionsRequest) !void {
    if (value.amd_sev_snp) |v| {
        try buf.appendSlice(allocator, "<AmdSevSnp>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AmdSevSnp>");
    }
    if (value.core_count) |v| {
        try buf.appendSlice(allocator, "<CoreCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</CoreCount>");
    }
    if (value.threads_per_core) |v| {
        try buf.appendSlice(allocator, "<ThreadsPerCore>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ThreadsPerCore>");
    }
}

pub fn serializeCpuPerformanceFactor(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CpuPerformanceFactor) !void {
    if (value.references) |v| {
        try buf.appendSlice(allocator, "<referenceSet>");
        try serializePerformanceFactorReferenceSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</referenceSet>");
    }
}

pub fn serializeCpuPerformanceFactorRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CpuPerformanceFactorRequest) !void {
    if (value.references) |v| {
        try buf.appendSlice(allocator, "<Reference>");
        try serializePerformanceFactorReferenceSetRequest(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Reference>");
    }
}

pub fn serializeCreateTransitGatewayConnectRequestOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayConnectRequestOptions) !void {
    try buf.appendSlice(allocator, "<Protocol>");
    try buf.appendSlice(allocator, value.protocol.wireName());
    try buf.appendSlice(allocator, "</Protocol>");
}

pub fn serializeCreateTransitGatewayMulticastDomainRequestOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayMulticastDomainRequestOptions) !void {
    if (value.auto_accept_shared_associations) |v| {
        try buf.appendSlice(allocator, "<AutoAcceptSharedAssociations>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AutoAcceptSharedAssociations>");
    }
    if (value.igmpv_2_support) |v| {
        try buf.appendSlice(allocator, "<Igmpv2Support>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Igmpv2Support>");
    }
    if (value.static_sources_support) |v| {
        try buf.appendSlice(allocator, "<StaticSourcesSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</StaticSourcesSupport>");
    }
}

pub fn serializeCreateTransitGatewayPeeringAttachmentRequestOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayPeeringAttachmentRequestOptions) !void {
    if (value.dynamic_routing) |v| {
        try buf.appendSlice(allocator, "<DynamicRouting>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DynamicRouting>");
    }
}

pub fn serializeCreateTransitGatewayVpcAttachmentRequestOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayVpcAttachmentRequestOptions) !void {
    if (value.appliance_mode_support) |v| {
        try buf.appendSlice(allocator, "<ApplianceModeSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ApplianceModeSupport>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(allocator, "<DnsSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DnsSupport>");
    }
    if (value.ipv_6_support) |v| {
        try buf.appendSlice(allocator, "<Ipv6Support>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Ipv6Support>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</SecurityGroupReferencingSupport>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointCidrOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointCidrOptions) !void {
    if (value.cidr) |v| {
        try buf.appendSlice(allocator, "<Cidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Cidr>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(allocator, "<PortRange>");
        try serializeCreateVerifiedAccessEndpointPortRangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(allocator, "<Protocol>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Protocol>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try serializeCreateVerifiedAccessEndpointSubnetIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointEniOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointEniOptions) !void {
    if (value.network_interface_id) |v| {
        try buf.appendSlice(allocator, "<NetworkInterfaceId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</NetworkInterfaceId>");
    }
    if (value.port) |v| {
        try buf.appendSlice(allocator, "<Port>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(allocator, "<PortRange>");
        try serializeCreateVerifiedAccessEndpointPortRangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(allocator, "<Protocol>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Protocol>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointLoadBalancerOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointLoadBalancerOptions) !void {
    if (value.load_balancer_arn) |v| {
        try buf.appendSlice(allocator, "<LoadBalancerArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LoadBalancerArn>");
    }
    if (value.port) |v| {
        try buf.appendSlice(allocator, "<Port>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(allocator, "<PortRange>");
        try serializeCreateVerifiedAccessEndpointPortRangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(allocator, "<Protocol>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Protocol>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try serializeCreateVerifiedAccessEndpointSubnetIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointPortRange(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointPortRange) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(allocator, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(allocator, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ToPort>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointRdsOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointRdsOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(allocator, "<Port>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Port>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(allocator, "<Protocol>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Protocol>");
    }
    if (value.rds_db_cluster_arn) |v| {
        try buf.appendSlice(allocator, "<RdsDbClusterArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RdsDbClusterArn>");
    }
    if (value.rds_db_instance_arn) |v| {
        try buf.appendSlice(allocator, "<RdsDbInstanceArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RdsDbInstanceArn>");
    }
    if (value.rds_db_proxy_arn) |v| {
        try buf.appendSlice(allocator, "<RdsDbProxyArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RdsDbProxyArn>");
    }
    if (value.rds_endpoint) |v| {
        try buf.appendSlice(allocator, "<RdsEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RdsEndpoint>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try serializeCreateVerifiedAccessEndpointSubnetIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeCreateVerifiedAccessNativeApplicationOidcOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessNativeApplicationOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(allocator, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(allocator, "<ClientId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(allocator, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(allocator, "<Issuer>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Issuer>");
    }
    if (value.public_signing_key_endpoint) |v| {
        try buf.appendSlice(allocator, "<PublicSigningKeyEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PublicSigningKeyEndpoint>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(allocator, "<Scope>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(allocator, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(allocator, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserInfoEndpoint>");
    }
}

pub fn serializeCreateVerifiedAccessTrustProviderDeviceOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessTrustProviderDeviceOptions) !void {
    if (value.public_signing_key_url) |v| {
        try buf.appendSlice(allocator, "<PublicSigningKeyUrl>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PublicSigningKeyUrl>");
    }
    if (value.tenant_id) |v| {
        try buf.appendSlice(allocator, "<TenantId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TenantId>");
    }
}

pub fn serializeCreateVerifiedAccessTrustProviderOidcOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessTrustProviderOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(allocator, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(allocator, "<ClientId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(allocator, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(allocator, "<Issuer>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Issuer>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(allocator, "<Scope>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(allocator, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(allocator, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserInfoEndpoint>");
    }
}

pub fn serializeCreateVolumePermission(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVolumePermission) !void {
    if (value.group) |v| {
        try buf.appendSlice(allocator, "<group>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</group>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(allocator, "<userId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</userId>");
    }
}

pub fn serializeCreateVolumePermissionModifications(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVolumePermissionModifications) !void {
    if (value.add) |v| {
        try buf.appendSlice(allocator, "<Add>");
        try serializeCreateVolumePermissionList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Add>");
    }
    if (value.remove) |v| {
        try buf.appendSlice(allocator, "<Remove>");
        try serializeCreateVolumePermissionList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Remove>");
    }
}

pub fn serializeCreationDateConditionRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreationDateConditionRequest) !void {
    if (value.maximum_days_since_created) |v| {
        try buf.appendSlice(allocator, "<MaximumDaysSinceCreated>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</MaximumDaysSinceCreated>");
    }
}

pub fn serializeCreditSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreditSpecificationRequest) !void {
    try buf.appendSlice(allocator, "<CpuCredits>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.cpu_credits);
    try buf.appendSlice(allocator, "</CpuCredits>");
}

pub fn serializeDataQuery(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DataQuery) !void {
    if (value.destination) |v| {
        try buf.appendSlice(allocator, "<Destination>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Destination>");
    }
    if (value.id) |v| {
        try buf.appendSlice(allocator, "<Id>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Id>");
    }
    if (value.metric) |v| {
        try buf.appendSlice(allocator, "<Metric>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Metric>");
    }
    if (value.period) |v| {
        try buf.appendSlice(allocator, "<Period>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Period>");
    }
    if (value.source) |v| {
        try buf.appendSlice(allocator, "<Source>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Source>");
    }
    if (value.statistic) |v| {
        try buf.appendSlice(allocator, "<Statistic>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Statistic>");
    }
}

pub fn serializeDeprecationTimeConditionRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DeprecationTimeConditionRequest) !void {
    if (value.maximum_days_since_deprecated) |v| {
        try buf.appendSlice(allocator, "<MaximumDaysSinceDeprecated>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</MaximumDaysSinceDeprecated>");
    }
}

pub fn serializeDeregisterInstanceTagAttributeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DeregisterInstanceTagAttributeRequest) !void {
    if (value.include_all_tags_of_instance) |v| {
        try buf.appendSlice(allocator, "<IncludeAllTagsOfInstance>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</IncludeAllTagsOfInstance>");
    }
    if (value.instance_tag_keys) |v| {
        try buf.appendSlice(allocator, "<InstanceTagKey>");
        try serializeInstanceTagKeySet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</InstanceTagKey>");
    }
}

pub fn serializeDestinationOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DestinationOptionsRequest) !void {
    if (value.file_format) |v| {
        try buf.appendSlice(allocator, "<FileFormat>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</FileFormat>");
    }
    if (value.hive_compatible_partitions) |v| {
        try buf.appendSlice(allocator, "<HiveCompatiblePartitions>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</HiveCompatiblePartitions>");
    }
    if (value.per_hour_partition) |v| {
        try buf.appendSlice(allocator, "<PerHourPartition>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</PerHourPartition>");
    }
}

pub fn serializeDimensionCondition(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DimensionCondition) !void {
    if (value.comparison) |v| {
        try buf.appendSlice(allocator, "<Comparison>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Comparison>");
    }
    if (value.dimension) |v| {
        try buf.appendSlice(allocator, "<Dimension>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Dimension>");
    }
    if (value.values) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try serializeConditionValueList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializeDirectoryServiceAuthenticationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DirectoryServiceAuthenticationRequest) !void {
    if (value.directory_id) |v| {
        try buf.appendSlice(allocator, "<DirectoryId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DirectoryId>");
    }
}

pub fn serializeDiskImage(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DiskImage) !void {
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
    if (value.image) |v| {
        try buf.appendSlice(allocator, "<Image>");
        try serializeDiskImageDetail(allocator, buf, v);
        try buf.appendSlice(allocator, "</Image>");
    }
    if (value.volume) |v| {
        try buf.appendSlice(allocator, "<Volume>");
        try serializeVolumeDetail(allocator, buf, v);
        try buf.appendSlice(allocator, "</Volume>");
    }
}

pub fn serializeDiskImageDetail(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DiskImageDetail) !void {
    try buf.appendSlice(allocator, "<bytes>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.bytes}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</bytes>");
    try buf.appendSlice(allocator, "<format>");
    try buf.appendSlice(allocator, value.format.wireName());
    try buf.appendSlice(allocator, "</format>");
    try buf.appendSlice(allocator, "<importManifestUrl>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.import_manifest_url);
    try buf.appendSlice(allocator, "</importManifestUrl>");
}

pub fn serializeDnsOptionsSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DnsOptionsSpecification) !void {
    if (value.dns_record_ip_type) |v| {
        try buf.appendSlice(allocator, "<DnsRecordIpType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DnsRecordIpType>");
    }
    if (value.private_dns_only_for_inbound_resolver_endpoint) |v| {
        try buf.appendSlice(allocator, "<PrivateDnsOnlyForInboundResolverEndpoint>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</PrivateDnsOnlyForInboundResolverEndpoint>");
    }
    if (value.private_dns_preference) |v| {
        try buf.appendSlice(allocator, "<PrivateDnsPreference>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PrivateDnsPreference>");
    }
    if (value.private_dns_specified_domains) |v| {
        try buf.appendSlice(allocator, "<PrivateDnsSpecifiedDomain>");
        try serializePrivateDnsSpecifiedDomainSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PrivateDnsSpecifiedDomain>");
    }
}

pub fn serializeDnsServersOptionsModifyStructure(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: DnsServersOptionsModifyStructure) !void {
    if (value.custom_dns_servers) |v| {
        try buf.appendSlice(allocator, "<CustomDnsServers>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</CustomDnsServers>");
    }
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
}

pub fn serializeEbsBlockDevice(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: EbsBlockDevice) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZoneId>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<deleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</deleteOnTermination>");
    }
    if (value.ebs_card_index) |v| {
        try buf.appendSlice(allocator, "<EbsCardIndex>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</EbsCardIndex>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(allocator, "<encrypted>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(allocator, "<iops>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</iops>");
    }
    if (value.kms_key_id) |v| {
        try buf.appendSlice(allocator, "<kmsKeyId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</kmsKeyId>");
    }
    if (value.outpost_arn) |v| {
        try buf.appendSlice(allocator, "<outpostArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</outpostArn>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(allocator, "<snapshotId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</snapshotId>");
    }
    if (value.throughput) |v| {
        try buf.appendSlice(allocator, "<throughput>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</throughput>");
    }
    if (value.volume_initialization_rate) |v| {
        try buf.appendSlice(allocator, "<VolumeInitializationRate>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</VolumeInitializationRate>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(allocator, "<volumeSize>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</volumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(allocator, "<volumeType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</volumeType>");
    }
}

pub fn serializeEbsInstanceBlockDeviceSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: EbsInstanceBlockDeviceSpecification) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<deleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</deleteOnTermination>");
    }
    if (value.volume_id) |v| {
        try buf.appendSlice(allocator, "<volumeId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</volumeId>");
    }
}

pub fn serializeElasticGpuSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ElasticGpuSpecification) !void {
    try buf.appendSlice(allocator, "<Type>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.@"type");
    try buf.appendSlice(allocator, "</Type>");
}

pub fn serializeElasticInferenceAccelerator(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ElasticInferenceAccelerator) !void {
    if (value.count) |v| {
        try buf.appendSlice(allocator, "<Count>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Count>");
    }
    try buf.appendSlice(allocator, "<Type>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.@"type");
    try buf.appendSlice(allocator, "</Type>");
}

pub fn serializeEnaSrdSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdSpecification) !void {
    if (value.ena_srd_enabled) |v| {
        try buf.appendSlice(allocator, "<EnaSrdEnabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnaSrdEnabled>");
    }
    if (value.ena_srd_udp_specification) |v| {
        try buf.appendSlice(allocator, "<EnaSrdUdpSpecification>");
        try serializeEnaSrdUdpSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</EnaSrdUdpSpecification>");
    }
}

pub fn serializeEnaSrdSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdSpecificationRequest) !void {
    if (value.ena_srd_enabled) |v| {
        try buf.appendSlice(allocator, "<EnaSrdEnabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnaSrdEnabled>");
    }
    if (value.ena_srd_udp_specification) |v| {
        try buf.appendSlice(allocator, "<EnaSrdUdpSpecification>");
        try serializeEnaSrdUdpSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</EnaSrdUdpSpecification>");
    }
}

pub fn serializeEnaSrdUdpSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdUdpSpecification) !void {
    if (value.ena_srd_udp_enabled) |v| {
        try buf.appendSlice(allocator, "<EnaSrdUdpEnabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnaSrdUdpEnabled>");
    }
}

pub fn serializeEnaSrdUdpSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdUdpSpecificationRequest) !void {
    if (value.ena_srd_udp_enabled) |v| {
        try buf.appendSlice(allocator, "<EnaSrdUdpEnabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnaSrdUdpEnabled>");
    }
}

pub fn serializeEnclaveOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnclaveOptionsRequest) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
}

pub fn serializeExportTaskS3LocationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ExportTaskS3LocationRequest) !void {
    try buf.appendSlice(allocator, "<S3Bucket>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.s3_bucket);
    try buf.appendSlice(allocator, "</S3Bucket>");
    if (value.s3_prefix) |v| {
        try buf.appendSlice(allocator, "<S3Prefix>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</S3Prefix>");
    }
}

pub fn serializeExportToS3TaskSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ExportToS3TaskSpecification) !void {
    if (value.container_format) |v| {
        try buf.appendSlice(allocator, "<containerFormat>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</containerFormat>");
    }
    if (value.disk_image_format) |v| {
        try buf.appendSlice(allocator, "<diskImageFormat>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</diskImageFormat>");
    }
    if (value.s3_bucket) |v| {
        try buf.appendSlice(allocator, "<s3Bucket>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</s3Bucket>");
    }
    if (value.s3_prefix) |v| {
        try buf.appendSlice(allocator, "<s3Prefix>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</s3Prefix>");
    }
}

pub fn serializeExternalAuthorityConfiguration(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ExternalAuthorityConfiguration) !void {
    if (value.external_resource_identifier) |v| {
        try buf.appendSlice(allocator, "<ExternalResourceIdentifier>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ExternalResourceIdentifier>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(allocator, "<Type>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Type>");
    }
}

pub fn serializeFastLaunchLaunchTemplateSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FastLaunchLaunchTemplateSpecificationRequest) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(allocator, "<LaunchTemplateId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LaunchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(allocator, "<LaunchTemplateName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LaunchTemplateName>");
    }
    try buf.appendSlice(allocator, "<Version>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.version);
    try buf.appendSlice(allocator, "</Version>");
}

pub fn serializeFastLaunchSnapshotConfigurationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FastLaunchSnapshotConfigurationRequest) !void {
    if (value.target_resource_count) |v| {
        try buf.appendSlice(allocator, "<TargetResourceCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</TargetResourceCount>");
    }
}

pub fn serializeFederatedAuthenticationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FederatedAuthenticationRequest) !void {
    if (value.saml_provider_arn) |v| {
        try buf.appendSlice(allocator, "<SAMLProviderArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SAMLProviderArn>");
    }
    if (value.self_service_saml_provider_arn) |v| {
        try buf.appendSlice(allocator, "<SelfServiceSAMLProviderArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SelfServiceSAMLProviderArn>");
    }
}

pub fn serializeFilter(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Filter) !void {
    if (value.name) |v| {
        try buf.appendSlice(allocator, "<Name>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Name>");
    }
    if (value.values) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializeFleetBlockDeviceMappingRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetBlockDeviceMappingRequest) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(allocator, "<DeviceName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DeviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(allocator, "<Ebs>");
        try serializeFleetEbsBlockDeviceRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(allocator, "<NoDevice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</NoDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(allocator, "<VirtualName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</VirtualName>");
    }
}

pub fn serializeFleetEbsBlockDeviceRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetEbsBlockDeviceRequest) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<DeleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DeleteOnTermination>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(allocator, "<Encrypted>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(allocator, "<Iops>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Iops>");
    }
    if (value.kms_key_id) |v| {
        try buf.appendSlice(allocator, "<KmsKeyId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</KmsKeyId>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(allocator, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SnapshotId>");
    }
    if (value.throughput) |v| {
        try buf.appendSlice(allocator, "<Throughput>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Throughput>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(allocator, "<VolumeSize>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</VolumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(allocator, "<VolumeType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</VolumeType>");
    }
}

pub fn serializeFleetLaunchTemplateConfigRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateConfigRequest) !void {
    if (value.launch_template_specification) |v| {
        try buf.appendSlice(allocator, "<LaunchTemplateSpecification>");
        try serializeFleetLaunchTemplateSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</LaunchTemplateSpecification>");
    }
    if (value.overrides) |v| {
        try buf.appendSlice(allocator, "<Overrides>");
        try serializeFleetLaunchTemplateOverridesListRequest(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Overrides>");
    }
}

pub fn serializeFleetLaunchTemplateOverridesRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateOverridesRequest) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZoneId>");
    }
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(allocator, "<BlockDeviceMapping>");
        try serializeFleetBlockDeviceMappingRequestList(allocator, buf, v, "BlockDeviceMapping");
        try buf.appendSlice(allocator, "</BlockDeviceMapping>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(allocator, "<ImageId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ImageId>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(allocator, "<InstanceRequirements>");
        try serializeInstanceRequirementsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</InstanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<InstanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceType>");
    }
    if (value.max_price) |v| {
        try buf.appendSlice(allocator, "<MaxPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</MaxPrice>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(allocator, "<Placement>");
        try serializePlacement(allocator, buf, v);
        try buf.appendSlice(allocator, "</Placement>");
    }
    if (value.priority) |v| {
        try buf.appendSlice(allocator, "<Priority>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Priority>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SubnetId>");
    }
    if (value.weighted_capacity) |v| {
        try buf.appendSlice(allocator, "<WeightedCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</WeightedCapacity>");
    }
}

pub fn serializeFleetLaunchTemplateSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateSpecification) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(allocator, "<launchTemplateId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</launchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(allocator, "<launchTemplateName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</launchTemplateName>");
    }
    if (value.version) |v| {
        try buf.appendSlice(allocator, "<version>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</version>");
    }
}

pub fn serializeFleetLaunchTemplateSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateSpecificationRequest) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(allocator, "<LaunchTemplateId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LaunchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(allocator, "<LaunchTemplateName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LaunchTemplateName>");
    }
    if (value.version) |v| {
        try buf.appendSlice(allocator, "<Version>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Version>");
    }
}

pub fn serializeFleetSpotCapacityRebalanceRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetSpotCapacityRebalanceRequest) !void {
    if (value.replacement_strategy) |v| {
        try buf.appendSlice(allocator, "<ReplacementStrategy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ReplacementStrategy>");
    }
    if (value.termination_delay) |v| {
        try buf.appendSlice(allocator, "<TerminationDelay>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</TerminationDelay>");
    }
}

pub fn serializeFleetSpotMaintenanceStrategiesRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetSpotMaintenanceStrategiesRequest) !void {
    if (value.capacity_rebalance) |v| {
        try buf.appendSlice(allocator, "<CapacityRebalance>");
        try serializeFleetSpotCapacityRebalanceRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</CapacityRebalance>");
    }
}

pub fn serializeGroupIdentifier(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: GroupIdentifier) !void {
    if (value.group_id) |v| {
        try buf.appendSlice(allocator, "<groupId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</groupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(allocator, "<groupName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</groupName>");
    }
}

pub fn serializeHibernationOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: HibernationOptionsRequest) !void {
    if (value.configured) |v| {
        try buf.appendSlice(allocator, "<Configured>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Configured>");
    }
}

pub fn serializeIKEVersionsRequestListValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IKEVersionsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializeIamInstanceProfileSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IamInstanceProfileSpecification) !void {
    if (value.arn) |v| {
        try buf.appendSlice(allocator, "<arn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</arn>");
    }
    if (value.name) |v| {
        try buf.appendSlice(allocator, "<name>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</name>");
    }
}

pub fn serializeIcmpTypeCode(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IcmpTypeCode) !void {
    if (value.code) |v| {
        try buf.appendSlice(allocator, "<code>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</code>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(allocator, "<type>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</type>");
    }
}

pub fn serializeImageCriterionRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageCriterionRequest) !void {
    if (value.creation_date_condition) |v| {
        try buf.appendSlice(allocator, "<CreationDateCondition>");
        try serializeCreationDateConditionRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</CreationDateCondition>");
    }
    if (value.deprecation_time_condition) |v| {
        try buf.appendSlice(allocator, "<DeprecationTimeCondition>");
        try serializeDeprecationTimeConditionRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</DeprecationTimeCondition>");
    }
    if (value.image_names) |v| {
        try buf.appendSlice(allocator, "<ImageName>");
        try serializeImageNameRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ImageName>");
    }
    if (value.image_providers) |v| {
        try buf.appendSlice(allocator, "<ImageProvider>");
        try serializeImageProviderRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ImageProvider>");
    }
    if (value.marketplace_product_codes) |v| {
        try buf.appendSlice(allocator, "<MarketplaceProductCode>");
        try serializeMarketplaceProductCodeRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</MarketplaceProductCode>");
    }
}

pub fn serializeImageDiskContainer(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageDiskContainer) !void {
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
    if (value.device_name) |v| {
        try buf.appendSlice(allocator, "<DeviceName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DeviceName>");
    }
    if (value.format) |v| {
        try buf.appendSlice(allocator, "<Format>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Format>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(allocator, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SnapshotId>");
    }
    if (value.url) |v| {
        try buf.appendSlice(allocator, "<Url>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Url>");
    }
    if (value.user_bucket) |v| {
        try buf.appendSlice(allocator, "<UserBucket>");
        try serializeUserBucket(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserBucket>");
    }
}

pub fn serializeImageUsageResourceTypeOptionRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageUsageResourceTypeOptionRequest) !void {
    if (value.option_name) |v| {
        try buf.appendSlice(allocator, "<OptionName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</OptionName>");
    }
    if (value.option_values) |v| {
        try buf.appendSlice(allocator, "<OptionValue>");
        try serializeImageUsageResourceTypeOptionValuesList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</OptionValue>");
    }
}

pub fn serializeImageUsageResourceTypeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageUsageResourceTypeRequest) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(allocator, "<ResourceType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceType>");
    }
    if (value.resource_type_options) |v| {
        try buf.appendSlice(allocator, "<ResourceTypeOption>");
        try serializeImageUsageResourceTypeOptionRequestList(allocator, buf, v, "member");
        try buf.appendSlice(allocator, "</ResourceTypeOption>");
    }
}

pub fn serializeImportImageLicenseConfigurationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImportImageLicenseConfigurationRequest) !void {
    if (value.license_configuration_arn) |v| {
        try buf.appendSlice(allocator, "<LicenseConfigurationArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LicenseConfigurationArn>");
    }
}

pub fn serializeImportInstanceLaunchSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImportInstanceLaunchSpecification) !void {
    if (value.additional_info) |v| {
        try buf.appendSlice(allocator, "<additionalInfo>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</additionalInfo>");
    }
    if (value.architecture) |v| {
        try buf.appendSlice(allocator, "<architecture>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</architecture>");
    }
    if (value.group_ids) |v| {
        try buf.appendSlice(allocator, "<GroupId>");
        try serializeSecurityGroupIdStringList(allocator, buf, v, "SecurityGroupId");
        try buf.appendSlice(allocator, "</GroupId>");
    }
    if (value.group_names) |v| {
        try buf.appendSlice(allocator, "<GroupName>");
        try serializeSecurityGroupStringList(allocator, buf, v, "SecurityGroup");
        try buf.appendSlice(allocator, "</GroupName>");
    }
    if (value.instance_initiated_shutdown_behavior) |v| {
        try buf.appendSlice(allocator, "<instanceInitiatedShutdownBehavior>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</instanceInitiatedShutdownBehavior>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<instanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</instanceType>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(allocator, "<monitoring>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</monitoring>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(allocator, "<placement>");
        try serializePlacement(allocator, buf, v);
        try buf.appendSlice(allocator, "</placement>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(allocator, "<privateIpAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</privateIpAddress>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<subnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</subnetId>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(allocator, "<userData>");
        try serializeUserData(allocator, buf, v);
        try buf.appendSlice(allocator, "</userData>");
    }
}

pub fn serializeInstanceBlockDeviceMappingSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceBlockDeviceMappingSpecification) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(allocator, "<deviceName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</deviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(allocator, "<ebs>");
        try serializeEbsInstanceBlockDeviceSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(allocator, "<noDevice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</noDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(allocator, "<virtualName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</virtualName>");
    }
}

pub fn serializeInstanceCreditSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceCreditSpecificationRequest) !void {
    if (value.cpu_credits) |v| {
        try buf.appendSlice(allocator, "<CpuCredits>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</CpuCredits>");
    }
    try buf.appendSlice(allocator, "<InstanceId>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.instance_id);
    try buf.appendSlice(allocator, "</InstanceId>");
}

pub fn serializeInstanceEventWindowAssociationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceEventWindowAssociationRequest) !void {
    if (value.dedicated_host_ids) |v| {
        try buf.appendSlice(allocator, "<DedicatedHostId>");
        try serializeDedicatedHostIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</DedicatedHostId>");
    }
    if (value.instance_ids) |v| {
        try buf.appendSlice(allocator, "<InstanceId>");
        try serializeInstanceIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</InstanceId>");
    }
    if (value.instance_tags) |v| {
        try buf.appendSlice(allocator, "<InstanceTag>");
        try serializeTagList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</InstanceTag>");
    }
}

pub fn serializeInstanceEventWindowDisassociationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceEventWindowDisassociationRequest) !void {
    if (value.dedicated_host_ids) |v| {
        try buf.appendSlice(allocator, "<DedicatedHostId>");
        try serializeDedicatedHostIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</DedicatedHostId>");
    }
    if (value.instance_ids) |v| {
        try buf.appendSlice(allocator, "<InstanceId>");
        try serializeInstanceIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</InstanceId>");
    }
    if (value.instance_tags) |v| {
        try buf.appendSlice(allocator, "<InstanceTag>");
        try serializeTagList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</InstanceTag>");
    }
}

pub fn serializeInstanceEventWindowTimeRangeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceEventWindowTimeRangeRequest) !void {
    if (value.end_hour) |v| {
        try buf.appendSlice(allocator, "<EndHour>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</EndHour>");
    }
    if (value.end_week_day) |v| {
        try buf.appendSlice(allocator, "<EndWeekDay>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</EndWeekDay>");
    }
    if (value.start_hour) |v| {
        try buf.appendSlice(allocator, "<StartHour>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</StartHour>");
    }
    if (value.start_week_day) |v| {
        try buf.appendSlice(allocator, "<StartWeekDay>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</StartWeekDay>");
    }
}

pub fn serializeInstanceIpv6Address(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceIpv6Address) !void {
    if (value.ipv_6_address) |v| {
        try buf.appendSlice(allocator, "<ipv6Address>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ipv6Address>");
    }
    if (value.is_primary_ipv_6) |v| {
        try buf.appendSlice(allocator, "<isPrimaryIpv6>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</isPrimaryIpv6>");
    }
}

pub fn serializeInstanceIpv6AddressRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceIpv6AddressRequest) !void {
    if (value.ipv_6_address) |v| {
        try buf.appendSlice(allocator, "<Ipv6Address>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ipv6Address>");
    }
}

pub fn serializeInstanceMaintenanceOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceMaintenanceOptionsRequest) !void {
    if (value.auto_recovery) |v| {
        try buf.appendSlice(allocator, "<AutoRecovery>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AutoRecovery>");
    }
}

pub fn serializeInstanceMarketOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceMarketOptionsRequest) !void {
    if (value.market_type) |v| {
        try buf.appendSlice(allocator, "<MarketType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</MarketType>");
    }
    if (value.spot_options) |v| {
        try buf.appendSlice(allocator, "<SpotOptions>");
        try serializeSpotMarketOptions(allocator, buf, v);
        try buf.appendSlice(allocator, "</SpotOptions>");
    }
}

pub fn serializeInstanceMetadataOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceMetadataOptionsRequest) !void {
    if (value.http_endpoint) |v| {
        try buf.appendSlice(allocator, "<HttpEndpoint>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HttpEndpoint>");
    }
    if (value.http_protocol_ipv_6) |v| {
        try buf.appendSlice(allocator, "<HttpProtocolIpv6>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HttpProtocolIpv6>");
    }
    if (value.http_put_response_hop_limit) |v| {
        try buf.appendSlice(allocator, "<HttpPutResponseHopLimit>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</HttpPutResponseHopLimit>");
    }
    if (value.http_tokens) |v| {
        try buf.appendSlice(allocator, "<HttpTokens>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HttpTokens>");
    }
    if (value.instance_metadata_tags) |v| {
        try buf.appendSlice(allocator, "<InstanceMetadataTags>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceMetadataTags>");
    }
}

pub fn serializeInstanceNetworkInterfaceSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceNetworkInterfaceSpecification) !void {
    if (value.associate_carrier_ip_address) |v| {
        try buf.appendSlice(allocator, "<AssociateCarrierIpAddress>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</AssociateCarrierIpAddress>");
    }
    if (value.associate_public_ip_address) |v| {
        try buf.appendSlice(allocator, "<associatePublicIpAddress>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</associatePublicIpAddress>");
    }
    if (value.connection_tracking_specification) |v| {
        try buf.appendSlice(allocator, "<ConnectionTrackingSpecification>");
        try serializeConnectionTrackingSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</ConnectionTrackingSpecification>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<deleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</deleteOnTermination>");
    }
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</description>");
    }
    if (value.device_index) |v| {
        try buf.appendSlice(allocator, "<deviceIndex>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</deviceIndex>");
    }
    if (value.ena_queue_count) |v| {
        try buf.appendSlice(allocator, "<EnaQueueCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</EnaQueueCount>");
    }
    if (value.ena_srd_specification) |v| {
        try buf.appendSlice(allocator, "<EnaSrdSpecification>");
        try serializeEnaSrdSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</EnaSrdSpecification>");
    }
    if (value.groups) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupId>");
        try serializeSecurityGroupIdStringList(allocator, buf, v, "SecurityGroupId");
        try buf.appendSlice(allocator, "</SecurityGroupId>");
    }
    if (value.interface_type) |v| {
        try buf.appendSlice(allocator, "<InterfaceType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</InterfaceType>");
    }
    if (value.ipv_4_prefix_count) |v| {
        try buf.appendSlice(allocator, "<Ipv4PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Ipv4PrefixCount>");
    }
    if (value.ipv_4_prefixes) |v| {
        try buf.appendSlice(allocator, "<Ipv4Prefix>");
        try serializeIpv4PrefixList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Ipv4Prefix>");
    }
    if (value.ipv_6_address_count) |v| {
        try buf.appendSlice(allocator, "<ipv6AddressCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ipv6AddressCount>");
    }
    if (value.ipv_6_addresses) |v| {
        try buf.appendSlice(allocator, "<ipv6AddressesSet>");
        try serializeInstanceIpv6AddressList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ipv6AddressesSet>");
    }
    if (value.ipv_6_prefix_count) |v| {
        try buf.appendSlice(allocator, "<Ipv6PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Ipv6PrefixCount>");
    }
    if (value.ipv_6_prefixes) |v| {
        try buf.appendSlice(allocator, "<Ipv6Prefix>");
        try serializeIpv6PrefixList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Ipv6Prefix>");
    }
    if (value.network_card_index) |v| {
        try buf.appendSlice(allocator, "<NetworkCardIndex>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</NetworkCardIndex>");
    }
    if (value.network_interface_id) |v| {
        try buf.appendSlice(allocator, "<networkInterfaceId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</networkInterfaceId>");
    }
    if (value.primary_ipv_6) |v| {
        try buf.appendSlice(allocator, "<PrimaryIpv6>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</PrimaryIpv6>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(allocator, "<privateIpAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</privateIpAddress>");
    }
    if (value.private_ip_addresses) |v| {
        try buf.appendSlice(allocator, "<privateIpAddressesSet>");
        try serializePrivateIpAddressSpecificationList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</privateIpAddressesSet>");
    }
    if (value.secondary_private_ip_address_count) |v| {
        try buf.appendSlice(allocator, "<secondaryPrivateIpAddressCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</secondaryPrivateIpAddressCount>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<subnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</subnetId>");
    }
}

pub fn serializeInstanceNetworkPerformanceOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceNetworkPerformanceOptionsRequest) !void {
    if (value.bandwidth_weighting) |v| {
        try buf.appendSlice(allocator, "<BandwidthWeighting>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</BandwidthWeighting>");
    }
}

pub fn serializeInstanceRequirements(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceRequirements) !void {
    if (value.accelerator_count) |v| {
        try buf.appendSlice(allocator, "<acceleratorCount>");
        try serializeAcceleratorCount(allocator, buf, v);
        try buf.appendSlice(allocator, "</acceleratorCount>");
    }
    if (value.accelerator_manufacturers) |v| {
        try buf.appendSlice(allocator, "<acceleratorManufacturerSet>");
        try serializeAcceleratorManufacturerSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</acceleratorManufacturerSet>");
    }
    if (value.accelerator_names) |v| {
        try buf.appendSlice(allocator, "<acceleratorNameSet>");
        try serializeAcceleratorNameSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</acceleratorNameSet>");
    }
    if (value.accelerator_total_memory_mi_b) |v| {
        try buf.appendSlice(allocator, "<acceleratorTotalMemoryMiB>");
        try serializeAcceleratorTotalMemoryMiB(allocator, buf, v);
        try buf.appendSlice(allocator, "</acceleratorTotalMemoryMiB>");
    }
    if (value.accelerator_types) |v| {
        try buf.appendSlice(allocator, "<acceleratorTypeSet>");
        try serializeAcceleratorTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</acceleratorTypeSet>");
    }
    if (value.allowed_instance_types) |v| {
        try buf.appendSlice(allocator, "<allowedInstanceTypeSet>");
        try serializeAllowedInstanceTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</allowedInstanceTypeSet>");
    }
    if (value.bare_metal) |v| {
        try buf.appendSlice(allocator, "<bareMetal>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</bareMetal>");
    }
    if (value.baseline_ebs_bandwidth_mbps) |v| {
        try buf.appendSlice(allocator, "<baselineEbsBandwidthMbps>");
        try serializeBaselineEbsBandwidthMbps(allocator, buf, v);
        try buf.appendSlice(allocator, "</baselineEbsBandwidthMbps>");
    }
    if (value.baseline_performance_factors) |v| {
        try buf.appendSlice(allocator, "<baselinePerformanceFactors>");
        try serializeBaselinePerformanceFactors(allocator, buf, v);
        try buf.appendSlice(allocator, "</baselinePerformanceFactors>");
    }
    if (value.burstable_performance) |v| {
        try buf.appendSlice(allocator, "<burstablePerformance>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</burstablePerformance>");
    }
    if (value.cpu_manufacturers) |v| {
        try buf.appendSlice(allocator, "<cpuManufacturerSet>");
        try serializeCpuManufacturerSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</cpuManufacturerSet>");
    }
    if (value.excluded_instance_types) |v| {
        try buf.appendSlice(allocator, "<excludedInstanceTypeSet>");
        try serializeExcludedInstanceTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</excludedInstanceTypeSet>");
    }
    if (value.instance_generations) |v| {
        try buf.appendSlice(allocator, "<instanceGenerationSet>");
        try serializeInstanceGenerationSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</instanceGenerationSet>");
    }
    if (value.local_storage) |v| {
        try buf.appendSlice(allocator, "<localStorage>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</localStorage>");
    }
    if (value.local_storage_types) |v| {
        try buf.appendSlice(allocator, "<localStorageTypeSet>");
        try serializeLocalStorageTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</localStorageTypeSet>");
    }
    if (value.max_spot_price_as_percentage_of_optimal_on_demand_price) |v| {
        try buf.appendSlice(allocator, "<maxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</maxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
    }
    if (value.memory_gi_b_per_v_cpu) |v| {
        try buf.appendSlice(allocator, "<memoryGiBPerVCpu>");
        try serializeMemoryGiBPerVCpu(allocator, buf, v);
        try buf.appendSlice(allocator, "</memoryGiBPerVCpu>");
    }
    if (value.memory_mi_b) |v| {
        try buf.appendSlice(allocator, "<memoryMiB>");
        try serializeMemoryMiB(allocator, buf, v);
        try buf.appendSlice(allocator, "</memoryMiB>");
    }
    if (value.network_bandwidth_gbps) |v| {
        try buf.appendSlice(allocator, "<networkBandwidthGbps>");
        try serializeNetworkBandwidthGbps(allocator, buf, v);
        try buf.appendSlice(allocator, "</networkBandwidthGbps>");
    }
    if (value.network_interface_count) |v| {
        try buf.appendSlice(allocator, "<networkInterfaceCount>");
        try serializeNetworkInterfaceCount(allocator, buf, v);
        try buf.appendSlice(allocator, "</networkInterfaceCount>");
    }
    if (value.on_demand_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(allocator, "<onDemandMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</onDemandMaxPricePercentageOverLowestPrice>");
    }
    if (value.require_encryption_in_transit) |v| {
        try buf.appendSlice(allocator, "<requireEncryptionInTransit>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</requireEncryptionInTransit>");
    }
    if (value.require_hibernate_support) |v| {
        try buf.appendSlice(allocator, "<requireHibernateSupport>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</requireHibernateSupport>");
    }
    if (value.spot_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(allocator, "<spotMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</spotMaxPricePercentageOverLowestPrice>");
    }
    if (value.total_local_storage_gb) |v| {
        try buf.appendSlice(allocator, "<totalLocalStorageGB>");
        try serializeTotalLocalStorageGB(allocator, buf, v);
        try buf.appendSlice(allocator, "</totalLocalStorageGB>");
    }
    if (value.v_cpu_count) |v| {
        try buf.appendSlice(allocator, "<vCpuCount>");
        try serializeVCpuCountRange(allocator, buf, v);
        try buf.appendSlice(allocator, "</vCpuCount>");
    }
}

pub fn serializeInstanceRequirementsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceRequirementsRequest) !void {
    if (value.accelerator_count) |v| {
        try buf.appendSlice(allocator, "<AcceleratorCount>");
        try serializeAcceleratorCountRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</AcceleratorCount>");
    }
    if (value.accelerator_manufacturers) |v| {
        try buf.appendSlice(allocator, "<AcceleratorManufacturer>");
        try serializeAcceleratorManufacturerSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</AcceleratorManufacturer>");
    }
    if (value.accelerator_names) |v| {
        try buf.appendSlice(allocator, "<AcceleratorName>");
        try serializeAcceleratorNameSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</AcceleratorName>");
    }
    if (value.accelerator_total_memory_mi_b) |v| {
        try buf.appendSlice(allocator, "<AcceleratorTotalMemoryMiB>");
        try serializeAcceleratorTotalMemoryMiBRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</AcceleratorTotalMemoryMiB>");
    }
    if (value.accelerator_types) |v| {
        try buf.appendSlice(allocator, "<AcceleratorType>");
        try serializeAcceleratorTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</AcceleratorType>");
    }
    if (value.allowed_instance_types) |v| {
        try buf.appendSlice(allocator, "<AllowedInstanceType>");
        try serializeAllowedInstanceTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</AllowedInstanceType>");
    }
    if (value.bare_metal) |v| {
        try buf.appendSlice(allocator, "<BareMetal>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</BareMetal>");
    }
    if (value.baseline_ebs_bandwidth_mbps) |v| {
        try buf.appendSlice(allocator, "<BaselineEbsBandwidthMbps>");
        try serializeBaselineEbsBandwidthMbpsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</BaselineEbsBandwidthMbps>");
    }
    if (value.baseline_performance_factors) |v| {
        try buf.appendSlice(allocator, "<BaselinePerformanceFactors>");
        try serializeBaselinePerformanceFactorsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</BaselinePerformanceFactors>");
    }
    if (value.burstable_performance) |v| {
        try buf.appendSlice(allocator, "<BurstablePerformance>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</BurstablePerformance>");
    }
    if (value.cpu_manufacturers) |v| {
        try buf.appendSlice(allocator, "<CpuManufacturer>");
        try serializeCpuManufacturerSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</CpuManufacturer>");
    }
    if (value.excluded_instance_types) |v| {
        try buf.appendSlice(allocator, "<ExcludedInstanceType>");
        try serializeExcludedInstanceTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ExcludedInstanceType>");
    }
    if (value.instance_generations) |v| {
        try buf.appendSlice(allocator, "<InstanceGeneration>");
        try serializeInstanceGenerationSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</InstanceGeneration>");
    }
    if (value.local_storage) |v| {
        try buf.appendSlice(allocator, "<LocalStorage>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</LocalStorage>");
    }
    if (value.local_storage_types) |v| {
        try buf.appendSlice(allocator, "<LocalStorageType>");
        try serializeLocalStorageTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</LocalStorageType>");
    }
    if (value.max_spot_price_as_percentage_of_optimal_on_demand_price) |v| {
        try buf.appendSlice(allocator, "<MaxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</MaxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
    }
    if (value.memory_gi_b_per_v_cpu) |v| {
        try buf.appendSlice(allocator, "<MemoryGiBPerVCpu>");
        try serializeMemoryGiBPerVCpuRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</MemoryGiBPerVCpu>");
    }
    try buf.appendSlice(allocator, "<MemoryMiB>");
    try serializeMemoryMiBRequest(allocator, buf, value.memory_mi_b);
    try buf.appendSlice(allocator, "</MemoryMiB>");
    if (value.network_bandwidth_gbps) |v| {
        try buf.appendSlice(allocator, "<NetworkBandwidthGbps>");
        try serializeNetworkBandwidthGbpsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</NetworkBandwidthGbps>");
    }
    if (value.network_interface_count) |v| {
        try buf.appendSlice(allocator, "<NetworkInterfaceCount>");
        try serializeNetworkInterfaceCountRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</NetworkInterfaceCount>");
    }
    if (value.on_demand_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(allocator, "<OnDemandMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</OnDemandMaxPricePercentageOverLowestPrice>");
    }
    if (value.require_encryption_in_transit) |v| {
        try buf.appendSlice(allocator, "<RequireEncryptionInTransit>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</RequireEncryptionInTransit>");
    }
    if (value.require_hibernate_support) |v| {
        try buf.appendSlice(allocator, "<RequireHibernateSupport>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</RequireHibernateSupport>");
    }
    if (value.spot_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(allocator, "<SpotMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</SpotMaxPricePercentageOverLowestPrice>");
    }
    if (value.total_local_storage_gb) |v| {
        try buf.appendSlice(allocator, "<TotalLocalStorageGB>");
        try serializeTotalLocalStorageGBRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</TotalLocalStorageGB>");
    }
    try buf.appendSlice(allocator, "<VCpuCount>");
    try serializeVCpuCountRangeRequest(allocator, buf, value.v_cpu_count);
    try buf.appendSlice(allocator, "</VCpuCount>");
}

pub fn serializeInstanceRequirementsWithMetadataRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceRequirementsWithMetadataRequest) !void {
    if (value.architecture_types) |v| {
        try buf.appendSlice(allocator, "<ArchitectureType>");
        try serializeArchitectureTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ArchitectureType>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(allocator, "<InstanceRequirements>");
        try serializeInstanceRequirementsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</InstanceRequirements>");
    }
    if (value.virtualization_types) |v| {
        try buf.appendSlice(allocator, "<VirtualizationType>");
        try serializeVirtualizationTypeSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</VirtualizationType>");
    }
}

pub fn serializeInstanceSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceSpecification) !void {
    if (value.exclude_boot_volume) |v| {
        try buf.appendSlice(allocator, "<ExcludeBootVolume>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</ExcludeBootVolume>");
    }
    if (value.exclude_data_volume_ids) |v| {
        try buf.appendSlice(allocator, "<ExcludeDataVolumeId>");
        try serializeVolumeIdStringList(allocator, buf, v, "VolumeId");
        try buf.appendSlice(allocator, "</ExcludeDataVolumeId>");
    }
    try buf.appendSlice(allocator, "<InstanceId>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.instance_id);
    try buf.appendSlice(allocator, "</InstanceId>");
}

pub fn serializeIntegrateServices(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IntegrateServices) !void {
    if (value.athena_integrations) |v| {
        try buf.appendSlice(allocator, "<AthenaIntegration>");
        try serializeAthenaIntegrationsSet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</AthenaIntegration>");
    }
}

pub fn serializeIpPermission(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpPermission) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(allocator, "<fromPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</fromPort>");
    }
    if (value.ip_protocol) |v| {
        try buf.appendSlice(allocator, "<ipProtocol>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ipProtocol>");
    }
    if (value.ip_ranges) |v| {
        try buf.appendSlice(allocator, "<ipRanges>");
        try serializeIpRangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ipRanges>");
    }
    if (value.ipv_6_ranges) |v| {
        try buf.appendSlice(allocator, "<ipv6Ranges>");
        try serializeIpv6RangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ipv6Ranges>");
    }
    if (value.prefix_list_ids) |v| {
        try buf.appendSlice(allocator, "<prefixListIds>");
        try serializePrefixListIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</prefixListIds>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(allocator, "<toPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</toPort>");
    }
    if (value.user_id_group_pairs) |v| {
        try buf.appendSlice(allocator, "<groups>");
        try serializeUserIdGroupPairList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</groups>");
    }
}

pub fn serializeIpRange(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpRange) !void {
    if (value.cidr_ip) |v| {
        try buf.appendSlice(allocator, "<cidrIp>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</cidrIp>");
    }
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</description>");
    }
}

pub fn serializeIpamCidrAuthorizationContext(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamCidrAuthorizationContext) !void {
    if (value.message) |v| {
        try buf.appendSlice(allocator, "<Message>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Message>");
    }
    if (value.signature) |v| {
        try buf.appendSlice(allocator, "<Signature>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Signature>");
    }
}

pub fn serializeIpamPolicyAllocationRuleRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPolicyAllocationRuleRequest) !void {
    if (value.source_ipam_pool_id) |v| {
        try buf.appendSlice(allocator, "<SourceIpamPoolId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SourceIpamPoolId>");
    }
}

pub fn serializeIpamPoolSourceResourceRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPoolSourceResourceRequest) !void {
    if (value.resource_id) |v| {
        try buf.appendSlice(allocator, "<ResourceId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceId>");
    }
    if (value.resource_owner) |v| {
        try buf.appendSlice(allocator, "<ResourceOwner>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceOwner>");
    }
    if (value.resource_region) |v| {
        try buf.appendSlice(allocator, "<ResourceRegion>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceRegion>");
    }
    if (value.resource_type) |v| {
        try buf.appendSlice(allocator, "<ResourceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ResourceType>");
    }
}

pub fn serializeIpamPrefixListResolverRuleConditionRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPrefixListResolverRuleConditionRequest) !void {
    if (value.cidr) |v| {
        try buf.appendSlice(allocator, "<Cidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Cidr>");
    }
    if (value.ipam_pool_id) |v| {
        try buf.appendSlice(allocator, "<IpamPoolId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</IpamPoolId>");
    }
    try buf.appendSlice(allocator, "<Operation>");
    try buf.appendSlice(allocator, value.operation.wireName());
    try buf.appendSlice(allocator, "</Operation>");
    if (value.resource_id) |v| {
        try buf.appendSlice(allocator, "<ResourceId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceId>");
    }
    if (value.resource_owner) |v| {
        try buf.appendSlice(allocator, "<ResourceOwner>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceOwner>");
    }
    if (value.resource_region) |v| {
        try buf.appendSlice(allocator, "<ResourceRegion>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceRegion>");
    }
    if (value.resource_tag) |v| {
        try buf.appendSlice(allocator, "<ResourceTag>");
        try serializeRequestIpamResourceTag(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceTag>");
    }
}

pub fn serializeIpamPrefixListResolverRuleRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPrefixListResolverRuleRequest) !void {
    if (value.conditions) |v| {
        try buf.appendSlice(allocator, "<Condition>");
        try serializeIpamPrefixListResolverRuleConditionRequestSet(allocator, buf, v, "Condition");
        try buf.appendSlice(allocator, "</Condition>");
    }
    if (value.ipam_scope_id) |v| {
        try buf.appendSlice(allocator, "<IpamScopeId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</IpamScopeId>");
    }
    if (value.resource_type) |v| {
        try buf.appendSlice(allocator, "<ResourceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ResourceType>");
    }
    try buf.appendSlice(allocator, "<RuleType>");
    try buf.appendSlice(allocator, value.rule_type.wireName());
    try buf.appendSlice(allocator, "</RuleType>");
    if (value.static_cidr) |v| {
        try buf.appendSlice(allocator, "<StaticCidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</StaticCidr>");
    }
}

pub fn serializeIpv4PrefixSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Ipv4PrefixSpecificationRequest) !void {
    if (value.ipv_4_prefix) |v| {
        try buf.appendSlice(allocator, "<Ipv4Prefix>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ipv4Prefix>");
    }
}

pub fn serializeIpv6PrefixSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Ipv6PrefixSpecificationRequest) !void {
    if (value.ipv_6_prefix) |v| {
        try buf.appendSlice(allocator, "<Ipv6Prefix>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ipv6Prefix>");
    }
}

pub fn serializeIpv6Range(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Ipv6Range) !void {
    if (value.cidr_ipv_6) |v| {
        try buf.appendSlice(allocator, "<cidrIpv6>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</cidrIpv6>");
    }
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</description>");
    }
}

pub fn serializeLaunchPermission(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchPermission) !void {
    if (value.group) |v| {
        try buf.appendSlice(allocator, "<group>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</group>");
    }
    if (value.organizational_unit_arn) |v| {
        try buf.appendSlice(allocator, "<organizationalUnitArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</organizationalUnitArn>");
    }
    if (value.organization_arn) |v| {
        try buf.appendSlice(allocator, "<organizationArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</organizationArn>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(allocator, "<userId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</userId>");
    }
}

pub fn serializeLaunchPermissionModifications(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchPermissionModifications) !void {
    if (value.add) |v| {
        try buf.appendSlice(allocator, "<Add>");
        try serializeLaunchPermissionList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Add>");
    }
    if (value.remove) |v| {
        try buf.appendSlice(allocator, "<Remove>");
        try serializeLaunchPermissionList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Remove>");
    }
}

pub fn serializeLaunchTemplateBlockDeviceMappingRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateBlockDeviceMappingRequest) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(allocator, "<DeviceName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DeviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(allocator, "<Ebs>");
        try serializeLaunchTemplateEbsBlockDeviceRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(allocator, "<NoDevice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</NoDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(allocator, "<VirtualName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</VirtualName>");
    }
}

pub fn serializeLaunchTemplateCapacityReservationSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateCapacityReservationSpecificationRequest) !void {
    if (value.capacity_reservation_preference) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationPreference>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</CapacityReservationPreference>");
    }
    if (value.capacity_reservation_target) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationTarget>");
        try serializeCapacityReservationTarget(allocator, buf, v);
        try buf.appendSlice(allocator, "</CapacityReservationTarget>");
    }
}

pub fn serializeLaunchTemplateConfig(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateConfig) !void {
    if (value.launch_template_specification) |v| {
        try buf.appendSlice(allocator, "<launchTemplateSpecification>");
        try serializeFleetLaunchTemplateSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</launchTemplateSpecification>");
    }
    if (value.overrides) |v| {
        try buf.appendSlice(allocator, "<overrides>");
        try serializeLaunchTemplateOverridesList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</overrides>");
    }
}

pub fn serializeLaunchTemplateCpuOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateCpuOptionsRequest) !void {
    if (value.amd_sev_snp) |v| {
        try buf.appendSlice(allocator, "<AmdSevSnp>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AmdSevSnp>");
    }
    if (value.core_count) |v| {
        try buf.appendSlice(allocator, "<CoreCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</CoreCount>");
    }
    if (value.threads_per_core) |v| {
        try buf.appendSlice(allocator, "<ThreadsPerCore>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ThreadsPerCore>");
    }
}

pub fn serializeLaunchTemplateEbsBlockDeviceRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateEbsBlockDeviceRequest) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<DeleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DeleteOnTermination>");
    }
    if (value.ebs_card_index) |v| {
        try buf.appendSlice(allocator, "<EbsCardIndex>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</EbsCardIndex>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(allocator, "<Encrypted>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(allocator, "<Iops>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Iops>");
    }
    if (value.kms_key_id) |v| {
        try buf.appendSlice(allocator, "<KmsKeyId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</KmsKeyId>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(allocator, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SnapshotId>");
    }
    if (value.throughput) |v| {
        try buf.appendSlice(allocator, "<Throughput>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Throughput>");
    }
    if (value.volume_initialization_rate) |v| {
        try buf.appendSlice(allocator, "<VolumeInitializationRate>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</VolumeInitializationRate>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(allocator, "<VolumeSize>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</VolumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(allocator, "<VolumeType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</VolumeType>");
    }
}

pub fn serializeLaunchTemplateElasticInferenceAccelerator(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateElasticInferenceAccelerator) !void {
    if (value.count) |v| {
        try buf.appendSlice(allocator, "<Count>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Count>");
    }
    try buf.appendSlice(allocator, "<Type>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.@"type");
    try buf.appendSlice(allocator, "</Type>");
}

pub fn serializeLaunchTemplateEnclaveOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateEnclaveOptionsRequest) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
}

pub fn serializeLaunchTemplateHibernationOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateHibernationOptionsRequest) !void {
    if (value.configured) |v| {
        try buf.appendSlice(allocator, "<Configured>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Configured>");
    }
}

pub fn serializeLaunchTemplateIamInstanceProfileSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateIamInstanceProfileSpecificationRequest) !void {
    if (value.arn) |v| {
        try buf.appendSlice(allocator, "<Arn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Arn>");
    }
    if (value.name) |v| {
        try buf.appendSlice(allocator, "<Name>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Name>");
    }
}

pub fn serializeLaunchTemplateInstanceMaintenanceOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceMaintenanceOptionsRequest) !void {
    if (value.auto_recovery) |v| {
        try buf.appendSlice(allocator, "<AutoRecovery>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AutoRecovery>");
    }
}

pub fn serializeLaunchTemplateInstanceMarketOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceMarketOptionsRequest) !void {
    if (value.market_type) |v| {
        try buf.appendSlice(allocator, "<MarketType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</MarketType>");
    }
    if (value.spot_options) |v| {
        try buf.appendSlice(allocator, "<SpotOptions>");
        try serializeLaunchTemplateSpotMarketOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</SpotOptions>");
    }
}

pub fn serializeLaunchTemplateInstanceMetadataOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceMetadataOptionsRequest) !void {
    if (value.http_endpoint) |v| {
        try buf.appendSlice(allocator, "<HttpEndpoint>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HttpEndpoint>");
    }
    if (value.http_protocol_ipv_6) |v| {
        try buf.appendSlice(allocator, "<HttpProtocolIpv6>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HttpProtocolIpv6>");
    }
    if (value.http_put_response_hop_limit) |v| {
        try buf.appendSlice(allocator, "<HttpPutResponseHopLimit>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</HttpPutResponseHopLimit>");
    }
    if (value.http_tokens) |v| {
        try buf.appendSlice(allocator, "<HttpTokens>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HttpTokens>");
    }
    if (value.instance_metadata_tags) |v| {
        try buf.appendSlice(allocator, "<InstanceMetadataTags>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceMetadataTags>");
    }
}

pub fn serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceNetworkInterfaceSpecificationRequest) !void {
    if (value.associate_carrier_ip_address) |v| {
        try buf.appendSlice(allocator, "<AssociateCarrierIpAddress>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</AssociateCarrierIpAddress>");
    }
    if (value.associate_public_ip_address) |v| {
        try buf.appendSlice(allocator, "<AssociatePublicIpAddress>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</AssociatePublicIpAddress>");
    }
    if (value.connection_tracking_specification) |v| {
        try buf.appendSlice(allocator, "<ConnectionTrackingSpecification>");
        try serializeConnectionTrackingSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</ConnectionTrackingSpecification>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<DeleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DeleteOnTermination>");
    }
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
    if (value.device_index) |v| {
        try buf.appendSlice(allocator, "<DeviceIndex>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</DeviceIndex>");
    }
    if (value.ena_queue_count) |v| {
        try buf.appendSlice(allocator, "<EnaQueueCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</EnaQueueCount>");
    }
    if (value.ena_srd_specification) |v| {
        try buf.appendSlice(allocator, "<EnaSrdSpecification>");
        try serializeEnaSrdSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</EnaSrdSpecification>");
    }
    if (value.groups) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupId>");
        try serializeSecurityGroupIdStringList(allocator, buf, v, "SecurityGroupId");
        try buf.appendSlice(allocator, "</SecurityGroupId>");
    }
    if (value.interface_type) |v| {
        try buf.appendSlice(allocator, "<InterfaceType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</InterfaceType>");
    }
    if (value.ipv_4_prefix_count) |v| {
        try buf.appendSlice(allocator, "<Ipv4PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Ipv4PrefixCount>");
    }
    if (value.ipv_4_prefixes) |v| {
        try buf.appendSlice(allocator, "<Ipv4Prefix>");
        try serializeIpv4PrefixList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Ipv4Prefix>");
    }
    if (value.ipv_6_address_count) |v| {
        try buf.appendSlice(allocator, "<Ipv6AddressCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Ipv6AddressCount>");
    }
    if (value.ipv_6_addresses) |v| {
        try buf.appendSlice(allocator, "<Ipv6Addresses>");
        try serializeInstanceIpv6AddressListRequest(allocator, buf, v, "InstanceIpv6Address");
        try buf.appendSlice(allocator, "</Ipv6Addresses>");
    }
    if (value.ipv_6_prefix_count) |v| {
        try buf.appendSlice(allocator, "<Ipv6PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Ipv6PrefixCount>");
    }
    if (value.ipv_6_prefixes) |v| {
        try buf.appendSlice(allocator, "<Ipv6Prefix>");
        try serializeIpv6PrefixList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Ipv6Prefix>");
    }
    if (value.network_card_index) |v| {
        try buf.appendSlice(allocator, "<NetworkCardIndex>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</NetworkCardIndex>");
    }
    if (value.network_interface_id) |v| {
        try buf.appendSlice(allocator, "<NetworkInterfaceId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</NetworkInterfaceId>");
    }
    if (value.primary_ipv_6) |v| {
        try buf.appendSlice(allocator, "<PrimaryIpv6>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</PrimaryIpv6>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(allocator, "<PrivateIpAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PrivateIpAddress>");
    }
    if (value.private_ip_addresses) |v| {
        try buf.appendSlice(allocator, "<PrivateIpAddresses>");
        try serializePrivateIpAddressSpecificationList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PrivateIpAddresses>");
    }
    if (value.secondary_private_ip_address_count) |v| {
        try buf.appendSlice(allocator, "<SecondaryPrivateIpAddressCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</SecondaryPrivateIpAddressCount>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeLaunchTemplateLicenseConfigurationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateLicenseConfigurationRequest) !void {
    if (value.license_configuration_arn) |v| {
        try buf.appendSlice(allocator, "<LicenseConfigurationArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LicenseConfigurationArn>");
    }
}

pub fn serializeLaunchTemplateNetworkPerformanceOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateNetworkPerformanceOptionsRequest) !void {
    if (value.bandwidth_weighting) |v| {
        try buf.appendSlice(allocator, "<BandwidthWeighting>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</BandwidthWeighting>");
    }
}

pub fn serializeLaunchTemplateOverrides(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateOverrides) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZoneId>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(allocator, "<instanceRequirements>");
        try serializeInstanceRequirements(allocator, buf, v);
        try buf.appendSlice(allocator, "</instanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<instanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</instanceType>");
    }
    if (value.priority) |v| {
        try buf.appendSlice(allocator, "<priority>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</priority>");
    }
    if (value.spot_price) |v| {
        try buf.appendSlice(allocator, "<spotPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</spotPrice>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<subnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</subnetId>");
    }
    if (value.weighted_capacity) |v| {
        try buf.appendSlice(allocator, "<weightedCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</weightedCapacity>");
    }
}

pub fn serializeLaunchTemplatePlacementRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplatePlacementRequest) !void {
    if (value.affinity) |v| {
        try buf.appendSlice(allocator, "<Affinity>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Affinity>");
    }
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZoneId>");
    }
    if (value.group_id) |v| {
        try buf.appendSlice(allocator, "<GroupId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</GroupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(allocator, "<GroupName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</GroupName>");
    }
    if (value.host_id) |v| {
        try buf.appendSlice(allocator, "<HostId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</HostId>");
    }
    if (value.host_resource_group_arn) |v| {
        try buf.appendSlice(allocator, "<HostResourceGroupArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</HostResourceGroupArn>");
    }
    if (value.partition_number) |v| {
        try buf.appendSlice(allocator, "<PartitionNumber>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</PartitionNumber>");
    }
    if (value.spread_domain) |v| {
        try buf.appendSlice(allocator, "<SpreadDomain>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SpreadDomain>");
    }
    if (value.tenancy) |v| {
        try buf.appendSlice(allocator, "<Tenancy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Tenancy>");
    }
}

pub fn serializeLaunchTemplatePrivateDnsNameOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplatePrivateDnsNameOptionsRequest) !void {
    if (value.enable_resource_name_dns_aaaa_record) |v| {
        try buf.appendSlice(allocator, "<EnableResourceNameDnsAAAARecord>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnableResourceNameDnsAAAARecord>");
    }
    if (value.enable_resource_name_dns_a_record) |v| {
        try buf.appendSlice(allocator, "<EnableResourceNameDnsARecord>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnableResourceNameDnsARecord>");
    }
    if (value.hostname_type) |v| {
        try buf.appendSlice(allocator, "<HostnameType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HostnameType>");
    }
}

pub fn serializeLaunchTemplateSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateSpecification) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(allocator, "<LaunchTemplateId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LaunchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(allocator, "<LaunchTemplateName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LaunchTemplateName>");
    }
    if (value.version) |v| {
        try buf.appendSlice(allocator, "<Version>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Version>");
    }
}

pub fn serializeLaunchTemplateSpotMarketOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateSpotMarketOptionsRequest) !void {
    if (value.block_duration_minutes) |v| {
        try buf.appendSlice(allocator, "<BlockDurationMinutes>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</BlockDurationMinutes>");
    }
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(allocator, "<InstanceInterruptionBehavior>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceInterruptionBehavior>");
    }
    if (value.max_price) |v| {
        try buf.appendSlice(allocator, "<MaxPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</MaxPrice>");
    }
    if (value.spot_instance_type) |v| {
        try buf.appendSlice(allocator, "<SpotInstanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</SpotInstanceType>");
    }
    if (value.valid_until) |v| {
        try buf.appendSlice(allocator, "<ValidUntil>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</ValidUntil>");
    }
}

pub fn serializeLaunchTemplateTagSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateTagSpecificationRequest) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(allocator, "<ResourceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ResourceType>");
    }
    if (value.tags) |v| {
        try buf.appendSlice(allocator, "<Tag>");
        try serializeTagList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Tag>");
    }
}

pub fn serializeLaunchTemplatesMonitoringRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplatesMonitoringRequest) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
}

pub fn serializeLicenseConfigurationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LicenseConfigurationRequest) !void {
    if (value.license_configuration_arn) |v| {
        try buf.appendSlice(allocator, "<LicenseConfigurationArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LicenseConfigurationArn>");
    }
}

pub fn serializeLoadBalancersConfig(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LoadBalancersConfig) !void {
    if (value.classic_load_balancers_config) |v| {
        try buf.appendSlice(allocator, "<classicLoadBalancersConfig>");
        try serializeClassicLoadBalancersConfig(allocator, buf, v);
        try buf.appendSlice(allocator, "</classicLoadBalancersConfig>");
    }
    if (value.target_groups_config) |v| {
        try buf.appendSlice(allocator, "<targetGroupsConfig>");
        try serializeTargetGroupsConfig(allocator, buf, v);
        try buf.appendSlice(allocator, "</targetGroupsConfig>");
    }
}

pub fn serializeLoadPermissionModifications(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LoadPermissionModifications) !void {
    if (value.add) |v| {
        try buf.appendSlice(allocator, "<Add>");
        try serializeLoadPermissionListRequest(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Add>");
    }
    if (value.remove) |v| {
        try buf.appendSlice(allocator, "<Remove>");
        try serializeLoadPermissionListRequest(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Remove>");
    }
}

pub fn serializeLoadPermissionRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: LoadPermissionRequest) !void {
    if (value.group) |v| {
        try buf.appendSlice(allocator, "<Group>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Group>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(allocator, "<UserId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserId>");
    }
}

pub fn serializeMacSystemIntegrityProtectionConfigurationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: MacSystemIntegrityProtectionConfigurationRequest) !void {
    if (value.apple_internal) |v| {
        try buf.appendSlice(allocator, "<AppleInternal>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AppleInternal>");
    }
    if (value.base_system) |v| {
        try buf.appendSlice(allocator, "<BaseSystem>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</BaseSystem>");
    }
    if (value.debugging_restrictions) |v| {
        try buf.appendSlice(allocator, "<DebuggingRestrictions>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DebuggingRestrictions>");
    }
    if (value.d_trace_restrictions) |v| {
        try buf.appendSlice(allocator, "<DTraceRestrictions>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DTraceRestrictions>");
    }
    if (value.filesystem_protections) |v| {
        try buf.appendSlice(allocator, "<FilesystemProtections>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</FilesystemProtections>");
    }
    if (value.kext_signing) |v| {
        try buf.appendSlice(allocator, "<KextSigning>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</KextSigning>");
    }
    if (value.nvram_protections) |v| {
        try buf.appendSlice(allocator, "<NvramProtections>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</NvramProtections>");
    }
}

pub fn serializeMemoryGiBPerVCpu(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryGiBPerVCpu) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeMemoryGiBPerVCpuRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryGiBPerVCpuRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<Min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Min>");
    }
}

pub fn serializeMemoryMiB(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryMiB) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeMemoryMiBRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryMiBRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    try buf.appendSlice(allocator, "<Min>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.min}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</Min>");
}

pub fn serializeModifyTransitGatewayOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyTransitGatewayOptions) !void {
    if (value.add_transit_gateway_cidr_blocks) |v| {
        try buf.appendSlice(allocator, "<AddTransitGatewayCidrBlocks>");
        try serializeTransitGatewayCidrBlockStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</AddTransitGatewayCidrBlocks>");
    }
    if (value.amazon_side_asn) |v| {
        try buf.appendSlice(allocator, "<AmazonSideAsn>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</AmazonSideAsn>");
    }
    if (value.association_default_route_table_id) |v| {
        try buf.appendSlice(allocator, "<AssociationDefaultRouteTableId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AssociationDefaultRouteTableId>");
    }
    if (value.auto_accept_shared_attachments) |v| {
        try buf.appendSlice(allocator, "<AutoAcceptSharedAttachments>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AutoAcceptSharedAttachments>");
    }
    if (value.default_route_table_association) |v| {
        try buf.appendSlice(allocator, "<DefaultRouteTableAssociation>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DefaultRouteTableAssociation>");
    }
    if (value.default_route_table_propagation) |v| {
        try buf.appendSlice(allocator, "<DefaultRouteTablePropagation>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DefaultRouteTablePropagation>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(allocator, "<DnsSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DnsSupport>");
    }
    if (value.encryption_support) |v| {
        try buf.appendSlice(allocator, "<EncryptionSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</EncryptionSupport>");
    }
    if (value.propagation_default_route_table_id) |v| {
        try buf.appendSlice(allocator, "<PropagationDefaultRouteTableId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PropagationDefaultRouteTableId>");
    }
    if (value.remove_transit_gateway_cidr_blocks) |v| {
        try buf.appendSlice(allocator, "<RemoveTransitGatewayCidrBlocks>");
        try serializeTransitGatewayCidrBlockStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</RemoveTransitGatewayCidrBlocks>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</SecurityGroupReferencingSupport>");
    }
    if (value.vpn_ecmp_support) |v| {
        try buf.appendSlice(allocator, "<VpnEcmpSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</VpnEcmpSupport>");
    }
}

pub fn serializeModifyTransitGatewayVpcAttachmentRequestOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyTransitGatewayVpcAttachmentRequestOptions) !void {
    if (value.appliance_mode_support) |v| {
        try buf.appendSlice(allocator, "<ApplianceModeSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ApplianceModeSupport>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(allocator, "<DnsSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DnsSupport>");
    }
    if (value.ipv_6_support) |v| {
        try buf.appendSlice(allocator, "<Ipv6Support>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Ipv6Support>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</SecurityGroupReferencingSupport>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointCidrOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointCidrOptions) !void {
    if (value.port_ranges) |v| {
        try buf.appendSlice(allocator, "<PortRange>");
        try serializeModifyVerifiedAccessEndpointPortRangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PortRange>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointEniOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointEniOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(allocator, "<Port>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(allocator, "<PortRange>");
        try serializeModifyVerifiedAccessEndpointPortRangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(allocator, "<Protocol>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Protocol>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointLoadBalancerOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointLoadBalancerOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(allocator, "<Port>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(allocator, "<PortRange>");
        try serializeModifyVerifiedAccessEndpointPortRangeList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(allocator, "<Protocol>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</Protocol>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try serializeModifyVerifiedAccessEndpointSubnetIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointPortRange(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointPortRange) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(allocator, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(allocator, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ToPort>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointRdsOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointRdsOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(allocator, "<Port>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Port>");
    }
    if (value.rds_endpoint) |v| {
        try buf.appendSlice(allocator, "<RdsEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RdsEndpoint>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try serializeModifyVerifiedAccessEndpointSubnetIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeModifyVerifiedAccessNativeApplicationOidcOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessNativeApplicationOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(allocator, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(allocator, "<ClientId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(allocator, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(allocator, "<Issuer>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Issuer>");
    }
    if (value.public_signing_key_endpoint) |v| {
        try buf.appendSlice(allocator, "<PublicSigningKeyEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PublicSigningKeyEndpoint>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(allocator, "<Scope>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(allocator, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(allocator, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserInfoEndpoint>");
    }
}

pub fn serializeModifyVerifiedAccessTrustProviderDeviceOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessTrustProviderDeviceOptions) !void {
    if (value.public_signing_key_url) |v| {
        try buf.appendSlice(allocator, "<PublicSigningKeyUrl>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PublicSigningKeyUrl>");
    }
}

pub fn serializeModifyVerifiedAccessTrustProviderOidcOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessTrustProviderOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(allocator, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(allocator, "<ClientId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(allocator, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(allocator, "<Issuer>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Issuer>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(allocator, "<Scope>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(allocator, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(allocator, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserInfoEndpoint>");
    }
}

pub fn serializeModifyVpnTunnelOptionsSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVpnTunnelOptionsSpecification) !void {
    if (value.dpd_timeout_action) |v| {
        try buf.appendSlice(allocator, "<DPDTimeoutAction>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DPDTimeoutAction>");
    }
    if (value.dpd_timeout_seconds) |v| {
        try buf.appendSlice(allocator, "<DPDTimeoutSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</DPDTimeoutSeconds>");
    }
    if (value.enable_tunnel_lifecycle_control) |v| {
        try buf.appendSlice(allocator, "<EnableTunnelLifecycleControl>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnableTunnelLifecycleControl>");
    }
    if (value.ike_versions) |v| {
        try buf.appendSlice(allocator, "<IKEVersion>");
        try serializeIKEVersionsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</IKEVersion>");
    }
    if (value.log_options) |v| {
        try buf.appendSlice(allocator, "<LogOptions>");
        try serializeVpnTunnelLogOptionsSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</LogOptions>");
    }
    if (value.phase_1_dh_group_numbers) |v| {
        try buf.appendSlice(allocator, "<Phase1DHGroupNumber>");
        try serializePhase1DHGroupNumbersRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase1DHGroupNumber>");
    }
    if (value.phase_1_encryption_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase1EncryptionAlgorithm>");
        try serializePhase1EncryptionAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase1EncryptionAlgorithm>");
    }
    if (value.phase_1_integrity_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase1IntegrityAlgorithm>");
        try serializePhase1IntegrityAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase1IntegrityAlgorithm>");
    }
    if (value.phase_1_lifetime_seconds) |v| {
        try buf.appendSlice(allocator, "<Phase1LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Phase1LifetimeSeconds>");
    }
    if (value.phase_2_dh_group_numbers) |v| {
        try buf.appendSlice(allocator, "<Phase2DHGroupNumber>");
        try serializePhase2DHGroupNumbersRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase2DHGroupNumber>");
    }
    if (value.phase_2_encryption_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase2EncryptionAlgorithm>");
        try serializePhase2EncryptionAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase2EncryptionAlgorithm>");
    }
    if (value.phase_2_integrity_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase2IntegrityAlgorithm>");
        try serializePhase2IntegrityAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase2IntegrityAlgorithm>");
    }
    if (value.phase_2_lifetime_seconds) |v| {
        try buf.appendSlice(allocator, "<Phase2LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Phase2LifetimeSeconds>");
    }
    if (value.pre_shared_key) |v| {
        try buf.appendSlice(allocator, "<PreSharedKey>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PreSharedKey>");
    }
    if (value.rekey_fuzz_percentage) |v| {
        try buf.appendSlice(allocator, "<RekeyFuzzPercentage>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</RekeyFuzzPercentage>");
    }
    if (value.rekey_margin_time_seconds) |v| {
        try buf.appendSlice(allocator, "<RekeyMarginTimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</RekeyMarginTimeSeconds>");
    }
    if (value.replay_window_size) |v| {
        try buf.appendSlice(allocator, "<ReplayWindowSize>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ReplayWindowSize>");
    }
    if (value.startup_action) |v| {
        try buf.appendSlice(allocator, "<StartupAction>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</StartupAction>");
    }
    if (value.tunnel_inside_cidr) |v| {
        try buf.appendSlice(allocator, "<TunnelInsideCidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TunnelInsideCidr>");
    }
    if (value.tunnel_inside_ipv_6_cidr) |v| {
        try buf.appendSlice(allocator, "<TunnelInsideIpv6Cidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TunnelInsideIpv6Cidr>");
    }
}

pub fn serializeNetworkBandwidthGbps(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkBandwidthGbps) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeNetworkBandwidthGbpsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkBandwidthGbpsRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<Min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Min>");
    }
}

pub fn serializeNetworkInterfaceAttachmentChanges(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkInterfaceAttachmentChanges) !void {
    if (value.attachment_id) |v| {
        try buf.appendSlice(allocator, "<attachmentId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</attachmentId>");
    }
    if (value.default_ena_queue_count) |v| {
        try buf.appendSlice(allocator, "<DefaultEnaQueueCount>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DefaultEnaQueueCount>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<deleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</deleteOnTermination>");
    }
    if (value.ena_queue_count) |v| {
        try buf.appendSlice(allocator, "<EnaQueueCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</EnaQueueCount>");
    }
}

pub fn serializeNetworkInterfaceCount(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkInterfaceCount) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeNetworkInterfaceCountRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkInterfaceCountRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<Min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Min>");
    }
}

pub fn serializeNewDhcpConfiguration(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: NewDhcpConfiguration) !void {
    if (value.key) |v| {
        try buf.appendSlice(allocator, "<key>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</key>");
    }
    if (value.values) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializeOnDemandOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: OnDemandOptionsRequest) !void {
    if (value.allocation_strategy) |v| {
        try buf.appendSlice(allocator, "<AllocationStrategy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AllocationStrategy>");
    }
    if (value.capacity_reservation_options) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationOptions>");
        try serializeCapacityReservationOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</CapacityReservationOptions>");
    }
    if (value.max_total_price) |v| {
        try buf.appendSlice(allocator, "<MaxTotalPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</MaxTotalPrice>");
    }
    if (value.min_target_capacity) |v| {
        try buf.appendSlice(allocator, "<MinTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</MinTargetCapacity>");
    }
    if (value.single_availability_zone) |v| {
        try buf.appendSlice(allocator, "<SingleAvailabilityZone>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</SingleAvailabilityZone>");
    }
    if (value.single_instance_type) |v| {
        try buf.appendSlice(allocator, "<SingleInstanceType>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</SingleInstanceType>");
    }
}

pub fn serializeOperatorRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: OperatorRequest) !void {
    if (value.principal) |v| {
        try buf.appendSlice(allocator, "<Principal>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Principal>");
    }
}

pub fn serializePacketHeaderStatementRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PacketHeaderStatementRequest) !void {
    if (value.destination_addresses) |v| {
        try buf.appendSlice(allocator, "<DestinationAddress>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</DestinationAddress>");
    }
    if (value.destination_ports) |v| {
        try buf.appendSlice(allocator, "<DestinationPort>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</DestinationPort>");
    }
    if (value.destination_prefix_lists) |v| {
        try buf.appendSlice(allocator, "<DestinationPrefixList>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</DestinationPrefixList>");
    }
    if (value.protocols) |v| {
        try buf.appendSlice(allocator, "<Protocol>");
        try serializeProtocolList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Protocol>");
    }
    if (value.source_addresses) |v| {
        try buf.appendSlice(allocator, "<SourceAddress>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SourceAddress>");
    }
    if (value.source_ports) |v| {
        try buf.appendSlice(allocator, "<SourcePort>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SourcePort>");
    }
    if (value.source_prefix_lists) |v| {
        try buf.appendSlice(allocator, "<SourcePrefixList>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SourcePrefixList>");
    }
}

pub fn serializePathRequestFilter(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PathRequestFilter) !void {
    if (value.destination_address) |v| {
        try buf.appendSlice(allocator, "<DestinationAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DestinationAddress>");
    }
    if (value.destination_port_range) |v| {
        try buf.appendSlice(allocator, "<DestinationPortRange>");
        try serializeRequestFilterPortRange(allocator, buf, v);
        try buf.appendSlice(allocator, "</DestinationPortRange>");
    }
    if (value.source_address) |v| {
        try buf.appendSlice(allocator, "<SourceAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SourceAddress>");
    }
    if (value.source_port_range) |v| {
        try buf.appendSlice(allocator, "<SourcePortRange>");
        try serializeRequestFilterPortRange(allocator, buf, v);
        try buf.appendSlice(allocator, "</SourcePortRange>");
    }
}

pub fn serializePathStatementRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PathStatementRequest) !void {
    if (value.packet_header_statement) |v| {
        try buf.appendSlice(allocator, "<PacketHeaderStatement>");
        try serializePacketHeaderStatementRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</PacketHeaderStatement>");
    }
    if (value.resource_statement) |v| {
        try buf.appendSlice(allocator, "<ResourceStatement>");
        try serializeResourceStatementRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceStatement>");
    }
}

pub fn serializePeeringConnectionOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PeeringConnectionOptionsRequest) !void {
    if (value.allow_dns_resolution_from_remote_vpc) |v| {
        try buf.appendSlice(allocator, "<AllowDnsResolutionFromRemoteVpc>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</AllowDnsResolutionFromRemoteVpc>");
    }
    if (value.allow_egress_from_local_classic_link_to_remote_vpc) |v| {
        try buf.appendSlice(allocator, "<AllowEgressFromLocalClassicLinkToRemoteVpc>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</AllowEgressFromLocalClassicLinkToRemoteVpc>");
    }
    if (value.allow_egress_from_local_vpc_to_remote_classic_link) |v| {
        try buf.appendSlice(allocator, "<AllowEgressFromLocalVpcToRemoteClassicLink>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</AllowEgressFromLocalVpcToRemoteClassicLink>");
    }
}

pub fn serializePerformanceFactorReference(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PerformanceFactorReference) !void {
    if (value.instance_family) |v| {
        try buf.appendSlice(allocator, "<instanceFamily>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</instanceFamily>");
    }
}

pub fn serializePerformanceFactorReferenceRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PerformanceFactorReferenceRequest) !void {
    if (value.instance_family) |v| {
        try buf.appendSlice(allocator, "<InstanceFamily>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</InstanceFamily>");
    }
}

pub fn serializePhase1DHGroupNumbersRequestListValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase1DHGroupNumbersRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializePhase1EncryptionAlgorithmsRequestListValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase1EncryptionAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializePhase1IntegrityAlgorithmsRequestListValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase1IntegrityAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializePhase2DHGroupNumbersRequestListValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase2DHGroupNumbersRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializePhase2EncryptionAlgorithmsRequestListValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase2EncryptionAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializePhase2IntegrityAlgorithmsRequestListValue(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase2IntegrityAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializePlacement(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Placement) !void {
    if (value.affinity) |v| {
        try buf.appendSlice(allocator, "<affinity>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</affinity>");
    }
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZoneId>");
    }
    if (value.group_id) |v| {
        try buf.appendSlice(allocator, "<groupId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</groupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(allocator, "<groupName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</groupName>");
    }
    if (value.host_id) |v| {
        try buf.appendSlice(allocator, "<hostId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</hostId>");
    }
    if (value.host_resource_group_arn) |v| {
        try buf.appendSlice(allocator, "<hostResourceGroupArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</hostResourceGroupArn>");
    }
    if (value.partition_number) |v| {
        try buf.appendSlice(allocator, "<partitionNumber>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</partitionNumber>");
    }
    if (value.spread_domain) |v| {
        try buf.appendSlice(allocator, "<spreadDomain>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</spreadDomain>");
    }
    if (value.tenancy) |v| {
        try buf.appendSlice(allocator, "<tenancy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</tenancy>");
    }
}

pub fn serializePortRange(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PortRange) !void {
    if (value.from) |v| {
        try buf.appendSlice(allocator, "<from>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</from>");
    }
    if (value.to) |v| {
        try buf.appendSlice(allocator, "<to>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</to>");
    }
}

pub fn serializePrefixListId(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PrefixListId) !void {
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</description>");
    }
    if (value.prefix_list_id) |v| {
        try buf.appendSlice(allocator, "<prefixListId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</prefixListId>");
    }
}

pub fn serializePriceScheduleSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PriceScheduleSpecification) !void {
    if (value.currency_code) |v| {
        try buf.appendSlice(allocator, "<currencyCode>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</currencyCode>");
    }
    if (value.price) |v| {
        try buf.appendSlice(allocator, "<price>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</price>");
    }
    if (value.term) |v| {
        try buf.appendSlice(allocator, "<term>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</term>");
    }
}

pub fn serializePrivateDnsNameOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PrivateDnsNameOptionsRequest) !void {
    if (value.enable_resource_name_dns_aaaa_record) |v| {
        try buf.appendSlice(allocator, "<EnableResourceNameDnsAAAARecord>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnableResourceNameDnsAAAARecord>");
    }
    if (value.enable_resource_name_dns_a_record) |v| {
        try buf.appendSlice(allocator, "<EnableResourceNameDnsARecord>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnableResourceNameDnsARecord>");
    }
    if (value.hostname_type) |v| {
        try buf.appendSlice(allocator, "<HostnameType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</HostnameType>");
    }
}

pub fn serializePrivateIpAddressSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PrivateIpAddressSpecification) !void {
    if (value.primary) |v| {
        try buf.appendSlice(allocator, "<primary>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</primary>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(allocator, "<privateIpAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</privateIpAddress>");
    }
}

pub fn serializePurchaseRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: PurchaseRequest) !void {
    try buf.appendSlice(allocator, "<InstanceCount>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.instance_count}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</InstanceCount>");
    try buf.appendSlice(allocator, "<PurchaseToken>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.purchase_token);
    try buf.appendSlice(allocator, "</PurchaseToken>");
}

pub fn serializeRegisterInstanceTagAttributeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RegisterInstanceTagAttributeRequest) !void {
    if (value.include_all_tags_of_instance) |v| {
        try buf.appendSlice(allocator, "<IncludeAllTagsOfInstance>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</IncludeAllTagsOfInstance>");
    }
    if (value.instance_tag_keys) |v| {
        try buf.appendSlice(allocator, "<InstanceTagKey>");
        try serializeInstanceTagKeySet(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</InstanceTagKey>");
    }
}

pub fn serializeRemoveIpamOperatingRegion(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RemoveIpamOperatingRegion) !void {
    if (value.region_name) |v| {
        try buf.appendSlice(allocator, "<RegionName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RegionName>");
    }
}

pub fn serializeRemoveIpamOrganizationalUnitExclusion(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RemoveIpamOrganizationalUnitExclusion) !void {
    if (value.organizations_entity_path) |v| {
        try buf.appendSlice(allocator, "<OrganizationsEntityPath>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</OrganizationsEntityPath>");
    }
}

pub fn serializeRemovePrefixListEntry(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RemovePrefixListEntry) !void {
    try buf.appendSlice(allocator, "<Cidr>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.cidr);
    try buf.appendSlice(allocator, "</Cidr>");
}

pub fn serializeRequestFilterPortRange(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestFilterPortRange) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(allocator, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(allocator, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ToPort>");
    }
}

pub fn serializeRequestIpamResourceTag(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestIpamResourceTag) !void {
    if (value.key) |v| {
        try buf.appendSlice(allocator, "<Key>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Key>");
    }
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializeRequestLaunchTemplateData(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestLaunchTemplateData) !void {
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(allocator, "<BlockDeviceMapping>");
        try serializeLaunchTemplateBlockDeviceMappingRequestList(allocator, buf, v, "BlockDeviceMapping");
        try buf.appendSlice(allocator, "</BlockDeviceMapping>");
    }
    if (value.capacity_reservation_specification) |v| {
        try buf.appendSlice(allocator, "<CapacityReservationSpecification>");
        try serializeLaunchTemplateCapacityReservationSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</CapacityReservationSpecification>");
    }
    if (value.cpu_options) |v| {
        try buf.appendSlice(allocator, "<CpuOptions>");
        try serializeLaunchTemplateCpuOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</CpuOptions>");
    }
    if (value.credit_specification) |v| {
        try buf.appendSlice(allocator, "<CreditSpecification>");
        try serializeCreditSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</CreditSpecification>");
    }
    if (value.disable_api_stop) |v| {
        try buf.appendSlice(allocator, "<DisableApiStop>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DisableApiStop>");
    }
    if (value.disable_api_termination) |v| {
        try buf.appendSlice(allocator, "<DisableApiTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DisableApiTermination>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(allocator, "<EbsOptimized>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EbsOptimized>");
    }
    if (value.elastic_gpu_specifications) |v| {
        try buf.appendSlice(allocator, "<ElasticGpuSpecification>");
        try serializeElasticGpuSpecificationList(allocator, buf, v, "ElasticGpuSpecification");
        try buf.appendSlice(allocator, "</ElasticGpuSpecification>");
    }
    if (value.elastic_inference_accelerators) |v| {
        try buf.appendSlice(allocator, "<ElasticInferenceAccelerator>");
        try serializeLaunchTemplateElasticInferenceAcceleratorList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ElasticInferenceAccelerator>");
    }
    if (value.enclave_options) |v| {
        try buf.appendSlice(allocator, "<EnclaveOptions>");
        try serializeLaunchTemplateEnclaveOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</EnclaveOptions>");
    }
    if (value.hibernation_options) |v| {
        try buf.appendSlice(allocator, "<HibernationOptions>");
        try serializeLaunchTemplateHibernationOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</HibernationOptions>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(allocator, "<IamInstanceProfile>");
        try serializeLaunchTemplateIamInstanceProfileSpecificationRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</IamInstanceProfile>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(allocator, "<ImageId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ImageId>");
    }
    if (value.instance_initiated_shutdown_behavior) |v| {
        try buf.appendSlice(allocator, "<InstanceInitiatedShutdownBehavior>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceInitiatedShutdownBehavior>");
    }
    if (value.instance_market_options) |v| {
        try buf.appendSlice(allocator, "<InstanceMarketOptions>");
        try serializeLaunchTemplateInstanceMarketOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</InstanceMarketOptions>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(allocator, "<InstanceRequirements>");
        try serializeInstanceRequirementsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</InstanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<InstanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(allocator, "<KernelId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</KernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(allocator, "<KeyName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</KeyName>");
    }
    if (value.license_specifications) |v| {
        try buf.appendSlice(allocator, "<LicenseSpecification>");
        try serializeLaunchTemplateLicenseSpecificationListRequest(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</LicenseSpecification>");
    }
    if (value.maintenance_options) |v| {
        try buf.appendSlice(allocator, "<MaintenanceOptions>");
        try serializeLaunchTemplateInstanceMaintenanceOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</MaintenanceOptions>");
    }
    if (value.metadata_options) |v| {
        try buf.appendSlice(allocator, "<MetadataOptions>");
        try serializeLaunchTemplateInstanceMetadataOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</MetadataOptions>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(allocator, "<Monitoring>");
        try serializeLaunchTemplatesMonitoringRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(allocator, "<NetworkInterface>");
        try serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequestList(allocator, buf, v, "InstanceNetworkInterfaceSpecification");
        try buf.appendSlice(allocator, "</NetworkInterface>");
    }
    if (value.network_performance_options) |v| {
        try buf.appendSlice(allocator, "<NetworkPerformanceOptions>");
        try serializeLaunchTemplateNetworkPerformanceOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</NetworkPerformanceOptions>");
    }
    if (value.operator) |v| {
        try buf.appendSlice(allocator, "<Operator>");
        try serializeOperatorRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Operator>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(allocator, "<Placement>");
        try serializeLaunchTemplatePlacementRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</Placement>");
    }
    if (value.private_dns_name_options) |v| {
        try buf.appendSlice(allocator, "<PrivateDnsNameOptions>");
        try serializeLaunchTemplatePrivateDnsNameOptionsRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</PrivateDnsNameOptions>");
    }
    if (value.ram_disk_id) |v| {
        try buf.appendSlice(allocator, "<RamDiskId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RamDiskId>");
    }
    if (value.security_group_ids) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupId>");
        try serializeSecurityGroupIdStringList(allocator, buf, v, "SecurityGroupId");
        try buf.appendSlice(allocator, "</SecurityGroupId>");
    }
    if (value.security_groups) |v| {
        try buf.appendSlice(allocator, "<SecurityGroup>");
        try serializeSecurityGroupStringList(allocator, buf, v, "SecurityGroup");
        try buf.appendSlice(allocator, "</SecurityGroup>");
    }
    if (value.tag_specifications) |v| {
        try buf.appendSlice(allocator, "<TagSpecification>");
        try serializeLaunchTemplateTagSpecificationRequestList(allocator, buf, v, "LaunchTemplateTagSpecificationRequest");
        try buf.appendSlice(allocator, "</TagSpecification>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(allocator, "<UserData>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserData>");
    }
}

pub fn serializeRequestSpotLaunchSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestSpotLaunchSpecification) !void {
    if (value.addressing_type) |v| {
        try buf.appendSlice(allocator, "<addressingType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</addressingType>");
    }
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(allocator, "<blockDeviceMapping>");
        try serializeBlockDeviceMappingList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</blockDeviceMapping>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(allocator, "<ebsOptimized>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</ebsOptimized>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(allocator, "<iamInstanceProfile>");
        try serializeIamInstanceProfileSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</iamInstanceProfile>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(allocator, "<imageId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</imageId>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<instanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</instanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(allocator, "<kernelId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</kernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(allocator, "<keyName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</keyName>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(allocator, "<monitoring>");
        try serializeRunInstancesMonitoringEnabled(allocator, buf, v);
        try buf.appendSlice(allocator, "</monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(allocator, "<NetworkInterface>");
        try serializeInstanceNetworkInterfaceSpecificationList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</NetworkInterface>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(allocator, "<placement>");
        try serializeSpotPlacement(allocator, buf, v);
        try buf.appendSlice(allocator, "</placement>");
    }
    if (value.ramdisk_id) |v| {
        try buf.appendSlice(allocator, "<ramdiskId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ramdiskId>");
    }
    if (value.security_group_ids) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupId>");
        try serializeRequestSpotLaunchSpecificationSecurityGroupIdList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SecurityGroupId>");
    }
    if (value.security_groups) |v| {
        try buf.appendSlice(allocator, "<SecurityGroup>");
        try serializeRequestSpotLaunchSpecificationSecurityGroupList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</SecurityGroup>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<subnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</subnetId>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(allocator, "<userData>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</userData>");
    }
}

pub fn serializeReservationFleetInstanceSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ReservationFleetInstanceSpecification) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZoneId>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(allocator, "<EbsOptimized>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EbsOptimized>");
    }
    if (value.instance_platform) |v| {
        try buf.appendSlice(allocator, "<InstancePlatform>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstancePlatform>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<InstanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceType>");
    }
    if (value.priority) |v| {
        try buf.appendSlice(allocator, "<Priority>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Priority>");
    }
    if (value.weight) |v| {
        try buf.appendSlice(allocator, "<Weight>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Weight>");
    }
}

pub fn serializeReservedInstanceLimitPrice(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ReservedInstanceLimitPrice) !void {
    if (value.amount) |v| {
        try buf.appendSlice(allocator, "<amount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</amount>");
    }
    if (value.currency_code) |v| {
        try buf.appendSlice(allocator, "<currencyCode>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</currencyCode>");
    }
}

pub fn serializeReservedInstancesConfiguration(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ReservedInstancesConfiguration) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZoneId>");
    }
    if (value.instance_count) |v| {
        try buf.appendSlice(allocator, "<instanceCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</instanceCount>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<instanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</instanceType>");
    }
    if (value.platform) |v| {
        try buf.appendSlice(allocator, "<platform>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</platform>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(allocator, "<scope>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</scope>");
    }
}

pub fn serializeResourceStatementRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ResourceStatementRequest) !void {
    if (value.resources) |v| {
        try buf.appendSlice(allocator, "<Resource>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Resource>");
    }
    if (value.resource_types) |v| {
        try buf.appendSlice(allocator, "<ResourceType>");
        try serializeValueStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</ResourceType>");
    }
}

pub fn serializeResourceTypeOption(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ResourceTypeOption) !void {
    if (value.option_name) |v| {
        try buf.appendSlice(allocator, "<OptionName>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</OptionName>");
    }
    if (value.option_values) |v| {
        try buf.appendSlice(allocator, "<OptionValue>");
        try serializeResourceTypeOptionValuesList(allocator, buf, v, "member");
        try buf.appendSlice(allocator, "</OptionValue>");
    }
}

pub fn serializeResourceTypeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ResourceTypeRequest) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(allocator, "<ResourceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ResourceType>");
    }
    if (value.resource_type_options) |v| {
        try buf.appendSlice(allocator, "<ResourceTypeOption>");
        try serializeResourceTypeOptionList(allocator, buf, v, "member");
        try buf.appendSlice(allocator, "</ResourceTypeOption>");
    }
}

pub fn serializeRouteServerBgpOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RouteServerBgpOptionsRequest) !void {
    try buf.appendSlice(allocator, "<PeerAsn>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.peer_asn}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</PeerAsn>");
    if (value.peer_liveness_detection) |v| {
        try buf.appendSlice(allocator, "<PeerLivenessDetection>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</PeerLivenessDetection>");
    }
}

pub fn serializeRunInstancesMonitoringEnabled(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: RunInstancesMonitoringEnabled) !void {
    try buf.appendSlice(allocator, "<enabled>");
    try buf.appendSlice(allocator, if (value.enabled) "true" else "false");
    try buf.appendSlice(allocator, "</enabled>");
}

pub fn serializeS3ObjectTag(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: S3ObjectTag) !void {
    if (value.key) |v| {
        try buf.appendSlice(allocator, "<Key>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Key>");
    }
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<Value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Value>");
    }
}

pub fn serializeS3Storage(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: S3Storage) !void {
    if (value.aws_access_key_id) |v| {
        try buf.appendSlice(allocator, "<AWSAccessKeyId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AWSAccessKeyId>");
    }
    if (value.bucket) |v| {
        try buf.appendSlice(allocator, "<bucket>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</bucket>");
    }
    if (value.prefix) |v| {
        try buf.appendSlice(allocator, "<prefix>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</prefix>");
    }
    if (value.upload_policy_signature) |v| {
        try buf.appendSlice(allocator, "<uploadPolicySignature>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</uploadPolicySignature>");
    }
}

pub fn serializeScheduledInstanceRecurrenceRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstanceRecurrenceRequest) !void {
    if (value.frequency) |v| {
        try buf.appendSlice(allocator, "<Frequency>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Frequency>");
    }
    if (value.interval) |v| {
        try buf.appendSlice(allocator, "<Interval>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Interval>");
    }
    if (value.occurrence_days) |v| {
        try buf.appendSlice(allocator, "<OccurrenceDay>");
        try serializeOccurrenceDayRequestSet(allocator, buf, v, "OccurenceDay");
        try buf.appendSlice(allocator, "</OccurrenceDay>");
    }
    if (value.occurrence_relative_to_end) |v| {
        try buf.appendSlice(allocator, "<OccurrenceRelativeToEnd>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</OccurrenceRelativeToEnd>");
    }
    if (value.occurrence_unit) |v| {
        try buf.appendSlice(allocator, "<OccurrenceUnit>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</OccurrenceUnit>");
    }
}

pub fn serializeScheduledInstancesBlockDeviceMapping(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesBlockDeviceMapping) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(allocator, "<DeviceName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DeviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(allocator, "<Ebs>");
        try serializeScheduledInstancesEbs(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(allocator, "<NoDevice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</NoDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(allocator, "<VirtualName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</VirtualName>");
    }
}

pub fn serializeScheduledInstancesEbs(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesEbs) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<DeleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DeleteOnTermination>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(allocator, "<Encrypted>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(allocator, "<Iops>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Iops>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(allocator, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SnapshotId>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(allocator, "<VolumeSize>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</VolumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(allocator, "<VolumeType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</VolumeType>");
    }
}

pub fn serializeScheduledInstancesIamInstanceProfile(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesIamInstanceProfile) !void {
    if (value.arn) |v| {
        try buf.appendSlice(allocator, "<Arn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Arn>");
    }
    if (value.name) |v| {
        try buf.appendSlice(allocator, "<Name>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Name>");
    }
}

pub fn serializeScheduledInstancesIpv6Address(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesIpv6Address) !void {
    if (value.ipv_6_address) |v| {
        try buf.appendSlice(allocator, "<Ipv6Address>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ipv6Address>");
    }
}

pub fn serializeScheduledInstancesLaunchSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesLaunchSpecification) !void {
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(allocator, "<BlockDeviceMapping>");
        try serializeScheduledInstancesBlockDeviceMappingSet(allocator, buf, v, "BlockDeviceMapping");
        try buf.appendSlice(allocator, "</BlockDeviceMapping>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(allocator, "<EbsOptimized>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EbsOptimized>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(allocator, "<IamInstanceProfile>");
        try serializeScheduledInstancesIamInstanceProfile(allocator, buf, v);
        try buf.appendSlice(allocator, "</IamInstanceProfile>");
    }
    try buf.appendSlice(allocator, "<ImageId>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.image_id);
    try buf.appendSlice(allocator, "</ImageId>");
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<InstanceType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</InstanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(allocator, "<KernelId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</KernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(allocator, "<KeyName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</KeyName>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(allocator, "<Monitoring>");
        try serializeScheduledInstancesMonitoring(allocator, buf, v);
        try buf.appendSlice(allocator, "</Monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(allocator, "<NetworkInterface>");
        try serializeScheduledInstancesNetworkInterfaceSet(allocator, buf, v, "NetworkInterface");
        try buf.appendSlice(allocator, "</NetworkInterface>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(allocator, "<Placement>");
        try serializeScheduledInstancesPlacement(allocator, buf, v);
        try buf.appendSlice(allocator, "</Placement>");
    }
    if (value.ramdisk_id) |v| {
        try buf.appendSlice(allocator, "<RamdiskId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RamdiskId>");
    }
    if (value.security_group_ids) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupId>");
        try serializeScheduledInstancesSecurityGroupIdSet(allocator, buf, v, "SecurityGroupId");
        try buf.appendSlice(allocator, "</SecurityGroupId>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SubnetId>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(allocator, "<UserData>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserData>");
    }
}

pub fn serializeScheduledInstancesMonitoring(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesMonitoring) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<Enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Enabled>");
    }
}

pub fn serializeScheduledInstancesNetworkInterface(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesNetworkInterface) !void {
    if (value.associate_public_ip_address) |v| {
        try buf.appendSlice(allocator, "<AssociatePublicIpAddress>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</AssociatePublicIpAddress>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(allocator, "<DeleteOnTermination>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</DeleteOnTermination>");
    }
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
    if (value.device_index) |v| {
        try buf.appendSlice(allocator, "<DeviceIndex>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</DeviceIndex>");
    }
    if (value.groups) |v| {
        try buf.appendSlice(allocator, "<Group>");
        try serializeScheduledInstancesSecurityGroupIdSet(allocator, buf, v, "SecurityGroupId");
        try buf.appendSlice(allocator, "</Group>");
    }
    if (value.ipv_6_address_count) |v| {
        try buf.appendSlice(allocator, "<Ipv6AddressCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Ipv6AddressCount>");
    }
    if (value.ipv_6_addresses) |v| {
        try buf.appendSlice(allocator, "<Ipv6Address>");
        try serializeScheduledInstancesIpv6AddressList(allocator, buf, v, "Ipv6Address");
        try buf.appendSlice(allocator, "</Ipv6Address>");
    }
    if (value.network_interface_id) |v| {
        try buf.appendSlice(allocator, "<NetworkInterfaceId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</NetworkInterfaceId>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(allocator, "<PrivateIpAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PrivateIpAddress>");
    }
    if (value.private_ip_address_configs) |v| {
        try buf.appendSlice(allocator, "<PrivateIpAddressConfig>");
        try serializePrivateIpAddressConfigSet(allocator, buf, v, "PrivateIpAddressConfigSet");
        try buf.appendSlice(allocator, "</PrivateIpAddressConfig>");
    }
    if (value.secondary_private_ip_address_count) |v| {
        try buf.appendSlice(allocator, "<SecondaryPrivateIpAddressCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</SecondaryPrivateIpAddressCount>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeScheduledInstancesPlacement(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesPlacement) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</AvailabilityZone>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(allocator, "<GroupName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</GroupName>");
    }
}

pub fn serializeScheduledInstancesPrivateIpAddressConfig(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesPrivateIpAddressConfig) !void {
    if (value.primary) |v| {
        try buf.appendSlice(allocator, "<Primary>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</Primary>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(allocator, "<PrivateIpAddress>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PrivateIpAddress>");
    }
}

pub fn serializeSecurityGroupRuleDescription(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SecurityGroupRuleDescription) !void {
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
    if (value.security_group_rule_id) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupRuleId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SecurityGroupRuleId>");
    }
}

pub fn serializeSecurityGroupRuleRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SecurityGroupRuleRequest) !void {
    if (value.cidr_ipv_4) |v| {
        try buf.appendSlice(allocator, "<CidrIpv4>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</CidrIpv4>");
    }
    if (value.cidr_ipv_6) |v| {
        try buf.appendSlice(allocator, "<CidrIpv6>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</CidrIpv6>");
    }
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
    if (value.from_port) |v| {
        try buf.appendSlice(allocator, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</FromPort>");
    }
    if (value.ip_protocol) |v| {
        try buf.appendSlice(allocator, "<IpProtocol>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</IpProtocol>");
    }
    if (value.prefix_list_id) |v| {
        try buf.appendSlice(allocator, "<PrefixListId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PrefixListId>");
    }
    if (value.referenced_group_id) |v| {
        try buf.appendSlice(allocator, "<ReferencedGroupId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ReferencedGroupId>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(allocator, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ToPort>");
    }
}

pub fn serializeSecurityGroupRuleUpdate(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SecurityGroupRuleUpdate) !void {
    if (value.security_group_rule) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupRule>");
        try serializeSecurityGroupRuleRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</SecurityGroupRule>");
    }
    try buf.appendSlice(allocator, "<SecurityGroupRuleId>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.security_group_rule_id);
    try buf.appendSlice(allocator, "</SecurityGroupRuleId>");
}

pub fn serializeSlotDateTimeRangeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SlotDateTimeRangeRequest) !void {
    try buf.appendSlice(allocator, "<EarliestTime>");
    {
        const ts_str = std.fmt.allocPrint(allocator, "{d}", .{value.earliest_time}) catch "";
        try buf.appendSlice(allocator, ts_str);
    }
    try buf.appendSlice(allocator, "</EarliestTime>");
    try buf.appendSlice(allocator, "<LatestTime>");
    {
        const ts_str = std.fmt.allocPrint(allocator, "{d}", .{value.latest_time}) catch "";
        try buf.appendSlice(allocator, ts_str);
    }
    try buf.appendSlice(allocator, "</LatestTime>");
}

pub fn serializeSlotStartTimeRangeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SlotStartTimeRangeRequest) !void {
    if (value.earliest_time) |v| {
        try buf.appendSlice(allocator, "<EarliestTime>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</EarliestTime>");
    }
    if (value.latest_time) |v| {
        try buf.appendSlice(allocator, "<LatestTime>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</LatestTime>");
    }
}

pub fn serializeSnapshotDiskContainer(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SnapshotDiskContainer) !void {
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<Description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Description>");
    }
    if (value.format) |v| {
        try buf.appendSlice(allocator, "<Format>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Format>");
    }
    if (value.url) |v| {
        try buf.appendSlice(allocator, "<Url>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Url>");
    }
    if (value.user_bucket) |v| {
        try buf.appendSlice(allocator, "<UserBucket>");
        try serializeUserBucket(allocator, buf, v);
        try buf.appendSlice(allocator, "</UserBucket>");
    }
}

pub fn serializeSpotCapacityRebalance(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotCapacityRebalance) !void {
    if (value.replacement_strategy) |v| {
        try buf.appendSlice(allocator, "<replacementStrategy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</replacementStrategy>");
    }
    if (value.termination_delay) |v| {
        try buf.appendSlice(allocator, "<terminationDelay>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</terminationDelay>");
    }
}

pub fn serializeSpotFleetLaunchSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetLaunchSpecification) !void {
    if (value.addressing_type) |v| {
        try buf.appendSlice(allocator, "<addressingType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</addressingType>");
    }
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(allocator, "<blockDeviceMapping>");
        try serializeBlockDeviceMappingList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</blockDeviceMapping>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(allocator, "<ebsOptimized>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</ebsOptimized>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(allocator, "<iamInstanceProfile>");
        try serializeIamInstanceProfileSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</iamInstanceProfile>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(allocator, "<imageId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</imageId>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(allocator, "<instanceRequirements>");
        try serializeInstanceRequirements(allocator, buf, v);
        try buf.appendSlice(allocator, "</instanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(allocator, "<instanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</instanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(allocator, "<kernelId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</kernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(allocator, "<keyName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</keyName>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(allocator, "<monitoring>");
        try serializeSpotFleetMonitoring(allocator, buf, v);
        try buf.appendSlice(allocator, "</monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(allocator, "<networkInterfaceSet>");
        try serializeInstanceNetworkInterfaceSpecificationList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</networkInterfaceSet>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(allocator, "<placement>");
        try serializeSpotPlacement(allocator, buf, v);
        try buf.appendSlice(allocator, "</placement>");
    }
    if (value.ramdisk_id) |v| {
        try buf.appendSlice(allocator, "<ramdiskId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</ramdiskId>");
    }
    if (value.security_groups) |v| {
        try buf.appendSlice(allocator, "<groupSet>");
        try serializeGroupIdentifierList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</groupSet>");
    }
    if (value.spot_price) |v| {
        try buf.appendSlice(allocator, "<spotPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</spotPrice>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<subnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</subnetId>");
    }
    if (value.tag_specifications) |v| {
        try buf.appendSlice(allocator, "<tagSpecificationSet>");
        try serializeSpotFleetTagSpecificationList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</tagSpecificationSet>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(allocator, "<userData>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</userData>");
    }
    if (value.weighted_capacity) |v| {
        try buf.appendSlice(allocator, "<weightedCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</weightedCapacity>");
    }
}

pub fn serializeSpotFleetMonitoring(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetMonitoring) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(allocator, "<enabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</enabled>");
    }
}

pub fn serializeSpotFleetRequestConfigData(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetRequestConfigData) !void {
    if (value.allocation_strategy) |v| {
        try buf.appendSlice(allocator, "<allocationStrategy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</allocationStrategy>");
    }
    if (value.client_token) |v| {
        try buf.appendSlice(allocator, "<clientToken>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</clientToken>");
    }
    if (value.context) |v| {
        try buf.appendSlice(allocator, "<context>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</context>");
    }
    if (value.excess_capacity_termination_policy) |v| {
        try buf.appendSlice(allocator, "<excessCapacityTerminationPolicy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</excessCapacityTerminationPolicy>");
    }
    if (value.fulfilled_capacity) |v| {
        try buf.appendSlice(allocator, "<fulfilledCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</fulfilledCapacity>");
    }
    try buf.appendSlice(allocator, "<iamFleetRole>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.iam_fleet_role);
    try buf.appendSlice(allocator, "</iamFleetRole>");
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(allocator, "<instanceInterruptionBehavior>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</instanceInterruptionBehavior>");
    }
    if (value.instance_pools_to_use_count) |v| {
        try buf.appendSlice(allocator, "<instancePoolsToUseCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</instancePoolsToUseCount>");
    }
    if (value.launch_specifications) |v| {
        try buf.appendSlice(allocator, "<launchSpecifications>");
        try serializeLaunchSpecsList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</launchSpecifications>");
    }
    if (value.launch_template_configs) |v| {
        try buf.appendSlice(allocator, "<launchTemplateConfigs>");
        try serializeLaunchTemplateConfigList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</launchTemplateConfigs>");
    }
    if (value.load_balancers_config) |v| {
        try buf.appendSlice(allocator, "<loadBalancersConfig>");
        try serializeLoadBalancersConfig(allocator, buf, v);
        try buf.appendSlice(allocator, "</loadBalancersConfig>");
    }
    if (value.on_demand_allocation_strategy) |v| {
        try buf.appendSlice(allocator, "<onDemandAllocationStrategy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</onDemandAllocationStrategy>");
    }
    if (value.on_demand_fulfilled_capacity) |v| {
        try buf.appendSlice(allocator, "<onDemandFulfilledCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</onDemandFulfilledCapacity>");
    }
    if (value.on_demand_max_total_price) |v| {
        try buf.appendSlice(allocator, "<onDemandMaxTotalPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</onDemandMaxTotalPrice>");
    }
    if (value.on_demand_target_capacity) |v| {
        try buf.appendSlice(allocator, "<onDemandTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</onDemandTargetCapacity>");
    }
    if (value.replace_unhealthy_instances) |v| {
        try buf.appendSlice(allocator, "<replaceUnhealthyInstances>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</replaceUnhealthyInstances>");
    }
    if (value.spot_maintenance_strategies) |v| {
        try buf.appendSlice(allocator, "<spotMaintenanceStrategies>");
        try serializeSpotMaintenanceStrategies(allocator, buf, v);
        try buf.appendSlice(allocator, "</spotMaintenanceStrategies>");
    }
    if (value.spot_max_total_price) |v| {
        try buf.appendSlice(allocator, "<spotMaxTotalPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</spotMaxTotalPrice>");
    }
    if (value.spot_price) |v| {
        try buf.appendSlice(allocator, "<spotPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</spotPrice>");
    }
    if (value.tag_specifications) |v| {
        try buf.appendSlice(allocator, "<TagSpecification>");
        try serializeTagSpecificationList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</TagSpecification>");
    }
    try buf.appendSlice(allocator, "<targetCapacity>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.target_capacity}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</targetCapacity>");
    if (value.target_capacity_unit_type) |v| {
        try buf.appendSlice(allocator, "<targetCapacityUnitType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</targetCapacityUnitType>");
    }
    if (value.terminate_instances_with_expiration) |v| {
        try buf.appendSlice(allocator, "<terminateInstancesWithExpiration>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</terminateInstancesWithExpiration>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(allocator, "<type>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</type>");
    }
    if (value.valid_from) |v| {
        try buf.appendSlice(allocator, "<validFrom>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</validFrom>");
    }
    if (value.valid_until) |v| {
        try buf.appendSlice(allocator, "<validUntil>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</validUntil>");
    }
}

pub fn serializeSpotFleetTagSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetTagSpecification) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(allocator, "<resourceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</resourceType>");
    }
    if (value.tags) |v| {
        try buf.appendSlice(allocator, "<tag>");
        try serializeTagList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</tag>");
    }
}

pub fn serializeSpotMaintenanceStrategies(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotMaintenanceStrategies) !void {
    if (value.capacity_rebalance) |v| {
        try buf.appendSlice(allocator, "<capacityRebalance>");
        try serializeSpotCapacityRebalance(allocator, buf, v);
        try buf.appendSlice(allocator, "</capacityRebalance>");
    }
}

pub fn serializeSpotMarketOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotMarketOptions) !void {
    if (value.block_duration_minutes) |v| {
        try buf.appendSlice(allocator, "<BlockDurationMinutes>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</BlockDurationMinutes>");
    }
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(allocator, "<InstanceInterruptionBehavior>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceInterruptionBehavior>");
    }
    if (value.max_price) |v| {
        try buf.appendSlice(allocator, "<MaxPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</MaxPrice>");
    }
    if (value.spot_instance_type) |v| {
        try buf.appendSlice(allocator, "<SpotInstanceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</SpotInstanceType>");
    }
    if (value.valid_until) |v| {
        try buf.appendSlice(allocator, "<ValidUntil>");
        {
            const ts_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, ts_str);
        }
        try buf.appendSlice(allocator, "</ValidUntil>");
    }
}

pub fn serializeSpotOptionsRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotOptionsRequest) !void {
    if (value.allocation_strategy) |v| {
        try buf.appendSlice(allocator, "<AllocationStrategy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AllocationStrategy>");
    }
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(allocator, "<InstanceInterruptionBehavior>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InstanceInterruptionBehavior>");
    }
    if (value.instance_pools_to_use_count) |v| {
        try buf.appendSlice(allocator, "<InstancePoolsToUseCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</InstancePoolsToUseCount>");
    }
    if (value.maintenance_strategies) |v| {
        try buf.appendSlice(allocator, "<MaintenanceStrategies>");
        try serializeFleetSpotMaintenanceStrategiesRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</MaintenanceStrategies>");
    }
    if (value.max_total_price) |v| {
        try buf.appendSlice(allocator, "<MaxTotalPrice>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</MaxTotalPrice>");
    }
    if (value.min_target_capacity) |v| {
        try buf.appendSlice(allocator, "<MinTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</MinTargetCapacity>");
    }
    if (value.single_availability_zone) |v| {
        try buf.appendSlice(allocator, "<SingleAvailabilityZone>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</SingleAvailabilityZone>");
    }
    if (value.single_instance_type) |v| {
        try buf.appendSlice(allocator, "<SingleInstanceType>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</SingleInstanceType>");
    }
}

pub fn serializeSpotPlacement(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotPlacement) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(allocator, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(allocator, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</availabilityZoneId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(allocator, "<groupName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</groupName>");
    }
    if (value.tenancy) |v| {
        try buf.appendSlice(allocator, "<tenancy>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</tenancy>");
    }
}

pub fn serializeStorage(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Storage) !void {
    if (value.s3) |v| {
        try buf.appendSlice(allocator, "<S3>");
        try serializeS3Storage(allocator, buf, v);
        try buf.appendSlice(allocator, "</S3>");
    }
}

pub fn serializeStorageLocation(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: StorageLocation) !void {
    if (value.bucket) |v| {
        try buf.appendSlice(allocator, "<Bucket>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Bucket>");
    }
    if (value.key) |v| {
        try buf.appendSlice(allocator, "<Key>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Key>");
    }
}

pub fn serializeSubnetConfiguration(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: SubnetConfiguration) !void {
    if (value.ipv_4) |v| {
        try buf.appendSlice(allocator, "<Ipv4>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ipv4>");
    }
    if (value.ipv_6) |v| {
        try buf.appendSlice(allocator, "<Ipv6>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Ipv6>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(allocator, "<SubnetId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</SubnetId>");
    }
}

pub fn serializeTag(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: Tag) !void {
    if (value.key) |v| {
        try buf.appendSlice(allocator, "<key>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</key>");
    }
    if (value.value) |v| {
        try buf.appendSlice(allocator, "<value>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</value>");
    }
}

pub fn serializeTagSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TagSpecification) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(allocator, "<resourceType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</resourceType>");
    }
    if (value.tags) |v| {
        try buf.appendSlice(allocator, "<Tag>");
        try serializeTagList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Tag>");
    }
}

pub fn serializeTargetCapacitySpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetCapacitySpecificationRequest) !void {
    if (value.default_target_capacity_type) |v| {
        try buf.appendSlice(allocator, "<DefaultTargetCapacityType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DefaultTargetCapacityType>");
    }
    if (value.on_demand_target_capacity) |v| {
        try buf.appendSlice(allocator, "<OnDemandTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</OnDemandTargetCapacity>");
    }
    if (value.spot_target_capacity) |v| {
        try buf.appendSlice(allocator, "<SpotTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</SpotTargetCapacity>");
    }
    if (value.target_capacity_unit_type) |v| {
        try buf.appendSlice(allocator, "<TargetCapacityUnitType>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</TargetCapacityUnitType>");
    }
    try buf.appendSlice(allocator, "<TotalTargetCapacity>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.total_target_capacity}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</TotalTargetCapacity>");
}

pub fn serializeTargetConfigurationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetConfigurationRequest) !void {
    if (value.instance_count) |v| {
        try buf.appendSlice(allocator, "<InstanceCount>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</InstanceCount>");
    }
    try buf.appendSlice(allocator, "<OfferingId>");
    try aws.xml.appendXmlEscaped(allocator, buf, value.offering_id);
    try buf.appendSlice(allocator, "</OfferingId>");
}

pub fn serializeTargetGroup(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetGroup) !void {
    if (value.arn) |v| {
        try buf.appendSlice(allocator, "<arn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</arn>");
    }
}

pub fn serializeTargetGroupsConfig(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetGroupsConfig) !void {
    if (value.target_groups) |v| {
        try buf.appendSlice(allocator, "<targetGroups>");
        try serializeTargetGroups(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</targetGroups>");
    }
}

pub fn serializeThroughResourcesStatementRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: ThroughResourcesStatementRequest) !void {
    if (value.resource_statement) |v| {
        try buf.appendSlice(allocator, "<ResourceStatement>");
        try serializeResourceStatementRequest(allocator, buf, v);
        try buf.appendSlice(allocator, "</ResourceStatement>");
    }
}

pub fn serializeTotalLocalStorageGB(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TotalLocalStorageGB) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeTotalLocalStorageGBRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TotalLocalStorageGBRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<Min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Min>");
    }
}

pub fn serializeTrafficMirrorPortRangeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TrafficMirrorPortRangeRequest) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(allocator, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(allocator, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ToPort>");
    }
}

pub fn serializeTransitGatewayConnectRequestBgpOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TransitGatewayConnectRequestBgpOptions) !void {
    if (value.peer_asn) |v| {
        try buf.appendSlice(allocator, "<PeerAsn>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</PeerAsn>");
    }
}

pub fn serializeTransitGatewayRequestOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: TransitGatewayRequestOptions) !void {
    if (value.amazon_side_asn) |v| {
        try buf.appendSlice(allocator, "<AmazonSideAsn>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</AmazonSideAsn>");
    }
    if (value.auto_accept_shared_attachments) |v| {
        try buf.appendSlice(allocator, "<AutoAcceptSharedAttachments>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</AutoAcceptSharedAttachments>");
    }
    if (value.default_route_table_association) |v| {
        try buf.appendSlice(allocator, "<DefaultRouteTableAssociation>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DefaultRouteTableAssociation>");
    }
    if (value.default_route_table_propagation) |v| {
        try buf.appendSlice(allocator, "<DefaultRouteTablePropagation>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DefaultRouteTablePropagation>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(allocator, "<DnsSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</DnsSupport>");
    }
    if (value.multicast_support) |v| {
        try buf.appendSlice(allocator, "<MulticastSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</MulticastSupport>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(allocator, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</SecurityGroupReferencingSupport>");
    }
    if (value.transit_gateway_cidr_blocks) |v| {
        try buf.appendSlice(allocator, "<TransitGatewayCidrBlocks>");
        try serializeTransitGatewayCidrBlockStringList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</TransitGatewayCidrBlocks>");
    }
    if (value.vpn_ecmp_support) |v| {
        try buf.appendSlice(allocator, "<VpnEcmpSupport>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</VpnEcmpSupport>");
    }
}

pub fn serializeUserBucket(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: UserBucket) !void {
    if (value.s3_bucket) |v| {
        try buf.appendSlice(allocator, "<S3Bucket>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</S3Bucket>");
    }
    if (value.s3_key) |v| {
        try buf.appendSlice(allocator, "<S3Key>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</S3Key>");
    }
}

pub fn serializeUserData(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: UserData) !void {
    if (value.data) |v| {
        try buf.appendSlice(allocator, "<data>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</data>");
    }
}

pub fn serializeUserIdGroupPair(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: UserIdGroupPair) !void {
    if (value.description) |v| {
        try buf.appendSlice(allocator, "<description>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</description>");
    }
    if (value.group_id) |v| {
        try buf.appendSlice(allocator, "<groupId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</groupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(allocator, "<groupName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</groupName>");
    }
    if (value.peering_status) |v| {
        try buf.appendSlice(allocator, "<peeringStatus>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</peeringStatus>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(allocator, "<userId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</userId>");
    }
    if (value.vpc_id) |v| {
        try buf.appendSlice(allocator, "<vpcId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</vpcId>");
    }
    if (value.vpc_peering_connection_id) |v| {
        try buf.appendSlice(allocator, "<vpcPeeringConnectionId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</vpcPeeringConnectionId>");
    }
}

pub fn serializeVCpuCountRange(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VCpuCountRange) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(allocator, "<min>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</min>");
    }
}

pub fn serializeVCpuCountRangeRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VCpuCountRangeRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(allocator, "<Max>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Max>");
    }
    try buf.appendSlice(allocator, "<Min>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.min}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</Min>");
}

pub fn serializeVerifiedAccessLogCloudWatchLogsDestinationOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogCloudWatchLogsDestinationOptions) !void {
    try buf.appendSlice(allocator, "<Enabled>");
    try buf.appendSlice(allocator, if (value.enabled) "true" else "false");
    try buf.appendSlice(allocator, "</Enabled>");
    if (value.log_group) |v| {
        try buf.appendSlice(allocator, "<LogGroup>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LogGroup>");
    }
}

pub fn serializeVerifiedAccessLogKinesisDataFirehoseDestinationOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogKinesisDataFirehoseDestinationOptions) !void {
    if (value.delivery_stream) |v| {
        try buf.appendSlice(allocator, "<DeliveryStream>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DeliveryStream>");
    }
    try buf.appendSlice(allocator, "<Enabled>");
    try buf.appendSlice(allocator, if (value.enabled) "true" else "false");
    try buf.appendSlice(allocator, "</Enabled>");
}

pub fn serializeVerifiedAccessLogOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogOptions) !void {
    if (value.cloud_watch_logs) |v| {
        try buf.appendSlice(allocator, "<CloudWatchLogs>");
        try serializeVerifiedAccessLogCloudWatchLogsDestinationOptions(allocator, buf, v);
        try buf.appendSlice(allocator, "</CloudWatchLogs>");
    }
    if (value.include_trust_context) |v| {
        try buf.appendSlice(allocator, "<IncludeTrustContext>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</IncludeTrustContext>");
    }
    if (value.kinesis_data_firehose) |v| {
        try buf.appendSlice(allocator, "<KinesisDataFirehose>");
        try serializeVerifiedAccessLogKinesisDataFirehoseDestinationOptions(allocator, buf, v);
        try buf.appendSlice(allocator, "</KinesisDataFirehose>");
    }
    if (value.log_version) |v| {
        try buf.appendSlice(allocator, "<LogVersion>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LogVersion>");
    }
    if (value.s3) |v| {
        try buf.appendSlice(allocator, "<S3>");
        try serializeVerifiedAccessLogS3DestinationOptions(allocator, buf, v);
        try buf.appendSlice(allocator, "</S3>");
    }
}

pub fn serializeVerifiedAccessLogS3DestinationOptions(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogS3DestinationOptions) !void {
    if (value.bucket_name) |v| {
        try buf.appendSlice(allocator, "<BucketName>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</BucketName>");
    }
    if (value.bucket_owner) |v| {
        try buf.appendSlice(allocator, "<BucketOwner>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</BucketOwner>");
    }
    try buf.appendSlice(allocator, "<Enabled>");
    try buf.appendSlice(allocator, if (value.enabled) "true" else "false");
    try buf.appendSlice(allocator, "</Enabled>");
    if (value.prefix) |v| {
        try buf.appendSlice(allocator, "<Prefix>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</Prefix>");
    }
}

pub fn serializeVerifiedAccessSseSpecificationRequest(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessSseSpecificationRequest) !void {
    if (value.customer_managed_key_enabled) |v| {
        try buf.appendSlice(allocator, "<CustomerManagedKeyEnabled>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</CustomerManagedKeyEnabled>");
    }
    if (value.kms_key_arn) |v| {
        try buf.appendSlice(allocator, "<KmsKeyArn>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</KmsKeyArn>");
    }
}

pub fn serializeVolumeDetail(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VolumeDetail) !void {
    try buf.appendSlice(allocator, "<size>");
    {
        const num_str = std.fmt.allocPrint(allocator, "{d}", .{value.size}) catch "";
        try buf.appendSlice(allocator, num_str);
    }
    try buf.appendSlice(allocator, "</size>");
}

pub fn serializeVpcEncryptionControlConfiguration(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpcEncryptionControlConfiguration) !void {
    if (value.egress_only_internet_gateway_exclusion) |v| {
        try buf.appendSlice(allocator, "<EgressOnlyInternetGatewayExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</EgressOnlyInternetGatewayExclusion>");
    }
    if (value.elastic_file_system_exclusion) |v| {
        try buf.appendSlice(allocator, "<ElasticFileSystemExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</ElasticFileSystemExclusion>");
    }
    if (value.internet_gateway_exclusion) |v| {
        try buf.appendSlice(allocator, "<InternetGatewayExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</InternetGatewayExclusion>");
    }
    if (value.lambda_exclusion) |v| {
        try buf.appendSlice(allocator, "<LambdaExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</LambdaExclusion>");
    }
    try buf.appendSlice(allocator, "<Mode>");
    try buf.appendSlice(allocator, value.mode.wireName());
    try buf.appendSlice(allocator, "</Mode>");
    if (value.nat_gateway_exclusion) |v| {
        try buf.appendSlice(allocator, "<NatGatewayExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</NatGatewayExclusion>");
    }
    if (value.virtual_private_gateway_exclusion) |v| {
        try buf.appendSlice(allocator, "<VirtualPrivateGatewayExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</VirtualPrivateGatewayExclusion>");
    }
    if (value.vpc_lattice_exclusion) |v| {
        try buf.appendSlice(allocator, "<VpcLatticeExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</VpcLatticeExclusion>");
    }
    if (value.vpc_peering_exclusion) |v| {
        try buf.appendSlice(allocator, "<VpcPeeringExclusion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</VpcPeeringExclusion>");
    }
}

pub fn serializeVpnConnectionOptionsSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpnConnectionOptionsSpecification) !void {
    if (value.enable_acceleration) |v| {
        try buf.appendSlice(allocator, "<EnableAcceleration>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnableAcceleration>");
    }
    if (value.local_ipv_4_network_cidr) |v| {
        try buf.appendSlice(allocator, "<LocalIpv4NetworkCidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LocalIpv4NetworkCidr>");
    }
    if (value.local_ipv_6_network_cidr) |v| {
        try buf.appendSlice(allocator, "<LocalIpv6NetworkCidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</LocalIpv6NetworkCidr>");
    }
    if (value.outside_ip_address_type) |v| {
        try buf.appendSlice(allocator, "<OutsideIpAddressType>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</OutsideIpAddressType>");
    }
    if (value.remote_ipv_4_network_cidr) |v| {
        try buf.appendSlice(allocator, "<RemoteIpv4NetworkCidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RemoteIpv4NetworkCidr>");
    }
    if (value.remote_ipv_6_network_cidr) |v| {
        try buf.appendSlice(allocator, "<RemoteIpv6NetworkCidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</RemoteIpv6NetworkCidr>");
    }
    if (value.static_routes_only) |v| {
        try buf.appendSlice(allocator, "<staticRoutesOnly>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</staticRoutesOnly>");
    }
    if (value.transport_transit_gateway_attachment_id) |v| {
        try buf.appendSlice(allocator, "<TransportTransitGatewayAttachmentId>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TransportTransitGatewayAttachmentId>");
    }
    if (value.tunnel_bandwidth) |v| {
        try buf.appendSlice(allocator, "<TunnelBandwidth>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</TunnelBandwidth>");
    }
    if (value.tunnel_inside_ip_version) |v| {
        try buf.appendSlice(allocator, "<TunnelInsideIpVersion>");
        try buf.appendSlice(allocator, v.wireName());
        try buf.appendSlice(allocator, "</TunnelInsideIpVersion>");
    }
    if (value.tunnel_options) |v| {
        try buf.appendSlice(allocator, "<TunnelOptions>");
        try serializeVpnTunnelOptionsSpecificationsList(allocator, buf, v, "member");
        try buf.appendSlice(allocator, "</TunnelOptions>");
    }
}

pub fn serializeVpnTunnelLogOptionsSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpnTunnelLogOptionsSpecification) !void {
    if (value.cloud_watch_log_options) |v| {
        try buf.appendSlice(allocator, "<CloudWatchLogOptions>");
        try serializeCloudWatchLogOptionsSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</CloudWatchLogOptions>");
    }
}

pub fn serializeVpnTunnelOptionsSpecification(allocator: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpnTunnelOptionsSpecification) !void {
    if (value.dpd_timeout_action) |v| {
        try buf.appendSlice(allocator, "<DPDTimeoutAction>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</DPDTimeoutAction>");
    }
    if (value.dpd_timeout_seconds) |v| {
        try buf.appendSlice(allocator, "<DPDTimeoutSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</DPDTimeoutSeconds>");
    }
    if (value.enable_tunnel_lifecycle_control) |v| {
        try buf.appendSlice(allocator, "<EnableTunnelLifecycleControl>");
        try buf.appendSlice(allocator, if (v) "true" else "false");
        try buf.appendSlice(allocator, "</EnableTunnelLifecycleControl>");
    }
    if (value.ike_versions) |v| {
        try buf.appendSlice(allocator, "<IKEVersion>");
        try serializeIKEVersionsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</IKEVersion>");
    }
    if (value.log_options) |v| {
        try buf.appendSlice(allocator, "<LogOptions>");
        try serializeVpnTunnelLogOptionsSpecification(allocator, buf, v);
        try buf.appendSlice(allocator, "</LogOptions>");
    }
    if (value.phase_1_dh_group_numbers) |v| {
        try buf.appendSlice(allocator, "<Phase1DHGroupNumber>");
        try serializePhase1DHGroupNumbersRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase1DHGroupNumber>");
    }
    if (value.phase_1_encryption_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase1EncryptionAlgorithm>");
        try serializePhase1EncryptionAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase1EncryptionAlgorithm>");
    }
    if (value.phase_1_integrity_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase1IntegrityAlgorithm>");
        try serializePhase1IntegrityAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase1IntegrityAlgorithm>");
    }
    if (value.phase_1_lifetime_seconds) |v| {
        try buf.appendSlice(allocator, "<Phase1LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Phase1LifetimeSeconds>");
    }
    if (value.phase_2_dh_group_numbers) |v| {
        try buf.appendSlice(allocator, "<Phase2DHGroupNumber>");
        try serializePhase2DHGroupNumbersRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase2DHGroupNumber>");
    }
    if (value.phase_2_encryption_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase2EncryptionAlgorithm>");
        try serializePhase2EncryptionAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase2EncryptionAlgorithm>");
    }
    if (value.phase_2_integrity_algorithms) |v| {
        try buf.appendSlice(allocator, "<Phase2IntegrityAlgorithm>");
        try serializePhase2IntegrityAlgorithmsRequestList(allocator, buf, v, "item");
        try buf.appendSlice(allocator, "</Phase2IntegrityAlgorithm>");
    }
    if (value.phase_2_lifetime_seconds) |v| {
        try buf.appendSlice(allocator, "<Phase2LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</Phase2LifetimeSeconds>");
    }
    if (value.pre_shared_key) |v| {
        try buf.appendSlice(allocator, "<PreSharedKey>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</PreSharedKey>");
    }
    if (value.rekey_fuzz_percentage) |v| {
        try buf.appendSlice(allocator, "<RekeyFuzzPercentage>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</RekeyFuzzPercentage>");
    }
    if (value.rekey_margin_time_seconds) |v| {
        try buf.appendSlice(allocator, "<RekeyMarginTimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</RekeyMarginTimeSeconds>");
    }
    if (value.replay_window_size) |v| {
        try buf.appendSlice(allocator, "<ReplayWindowSize>");
        {
            const num_str = std.fmt.allocPrint(allocator, "{d}", .{v}) catch "";
            try buf.appendSlice(allocator, num_str);
        }
        try buf.appendSlice(allocator, "</ReplayWindowSize>");
    }
    if (value.startup_action) |v| {
        try buf.appendSlice(allocator, "<StartupAction>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</StartupAction>");
    }
    if (value.tunnel_inside_cidr) |v| {
        try buf.appendSlice(allocator, "<TunnelInsideCidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TunnelInsideCidr>");
    }
    if (value.tunnel_inside_ipv_6_cidr) |v| {
        try buf.appendSlice(allocator, "<TunnelInsideIpv6Cidr>");
        try aws.xml.appendXmlEscaped(allocator, buf, v);
        try buf.appendSlice(allocator, "</TunnelInsideIpv6Cidr>");
    }
}

