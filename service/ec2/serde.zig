const aws = @import("aws");
const std = @import("std");

const AcceleratorCount = @import("accelerator_count.zig").AcceleratorCount;
const AcceleratorCountRequest = @import("accelerator_count_request.zig").AcceleratorCountRequest;
const AcceleratorManufacturer = @import("accelerator_manufacturer.zig").AcceleratorManufacturer;
const AcceleratorName = @import("accelerator_name.zig").AcceleratorName;
const AcceleratorTotalMemoryMiB = @import("accelerator_total_memory_mi_b.zig").AcceleratorTotalMemoryMiB;
const AcceleratorTotalMemoryMiBRequest = @import("accelerator_total_memory_mi_b_request.zig").AcceleratorTotalMemoryMiBRequest;
const AcceleratorType = @import("accelerator_type.zig").AcceleratorType;
const AccessScopeAnalysisFinding = @import("access_scope_analysis_finding.zig").AccessScopeAnalysisFinding;
const AccessScopePath = @import("access_scope_path.zig").AccessScopePath;
const AccessScopePathRequest = @import("access_scope_path_request.zig").AccessScopePathRequest;
const AccountAttribute = @import("account_attribute.zig").AccountAttribute;
const AccountAttributeName = @import("account_attribute_name.zig").AccountAttributeName;
const AccountAttributeValue = @import("account_attribute_value.zig").AccountAttributeValue;
const ActiveInstance = @import("active_instance.zig").ActiveInstance;
const ActiveVpnTunnelStatus = @import("active_vpn_tunnel_status.zig").ActiveVpnTunnelStatus;
const ActivityStatus = @import("activity_status.zig").ActivityStatus;
const AddIpamOperatingRegion = @import("add_ipam_operating_region.zig").AddIpamOperatingRegion;
const AddIpamOrganizationalUnitExclusion = @import("add_ipam_organizational_unit_exclusion.zig").AddIpamOrganizationalUnitExclusion;
const AddPrefixListEntry = @import("add_prefix_list_entry.zig").AddPrefixListEntry;
const AddedPrincipal = @import("added_principal.zig").AddedPrincipal;
const AdditionalDetail = @import("additional_detail.zig").AdditionalDetail;
const Address = @import("address.zig").Address;
const AddressAttribute = @import("address_attribute.zig").AddressAttribute;
const AddressFamily = @import("address_family.zig").AddressFamily;
const AddressTransfer = @import("address_transfer.zig").AddressTransfer;
const AddressTransferStatus = @import("address_transfer_status.zig").AddressTransferStatus;
const AllocationState = @import("allocation_state.zig").AllocationState;
const AllocationStrategy = @import("allocation_strategy.zig").AllocationStrategy;
const AllocationType = @import("allocation_type.zig").AllocationType;
const AllowedPrincipal = @import("allowed_principal.zig").AllowedPrincipal;
const AllowsMultipleInstanceTypes = @import("allows_multiple_instance_types.zig").AllowsMultipleInstanceTypes;
const AlternatePathHint = @import("alternate_path_hint.zig").AlternatePathHint;
const AmdSevSnpSpecification = @import("amd_sev_snp_specification.zig").AmdSevSnpSpecification;
const AnalysisAclRule = @import("analysis_acl_rule.zig").AnalysisAclRule;
const AnalysisComponent = @import("analysis_component.zig").AnalysisComponent;
const AnalysisLoadBalancerListener = @import("analysis_load_balancer_listener.zig").AnalysisLoadBalancerListener;
const AnalysisLoadBalancerTarget = @import("analysis_load_balancer_target.zig").AnalysisLoadBalancerTarget;
const AnalysisPacketHeader = @import("analysis_packet_header.zig").AnalysisPacketHeader;
const AnalysisRouteTableRoute = @import("analysis_route_table_route.zig").AnalysisRouteTableRoute;
const AnalysisSecurityGroupRule = @import("analysis_security_group_rule.zig").AnalysisSecurityGroupRule;
const AnalysisStatus = @import("analysis_status.zig").AnalysisStatus;
const ApplianceModeSupportValue = @import("appliance_mode_support_value.zig").ApplianceModeSupportValue;
const ArchitectureType = @import("architecture_type.zig").ArchitectureType;
const ArchitectureValues = @import("architecture_values.zig").ArchitectureValues;
const AsnAssociation = @import("asn_association.zig").AsnAssociation;
const AsnAssociationState = @import("asn_association_state.zig").AsnAssociationState;
const AsnAuthorizationContext = @import("asn_authorization_context.zig").AsnAuthorizationContext;
const AsnState = @import("asn_state.zig").AsnState;
const AssignedPrivateIpAddress = @import("assigned_private_ip_address.zig").AssignedPrivateIpAddress;
const AssociatedNetworkType = @import("associated_network_type.zig").AssociatedNetworkType;
const AssociatedRole = @import("associated_role.zig").AssociatedRole;
const AssociatedTargetNetwork = @import("associated_target_network.zig").AssociatedTargetNetwork;
const AssociationStatus = @import("association_status.zig").AssociationStatus;
const AssociationStatusCode = @import("association_status_code.zig").AssociationStatusCode;
const AthenaIntegration = @import("athena_integration.zig").AthenaIntegration;
const AttachmentEnaSrdSpecification = @import("attachment_ena_srd_specification.zig").AttachmentEnaSrdSpecification;
const AttachmentEnaSrdUdpSpecification = @import("attachment_ena_srd_udp_specification.zig").AttachmentEnaSrdUdpSpecification;
const AttachmentLimitType = @import("attachment_limit_type.zig").AttachmentLimitType;
const AttachmentStatus = @import("attachment_status.zig").AttachmentStatus;
const AttributeBooleanValue = @import("attribute_boolean_value.zig").AttributeBooleanValue;
const AttributeSummary = @import("attribute_summary.zig").AttributeSummary;
const AttributeValue = @import("attribute_value.zig").AttributeValue;
const AuthorizationRule = @import("authorization_rule.zig").AuthorizationRule;
const AutoAcceptSharedAssociationsValue = @import("auto_accept_shared_associations_value.zig").AutoAcceptSharedAssociationsValue;
const AutoAcceptSharedAttachmentsValue = @import("auto_accept_shared_attachments_value.zig").AutoAcceptSharedAttachmentsValue;
const AutoPlacement = @import("auto_placement.zig").AutoPlacement;
const AutoProvisionZonesState = @import("auto_provision_zones_state.zig").AutoProvisionZonesState;
const AutoScalingIpsState = @import("auto_scaling_ips_state.zig").AutoScalingIpsState;
const AvailabilityMode = @import("availability_mode.zig").AvailabilityMode;
const AvailabilityZone = @import("availability_zone.zig").AvailabilityZone;
const AvailabilityZoneAddress = @import("availability_zone_address.zig").AvailabilityZoneAddress;
const AvailabilityZoneMessage = @import("availability_zone_message.zig").AvailabilityZoneMessage;
const AvailabilityZoneOptInStatus = @import("availability_zone_opt_in_status.zig").AvailabilityZoneOptInStatus;
const AvailabilityZoneState = @import("availability_zone_state.zig").AvailabilityZoneState;
const AvailableCapacity = @import("available_capacity.zig").AvailableCapacity;
const BandwidthWeightingType = @import("bandwidth_weighting_type.zig").BandwidthWeightingType;
const BareMetal = @import("bare_metal.zig").BareMetal;
const BaselineEbsBandwidthMbps = @import("baseline_ebs_bandwidth_mbps.zig").BaselineEbsBandwidthMbps;
const BaselineEbsBandwidthMbpsRequest = @import("baseline_ebs_bandwidth_mbps_request.zig").BaselineEbsBandwidthMbpsRequest;
const BaselinePerformanceFactors = @import("baseline_performance_factors.zig").BaselinePerformanceFactors;
const BaselinePerformanceFactorsRequest = @import("baseline_performance_factors_request.zig").BaselinePerformanceFactorsRequest;
const BatchState = @import("batch_state.zig").BatchState;
const BgpStatus = @import("bgp_status.zig").BgpStatus;
const BlobAttributeValue = @import("blob_attribute_value.zig").BlobAttributeValue;
const BlockDeviceMapping = @import("block_device_mapping.zig").BlockDeviceMapping;
const BlockDeviceMappingResponse = @import("block_device_mapping_response.zig").BlockDeviceMappingResponse;
const BlockPublicAccessMode = @import("block_public_access_mode.zig").BlockPublicAccessMode;
const BlockPublicAccessStates = @import("block_public_access_states.zig").BlockPublicAccessStates;
const BootModeType = @import("boot_mode_type.zig").BootModeType;
const BootModeValues = @import("boot_mode_values.zig").BootModeValues;
const BundleTask = @import("bundle_task.zig").BundleTask;
const BundleTaskError = @import("bundle_task_error.zig").BundleTaskError;
const BundleTaskState = @import("bundle_task_state.zig").BundleTaskState;
const BurstablePerformance = @import("burstable_performance.zig").BurstablePerformance;
const Byoasn = @import("byoasn.zig").Byoasn;
const ByoipCidr = @import("byoip_cidr.zig").ByoipCidr;
const ByoipCidrState = @import("byoip_cidr_state.zig").ByoipCidrState;
const CancelBatchErrorCode = @import("cancel_batch_error_code.zig").CancelBatchErrorCode;
const CancelCapacityReservationFleetError = @import("cancel_capacity_reservation_fleet_error.zig").CancelCapacityReservationFleetError;
const CancelSpotFleetRequestsError = @import("cancel_spot_fleet_requests_error.zig").CancelSpotFleetRequestsError;
const CancelSpotFleetRequestsErrorItem = @import("cancel_spot_fleet_requests_error_item.zig").CancelSpotFleetRequestsErrorItem;
const CancelSpotFleetRequestsSuccessItem = @import("cancel_spot_fleet_requests_success_item.zig").CancelSpotFleetRequestsSuccessItem;
const CancelSpotInstanceRequestState = @import("cancel_spot_instance_request_state.zig").CancelSpotInstanceRequestState;
const CancelledSpotInstanceRequest = @import("cancelled_spot_instance_request.zig").CancelledSpotInstanceRequest;
const CapacityAllocation = @import("capacity_allocation.zig").CapacityAllocation;
const CapacityBlock = @import("capacity_block.zig").CapacityBlock;
const CapacityBlockExtension = @import("capacity_block_extension.zig").CapacityBlockExtension;
const CapacityBlockExtensionOffering = @import("capacity_block_extension_offering.zig").CapacityBlockExtensionOffering;
const CapacityBlockExtensionStatus = @import("capacity_block_extension_status.zig").CapacityBlockExtensionStatus;
const CapacityBlockInterconnectStatus = @import("capacity_block_interconnect_status.zig").CapacityBlockInterconnectStatus;
const CapacityBlockOffering = @import("capacity_block_offering.zig").CapacityBlockOffering;
const CapacityBlockResourceState = @import("capacity_block_resource_state.zig").CapacityBlockResourceState;
const CapacityBlockStatus = @import("capacity_block_status.zig").CapacityBlockStatus;
const CapacityManagerCondition = @import("capacity_manager_condition.zig").CapacityManagerCondition;
const CapacityManagerDataExportResponse = @import("capacity_manager_data_export_response.zig").CapacityManagerDataExportResponse;
const CapacityManagerDataExportStatus = @import("capacity_manager_data_export_status.zig").CapacityManagerDataExportStatus;
const CapacityManagerDimension = @import("capacity_manager_dimension.zig").CapacityManagerDimension;
const CapacityReservation = @import("capacity_reservation.zig").CapacityReservation;
const CapacityReservationBillingRequest = @import("capacity_reservation_billing_request.zig").CapacityReservationBillingRequest;
const CapacityReservationBillingRequestStatus = @import("capacity_reservation_billing_request_status.zig").CapacityReservationBillingRequestStatus;
const CapacityReservationCommitmentInfo = @import("capacity_reservation_commitment_info.zig").CapacityReservationCommitmentInfo;
const CapacityReservationDeliveryPreference = @import("capacity_reservation_delivery_preference.zig").CapacityReservationDeliveryPreference;
const CapacityReservationFleet = @import("capacity_reservation_fleet.zig").CapacityReservationFleet;
const CapacityReservationFleetCancellationState = @import("capacity_reservation_fleet_cancellation_state.zig").CapacityReservationFleetCancellationState;
const CapacityReservationFleetState = @import("capacity_reservation_fleet_state.zig").CapacityReservationFleetState;
const CapacityReservationGroup = @import("capacity_reservation_group.zig").CapacityReservationGroup;
const CapacityReservationInfo = @import("capacity_reservation_info.zig").CapacityReservationInfo;
const CapacityReservationInstancePlatform = @import("capacity_reservation_instance_platform.zig").CapacityReservationInstancePlatform;
const CapacityReservationOptions = @import("capacity_reservation_options.zig").CapacityReservationOptions;
const CapacityReservationOptionsRequest = @import("capacity_reservation_options_request.zig").CapacityReservationOptionsRequest;
const CapacityReservationPreference = @import("capacity_reservation_preference.zig").CapacityReservationPreference;
const CapacityReservationSpecification = @import("capacity_reservation_specification.zig").CapacityReservationSpecification;
const CapacityReservationSpecificationResponse = @import("capacity_reservation_specification_response.zig").CapacityReservationSpecificationResponse;
const CapacityReservationState = @import("capacity_reservation_state.zig").CapacityReservationState;
const CapacityReservationStatus = @import("capacity_reservation_status.zig").CapacityReservationStatus;
const CapacityReservationTarget = @import("capacity_reservation_target.zig").CapacityReservationTarget;
const CapacityReservationTargetResponse = @import("capacity_reservation_target_response.zig").CapacityReservationTargetResponse;
const CapacityReservationTenancy = @import("capacity_reservation_tenancy.zig").CapacityReservationTenancy;
const CapacityReservationTopology = @import("capacity_reservation_topology.zig").CapacityReservationTopology;
const CapacityReservationType = @import("capacity_reservation_type.zig").CapacityReservationType;
const CapacityTenancy = @import("capacity_tenancy.zig").CapacityTenancy;
const CarrierGateway = @import("carrier_gateway.zig").CarrierGateway;
const CarrierGatewayState = @import("carrier_gateway_state.zig").CarrierGatewayState;
const CertificateAuthentication = @import("certificate_authentication.zig").CertificateAuthentication;
const CertificateAuthenticationRequest = @import("certificate_authentication_request.zig").CertificateAuthenticationRequest;
const CidrAuthorizationContext = @import("cidr_authorization_context.zig").CidrAuthorizationContext;
const CidrBlock = @import("cidr_block.zig").CidrBlock;
const ClassicLinkDnsSupport = @import("classic_link_dns_support.zig").ClassicLinkDnsSupport;
const ClassicLinkInstance = @import("classic_link_instance.zig").ClassicLinkInstance;
const ClassicLoadBalancer = @import("classic_load_balancer.zig").ClassicLoadBalancer;
const ClassicLoadBalancersConfig = @import("classic_load_balancers_config.zig").ClassicLoadBalancersConfig;
const ClientCertificateRevocationListStatus = @import("client_certificate_revocation_list_status.zig").ClientCertificateRevocationListStatus;
const ClientCertificateRevocationListStatusCode = @import("client_certificate_revocation_list_status_code.zig").ClientCertificateRevocationListStatusCode;
const ClientConnectOptions = @import("client_connect_options.zig").ClientConnectOptions;
const ClientConnectResponseOptions = @import("client_connect_response_options.zig").ClientConnectResponseOptions;
const ClientData = @import("client_data.zig").ClientData;
const ClientLoginBannerOptions = @import("client_login_banner_options.zig").ClientLoginBannerOptions;
const ClientLoginBannerResponseOptions = @import("client_login_banner_response_options.zig").ClientLoginBannerResponseOptions;
const ClientRouteEnforcementOptions = @import("client_route_enforcement_options.zig").ClientRouteEnforcementOptions;
const ClientRouteEnforcementResponseOptions = @import("client_route_enforcement_response_options.zig").ClientRouteEnforcementResponseOptions;
const ClientVpnAuthentication = @import("client_vpn_authentication.zig").ClientVpnAuthentication;
const ClientVpnAuthenticationRequest = @import("client_vpn_authentication_request.zig").ClientVpnAuthenticationRequest;
const ClientVpnAuthenticationType = @import("client_vpn_authentication_type.zig").ClientVpnAuthenticationType;
const ClientVpnAuthorizationRuleStatus = @import("client_vpn_authorization_rule_status.zig").ClientVpnAuthorizationRuleStatus;
const ClientVpnAuthorizationRuleStatusCode = @import("client_vpn_authorization_rule_status_code.zig").ClientVpnAuthorizationRuleStatusCode;
const ClientVpnConnection = @import("client_vpn_connection.zig").ClientVpnConnection;
const ClientVpnConnectionStatus = @import("client_vpn_connection_status.zig").ClientVpnConnectionStatus;
const ClientVpnConnectionStatusCode = @import("client_vpn_connection_status_code.zig").ClientVpnConnectionStatusCode;
const ClientVpnEndpoint = @import("client_vpn_endpoint.zig").ClientVpnEndpoint;
const ClientVpnEndpointAttributeStatus = @import("client_vpn_endpoint_attribute_status.zig").ClientVpnEndpointAttributeStatus;
const ClientVpnEndpointAttributeStatusCode = @import("client_vpn_endpoint_attribute_status_code.zig").ClientVpnEndpointAttributeStatusCode;
const ClientVpnEndpointStatus = @import("client_vpn_endpoint_status.zig").ClientVpnEndpointStatus;
const ClientVpnEndpointStatusCode = @import("client_vpn_endpoint_status_code.zig").ClientVpnEndpointStatusCode;
const ClientVpnRoute = @import("client_vpn_route.zig").ClientVpnRoute;
const ClientVpnRouteStatus = @import("client_vpn_route_status.zig").ClientVpnRouteStatus;
const ClientVpnRouteStatusCode = @import("client_vpn_route_status_code.zig").ClientVpnRouteStatusCode;
const CloudWatchLogOptions = @import("cloud_watch_log_options.zig").CloudWatchLogOptions;
const CloudWatchLogOptionsSpecification = @import("cloud_watch_log_options_specification.zig").CloudWatchLogOptionsSpecification;
const CoipAddressUsage = @import("coip_address_usage.zig").CoipAddressUsage;
const CoipCidr = @import("coip_cidr.zig").CoipCidr;
const CoipPool = @import("coip_pool.zig").CoipPool;
const Comparison = @import("comparison.zig").Comparison;
const ConnectionLogOptions = @import("connection_log_options.zig").ConnectionLogOptions;
const ConnectionLogResponseOptions = @import("connection_log_response_options.zig").ConnectionLogResponseOptions;
const ConnectionNotification = @import("connection_notification.zig").ConnectionNotification;
const ConnectionNotificationState = @import("connection_notification_state.zig").ConnectionNotificationState;
const ConnectionNotificationType = @import("connection_notification_type.zig").ConnectionNotificationType;
const ConnectionTrackingConfiguration = @import("connection_tracking_configuration.zig").ConnectionTrackingConfiguration;
const ConnectionTrackingSpecification = @import("connection_tracking_specification.zig").ConnectionTrackingSpecification;
const ConnectionTrackingSpecificationRequest = @import("connection_tracking_specification_request.zig").ConnectionTrackingSpecificationRequest;
const ConnectionTrackingSpecificationResponse = @import("connection_tracking_specification_response.zig").ConnectionTrackingSpecificationResponse;
const ConnectivityType = @import("connectivity_type.zig").ConnectivityType;
const ContainerFormat = @import("container_format.zig").ContainerFormat;
const ConversionTask = @import("conversion_task.zig").ConversionTask;
const ConversionTaskState = @import("conversion_task_state.zig").ConversionTaskState;
const CpuManufacturer = @import("cpu_manufacturer.zig").CpuManufacturer;
const CpuOptions = @import("cpu_options.zig").CpuOptions;
const CpuOptionsRequest = @import("cpu_options_request.zig").CpuOptionsRequest;
const CpuPerformanceFactor = @import("cpu_performance_factor.zig").CpuPerformanceFactor;
const CpuPerformanceFactorRequest = @import("cpu_performance_factor_request.zig").CpuPerformanceFactorRequest;
const CreateFleetError = @import("create_fleet_error.zig").CreateFleetError;
const CreateFleetInstance = @import("create_fleet_instance.zig").CreateFleetInstance;
const CreateTransitGatewayConnectRequestOptions = @import("create_transit_gateway_connect_request_options.zig").CreateTransitGatewayConnectRequestOptions;
const CreateTransitGatewayMulticastDomainRequestOptions = @import("create_transit_gateway_multicast_domain_request_options.zig").CreateTransitGatewayMulticastDomainRequestOptions;
const CreateTransitGatewayPeeringAttachmentRequestOptions = @import("create_transit_gateway_peering_attachment_request_options.zig").CreateTransitGatewayPeeringAttachmentRequestOptions;
const CreateTransitGatewayVpcAttachmentRequestOptions = @import("create_transit_gateway_vpc_attachment_request_options.zig").CreateTransitGatewayVpcAttachmentRequestOptions;
const CreateVerifiedAccessEndpointCidrOptions = @import("create_verified_access_endpoint_cidr_options.zig").CreateVerifiedAccessEndpointCidrOptions;
const CreateVerifiedAccessEndpointEniOptions = @import("create_verified_access_endpoint_eni_options.zig").CreateVerifiedAccessEndpointEniOptions;
const CreateVerifiedAccessEndpointLoadBalancerOptions = @import("create_verified_access_endpoint_load_balancer_options.zig").CreateVerifiedAccessEndpointLoadBalancerOptions;
const CreateVerifiedAccessEndpointPortRange = @import("create_verified_access_endpoint_port_range.zig").CreateVerifiedAccessEndpointPortRange;
const CreateVerifiedAccessEndpointRdsOptions = @import("create_verified_access_endpoint_rds_options.zig").CreateVerifiedAccessEndpointRdsOptions;
const CreateVerifiedAccessNativeApplicationOidcOptions = @import("create_verified_access_native_application_oidc_options.zig").CreateVerifiedAccessNativeApplicationOidcOptions;
const CreateVerifiedAccessTrustProviderDeviceOptions = @import("create_verified_access_trust_provider_device_options.zig").CreateVerifiedAccessTrustProviderDeviceOptions;
const CreateVerifiedAccessTrustProviderOidcOptions = @import("create_verified_access_trust_provider_oidc_options.zig").CreateVerifiedAccessTrustProviderOidcOptions;
const CreateVolumePermission = @import("create_volume_permission.zig").CreateVolumePermission;
const CreateVolumePermissionModifications = @import("create_volume_permission_modifications.zig").CreateVolumePermissionModifications;
const CreationDateCondition = @import("creation_date_condition.zig").CreationDateCondition;
const CreationDateConditionRequest = @import("creation_date_condition_request.zig").CreationDateConditionRequest;
const CreditSpecification = @import("credit_specification.zig").CreditSpecification;
const CreditSpecificationRequest = @import("credit_specification_request.zig").CreditSpecificationRequest;
const CurrencyCodeValues = @import("currency_code_values.zig").CurrencyCodeValues;
const CustomerGateway = @import("customer_gateway.zig").CustomerGateway;
const DataQuery = @import("data_query.zig").DataQuery;
const DataResponse = @import("data_response.zig").DataResponse;
const DatafeedSubscriptionState = @import("datafeed_subscription_state.zig").DatafeedSubscriptionState;
const DeclarativePoliciesReport = @import("declarative_policies_report.zig").DeclarativePoliciesReport;
const DefaultRouteTableAssociationValue = @import("default_route_table_association_value.zig").DefaultRouteTableAssociationValue;
const DefaultRouteTablePropagationValue = @import("default_route_table_propagation_value.zig").DefaultRouteTablePropagationValue;
const DefaultTargetCapacityType = @import("default_target_capacity_type.zig").DefaultTargetCapacityType;
const DeleteFleetError = @import("delete_fleet_error.zig").DeleteFleetError;
const DeleteFleetErrorCode = @import("delete_fleet_error_code.zig").DeleteFleetErrorCode;
const DeleteFleetErrorItem = @import("delete_fleet_error_item.zig").DeleteFleetErrorItem;
const DeleteFleetSuccessItem = @import("delete_fleet_success_item.zig").DeleteFleetSuccessItem;
const DeleteLaunchTemplateVersionsResponseErrorItem = @import("delete_launch_template_versions_response_error_item.zig").DeleteLaunchTemplateVersionsResponseErrorItem;
const DeleteLaunchTemplateVersionsResponseSuccessItem = @import("delete_launch_template_versions_response_success_item.zig").DeleteLaunchTemplateVersionsResponseSuccessItem;
const DeleteQueuedReservedInstancesError = @import("delete_queued_reserved_instances_error.zig").DeleteQueuedReservedInstancesError;
const DeleteQueuedReservedInstancesErrorCode = @import("delete_queued_reserved_instances_error_code.zig").DeleteQueuedReservedInstancesErrorCode;
const DeleteSnapshotReturnCode = @import("delete_snapshot_return_code.zig").DeleteSnapshotReturnCode;
const DeprecationTimeCondition = @import("deprecation_time_condition.zig").DeprecationTimeCondition;
const DeprecationTimeConditionRequest = @import("deprecation_time_condition_request.zig").DeprecationTimeConditionRequest;
const DeregisterInstanceTagAttributeRequest = @import("deregister_instance_tag_attribute_request.zig").DeregisterInstanceTagAttributeRequest;
const DescribeFastLaunchImagesSuccessItem = @import("describe_fast_launch_images_success_item.zig").DescribeFastLaunchImagesSuccessItem;
const DescribeFastSnapshotRestoreSuccessItem = @import("describe_fast_snapshot_restore_success_item.zig").DescribeFastSnapshotRestoreSuccessItem;
const DescribeFleetError = @import("describe_fleet_error.zig").DescribeFleetError;
const DescribeFleetsInstances = @import("describe_fleets_instances.zig").DescribeFleetsInstances;
const DestinationFileFormat = @import("destination_file_format.zig").DestinationFileFormat;
const DestinationOptionsRequest = @import("destination_options_request.zig").DestinationOptionsRequest;
const DestinationOptionsResponse = @import("destination_options_response.zig").DestinationOptionsResponse;
const DeviceOptions = @import("device_options.zig").DeviceOptions;
const DeviceTrustProviderType = @import("device_trust_provider_type.zig").DeviceTrustProviderType;
const DeviceType = @import("device_type.zig").DeviceType;
const DhcpConfiguration = @import("dhcp_configuration.zig").DhcpConfiguration;
const DhcpOptions = @import("dhcp_options.zig").DhcpOptions;
const DimensionCondition = @import("dimension_condition.zig").DimensionCondition;
const DirectoryServiceAuthentication = @import("directory_service_authentication.zig").DirectoryServiceAuthentication;
const DirectoryServiceAuthenticationRequest = @import("directory_service_authentication_request.zig").DirectoryServiceAuthenticationRequest;
const DisableFastSnapshotRestoreErrorItem = @import("disable_fast_snapshot_restore_error_item.zig").DisableFastSnapshotRestoreErrorItem;
const DisableFastSnapshotRestoreStateError = @import("disable_fast_snapshot_restore_state_error.zig").DisableFastSnapshotRestoreStateError;
const DisableFastSnapshotRestoreStateErrorItem = @import("disable_fast_snapshot_restore_state_error_item.zig").DisableFastSnapshotRestoreStateErrorItem;
const DisableFastSnapshotRestoreSuccessItem = @import("disable_fast_snapshot_restore_success_item.zig").DisableFastSnapshotRestoreSuccessItem;
const DiskImage = @import("disk_image.zig").DiskImage;
const DiskImageDescription = @import("disk_image_description.zig").DiskImageDescription;
const DiskImageDetail = @import("disk_image_detail.zig").DiskImageDetail;
const DiskImageFormat = @import("disk_image_format.zig").DiskImageFormat;
const DiskImageVolumeDescription = @import("disk_image_volume_description.zig").DiskImageVolumeDescription;
const DiskInfo = @import("disk_info.zig").DiskInfo;
const DiskType = @import("disk_type.zig").DiskType;
const DnsEntry = @import("dns_entry.zig").DnsEntry;
const DnsNameState = @import("dns_name_state.zig").DnsNameState;
const DnsOptions = @import("dns_options.zig").DnsOptions;
const DnsOptionsSpecification = @import("dns_options_specification.zig").DnsOptionsSpecification;
const DnsRecordIpType = @import("dns_record_ip_type.zig").DnsRecordIpType;
const DnsServersOptionsModifyStructure = @import("dns_servers_options_modify_structure.zig").DnsServersOptionsModifyStructure;
const DnsSupportValue = @import("dns_support_value.zig").DnsSupportValue;
const DomainType = @import("domain_type.zig").DomainType;
const DynamicRoutingValue = @import("dynamic_routing_value.zig").DynamicRoutingValue;
const EbsBlockDevice = @import("ebs_block_device.zig").EbsBlockDevice;
const EbsBlockDeviceResponse = @import("ebs_block_device_response.zig").EbsBlockDeviceResponse;
const EbsCardInfo = @import("ebs_card_info.zig").EbsCardInfo;
const EbsEncryptionSupport = @import("ebs_encryption_support.zig").EbsEncryptionSupport;
const EbsInfo = @import("ebs_info.zig").EbsInfo;
const EbsInstanceBlockDevice = @import("ebs_instance_block_device.zig").EbsInstanceBlockDevice;
const EbsInstanceBlockDeviceSpecification = @import("ebs_instance_block_device_specification.zig").EbsInstanceBlockDeviceSpecification;
const EbsNvmeSupport = @import("ebs_nvme_support.zig").EbsNvmeSupport;
const EbsOptimizedInfo = @import("ebs_optimized_info.zig").EbsOptimizedInfo;
const EbsOptimizedSupport = @import("ebs_optimized_support.zig").EbsOptimizedSupport;
const EbsStatusDetails = @import("ebs_status_details.zig").EbsStatusDetails;
const EbsStatusSummary = @import("ebs_status_summary.zig").EbsStatusSummary;
const Ec2InstanceConnectEndpoint = @import("ec_2_instance_connect_endpoint.zig").Ec2InstanceConnectEndpoint;
const Ec2InstanceConnectEndpointState = @import("ec_2_instance_connect_endpoint_state.zig").Ec2InstanceConnectEndpointState;
const EfaInfo = @import("efa_info.zig").EfaInfo;
const EgressOnlyInternetGateway = @import("egress_only_internet_gateway.zig").EgressOnlyInternetGateway;
const ElasticGpuAssociation = @import("elastic_gpu_association.zig").ElasticGpuAssociation;
const ElasticGpuHealth = @import("elastic_gpu_health.zig").ElasticGpuHealth;
const ElasticGpuSpecification = @import("elastic_gpu_specification.zig").ElasticGpuSpecification;
const ElasticGpuSpecificationResponse = @import("elastic_gpu_specification_response.zig").ElasticGpuSpecificationResponse;
const ElasticGpuState = @import("elastic_gpu_state.zig").ElasticGpuState;
const ElasticGpuStatus = @import("elastic_gpu_status.zig").ElasticGpuStatus;
const ElasticGpus = @import("elastic_gpus.zig").ElasticGpus;
const ElasticInferenceAccelerator = @import("elastic_inference_accelerator.zig").ElasticInferenceAccelerator;
const ElasticInferenceAcceleratorAssociation = @import("elastic_inference_accelerator_association.zig").ElasticInferenceAcceleratorAssociation;
const EnaSrdSpecification = @import("ena_srd_specification.zig").EnaSrdSpecification;
const EnaSrdSpecificationRequest = @import("ena_srd_specification_request.zig").EnaSrdSpecificationRequest;
const EnaSrdUdpSpecification = @import("ena_srd_udp_specification.zig").EnaSrdUdpSpecification;
const EnaSrdUdpSpecificationRequest = @import("ena_srd_udp_specification_request.zig").EnaSrdUdpSpecificationRequest;
const EnaSupport = @import("ena_support.zig").EnaSupport;
const EnableFastSnapshotRestoreErrorItem = @import("enable_fast_snapshot_restore_error_item.zig").EnableFastSnapshotRestoreErrorItem;
const EnableFastSnapshotRestoreStateError = @import("enable_fast_snapshot_restore_state_error.zig").EnableFastSnapshotRestoreStateError;
const EnableFastSnapshotRestoreStateErrorItem = @import("enable_fast_snapshot_restore_state_error_item.zig").EnableFastSnapshotRestoreStateErrorItem;
const EnableFastSnapshotRestoreSuccessItem = @import("enable_fast_snapshot_restore_success_item.zig").EnableFastSnapshotRestoreSuccessItem;
const EnclaveOptions = @import("enclave_options.zig").EnclaveOptions;
const EnclaveOptionsRequest = @import("enclave_options_request.zig").EnclaveOptionsRequest;
const EncryptionStateValue = @import("encryption_state_value.zig").EncryptionStateValue;
const EncryptionSupport = @import("encryption_support.zig").EncryptionSupport;
const EncryptionSupportOptionValue = @import("encryption_support_option_value.zig").EncryptionSupportOptionValue;
const EndDateType = @import("end_date_type.zig").EndDateType;
const EndpointIpAddressType = @import("endpoint_ip_address_type.zig").EndpointIpAddressType;
const EphemeralNvmeSupport = @import("ephemeral_nvme_support.zig").EphemeralNvmeSupport;
const EventCode = @import("event_code.zig").EventCode;
const EventInformation = @import("event_information.zig").EventInformation;
const EventType = @import("event_type.zig").EventType;
const ExcessCapacityTerminationPolicy = @import("excess_capacity_termination_policy.zig").ExcessCapacityTerminationPolicy;
const Explanation = @import("explanation.zig").Explanation;
const ExportEnvironment = @import("export_environment.zig").ExportEnvironment;
const ExportImageTask = @import("export_image_task.zig").ExportImageTask;
const ExportTask = @import("export_task.zig").ExportTask;
const ExportTaskS3Location = @import("export_task_s3_location.zig").ExportTaskS3Location;
const ExportTaskS3LocationRequest = @import("export_task_s3_location_request.zig").ExportTaskS3LocationRequest;
const ExportTaskState = @import("export_task_state.zig").ExportTaskState;
const ExportToS3Task = @import("export_to_s3_task.zig").ExportToS3Task;
const ExportToS3TaskSpecification = @import("export_to_s3_task_specification.zig").ExportToS3TaskSpecification;
const ExternalAuthorityConfiguration = @import("external_authority_configuration.zig").ExternalAuthorityConfiguration;
const FailedCapacityReservationFleetCancellationResult = @import("failed_capacity_reservation_fleet_cancellation_result.zig").FailedCapacityReservationFleetCancellationResult;
const FailedQueuedPurchaseDeletion = @import("failed_queued_purchase_deletion.zig").FailedQueuedPurchaseDeletion;
const FastLaunchLaunchTemplateSpecificationRequest = @import("fast_launch_launch_template_specification_request.zig").FastLaunchLaunchTemplateSpecificationRequest;
const FastLaunchLaunchTemplateSpecificationResponse = @import("fast_launch_launch_template_specification_response.zig").FastLaunchLaunchTemplateSpecificationResponse;
const FastLaunchResourceType = @import("fast_launch_resource_type.zig").FastLaunchResourceType;
const FastLaunchSnapshotConfigurationRequest = @import("fast_launch_snapshot_configuration_request.zig").FastLaunchSnapshotConfigurationRequest;
const FastLaunchSnapshotConfigurationResponse = @import("fast_launch_snapshot_configuration_response.zig").FastLaunchSnapshotConfigurationResponse;
const FastLaunchStateCode = @import("fast_launch_state_code.zig").FastLaunchStateCode;
const FastSnapshotRestoreStateCode = @import("fast_snapshot_restore_state_code.zig").FastSnapshotRestoreStateCode;
const FederatedAuthentication = @import("federated_authentication.zig").FederatedAuthentication;
const FederatedAuthenticationRequest = @import("federated_authentication_request.zig").FederatedAuthenticationRequest;
const Filter = @import("filter.zig").Filter;
const FilterByDimension = @import("filter_by_dimension.zig").FilterByDimension;
const FilterPortRange = @import("filter_port_range.zig").FilterPortRange;
const FindingsFound = @import("findings_found.zig").FindingsFound;
const FirewallStatefulRule = @import("firewall_stateful_rule.zig").FirewallStatefulRule;
const FirewallStatelessRule = @import("firewall_stateless_rule.zig").FirewallStatelessRule;
const FleetActivityStatus = @import("fleet_activity_status.zig").FleetActivityStatus;
const FleetBlockDeviceMappingRequest = @import("fleet_block_device_mapping_request.zig").FleetBlockDeviceMappingRequest;
const FleetCapacityReservation = @import("fleet_capacity_reservation.zig").FleetCapacityReservation;
const FleetCapacityReservationTenancy = @import("fleet_capacity_reservation_tenancy.zig").FleetCapacityReservationTenancy;
const FleetCapacityReservationUsageStrategy = @import("fleet_capacity_reservation_usage_strategy.zig").FleetCapacityReservationUsageStrategy;
const FleetData = @import("fleet_data.zig").FleetData;
const FleetEbsBlockDeviceRequest = @import("fleet_ebs_block_device_request.zig").FleetEbsBlockDeviceRequest;
const FleetEventType = @import("fleet_event_type.zig").FleetEventType;
const FleetExcessCapacityTerminationPolicy = @import("fleet_excess_capacity_termination_policy.zig").FleetExcessCapacityTerminationPolicy;
const FleetInstanceMatchCriteria = @import("fleet_instance_match_criteria.zig").FleetInstanceMatchCriteria;
const FleetLaunchTemplateConfig = @import("fleet_launch_template_config.zig").FleetLaunchTemplateConfig;
const FleetLaunchTemplateConfigRequest = @import("fleet_launch_template_config_request.zig").FleetLaunchTemplateConfigRequest;
const FleetLaunchTemplateOverrides = @import("fleet_launch_template_overrides.zig").FleetLaunchTemplateOverrides;
const FleetLaunchTemplateOverridesRequest = @import("fleet_launch_template_overrides_request.zig").FleetLaunchTemplateOverridesRequest;
const FleetLaunchTemplateSpecification = @import("fleet_launch_template_specification.zig").FleetLaunchTemplateSpecification;
const FleetLaunchTemplateSpecificationRequest = @import("fleet_launch_template_specification_request.zig").FleetLaunchTemplateSpecificationRequest;
const FleetOnDemandAllocationStrategy = @import("fleet_on_demand_allocation_strategy.zig").FleetOnDemandAllocationStrategy;
const FleetReplacementStrategy = @import("fleet_replacement_strategy.zig").FleetReplacementStrategy;
const FleetSpotCapacityRebalance = @import("fleet_spot_capacity_rebalance.zig").FleetSpotCapacityRebalance;
const FleetSpotCapacityRebalanceRequest = @import("fleet_spot_capacity_rebalance_request.zig").FleetSpotCapacityRebalanceRequest;
const FleetSpotMaintenanceStrategies = @import("fleet_spot_maintenance_strategies.zig").FleetSpotMaintenanceStrategies;
const FleetSpotMaintenanceStrategiesRequest = @import("fleet_spot_maintenance_strategies_request.zig").FleetSpotMaintenanceStrategiesRequest;
const FleetStateCode = @import("fleet_state_code.zig").FleetStateCode;
const FleetType = @import("fleet_type.zig").FleetType;
const FlexibleEnaQueuesSupport = @import("flexible_ena_queues_support.zig").FlexibleEnaQueuesSupport;
const FlowLog = @import("flow_log.zig").FlowLog;
const FpgaDeviceInfo = @import("fpga_device_info.zig").FpgaDeviceInfo;
const FpgaDeviceMemoryInfo = @import("fpga_device_memory_info.zig").FpgaDeviceMemoryInfo;
const FpgaImage = @import("fpga_image.zig").FpgaImage;
const FpgaImageAttribute = @import("fpga_image_attribute.zig").FpgaImageAttribute;
const FpgaImageState = @import("fpga_image_state.zig").FpgaImageState;
const FpgaImageStateCode = @import("fpga_image_state_code.zig").FpgaImageStateCode;
const FpgaInfo = @import("fpga_info.zig").FpgaInfo;
const GatewayAssociationState = @import("gateway_association_state.zig").GatewayAssociationState;
const GatewayType = @import("gateway_type.zig").GatewayType;
const GpuDeviceInfo = @import("gpu_device_info.zig").GpuDeviceInfo;
const GpuDeviceMemoryInfo = @import("gpu_device_memory_info.zig").GpuDeviceMemoryInfo;
const GpuInfo = @import("gpu_info.zig").GpuInfo;
const GroupBy = @import("group_by.zig").GroupBy;
const GroupIdentifier = @import("group_identifier.zig").GroupIdentifier;
const HaStatus = @import("ha_status.zig").HaStatus;
const HibernationOptions = @import("hibernation_options.zig").HibernationOptions;
const HibernationOptionsRequest = @import("hibernation_options_request.zig").HibernationOptionsRequest;
const HistoryRecord = @import("history_record.zig").HistoryRecord;
const HistoryRecordEntry = @import("history_record_entry.zig").HistoryRecordEntry;
const Host = @import("host.zig").Host;
const HostInstance = @import("host_instance.zig").HostInstance;
const HostMaintenance = @import("host_maintenance.zig").HostMaintenance;
const HostOffering = @import("host_offering.zig").HostOffering;
const HostProperties = @import("host_properties.zig").HostProperties;
const HostRecovery = @import("host_recovery.zig").HostRecovery;
const HostReservation = @import("host_reservation.zig").HostReservation;
const HostnameType = @import("hostname_type.zig").HostnameType;
const HttpTokensState = @import("http_tokens_state.zig").HttpTokensState;
const HypervisorType = @import("hypervisor_type.zig").HypervisorType;
const IKEVersionsListValue = @import("ike_versions_list_value.zig").IKEVersionsListValue;
const IKEVersionsRequestListValue = @import("ike_versions_request_list_value.zig").IKEVersionsRequestListValue;
const IamInstanceProfile = @import("iam_instance_profile.zig").IamInstanceProfile;
const IamInstanceProfileAssociation = @import("iam_instance_profile_association.zig").IamInstanceProfileAssociation;
const IamInstanceProfileAssociationState = @import("iam_instance_profile_association_state.zig").IamInstanceProfileAssociationState;
const IamInstanceProfileSpecification = @import("iam_instance_profile_specification.zig").IamInstanceProfileSpecification;
const IcmpTypeCode = @import("icmp_type_code.zig").IcmpTypeCode;
const IdFormat = @import("id_format.zig").IdFormat;
const Igmpv2SupportValue = @import("igmpv_2_support_value.zig").Igmpv2SupportValue;
const Image = @import("image.zig").Image;
const ImageAncestryEntry = @import("image_ancestry_entry.zig").ImageAncestryEntry;
const ImageCriterion = @import("image_criterion.zig").ImageCriterion;
const ImageCriterionRequest = @import("image_criterion_request.zig").ImageCriterionRequest;
const ImageDiskContainer = @import("image_disk_container.zig").ImageDiskContainer;
const ImageMetadata = @import("image_metadata.zig").ImageMetadata;
const ImageRecycleBinInfo = @import("image_recycle_bin_info.zig").ImageRecycleBinInfo;
const ImageReference = @import("image_reference.zig").ImageReference;
const ImageReferenceOptionName = @import("image_reference_option_name.zig").ImageReferenceOptionName;
const ImageReferenceResourceType = @import("image_reference_resource_type.zig").ImageReferenceResourceType;
const ImageState = @import("image_state.zig").ImageState;
const ImageTypeValues = @import("image_type_values.zig").ImageTypeValues;
const ImageUsageReport = @import("image_usage_report.zig").ImageUsageReport;
const ImageUsageReportEntry = @import("image_usage_report_entry.zig").ImageUsageReportEntry;
const ImageUsageResourceType = @import("image_usage_resource_type.zig").ImageUsageResourceType;
const ImageUsageResourceTypeOption = @import("image_usage_resource_type_option.zig").ImageUsageResourceTypeOption;
const ImageUsageResourceTypeOptionRequest = @import("image_usage_resource_type_option_request.zig").ImageUsageResourceTypeOptionRequest;
const ImageUsageResourceTypeRequest = @import("image_usage_resource_type_request.zig").ImageUsageResourceTypeRequest;
const ImdsSupportValues = @import("imds_support_values.zig").ImdsSupportValues;
const ImportImageLicenseConfigurationRequest = @import("import_image_license_configuration_request.zig").ImportImageLicenseConfigurationRequest;
const ImportImageLicenseConfigurationResponse = @import("import_image_license_configuration_response.zig").ImportImageLicenseConfigurationResponse;
const ImportImageTask = @import("import_image_task.zig").ImportImageTask;
const ImportInstanceLaunchSpecification = @import("import_instance_launch_specification.zig").ImportInstanceLaunchSpecification;
const ImportInstanceTaskDetails = @import("import_instance_task_details.zig").ImportInstanceTaskDetails;
const ImportInstanceVolumeDetailItem = @import("import_instance_volume_detail_item.zig").ImportInstanceVolumeDetailItem;
const ImportSnapshotTask = @import("import_snapshot_task.zig").ImportSnapshotTask;
const ImportVolumeTaskDetails = @import("import_volume_task_details.zig").ImportVolumeTaskDetails;
const InferenceAcceleratorInfo = @import("inference_accelerator_info.zig").InferenceAcceleratorInfo;
const InferenceDeviceInfo = @import("inference_device_info.zig").InferenceDeviceInfo;
const InferenceDeviceMemoryInfo = @import("inference_device_memory_info.zig").InferenceDeviceMemoryInfo;
const InitializationStatusDetails = @import("initialization_status_details.zig").InitializationStatusDetails;
const InitializationType = @import("initialization_type.zig").InitializationType;
const Instance = @import("instance.zig").Instance;
const InstanceAttachmentEnaSrdSpecification = @import("instance_attachment_ena_srd_specification.zig").InstanceAttachmentEnaSrdSpecification;
const InstanceAttachmentEnaSrdUdpSpecification = @import("instance_attachment_ena_srd_udp_specification.zig").InstanceAttachmentEnaSrdUdpSpecification;
const InstanceAutoRecoveryState = @import("instance_auto_recovery_state.zig").InstanceAutoRecoveryState;
const InstanceBandwidthWeighting = @import("instance_bandwidth_weighting.zig").InstanceBandwidthWeighting;
const InstanceBlockDeviceMapping = @import("instance_block_device_mapping.zig").InstanceBlockDeviceMapping;
const InstanceBlockDeviceMappingSpecification = @import("instance_block_device_mapping_specification.zig").InstanceBlockDeviceMappingSpecification;
const InstanceBootModeValues = @import("instance_boot_mode_values.zig").InstanceBootModeValues;
const InstanceCapacity = @import("instance_capacity.zig").InstanceCapacity;
const InstanceConnectEndpointDnsNames = @import("instance_connect_endpoint_dns_names.zig").InstanceConnectEndpointDnsNames;
const InstanceConnectEndpointPublicDnsNames = @import("instance_connect_endpoint_public_dns_names.zig").InstanceConnectEndpointPublicDnsNames;
const InstanceCount = @import("instance_count.zig").InstanceCount;
const InstanceCreditSpecification = @import("instance_credit_specification.zig").InstanceCreditSpecification;
const InstanceCreditSpecificationRequest = @import("instance_credit_specification_request.zig").InstanceCreditSpecificationRequest;
const InstanceEventWindow = @import("instance_event_window.zig").InstanceEventWindow;
const InstanceEventWindowAssociationRequest = @import("instance_event_window_association_request.zig").InstanceEventWindowAssociationRequest;
const InstanceEventWindowAssociationTarget = @import("instance_event_window_association_target.zig").InstanceEventWindowAssociationTarget;
const InstanceEventWindowDisassociationRequest = @import("instance_event_window_disassociation_request.zig").InstanceEventWindowDisassociationRequest;
const InstanceEventWindowState = @import("instance_event_window_state.zig").InstanceEventWindowState;
const InstanceEventWindowStateChange = @import("instance_event_window_state_change.zig").InstanceEventWindowStateChange;
const InstanceEventWindowTimeRange = @import("instance_event_window_time_range.zig").InstanceEventWindowTimeRange;
const InstanceEventWindowTimeRangeRequest = @import("instance_event_window_time_range_request.zig").InstanceEventWindowTimeRangeRequest;
const InstanceExportDetails = @import("instance_export_details.zig").InstanceExportDetails;
const InstanceFamilyCreditSpecification = @import("instance_family_credit_specification.zig").InstanceFamilyCreditSpecification;
const InstanceGeneration = @import("instance_generation.zig").InstanceGeneration;
const InstanceHealthStatus = @import("instance_health_status.zig").InstanceHealthStatus;
const InstanceImageMetadata = @import("instance_image_metadata.zig").InstanceImageMetadata;
const InstanceInterruptionBehavior = @import("instance_interruption_behavior.zig").InstanceInterruptionBehavior;
const InstanceIpv4Prefix = @import("instance_ipv_4_prefix.zig").InstanceIpv4Prefix;
const InstanceIpv6Address = @import("instance_ipv_6_address.zig").InstanceIpv6Address;
const InstanceIpv6AddressRequest = @import("instance_ipv_6_address_request.zig").InstanceIpv6AddressRequest;
const InstanceIpv6Prefix = @import("instance_ipv_6_prefix.zig").InstanceIpv6Prefix;
const InstanceLifecycle = @import("instance_lifecycle.zig").InstanceLifecycle;
const InstanceLifecycleType = @import("instance_lifecycle_type.zig").InstanceLifecycleType;
const InstanceMaintenanceOptions = @import("instance_maintenance_options.zig").InstanceMaintenanceOptions;
const InstanceMaintenanceOptionsRequest = @import("instance_maintenance_options_request.zig").InstanceMaintenanceOptionsRequest;
const InstanceMarketOptionsRequest = @import("instance_market_options_request.zig").InstanceMarketOptionsRequest;
const InstanceMatchCriteria = @import("instance_match_criteria.zig").InstanceMatchCriteria;
const InstanceMetadataDefaultsResponse = @import("instance_metadata_defaults_response.zig").InstanceMetadataDefaultsResponse;
const InstanceMetadataEndpointState = @import("instance_metadata_endpoint_state.zig").InstanceMetadataEndpointState;
const InstanceMetadataOptionsRequest = @import("instance_metadata_options_request.zig").InstanceMetadataOptionsRequest;
const InstanceMetadataOptionsResponse = @import("instance_metadata_options_response.zig").InstanceMetadataOptionsResponse;
const InstanceMetadataOptionsState = @import("instance_metadata_options_state.zig").InstanceMetadataOptionsState;
const InstanceMetadataProtocolState = @import("instance_metadata_protocol_state.zig").InstanceMetadataProtocolState;
const InstanceMetadataTagsState = @import("instance_metadata_tags_state.zig").InstanceMetadataTagsState;
const InstanceMonitoring = @import("instance_monitoring.zig").InstanceMonitoring;
const InstanceNetworkInterface = @import("instance_network_interface.zig").InstanceNetworkInterface;
const InstanceNetworkInterfaceAssociation = @import("instance_network_interface_association.zig").InstanceNetworkInterfaceAssociation;
const InstanceNetworkInterfaceAttachment = @import("instance_network_interface_attachment.zig").InstanceNetworkInterfaceAttachment;
const InstanceNetworkInterfaceSpecification = @import("instance_network_interface_specification.zig").InstanceNetworkInterfaceSpecification;
const InstanceNetworkPerformanceOptions = @import("instance_network_performance_options.zig").InstanceNetworkPerformanceOptions;
const InstanceNetworkPerformanceOptionsRequest = @import("instance_network_performance_options_request.zig").InstanceNetworkPerformanceOptionsRequest;
const InstancePrivateIpAddress = @import("instance_private_ip_address.zig").InstancePrivateIpAddress;
const InstanceRebootMigrationState = @import("instance_reboot_migration_state.zig").InstanceRebootMigrationState;
const InstanceRequirements = @import("instance_requirements.zig").InstanceRequirements;
const InstanceRequirementsRequest = @import("instance_requirements_request.zig").InstanceRequirementsRequest;
const InstanceRequirementsWithMetadataRequest = @import("instance_requirements_with_metadata_request.zig").InstanceRequirementsWithMetadataRequest;
const InstanceSpecification = @import("instance_specification.zig").InstanceSpecification;
const InstanceState = @import("instance_state.zig").InstanceState;
const InstanceStateChange = @import("instance_state_change.zig").InstanceStateChange;
const InstanceStateName = @import("instance_state_name.zig").InstanceStateName;
const InstanceStatus = @import("instance_status.zig").InstanceStatus;
const InstanceStatusDetails = @import("instance_status_details.zig").InstanceStatusDetails;
const InstanceStatusEvent = @import("instance_status_event.zig").InstanceStatusEvent;
const InstanceStatusSummary = @import("instance_status_summary.zig").InstanceStatusSummary;
const InstanceStorageEncryptionSupport = @import("instance_storage_encryption_support.zig").InstanceStorageEncryptionSupport;
const InstanceStorageInfo = @import("instance_storage_info.zig").InstanceStorageInfo;
const InstanceTagNotificationAttribute = @import("instance_tag_notification_attribute.zig").InstanceTagNotificationAttribute;
const InstanceTopology = @import("instance_topology.zig").InstanceTopology;
const InstanceType = @import("instance_type.zig").InstanceType;
const InstanceTypeHypervisor = @import("instance_type_hypervisor.zig").InstanceTypeHypervisor;
const InstanceTypeInfo = @import("instance_type_info.zig").InstanceTypeInfo;
const InstanceTypeInfoFromInstanceRequirements = @import("instance_type_info_from_instance_requirements.zig").InstanceTypeInfoFromInstanceRequirements;
const InstanceTypeOffering = @import("instance_type_offering.zig").InstanceTypeOffering;
const InstanceUsage = @import("instance_usage.zig").InstanceUsage;
const IntegrateServices = @import("integrate_services.zig").IntegrateServices;
const InterfacePermissionType = @import("interface_permission_type.zig").InterfacePermissionType;
const InterfaceProtocolType = @import("interface_protocol_type.zig").InterfaceProtocolType;
const InternetGateway = @import("internet_gateway.zig").InternetGateway;
const InternetGatewayAttachment = @import("internet_gateway_attachment.zig").InternetGatewayAttachment;
const InternetGatewayBlockMode = @import("internet_gateway_block_mode.zig").InternetGatewayBlockMode;
const InternetGatewayExclusionMode = @import("internet_gateway_exclusion_mode.zig").InternetGatewayExclusionMode;
const InterruptibleCapacityAllocation = @import("interruptible_capacity_allocation.zig").InterruptibleCapacityAllocation;
const InterruptibleCapacityReservationAllocationStatus = @import("interruptible_capacity_reservation_allocation_status.zig").InterruptibleCapacityReservationAllocationStatus;
const InterruptionInfo = @import("interruption_info.zig").InterruptionInfo;
const InterruptionType = @import("interruption_type.zig").InterruptionType;
const IpAddressType = @import("ip_address_type.zig").IpAddressType;
const IpPermission = @import("ip_permission.zig").IpPermission;
const IpRange = @import("ip_range.zig").IpRange;
const IpSource = @import("ip_source.zig").IpSource;
const Ipam = @import("ipam.zig").Ipam;
const IpamAddressHistoryRecord = @import("ipam_address_history_record.zig").IpamAddressHistoryRecord;
const IpamAddressHistoryResourceType = @import("ipam_address_history_resource_type.zig").IpamAddressHistoryResourceType;
const IpamAssociatedResourceDiscoveryStatus = @import("ipam_associated_resource_discovery_status.zig").IpamAssociatedResourceDiscoveryStatus;
const IpamCidrAuthorizationContext = @import("ipam_cidr_authorization_context.zig").IpamCidrAuthorizationContext;
const IpamComplianceStatus = @import("ipam_compliance_status.zig").IpamComplianceStatus;
const IpamDiscoveredAccount = @import("ipam_discovered_account.zig").IpamDiscoveredAccount;
const IpamDiscoveredPublicAddress = @import("ipam_discovered_public_address.zig").IpamDiscoveredPublicAddress;
const IpamDiscoveredResourceCidr = @import("ipam_discovered_resource_cidr.zig").IpamDiscoveredResourceCidr;
const IpamDiscoveryFailureCode = @import("ipam_discovery_failure_code.zig").IpamDiscoveryFailureCode;
const IpamDiscoveryFailureReason = @import("ipam_discovery_failure_reason.zig").IpamDiscoveryFailureReason;
const IpamExternalResourceVerificationToken = @import("ipam_external_resource_verification_token.zig").IpamExternalResourceVerificationToken;
const IpamExternalResourceVerificationTokenState = @import("ipam_external_resource_verification_token_state.zig").IpamExternalResourceVerificationTokenState;
const IpamManagementState = @import("ipam_management_state.zig").IpamManagementState;
const IpamMeteredAccount = @import("ipam_metered_account.zig").IpamMeteredAccount;
const IpamNetworkInterfaceAttachmentStatus = @import("ipam_network_interface_attachment_status.zig").IpamNetworkInterfaceAttachmentStatus;
const IpamOperatingRegion = @import("ipam_operating_region.zig").IpamOperatingRegion;
const IpamOrganizationalUnitExclusion = @import("ipam_organizational_unit_exclusion.zig").IpamOrganizationalUnitExclusion;
const IpamOverlapStatus = @import("ipam_overlap_status.zig").IpamOverlapStatus;
const IpamPolicy = @import("ipam_policy.zig").IpamPolicy;
const IpamPolicyAllocationRule = @import("ipam_policy_allocation_rule.zig").IpamPolicyAllocationRule;
const IpamPolicyAllocationRuleRequest = @import("ipam_policy_allocation_rule_request.zig").IpamPolicyAllocationRuleRequest;
const IpamPolicyDocument = @import("ipam_policy_document.zig").IpamPolicyDocument;
const IpamPolicyOrganizationTarget = @import("ipam_policy_organization_target.zig").IpamPolicyOrganizationTarget;
const IpamPolicyResourceType = @import("ipam_policy_resource_type.zig").IpamPolicyResourceType;
const IpamPolicyState = @import("ipam_policy_state.zig").IpamPolicyState;
const IpamPool = @import("ipam_pool.zig").IpamPool;
const IpamPoolAllocation = @import("ipam_pool_allocation.zig").IpamPoolAllocation;
const IpamPoolAllocationResourceType = @import("ipam_pool_allocation_resource_type.zig").IpamPoolAllocationResourceType;
const IpamPoolAwsService = @import("ipam_pool_aws_service.zig").IpamPoolAwsService;
const IpamPoolCidr = @import("ipam_pool_cidr.zig").IpamPoolCidr;
const IpamPoolCidrFailureCode = @import("ipam_pool_cidr_failure_code.zig").IpamPoolCidrFailureCode;
const IpamPoolCidrFailureReason = @import("ipam_pool_cidr_failure_reason.zig").IpamPoolCidrFailureReason;
const IpamPoolCidrState = @import("ipam_pool_cidr_state.zig").IpamPoolCidrState;
const IpamPoolPublicIpSource = @import("ipam_pool_public_ip_source.zig").IpamPoolPublicIpSource;
const IpamPoolSourceResource = @import("ipam_pool_source_resource.zig").IpamPoolSourceResource;
const IpamPoolSourceResourceRequest = @import("ipam_pool_source_resource_request.zig").IpamPoolSourceResourceRequest;
const IpamPoolSourceResourceType = @import("ipam_pool_source_resource_type.zig").IpamPoolSourceResourceType;
const IpamPoolState = @import("ipam_pool_state.zig").IpamPoolState;
const IpamPrefixListResolver = @import("ipam_prefix_list_resolver.zig").IpamPrefixListResolver;
const IpamPrefixListResolverRule = @import("ipam_prefix_list_resolver_rule.zig").IpamPrefixListResolverRule;
const IpamPrefixListResolverRuleCondition = @import("ipam_prefix_list_resolver_rule_condition.zig").IpamPrefixListResolverRuleCondition;
const IpamPrefixListResolverRuleConditionOperation = @import("ipam_prefix_list_resolver_rule_condition_operation.zig").IpamPrefixListResolverRuleConditionOperation;
const IpamPrefixListResolverRuleConditionRequest = @import("ipam_prefix_list_resolver_rule_condition_request.zig").IpamPrefixListResolverRuleConditionRequest;
const IpamPrefixListResolverRuleRequest = @import("ipam_prefix_list_resolver_rule_request.zig").IpamPrefixListResolverRuleRequest;
const IpamPrefixListResolverRuleType = @import("ipam_prefix_list_resolver_rule_type.zig").IpamPrefixListResolverRuleType;
const IpamPrefixListResolverState = @import("ipam_prefix_list_resolver_state.zig").IpamPrefixListResolverState;
const IpamPrefixListResolverTarget = @import("ipam_prefix_list_resolver_target.zig").IpamPrefixListResolverTarget;
const IpamPrefixListResolverTargetState = @import("ipam_prefix_list_resolver_target_state.zig").IpamPrefixListResolverTargetState;
const IpamPrefixListResolverVersion = @import("ipam_prefix_list_resolver_version.zig").IpamPrefixListResolverVersion;
const IpamPrefixListResolverVersionCreationStatus = @import("ipam_prefix_list_resolver_version_creation_status.zig").IpamPrefixListResolverVersionCreationStatus;
const IpamPrefixListResolverVersionEntry = @import("ipam_prefix_list_resolver_version_entry.zig").IpamPrefixListResolverVersionEntry;
const IpamPublicAddressAssociationStatus = @import("ipam_public_address_association_status.zig").IpamPublicAddressAssociationStatus;
const IpamPublicAddressAwsService = @import("ipam_public_address_aws_service.zig").IpamPublicAddressAwsService;
const IpamPublicAddressSecurityGroup = @import("ipam_public_address_security_group.zig").IpamPublicAddressSecurityGroup;
const IpamPublicAddressTag = @import("ipam_public_address_tag.zig").IpamPublicAddressTag;
const IpamPublicAddressTags = @import("ipam_public_address_tags.zig").IpamPublicAddressTags;
const IpamPublicAddressType = @import("ipam_public_address_type.zig").IpamPublicAddressType;
const IpamResourceCidr = @import("ipam_resource_cidr.zig").IpamResourceCidr;
const IpamResourceCidrIpSource = @import("ipam_resource_cidr_ip_source.zig").IpamResourceCidrIpSource;
const IpamResourceDiscovery = @import("ipam_resource_discovery.zig").IpamResourceDiscovery;
const IpamResourceDiscoveryAssociation = @import("ipam_resource_discovery_association.zig").IpamResourceDiscoveryAssociation;
const IpamResourceDiscoveryAssociationState = @import("ipam_resource_discovery_association_state.zig").IpamResourceDiscoveryAssociationState;
const IpamResourceDiscoveryState = @import("ipam_resource_discovery_state.zig").IpamResourceDiscoveryState;
const IpamResourceTag = @import("ipam_resource_tag.zig").IpamResourceTag;
const IpamResourceType = @import("ipam_resource_type.zig").IpamResourceType;
const IpamScope = @import("ipam_scope.zig").IpamScope;
const IpamScopeExternalAuthorityConfiguration = @import("ipam_scope_external_authority_configuration.zig").IpamScopeExternalAuthorityConfiguration;
const IpamScopeExternalAuthorityType = @import("ipam_scope_external_authority_type.zig").IpamScopeExternalAuthorityType;
const IpamScopeState = @import("ipam_scope_state.zig").IpamScopeState;
const IpamScopeType = @import("ipam_scope_type.zig").IpamScopeType;
const IpamState = @import("ipam_state.zig").IpamState;
const IpamTier = @import("ipam_tier.zig").IpamTier;
const Ipv4PrefixSpecification = @import("ipv_4_prefix_specification.zig").Ipv4PrefixSpecification;
const Ipv4PrefixSpecificationRequest = @import("ipv_4_prefix_specification_request.zig").Ipv4PrefixSpecificationRequest;
const Ipv4PrefixSpecificationResponse = @import("ipv_4_prefix_specification_response.zig").Ipv4PrefixSpecificationResponse;
const Ipv6AddressAttribute = @import("ipv_6_address_attribute.zig").Ipv6AddressAttribute;
const Ipv6CidrAssociation = @import("ipv_6_cidr_association.zig").Ipv6CidrAssociation;
const Ipv6CidrBlock = @import("ipv_6_cidr_block.zig").Ipv6CidrBlock;
const Ipv6Pool = @import("ipv_6_pool.zig").Ipv6Pool;
const Ipv6PrefixSpecification = @import("ipv_6_prefix_specification.zig").Ipv6PrefixSpecification;
const Ipv6PrefixSpecificationRequest = @import("ipv_6_prefix_specification_request.zig").Ipv6PrefixSpecificationRequest;
const Ipv6PrefixSpecificationResponse = @import("ipv_6_prefix_specification_response.zig").Ipv6PrefixSpecificationResponse;
const Ipv6Range = @import("ipv_6_range.zig").Ipv6Range;
const Ipv6SupportValue = @import("ipv_6_support_value.zig").Ipv6SupportValue;
const KeyPairInfo = @import("key_pair_info.zig").KeyPairInfo;
const KeyType = @import("key_type.zig").KeyType;
const LastError = @import("last_error.zig").LastError;
const LaunchPermission = @import("launch_permission.zig").LaunchPermission;
const LaunchPermissionModifications = @import("launch_permission_modifications.zig").LaunchPermissionModifications;
const LaunchSpecification = @import("launch_specification.zig").LaunchSpecification;
const LaunchTemplate = @import("launch_template.zig").LaunchTemplate;
const LaunchTemplateAndOverridesResponse = @import("launch_template_and_overrides_response.zig").LaunchTemplateAndOverridesResponse;
const LaunchTemplateAutoRecoveryState = @import("launch_template_auto_recovery_state.zig").LaunchTemplateAutoRecoveryState;
const LaunchTemplateBlockDeviceMapping = @import("launch_template_block_device_mapping.zig").LaunchTemplateBlockDeviceMapping;
const LaunchTemplateBlockDeviceMappingRequest = @import("launch_template_block_device_mapping_request.zig").LaunchTemplateBlockDeviceMappingRequest;
const LaunchTemplateCapacityReservationSpecificationRequest = @import("launch_template_capacity_reservation_specification_request.zig").LaunchTemplateCapacityReservationSpecificationRequest;
const LaunchTemplateCapacityReservationSpecificationResponse = @import("launch_template_capacity_reservation_specification_response.zig").LaunchTemplateCapacityReservationSpecificationResponse;
const LaunchTemplateConfig = @import("launch_template_config.zig").LaunchTemplateConfig;
const LaunchTemplateCpuOptions = @import("launch_template_cpu_options.zig").LaunchTemplateCpuOptions;
const LaunchTemplateCpuOptionsRequest = @import("launch_template_cpu_options_request.zig").LaunchTemplateCpuOptionsRequest;
const LaunchTemplateEbsBlockDevice = @import("launch_template_ebs_block_device.zig").LaunchTemplateEbsBlockDevice;
const LaunchTemplateEbsBlockDeviceRequest = @import("launch_template_ebs_block_device_request.zig").LaunchTemplateEbsBlockDeviceRequest;
const LaunchTemplateElasticInferenceAccelerator = @import("launch_template_elastic_inference_accelerator.zig").LaunchTemplateElasticInferenceAccelerator;
const LaunchTemplateElasticInferenceAcceleratorResponse = @import("launch_template_elastic_inference_accelerator_response.zig").LaunchTemplateElasticInferenceAcceleratorResponse;
const LaunchTemplateEnaSrdSpecification = @import("launch_template_ena_srd_specification.zig").LaunchTemplateEnaSrdSpecification;
const LaunchTemplateEnaSrdUdpSpecification = @import("launch_template_ena_srd_udp_specification.zig").LaunchTemplateEnaSrdUdpSpecification;
const LaunchTemplateEnclaveOptions = @import("launch_template_enclave_options.zig").LaunchTemplateEnclaveOptions;
const LaunchTemplateEnclaveOptionsRequest = @import("launch_template_enclave_options_request.zig").LaunchTemplateEnclaveOptionsRequest;
const LaunchTemplateErrorCode = @import("launch_template_error_code.zig").LaunchTemplateErrorCode;
const LaunchTemplateHibernationOptions = @import("launch_template_hibernation_options.zig").LaunchTemplateHibernationOptions;
const LaunchTemplateHibernationOptionsRequest = @import("launch_template_hibernation_options_request.zig").LaunchTemplateHibernationOptionsRequest;
const LaunchTemplateHttpTokensState = @import("launch_template_http_tokens_state.zig").LaunchTemplateHttpTokensState;
const LaunchTemplateIamInstanceProfileSpecification = @import("launch_template_iam_instance_profile_specification.zig").LaunchTemplateIamInstanceProfileSpecification;
const LaunchTemplateIamInstanceProfileSpecificationRequest = @import("launch_template_iam_instance_profile_specification_request.zig").LaunchTemplateIamInstanceProfileSpecificationRequest;
const LaunchTemplateInstanceMaintenanceOptions = @import("launch_template_instance_maintenance_options.zig").LaunchTemplateInstanceMaintenanceOptions;
const LaunchTemplateInstanceMaintenanceOptionsRequest = @import("launch_template_instance_maintenance_options_request.zig").LaunchTemplateInstanceMaintenanceOptionsRequest;
const LaunchTemplateInstanceMarketOptions = @import("launch_template_instance_market_options.zig").LaunchTemplateInstanceMarketOptions;
const LaunchTemplateInstanceMarketOptionsRequest = @import("launch_template_instance_market_options_request.zig").LaunchTemplateInstanceMarketOptionsRequest;
const LaunchTemplateInstanceMetadataEndpointState = @import("launch_template_instance_metadata_endpoint_state.zig").LaunchTemplateInstanceMetadataEndpointState;
const LaunchTemplateInstanceMetadataOptions = @import("launch_template_instance_metadata_options.zig").LaunchTemplateInstanceMetadataOptions;
const LaunchTemplateInstanceMetadataOptionsRequest = @import("launch_template_instance_metadata_options_request.zig").LaunchTemplateInstanceMetadataOptionsRequest;
const LaunchTemplateInstanceMetadataOptionsState = @import("launch_template_instance_metadata_options_state.zig").LaunchTemplateInstanceMetadataOptionsState;
const LaunchTemplateInstanceMetadataProtocolIpv6 = @import("launch_template_instance_metadata_protocol_ipv_6.zig").LaunchTemplateInstanceMetadataProtocolIpv6;
const LaunchTemplateInstanceMetadataTagsState = @import("launch_template_instance_metadata_tags_state.zig").LaunchTemplateInstanceMetadataTagsState;
const LaunchTemplateInstanceNetworkInterfaceSpecification = @import("launch_template_instance_network_interface_specification.zig").LaunchTemplateInstanceNetworkInterfaceSpecification;
const LaunchTemplateInstanceNetworkInterfaceSpecificationRequest = @import("launch_template_instance_network_interface_specification_request.zig").LaunchTemplateInstanceNetworkInterfaceSpecificationRequest;
const LaunchTemplateLicenseConfiguration = @import("launch_template_license_configuration.zig").LaunchTemplateLicenseConfiguration;
const LaunchTemplateLicenseConfigurationRequest = @import("launch_template_license_configuration_request.zig").LaunchTemplateLicenseConfigurationRequest;
const LaunchTemplateNetworkPerformanceOptions = @import("launch_template_network_performance_options.zig").LaunchTemplateNetworkPerformanceOptions;
const LaunchTemplateNetworkPerformanceOptionsRequest = @import("launch_template_network_performance_options_request.zig").LaunchTemplateNetworkPerformanceOptionsRequest;
const LaunchTemplateOverrides = @import("launch_template_overrides.zig").LaunchTemplateOverrides;
const LaunchTemplatePlacement = @import("launch_template_placement.zig").LaunchTemplatePlacement;
const LaunchTemplatePlacementRequest = @import("launch_template_placement_request.zig").LaunchTemplatePlacementRequest;
const LaunchTemplatePrivateDnsNameOptions = @import("launch_template_private_dns_name_options.zig").LaunchTemplatePrivateDnsNameOptions;
const LaunchTemplatePrivateDnsNameOptionsRequest = @import("launch_template_private_dns_name_options_request.zig").LaunchTemplatePrivateDnsNameOptionsRequest;
const LaunchTemplateSpecification = @import("launch_template_specification.zig").LaunchTemplateSpecification;
const LaunchTemplateSpotMarketOptions = @import("launch_template_spot_market_options.zig").LaunchTemplateSpotMarketOptions;
const LaunchTemplateSpotMarketOptionsRequest = @import("launch_template_spot_market_options_request.zig").LaunchTemplateSpotMarketOptionsRequest;
const LaunchTemplateTagSpecification = @import("launch_template_tag_specification.zig").LaunchTemplateTagSpecification;
const LaunchTemplateTagSpecificationRequest = @import("launch_template_tag_specification_request.zig").LaunchTemplateTagSpecificationRequest;
const LaunchTemplateVersion = @import("launch_template_version.zig").LaunchTemplateVersion;
const LaunchTemplatesMonitoring = @import("launch_templates_monitoring.zig").LaunchTemplatesMonitoring;
const LaunchTemplatesMonitoringRequest = @import("launch_templates_monitoring_request.zig").LaunchTemplatesMonitoringRequest;
const LicenseConfiguration = @import("license_configuration.zig").LicenseConfiguration;
const LicenseConfigurationRequest = @import("license_configuration_request.zig").LicenseConfigurationRequest;
const ListingState = @import("listing_state.zig").ListingState;
const ListingStatus = @import("listing_status.zig").ListingStatus;
const LoadBalancersConfig = @import("load_balancers_config.zig").LoadBalancersConfig;
const LoadPermission = @import("load_permission.zig").LoadPermission;
const LoadPermissionModifications = @import("load_permission_modifications.zig").LoadPermissionModifications;
const LoadPermissionRequest = @import("load_permission_request.zig").LoadPermissionRequest;
const LocalGateway = @import("local_gateway.zig").LocalGateway;
const LocalGatewayRoute = @import("local_gateway_route.zig").LocalGatewayRoute;
const LocalGatewayRouteState = @import("local_gateway_route_state.zig").LocalGatewayRouteState;
const LocalGatewayRouteTable = @import("local_gateway_route_table.zig").LocalGatewayRouteTable;
const LocalGatewayRouteTableMode = @import("local_gateway_route_table_mode.zig").LocalGatewayRouteTableMode;
const LocalGatewayRouteTableVirtualInterfaceGroupAssociation = @import("local_gateway_route_table_virtual_interface_group_association.zig").LocalGatewayRouteTableVirtualInterfaceGroupAssociation;
const LocalGatewayRouteTableVpcAssociation = @import("local_gateway_route_table_vpc_association.zig").LocalGatewayRouteTableVpcAssociation;
const LocalGatewayRouteType = @import("local_gateway_route_type.zig").LocalGatewayRouteType;
const LocalGatewayVirtualInterface = @import("local_gateway_virtual_interface.zig").LocalGatewayVirtualInterface;
const LocalGatewayVirtualInterfaceConfigurationState = @import("local_gateway_virtual_interface_configuration_state.zig").LocalGatewayVirtualInterfaceConfigurationState;
const LocalGatewayVirtualInterfaceGroup = @import("local_gateway_virtual_interface_group.zig").LocalGatewayVirtualInterfaceGroup;
const LocalGatewayVirtualInterfaceGroupConfigurationState = @import("local_gateway_virtual_interface_group_configuration_state.zig").LocalGatewayVirtualInterfaceGroupConfigurationState;
const LocalStorage = @import("local_storage.zig").LocalStorage;
const LocalStorageType = @import("local_storage_type.zig").LocalStorageType;
const LocationType = @import("location_type.zig").LocationType;
const LockState = @import("lock_state.zig").LockState;
const LockedSnapshotsInfo = @import("locked_snapshots_info.zig").LockedSnapshotsInfo;
const LogDestinationType = @import("log_destination_type.zig").LogDestinationType;
const MacHost = @import("mac_host.zig").MacHost;
const MacModificationTask = @import("mac_modification_task.zig").MacModificationTask;
const MacModificationTaskState = @import("mac_modification_task_state.zig").MacModificationTaskState;
const MacModificationTaskType = @import("mac_modification_task_type.zig").MacModificationTaskType;
const MacSystemIntegrityProtectionConfiguration = @import("mac_system_integrity_protection_configuration.zig").MacSystemIntegrityProtectionConfiguration;
const MacSystemIntegrityProtectionConfigurationRequest = @import("mac_system_integrity_protection_configuration_request.zig").MacSystemIntegrityProtectionConfigurationRequest;
const MacSystemIntegrityProtectionSettingStatus = @import("mac_system_integrity_protection_setting_status.zig").MacSystemIntegrityProtectionSettingStatus;
const MaintenanceDetails = @import("maintenance_details.zig").MaintenanceDetails;
const ManagedBy = @import("managed_by.zig").ManagedBy;
const ManagedPrefixList = @import("managed_prefix_list.zig").ManagedPrefixList;
const MarketType = @import("market_type.zig").MarketType;
const MediaAcceleratorInfo = @import("media_accelerator_info.zig").MediaAcceleratorInfo;
const MediaDeviceInfo = @import("media_device_info.zig").MediaDeviceInfo;
const MediaDeviceMemoryInfo = @import("media_device_memory_info.zig").MediaDeviceMemoryInfo;
const MembershipType = @import("membership_type.zig").MembershipType;
const MemoryGiBPerVCpu = @import("memory_gi_b_per_v_cpu.zig").MemoryGiBPerVCpu;
const MemoryGiBPerVCpuRequest = @import("memory_gi_b_per_v_cpu_request.zig").MemoryGiBPerVCpuRequest;
const MemoryInfo = @import("memory_info.zig").MemoryInfo;
const MemoryMiB = @import("memory_mi_b.zig").MemoryMiB;
const MemoryMiBRequest = @import("memory_mi_b_request.zig").MemoryMiBRequest;
const Metric = @import("metric.zig").Metric;
const MetricDataResult = @import("metric_data_result.zig").MetricDataResult;
const MetricPoint = @import("metric_point.zig").MetricPoint;
const MetricType = @import("metric_type.zig").MetricType;
const MetricValue = @import("metric_value.zig").MetricValue;
const ModifyTransitGatewayOptions = @import("modify_transit_gateway_options.zig").ModifyTransitGatewayOptions;
const ModifyTransitGatewayVpcAttachmentRequestOptions = @import("modify_transit_gateway_vpc_attachment_request_options.zig").ModifyTransitGatewayVpcAttachmentRequestOptions;
const ModifyVerifiedAccessEndpointCidrOptions = @import("modify_verified_access_endpoint_cidr_options.zig").ModifyVerifiedAccessEndpointCidrOptions;
const ModifyVerifiedAccessEndpointEniOptions = @import("modify_verified_access_endpoint_eni_options.zig").ModifyVerifiedAccessEndpointEniOptions;
const ModifyVerifiedAccessEndpointLoadBalancerOptions = @import("modify_verified_access_endpoint_load_balancer_options.zig").ModifyVerifiedAccessEndpointLoadBalancerOptions;
const ModifyVerifiedAccessEndpointPortRange = @import("modify_verified_access_endpoint_port_range.zig").ModifyVerifiedAccessEndpointPortRange;
const ModifyVerifiedAccessEndpointRdsOptions = @import("modify_verified_access_endpoint_rds_options.zig").ModifyVerifiedAccessEndpointRdsOptions;
const ModifyVerifiedAccessNativeApplicationOidcOptions = @import("modify_verified_access_native_application_oidc_options.zig").ModifyVerifiedAccessNativeApplicationOidcOptions;
const ModifyVerifiedAccessTrustProviderDeviceOptions = @import("modify_verified_access_trust_provider_device_options.zig").ModifyVerifiedAccessTrustProviderDeviceOptions;
const ModifyVerifiedAccessTrustProviderOidcOptions = @import("modify_verified_access_trust_provider_oidc_options.zig").ModifyVerifiedAccessTrustProviderOidcOptions;
const ModifyVpnTunnelOptionsSpecification = @import("modify_vpn_tunnel_options_specification.zig").ModifyVpnTunnelOptionsSpecification;
const Monitoring = @import("monitoring.zig").Monitoring;
const MonitoringState = @import("monitoring_state.zig").MonitoringState;
const MoveStatus = @import("move_status.zig").MoveStatus;
const MovingAddressStatus = @import("moving_address_status.zig").MovingAddressStatus;
const MulticastSupportValue = @import("multicast_support_value.zig").MulticastSupportValue;
const NatGateway = @import("nat_gateway.zig").NatGateway;
const NatGatewayAddress = @import("nat_gateway_address.zig").NatGatewayAddress;
const NatGatewayAddressStatus = @import("nat_gateway_address_status.zig").NatGatewayAddressStatus;
const NatGatewayApplianceModifyState = @import("nat_gateway_appliance_modify_state.zig").NatGatewayApplianceModifyState;
const NatGatewayApplianceState = @import("nat_gateway_appliance_state.zig").NatGatewayApplianceState;
const NatGatewayApplianceType = @import("nat_gateway_appliance_type.zig").NatGatewayApplianceType;
const NatGatewayAttachedAppliance = @import("nat_gateway_attached_appliance.zig").NatGatewayAttachedAppliance;
const NatGatewayState = @import("nat_gateway_state.zig").NatGatewayState;
const NativeApplicationOidcOptions = @import("native_application_oidc_options.zig").NativeApplicationOidcOptions;
const NetworkAcl = @import("network_acl.zig").NetworkAcl;
const NetworkAclAssociation = @import("network_acl_association.zig").NetworkAclAssociation;
const NetworkAclEntry = @import("network_acl_entry.zig").NetworkAclEntry;
const NetworkBandwidthGbps = @import("network_bandwidth_gbps.zig").NetworkBandwidthGbps;
const NetworkBandwidthGbpsRequest = @import("network_bandwidth_gbps_request.zig").NetworkBandwidthGbpsRequest;
const NetworkCardInfo = @import("network_card_info.zig").NetworkCardInfo;
const NetworkInfo = @import("network_info.zig").NetworkInfo;
const NetworkInsightsAccessScope = @import("network_insights_access_scope.zig").NetworkInsightsAccessScope;
const NetworkInsightsAccessScopeAnalysis = @import("network_insights_access_scope_analysis.zig").NetworkInsightsAccessScopeAnalysis;
const NetworkInsightsAccessScopeContent = @import("network_insights_access_scope_content.zig").NetworkInsightsAccessScopeContent;
const NetworkInsightsAnalysis = @import("network_insights_analysis.zig").NetworkInsightsAnalysis;
const NetworkInsightsPath = @import("network_insights_path.zig").NetworkInsightsPath;
const NetworkInterface = @import("network_interface.zig").NetworkInterface;
const NetworkInterfaceAssociation = @import("network_interface_association.zig").NetworkInterfaceAssociation;
const NetworkInterfaceAttachment = @import("network_interface_attachment.zig").NetworkInterfaceAttachment;
const NetworkInterfaceAttachmentChanges = @import("network_interface_attachment_changes.zig").NetworkInterfaceAttachmentChanges;
const NetworkInterfaceCount = @import("network_interface_count.zig").NetworkInterfaceCount;
const NetworkInterfaceCountRequest = @import("network_interface_count_request.zig").NetworkInterfaceCountRequest;
const NetworkInterfaceIpv6Address = @import("network_interface_ipv_6_address.zig").NetworkInterfaceIpv6Address;
const NetworkInterfacePermission = @import("network_interface_permission.zig").NetworkInterfacePermission;
const NetworkInterfacePermissionState = @import("network_interface_permission_state.zig").NetworkInterfacePermissionState;
const NetworkInterfacePermissionStateCode = @import("network_interface_permission_state_code.zig").NetworkInterfacePermissionStateCode;
const NetworkInterfacePrivateIpAddress = @import("network_interface_private_ip_address.zig").NetworkInterfacePrivateIpAddress;
const NetworkInterfaceStatus = @import("network_interface_status.zig").NetworkInterfaceStatus;
const NetworkInterfaceType = @import("network_interface_type.zig").NetworkInterfaceType;
const NeuronDeviceCoreInfo = @import("neuron_device_core_info.zig").NeuronDeviceCoreInfo;
const NeuronDeviceInfo = @import("neuron_device_info.zig").NeuronDeviceInfo;
const NeuronDeviceMemoryInfo = @import("neuron_device_memory_info.zig").NeuronDeviceMemoryInfo;
const NeuronInfo = @import("neuron_info.zig").NeuronInfo;
const NewDhcpConfiguration = @import("new_dhcp_configuration.zig").NewDhcpConfiguration;
const NitroEnclavesSupport = @import("nitro_enclaves_support.zig").NitroEnclavesSupport;
const NitroTpmInfo = @import("nitro_tpm_info.zig").NitroTpmInfo;
const NitroTpmSupport = @import("nitro_tpm_support.zig").NitroTpmSupport;
const OfferingClassType = @import("offering_class_type.zig").OfferingClassType;
const OfferingTypeValues = @import("offering_type_values.zig").OfferingTypeValues;
const OidcOptions = @import("oidc_options.zig").OidcOptions;
const OnDemandAllocationStrategy = @import("on_demand_allocation_strategy.zig").OnDemandAllocationStrategy;
const OnDemandOptions = @import("on_demand_options.zig").OnDemandOptions;
const OnDemandOptionsRequest = @import("on_demand_options_request.zig").OnDemandOptionsRequest;
const OperatorRequest = @import("operator_request.zig").OperatorRequest;
const OperatorResponse = @import("operator_response.zig").OperatorResponse;
const OutpostLag = @import("outpost_lag.zig").OutpostLag;
const OutputFormat = @import("output_format.zig").OutputFormat;
const PacketHeaderStatement = @import("packet_header_statement.zig").PacketHeaderStatement;
const PacketHeaderStatementRequest = @import("packet_header_statement_request.zig").PacketHeaderStatementRequest;
const PartitionLoadFrequency = @import("partition_load_frequency.zig").PartitionLoadFrequency;
const PathComponent = @import("path_component.zig").PathComponent;
const PathFilter = @import("path_filter.zig").PathFilter;
const PathRequestFilter = @import("path_request_filter.zig").PathRequestFilter;
const PathStatement = @import("path_statement.zig").PathStatement;
const PathStatementRequest = @import("path_statement_request.zig").PathStatementRequest;
const PayerResponsibility = @import("payer_responsibility.zig").PayerResponsibility;
const PaymentOption = @import("payment_option.zig").PaymentOption;
const PciId = @import("pci_id.zig").PciId;
const PeeringAttachmentStatus = @import("peering_attachment_status.zig").PeeringAttachmentStatus;
const PeeringConnectionOptions = @import("peering_connection_options.zig").PeeringConnectionOptions;
const PeeringConnectionOptionsRequest = @import("peering_connection_options_request.zig").PeeringConnectionOptionsRequest;
const PeeringTgwInfo = @import("peering_tgw_info.zig").PeeringTgwInfo;
const PerformanceFactorReference = @import("performance_factor_reference.zig").PerformanceFactorReference;
const PerformanceFactorReferenceRequest = @import("performance_factor_reference_request.zig").PerformanceFactorReferenceRequest;
const PeriodType = @import("period_type.zig").PeriodType;
const PermissionGroup = @import("permission_group.zig").PermissionGroup;
const Phase1DHGroupNumbersListValue = @import("phase_1_dh_group_numbers_list_value.zig").Phase1DHGroupNumbersListValue;
const Phase1DHGroupNumbersRequestListValue = @import("phase_1_dh_group_numbers_request_list_value.zig").Phase1DHGroupNumbersRequestListValue;
const Phase1EncryptionAlgorithmsListValue = @import("phase_1_encryption_algorithms_list_value.zig").Phase1EncryptionAlgorithmsListValue;
const Phase1EncryptionAlgorithmsRequestListValue = @import("phase_1_encryption_algorithms_request_list_value.zig").Phase1EncryptionAlgorithmsRequestListValue;
const Phase1IntegrityAlgorithmsListValue = @import("phase_1_integrity_algorithms_list_value.zig").Phase1IntegrityAlgorithmsListValue;
const Phase1IntegrityAlgorithmsRequestListValue = @import("phase_1_integrity_algorithms_request_list_value.zig").Phase1IntegrityAlgorithmsRequestListValue;
const Phase2DHGroupNumbersListValue = @import("phase_2_dh_group_numbers_list_value.zig").Phase2DHGroupNumbersListValue;
const Phase2DHGroupNumbersRequestListValue = @import("phase_2_dh_group_numbers_request_list_value.zig").Phase2DHGroupNumbersRequestListValue;
const Phase2EncryptionAlgorithmsListValue = @import("phase_2_encryption_algorithms_list_value.zig").Phase2EncryptionAlgorithmsListValue;
const Phase2EncryptionAlgorithmsRequestListValue = @import("phase_2_encryption_algorithms_request_list_value.zig").Phase2EncryptionAlgorithmsRequestListValue;
const Phase2IntegrityAlgorithmsListValue = @import("phase_2_integrity_algorithms_list_value.zig").Phase2IntegrityAlgorithmsListValue;
const Phase2IntegrityAlgorithmsRequestListValue = @import("phase_2_integrity_algorithms_request_list_value.zig").Phase2IntegrityAlgorithmsRequestListValue;
const PhcSupport = @import("phc_support.zig").PhcSupport;
const Placement = @import("placement.zig").Placement;
const PlacementGroup = @import("placement_group.zig").PlacementGroup;
const PlacementGroupInfo = @import("placement_group_info.zig").PlacementGroupInfo;
const PlacementGroupState = @import("placement_group_state.zig").PlacementGroupState;
const PlacementGroupStrategy = @import("placement_group_strategy.zig").PlacementGroupStrategy;
const PlacementResponse = @import("placement_response.zig").PlacementResponse;
const PlacementStrategy = @import("placement_strategy.zig").PlacementStrategy;
const PlatformValues = @import("platform_values.zig").PlatformValues;
const PoolCidrBlock = @import("pool_cidr_block.zig").PoolCidrBlock;
const PortRange = @import("port_range.zig").PortRange;
const PrefixList = @import("prefix_list.zig").PrefixList;
const PrefixListAssociation = @import("prefix_list_association.zig").PrefixListAssociation;
const PrefixListEntry = @import("prefix_list_entry.zig").PrefixListEntry;
const PrefixListId = @import("prefix_list_id.zig").PrefixListId;
const PrefixListState = @import("prefix_list_state.zig").PrefixListState;
const PriceSchedule = @import("price_schedule.zig").PriceSchedule;
const PriceScheduleSpecification = @import("price_schedule_specification.zig").PriceScheduleSpecification;
const PricingDetail = @import("pricing_detail.zig").PricingDetail;
const PrincipalIdFormat = @import("principal_id_format.zig").PrincipalIdFormat;
const PrincipalType = @import("principal_type.zig").PrincipalType;
const PrivateDnsDetails = @import("private_dns_details.zig").PrivateDnsDetails;
const PrivateDnsNameConfiguration = @import("private_dns_name_configuration.zig").PrivateDnsNameConfiguration;
const PrivateDnsNameOptionsOnLaunch = @import("private_dns_name_options_on_launch.zig").PrivateDnsNameOptionsOnLaunch;
const PrivateDnsNameOptionsRequest = @import("private_dns_name_options_request.zig").PrivateDnsNameOptionsRequest;
const PrivateDnsNameOptionsResponse = @import("private_dns_name_options_response.zig").PrivateDnsNameOptionsResponse;
const PrivateIpAddressSpecification = @import("private_ip_address_specification.zig").PrivateIpAddressSpecification;
const ProcessorInfo = @import("processor_info.zig").ProcessorInfo;
const ProductCode = @import("product_code.zig").ProductCode;
const ProductCodeValues = @import("product_code_values.zig").ProductCodeValues;
const PropagatingVgw = @import("propagating_vgw.zig").PropagatingVgw;
const Protocol = @import("protocol.zig").Protocol;
const ProtocolValue = @import("protocol_value.zig").ProtocolValue;
const ProvisionedBandwidth = @import("provisioned_bandwidth.zig").ProvisionedBandwidth;
const PtrUpdateStatus = @import("ptr_update_status.zig").PtrUpdateStatus;
const PublicIpDnsNameOptions = @import("public_ip_dns_name_options.zig").PublicIpDnsNameOptions;
const PublicIpv4Pool = @import("public_ipv_4_pool.zig").PublicIpv4Pool;
const PublicIpv4PoolRange = @import("public_ipv_4_pool_range.zig").PublicIpv4PoolRange;
const Purchase = @import("purchase.zig").Purchase;
const PurchaseRequest = @import("purchase_request.zig").PurchaseRequest;
const RIProductDescription = @import("ri_product_description.zig").RIProductDescription;
const RebootMigrationSupport = @import("reboot_migration_support.zig").RebootMigrationSupport;
const RecurringCharge = @import("recurring_charge.zig").RecurringCharge;
const RecurringChargeFrequency = @import("recurring_charge_frequency.zig").RecurringChargeFrequency;
const ReferencedSecurityGroup = @import("referenced_security_group.zig").ReferencedSecurityGroup;
const Region = @import("region.zig").Region;
const RegionalSummary = @import("regional_summary.zig").RegionalSummary;
const RegisterInstanceTagAttributeRequest = @import("register_instance_tag_attribute_request.zig").RegisterInstanceTagAttributeRequest;
const RegisteredInstance = @import("registered_instance.zig").RegisteredInstance;
const RemoveIpamOperatingRegion = @import("remove_ipam_operating_region.zig").RemoveIpamOperatingRegion;
const RemoveIpamOrganizationalUnitExclusion = @import("remove_ipam_organizational_unit_exclusion.zig").RemoveIpamOrganizationalUnitExclusion;
const RemovePrefixListEntry = @import("remove_prefix_list_entry.zig").RemovePrefixListEntry;
const ReplaceRootVolumeTask = @import("replace_root_volume_task.zig").ReplaceRootVolumeTask;
const ReplaceRootVolumeTaskState = @import("replace_root_volume_task_state.zig").ReplaceRootVolumeTaskState;
const ReplacementStrategy = @import("replacement_strategy.zig").ReplacementStrategy;
const ReportInstanceReasonCodes = @import("report_instance_reason_codes.zig").ReportInstanceReasonCodes;
const ReportState = @import("report_state.zig").ReportState;
const RequestFilterPortRange = @import("request_filter_port_range.zig").RequestFilterPortRange;
const RequestIpamResourceTag = @import("request_ipam_resource_tag.zig").RequestIpamResourceTag;
const RequestLaunchTemplateData = @import("request_launch_template_data.zig").RequestLaunchTemplateData;
const RequestSpotLaunchSpecification = @import("request_spot_launch_specification.zig").RequestSpotLaunchSpecification;
const Reservation = @import("reservation.zig").Reservation;
const ReservationEndDateType = @import("reservation_end_date_type.zig").ReservationEndDateType;
const ReservationFleetInstanceSpecification = @import("reservation_fleet_instance_specification.zig").ReservationFleetInstanceSpecification;
const ReservationState = @import("reservation_state.zig").ReservationState;
const ReservationType = @import("reservation_type.zig").ReservationType;
const ReservationValue = @import("reservation_value.zig").ReservationValue;
const ReservedInstanceLimitPrice = @import("reserved_instance_limit_price.zig").ReservedInstanceLimitPrice;
const ReservedInstanceReservationValue = @import("reserved_instance_reservation_value.zig").ReservedInstanceReservationValue;
const ReservedInstanceState = @import("reserved_instance_state.zig").ReservedInstanceState;
const ReservedInstances = @import("reserved_instances.zig").ReservedInstances;
const ReservedInstancesConfiguration = @import("reserved_instances_configuration.zig").ReservedInstancesConfiguration;
const ReservedInstancesId = @import("reserved_instances_id.zig").ReservedInstancesId;
const ReservedInstancesListing = @import("reserved_instances_listing.zig").ReservedInstancesListing;
const ReservedInstancesModification = @import("reserved_instances_modification.zig").ReservedInstancesModification;
const ReservedInstancesModificationResult = @import("reserved_instances_modification_result.zig").ReservedInstancesModificationResult;
const ReservedInstancesOffering = @import("reserved_instances_offering.zig").ReservedInstancesOffering;
const ResourceStatement = @import("resource_statement.zig").ResourceStatement;
const ResourceStatementRequest = @import("resource_statement_request.zig").ResourceStatementRequest;
const ResourceType = @import("resource_type.zig").ResourceType;
const ResourceTypeOption = @import("resource_type_option.zig").ResourceTypeOption;
const ResourceTypeRequest = @import("resource_type_request.zig").ResourceTypeRequest;
const ResponseError = @import("response_error.zig").ResponseError;
const ResponseLaunchTemplateData = @import("response_launch_template_data.zig").ResponseLaunchTemplateData;
const RevokedSecurityGroupRule = @import("revoked_security_group_rule.zig").RevokedSecurityGroupRule;
const RootDeviceType = @import("root_device_type.zig").RootDeviceType;
const Route = @import("route.zig").Route;
const RouteOrigin = @import("route_origin.zig").RouteOrigin;
const RouteServer = @import("route_server.zig").RouteServer;
const RouteServerAssociation = @import("route_server_association.zig").RouteServerAssociation;
const RouteServerAssociationState = @import("route_server_association_state.zig").RouteServerAssociationState;
const RouteServerBfdState = @import("route_server_bfd_state.zig").RouteServerBfdState;
const RouteServerBfdStatus = @import("route_server_bfd_status.zig").RouteServerBfdStatus;
const RouteServerBgpOptions = @import("route_server_bgp_options.zig").RouteServerBgpOptions;
const RouteServerBgpOptionsRequest = @import("route_server_bgp_options_request.zig").RouteServerBgpOptionsRequest;
const RouteServerBgpState = @import("route_server_bgp_state.zig").RouteServerBgpState;
const RouteServerBgpStatus = @import("route_server_bgp_status.zig").RouteServerBgpStatus;
const RouteServerEndpoint = @import("route_server_endpoint.zig").RouteServerEndpoint;
const RouteServerEndpointState = @import("route_server_endpoint_state.zig").RouteServerEndpointState;
const RouteServerPeer = @import("route_server_peer.zig").RouteServerPeer;
const RouteServerPeerLivenessMode = @import("route_server_peer_liveness_mode.zig").RouteServerPeerLivenessMode;
const RouteServerPeerState = @import("route_server_peer_state.zig").RouteServerPeerState;
const RouteServerPersistRoutesState = @import("route_server_persist_routes_state.zig").RouteServerPersistRoutesState;
const RouteServerPropagation = @import("route_server_propagation.zig").RouteServerPropagation;
const RouteServerPropagationState = @import("route_server_propagation_state.zig").RouteServerPropagationState;
const RouteServerRoute = @import("route_server_route.zig").RouteServerRoute;
const RouteServerRouteInstallationDetail = @import("route_server_route_installation_detail.zig").RouteServerRouteInstallationDetail;
const RouteServerRouteInstallationStatus = @import("route_server_route_installation_status.zig").RouteServerRouteInstallationStatus;
const RouteServerRouteStatus = @import("route_server_route_status.zig").RouteServerRouteStatus;
const RouteServerState = @import("route_server_state.zig").RouteServerState;
const RouteState = @import("route_state.zig").RouteState;
const RouteTable = @import("route_table.zig").RouteTable;
const RouteTableAssociation = @import("route_table_association.zig").RouteTableAssociation;
const RouteTableAssociationState = @import("route_table_association_state.zig").RouteTableAssociationState;
const RouteTableAssociationStateCode = @import("route_table_association_state_code.zig").RouteTableAssociationStateCode;
const RuleAction = @import("rule_action.zig").RuleAction;
const RuleGroupRuleOptionsPair = @import("rule_group_rule_options_pair.zig").RuleGroupRuleOptionsPair;
const RuleGroupTypePair = @import("rule_group_type_pair.zig").RuleGroupTypePair;
const RuleOption = @import("rule_option.zig").RuleOption;
const RunInstancesMonitoringEnabled = @import("run_instances_monitoring_enabled.zig").RunInstancesMonitoringEnabled;
const S3ObjectTag = @import("s3_object_tag.zig").S3ObjectTag;
const S3Storage = @import("s3_storage.zig").S3Storage;
const SSEType = @import("sse_type.zig").SSEType;
const Schedule = @import("schedule.zig").Schedule;
const ScheduledInstance = @import("scheduled_instance.zig").ScheduledInstance;
const ScheduledInstanceAvailability = @import("scheduled_instance_availability.zig").ScheduledInstanceAvailability;
const ScheduledInstanceRecurrence = @import("scheduled_instance_recurrence.zig").ScheduledInstanceRecurrence;
const ScheduledInstanceRecurrenceRequest = @import("scheduled_instance_recurrence_request.zig").ScheduledInstanceRecurrenceRequest;
const ScheduledInstancesBlockDeviceMapping = @import("scheduled_instances_block_device_mapping.zig").ScheduledInstancesBlockDeviceMapping;
const ScheduledInstancesEbs = @import("scheduled_instances_ebs.zig").ScheduledInstancesEbs;
const ScheduledInstancesIamInstanceProfile = @import("scheduled_instances_iam_instance_profile.zig").ScheduledInstancesIamInstanceProfile;
const ScheduledInstancesIpv6Address = @import("scheduled_instances_ipv_6_address.zig").ScheduledInstancesIpv6Address;
const ScheduledInstancesLaunchSpecification = @import("scheduled_instances_launch_specification.zig").ScheduledInstancesLaunchSpecification;
const ScheduledInstancesMonitoring = @import("scheduled_instances_monitoring.zig").ScheduledInstancesMonitoring;
const ScheduledInstancesNetworkInterface = @import("scheduled_instances_network_interface.zig").ScheduledInstancesNetworkInterface;
const ScheduledInstancesPlacement = @import("scheduled_instances_placement.zig").ScheduledInstancesPlacement;
const ScheduledInstancesPrivateIpAddressConfig = @import("scheduled_instances_private_ip_address_config.zig").ScheduledInstancesPrivateIpAddressConfig;
const SecurityGroup = @import("security_group.zig").SecurityGroup;
const SecurityGroupForVpc = @import("security_group_for_vpc.zig").SecurityGroupForVpc;
const SecurityGroupIdentifier = @import("security_group_identifier.zig").SecurityGroupIdentifier;
const SecurityGroupReference = @import("security_group_reference.zig").SecurityGroupReference;
const SecurityGroupReferencingSupportValue = @import("security_group_referencing_support_value.zig").SecurityGroupReferencingSupportValue;
const SecurityGroupRule = @import("security_group_rule.zig").SecurityGroupRule;
const SecurityGroupRuleDescription = @import("security_group_rule_description.zig").SecurityGroupRuleDescription;
const SecurityGroupRuleRequest = @import("security_group_rule_request.zig").SecurityGroupRuleRequest;
const SecurityGroupRuleUpdate = @import("security_group_rule_update.zig").SecurityGroupRuleUpdate;
const SecurityGroupVpcAssociation = @import("security_group_vpc_association.zig").SecurityGroupVpcAssociation;
const SecurityGroupVpcAssociationState = @import("security_group_vpc_association_state.zig").SecurityGroupVpcAssociationState;
const ServiceConfiguration = @import("service_configuration.zig").ServiceConfiguration;
const ServiceConnectivityType = @import("service_connectivity_type.zig").ServiceConnectivityType;
const ServiceDetail = @import("service_detail.zig").ServiceDetail;
const ServiceLinkVirtualInterface = @import("service_link_virtual_interface.zig").ServiceLinkVirtualInterface;
const ServiceLinkVirtualInterfaceConfigurationState = @import("service_link_virtual_interface_configuration_state.zig").ServiceLinkVirtualInterfaceConfigurationState;
const ServiceManaged = @import("service_managed.zig").ServiceManaged;
const ServiceState = @import("service_state.zig").ServiceState;
const ServiceType = @import("service_type.zig").ServiceType;
const ServiceTypeDetail = @import("service_type_detail.zig").ServiceTypeDetail;
const ShutdownBehavior = @import("shutdown_behavior.zig").ShutdownBehavior;
const SlotDateTimeRangeRequest = @import("slot_date_time_range_request.zig").SlotDateTimeRangeRequest;
const SlotStartTimeRangeRequest = @import("slot_start_time_range_request.zig").SlotStartTimeRangeRequest;
const Snapshot = @import("snapshot.zig").Snapshot;
const SnapshotDetail = @import("snapshot_detail.zig").SnapshotDetail;
const SnapshotDiskContainer = @import("snapshot_disk_container.zig").SnapshotDiskContainer;
const SnapshotInfo = @import("snapshot_info.zig").SnapshotInfo;
const SnapshotRecycleBinInfo = @import("snapshot_recycle_bin_info.zig").SnapshotRecycleBinInfo;
const SnapshotReturnCodes = @import("snapshot_return_codes.zig").SnapshotReturnCodes;
const SnapshotState = @import("snapshot_state.zig").SnapshotState;
const SnapshotTaskDetail = @import("snapshot_task_detail.zig").SnapshotTaskDetail;
const SnapshotTierStatus = @import("snapshot_tier_status.zig").SnapshotTierStatus;
const SpotAllocationStrategy = @import("spot_allocation_strategy.zig").SpotAllocationStrategy;
const SpotCapacityRebalance = @import("spot_capacity_rebalance.zig").SpotCapacityRebalance;
const SpotDatafeedSubscription = @import("spot_datafeed_subscription.zig").SpotDatafeedSubscription;
const SpotFleetLaunchSpecification = @import("spot_fleet_launch_specification.zig").SpotFleetLaunchSpecification;
const SpotFleetMonitoring = @import("spot_fleet_monitoring.zig").SpotFleetMonitoring;
const SpotFleetRequestConfig = @import("spot_fleet_request_config.zig").SpotFleetRequestConfig;
const SpotFleetRequestConfigData = @import("spot_fleet_request_config_data.zig").SpotFleetRequestConfigData;
const SpotFleetTagSpecification = @import("spot_fleet_tag_specification.zig").SpotFleetTagSpecification;
const SpotInstanceInterruptionBehavior = @import("spot_instance_interruption_behavior.zig").SpotInstanceInterruptionBehavior;
const SpotInstanceRequest = @import("spot_instance_request.zig").SpotInstanceRequest;
const SpotInstanceState = @import("spot_instance_state.zig").SpotInstanceState;
const SpotInstanceStateFault = @import("spot_instance_state_fault.zig").SpotInstanceStateFault;
const SpotInstanceStatus = @import("spot_instance_status.zig").SpotInstanceStatus;
const SpotInstanceType = @import("spot_instance_type.zig").SpotInstanceType;
const SpotMaintenanceStrategies = @import("spot_maintenance_strategies.zig").SpotMaintenanceStrategies;
const SpotMarketOptions = @import("spot_market_options.zig").SpotMarketOptions;
const SpotOptions = @import("spot_options.zig").SpotOptions;
const SpotOptionsRequest = @import("spot_options_request.zig").SpotOptionsRequest;
const SpotPlacement = @import("spot_placement.zig").SpotPlacement;
const SpotPlacementScore = @import("spot_placement_score.zig").SpotPlacementScore;
const SpotPrice = @import("spot_price.zig").SpotPrice;
const SpreadLevel = @import("spread_level.zig").SpreadLevel;
const SqlServerLicenseUsage = @import("sql_server_license_usage.zig").SqlServerLicenseUsage;
const StaleIpPermission = @import("stale_ip_permission.zig").StaleIpPermission;
const StaleSecurityGroup = @import("stale_security_group.zig").StaleSecurityGroup;
const State = @import("state.zig").State;
const StateReason = @import("state_reason.zig").StateReason;
const StaticSourcesSupportValue = @import("static_sources_support_value.zig").StaticSourcesSupportValue;
const StatisticType = @import("statistic_type.zig").StatisticType;
const StatusName = @import("status_name.zig").StatusName;
const StatusType = @import("status_type.zig").StatusType;
const Storage = @import("storage.zig").Storage;
const StorageLocation = @import("storage_location.zig").StorageLocation;
const StorageTier = @import("storage_tier.zig").StorageTier;
const StoreImageTaskResult = @import("store_image_task_result.zig").StoreImageTaskResult;
const Subnet = @import("subnet.zig").Subnet;
const SubnetAssociation = @import("subnet_association.zig").SubnetAssociation;
const SubnetCidrBlockState = @import("subnet_cidr_block_state.zig").SubnetCidrBlockState;
const SubnetCidrBlockStateCode = @import("subnet_cidr_block_state_code.zig").SubnetCidrBlockStateCode;
const SubnetCidrReservation = @import("subnet_cidr_reservation.zig").SubnetCidrReservation;
const SubnetCidrReservationType = @import("subnet_cidr_reservation_type.zig").SubnetCidrReservationType;
const SubnetConfiguration = @import("subnet_configuration.zig").SubnetConfiguration;
const SubnetIpPrefixes = @import("subnet_ip_prefixes.zig").SubnetIpPrefixes;
const SubnetIpv6CidrBlockAssociation = @import("subnet_ipv_6_cidr_block_association.zig").SubnetIpv6CidrBlockAssociation;
const SubnetState = @import("subnet_state.zig").SubnetState;
const Subscription = @import("subscription.zig").Subscription;
const SuccessfulInstanceCreditSpecificationItem = @import("successful_instance_credit_specification_item.zig").SuccessfulInstanceCreditSpecificationItem;
const SuccessfulQueuedPurchaseDeletion = @import("successful_queued_purchase_deletion.zig").SuccessfulQueuedPurchaseDeletion;
const SummaryStatus = @import("summary_status.zig").SummaryStatus;
const SupportedAdditionalProcessorFeature = @import("supported_additional_processor_feature.zig").SupportedAdditionalProcessorFeature;
const SupportedRegionDetail = @import("supported_region_detail.zig").SupportedRegionDetail;
const Tag = @import("tag.zig").Tag;
const TagDescription = @import("tag_description.zig").TagDescription;
const TagSpecification = @import("tag_specification.zig").TagSpecification;
const TargetCapacitySpecification = @import("target_capacity_specification.zig").TargetCapacitySpecification;
const TargetCapacitySpecificationRequest = @import("target_capacity_specification_request.zig").TargetCapacitySpecificationRequest;
const TargetCapacityUnitType = @import("target_capacity_unit_type.zig").TargetCapacityUnitType;
const TargetConfiguration = @import("target_configuration.zig").TargetConfiguration;
const TargetConfigurationRequest = @import("target_configuration_request.zig").TargetConfigurationRequest;
const TargetGroup = @import("target_group.zig").TargetGroup;
const TargetGroupsConfig = @import("target_groups_config.zig").TargetGroupsConfig;
const TargetNetwork = @import("target_network.zig").TargetNetwork;
const TargetReservationValue = @import("target_reservation_value.zig").TargetReservationValue;
const TelemetryStatus = @import("telemetry_status.zig").TelemetryStatus;
const Tenancy = @import("tenancy.zig").Tenancy;
const TerminateConnectionStatus = @import("terminate_connection_status.zig").TerminateConnectionStatus;
const ThroughResourcesStatement = @import("through_resources_statement.zig").ThroughResourcesStatement;
const ThroughResourcesStatementRequest = @import("through_resources_statement_request.zig").ThroughResourcesStatementRequest;
const TieringOperationStatus = @import("tiering_operation_status.zig").TieringOperationStatus;
const TokenState = @import("token_state.zig").TokenState;
const TotalLocalStorageGB = @import("total_local_storage_gb.zig").TotalLocalStorageGB;
const TotalLocalStorageGBRequest = @import("total_local_storage_gb_request.zig").TotalLocalStorageGBRequest;
const TpmSupportValues = @import("tpm_support_values.zig").TpmSupportValues;
const TrafficDirection = @import("traffic_direction.zig").TrafficDirection;
const TrafficIpAddressType = @import("traffic_ip_address_type.zig").TrafficIpAddressType;
const TrafficMirrorFilter = @import("traffic_mirror_filter.zig").TrafficMirrorFilter;
const TrafficMirrorFilterRule = @import("traffic_mirror_filter_rule.zig").TrafficMirrorFilterRule;
const TrafficMirrorFilterRuleField = @import("traffic_mirror_filter_rule_field.zig").TrafficMirrorFilterRuleField;
const TrafficMirrorNetworkService = @import("traffic_mirror_network_service.zig").TrafficMirrorNetworkService;
const TrafficMirrorPortRange = @import("traffic_mirror_port_range.zig").TrafficMirrorPortRange;
const TrafficMirrorPortRangeRequest = @import("traffic_mirror_port_range_request.zig").TrafficMirrorPortRangeRequest;
const TrafficMirrorRuleAction = @import("traffic_mirror_rule_action.zig").TrafficMirrorRuleAction;
const TrafficMirrorSession = @import("traffic_mirror_session.zig").TrafficMirrorSession;
const TrafficMirrorSessionField = @import("traffic_mirror_session_field.zig").TrafficMirrorSessionField;
const TrafficMirrorTarget = @import("traffic_mirror_target.zig").TrafficMirrorTarget;
const TrafficMirrorTargetType = @import("traffic_mirror_target_type.zig").TrafficMirrorTargetType;
const TrafficType = @import("traffic_type.zig").TrafficType;
const TransferType = @import("transfer_type.zig").TransferType;
const TransitGateway = @import("transit_gateway.zig").TransitGateway;
const TransitGatewayAssociation = @import("transit_gateway_association.zig").TransitGatewayAssociation;
const TransitGatewayAssociationState = @import("transit_gateway_association_state.zig").TransitGatewayAssociationState;
const TransitGatewayAttachment = @import("transit_gateway_attachment.zig").TransitGatewayAttachment;
const TransitGatewayAttachmentAssociation = @import("transit_gateway_attachment_association.zig").TransitGatewayAttachmentAssociation;
const TransitGatewayAttachmentBgpConfiguration = @import("transit_gateway_attachment_bgp_configuration.zig").TransitGatewayAttachmentBgpConfiguration;
const TransitGatewayAttachmentPropagation = @import("transit_gateway_attachment_propagation.zig").TransitGatewayAttachmentPropagation;
const TransitGatewayAttachmentResourceType = @import("transit_gateway_attachment_resource_type.zig").TransitGatewayAttachmentResourceType;
const TransitGatewayAttachmentState = @import("transit_gateway_attachment_state.zig").TransitGatewayAttachmentState;
const TransitGatewayConnect = @import("transit_gateway_connect.zig").TransitGatewayConnect;
const TransitGatewayConnectOptions = @import("transit_gateway_connect_options.zig").TransitGatewayConnectOptions;
const TransitGatewayConnectPeer = @import("transit_gateway_connect_peer.zig").TransitGatewayConnectPeer;
const TransitGatewayConnectPeerConfiguration = @import("transit_gateway_connect_peer_configuration.zig").TransitGatewayConnectPeerConfiguration;
const TransitGatewayConnectPeerState = @import("transit_gateway_connect_peer_state.zig").TransitGatewayConnectPeerState;
const TransitGatewayConnectRequestBgpOptions = @import("transit_gateway_connect_request_bgp_options.zig").TransitGatewayConnectRequestBgpOptions;
const TransitGatewayMeteringPayerType = @import("transit_gateway_metering_payer_type.zig").TransitGatewayMeteringPayerType;
const TransitGatewayMeteringPolicy = @import("transit_gateway_metering_policy.zig").TransitGatewayMeteringPolicy;
const TransitGatewayMeteringPolicyEntry = @import("transit_gateway_metering_policy_entry.zig").TransitGatewayMeteringPolicyEntry;
const TransitGatewayMeteringPolicyEntryState = @import("transit_gateway_metering_policy_entry_state.zig").TransitGatewayMeteringPolicyEntryState;
const TransitGatewayMeteringPolicyRule = @import("transit_gateway_metering_policy_rule.zig").TransitGatewayMeteringPolicyRule;
const TransitGatewayMeteringPolicyState = @import("transit_gateway_metering_policy_state.zig").TransitGatewayMeteringPolicyState;
const TransitGatewayMulitcastDomainAssociationState = @import("transit_gateway_mulitcast_domain_association_state.zig").TransitGatewayMulitcastDomainAssociationState;
const TransitGatewayMulticastDeregisteredGroupMembers = @import("transit_gateway_multicast_deregistered_group_members.zig").TransitGatewayMulticastDeregisteredGroupMembers;
const TransitGatewayMulticastDeregisteredGroupSources = @import("transit_gateway_multicast_deregistered_group_sources.zig").TransitGatewayMulticastDeregisteredGroupSources;
const TransitGatewayMulticastDomain = @import("transit_gateway_multicast_domain.zig").TransitGatewayMulticastDomain;
const TransitGatewayMulticastDomainAssociation = @import("transit_gateway_multicast_domain_association.zig").TransitGatewayMulticastDomainAssociation;
const TransitGatewayMulticastDomainAssociations = @import("transit_gateway_multicast_domain_associations.zig").TransitGatewayMulticastDomainAssociations;
const TransitGatewayMulticastDomainOptions = @import("transit_gateway_multicast_domain_options.zig").TransitGatewayMulticastDomainOptions;
const TransitGatewayMulticastDomainState = @import("transit_gateway_multicast_domain_state.zig").TransitGatewayMulticastDomainState;
const TransitGatewayMulticastGroup = @import("transit_gateway_multicast_group.zig").TransitGatewayMulticastGroup;
const TransitGatewayMulticastRegisteredGroupMembers = @import("transit_gateway_multicast_registered_group_members.zig").TransitGatewayMulticastRegisteredGroupMembers;
const TransitGatewayMulticastRegisteredGroupSources = @import("transit_gateway_multicast_registered_group_sources.zig").TransitGatewayMulticastRegisteredGroupSources;
const TransitGatewayOptions = @import("transit_gateway_options.zig").TransitGatewayOptions;
const TransitGatewayPeeringAttachment = @import("transit_gateway_peering_attachment.zig").TransitGatewayPeeringAttachment;
const TransitGatewayPeeringAttachmentOptions = @import("transit_gateway_peering_attachment_options.zig").TransitGatewayPeeringAttachmentOptions;
const TransitGatewayPolicyRule = @import("transit_gateway_policy_rule.zig").TransitGatewayPolicyRule;
const TransitGatewayPolicyRuleMetaData = @import("transit_gateway_policy_rule_meta_data.zig").TransitGatewayPolicyRuleMetaData;
const TransitGatewayPolicyTable = @import("transit_gateway_policy_table.zig").TransitGatewayPolicyTable;
const TransitGatewayPolicyTableAssociation = @import("transit_gateway_policy_table_association.zig").TransitGatewayPolicyTableAssociation;
const TransitGatewayPolicyTableEntry = @import("transit_gateway_policy_table_entry.zig").TransitGatewayPolicyTableEntry;
const TransitGatewayPolicyTableState = @import("transit_gateway_policy_table_state.zig").TransitGatewayPolicyTableState;
const TransitGatewayPrefixListAttachment = @import("transit_gateway_prefix_list_attachment.zig").TransitGatewayPrefixListAttachment;
const TransitGatewayPrefixListReference = @import("transit_gateway_prefix_list_reference.zig").TransitGatewayPrefixListReference;
const TransitGatewayPrefixListReferenceState = @import("transit_gateway_prefix_list_reference_state.zig").TransitGatewayPrefixListReferenceState;
const TransitGatewayPropagation = @import("transit_gateway_propagation.zig").TransitGatewayPropagation;
const TransitGatewayPropagationState = @import("transit_gateway_propagation_state.zig").TransitGatewayPropagationState;
const TransitGatewayRequestOptions = @import("transit_gateway_request_options.zig").TransitGatewayRequestOptions;
const TransitGatewayRoute = @import("transit_gateway_route.zig").TransitGatewayRoute;
const TransitGatewayRouteAttachment = @import("transit_gateway_route_attachment.zig").TransitGatewayRouteAttachment;
const TransitGatewayRouteState = @import("transit_gateway_route_state.zig").TransitGatewayRouteState;
const TransitGatewayRouteTable = @import("transit_gateway_route_table.zig").TransitGatewayRouteTable;
const TransitGatewayRouteTableAnnouncement = @import("transit_gateway_route_table_announcement.zig").TransitGatewayRouteTableAnnouncement;
const TransitGatewayRouteTableAnnouncementDirection = @import("transit_gateway_route_table_announcement_direction.zig").TransitGatewayRouteTableAnnouncementDirection;
const TransitGatewayRouteTableAnnouncementState = @import("transit_gateway_route_table_announcement_state.zig").TransitGatewayRouteTableAnnouncementState;
const TransitGatewayRouteTableAssociation = @import("transit_gateway_route_table_association.zig").TransitGatewayRouteTableAssociation;
const TransitGatewayRouteTablePropagation = @import("transit_gateway_route_table_propagation.zig").TransitGatewayRouteTablePropagation;
const TransitGatewayRouteTableRoute = @import("transit_gateway_route_table_route.zig").TransitGatewayRouteTableRoute;
const TransitGatewayRouteTableState = @import("transit_gateway_route_table_state.zig").TransitGatewayRouteTableState;
const TransitGatewayRouteType = @import("transit_gateway_route_type.zig").TransitGatewayRouteType;
const TransitGatewayState = @import("transit_gateway_state.zig").TransitGatewayState;
const TransitGatewayVpcAttachment = @import("transit_gateway_vpc_attachment.zig").TransitGatewayVpcAttachment;
const TransitGatewayVpcAttachmentOptions = @import("transit_gateway_vpc_attachment_options.zig").TransitGatewayVpcAttachmentOptions;
const TransportProtocol = @import("transport_protocol.zig").TransportProtocol;
const TrunkInterfaceAssociation = @import("trunk_interface_association.zig").TrunkInterfaceAssociation;
const TrustProviderType = @import("trust_provider_type.zig").TrustProviderType;
const TunnelInsideIpVersion = @import("tunnel_inside_ip_version.zig").TunnelInsideIpVersion;
const TunnelOption = @import("tunnel_option.zig").TunnelOption;
const UnlimitedSupportedInstanceFamily = @import("unlimited_supported_instance_family.zig").UnlimitedSupportedInstanceFamily;
const UnsuccessfulInstanceCreditSpecificationErrorCode = @import("unsuccessful_instance_credit_specification_error_code.zig").UnsuccessfulInstanceCreditSpecificationErrorCode;
const UnsuccessfulInstanceCreditSpecificationItem = @import("unsuccessful_instance_credit_specification_item.zig").UnsuccessfulInstanceCreditSpecificationItem;
const UnsuccessfulInstanceCreditSpecificationItemError = @import("unsuccessful_instance_credit_specification_item_error.zig").UnsuccessfulInstanceCreditSpecificationItemError;
const UnsuccessfulItem = @import("unsuccessful_item.zig").UnsuccessfulItem;
const UnsuccessfulItemError = @import("unsuccessful_item_error.zig").UnsuccessfulItemError;
const UsageClassType = @import("usage_class_type.zig").UsageClassType;
const UserBucket = @import("user_bucket.zig").UserBucket;
const UserBucketDetails = @import("user_bucket_details.zig").UserBucketDetails;
const UserData = @import("user_data.zig").UserData;
const UserIdGroupPair = @import("user_id_group_pair.zig").UserIdGroupPair;
const UserTrustProviderType = @import("user_trust_provider_type.zig").UserTrustProviderType;
const VCpuCountRange = @import("v_cpu_count_range.zig").VCpuCountRange;
const VCpuCountRangeRequest = @import("v_cpu_count_range_request.zig").VCpuCountRangeRequest;
const VCpuInfo = @import("v_cpu_info.zig").VCpuInfo;
const ValidationError = @import("validation_error.zig").ValidationError;
const ValidationWarning = @import("validation_warning.zig").ValidationWarning;
const VerifiedAccessEndpoint = @import("verified_access_endpoint.zig").VerifiedAccessEndpoint;
const VerifiedAccessEndpointAttachmentType = @import("verified_access_endpoint_attachment_type.zig").VerifiedAccessEndpointAttachmentType;
const VerifiedAccessEndpointCidrOptions = @import("verified_access_endpoint_cidr_options.zig").VerifiedAccessEndpointCidrOptions;
const VerifiedAccessEndpointEniOptions = @import("verified_access_endpoint_eni_options.zig").VerifiedAccessEndpointEniOptions;
const VerifiedAccessEndpointLoadBalancerOptions = @import("verified_access_endpoint_load_balancer_options.zig").VerifiedAccessEndpointLoadBalancerOptions;
const VerifiedAccessEndpointPortRange = @import("verified_access_endpoint_port_range.zig").VerifiedAccessEndpointPortRange;
const VerifiedAccessEndpointProtocol = @import("verified_access_endpoint_protocol.zig").VerifiedAccessEndpointProtocol;
const VerifiedAccessEndpointRdsOptions = @import("verified_access_endpoint_rds_options.zig").VerifiedAccessEndpointRdsOptions;
const VerifiedAccessEndpointStatus = @import("verified_access_endpoint_status.zig").VerifiedAccessEndpointStatus;
const VerifiedAccessEndpointStatusCode = @import("verified_access_endpoint_status_code.zig").VerifiedAccessEndpointStatusCode;
const VerifiedAccessEndpointTarget = @import("verified_access_endpoint_target.zig").VerifiedAccessEndpointTarget;
const VerifiedAccessEndpointType = @import("verified_access_endpoint_type.zig").VerifiedAccessEndpointType;
const VerifiedAccessGroup = @import("verified_access_group.zig").VerifiedAccessGroup;
const VerifiedAccessInstance = @import("verified_access_instance.zig").VerifiedAccessInstance;
const VerifiedAccessInstanceCustomSubDomain = @import("verified_access_instance_custom_sub_domain.zig").VerifiedAccessInstanceCustomSubDomain;
const VerifiedAccessInstanceLoggingConfiguration = @import("verified_access_instance_logging_configuration.zig").VerifiedAccessInstanceLoggingConfiguration;
const VerifiedAccessInstanceOpenVpnClientConfiguration = @import("verified_access_instance_open_vpn_client_configuration.zig").VerifiedAccessInstanceOpenVpnClientConfiguration;
const VerifiedAccessInstanceOpenVpnClientConfigurationRoute = @import("verified_access_instance_open_vpn_client_configuration_route.zig").VerifiedAccessInstanceOpenVpnClientConfigurationRoute;
const VerifiedAccessInstanceUserTrustProviderClientConfiguration = @import("verified_access_instance_user_trust_provider_client_configuration.zig").VerifiedAccessInstanceUserTrustProviderClientConfiguration;
const VerifiedAccessLogCloudWatchLogsDestination = @import("verified_access_log_cloud_watch_logs_destination.zig").VerifiedAccessLogCloudWatchLogsDestination;
const VerifiedAccessLogCloudWatchLogsDestinationOptions = @import("verified_access_log_cloud_watch_logs_destination_options.zig").VerifiedAccessLogCloudWatchLogsDestinationOptions;
const VerifiedAccessLogDeliveryStatus = @import("verified_access_log_delivery_status.zig").VerifiedAccessLogDeliveryStatus;
const VerifiedAccessLogDeliveryStatusCode = @import("verified_access_log_delivery_status_code.zig").VerifiedAccessLogDeliveryStatusCode;
const VerifiedAccessLogKinesisDataFirehoseDestination = @import("verified_access_log_kinesis_data_firehose_destination.zig").VerifiedAccessLogKinesisDataFirehoseDestination;
const VerifiedAccessLogKinesisDataFirehoseDestinationOptions = @import("verified_access_log_kinesis_data_firehose_destination_options.zig").VerifiedAccessLogKinesisDataFirehoseDestinationOptions;
const VerifiedAccessLogOptions = @import("verified_access_log_options.zig").VerifiedAccessLogOptions;
const VerifiedAccessLogS3Destination = @import("verified_access_log_s3_destination.zig").VerifiedAccessLogS3Destination;
const VerifiedAccessLogS3DestinationOptions = @import("verified_access_log_s3_destination_options.zig").VerifiedAccessLogS3DestinationOptions;
const VerifiedAccessLogs = @import("verified_access_logs.zig").VerifiedAccessLogs;
const VerifiedAccessSseSpecificationRequest = @import("verified_access_sse_specification_request.zig").VerifiedAccessSseSpecificationRequest;
const VerifiedAccessSseSpecificationResponse = @import("verified_access_sse_specification_response.zig").VerifiedAccessSseSpecificationResponse;
const VerifiedAccessTrustProvider = @import("verified_access_trust_provider.zig").VerifiedAccessTrustProvider;
const VerifiedAccessTrustProviderCondensed = @import("verified_access_trust_provider_condensed.zig").VerifiedAccessTrustProviderCondensed;
const VgwTelemetry = @import("vgw_telemetry.zig").VgwTelemetry;
const VirtualizationType = @import("virtualization_type.zig").VirtualizationType;
const Volume = @import("volume.zig").Volume;
const VolumeAttachment = @import("volume_attachment.zig").VolumeAttachment;
const VolumeAttachmentState = @import("volume_attachment_state.zig").VolumeAttachmentState;
const VolumeDetail = @import("volume_detail.zig").VolumeDetail;
const VolumeModification = @import("volume_modification.zig").VolumeModification;
const VolumeModificationState = @import("volume_modification_state.zig").VolumeModificationState;
const VolumeRecycleBinInfo = @import("volume_recycle_bin_info.zig").VolumeRecycleBinInfo;
const VolumeState = @import("volume_state.zig").VolumeState;
const VolumeStatusAction = @import("volume_status_action.zig").VolumeStatusAction;
const VolumeStatusAttachmentStatus = @import("volume_status_attachment_status.zig").VolumeStatusAttachmentStatus;
const VolumeStatusDetails = @import("volume_status_details.zig").VolumeStatusDetails;
const VolumeStatusEvent = @import("volume_status_event.zig").VolumeStatusEvent;
const VolumeStatusInfo = @import("volume_status_info.zig").VolumeStatusInfo;
const VolumeStatusInfoStatus = @import("volume_status_info_status.zig").VolumeStatusInfoStatus;
const VolumeStatusItem = @import("volume_status_item.zig").VolumeStatusItem;
const VolumeStatusName = @import("volume_status_name.zig").VolumeStatusName;
const VolumeType = @import("volume_type.zig").VolumeType;
const Vpc = @import("vpc.zig").Vpc;
const VpcAttachment = @import("vpc_attachment.zig").VpcAttachment;
const VpcBlockPublicAccessExclusion = @import("vpc_block_public_access_exclusion.zig").VpcBlockPublicAccessExclusion;
const VpcBlockPublicAccessExclusionState = @import("vpc_block_public_access_exclusion_state.zig").VpcBlockPublicAccessExclusionState;
const VpcBlockPublicAccessExclusionsAllowed = @import("vpc_block_public_access_exclusions_allowed.zig").VpcBlockPublicAccessExclusionsAllowed;
const VpcBlockPublicAccessOptions = @import("vpc_block_public_access_options.zig").VpcBlockPublicAccessOptions;
const VpcBlockPublicAccessState = @import("vpc_block_public_access_state.zig").VpcBlockPublicAccessState;
const VpcCidrBlockAssociation = @import("vpc_cidr_block_association.zig").VpcCidrBlockAssociation;
const VpcCidrBlockState = @import("vpc_cidr_block_state.zig").VpcCidrBlockState;
const VpcCidrBlockStateCode = @import("vpc_cidr_block_state_code.zig").VpcCidrBlockStateCode;
const VpcClassicLink = @import("vpc_classic_link.zig").VpcClassicLink;
const VpcEncryptionControl = @import("vpc_encryption_control.zig").VpcEncryptionControl;
const VpcEncryptionControlConfiguration = @import("vpc_encryption_control_configuration.zig").VpcEncryptionControlConfiguration;
const VpcEncryptionControlExclusion = @import("vpc_encryption_control_exclusion.zig").VpcEncryptionControlExclusion;
const VpcEncryptionControlExclusionState = @import("vpc_encryption_control_exclusion_state.zig").VpcEncryptionControlExclusionState;
const VpcEncryptionControlExclusionStateInput = @import("vpc_encryption_control_exclusion_state_input.zig").VpcEncryptionControlExclusionStateInput;
const VpcEncryptionControlExclusions = @import("vpc_encryption_control_exclusions.zig").VpcEncryptionControlExclusions;
const VpcEncryptionControlMode = @import("vpc_encryption_control_mode.zig").VpcEncryptionControlMode;
const VpcEncryptionControlState = @import("vpc_encryption_control_state.zig").VpcEncryptionControlState;
const VpcEncryptionNonCompliantResource = @import("vpc_encryption_non_compliant_resource.zig").VpcEncryptionNonCompliantResource;
const VpcEndpoint = @import("vpc_endpoint.zig").VpcEndpoint;
const VpcEndpointAssociation = @import("vpc_endpoint_association.zig").VpcEndpointAssociation;
const VpcEndpointConnection = @import("vpc_endpoint_connection.zig").VpcEndpointConnection;
const VpcEndpointType = @import("vpc_endpoint_type.zig").VpcEndpointType;
const VpcIpv6CidrBlockAssociation = @import("vpc_ipv_6_cidr_block_association.zig").VpcIpv6CidrBlockAssociation;
const VpcPeeringConnection = @import("vpc_peering_connection.zig").VpcPeeringConnection;
const VpcPeeringConnectionOptionsDescription = @import("vpc_peering_connection_options_description.zig").VpcPeeringConnectionOptionsDescription;
const VpcPeeringConnectionStateReason = @import("vpc_peering_connection_state_reason.zig").VpcPeeringConnectionStateReason;
const VpcPeeringConnectionStateReasonCode = @import("vpc_peering_connection_state_reason_code.zig").VpcPeeringConnectionStateReasonCode;
const VpcPeeringConnectionVpcInfo = @import("vpc_peering_connection_vpc_info.zig").VpcPeeringConnectionVpcInfo;
const VpcState = @import("vpc_state.zig").VpcState;
const VpnConcentrator = @import("vpn_concentrator.zig").VpnConcentrator;
const VpnConnection = @import("vpn_connection.zig").VpnConnection;
const VpnConnectionDeviceType = @import("vpn_connection_device_type.zig").VpnConnectionDeviceType;
const VpnConnectionOptions = @import("vpn_connection_options.zig").VpnConnectionOptions;
const VpnConnectionOptionsSpecification = @import("vpn_connection_options_specification.zig").VpnConnectionOptionsSpecification;
const VpnEcmpSupportValue = @import("vpn_ecmp_support_value.zig").VpnEcmpSupportValue;
const VpnGateway = @import("vpn_gateway.zig").VpnGateway;
const VpnProtocol = @import("vpn_protocol.zig").VpnProtocol;
const VpnState = @import("vpn_state.zig").VpnState;
const VpnStaticRoute = @import("vpn_static_route.zig").VpnStaticRoute;
const VpnStaticRouteSource = @import("vpn_static_route_source.zig").VpnStaticRouteSource;
const VpnTunnelBandwidth = @import("vpn_tunnel_bandwidth.zig").VpnTunnelBandwidth;
const VpnTunnelLogOptions = @import("vpn_tunnel_log_options.zig").VpnTunnelLogOptions;
const VpnTunnelLogOptionsSpecification = @import("vpn_tunnel_log_options_specification.zig").VpnTunnelLogOptionsSpecification;
const VpnTunnelOptionsSpecification = @import("vpn_tunnel_options_specification.zig").VpnTunnelOptionsSpecification;
const VpnTunnelProvisioningStatus = @import("vpn_tunnel_provisioning_status.zig").VpnTunnelProvisioningStatus;
const WeekDay = @import("week_day.zig").WeekDay;
const scope = @import("scope.zig").scope;

pub fn deserializeAcceleratorManufacturerSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AcceleratorManufacturer {
    var list: std.ArrayList(AcceleratorManufacturer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(AcceleratorManufacturer, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAcceleratorNameSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AcceleratorName {
    var list: std.ArrayList(AcceleratorName) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(AcceleratorName, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAcceleratorTypeSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AcceleratorType {
    var list: std.ArrayList(AcceleratorType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(AcceleratorType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAccessScopeAnalysisFindingList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AccessScopeAnalysisFinding {
    var list: std.ArrayList(AccessScopeAnalysisFinding) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAccessScopeAnalysisFinding(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAccessScopePathList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AccessScopePath {
    var list: std.ArrayList(AccessScopePath) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAccessScopePath(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAccountAttributeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AccountAttribute {
    var list: std.ArrayList(AccountAttribute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAccountAttribute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAccountAttributeValueList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AccountAttributeValue {
    var list: std.ArrayList(AccountAttributeValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAccountAttributeValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeActiveInstanceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ActiveInstance {
    var list: std.ArrayList(ActiveInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeActiveInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAddedPrincipalSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AddedPrincipal {
    var list: std.ArrayList(AddedPrincipal) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAddedPrincipal(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAdditionalDetailList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AdditionalDetail {
    var list: std.ArrayList(AdditionalDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAdditionalDetail(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Address {
    var list: std.ArrayList(Address) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAddress(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAddressSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AddressAttribute {
    var list: std.ArrayList(AddressAttribute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAddressAttribute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAddressTransferList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AddressTransfer {
    var list: std.ArrayList(AddressTransfer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAddressTransfer(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAllowedInstanceTypeSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAllowedPrincipalSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AllowedPrincipal {
    var list: std.ArrayList(AllowedPrincipal) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAllowedPrincipal(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAlternatePathHintList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AlternatePathHint {
    var list: std.ArrayList(AlternatePathHint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAlternatePathHint(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAnalysisComponentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AnalysisComponent {
    var list: std.ArrayList(AnalysisComponent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAnalysisComponent(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeArchitectureTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ArchitectureType {
    var list: std.ArrayList(ArchitectureType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(ArchitectureType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeArnList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAsPath(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAsnAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AsnAssociation {
    var list: std.ArrayList(AsnAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAsnAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAssignedPrivateIpAddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AssignedPrivateIpAddress {
    var list: std.ArrayList(AssignedPrivateIpAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAssignedPrivateIpAddress(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAssociatedRolesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AssociatedRole {
    var list: std.ArrayList(AssociatedRole) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAssociatedRole(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAssociatedSubnetList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAssociatedTargetNetworkSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AssociatedTargetNetwork {
    var list: std.ArrayList(AssociatedTargetNetwork) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAssociatedTargetNetwork(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAttributeSummaryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AttributeSummary {
    var list: std.ArrayList(AttributeSummary) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAttributeSummary(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAuthorizationRuleSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AuthorizationRule {
    var list: std.ArrayList(AuthorizationRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAuthorizationRule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAvailabilityZoneList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AvailabilityZone {
    var list: std.ArrayList(AvailabilityZone) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAvailabilityZone(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAvailabilityZoneMessageList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AvailabilityZoneMessage {
    var list: std.ArrayList(AvailabilityZoneMessage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAvailabilityZoneMessage(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAvailableInstanceCapacityList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceCapacity {
    var list: std.ArrayList(InstanceCapacity) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceCapacity(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeBandwidthWeightingTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const BandwidthWeightingType {
    var list: std.ArrayList(BandwidthWeightingType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(BandwidthWeightingType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeBlockDeviceMappingList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const BlockDeviceMapping {
    var list: std.ArrayList(BlockDeviceMapping) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeBlockDeviceMapping(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeBlockDeviceMappingResponseList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const BlockDeviceMappingResponse {
    var list: std.ArrayList(BlockDeviceMappingResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeBlockDeviceMappingResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeBootModeTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const BootModeType {
    var list: std.ArrayList(BootModeType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(BootModeType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeBundleTaskList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const BundleTask {
    var list: std.ArrayList(BundleTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeBundleTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeByoasnSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Byoasn {
    var list: std.ArrayList(Byoasn) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeByoasn(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeByoipCidrSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ByoipCidr {
    var list: std.ArrayList(ByoipCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeByoipCidr(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCancelSpotFleetRequestsErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CancelSpotFleetRequestsErrorItem {
    var list: std.ArrayList(CancelSpotFleetRequestsErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCancelSpotFleetRequestsErrorItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCancelSpotFleetRequestsSuccessSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CancelSpotFleetRequestsSuccessItem {
    var list: std.ArrayList(CancelSpotFleetRequestsSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCancelSpotFleetRequestsSuccessItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCancelledSpotInstanceRequestList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CancelledSpotInstanceRequest {
    var list: std.ArrayList(CancelledSpotInstanceRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCancelledSpotInstanceRequest(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityAllocations(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityAllocation {
    var list: std.ArrayList(CapacityAllocation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityAllocation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityBlockExtensionOfferingSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityBlockExtensionOffering {
    var list: std.ArrayList(CapacityBlockExtensionOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityBlockExtensionOffering(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityBlockExtensionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityBlockExtension {
    var list: std.ArrayList(CapacityBlockExtension) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityBlockExtension(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityBlockOfferingSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityBlockOffering {
    var list: std.ArrayList(CapacityBlockOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityBlockOffering(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityBlockSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityBlock {
    var list: std.ArrayList(CapacityBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityBlock(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityBlockStatusSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityBlockStatus {
    var list: std.ArrayList(CapacityBlockStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityBlockStatus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityManagerDataExportResponseSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityManagerDataExportResponse {
    var list: std.ArrayList(CapacityManagerDataExportResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityManagerDataExportResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationBillingRequestSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityReservationBillingRequest {
    var list: std.ArrayList(CapacityReservationBillingRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityReservationBillingRequest(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationFleetCancellationStateSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityReservationFleetCancellationState {
    var list: std.ArrayList(CapacityReservationFleetCancellationState) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityReservationFleetCancellationState(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationFleetSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityReservationFleet {
    var list: std.ArrayList(CapacityReservationFleet) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityReservationFleet(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationGroupSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityReservationGroup {
    var list: std.ArrayList(CapacityReservationGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityReservationGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityReservation {
    var list: std.ArrayList(CapacityReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityReservation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationStatusSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityReservationStatus {
    var list: std.ArrayList(CapacityReservationStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityReservationStatus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCapacityReservationTopologySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityReservationTopology {
    var list: std.ArrayList(CapacityReservationTopology) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityReservationTopology(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCarrierGatewaySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CarrierGateway {
    var list: std.ArrayList(CarrierGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCarrierGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCidrBlockSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CidrBlock {
    var list: std.ArrayList(CidrBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCidrBlock(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeClassicLinkDnsSupportList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ClassicLinkDnsSupport {
    var list: std.ArrayList(ClassicLinkDnsSupport) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeClassicLinkDnsSupport(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeClassicLinkInstanceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ClassicLinkInstance {
    var list: std.ArrayList(ClassicLinkInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeClassicLinkInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeClassicLoadBalancers(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ClassicLoadBalancer {
    var list: std.ArrayList(ClassicLoadBalancer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeClassicLoadBalancer(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeClientVpnAuthenticationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ClientVpnAuthentication {
    var list: std.ArrayList(ClientVpnAuthentication) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeClientVpnAuthentication(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeClientVpnConnectionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ClientVpnConnection {
    var list: std.ArrayList(ClientVpnConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeClientVpnConnection(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeClientVpnRouteSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ClientVpnRoute {
    var list: std.ArrayList(ClientVpnRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeClientVpnRoute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeClientVpnSecurityGroupIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCoipAddressUsageSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CoipAddressUsage {
    var list: std.ArrayList(CoipAddressUsage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCoipAddressUsage(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCoipPoolSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CoipPool {
    var list: std.ArrayList(CoipPool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCoipPool(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeConnectionNotificationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ConnectionNotification {
    var list: std.ArrayList(ConnectionNotification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeConnectionNotification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCoreCountList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCpuManufacturerSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CpuManufacturer {
    var list: std.ArrayList(CpuManufacturer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(CpuManufacturer, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCreateFleetErrorsSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CreateFleetError {
    var list: std.ArrayList(CreateFleetError) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCreateFleetError(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCreateFleetInstancesSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CreateFleetInstance {
    var list: std.ArrayList(CreateFleetInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCreateFleetInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCreateVolumePermissionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CreateVolumePermission {
    var list: std.ArrayList(CreateVolumePermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCreateVolumePermission(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeCustomerGatewayList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CustomerGateway {
    var list: std.ArrayList(CustomerGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCustomerGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDataResponses(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DataResponse {
    var list: std.ArrayList(DataResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDataResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeclarativePoliciesReportList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DeclarativePoliciesReport {
    var list: std.ArrayList(DeclarativePoliciesReport) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDeclarativePoliciesReport(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDedicatedHostIdList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeleteFleetErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DeleteFleetErrorItem {
    var list: std.ArrayList(DeleteFleetErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDeleteFleetErrorItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeleteFleetSuccessSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DeleteFleetSuccessItem {
    var list: std.ArrayList(DeleteFleetSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDeleteFleetSuccessItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DeleteLaunchTemplateVersionsResponseErrorItem {
    var list: std.ArrayList(DeleteLaunchTemplateVersionsResponseErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDeleteLaunchTemplateVersionsResponseErrorItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseSuccessSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DeleteLaunchTemplateVersionsResponseSuccessItem {
    var list: std.ArrayList(DeleteLaunchTemplateVersionsResponseSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDeleteLaunchTemplateVersionsResponseSuccessItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeleteSnapshotResultSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DeleteSnapshotReturnCode {
    var list: std.ArrayList(DeleteSnapshotReturnCode) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDeleteSnapshotReturnCode(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeprovisionedAddressSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDescribeConversionTaskList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ConversionTask {
    var list: std.ArrayList(ConversionTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeConversionTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDescribeFastLaunchImagesSuccessSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DescribeFastLaunchImagesSuccessItem {
    var list: std.ArrayList(DescribeFastLaunchImagesSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDescribeFastLaunchImagesSuccessItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDescribeFastSnapshotRestoreSuccessSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DescribeFastSnapshotRestoreSuccessItem {
    var list: std.ArrayList(DescribeFastSnapshotRestoreSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDescribeFastSnapshotRestoreSuccessItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDescribeFleetsErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DescribeFleetError {
    var list: std.ArrayList(DescribeFleetError) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDescribeFleetError(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDescribeFleetsInstancesSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DescribeFleetsInstances {
    var list: std.ArrayList(DescribeFleetsInstances) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDescribeFleetsInstances(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDeviceTrustProviderTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DeviceTrustProviderType {
    var list: std.ArrayList(DeviceTrustProviderType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(DeviceTrustProviderType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDhcpConfigurationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DhcpConfiguration {
    var list: std.ArrayList(DhcpConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDhcpConfiguration(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDhcpConfigurationValueList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const AttributeValue {
    var list: std.ArrayList(AttributeValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeAttributeValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDhcpOptionsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DhcpOptions {
    var list: std.ArrayList(DhcpOptions) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDhcpOptions(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDisableFastSnapshotRestoreErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DisableFastSnapshotRestoreErrorItem {
    var list: std.ArrayList(DisableFastSnapshotRestoreErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDisableFastSnapshotRestoreErrorItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDisableFastSnapshotRestoreStateErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DisableFastSnapshotRestoreStateErrorItem {
    var list: std.ArrayList(DisableFastSnapshotRestoreStateErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDisableFastSnapshotRestoreStateErrorItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDisableFastSnapshotRestoreSuccessSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DisableFastSnapshotRestoreSuccessItem {
    var list: std.ArrayList(DisableFastSnapshotRestoreSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDisableFastSnapshotRestoreSuccessItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDiskInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DiskInfo {
    var list: std.ArrayList(DiskInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDiskInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeDnsEntrySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const DnsEntry {
    var list: std.ArrayList(DnsEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeDnsEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeEbsCardInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const EbsCardInfo {
    var list: std.ArrayList(EbsCardInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeEbsCardInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeEbsStatusDetailsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const EbsStatusDetails {
    var list: std.ArrayList(EbsStatusDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeEbsStatusDetails(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeEgressOnlyInternetGatewayList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const EgressOnlyInternetGateway {
    var list: std.ArrayList(EgressOnlyInternetGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeEgressOnlyInternetGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeElasticGpuAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ElasticGpuAssociation {
    var list: std.ArrayList(ElasticGpuAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeElasticGpuAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeElasticGpuSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ElasticGpus {
    var list: std.ArrayList(ElasticGpus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeElasticGpus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeElasticGpuSpecificationResponseList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ElasticGpuSpecificationResponse {
    var list: std.ArrayList(ElasticGpuSpecificationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeElasticGpuSpecificationResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeElasticInferenceAcceleratorAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ElasticInferenceAcceleratorAssociation {
    var list: std.ArrayList(ElasticInferenceAcceleratorAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeElasticInferenceAcceleratorAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeEnableFastSnapshotRestoreErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const EnableFastSnapshotRestoreErrorItem {
    var list: std.ArrayList(EnableFastSnapshotRestoreErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeEnableFastSnapshotRestoreErrorItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeEnableFastSnapshotRestoreStateErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const EnableFastSnapshotRestoreStateErrorItem {
    var list: std.ArrayList(EnableFastSnapshotRestoreStateErrorItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeEnableFastSnapshotRestoreStateErrorItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeEnableFastSnapshotRestoreSuccessSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const EnableFastSnapshotRestoreSuccessItem {
    var list: std.ArrayList(EnableFastSnapshotRestoreSuccessItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeEnableFastSnapshotRestoreSuccessItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeEndpointSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ClientVpnEndpoint {
    var list: std.ArrayList(ClientVpnEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeClientVpnEndpoint(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeErrorSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ValidationError {
    var list: std.ArrayList(ValidationError) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeValidationError(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeExcludedInstanceTypeSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeExplanationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Explanation {
    var list: std.ArrayList(Explanation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeExplanation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeExportImageTaskList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ExportImageTask {
    var list: std.ArrayList(ExportImageTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeExportImageTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeExportTaskList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ExportTask {
    var list: std.ArrayList(ExportTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeExportTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFailedCapacityReservationFleetCancellationResultSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FailedCapacityReservationFleetCancellationResult {
    var list: std.ArrayList(FailedCapacityReservationFleetCancellationResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFailedCapacityReservationFleetCancellationResult(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFailedQueuedPurchaseDeletionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FailedQueuedPurchaseDeletion {
    var list: std.ArrayList(FailedQueuedPurchaseDeletion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFailedQueuedPurchaseDeletion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFleetCapacityReservationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FleetCapacityReservation {
    var list: std.ArrayList(FleetCapacityReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFleetCapacityReservation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFleetLaunchTemplateConfigList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FleetLaunchTemplateConfig {
    var list: std.ArrayList(FleetLaunchTemplateConfig) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFleetLaunchTemplateConfig(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFleetLaunchTemplateOverridesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FleetLaunchTemplateOverrides {
    var list: std.ArrayList(FleetLaunchTemplateOverrides) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFleetLaunchTemplateOverrides(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFleetSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FleetData {
    var list: std.ArrayList(FleetData) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFleetData(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFlowLogSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FlowLog {
    var list: std.ArrayList(FlowLog) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFlowLog(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFpgaDeviceInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FpgaDeviceInfo {
    var list: std.ArrayList(FpgaDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFpgaDeviceInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeFpgaImageList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const FpgaImage {
    var list: std.ArrayList(FpgaImage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeFpgaImage(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeGpuDeviceInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const GpuDeviceInfo {
    var list: std.ArrayList(GpuDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeGpuDeviceInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeGroupIdStringList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeGroupIdentifierList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const GroupIdentifier {
    var list: std.ArrayList(GroupIdentifier) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeGroupIdentifier(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeGroupIdentifierSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SecurityGroupIdentifier {
    var list: std.ArrayList(SecurityGroupIdentifier) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSecurityGroupIdentifier(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeHistoryRecordSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const HistoryRecordEntry {
    var list: std.ArrayList(HistoryRecordEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeHistoryRecordEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeHistoryRecords(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const HistoryRecord {
    var list: std.ArrayList(HistoryRecord) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeHistoryRecord(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeHostInstanceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const HostInstance {
    var list: std.ArrayList(HostInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeHostInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeHostList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Host {
    var list: std.ArrayList(Host) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeHost(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeHostOfferingSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const HostOffering {
    var list: std.ArrayList(HostOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeHostOffering(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeHostReservationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const HostReservation {
    var list: std.ArrayList(HostReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeHostReservation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIKEVersionsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IKEVersionsListValue {
    var list: std.ArrayList(IKEVersionsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIKEVersionsListValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIamInstanceProfileAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IamInstanceProfileAssociation {
    var list: std.ArrayList(IamInstanceProfileAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIamInstanceProfileAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIdFormatList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IdFormat {
    var list: std.ArrayList(IdFormat) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIdFormat(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageAncestryEntryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageAncestryEntry {
    var list: std.ArrayList(ImageAncestryEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageAncestryEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageCriterionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageCriterion {
    var list: std.ArrayList(ImageCriterion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageCriterion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Image {
    var list: std.ArrayList(Image) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImage(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageNameList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageProviderList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageRecycleBinInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageRecycleBinInfo {
    var list: std.ArrayList(ImageRecycleBinInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageRecycleBinInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageReferenceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageReference {
    var list: std.ArrayList(ImageReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageReference(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageUsageReportEntryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageUsageReportEntry {
    var list: std.ArrayList(ImageUsageReportEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageUsageReportEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageUsageReportList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageUsageReport {
    var list: std.ArrayList(ImageUsageReport) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageUsageReport(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageUsageResourceTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageUsageResourceType {
    var list: std.ArrayList(ImageUsageResourceType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageUsageResourceType(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageUsageResourceTypeOptionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImageUsageResourceTypeOption {
    var list: std.ArrayList(ImageUsageResourceTypeOption) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImageUsageResourceTypeOption(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImageUsageResourceTypeOptionValuesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImportImageLicenseSpecificationListResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImportImageLicenseConfigurationResponse {
    var list: std.ArrayList(ImportImageLicenseConfigurationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImportImageLicenseConfigurationResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImportImageTaskList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImportImageTask {
    var list: std.ArrayList(ImportImageTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImportImageTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImportInstanceVolumeDetailSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImportInstanceVolumeDetailItem {
    var list: std.ArrayList(ImportInstanceVolumeDetailItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImportInstanceVolumeDetailItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeImportSnapshotTaskList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ImportSnapshotTask {
    var list: std.ArrayList(ImportSnapshotTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeImportSnapshotTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInferenceDeviceInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InferenceDeviceInfo {
    var list: std.ArrayList(InferenceDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInferenceDeviceInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInsideCidrBlocksStringList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceBlockDeviceMappingList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceBlockDeviceMapping {
    var list: std.ArrayList(InstanceBlockDeviceMapping) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceBlockDeviceMapping(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceConnectEndpointSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ec2InstanceConnectEndpoint {
    var list: std.ArrayList(Ec2InstanceConnectEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeEc2InstanceConnectEndpoint(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceCountList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceCount {
    var list: std.ArrayList(InstanceCount) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceCount(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceCreditSpecificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceCreditSpecification {
    var list: std.ArrayList(InstanceCreditSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceCreditSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceEventWindowSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceEventWindow {
    var list: std.ArrayList(InstanceEventWindow) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceEventWindow(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceEventWindowTimeRangeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceEventWindowTimeRange {
    var list: std.ArrayList(InstanceEventWindowTimeRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceEventWindowTimeRange(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceGenerationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceGeneration {
    var list: std.ArrayList(InstanceGeneration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(InstanceGeneration, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceIdList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceIdsSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceImageMetadataList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceImageMetadata {
    var list: std.ArrayList(InstanceImageMetadata) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceImageMetadata(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceIpv4PrefixList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceIpv4Prefix {
    var list: std.ArrayList(InstanceIpv4Prefix) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceIpv4Prefix(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceIpv6AddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceIpv6Address {
    var list: std.ArrayList(InstanceIpv6Address) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceIpv6Address(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceIpv6PrefixList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceIpv6Prefix {
    var list: std.ArrayList(InstanceIpv6Prefix) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceIpv6Prefix(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Instance {
    var list: std.ArrayList(Instance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceMonitoringList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceMonitoring {
    var list: std.ArrayList(InstanceMonitoring) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceMonitoring(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceNetworkInterfaceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceNetworkInterface {
    var list: std.ArrayList(InstanceNetworkInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceNetworkInterface(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceNetworkInterfaceSpecificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceNetworkInterfaceSpecification {
    var list: std.ArrayList(InstanceNetworkInterfaceSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceNetworkInterfaceSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstancePrivateIpAddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstancePrivateIpAddress {
    var list: std.ArrayList(InstancePrivateIpAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstancePrivateIpAddress(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceTopology {
    var list: std.ArrayList(InstanceTopology) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceTopology(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceStateChangeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceStateChange {
    var list: std.ArrayList(InstanceStateChange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceStateChange(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceStatusDetailsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceStatusDetails {
    var list: std.ArrayList(InstanceStatusDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceStatusDetails(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceStatusEventList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceStatusEvent {
    var list: std.ArrayList(InstanceStatusEvent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceStatusEvent(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceStatusList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceStatus {
    var list: std.ArrayList(InstanceStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceStatus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceTagKeySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceTypeInfoFromInstanceRequirementsSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceTypeInfoFromInstanceRequirements {
    var list: std.ArrayList(InstanceTypeInfoFromInstanceRequirements) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceTypeInfoFromInstanceRequirements(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceTypeInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceTypeInfo {
    var list: std.ArrayList(InstanceTypeInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceTypeInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceTypeOfferingsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceTypeOffering {
    var list: std.ArrayList(InstanceTypeOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceTypeOffering(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceTypesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInstanceUsageSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InstanceUsage {
    var list: std.ArrayList(InstanceUsage) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInstanceUsage(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInternetGatewayAttachmentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InternetGatewayAttachment {
    var list: std.ArrayList(InternetGatewayAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInternetGatewayAttachment(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeInternetGatewayList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const InternetGateway {
    var list: std.ArrayList(InternetGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeInternetGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpAddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpPermissionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpPermission {
    var list: std.ArrayList(IpPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpPermission(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpPrefixList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpRangeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpRange {
    var list: std.ArrayList(IpRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpRange(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpRanges(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamAddressHistoryRecordSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamAddressHistoryRecord {
    var list: std.ArrayList(IpamAddressHistoryRecord) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamAddressHistoryRecord(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamDiscoveredAccountSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamDiscoveredAccount {
    var list: std.ArrayList(IpamDiscoveredAccount) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamDiscoveredAccount(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamDiscoveredPublicAddressSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamDiscoveredPublicAddress {
    var list: std.ArrayList(IpamDiscoveredPublicAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamDiscoveredPublicAddress(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamDiscoveredResourceCidrSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamDiscoveredResourceCidr {
    var list: std.ArrayList(IpamDiscoveredResourceCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamDiscoveredResourceCidr(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamExternalResourceVerificationTokenSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamExternalResourceVerificationToken {
    var list: std.ArrayList(IpamExternalResourceVerificationToken) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamExternalResourceVerificationToken(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamOperatingRegionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamOperatingRegion {
    var list: std.ArrayList(IpamOperatingRegion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamOperatingRegion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamOrganizationalUnitExclusionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamOrganizationalUnitExclusion {
    var list: std.ArrayList(IpamOrganizationalUnitExclusion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamOrganizationalUnitExclusion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPolicyAllocationRuleList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPolicyAllocationRule {
    var list: std.ArrayList(IpamPolicyAllocationRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPolicyAllocationRule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPolicyDocumentSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPolicyDocument {
    var list: std.ArrayList(IpamPolicyDocument) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPolicyDocument(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPolicyOrganizationTargetSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPolicyOrganizationTarget {
    var list: std.ArrayList(IpamPolicyOrganizationTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPolicyOrganizationTarget(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPolicySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPolicy {
    var list: std.ArrayList(IpamPolicy) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPolicy(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPoolAllocationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPoolAllocation {
    var list: std.ArrayList(IpamPoolAllocation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPoolAllocation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPoolCidrSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPoolCidr {
    var list: std.ArrayList(IpamPoolCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPoolCidr(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPoolSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPool {
    var list: std.ArrayList(IpamPool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPool(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPrefixListResolverRuleConditionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPrefixListResolverRuleCondition {
    var list: std.ArrayList(IpamPrefixListResolverRuleCondition) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPrefixListResolverRuleCondition(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPrefixListResolverRuleSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPrefixListResolverRule {
    var list: std.ArrayList(IpamPrefixListResolverRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPrefixListResolverRule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPrefixListResolverSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPrefixListResolver {
    var list: std.ArrayList(IpamPrefixListResolver) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPrefixListResolver(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPrefixListResolverTargetSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPrefixListResolverTarget {
    var list: std.ArrayList(IpamPrefixListResolverTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPrefixListResolverTarget(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPrefixListResolverVersionEntrySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPrefixListResolverVersionEntry {
    var list: std.ArrayList(IpamPrefixListResolverVersionEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPrefixListResolverVersionEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPrefixListResolverVersionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPrefixListResolverVersion {
    var list: std.ArrayList(IpamPrefixListResolverVersion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPrefixListResolverVersion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPublicAddressSecurityGroupList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPublicAddressSecurityGroup {
    var list: std.ArrayList(IpamPublicAddressSecurityGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPublicAddressSecurityGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamPublicAddressTagList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamPublicAddressTag {
    var list: std.ArrayList(IpamPublicAddressTag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamPublicAddressTag(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamResourceCidrSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamResourceCidr {
    var list: std.ArrayList(IpamResourceCidr) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamResourceCidr(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamResourceDiscoveryAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamResourceDiscoveryAssociation {
    var list: std.ArrayList(IpamResourceDiscoveryAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamResourceDiscoveryAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamResourceDiscoverySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamResourceDiscovery {
    var list: std.ArrayList(IpamResourceDiscovery) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamResourceDiscovery(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamResourceTagList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamResourceTag {
    var list: std.ArrayList(IpamResourceTag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamResourceTag(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamScopeSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const IpamScope {
    var list: std.ArrayList(IpamScope) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpamScope(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpamSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipam {
    var list: std.ArrayList(Ipam) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpam(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv4PrefixList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv4PrefixSpecificationRequest {
    var list: std.ArrayList(Ipv4PrefixSpecificationRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv4PrefixSpecificationRequest(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv4PrefixListResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv4PrefixSpecificationResponse {
    var list: std.ArrayList(Ipv4PrefixSpecificationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv4PrefixSpecificationResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv4PrefixesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv4PrefixSpecification {
    var list: std.ArrayList(Ipv4PrefixSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv4PrefixSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6AddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6CidrAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv6CidrAssociation {
    var list: std.ArrayList(Ipv6CidrAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv6CidrAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6CidrBlockSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv6CidrBlock {
    var list: std.ArrayList(Ipv6CidrBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv6CidrBlock(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6PoolSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv6Pool {
    var list: std.ArrayList(Ipv6Pool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv6Pool(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6PrefixList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv6PrefixSpecificationRequest {
    var list: std.ArrayList(Ipv6PrefixSpecificationRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv6PrefixSpecificationRequest(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6PrefixListResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv6PrefixSpecificationResponse {
    var list: std.ArrayList(Ipv6PrefixSpecificationResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv6PrefixSpecificationResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6PrefixesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv6PrefixSpecification {
    var list: std.ArrayList(Ipv6PrefixSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv6PrefixSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeIpv6RangeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Ipv6Range {
    var list: std.ArrayList(Ipv6Range) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeIpv6Range(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeKeyPairList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const KeyPairInfo {
    var list: std.ArrayList(KeyPairInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeKeyPairInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchPermissionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchPermission {
    var list: std.ArrayList(LaunchPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchPermission(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchSpecsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SpotFleetLaunchSpecification {
    var list: std.ArrayList(SpotFleetLaunchSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSpotFleetLaunchSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateBlockDeviceMappingList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateBlockDeviceMapping {
    var list: std.ArrayList(LaunchTemplateBlockDeviceMapping) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateBlockDeviceMapping(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateConfigList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateConfig {
    var list: std.ArrayList(LaunchTemplateConfig) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateConfig(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateElasticInferenceAcceleratorResponseList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateElasticInferenceAcceleratorResponse {
    var list: std.ArrayList(LaunchTemplateElasticInferenceAcceleratorResponse) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateElasticInferenceAcceleratorResponse(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateInstanceNetworkInterfaceSpecificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateInstanceNetworkInterfaceSpecification {
    var list: std.ArrayList(LaunchTemplateInstanceNetworkInterfaceSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateInstanceNetworkInterfaceSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateLicenseList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateLicenseConfiguration {
    var list: std.ArrayList(LaunchTemplateLicenseConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateLicenseConfiguration(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateOverridesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateOverrides {
    var list: std.ArrayList(LaunchTemplateOverrides) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateOverrides(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplate {
    var list: std.ArrayList(LaunchTemplate) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplate(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateTagSpecificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateTagSpecification {
    var list: std.ArrayList(LaunchTemplateTagSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateTagSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLaunchTemplateVersionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LaunchTemplateVersion {
    var list: std.ArrayList(LaunchTemplateVersion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLaunchTemplateVersion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLicenseList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LicenseConfiguration {
    var list: std.ArrayList(LicenseConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLicenseConfiguration(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLoadPermissionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LoadPermission {
    var list: std.ArrayList(LoadPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLoadPermission(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewayRouteList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalGatewayRoute {
    var list: std.ArrayList(LocalGatewayRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLocalGatewayRoute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewayRouteTableSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalGatewayRouteTable {
    var list: std.ArrayList(LocalGatewayRouteTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLocalGatewayRouteTable(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewayRouteTableVirtualInterfaceGroupAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalGatewayRouteTableVirtualInterfaceGroupAssociation {
    var list: std.ArrayList(LocalGatewayRouteTableVirtualInterfaceGroupAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLocalGatewayRouteTableVirtualInterfaceGroupAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewayRouteTableVpcAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalGatewayRouteTableVpcAssociation {
    var list: std.ArrayList(LocalGatewayRouteTableVpcAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLocalGatewayRouteTableVpcAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewaySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalGateway {
    var list: std.ArrayList(LocalGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLocalGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewayVirtualInterfaceGroupSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalGatewayVirtualInterfaceGroup {
    var list: std.ArrayList(LocalGatewayVirtualInterfaceGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLocalGatewayVirtualInterfaceGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewayVirtualInterfaceIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalGatewayVirtualInterfaceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalGatewayVirtualInterface {
    var list: std.ArrayList(LocalGatewayVirtualInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLocalGatewayVirtualInterface(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLocalStorageTypeSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LocalStorageType {
    var list: std.ArrayList(LocalStorageType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(LocalStorageType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeLockedSnapshotsInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const LockedSnapshotsInfo {
    var list: std.ArrayList(LockedSnapshotsInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeLockedSnapshotsInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMacHostList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const MacHost {
    var list: std.ArrayList(MacHost) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeMacHost(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMacModificationTaskList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const MacModificationTask {
    var list: std.ArrayList(MacModificationTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeMacModificationTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMacOSVersionStringList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeManagedPrefixListSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ManagedPrefixList {
    var list: std.ArrayList(ManagedPrefixList) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeManagedPrefixList(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMarketplaceProductCodeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMediaDeviceInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const MediaDeviceInfo {
    var list: std.ArrayList(MediaDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeMediaDeviceInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMetricDataResultSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const MetricDataResult {
    var list: std.ArrayList(MetricDataResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeMetricDataResult(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMetricDimensionResultSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const CapacityManagerDimension {
    var list: std.ArrayList(CapacityManagerDimension) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeCapacityManagerDimension(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMetricPoints(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const MetricPoint {
    var list: std.ArrayList(MetricPoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeMetricPoint(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMetricValueSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const MetricValue {
    var list: std.ArrayList(MetricValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeMetricValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeMovingAddressStatusSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const MovingAddressStatus {
    var list: std.ArrayList(MovingAddressStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeMovingAddressStatus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNatGatewayAddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NatGatewayAddress {
    var list: std.ArrayList(NatGatewayAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNatGatewayAddress(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNatGatewayAttachedApplianceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NatGatewayAttachedAppliance {
    var list: std.ArrayList(NatGatewayAttachedAppliance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNatGatewayAttachedAppliance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNatGatewayList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NatGateway {
    var list: std.ArrayList(NatGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNatGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkAclAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkAclAssociation {
    var list: std.ArrayList(NetworkAclAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkAclAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkAclEntryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkAclEntry {
    var list: std.ArrayList(NetworkAclEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkAclEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkAclList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkAcl {
    var list: std.ArrayList(NetworkAcl) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkAcl(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkCardInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkCardInfo {
    var list: std.ArrayList(NetworkCardInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkCardInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInsightsAccessScopeAnalysisList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInsightsAccessScopeAnalysis {
    var list: std.ArrayList(NetworkInsightsAccessScopeAnalysis) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInsightsAccessScopeAnalysis(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInsightsAccessScopeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInsightsAccessScope {
    var list: std.ArrayList(NetworkInsightsAccessScope) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInsightsAccessScope(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInsightsAnalysisList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInsightsAnalysis {
    var list: std.ArrayList(NetworkInsightsAnalysis) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInsightsAnalysis(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInsightsPathList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInsightsPath {
    var list: std.ArrayList(NetworkInsightsPath) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInsightsPath(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInterfaceIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInterfaceIpv6AddressesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInterfaceIpv6Address {
    var list: std.ArrayList(NetworkInterfaceIpv6Address) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInterfaceIpv6Address(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInterfaceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInterface {
    var list: std.ArrayList(NetworkInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInterface(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInterfacePermissionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInterfacePermission {
    var list: std.ArrayList(NetworkInterfacePermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInterfacePermission(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkInterfacePrivateIpAddressList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NetworkInterfacePrivateIpAddress {
    var list: std.ArrayList(NetworkInterfacePrivateIpAddress) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNetworkInterfacePrivateIpAddress(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkNodeSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNetworkNodesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNeuronDeviceInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const NeuronDeviceInfo {
    var list: std.ArrayList(NeuronDeviceInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeNeuronDeviceInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeNitroTpmSupportedVersionsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeOccurrenceDaySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeOutpostLagSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const OutpostLag {
    var list: std.ArrayList(OutpostLag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeOutpostLag(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePathComponentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PathComponent {
    var list: std.ArrayList(PathComponent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePathComponent(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePerformanceFactorReferenceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PerformanceFactorReference {
    var list: std.ArrayList(PerformanceFactorReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePerformanceFactorReference(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePhase1DHGroupNumbersList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Phase1DHGroupNumbersListValue {
    var list: std.ArrayList(Phase1DHGroupNumbersListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePhase1DHGroupNumbersListValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePhase1EncryptionAlgorithmsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Phase1EncryptionAlgorithmsListValue {
    var list: std.ArrayList(Phase1EncryptionAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePhase1EncryptionAlgorithmsListValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePhase1IntegrityAlgorithmsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Phase1IntegrityAlgorithmsListValue {
    var list: std.ArrayList(Phase1IntegrityAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePhase1IntegrityAlgorithmsListValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePhase2DHGroupNumbersList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Phase2DHGroupNumbersListValue {
    var list: std.ArrayList(Phase2DHGroupNumbersListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePhase2DHGroupNumbersListValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePhase2EncryptionAlgorithmsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Phase2EncryptionAlgorithmsListValue {
    var list: std.ArrayList(Phase2EncryptionAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePhase2EncryptionAlgorithmsListValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePhase2IntegrityAlgorithmsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Phase2IntegrityAlgorithmsListValue {
    var list: std.ArrayList(Phase2IntegrityAlgorithmsListValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePhase2IntegrityAlgorithmsListValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePlacementGroupList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PlacementGroup {
    var list: std.ArrayList(PlacementGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePlacementGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePlacementGroupStrategyList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PlacementGroupStrategy {
    var list: std.ArrayList(PlacementGroupStrategy) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(PlacementGroupStrategy, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePoolCidrBlocksSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PoolCidrBlock {
    var list: std.ArrayList(PoolCidrBlock) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePoolCidrBlock(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePortRangeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PortRange {
    var list: std.ArrayList(PortRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePortRange(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrefixListAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PrefixListAssociation {
    var list: std.ArrayList(PrefixListAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePrefixListAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrefixListEntrySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PrefixListEntry {
    var list: std.ArrayList(PrefixListEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePrefixListEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrefixListIdList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PrefixListId {
    var list: std.ArrayList(PrefixListId) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePrefixListId(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrefixListIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrefixListSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PrefixList {
    var list: std.ArrayList(PrefixList) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePrefixList(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePriceScheduleList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PriceSchedule {
    var list: std.ArrayList(PriceSchedule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePriceSchedule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePricingDetailsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PricingDetail {
    var list: std.ArrayList(PricingDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePricingDetail(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrincipalIdFormatList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PrincipalIdFormat {
    var list: std.ArrayList(PrincipalIdFormat) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePrincipalIdFormat(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrivateDnsDetailsSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PrivateDnsDetails {
    var list: std.ArrayList(PrivateDnsDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePrivateDnsDetails(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrivateDnsSpecifiedDomainSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePrivateIpAddressSpecificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PrivateIpAddressSpecification {
    var list: std.ArrayList(PrivateIpAddressSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePrivateIpAddressSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeProductCodeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ProductCode {
    var list: std.ArrayList(ProductCode) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeProductCode(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePropagatingVgwList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PropagatingVgw {
    var list: std.ArrayList(PropagatingVgw) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePropagatingVgw(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeProtocolIntList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeProtocolList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Protocol {
    var list: std.ArrayList(Protocol) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(Protocol, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePublicIpv4PoolRangeSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PublicIpv4PoolRange {
    var list: std.ArrayList(PublicIpv4PoolRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePublicIpv4PoolRange(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePublicIpv4PoolSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const PublicIpv4Pool {
    var list: std.ArrayList(PublicIpv4Pool) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePublicIpv4Pool(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePurchaseSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Purchase {
    var list: std.ArrayList(Purchase) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializePurchase(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializePurchasedScheduledInstanceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ScheduledInstance {
    var list: std.ArrayList(ScheduledInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeScheduledInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRecurringChargesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RecurringCharge {
    var list: std.ArrayList(RecurringCharge) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRecurringCharge(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRegionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Region {
    var list: std.ArrayList(Region) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRegion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRegionalSummaryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RegionalSummary {
    var list: std.ArrayList(RegionalSummary) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRegionalSummary(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRegisteredInstanceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RegisteredInstance {
    var list: std.ArrayList(RegisteredInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRegisteredInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReplaceRootVolumeTasks(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReplaceRootVolumeTask {
    var list: std.ArrayList(ReplaceRootVolumeTask) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReplaceRootVolumeTask(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Reservation {
    var list: std.ArrayList(Reservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservedInstanceReservationValueSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReservedInstanceReservationValue {
    var list: std.ArrayList(ReservedInstanceReservationValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservedInstanceReservationValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservedInstancesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReservedInstances {
    var list: std.ArrayList(ReservedInstances) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservedInstances(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservedInstancesListingList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReservedInstancesListing {
    var list: std.ArrayList(ReservedInstancesListing) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservedInstancesListing(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservedInstancesModificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReservedInstancesModification {
    var list: std.ArrayList(ReservedInstancesModification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservedInstancesModification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservedInstancesModificationResultList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReservedInstancesModificationResult {
    var list: std.ArrayList(ReservedInstancesModificationResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservedInstancesModificationResult(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservedInstancesOfferingList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReservedInstancesOffering {
    var list: std.ArrayList(ReservedInstancesOffering) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservedInstancesOffering(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeReservedIntancesIds(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ReservedInstancesId {
    var list: std.ArrayList(ReservedInstancesId) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeReservedInstancesId(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeResponseHostIdList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeResponseHostIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRevokedSecurityGroupRuleList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RevokedSecurityGroupRule {
    var list: std.ArrayList(RevokedSecurityGroupRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRevokedSecurityGroupRule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRootDeviceTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RootDeviceType {
    var list: std.ArrayList(RootDeviceType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(RootDeviceType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Route {
    var list: std.ArrayList(Route) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRoute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteServerAssociationsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteServerAssociation {
    var list: std.ArrayList(RouteServerAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteServerAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteServerEndpointsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteServerEndpoint {
    var list: std.ArrayList(RouteServerEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteServerEndpoint(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteServerPeersList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteServerPeer {
    var list: std.ArrayList(RouteServerPeer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteServerPeer(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteServerPropagationsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteServerPropagation {
    var list: std.ArrayList(RouteServerPropagation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteServerPropagation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteServerRouteInstallationDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteServerRouteInstallationDetail {
    var list: std.ArrayList(RouteServerRouteInstallationDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteServerRouteInstallationDetail(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteServerRouteList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteServerRoute {
    var list: std.ArrayList(RouteServerRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteServerRoute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteServersList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteServer {
    var list: std.ArrayList(RouteServer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteServer(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteTableAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteTableAssociation {
    var list: std.ArrayList(RouteTableAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteTableAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRouteTableList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RouteTable {
    var list: std.ArrayList(RouteTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRouteTable(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRuleGroupRuleOptionsPairList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RuleGroupRuleOptionsPair {
    var list: std.ArrayList(RuleGroupRuleOptionsPair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRuleGroupRuleOptionsPair(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRuleGroupTypePairList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RuleGroupTypePair {
    var list: std.ArrayList(RuleGroupTypePair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRuleGroupTypePair(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeRuleOptionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const RuleOption {
    var list: std.ArrayList(RuleOption) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeRuleOption(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeScheduledInstanceAvailabilitySet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ScheduledInstanceAvailability {
    var list: std.ArrayList(ScheduledInstanceAvailability) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeScheduledInstanceAvailability(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeScheduledInstanceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ScheduledInstance {
    var list: std.ArrayList(ScheduledInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeScheduledInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupForVpcList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SecurityGroupForVpc {
    var list: std.ArrayList(SecurityGroupForVpc) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSecurityGroupForVpc(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupIdList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupIdStringList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SecurityGroup {
    var list: std.ArrayList(SecurityGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSecurityGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupReferences(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SecurityGroupReference {
    var list: std.ArrayList(SecurityGroupReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSecurityGroupReference(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupRuleList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SecurityGroupRule {
    var list: std.ArrayList(SecurityGroupRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSecurityGroupRule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSecurityGroupVpcAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SecurityGroupVpcAssociation {
    var list: std.ArrayList(SecurityGroupVpcAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSecurityGroupVpcAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeServiceConfigurationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ServiceConfiguration {
    var list: std.ArrayList(ServiceConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeServiceConfiguration(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeServiceDetailSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ServiceDetail {
    var list: std.ArrayList(ServiceDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeServiceDetail(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeServiceLinkVirtualInterfaceIdSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeServiceLinkVirtualInterfaceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ServiceLinkVirtualInterface {
    var list: std.ArrayList(ServiceLinkVirtualInterface) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeServiceLinkVirtualInterface(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeServiceTypeDetailSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ServiceTypeDetail {
    var list: std.ArrayList(ServiceTypeDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeServiceTypeDetail(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSnapshotDetailList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SnapshotDetail {
    var list: std.ArrayList(SnapshotDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSnapshotDetail(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSnapshotList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Snapshot {
    var list: std.ArrayList(Snapshot) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSnapshot(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSnapshotRecycleBinInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SnapshotRecycleBinInfo {
    var list: std.ArrayList(SnapshotRecycleBinInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSnapshotRecycleBinInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSnapshotSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SnapshotInfo {
    var list: std.ArrayList(SnapshotInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSnapshotInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSpotFleetRequestConfigSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SpotFleetRequestConfig {
    var list: std.ArrayList(SpotFleetRequestConfig) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSpotFleetRequestConfig(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSpotFleetTagSpecificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SpotFleetTagSpecification {
    var list: std.ArrayList(SpotFleetTagSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSpotFleetTagSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSpotInstanceRequestList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SpotInstanceRequest {
    var list: std.ArrayList(SpotInstanceRequest) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSpotInstanceRequest(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSpotPlacementScores(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SpotPlacementScore {
    var list: std.ArrayList(SpotPlacementScore) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSpotPlacementScore(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSpotPriceHistoryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SpotPrice {
    var list: std.ArrayList(SpotPrice) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSpotPrice(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeStaleIpPermissionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const StaleIpPermission {
    var list: std.ArrayList(StaleIpPermission) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeStaleIpPermission(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeStaleSecurityGroupSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const StaleSecurityGroup {
    var list: std.ArrayList(StaleSecurityGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeStaleSecurityGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeStoreImageTaskResultSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const StoreImageTaskResult {
    var list: std.ArrayList(StoreImageTaskResult) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeStoreImageTaskResult(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeStringList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSubnetAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SubnetAssociation {
    var list: std.ArrayList(SubnetAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSubnetAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSubnetCidrReservationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SubnetCidrReservation {
    var list: std.ArrayList(SubnetCidrReservation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSubnetCidrReservation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSubnetIpPrefixesList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SubnetIpPrefixes {
    var list: std.ArrayList(SubnetIpPrefixes) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSubnetIpPrefixes(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSubnetIpv6CidrBlockAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SubnetIpv6CidrBlockAssociation {
    var list: std.ArrayList(SubnetIpv6CidrBlockAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSubnetIpv6CidrBlockAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSubnetList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Subnet {
    var list: std.ArrayList(Subnet) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSubnet(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSubscriptionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Subscription {
    var list: std.ArrayList(Subscription) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSubscription(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSuccessfulInstanceCreditSpecificationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SuccessfulInstanceCreditSpecificationItem {
    var list: std.ArrayList(SuccessfulInstanceCreditSpecificationItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSuccessfulInstanceCreditSpecificationItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSuccessfulQueuedPurchaseDeletionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SuccessfulQueuedPurchaseDeletion {
    var list: std.ArrayList(SuccessfulQueuedPurchaseDeletion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSuccessfulQueuedPurchaseDeletion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSupportedAdditionalProcessorFeatureList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SupportedAdditionalProcessorFeature {
    var list: std.ArrayList(SupportedAdditionalProcessorFeature) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(SupportedAdditionalProcessorFeature, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSupportedIpAddressTypes(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ServiceConnectivityType {
    var list: std.ArrayList(ServiceConnectivityType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(ServiceConnectivityType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeSupportedRegionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SupportedRegionDetail {
    var list: std.ArrayList(SupportedRegionDetail) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSupportedRegionDetail(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTagDescriptionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TagDescription {
    var list: std.ArrayList(TagDescription) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTagDescription(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTagList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Tag {
    var list: std.ArrayList(Tag) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTag(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTagSpecificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TagSpecification {
    var list: std.ArrayList(TagSpecification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTagSpecification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTargetGroups(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TargetGroup {
    var list: std.ArrayList(TargetGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTargetGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTargetNetworkSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TargetNetwork {
    var list: std.ArrayList(TargetNetwork) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTargetNetwork(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTargetReservationValueSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TargetReservationValue {
    var list: std.ArrayList(TargetReservationValue) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTargetReservationValue(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTerminateConnectionStatusSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TerminateConnectionStatus {
    var list: std.ArrayList(TerminateConnectionStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTerminateConnectionStatus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeThreadsPerCoreList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const i32 {
    var list: std.ArrayList(i32) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeThroughResourcesStatementList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const ThroughResourcesStatement {
    var list: std.ArrayList(ThroughResourcesStatement) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeThroughResourcesStatement(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTrafficMirrorFilterRuleList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TrafficMirrorFilterRule {
    var list: std.ArrayList(TrafficMirrorFilterRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTrafficMirrorFilterRule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTrafficMirrorFilterRuleSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TrafficMirrorFilterRule {
    var list: std.ArrayList(TrafficMirrorFilterRule) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTrafficMirrorFilterRule(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTrafficMirrorFilterSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TrafficMirrorFilter {
    var list: std.ArrayList(TrafficMirrorFilter) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTrafficMirrorFilter(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTrafficMirrorNetworkServiceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TrafficMirrorNetworkService {
    var list: std.ArrayList(TrafficMirrorNetworkService) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(TrafficMirrorNetworkService, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTrafficMirrorSessionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TrafficMirrorSession {
    var list: std.ArrayList(TrafficMirrorSession) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTrafficMirrorSession(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTrafficMirrorTargetSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TrafficMirrorTarget {
    var list: std.ArrayList(TrafficMirrorTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTrafficMirrorTarget(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayAttachmentBgpConfigurationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayAttachmentBgpConfiguration {
    var list: std.ArrayList(TransitGatewayAttachmentBgpConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayAttachmentBgpConfiguration(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayAttachmentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayAttachment {
    var list: std.ArrayList(TransitGatewayAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayAttachment(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayAttachmentPropagationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayAttachmentPropagation {
    var list: std.ArrayList(TransitGatewayAttachmentPropagation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayAttachmentPropagation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayConnectList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayConnect {
    var list: std.ArrayList(TransitGatewayConnect) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayConnect(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayConnectPeerList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayConnectPeer {
    var list: std.ArrayList(TransitGatewayConnectPeer) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayConnectPeer(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGateway {
    var list: std.ArrayList(TransitGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayMeteringPolicyEntryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayMeteringPolicyEntry {
    var list: std.ArrayList(TransitGatewayMeteringPolicyEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayMeteringPolicyEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayMeteringPolicyList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayMeteringPolicy {
    var list: std.ArrayList(TransitGatewayMeteringPolicy) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayMeteringPolicy(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayMulticastDomainAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayMulticastDomainAssociation {
    var list: std.ArrayList(TransitGatewayMulticastDomainAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayMulticastDomainAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayMulticastDomainList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayMulticastDomain {
    var list: std.ArrayList(TransitGatewayMulticastDomain) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayMulticastDomain(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayMulticastGroupList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayMulticastGroup {
    var list: std.ArrayList(TransitGatewayMulticastGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayMulticastGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayPeeringAttachmentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayPeeringAttachment {
    var list: std.ArrayList(TransitGatewayPeeringAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayPeeringAttachment(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayPolicyTableAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayPolicyTableAssociation {
    var list: std.ArrayList(TransitGatewayPolicyTableAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayPolicyTableAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayPolicyTableEntryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayPolicyTableEntry {
    var list: std.ArrayList(TransitGatewayPolicyTableEntry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayPolicyTableEntry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayPolicyTableList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayPolicyTable {
    var list: std.ArrayList(TransitGatewayPolicyTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayPolicyTable(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayPrefixListReferenceSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayPrefixListReference {
    var list: std.ArrayList(TransitGatewayPrefixListReference) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayPrefixListReference(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayRouteAttachmentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayRouteAttachment {
    var list: std.ArrayList(TransitGatewayRouteAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayRouteAttachment(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayRouteList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayRoute {
    var list: std.ArrayList(TransitGatewayRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayRoute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayRouteTableAnnouncementList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayRouteTableAnnouncement {
    var list: std.ArrayList(TransitGatewayRouteTableAnnouncement) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayRouteTableAnnouncement(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayRouteTableAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayRouteTableAssociation {
    var list: std.ArrayList(TransitGatewayRouteTableAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayRouteTableAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayRouteTableList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayRouteTable {
    var list: std.ArrayList(TransitGatewayRouteTable) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayRouteTable(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayRouteTablePropagationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayRouteTablePropagation {
    var list: std.ArrayList(TransitGatewayRouteTablePropagation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayRouteTablePropagation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTransitGatewayVpcAttachmentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TransitGatewayVpcAttachment {
    var list: std.ArrayList(TransitGatewayVpcAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTransitGatewayVpcAttachment(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTrunkInterfaceAssociationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TrunkInterfaceAssociation {
    var list: std.ArrayList(TrunkInterfaceAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTrunkInterfaceAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeTunnelOptionsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const TunnelOption {
    var list: std.ArrayList(TunnelOption) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeTunnelOption(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeUnsuccessfulInstanceCreditSpecificationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const UnsuccessfulInstanceCreditSpecificationItem {
    var list: std.ArrayList(UnsuccessfulInstanceCreditSpecificationItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeUnsuccessfulInstanceCreditSpecificationItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeUnsuccessfulItemList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const UnsuccessfulItem {
    var list: std.ArrayList(UnsuccessfulItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeUnsuccessfulItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeUnsuccessfulItemSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const UnsuccessfulItem {
    var list: std.ArrayList(UnsuccessfulItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeUnsuccessfulItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeUsageClassTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const UsageClassType {
    var list: std.ArrayList(UsageClassType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(UsageClassType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeUserIdGroupPairList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const UserIdGroupPair {
    var list: std.ArrayList(UserIdGroupPair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeUserIdGroupPair(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeUserIdGroupPairSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const UserIdGroupPair {
    var list: std.ArrayList(UserIdGroupPair) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeUserIdGroupPair(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeUserIdList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeValueStringList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessEndpointList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessEndpoint {
    var list: std.ArrayList(VerifiedAccessEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessEndpoint(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessEndpointPortRangeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessEndpointPortRange {
    var list: std.ArrayList(VerifiedAccessEndpointPortRange) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessEndpointPortRange(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessEndpointSubnetIdList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessEndpointTargetList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessEndpointTarget {
    var list: std.ArrayList(VerifiedAccessEndpointTarget) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessEndpointTarget(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessGroupList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessGroup {
    var list: std.ArrayList(VerifiedAccessGroup) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessGroup(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessInstanceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessInstance {
    var list: std.ArrayList(VerifiedAccessInstance) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessInstance(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessInstanceLoggingConfigurationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessInstanceLoggingConfiguration {
    var list: std.ArrayList(VerifiedAccessInstanceLoggingConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessInstanceLoggingConfiguration(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfigurationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessInstanceOpenVpnClientConfiguration {
    var list: std.ArrayList(VerifiedAccessInstanceOpenVpnClientConfiguration) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessInstanceOpenVpnClientConfiguration(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRouteList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessInstanceOpenVpnClientConfigurationRoute {
    var list: std.ArrayList(VerifiedAccessInstanceOpenVpnClientConfigurationRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRoute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessTrustProviderCondensedList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessTrustProviderCondensed {
    var list: std.ArrayList(VerifiedAccessTrustProviderCondensed) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessTrustProviderCondensed(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVerifiedAccessTrustProviderList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VerifiedAccessTrustProvider {
    var list: std.ArrayList(VerifiedAccessTrustProvider) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVerifiedAccessTrustProvider(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVgwTelemetryList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VgwTelemetry {
    var list: std.ArrayList(VgwTelemetry) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVgwTelemetry(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVirtualizationTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VirtualizationType {
    var list: std.ArrayList(VirtualizationType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    if (std.meta.stringToEnum(VirtualizationType, try reader.readElementText())) |v| try list.append(alloc, v);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeAttachmentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeAttachment {
    var list: std.ArrayList(VolumeAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeAttachment(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Volume {
    var list: std.ArrayList(Volume) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolume(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeModificationList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeModification {
    var list: std.ArrayList(VolumeModification) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeModification(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeRecycleBinInfoList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeRecycleBinInfo {
    var list: std.ArrayList(VolumeRecycleBinInfo) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeRecycleBinInfo(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeStatusActionsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeStatusAction {
    var list: std.ArrayList(VolumeStatusAction) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeStatusAction(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeStatusAttachmentStatusList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeStatusAttachmentStatus {
    var list: std.ArrayList(VolumeStatusAttachmentStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeStatusAttachmentStatus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeStatusDetailsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeStatusDetails {
    var list: std.ArrayList(VolumeStatusDetails) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeStatusDetails(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeStatusEventsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeStatusEvent {
    var list: std.ArrayList(VolumeStatusEvent) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeStatusEvent(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVolumeStatusList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VolumeStatusItem {
    var list: std.ArrayList(VolumeStatusItem) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVolumeStatusItem(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcAttachmentList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcAttachment {
    var list: std.ArrayList(VpcAttachment) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcAttachment(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcBlockPublicAccessExclusionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcBlockPublicAccessExclusion {
    var list: std.ArrayList(VpcBlockPublicAccessExclusion) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcBlockPublicAccessExclusion(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcCidrBlockAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcCidrBlockAssociation {
    var list: std.ArrayList(VpcCidrBlockAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcCidrBlockAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcClassicLinkList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcClassicLink {
    var list: std.ArrayList(VpcClassicLink) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcClassicLink(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcEncryptionControlList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcEncryptionControl {
    var list: std.ArrayList(VpcEncryptionControl) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcEncryptionControl(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcEncryptionNonCompliantResourceList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcEncryptionNonCompliantResource {
    var list: std.ArrayList(VpcEncryptionNonCompliantResource) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcEncryptionNonCompliantResource(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcEndpointAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcEndpointAssociation {
    var list: std.ArrayList(VpcEndpointAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcEndpointAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcEndpointConnectionSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcEndpointConnection {
    var list: std.ArrayList(VpcEndpointConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcEndpointConnection(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcEndpointSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcEndpoint {
    var list: std.ArrayList(VpcEndpoint) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcEndpoint(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcIpv6CidrBlockAssociationSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcIpv6CidrBlockAssociation {
    var list: std.ArrayList(VpcIpv6CidrBlockAssociation) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcIpv6CidrBlockAssociation(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const Vpc {
    var list: std.ArrayList(Vpc) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpc(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpcPeeringConnectionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpcPeeringConnection {
    var list: std.ArrayList(VpcPeeringConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpcPeeringConnection(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpnConcentratorList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpnConcentrator {
    var list: std.ArrayList(VpnConcentrator) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpnConcentrator(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpnConnectionDeviceTypeList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpnConnectionDeviceType {
    var list: std.ArrayList(VpnConnectionDeviceType) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpnConnectionDeviceType(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpnConnectionList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpnConnection {
    var list: std.ArrayList(VpnConnection) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpnConnection(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpnGatewayList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpnGateway {
    var list: std.ArrayList(VpnGateway) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpnGateway(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeVpnStaticRouteList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const VpnStaticRoute {
    var list: std.ArrayList(VpnStaticRoute) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeVpnStaticRoute(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeWorkloadsList(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const []const u8 {
    var list: std.ArrayList([]const u8) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try alloc.dupe(u8, try reader.readElementText()));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializesnapshotTierStatusSet(reader: *aws.xml.Reader, alloc: std.mem.Allocator, comptime item_tag: []const u8) ![]const SnapshotTierStatus {
    var list: std.ArrayList(SnapshotTierStatus) = .{};
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, item_tag)) {
                    try list.append(alloc, try deserializeSnapshotTierStatus(reader, alloc));
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return list.toOwnedSlice(alloc);
}

pub fn deserializeAcceleratorCount(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AcceleratorCount {
    _ = alloc;
    var result: AcceleratorCount = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAcceleratorTotalMemoryMiB(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AcceleratorTotalMemoryMiB {
    _ = alloc;
    var result: AcceleratorTotalMemoryMiB = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccessScopeAnalysisFinding(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AccessScopeAnalysisFinding {
    var result: AccessScopeAnalysisFinding = undefined;
    result.finding_components = null;
    result.finding_id = null;
    result.network_insights_access_scope_analysis_id = null;
    result.network_insights_access_scope_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "findingComponentSet")) {
                    result.finding_components = try deserializePathComponentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "findingId")) {
                    result.finding_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeAnalysisId")) {
                    result.network_insights_access_scope_analysis_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccessScopePath(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AccessScopePath {
    var result: AccessScopePath = undefined;
    result.destination = null;
    result.source = null;
    result.through_resources = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try deserializePathStatement(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try deserializePathStatement(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "throughResourceSet")) {
                    result.through_resources = try deserializeThroughResourcesStatementList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccountAttribute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AccountAttribute {
    var result: AccountAttribute = undefined;
    result.attribute_name = null;
    result.attribute_values = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attributeName")) {
                    result.attribute_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attributeValueSet")) {
                    result.attribute_values = try deserializeAccountAttributeValueList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAccountAttributeValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AccountAttributeValue {
    var result: AccountAttributeValue = undefined;
    result.attribute_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attributeValue")) {
                    result.attribute_value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeActiveInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ActiveInstance {
    var result: ActiveInstance = undefined;
    result.instance_health = null;
    result.instance_id = null;
    result.instance_type = null;
    result.spot_instance_request_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceHealth")) {
                    result.instance_health = std.meta.stringToEnum(InstanceHealthStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeActiveVpnTunnelStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ActiveVpnTunnelStatus {
    var result: ActiveVpnTunnelStatus = undefined;
    result.ike_version = null;
    result.phase_1_dh_group = null;
    result.phase_1_encryption_algorithm = null;
    result.phase_1_integrity_algorithm = null;
    result.phase_2_dh_group = null;
    result.phase_2_encryption_algorithm = null;
    result.phase_2_integrity_algorithm = null;
    result.provisioning_status = null;
    result.provisioning_status_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ikeVersion")) {
                    result.ike_version = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase1DHGroup")) {
                    result.phase_1_dh_group = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "phase1EncryptionAlgorithm")) {
                    result.phase_1_encryption_algorithm = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase1IntegrityAlgorithm")) {
                    result.phase_1_integrity_algorithm = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase2DHGroup")) {
                    result.phase_2_dh_group = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "phase2EncryptionAlgorithm")) {
                    result.phase_2_encryption_algorithm = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase2IntegrityAlgorithm")) {
                    result.phase_2_integrity_algorithm = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisioningStatus")) {
                    result.provisioning_status = std.meta.stringToEnum(VpnTunnelProvisioningStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisioningStatusReason")) {
                    result.provisioning_status_reason = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddedPrincipal(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AddedPrincipal {
    var result: AddedPrincipal = undefined;
    result.principal = null;
    result.principal_type = null;
    result.service_id = null;
    result.service_permission_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "principal")) {
                    result.principal = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "principalType")) {
                    result.principal_type = std.meta.stringToEnum(PrincipalType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "servicePermissionId")) {
                    result.service_permission_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAdditionalDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AdditionalDetail {
    var result: AdditionalDetail = undefined;
    result.additional_detail_type = null;
    result.component = null;
    result.load_balancers = null;
    result.rule_group_rule_options_pairs = null;
    result.rule_group_type_pairs = null;
    result.rule_options = null;
    result.service_name = null;
    result.vpc_endpoint_service = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "additionalDetailType")) {
                    result.additional_detail_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "component")) {
                    result.component = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "loadBalancerSet")) {
                    result.load_balancers = try deserializeAnalysisComponentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ruleGroupRuleOptionsPairSet")) {
                    result.rule_group_rule_options_pairs = try deserializeRuleGroupRuleOptionsPairList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ruleGroupTypePairSet")) {
                    result.rule_group_type_pairs = try deserializeRuleGroupTypePairList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ruleOptionSet")) {
                    result.rule_options = try deserializeRuleOptionList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointService")) {
                    result.vpc_endpoint_service = try deserializeAnalysisComponent(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddress(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Address {
    var result: Address = undefined;
    result.allocation_id = null;
    result.association_id = null;
    result.carrier_ip = null;
    result.customer_owned_ip = null;
    result.customer_owned_ipv_4_pool = null;
    result.domain = null;
    result.instance_id = null;
    result.network_border_group = null;
    result.network_interface_id = null;
    result.network_interface_owner_id = null;
    result.private_ip_address = null;
    result.public_ip = null;
    result.public_ipv_4_pool = null;
    result.service_managed = null;
    result.subnet_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "carrierIp")) {
                    result.carrier_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIp")) {
                    result.customer_owned_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIpv4Pool")) {
                    result.customer_owned_ipv_4_pool = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "domain")) {
                    result.domain = std.meta.stringToEnum(DomainType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceOwnerId")) {
                    result.network_interface_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv4Pool")) {
                    result.public_ipv_4_pool = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceManaged")) {
                    result.service_managed = std.meta.stringToEnum(ServiceManaged, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddressAttribute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AddressAttribute {
    var result: AddressAttribute = undefined;
    result.allocation_id = null;
    result.ptr_record = null;
    result.ptr_record_update = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ptrRecord")) {
                    result.ptr_record = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ptrRecordUpdate")) {
                    result.ptr_record_update = try deserializePtrUpdateStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAddressTransfer(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AddressTransfer {
    var result: AddressTransfer = undefined;
    result.address_transfer_status = null;
    result.allocation_id = null;
    result.public_ip = null;
    result.transfer_account_id = null;
    result.transfer_offer_accepted_timestamp = null;
    result.transfer_offer_expiration_timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressTransferStatus")) {
                    result.address_transfer_status = std.meta.stringToEnum(AddressTransferStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transferAccountId")) {
                    result.transfer_account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transferOfferAcceptedTimestamp")) {
                    result.transfer_offer_accepted_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "transferOfferExpirationTimestamp")) {
                    result.transfer_offer_expiration_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAllowedPrincipal(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AllowedPrincipal {
    var result: AllowedPrincipal = undefined;
    result.principal = null;
    result.principal_type = null;
    result.service_id = null;
    result.service_permission_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "principal")) {
                    result.principal = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "principalType")) {
                    result.principal_type = std.meta.stringToEnum(PrincipalType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "servicePermissionId")) {
                    result.service_permission_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAlternatePathHint(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AlternatePathHint {
    var result: AlternatePathHint = undefined;
    result.component_arn = null;
    result.component_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "componentArn")) {
                    result.component_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "componentId")) {
                    result.component_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisAclRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AnalysisAclRule {
    var result: AnalysisAclRule = undefined;
    result.cidr = null;
    result.egress = null;
    result.port_range = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egress")) {
                    result.egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "portRange")) {
                    result.port_range = try deserializePortRange(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleNumber")) {
                    result.rule_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisComponent(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AnalysisComponent {
    var result: AnalysisComponent = undefined;
    result.arn = null;
    result.id = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisLoadBalancerListener(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AnalysisLoadBalancerListener {
    _ = alloc;
    var result: AnalysisLoadBalancerListener = undefined;
    result.instance_port = null;
    result.load_balancer_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instancePort")) {
                    result.instance_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "loadBalancerPort")) {
                    result.load_balancer_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisLoadBalancerTarget(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AnalysisLoadBalancerTarget {
    var result: AnalysisLoadBalancerTarget = undefined;
    result.address = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance = null;
    result.port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "address")) {
                    result.address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instance")) {
                    result.instance = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisPacketHeader(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AnalysisPacketHeader {
    var result: AnalysisPacketHeader = undefined;
    result.destination_addresses = null;
    result.destination_port_ranges = null;
    result.protocol = null;
    result.source_addresses = null;
    result.source_port_ranges = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationAddressSet")) {
                    result.destination_addresses = try deserializeIpAddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "destinationPortRangeSet")) {
                    result.destination_port_ranges = try deserializePortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceAddressSet")) {
                    result.source_addresses = try deserializeIpAddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourcePortRangeSet")) {
                    result.source_port_ranges = try deserializePortRangeList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisRouteTableRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AnalysisRouteTableRoute {
    var result: AnalysisRouteTableRoute = undefined;
    result.carrier_gateway_id = null;
    result.core_network_arn = null;
    result.destination_cidr = null;
    result.destination_prefix_list_id = null;
    result.egress_only_internet_gateway_id = null;
    result.gateway_id = null;
    result.instance_id = null;
    result.local_gateway_id = null;
    result.nat_gateway_id = null;
    result.network_interface_id = null;
    result.origin = null;
    result.state = null;
    result.transit_gateway_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierGatewayId")) {
                    result.carrier_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkArn")) {
                    result.core_network_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListId")) {
                    result.destination_prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressOnlyInternetGatewayId")) {
                    result.egress_only_internet_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGatewayId")) {
                    result.nat_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "origin")) {
                    result.origin = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAnalysisSecurityGroupRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AnalysisSecurityGroupRule {
    var result: AnalysisSecurityGroupRule = undefined;
    result.cidr = null;
    result.direction = null;
    result.port_range = null;
    result.prefix_list_id = null;
    result.protocol = null;
    result.security_group_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "direction")) {
                    result.direction = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "portRange")) {
                    result.port_range = try deserializePortRange(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupId")) {
                    result.security_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAsnAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AsnAssociation {
    var result: AsnAssociation = undefined;
    result.asn = null;
    result.cidr = null;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "asn")) {
                    result.asn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(AsnAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssignedPrivateIpAddress(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AssignedPrivateIpAddress {
    var result: AssignedPrivateIpAddress = undefined;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssociatedRole(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AssociatedRole {
    var result: AssociatedRole = undefined;
    result.associated_role_arn = null;
    result.certificate_s3_bucket_name = null;
    result.certificate_s3_object_key = null;
    result.encryption_kms_key_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedRoleArn")) {
                    result.associated_role_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "certificateS3BucketName")) {
                    result.certificate_s3_bucket_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "certificateS3ObjectKey")) {
                    result.certificate_s3_object_key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionKmsKeyId")) {
                    result.encryption_kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssociatedTargetNetwork(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AssociatedTargetNetwork {
    var result: AssociatedTargetNetwork = undefined;
    result.network_id = null;
    result.network_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "networkId")) {
                    result.network_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkType")) {
                    result.network_type = std.meta.stringToEnum(AssociatedNetworkType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAssociationStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AssociationStatus {
    var result: AssociationStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(AssociationStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttachmentEnaSrdSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AttachmentEnaSrdSpecification {
    var result: AttachmentEnaSrdSpecification = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeAttachmentEnaSrdUdpSpecification(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttachmentEnaSrdUdpSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AttachmentEnaSrdUdpSpecification {
    _ = alloc;
    var result: AttachmentEnaSrdUdpSpecification = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttributeBooleanValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AttributeBooleanValue {
    _ = alloc;
    var result: AttributeBooleanValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttributeSummary(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AttributeSummary {
    var result: AttributeSummary = undefined;
    result.attribute_name = null;
    result.most_frequent_value = null;
    result.number_of_matched_accounts = null;
    result.number_of_unmatched_accounts = null;
    result.regional_summaries = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attributeName")) {
                    result.attribute_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "mostFrequentValue")) {
                    result.most_frequent_value = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "numberOfMatchedAccounts")) {
                    result.number_of_matched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "numberOfUnmatchedAccounts")) {
                    result.number_of_unmatched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "regionalSummarySet")) {
                    result.regional_summaries = try deserializeRegionalSummaryList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAttributeValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AttributeValue {
    var result: AttributeValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAuthorizationRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AuthorizationRule {
    var result: AuthorizationRule = undefined;
    result.access_all = null;
    result.client_vpn_endpoint_id = null;
    result.description = null;
    result.destination_cidr = null;
    result.group_id = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accessAll")) {
                    result.access_all = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnAuthorizationRuleStatus(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAvailabilityZone(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AvailabilityZone {
    var result: AvailabilityZone = undefined;
    result.group_long_name = null;
    result.group_name = null;
    result.messages = null;
    result.network_border_group = null;
    result.opt_in_status = null;
    result.parent_zone_id = null;
    result.parent_zone_name = null;
    result.region_name = null;
    result.state = null;
    result.zone_id = null;
    result.zone_name = null;
    result.zone_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupLongName")) {
                    result.group_long_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "messageSet")) {
                    result.messages = try deserializeAvailabilityZoneMessageList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "optInStatus")) {
                    result.opt_in_status = std.meta.stringToEnum(AvailabilityZoneOptInStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "parentZoneId")) {
                    result.parent_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "parentZoneName")) {
                    result.parent_zone_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneState")) {
                    result.state = std.meta.stringToEnum(AvailabilityZoneState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneId")) {
                    result.zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneName")) {
                    result.zone_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "zoneType")) {
                    result.zone_type = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAvailabilityZoneMessage(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AvailabilityZoneMessage {
    var result: AvailabilityZoneMessage = undefined;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeAvailableCapacity(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !AvailableCapacity {
    var result: AvailableCapacity = undefined;
    result.available_instance_capacity = null;
    result.available_v_cpus = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availableInstanceCapacity")) {
                    result.available_instance_capacity = try deserializeAvailableInstanceCapacityList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "availableVCpus")) {
                    result.available_v_cpus = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBaselineEbsBandwidthMbps(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !BaselineEbsBandwidthMbps {
    _ = alloc;
    var result: BaselineEbsBandwidthMbps = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBaselinePerformanceFactors(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !BaselinePerformanceFactors {
    var result: BaselinePerformanceFactors = undefined;
    result.cpu = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpu")) {
                    result.cpu = try deserializeCpuPerformanceFactor(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBlockDeviceMapping(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !BlockDeviceMapping {
    var result: BlockDeviceMapping = undefined;
    result.device_name = null;
    result.ebs = null;
    result.no_device = null;
    result.virtual_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeEbsBlockDevice(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "noDevice")) {
                    result.no_device = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualName")) {
                    result.virtual_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBlockDeviceMappingResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !BlockDeviceMappingResponse {
    var result: BlockDeviceMappingResponse = undefined;
    result.device_name = null;
    result.ebs = null;
    result.no_device = null;
    result.virtual_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeEbsBlockDeviceResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "noDevice")) {
                    result.no_device = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualName")) {
                    result.virtual_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBlockPublicAccessStates(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !BlockPublicAccessStates {
    _ = alloc;
    var result: BlockPublicAccessStates = undefined;
    result.internet_gateway_block_mode = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "internetGatewayBlockMode")) {
                    result.internet_gateway_block_mode = std.meta.stringToEnum(BlockPublicAccessMode, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBundleTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !BundleTask {
    var result: BundleTask = undefined;
    result.bundle_id = null;
    result.bundle_task_error = null;
    result.instance_id = null;
    result.progress = null;
    result.start_time = null;
    result.state = null;
    result.storage = null;
    result.update_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bundleId")) {
                    result.bundle_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "error")) {
                    result.bundle_task_error = try deserializeBundleTaskError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(BundleTaskState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storage")) {
                    result.storage = try deserializeStorage(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "updateTime")) {
                    result.update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeBundleTaskError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !BundleTaskError {
    var result: BundleTaskError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeByoasn(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Byoasn {
    var result: Byoasn = undefined;
    result.asn = null;
    result.ipam_id = null;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "asn")) {
                    result.asn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(AsnState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeByoipCidr(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ByoipCidr {
    var result: ByoipCidr = undefined;
    result.advertisement_type = null;
    result.asn_associations = null;
    result.cidr = null;
    result.description = null;
    result.network_border_group = null;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "advertisementType")) {
                    result.advertisement_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "asnAssociationSet")) {
                    result.asn_associations = try deserializeAsnAssociationSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(ByoipCidrState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelCapacityReservationFleetError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CancelCapacityReservationFleetError {
    var result: CancelCapacityReservationFleetError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelSpotFleetRequestsError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CancelSpotFleetRequestsError {
    var result: CancelSpotFleetRequestsError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(CancelBatchErrorCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelSpotFleetRequestsErrorItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CancelSpotFleetRequestsErrorItem {
    var result: CancelSpotFleetRequestsErrorItem = undefined;
    result.@"error" = null;
    result.spot_fleet_request_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeCancelSpotFleetRequestsError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestId")) {
                    result.spot_fleet_request_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelSpotFleetRequestsSuccessItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CancelSpotFleetRequestsSuccessItem {
    var result: CancelSpotFleetRequestsSuccessItem = undefined;
    result.current_spot_fleet_request_state = null;
    result.previous_spot_fleet_request_state = null;
    result.spot_fleet_request_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currentSpotFleetRequestState")) {
                    result.current_spot_fleet_request_state = std.meta.stringToEnum(BatchState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousSpotFleetRequestState")) {
                    result.previous_spot_fleet_request_state = std.meta.stringToEnum(BatchState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestId")) {
                    result.spot_fleet_request_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCancelledSpotInstanceRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CancelledSpotInstanceRequest {
    var result: CancelledSpotInstanceRequest = undefined;
    result.spot_instance_request_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(CancelSpotInstanceRequestState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityAllocation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityAllocation {
    _ = alloc;
    var result: CapacityAllocation = undefined;
    result.allocation_type = null;
    result.count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationType")) {
                    result.allocation_type = std.meta.stringToEnum(AllocationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlock(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityBlock {
    var result: CapacityBlock = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_id = null;
    result.capacity_reservation_ids = null;
    result.create_date = null;
    result.end_date = null;
    result.start_date = null;
    result.state = null;
    result.tags = null;
    result.ultraserver_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationIdSet")) {
                    result.capacity_reservation_ids = try deserializeCapacityReservationIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(CapacityBlockResourceState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ultraserverType")) {
                    result.ultraserver_type = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockExtension(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityBlockExtension {
    var result: CapacityBlockExtension = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_extension_duration_hours = null;
    result.capacity_block_extension_end_date = null;
    result.capacity_block_extension_offering_id = null;
    result.capacity_block_extension_purchase_date = null;
    result.capacity_block_extension_start_date = null;
    result.capacity_block_extension_status = null;
    result.capacity_reservation_id = null;
    result.currency_code = null;
    result.instance_count = null;
    result.instance_type = null;
    result.upfront_fee = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionDurationHours")) {
                    result.capacity_block_extension_duration_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionEndDate")) {
                    result.capacity_block_extension_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionOfferingId")) {
                    result.capacity_block_extension_offering_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionPurchaseDate")) {
                    result.capacity_block_extension_purchase_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionStartDate")) {
                    result.capacity_block_extension_start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionStatus")) {
                    result.capacity_block_extension_status = std.meta.stringToEnum(CapacityBlockExtensionStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontFee")) {
                    result.upfront_fee = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockExtensionOffering(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityBlockExtensionOffering {
    var result: CapacityBlockExtensionOffering = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_extension_duration_hours = null;
    result.capacity_block_extension_end_date = null;
    result.capacity_block_extension_offering_id = null;
    result.capacity_block_extension_start_date = null;
    result.currency_code = null;
    result.instance_count = null;
    result.instance_type = null;
    result.start_date = null;
    result.tenancy = null;
    result.upfront_fee = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionDurationHours")) {
                    result.capacity_block_extension_duration_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionEndDate")) {
                    result.capacity_block_extension_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionOfferingId")) {
                    result.capacity_block_extension_offering_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockExtensionStartDate")) {
                    result.capacity_block_extension_start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(CapacityReservationTenancy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontFee")) {
                    result.upfront_fee = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockOffering(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityBlockOffering {
    var result: CapacityBlockOffering = undefined;
    result.availability_zone = null;
    result.capacity_block_duration_hours = null;
    result.capacity_block_duration_minutes = null;
    result.capacity_block_offering_id = null;
    result.currency_code = null;
    result.end_date = null;
    result.instance_count = null;
    result.instance_type = null;
    result.start_date = null;
    result.tenancy = null;
    result.ultraserver_count = null;
    result.ultraserver_type = null;
    result.upfront_fee = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockDurationHours")) {
                    result.capacity_block_duration_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockDurationMinutes")) {
                    result.capacity_block_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityBlockOfferingId")) {
                    result.capacity_block_offering_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(CapacityReservationTenancy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ultraserverCount")) {
                    result.ultraserver_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ultraserverType")) {
                    result.ultraserver_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontFee")) {
                    result.upfront_fee = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityBlockStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityBlockStatus {
    var result: CapacityBlockStatus = undefined;
    result.capacity_block_id = null;
    result.capacity_reservation_statuses = null;
    result.interconnect_status = null;
    result.total_available_capacity = null;
    result.total_capacity = null;
    result.total_unavailable_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationStatusSet")) {
                    result.capacity_reservation_statuses = try deserializeCapacityReservationStatusSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "interconnectStatus")) {
                    result.interconnect_status = std.meta.stringToEnum(CapacityBlockInterconnectStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalAvailableCapacity")) {
                    result.total_available_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalCapacity")) {
                    result.total_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalUnavailableCapacity")) {
                    result.total_unavailable_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityManagerDataExportResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityManagerDataExportResponse {
    var result: CapacityManagerDataExportResponse = undefined;
    result.capacity_manager_data_export_id = null;
    result.create_time = null;
    result.latest_delivery_s3_location_uri = null;
    result.latest_delivery_status = null;
    result.latest_delivery_status_message = null;
    result.latest_delivery_time = null;
    result.output_format = null;
    result.s3_bucket_name = null;
    result.s3_bucket_prefix = null;
    result.schedule = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityManagerDataExportId")) {
                    result.capacity_manager_data_export_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "latestDeliveryS3LocationUri")) {
                    result.latest_delivery_s3_location_uri = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "latestDeliveryStatus")) {
                    result.latest_delivery_status = std.meta.stringToEnum(CapacityManagerDataExportStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "latestDeliveryStatusMessage")) {
                    result.latest_delivery_status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "latestDeliveryTime")) {
                    result.latest_delivery_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "outputFormat")) {
                    result.output_format = std.meta.stringToEnum(OutputFormat, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3BucketName")) {
                    result.s3_bucket_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3BucketPrefix")) {
                    result.s3_bucket_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "schedule")) {
                    result.schedule = std.meta.stringToEnum(Schedule, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityManagerDimension(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityManagerDimension {
    var result: CapacityManagerDimension = undefined;
    result.account_id = null;
    result.availability_zone_id = null;
    result.instance_family = null;
    result.instance_platform = null;
    result.instance_type = null;
    result.reservation_arn = null;
    result.reservation_create_timestamp = null;
    result.reservation_end_date_type = null;
    result.reservation_end_timestamp = null;
    result.reservation_id = null;
    result.reservation_instance_match_criteria = null;
    result.reservation_start_timestamp = null;
    result.reservation_state = null;
    result.reservation_type = null;
    result.reservation_unused_financial_owner = null;
    result.resource_region = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePlatform")) {
                    result.instance_platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationArn")) {
                    result.reservation_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationCreateTimestamp")) {
                    result.reservation_create_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reservationEndDateType")) {
                    result.reservation_end_date_type = std.meta.stringToEnum(ReservationEndDateType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationEndTimestamp")) {
                    result.reservation_end_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reservationId")) {
                    result.reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationInstanceMatchCriteria")) {
                    result.reservation_instance_match_criteria = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationStartTimestamp")) {
                    result.reservation_start_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reservationState")) {
                    result.reservation_state = std.meta.stringToEnum(ReservationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationType")) {
                    result.reservation_type = std.meta.stringToEnum(ReservationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationUnusedFinancialOwner")) {
                    result.reservation_unused_financial_owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(CapacityTenancy, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservation {
    var result: CapacityReservation = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.available_instance_count = null;
    result.capacity_allocations = null;
    result.capacity_block_id = null;
    result.capacity_reservation_arn = null;
    result.capacity_reservation_fleet_id = null;
    result.capacity_reservation_id = null;
    result.commitment_info = null;
    result.create_date = null;
    result.delivery_preference = null;
    result.ebs_optimized = null;
    result.end_date = null;
    result.end_date_type = null;
    result.ephemeral_storage = null;
    result.instance_match_criteria = null;
    result.instance_platform = null;
    result.instance_type = null;
    result.interruptible = null;
    result.interruptible_capacity_allocation = null;
    result.interruption_info = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.placement_group_arn = null;
    result.reservation_type = null;
    result.start_date = null;
    result.state = null;
    result.tags = null;
    result.tenancy = null;
    result.total_instance_count = null;
    result.unused_reservation_billing_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableInstanceCount")) {
                    result.available_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "capacityAllocationSet")) {
                    result.capacity_allocations = try deserializeCapacityAllocations(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationArn")) {
                    result.capacity_reservation_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "commitmentInfo")) {
                    result.commitment_info = try deserializeCapacityReservationCommitmentInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deliveryPreference")) {
                    result.delivery_preference = std.meta.stringToEnum(CapacityReservationDeliveryPreference, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "endDateType")) {
                    result.end_date_type = std.meta.stringToEnum(EndDateType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ephemeralStorage")) {
                    result.ephemeral_storage = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "instanceMatchCriteria")) {
                    result.instance_match_criteria = std.meta.stringToEnum(InstanceMatchCriteria, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePlatform")) {
                    result.instance_platform = std.meta.stringToEnum(CapacityReservationInstancePlatform, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "interruptible")) {
                    result.interruptible = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "interruptibleCapacityAllocation")) {
                    result.interruptible_capacity_allocation = try deserializeInterruptibleCapacityAllocation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "interruptionInfo")) {
                    result.interruption_info = try deserializeInterruptionInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placementGroupArn")) {
                    result.placement_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationType")) {
                    result.reservation_type = std.meta.stringToEnum(CapacityReservationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(CapacityReservationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(CapacityReservationTenancy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalInstanceCount")) {
                    result.total_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "unusedReservationBillingOwnerId")) {
                    result.unused_reservation_billing_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationBillingRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationBillingRequest {
    var result: CapacityReservationBillingRequest = undefined;
    result.capacity_reservation_id = null;
    result.capacity_reservation_info = null;
    result.last_update_time = null;
    result.requested_by = null;
    result.status = null;
    result.status_message = null;
    result.unused_reservation_billing_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationInfo")) {
                    result.capacity_reservation_info = try deserializeCapacityReservationInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "lastUpdateTime")) {
                    result.last_update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "requestedBy")) {
                    result.requested_by = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(CapacityReservationBillingRequestStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "unusedReservationBillingOwnerId")) {
                    result.unused_reservation_billing_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationCommitmentInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationCommitmentInfo {
    _ = alloc;
    var result: CapacityReservationCommitmentInfo = undefined;
    result.commitment_end_date = null;
    result.committed_instance_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "commitmentEndDate")) {
                    result.commitment_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "committedInstanceCount")) {
                    result.committed_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationFleet(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationFleet {
    var result: CapacityReservationFleet = undefined;
    result.allocation_strategy = null;
    result.capacity_reservation_fleet_arn = null;
    result.capacity_reservation_fleet_id = null;
    result.create_time = null;
    result.end_date = null;
    result.instance_match_criteria = null;
    result.instance_type_specifications = null;
    result.state = null;
    result.tags = null;
    result.tenancy = null;
    result.total_fulfilled_capacity = null;
    result.total_target_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetArn")) {
                    result.capacity_reservation_fleet_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceMatchCriteria")) {
                    result.instance_match_criteria = std.meta.stringToEnum(FleetInstanceMatchCriteria, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceTypeSpecificationSet")) {
                    result.instance_type_specifications = try deserializeFleetCapacityReservationSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(CapacityReservationFleetState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(FleetCapacityReservationTenancy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalFulfilledCapacity")) {
                    result.total_fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "totalTargetCapacity")) {
                    result.total_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationFleetCancellationState(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationFleetCancellationState {
    var result: CapacityReservationFleetCancellationState = undefined;
    result.capacity_reservation_fleet_id = null;
    result.current_fleet_state = null;
    result.previous_fleet_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currentFleetState")) {
                    result.current_fleet_state = std.meta.stringToEnum(CapacityReservationFleetState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousFleetState")) {
                    result.previous_fleet_state = std.meta.stringToEnum(CapacityReservationFleetState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationGroup {
    var result: CapacityReservationGroup = undefined;
    result.group_arn = null;
    result.owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupArn")) {
                    result.group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationInfo {
    var result: CapacityReservationInfo = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_type = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(CapacityReservationTenancy, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationOptions {
    _ = alloc;
    var result: CapacityReservationOptions = undefined;
    result.usage_strategy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "usageStrategy")) {
                    result.usage_strategy = std.meta.stringToEnum(FleetCapacityReservationUsageStrategy, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationSpecificationResponse {
    var result: CapacityReservationSpecificationResponse = undefined;
    result.capacity_reservation_preference = null;
    result.capacity_reservation_target = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationPreference")) {
                    result.capacity_reservation_preference = std.meta.stringToEnum(CapacityReservationPreference, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationTarget")) {
                    result.capacity_reservation_target = try deserializeCapacityReservationTargetResponse(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationStatus {
    var result: CapacityReservationStatus = undefined;
    result.capacity_reservation_id = null;
    result.total_available_capacity = null;
    result.total_capacity = null;
    result.total_unavailable_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalAvailableCapacity")) {
                    result.total_available_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalCapacity")) {
                    result.total_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalUnavailableCapacity")) {
                    result.total_unavailable_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationTargetResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationTargetResponse {
    var result: CapacityReservationTargetResponse = undefined;
    result.capacity_reservation_id = null;
    result.capacity_reservation_resource_group_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationResourceGroupArn")) {
                    result.capacity_reservation_resource_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCapacityReservationTopology(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CapacityReservationTopology {
    var result: CapacityReservationTopology = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_block_id = null;
    result.capacity_reservation_id = null;
    result.group_name = null;
    result.instance_type = null;
    result.network_nodes = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkNodeSet")) {
                    result.network_nodes = try deserializeNetworkNodeSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCarrierGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CarrierGateway {
    var result: CarrierGateway = undefined;
    result.carrier_gateway_id = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierGatewayId")) {
                    result.carrier_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(CarrierGatewayState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCertificateAuthentication(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CertificateAuthentication {
    var result: CertificateAuthentication = undefined;
    result.client_root_certificate_chain = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientRootCertificateChain")) {
                    result.client_root_certificate_chain = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCidrBlock(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CidrBlock {
    var result: CidrBlock = undefined;
    result.cidr_block = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLinkDnsSupport(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClassicLinkDnsSupport {
    var result: ClassicLinkDnsSupport = undefined;
    result.classic_link_dns_supported = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLinkDnsSupported")) {
                    result.classic_link_dns_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLinkInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClassicLinkInstance {
    var result: ClassicLinkInstance = undefined;
    result.groups = null;
    result.instance_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLoadBalancer(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClassicLoadBalancer {
    var result: ClassicLoadBalancer = undefined;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClassicLoadBalancersConfig(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClassicLoadBalancersConfig {
    var result: ClassicLoadBalancersConfig = undefined;
    result.classic_load_balancers = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLoadBalancers")) {
                    result.classic_load_balancers = try deserializeClassicLoadBalancers(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientCertificateRevocationListStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientCertificateRevocationListStatus {
    var result: ClientCertificateRevocationListStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(ClientCertificateRevocationListStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientConnectResponseOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientConnectResponseOptions {
    var result: ClientConnectResponseOptions = undefined;
    result.enabled = null;
    result.lambda_function_arn = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "lambdaFunctionArn")) {
                    result.lambda_function_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnEndpointAttributeStatus(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientLoginBannerResponseOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientLoginBannerResponseOptions {
    var result: ClientLoginBannerResponseOptions = undefined;
    result.banner_text = null;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bannerText")) {
                    result.banner_text = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientRouteEnforcementResponseOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientRouteEnforcementResponseOptions {
    _ = alloc;
    var result: ClientRouteEnforcementResponseOptions = undefined;
    result.enforced = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enforced")) {
                    result.enforced = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnAuthentication(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnAuthentication {
    var result: ClientVpnAuthentication = undefined;
    result.active_directory = null;
    result.federated_authentication = null;
    result.mutual_authentication = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "activeDirectory")) {
                    result.active_directory = try deserializeDirectoryServiceAuthentication(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "federatedAuthentication")) {
                    result.federated_authentication = try deserializeFederatedAuthentication(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "mutualAuthentication")) {
                    result.mutual_authentication = try deserializeCertificateAuthentication(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(ClientVpnAuthenticationType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnAuthorizationRuleStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnAuthorizationRuleStatus {
    var result: ClientVpnAuthorizationRuleStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(ClientVpnAuthorizationRuleStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnConnection(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnConnection {
    var result: ClientVpnConnection = undefined;
    result.client_ip = null;
    result.client_ipv_6_address = null;
    result.client_vpn_endpoint_id = null;
    result.common_name = null;
    result.connection_end_time = null;
    result.connection_established_time = null;
    result.connection_id = null;
    result.egress_bytes = null;
    result.egress_packets = null;
    result.ingress_bytes = null;
    result.ingress_packets = null;
    result.posture_compliance_statuses = null;
    result.status = null;
    result.timestamp = null;
    result.username = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientIp")) {
                    result.client_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientIpv6Address")) {
                    result.client_ipv_6_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "commonName")) {
                    result.common_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionEndTime")) {
                    result.connection_end_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionEstablishedTime")) {
                    result.connection_established_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionId")) {
                    result.connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressBytes")) {
                    result.egress_bytes = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressPackets")) {
                    result.egress_packets = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ingressBytes")) {
                    result.ingress_bytes = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ingressPackets")) {
                    result.ingress_packets = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "postureComplianceStatusSet")) {
                    result.posture_compliance_statuses = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnConnectionStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "username")) {
                    result.username = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnConnectionStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnConnectionStatus {
    var result: ClientVpnConnectionStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(ClientVpnConnectionStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnEndpoint(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnEndpoint {
    var result: ClientVpnEndpoint = undefined;
    result.associated_target_networks = null;
    result.authentication_options = null;
    result.client_cidr_block = null;
    result.client_connect_options = null;
    result.client_login_banner_options = null;
    result.client_route_enforcement_options = null;
    result.client_vpn_endpoint_id = null;
    result.connection_log_options = null;
    result.creation_time = null;
    result.deletion_time = null;
    result.description = null;
    result.disconnect_on_session_timeout = null;
    result.dns_name = null;
    result.dns_servers = null;
    result.endpoint_ip_address_type = null;
    result.security_group_ids = null;
    result.self_service_portal_url = null;
    result.server_certificate_arn = null;
    result.session_timeout_hours = null;
    result.split_tunnel = null;
    result.status = null;
    result.tags = null;
    result.traffic_ip_address_type = null;
    result.transport_protocol = null;
    result.vpc_id = null;
    result.vpn_port = null;
    result.vpn_protocol = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedTargetNetwork")) {
                    result.associated_target_networks = try deserializeAssociatedTargetNetworkSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "authenticationOptions")) {
                    result.authentication_options = try deserializeClientVpnAuthenticationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "clientCidrBlock")) {
                    result.client_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientConnectOptions")) {
                    result.client_connect_options = try deserializeClientConnectResponseOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "clientLoginBannerOptions")) {
                    result.client_login_banner_options = try deserializeClientLoginBannerResponseOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "clientRouteEnforcementOptions")) {
                    result.client_route_enforcement_options = try deserializeClientRouteEnforcementResponseOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionLogOptions")) {
                    result.connection_log_options = try deserializeConnectionLogResponseOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deletionTime")) {
                    result.deletion_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disconnectOnSessionTimeout")) {
                    result.disconnect_on_session_timeout = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsServer")) {
                    result.dns_servers = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "endpointIpAddressType")) {
                    result.endpoint_ip_address_type = std.meta.stringToEnum(EndpointIpAddressType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeClientVpnSecurityGroupIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "selfServicePortalUrl")) {
                    result.self_service_portal_url = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serverCertificateArn")) {
                    result.server_certificate_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sessionTimeoutHours")) {
                    result.session_timeout_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "splitTunnel")) {
                    result.split_tunnel = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnEndpointStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trafficIpAddressType")) {
                    result.traffic_ip_address_type = std.meta.stringToEnum(TrafficIpAddressType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transportProtocol")) {
                    result.transport_protocol = std.meta.stringToEnum(TransportProtocol, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnPort")) {
                    result.vpn_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "vpnProtocol")) {
                    result.vpn_protocol = std.meta.stringToEnum(VpnProtocol, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnEndpointAttributeStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnEndpointAttributeStatus {
    var result: ClientVpnEndpointAttributeStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(ClientVpnEndpointAttributeStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnEndpointStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnEndpointStatus {
    var result: ClientVpnEndpointStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(ClientVpnEndpointStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnRoute {
    var result: ClientVpnRoute = undefined;
    result.client_vpn_endpoint_id = null;
    result.description = null;
    result.destination_cidr = null;
    result.origin = null;
    result.status = null;
    result.target_subnet = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "origin")) {
                    result.origin = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeClientVpnRouteStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "targetSubnet")) {
                    result.target_subnet = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeClientVpnRouteStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ClientVpnRouteStatus {
    var result: ClientVpnRouteStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(ClientVpnRouteStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCloudWatchLogOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CloudWatchLogOptions {
    var result: CloudWatchLogOptions = undefined;
    result.bgp_log_enabled = null;
    result.bgp_log_group_arn = null;
    result.bgp_log_output_format = null;
    result.log_enabled = null;
    result.log_group_arn = null;
    result.log_output_format = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpLogEnabled")) {
                    result.bgp_log_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "bgpLogGroupArn")) {
                    result.bgp_log_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bgpLogOutputFormat")) {
                    result.bgp_log_output_format = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logEnabled")) {
                    result.log_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "logGroupArn")) {
                    result.log_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logOutputFormat")) {
                    result.log_output_format = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCoipAddressUsage(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CoipAddressUsage {
    var result: CoipAddressUsage = undefined;
    result.allocation_id = null;
    result.aws_account_id = null;
    result.aws_service = null;
    result.co_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsAccountId")) {
                    result.aws_account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsService")) {
                    result.aws_service = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coIp")) {
                    result.co_ip = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCoipCidr(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CoipCidr {
    var result: CoipCidr = undefined;
    result.cidr = null;
    result.coip_pool_id = null;
    result.local_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coipPoolId")) {
                    result.coip_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCoipPool(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CoipPool {
    var result: CoipPool = undefined;
    result.local_gateway_route_table_id = null;
    result.pool_arn = null;
    result.pool_cidrs = null;
    result.pool_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolArn")) {
                    result.pool_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolCidrSet")) {
                    result.pool_cidrs = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "poolId")) {
                    result.pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionLogResponseOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ConnectionLogResponseOptions {
    var result: ConnectionLogResponseOptions = undefined;
    result.cloudwatch_log_group = null;
    result.cloudwatch_log_stream = null;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "CloudwatchLogGroup")) {
                    result.cloudwatch_log_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "CloudwatchLogStream")) {
                    result.cloudwatch_log_stream = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "Enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionNotification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ConnectionNotification {
    var result: ConnectionNotification = undefined;
    result.connection_events = null;
    result.connection_notification_arn = null;
    result.connection_notification_id = null;
    result.connection_notification_state = null;
    result.connection_notification_type = null;
    result.service_id = null;
    result.service_region = null;
    result.vpc_endpoint_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "connectionEvents")) {
                    result.connection_events = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "connectionNotificationArn")) {
                    result.connection_notification_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionNotificationId")) {
                    result.connection_notification_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionNotificationState")) {
                    result.connection_notification_state = std.meta.stringToEnum(ConnectionNotificationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionNotificationType")) {
                    result.connection_notification_type = std.meta.stringToEnum(ConnectionNotificationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceRegion")) {
                    result.service_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ConnectionTrackingConfiguration {
    _ = alloc;
    var result: ConnectionTrackingConfiguration = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "tcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ConnectionTrackingSpecification {
    _ = alloc;
    var result: ConnectionTrackingSpecification = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "tcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingSpecificationRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ConnectionTrackingSpecificationRequest {
    _ = alloc;
    var result: ConnectionTrackingSpecificationRequest = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "TcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "UdpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "UdpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConnectionTrackingSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ConnectionTrackingSpecificationResponse {
    _ = alloc;
    var result: ConnectionTrackingSpecificationResponse = undefined;
    result.tcp_established_timeout = null;
    result.udp_stream_timeout = null;
    result.udp_timeout = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "tcpEstablishedTimeout")) {
                    result.tcp_established_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpStreamTimeout")) {
                    result.udp_stream_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "udpTimeout")) {
                    result.udp_timeout = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeConversionTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ConversionTask {
    var result: ConversionTask = undefined;
    result.conversion_task_id = null;
    result.expiration_time = null;
    result.import_instance = null;
    result.import_volume = null;
    result.state = null;
    result.status_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "conversionTaskId")) {
                    result.conversion_task_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "expirationTime")) {
                    result.expiration_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importInstance")) {
                    result.import_instance = try deserializeImportInstanceTaskDetails(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "importVolume")) {
                    result.import_volume = try deserializeImportVolumeTaskDetails(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(ConversionTaskState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCpuOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CpuOptions {
    _ = alloc;
    var result: CpuOptions = undefined;
    result.amd_sev_snp = null;
    result.core_count = null;
    result.threads_per_core = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amdSevSnp")) {
                    result.amd_sev_snp = std.meta.stringToEnum(AmdSevSnpSpecification, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreCount")) {
                    result.core_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "threadsPerCore")) {
                    result.threads_per_core = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCpuPerformanceFactor(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CpuPerformanceFactor {
    var result: CpuPerformanceFactor = undefined;
    result.references = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "referenceSet")) {
                    result.references = try deserializePerformanceFactorReferenceSet(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreateFleetError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CreateFleetError {
    var result: CreateFleetError = undefined;
    result.error_code = null;
    result.error_message = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "errorCode")) {
                    result.error_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "errorMessage")) {
                    result.error_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = std.meta.stringToEnum(InstanceLifecycle, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreateFleetInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CreateFleetInstance {
    var result: CreateFleetInstance = undefined;
    result.instance_ids = null;
    result.instance_type = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    result.platform = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceIds")) {
                    result.instance_ids = try deserializeInstanceIdsSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = std.meta.stringToEnum(InstanceLifecycle, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = std.meta.stringToEnum(PlatformValues, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreateVolumePermission(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CreateVolumePermission {
    var result: CreateVolumePermission = undefined;
    result.group = null;
    result.user_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "group")) {
                    result.group = std.meta.stringToEnum(PermissionGroup, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreationDateCondition(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CreationDateCondition {
    _ = alloc;
    var result: CreationDateCondition = undefined;
    result.maximum_days_since_created = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "maximumDaysSinceCreated")) {
                    result.maximum_days_since_created = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCreditSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CreditSpecification {
    var result: CreditSpecification = undefined;
    result.cpu_credits = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpuCredits")) {
                    result.cpu_credits = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeCustomerGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !CustomerGateway {
    var result: CustomerGateway = undefined;
    result.bgp_asn = null;
    result.bgp_asn_extended = null;
    result.certificate_arn = null;
    result.customer_gateway_id = null;
    result.device_name = null;
    result.ip_address = null;
    result.state = null;
    result.tags = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpAsn")) {
                    result.bgp_asn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bgpAsnExtended")) {
                    result.bgp_asn_extended = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "certificateArn")) {
                    result.certificate_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGatewayId")) {
                    result.customer_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddress")) {
                    result.ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDataResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DataResponse {
    var result: DataResponse = undefined;
    result.destination = null;
    result.id = null;
    result.metric = null;
    result.metric_points = null;
    result.period = null;
    result.source = null;
    result.statistic = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metric")) {
                    result.metric = std.meta.stringToEnum(MetricType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metricPointSet")) {
                    result.metric_points = try deserializeMetricPoints(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "period")) {
                    result.period = std.meta.stringToEnum(PeriodType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statistic")) {
                    result.statistic = std.meta.stringToEnum(StatisticType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeclarativePoliciesReport(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeclarativePoliciesReport {
    var result: DeclarativePoliciesReport = undefined;
    result.end_time = null;
    result.report_id = null;
    result.s3_bucket = null;
    result.s3_prefix = null;
    result.start_time = null;
    result.status = null;
    result.tags = null;
    result.target_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endTime")) {
                    result.end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reportId")) {
                    result.report_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Prefix")) {
                    result.s3_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(ReportState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "targetId")) {
                    result.target_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteFleetError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeleteFleetError {
    var result: DeleteFleetError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(DeleteFleetErrorCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteFleetErrorItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeleteFleetErrorItem {
    var result: DeleteFleetErrorItem = undefined;
    result.@"error" = null;
    result.fleet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeDeleteFleetError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "fleetId")) {
                    result.fleet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteFleetSuccessItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeleteFleetSuccessItem {
    var result: DeleteFleetSuccessItem = undefined;
    result.current_fleet_state = null;
    result.fleet_id = null;
    result.previous_fleet_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currentFleetState")) {
                    result.current_fleet_state = std.meta.stringToEnum(FleetStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fleetId")) {
                    result.fleet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousFleetState")) {
                    result.previous_fleet_state = std.meta.stringToEnum(FleetStateCode, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseErrorItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeleteLaunchTemplateVersionsResponseErrorItem {
    var result: DeleteLaunchTemplateVersionsResponseErrorItem = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.response_error = null;
    result.version_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "responseError")) {
                    result.response_error = try deserializeResponseError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "versionNumber")) {
                    result.version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteLaunchTemplateVersionsResponseSuccessItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeleteLaunchTemplateVersionsResponseSuccessItem {
    var result: DeleteLaunchTemplateVersionsResponseSuccessItem = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.version_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "versionNumber")) {
                    result.version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteQueuedReservedInstancesError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeleteQueuedReservedInstancesError {
    var result: DeleteQueuedReservedInstancesError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(DeleteQueuedReservedInstancesErrorCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeleteSnapshotReturnCode(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeleteSnapshotReturnCode {
    var result: DeleteSnapshotReturnCode = undefined;
    result.return_code = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "returnCode")) {
                    result.return_code = std.meta.stringToEnum(SnapshotReturnCodes, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeprecationTimeCondition(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeprecationTimeCondition {
    _ = alloc;
    var result: DeprecationTimeCondition = undefined;
    result.maximum_days_since_deprecated = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "maximumDaysSinceDeprecated")) {
                    result.maximum_days_since_deprecated = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFastLaunchImagesSuccessItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DescribeFastLaunchImagesSuccessItem {
    var result: DescribeFastLaunchImagesSuccessItem = undefined;
    result.image_id = null;
    result.launch_template = null;
    result.max_parallel_launches = null;
    result.owner_id = null;
    result.resource_type = null;
    result.snapshot_configuration = null;
    result.state = null;
    result.state_transition_reason = null;
    result.state_transition_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplate")) {
                    result.launch_template = try deserializeFastLaunchLaunchTemplateSpecificationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "maxParallelLaunches")) {
                    result.max_parallel_launches = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(FastLaunchResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotConfiguration")) {
                    result.snapshot_configuration = try deserializeFastLaunchSnapshotConfigurationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(FastLaunchStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionTime")) {
                    result.state_transition_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFastSnapshotRestoreSuccessItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DescribeFastSnapshotRestoreSuccessItem {
    var result: DescribeFastSnapshotRestoreSuccessItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.disabled_time = null;
    result.disabling_time = null;
    result.enabled_time = null;
    result.enabling_time = null;
    result.optimizing_time = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.snapshot_id = null;
    result.state = null;
    result.state_transition_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disabledTime")) {
                    result.disabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "disablingTime")) {
                    result.disabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enabledTime")) {
                    result.enabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enablingTime")) {
                    result.enabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "optimizingTime")) {
                    result.optimizing_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(FastSnapshotRestoreStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFleetError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DescribeFleetError {
    var result: DescribeFleetError = undefined;
    result.error_code = null;
    result.error_message = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "errorCode")) {
                    result.error_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "errorMessage")) {
                    result.error_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = std.meta.stringToEnum(InstanceLifecycle, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDescribeFleetsInstances(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DescribeFleetsInstances {
    var result: DescribeFleetsInstances = undefined;
    result.instance_ids = null;
    result.instance_type = null;
    result.launch_template_and_overrides = null;
    result.lifecycle = null;
    result.platform = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceIds")) {
                    result.instance_ids = try deserializeInstanceIdsSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateAndOverrides")) {
                    result.launch_template_and_overrides = try deserializeLaunchTemplateAndOverridesResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "lifecycle")) {
                    result.lifecycle = std.meta.stringToEnum(InstanceLifecycle, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = std.meta.stringToEnum(PlatformValues, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDestinationOptionsResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DestinationOptionsResponse {
    _ = alloc;
    var result: DestinationOptionsResponse = undefined;
    result.file_format = null;
    result.hive_compatible_partitions = null;
    result.per_hour_partition = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fileFormat")) {
                    result.file_format = std.meta.stringToEnum(DestinationFileFormat, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hiveCompatiblePartitions")) {
                    result.hive_compatible_partitions = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "perHourPartition")) {
                    result.per_hour_partition = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDeviceOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DeviceOptions {
    var result: DeviceOptions = undefined;
    result.public_signing_key_url = null;
    result.tenant_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "publicSigningKeyUrl")) {
                    result.public_signing_key_url = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenantId")) {
                    result.tenant_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDhcpConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DhcpConfiguration {
    var result: DhcpConfiguration = undefined;
    result.key = null;
    result.values = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "valueSet")) {
                    result.values = try deserializeDhcpConfigurationValueList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDhcpOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DhcpOptions {
    var result: DhcpOptions = undefined;
    result.dhcp_configurations = null;
    result.dhcp_options_id = null;
    result.owner_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dhcpConfigurationSet")) {
                    result.dhcp_configurations = try deserializeDhcpConfigurationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "dhcpOptionsId")) {
                    result.dhcp_options_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDirectoryServiceAuthentication(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DirectoryServiceAuthentication {
    var result: DirectoryServiceAuthentication = undefined;
    result.directory_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "directoryId")) {
                    result.directory_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreErrorItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DisableFastSnapshotRestoreErrorItem {
    var result: DisableFastSnapshotRestoreErrorItem = undefined;
    result.fast_snapshot_restore_state_errors = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fastSnapshotRestoreStateErrorSet")) {
                    result.fast_snapshot_restore_state_errors = try deserializeDisableFastSnapshotRestoreStateErrorSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreStateError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DisableFastSnapshotRestoreStateError {
    var result: DisableFastSnapshotRestoreStateError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreStateErrorItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DisableFastSnapshotRestoreStateErrorItem {
    var result: DisableFastSnapshotRestoreStateErrorItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.@"error" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeDisableFastSnapshotRestoreStateError(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDisableFastSnapshotRestoreSuccessItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DisableFastSnapshotRestoreSuccessItem {
    var result: DisableFastSnapshotRestoreSuccessItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.disabled_time = null;
    result.disabling_time = null;
    result.enabled_time = null;
    result.enabling_time = null;
    result.optimizing_time = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.snapshot_id = null;
    result.state = null;
    result.state_transition_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disabledTime")) {
                    result.disabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "disablingTime")) {
                    result.disabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enabledTime")) {
                    result.enabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enablingTime")) {
                    result.enabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "optimizingTime")) {
                    result.optimizing_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(FastSnapshotRestoreStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDiskImageDescription(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DiskImageDescription {
    var result: DiskImageDescription = undefined;
    result.checksum = null;
    result.format = null;
    result.import_manifest_url = null;
    result.size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "checksum")) {
                    result.checksum = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "format")) {
                    result.format = std.meta.stringToEnum(DiskImageFormat, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importManifestUrl")) {
                    result.import_manifest_url = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDiskImageVolumeDescription(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DiskImageVolumeDescription {
    var result: DiskImageVolumeDescription = undefined;
    result.id = null;
    result.size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDiskInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DiskInfo {
    _ = alloc;
    var result: DiskInfo = undefined;
    result.count = null;
    result.size_in_gb = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "sizeInGB")) {
                    result.size_in_gb = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(DiskType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDnsEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DnsEntry {
    var result: DnsEntry = undefined;
    result.dns_name = null;
    result.hosted_zone_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostedZoneId")) {
                    result.hosted_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeDnsOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !DnsOptions {
    var result: DnsOptions = undefined;
    result.dns_record_ip_type = null;
    result.private_dns_only_for_inbound_resolver_endpoint = null;
    result.private_dns_preference = null;
    result.private_dns_specified_domains = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsRecordIpType")) {
                    result.dns_record_ip_type = std.meta.stringToEnum(DnsRecordIpType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsOnlyForInboundResolverEndpoint")) {
                    result.private_dns_only_for_inbound_resolver_endpoint = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateDnsPreference")) {
                    result.private_dns_preference = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsSpecifiedDomainSet")) {
                    result.private_dns_specified_domains = try deserializePrivateDnsSpecifiedDomainSet(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsBlockDevice(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsBlockDevice {
    var result: EbsBlockDevice = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.delete_on_termination = null;
    result.ebs_card_index = null;
    result.encrypted = null;
    result.iops = null;
    result.kms_key_id = null;
    result.outpost_arn = null;
    result.snapshot_id = null;
    result.throughput = null;
    result.volume_initialization_rate = null;
    result.volume_size = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "AvailabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "EbsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "VolumeInitializationRate")) {
                    result.volume_initialization_rate = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = std.meta.stringToEnum(VolumeType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsBlockDeviceResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsBlockDeviceResponse {
    var result: EbsBlockDeviceResponse = undefined;
    result.delete_on_termination = null;
    result.encrypted = null;
    result.iops = null;
    result.kms_key_id = null;
    result.snapshot_id = null;
    result.throughput = null;
    result.volume_size = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = std.meta.stringToEnum(VolumeType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsCardInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsCardInfo {
    _ = alloc;
    var result: EbsCardInfo = undefined;
    result.baseline_bandwidth_in_mbps = null;
    result.baseline_iops = null;
    result.baseline_throughput_in_m_bps = null;
    result.ebs_card_index = null;
    result.maximum_bandwidth_in_mbps = null;
    result.maximum_iops = null;
    result.maximum_throughput_in_m_bps = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "baselineBandwidthInMbps")) {
                    result.baseline_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineIops")) {
                    result.baseline_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineThroughputInMBps")) {
                    result.baseline_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumBandwidthInMbps")) {
                    result.maximum_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumIops")) {
                    result.maximum_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumThroughputInMBps")) {
                    result.maximum_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsInfo {
    var result: EbsInfo = undefined;
    result.attachment_limit_type = null;
    result.ebs_cards = null;
    result.ebs_optimized_info = null;
    result.ebs_optimized_support = null;
    result.encryption_support = null;
    result.maximum_ebs_attachments = null;
    result.maximum_ebs_cards = null;
    result.nvme_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentLimitType")) {
                    result.attachment_limit_type = std.meta.stringToEnum(AttachmentLimitType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsCardSet")) {
                    result.ebs_cards = try deserializeEbsCardInfoList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ebsOptimizedInfo")) {
                    result.ebs_optimized_info = try deserializeEbsOptimizedInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ebsOptimizedSupport")) {
                    result.ebs_optimized_support = std.meta.stringToEnum(EbsOptimizedSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionSupport")) {
                    result.encryption_support = std.meta.stringToEnum(EbsEncryptionSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maximumEbsAttachments")) {
                    result.maximum_ebs_attachments = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumEbsCards")) {
                    result.maximum_ebs_cards = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "nvmeSupport")) {
                    result.nvme_support = std.meta.stringToEnum(EbsNvmeSupport, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsInstanceBlockDevice(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsInstanceBlockDevice {
    var result: EbsInstanceBlockDevice = undefined;
    result.associated_resource = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.ebs_card_index = null;
    result.operator = null;
    result.status = null;
    result.volume_id = null;
    result.volume_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResource")) {
                    result.associated_resource = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(AttachmentStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeOwnerId")) {
                    result.volume_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsOptimizedInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsOptimizedInfo {
    _ = alloc;
    var result: EbsOptimizedInfo = undefined;
    result.baseline_bandwidth_in_mbps = null;
    result.baseline_iops = null;
    result.baseline_throughput_in_m_bps = null;
    result.maximum_bandwidth_in_mbps = null;
    result.maximum_iops = null;
    result.maximum_throughput_in_m_bps = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "baselineBandwidthInMbps")) {
                    result.baseline_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineIops")) {
                    result.baseline_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineThroughputInMBps")) {
                    result.baseline_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumBandwidthInMbps")) {
                    result.maximum_bandwidth_in_mbps = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumIops")) {
                    result.maximum_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumThroughputInMBps")) {
                    result.maximum_throughput_in_m_bps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsStatusDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsStatusDetails {
    _ = alloc;
    var result: EbsStatusDetails = undefined;
    result.impaired_since = null;
    result.name = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "impairedSince")) {
                    result.impaired_since = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = std.meta.stringToEnum(StatusName, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(StatusType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEbsStatusSummary(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EbsStatusSummary {
    var result: EbsStatusSummary = undefined;
    result.details = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "details")) {
                    result.details = try deserializeEbsStatusDetailsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(SummaryStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEc2InstanceConnectEndpoint(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ec2InstanceConnectEndpoint {
    var result: Ec2InstanceConnectEndpoint = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.created_at = null;
    result.dns_name = null;
    result.fips_dns_name = null;
    result.instance_connect_endpoint_arn = null;
    result.instance_connect_endpoint_id = null;
    result.ip_address_type = null;
    result.network_interface_ids = null;
    result.owner_id = null;
    result.preserve_client_ip = null;
    result.public_dns_names = null;
    result.security_group_ids = null;
    result.state = null;
    result.state_message = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createdAt")) {
                    result.created_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fipsDnsName")) {
                    result.fips_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceConnectEndpointArn")) {
                    result.instance_connect_endpoint_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceConnectEndpointId")) {
                    result.instance_connect_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddressType")) {
                    result.ip_address_type = std.meta.stringToEnum(IpAddressType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceIdSet")) {
                    result.network_interface_ids = try deserializeNetworkInterfaceIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "preserveClientIp")) {
                    result.preserve_client_ip = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicDnsNames")) {
                    result.public_dns_names = try deserializeInstanceConnectEndpointPublicDnsNames(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeSecurityGroupIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(Ec2InstanceConnectEndpointState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEfaInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EfaInfo {
    _ = alloc;
    var result: EfaInfo = undefined;
    result.maximum_efa_interfaces = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "maximumEfaInterfaces")) {
                    result.maximum_efa_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEgressOnlyInternetGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EgressOnlyInternetGateway {
    var result: EgressOnlyInternetGateway = undefined;
    result.attachments = null;
    result.egress_only_internet_gateway_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentSet")) {
                    result.attachments = try deserializeInternetGatewayAttachmentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "egressOnlyInternetGatewayId")) {
                    result.egress_only_internet_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpuAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ElasticGpuAssociation {
    var result: ElasticGpuAssociation = undefined;
    result.elastic_gpu_association_id = null;
    result.elastic_gpu_association_state = null;
    result.elastic_gpu_association_time = null;
    result.elastic_gpu_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "elasticGpuAssociationId")) {
                    result.elastic_gpu_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuAssociationState")) {
                    result.elastic_gpu_association_state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuAssociationTime")) {
                    result.elastic_gpu_association_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuId")) {
                    result.elastic_gpu_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpuHealth(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ElasticGpuHealth {
    _ = alloc;
    var result: ElasticGpuHealth = undefined;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(ElasticGpuStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpuSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ElasticGpuSpecificationResponse {
    var result: ElasticGpuSpecificationResponse = undefined;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticGpus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ElasticGpus {
    var result: ElasticGpus = undefined;
    result.availability_zone = null;
    result.elastic_gpu_health = null;
    result.elastic_gpu_id = null;
    result.elastic_gpu_state = null;
    result.elastic_gpu_type = null;
    result.instance_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuHealth")) {
                    result.elastic_gpu_health = try deserializeElasticGpuHealth(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "elasticGpuId")) {
                    result.elastic_gpu_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuState")) {
                    result.elastic_gpu_state = std.meta.stringToEnum(ElasticGpuState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticGpuType")) {
                    result.elastic_gpu_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeElasticInferenceAcceleratorAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ElasticInferenceAcceleratorAssociation {
    var result: ElasticInferenceAcceleratorAssociation = undefined;
    result.elastic_inference_accelerator_arn = null;
    result.elastic_inference_accelerator_association_id = null;
    result.elastic_inference_accelerator_association_state = null;
    result.elastic_inference_accelerator_association_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorArn")) {
                    result.elastic_inference_accelerator_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationId")) {
                    result.elastic_inference_accelerator_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationState")) {
                    result.elastic_inference_accelerator_association_state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationTime")) {
                    result.elastic_inference_accelerator_association_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnaSrdSpecificationRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EnaSrdSpecificationRequest {
    var result: EnaSrdSpecificationRequest = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "EnaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "EnaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeEnaSrdUdpSpecificationRequest(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnaSrdUdpSpecificationRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EnaSrdUdpSpecificationRequest {
    _ = alloc;
    var result: EnaSrdUdpSpecificationRequest = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "EnaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreErrorItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EnableFastSnapshotRestoreErrorItem {
    var result: EnableFastSnapshotRestoreErrorItem = undefined;
    result.fast_snapshot_restore_state_errors = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fastSnapshotRestoreStateErrorSet")) {
                    result.fast_snapshot_restore_state_errors = try deserializeEnableFastSnapshotRestoreStateErrorSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreStateError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EnableFastSnapshotRestoreStateError {
    var result: EnableFastSnapshotRestoreStateError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreStateErrorItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EnableFastSnapshotRestoreStateErrorItem {
    var result: EnableFastSnapshotRestoreStateErrorItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.@"error" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeEnableFastSnapshotRestoreStateError(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnableFastSnapshotRestoreSuccessItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EnableFastSnapshotRestoreSuccessItem {
    var result: EnableFastSnapshotRestoreSuccessItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.disabled_time = null;
    result.disabling_time = null;
    result.enabled_time = null;
    result.enabling_time = null;
    result.optimizing_time = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.snapshot_id = null;
    result.state = null;
    result.state_transition_reason = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "disabledTime")) {
                    result.disabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "disablingTime")) {
                    result.disabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enabledTime")) {
                    result.enabled_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "enablingTime")) {
                    result.enabling_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "optimizingTime")) {
                    result.optimizing_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(FastSnapshotRestoreStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateTransitionReason")) {
                    result.state_transition_reason = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEnclaveOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EnclaveOptions {
    _ = alloc;
    var result: EnclaveOptions = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEncryptionSupport(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EncryptionSupport {
    var result: EncryptionSupport = undefined;
    result.encryption_state = null;
    result.state_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "encryptionState")) {
                    result.encryption_state = std.meta.stringToEnum(EncryptionStateValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeEventInformation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !EventInformation {
    var result: EventInformation = undefined;
    result.event_description = null;
    result.event_sub_type = null;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eventDescription")) {
                    result.event_description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventSubType")) {
                    result.event_sub_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExplanation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Explanation {
    var result: Explanation = undefined;
    result.acl = null;
    result.acl_rule = null;
    result.address = null;
    result.addresses = null;
    result.attached_to = null;
    result.availability_zone_ids = null;
    result.availability_zones = null;
    result.cidrs = null;
    result.classic_load_balancer_listener = null;
    result.component = null;
    result.component_account = null;
    result.component_region = null;
    result.customer_gateway = null;
    result.destination = null;
    result.destination_vpc = null;
    result.direction = null;
    result.elastic_load_balancer_listener = null;
    result.explanation_code = null;
    result.firewall_stateful_rule = null;
    result.firewall_stateless_rule = null;
    result.ingress_route_table = null;
    result.internet_gateway = null;
    result.load_balancer_arn = null;
    result.load_balancer_listener_port = null;
    result.load_balancer_target = null;
    result.load_balancer_target_group = null;
    result.load_balancer_target_groups = null;
    result.load_balancer_target_port = null;
    result.missing_component = null;
    result.nat_gateway = null;
    result.network_interface = null;
    result.packet_field = null;
    result.port = null;
    result.port_ranges = null;
    result.prefix_list = null;
    result.protocols = null;
    result.route_table = null;
    result.route_table_route = null;
    result.security_group = null;
    result.security_group_rule = null;
    result.security_groups = null;
    result.source_vpc = null;
    result.state = null;
    result.subnet = null;
    result.subnet_route_table = null;
    result.transit_gateway = null;
    result.transit_gateway_attachment = null;
    result.transit_gateway_route_table = null;
    result.transit_gateway_route_table_route = null;
    result.vpc = null;
    result.vpc_endpoint = null;
    result.vpc_peering_connection = null;
    result.vpn_connection = null;
    result.vpn_gateway = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acl")) {
                    result.acl = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "aclRule")) {
                    result.acl_rule = try deserializeAnalysisAclRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "address")) {
                    result.address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressSet")) {
                    result.addresses = try deserializeIpAddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "attachedTo")) {
                    result.attached_to = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "availabilityZoneIdSet")) {
                    result.availability_zone_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneSet")) {
                    result.availability_zones = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "cidrSet")) {
                    result.cidrs = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "classicLoadBalancerListener")) {
                    result.classic_load_balancer_listener = try deserializeAnalysisLoadBalancerListener(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "component")) {
                    result.component = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "componentAccount")) {
                    result.component_account = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "componentRegion")) {
                    result.component_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGateway")) {
                    result.customer_gateway = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "destinationVpc")) {
                    result.destination_vpc = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "direction")) {
                    result.direction = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "elasticLoadBalancerListener")) {
                    result.elastic_load_balancer_listener = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "explanationCode")) {
                    result.explanation_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "firewallStatefulRule")) {
                    result.firewall_stateful_rule = try deserializeFirewallStatefulRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "firewallStatelessRule")) {
                    result.firewall_stateless_rule = try deserializeFirewallStatelessRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ingressRouteTable")) {
                    result.ingress_route_table = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "internetGateway")) {
                    result.internet_gateway = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "loadBalancerArn")) {
                    result.load_balancer_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "loadBalancerListenerPort")) {
                    result.load_balancer_listener_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "loadBalancerTarget")) {
                    result.load_balancer_target = try deserializeAnalysisLoadBalancerTarget(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "loadBalancerTargetGroup")) {
                    result.load_balancer_target_group = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "loadBalancerTargetGroupSet")) {
                    result.load_balancer_target_groups = try deserializeAnalysisComponentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "loadBalancerTargetPort")) {
                    result.load_balancer_target_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "missingComponent")) {
                    result.missing_component = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGateway")) {
                    result.nat_gateway = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterface")) {
                    result.network_interface = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "packetField")) {
                    result.packet_field = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializePortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "prefixList")) {
                    result.prefix_list = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "protocolSet")) {
                    result.protocols = try deserializeStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "routeTable")) {
                    result.route_table = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "routeTableRoute")) {
                    result.route_table_route = try deserializeAnalysisRouteTableRoute(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "securityGroup")) {
                    result.security_group = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "securityGroupRule")) {
                    result.security_group_rule = try deserializeAnalysisSecurityGroupRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "securityGroupSet")) {
                    result.security_groups = try deserializeAnalysisComponentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourceVpc")) {
                    result.source_vpc = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnet")) {
                    result.subnet = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "subnetRouteTable")) {
                    result.subnet_route_table = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGateway")) {
                    result.transit_gateway = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachment")) {
                    result.transit_gateway_attachment = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTable")) {
                    result.transit_gateway_route_table = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableRoute")) {
                    result.transit_gateway_route_table_route = try deserializeTransitGatewayRouteTableRoute(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpc")) {
                    result.vpc = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpcEndpoint")) {
                    result.vpc_endpoint = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnection")) {
                    result.vpc_peering_connection = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpnConnection")) {
                    result.vpn_connection = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpnGateway")) {
                    result.vpn_gateway = try deserializeAnalysisComponent(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportImageTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ExportImageTask {
    var result: ExportImageTask = undefined;
    result.description = null;
    result.export_image_task_id = null;
    result.image_id = null;
    result.progress = null;
    result.s3_export_location = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exportImageTaskId")) {
                    result.export_image_task_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3ExportLocation")) {
                    result.s3_export_location = try deserializeExportTaskS3Location(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ExportTask {
    var result: ExportTask = undefined;
    result.description = null;
    result.export_task_id = null;
    result.export_to_s3_task = null;
    result.instance_export_details = null;
    result.state = null;
    result.status_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exportTaskId")) {
                    result.export_task_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exportToS3")) {
                    result.export_to_s3_task = try deserializeExportToS3Task(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceExport")) {
                    result.instance_export_details = try deserializeInstanceExportDetails(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(ExportTaskState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportTaskS3Location(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ExportTaskS3Location {
    var result: ExportTaskS3Location = undefined;
    result.s3_bucket = null;
    result.s3_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Prefix")) {
                    result.s3_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeExportToS3Task(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ExportToS3Task {
    var result: ExportToS3Task = undefined;
    result.container_format = null;
    result.disk_image_format = null;
    result.s3_bucket = null;
    result.s3_key = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "containerFormat")) {
                    result.container_format = std.meta.stringToEnum(ContainerFormat, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "diskImageFormat")) {
                    result.disk_image_format = std.meta.stringToEnum(DiskImageFormat, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Key")) {
                    result.s3_key = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFailedCapacityReservationFleetCancellationResult(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FailedCapacityReservationFleetCancellationResult {
    var result: FailedCapacityReservationFleetCancellationResult = undefined;
    result.cancel_capacity_reservation_fleet_error = null;
    result.capacity_reservation_fleet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cancelCapacityReservationFleetError")) {
                    result.cancel_capacity_reservation_fleet_error = try deserializeCancelCapacityReservationFleetError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "capacityReservationFleetId")) {
                    result.capacity_reservation_fleet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFailedQueuedPurchaseDeletion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FailedQueuedPurchaseDeletion {
    var result: FailedQueuedPurchaseDeletion = undefined;
    result.@"error" = null;
    result.reserved_instances_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeDeleteQueuedReservedInstancesError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFastLaunchLaunchTemplateSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FastLaunchLaunchTemplateSpecificationResponse {
    var result: FastLaunchLaunchTemplateSpecificationResponse = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFastLaunchSnapshotConfigurationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FastLaunchSnapshotConfigurationResponse {
    _ = alloc;
    var result: FastLaunchSnapshotConfigurationResponse = undefined;
    result.target_resource_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "targetResourceCount")) {
                    result.target_resource_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFederatedAuthentication(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FederatedAuthentication {
    var result: FederatedAuthentication = undefined;
    result.saml_provider_arn = null;
    result.self_service_saml_provider_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "samlProviderArn")) {
                    result.saml_provider_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "selfServiceSamlProviderArn")) {
                    result.self_service_saml_provider_arn = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFilterPortRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FilterPortRange {
    _ = alloc;
    var result: FilterPortRange = undefined;
    result.from_port = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFirewallStatefulRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FirewallStatefulRule {
    var result: FirewallStatefulRule = undefined;
    result.destination_ports = null;
    result.destinations = null;
    result.direction = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_group_arn = null;
    result.source_ports = null;
    result.sources = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationPortSet")) {
                    result.destination_ports = try deserializePortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "destinationSet")) {
                    result.destinations = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "direction")) {
                    result.direction = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortSet")) {
                    result.source_ports = try deserializePortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourceSet")) {
                    result.sources = try deserializeValueStringList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFirewallStatelessRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FirewallStatelessRule {
    var result: FirewallStatelessRule = undefined;
    result.destination_ports = null;
    result.destinations = null;
    result.priority = null;
    result.protocols = null;
    result.rule_action = null;
    result.rule_group_arn = null;
    result.source_ports = null;
    result.sources = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationPortSet")) {
                    result.destination_ports = try deserializePortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "destinationSet")) {
                    result.destinations = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "protocolSet")) {
                    result.protocols = try deserializeProtocolIntList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortSet")) {
                    result.source_ports = try deserializePortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourceSet")) {
                    result.sources = try deserializeValueStringList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetCapacityReservation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FleetCapacityReservation {
    var result: FleetCapacityReservation = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.capacity_reservation_id = null;
    result.create_date = null;
    result.ebs_optimized = null;
    result.fulfilled_capacity = null;
    result.instance_platform = null;
    result.instance_type = null;
    result.priority = null;
    result.total_instance_count = null;
    result.weight = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "fulfilledCapacity")) {
                    result.fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instancePlatform")) {
                    result.instance_platform = std.meta.stringToEnum(CapacityReservationInstancePlatform, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalInstanceCount")) {
                    result.total_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "weight")) {
                    result.weight = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetData(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FleetData {
    var result: FleetData = undefined;
    result.activity_status = null;
    result.client_token = null;
    result.context = null;
    result.create_time = null;
    result.errors = null;
    result.excess_capacity_termination_policy = null;
    result.fleet_id = null;
    result.fleet_state = null;
    result.fulfilled_capacity = null;
    result.fulfilled_on_demand_capacity = null;
    result.instances = null;
    result.launch_template_configs = null;
    result.on_demand_options = null;
    result.replace_unhealthy_instances = null;
    result.spot_options = null;
    result.tags = null;
    result.target_capacity_specification = null;
    result.terminate_instances_with_expiration = null;
    result.@"type" = null;
    result.valid_from = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "activityStatus")) {
                    result.activity_status = std.meta.stringToEnum(FleetActivityStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "context")) {
                    result.context = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "errorSet")) {
                    result.errors = try deserializeDescribeFleetsErrorSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "excessCapacityTerminationPolicy")) {
                    result.excess_capacity_termination_policy = std.meta.stringToEnum(FleetExcessCapacityTerminationPolicy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fleetId")) {
                    result.fleet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fleetState")) {
                    result.fleet_state = std.meta.stringToEnum(FleetStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fulfilledCapacity")) {
                    result.fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fulfilledOnDemandCapacity")) {
                    result.fulfilled_on_demand_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fleetInstanceSet")) {
                    result.instances = try deserializeDescribeFleetsInstancesSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "launchTemplateConfigs")) {
                    result.launch_template_configs = try deserializeFleetLaunchTemplateConfigList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "onDemandOptions")) {
                    result.on_demand_options = try deserializeOnDemandOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "replaceUnhealthyInstances")) {
                    result.replace_unhealthy_instances = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotOptions")) {
                    result.spot_options = try deserializeSpotOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "targetCapacitySpecification")) {
                    result.target_capacity_specification = try deserializeTargetCapacitySpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "terminateInstancesWithExpiration")) {
                    result.terminate_instances_with_expiration = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(FleetType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validFrom")) {
                    result.valid_from = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetLaunchTemplateConfig(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FleetLaunchTemplateConfig {
    var result: FleetLaunchTemplateConfig = undefined;
    result.launch_template_specification = null;
    result.overrides = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateSpecification")) {
                    result.launch_template_specification = try deserializeFleetLaunchTemplateSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "overrides")) {
                    result.overrides = try deserializeFleetLaunchTemplateOverridesList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetLaunchTemplateOverrides(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FleetLaunchTemplateOverrides {
    var result: FleetLaunchTemplateOverrides = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.block_device_mappings = null;
    result.image_id = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.max_price = null;
    result.placement = null;
    result.priority = null;
    result.subnet_id = null;
    result.weighted_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMappingSet")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingResponseList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxPrice")) {
                    result.max_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializePlacementResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "weightedCapacity")) {
                    result.weighted_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetLaunchTemplateSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FleetLaunchTemplateSpecification {
    var result: FleetLaunchTemplateSpecification = undefined;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetSpotCapacityRebalance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FleetSpotCapacityRebalance {
    _ = alloc;
    var result: FleetSpotCapacityRebalance = undefined;
    result.replacement_strategy = null;
    result.termination_delay = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "replacementStrategy")) {
                    result.replacement_strategy = std.meta.stringToEnum(FleetReplacementStrategy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "terminationDelay")) {
                    result.termination_delay = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFleetSpotMaintenanceStrategies(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FleetSpotMaintenanceStrategies {
    var result: FleetSpotMaintenanceStrategies = undefined;
    result.capacity_rebalance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityRebalance")) {
                    result.capacity_rebalance = try deserializeFleetSpotCapacityRebalance(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFlowLog(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FlowLog {
    var result: FlowLog = undefined;
    result.creation_time = null;
    result.deliver_cross_account_role = null;
    result.deliver_logs_error_message = null;
    result.deliver_logs_permission_arn = null;
    result.deliver_logs_status = null;
    result.destination_options = null;
    result.flow_log_id = null;
    result.flow_log_status = null;
    result.log_destination = null;
    result.log_destination_type = null;
    result.log_format = null;
    result.log_group_name = null;
    result.max_aggregation_interval = null;
    result.resource_id = null;
    result.tags = null;
    result.traffic_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deliverCrossAccountRole")) {
                    result.deliver_cross_account_role = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliverLogsErrorMessage")) {
                    result.deliver_logs_error_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliverLogsPermissionArn")) {
                    result.deliver_logs_permission_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliverLogsStatus")) {
                    result.deliver_logs_status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationOptions")) {
                    result.destination_options = try deserializeDestinationOptionsResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "flowLogId")) {
                    result.flow_log_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "flowLogStatus")) {
                    result.flow_log_status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logDestination")) {
                    result.log_destination = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logDestinationType")) {
                    result.log_destination_type = std.meta.stringToEnum(LogDestinationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logFormat")) {
                    result.log_format = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "logGroupName")) {
                    result.log_group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxAggregationInterval")) {
                    result.max_aggregation_interval = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trafficType")) {
                    result.traffic_type = std.meta.stringToEnum(TrafficType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaDeviceInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FpgaDeviceInfo {
    var result: FpgaDeviceInfo = undefined;
    result.count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeFpgaDeviceMemoryInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaDeviceMemoryInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FpgaDeviceMemoryInfo {
    _ = alloc;
    var result: FpgaDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaImage(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FpgaImage {
    var result: FpgaImage = undefined;
    result.create_time = null;
    result.data_retention_support = null;
    result.description = null;
    result.fpga_image_global_id = null;
    result.fpga_image_id = null;
    result.instance_types = null;
    result.name = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.pci_id = null;
    result.product_codes = null;
    result.public = null;
    result.shell_version = null;
    result.state = null;
    result.tags = null;
    result.update_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dataRetentionSupport")) {
                    result.data_retention_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fpgaImageGlobalId")) {
                    result.fpga_image_global_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fpgaImageId")) {
                    result.fpga_image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceTypes")) {
                    result.instance_types = try deserializeInstanceTypesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "pciId")) {
                    result.pci_id = try deserializePciId(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "public")) {
                    result.public = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "shellVersion")) {
                    result.shell_version = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try deserializeFpgaImageState(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tags")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "updateTime")) {
                    result.update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaImageAttribute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FpgaImageAttribute {
    var result: FpgaImageAttribute = undefined;
    result.description = null;
    result.fpga_image_id = null;
    result.load_permissions = null;
    result.name = null;
    result.product_codes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fpgaImageId")) {
                    result.fpga_image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "loadPermissions")) {
                    result.load_permissions = try deserializeLoadPermissionList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaImageState(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FpgaImageState {
    var result: FpgaImageState = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(FpgaImageStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeFpgaInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !FpgaInfo {
    var result: FpgaInfo = undefined;
    result.fpgas = null;
    result.total_fpga_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fpgas")) {
                    result.fpgas = try deserializeFpgaDeviceInfoList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "totalFpgaMemoryInMiB")) {
                    result.total_fpga_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGpuDeviceInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !GpuDeviceInfo {
    var result: GpuDeviceInfo = undefined;
    result.count = null;
    result.gpu_partition_size = null;
    result.logical_gpu_count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    result.workloads = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "gpuPartitionSize")) {
                    result.gpu_partition_size = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "logicalGpuCount")) {
                    result.logical_gpu_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeGpuDeviceMemoryInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "workloadSet")) {
                    result.workloads = try deserializeWorkloadsList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGpuDeviceMemoryInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !GpuDeviceMemoryInfo {
    _ = alloc;
    var result: GpuDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGpuInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !GpuInfo {
    var result: GpuInfo = undefined;
    result.gpus = null;
    result.total_gpu_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "gpus")) {
                    result.gpus = try deserializeGpuDeviceInfoList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "totalGpuMemoryInMiB")) {
                    result.total_gpu_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeGroupIdentifier(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !GroupIdentifier {
    var result: GroupIdentifier = undefined;
    result.group_id = null;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHibernationOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !HibernationOptions {
    _ = alloc;
    var result: HibernationOptions = undefined;
    result.configured = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configured")) {
                    result.configured = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHistoryRecord(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !HistoryRecord {
    var result: HistoryRecord = undefined;
    result.event_information = null;
    result.event_type = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eventInformation")) {
                    result.event_information = try deserializeEventInformation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = std.meta.stringToEnum(EventType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHistoryRecordEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !HistoryRecordEntry {
    var result: HistoryRecordEntry = undefined;
    result.event_information = null;
    result.event_type = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eventInformation")) {
                    result.event_information = try deserializeEventInformation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = std.meta.stringToEnum(FleetEventType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHost(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Host {
    var result: Host = undefined;
    result.allocation_time = null;
    result.allows_multiple_instance_types = null;
    result.asset_id = null;
    result.auto_placement = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.available_capacity = null;
    result.client_token = null;
    result.host_id = null;
    result.host_maintenance = null;
    result.host_properties = null;
    result.host_recovery = null;
    result.host_reservation_id = null;
    result.instances = null;
    result.member_of_service_linked_resource_group = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.release_time = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationTime")) {
                    result.allocation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "allowsMultipleInstanceTypes")) {
                    result.allows_multiple_instance_types = std.meta.stringToEnum(AllowsMultipleInstanceTypes, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "assetId")) {
                    result.asset_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "autoPlacement")) {
                    result.auto_placement = std.meta.stringToEnum(AutoPlacement, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableCapacity")) {
                    result.available_capacity = try deserializeAvailableCapacity(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostMaintenance")) {
                    result.host_maintenance = std.meta.stringToEnum(HostMaintenance, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostProperties")) {
                    result.host_properties = try deserializeHostProperties(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "hostRecovery")) {
                    result.host_recovery = std.meta.stringToEnum(HostRecovery, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostReservationId")) {
                    result.host_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instances")) {
                    result.instances = try deserializeHostInstanceList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "memberOfServiceLinkedResourceGroup")) {
                    result.member_of_service_linked_resource_group = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "releaseTime")) {
                    result.release_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(AllocationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !HostInstance {
    var result: HostInstance = undefined;
    result.instance_id = null;
    result.instance_type = null;
    result.owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostOffering(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !HostOffering {
    var result: HostOffering = undefined;
    result.currency_code = null;
    result.duration = null;
    result.hourly_price = null;
    result.instance_family = null;
    result.offering_id = null;
    result.payment_option = null;
    result.upfront_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = std.meta.stringToEnum(CurrencyCodeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringId")) {
                    result.offering_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "paymentOption")) {
                    result.payment_option = std.meta.stringToEnum(PaymentOption, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontPrice")) {
                    result.upfront_price = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostProperties(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !HostProperties {
    var result: HostProperties = undefined;
    result.cores = null;
    result.instance_family = null;
    result.instance_type = null;
    result.sockets = null;
    result.total_v_cpus = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cores")) {
                    result.cores = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sockets")) {
                    result.sockets = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalVCpus")) {
                    result.total_v_cpus = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeHostReservation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !HostReservation {
    var result: HostReservation = undefined;
    result.count = null;
    result.currency_code = null;
    result.duration = null;
    result.end = null;
    result.host_id_set = null;
    result.host_reservation_id = null;
    result.hourly_price = null;
    result.instance_family = null;
    result.offering_id = null;
    result.payment_option = null;
    result.start = null;
    result.state = null;
    result.tags = null;
    result.upfront_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = std.meta.stringToEnum(CurrencyCodeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "end")) {
                    result.end = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "hostIdSet")) {
                    result.host_id_set = try deserializeResponseHostIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "hostReservationId")) {
                    result.host_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringId")) {
                    result.offering_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "paymentOption")) {
                    result.payment_option = std.meta.stringToEnum(PaymentOption, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "start")) {
                    result.start = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(ReservationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "upfrontPrice")) {
                    result.upfront_price = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIKEVersionsListValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IKEVersionsListValue {
    var result: IKEVersionsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIamInstanceProfile(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IamInstanceProfile {
    var result: IamInstanceProfile = undefined;
    result.arn = null;
    result.id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIamInstanceProfileAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IamInstanceProfileAssociation {
    var result: IamInstanceProfileAssociation = undefined;
    result.association_id = null;
    result.iam_instance_profile = null;
    result.instance_id = null;
    result.state = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfile(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IamInstanceProfileAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIamInstanceProfileSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IamInstanceProfileSpecification {
    var result: IamInstanceProfileSpecification = undefined;
    result.arn = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIcmpTypeCode(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IcmpTypeCode {
    _ = alloc;
    var result: IcmpTypeCode = undefined;
    result.code = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIdFormat(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IdFormat {
    var result: IdFormat = undefined;
    result.deadline = null;
    result.resource = null;
    result.use_long_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deadline")) {
                    result.deadline = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "resource")) {
                    result.resource = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "useLongIds")) {
                    result.use_long_ids = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImage(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Image {
    var result: Image = undefined;
    result.architecture = null;
    result.block_device_mappings = null;
    result.boot_mode = null;
    result.creation_date = null;
    result.deprecation_time = null;
    result.deregistration_protection = null;
    result.description = null;
    result.ena_support = null;
    result.free_tier_eligible = null;
    result.hypervisor = null;
    result.image_allowed = null;
    result.image_id = null;
    result.image_location = null;
    result.image_owner_alias = null;
    result.image_type = null;
    result.imds_support = null;
    result.kernel_id = null;
    result.last_launched_time = null;
    result.name = null;
    result.owner_id = null;
    result.platform = null;
    result.platform_details = null;
    result.product_codes = null;
    result.public = null;
    result.ramdisk_id = null;
    result.root_device_name = null;
    result.root_device_type = null;
    result.source_image_id = null;
    result.source_image_region = null;
    result.source_instance_id = null;
    result.sriov_net_support = null;
    result.state = null;
    result.state_reason = null;
    result.tags = null;
    result.tpm_support = null;
    result.usage_operation = null;
    result.virtualization_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "architecture")) {
                    result.architecture = std.meta.stringToEnum(ArchitectureValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "bootMode")) {
                    result.boot_mode = std.meta.stringToEnum(BootModeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "creationDate")) {
                    result.creation_date = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deprecationTime")) {
                    result.deprecation_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deregistrationProtection")) {
                    result.deregistration_protection = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enaSupport")) {
                    result.ena_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "freeTierEligible")) {
                    result.free_tier_eligible = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = std.meta.stringToEnum(HypervisorType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageAllowed")) {
                    result.image_allowed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageLocation")) {
                    result.image_location = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerAlias")) {
                    result.image_owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageType")) {
                    result.image_type = std.meta.stringToEnum(ImageTypeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imdsSupport")) {
                    result.imds_support = std.meta.stringToEnum(ImdsSupportValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastLaunchedTime")) {
                    result.last_launched_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = std.meta.stringToEnum(PlatformValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platformDetails")) {
                    result.platform_details = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "isPublic")) {
                    result.public = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceName")) {
                    result.root_device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceType")) {
                    result.root_device_type = std.meta.stringToEnum(DeviceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageId")) {
                    result.source_image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageRegion")) {
                    result.source_image_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceInstanceId")) {
                    result.source_instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sriovNetSupport")) {
                    result.sriov_net_support = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageState")) {
                    result.state = std.meta.stringToEnum(ImageState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try deserializeStateReason(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tpmSupport")) {
                    result.tpm_support = std.meta.stringToEnum(TpmSupportValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageOperation")) {
                    result.usage_operation = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualizationType")) {
                    result.virtualization_type = std.meta.stringToEnum(VirtualizationType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageAncestryEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageAncestryEntry {
    var result: ImageAncestryEntry = undefined;
    result.creation_date = null;
    result.image_id = null;
    result.image_owner_alias = null;
    result.source_image_id = null;
    result.source_image_region = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationDate")) {
                    result.creation_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerAlias")) {
                    result.image_owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageId")) {
                    result.source_image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceImageRegion")) {
                    result.source_image_region = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageCriterion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageCriterion {
    var result: ImageCriterion = undefined;
    result.creation_date_condition = null;
    result.deprecation_time_condition = null;
    result.image_names = null;
    result.image_providers = null;
    result.marketplace_product_codes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationDateCondition")) {
                    result.creation_date_condition = try deserializeCreationDateCondition(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "deprecationTimeCondition")) {
                    result.deprecation_time_condition = try deserializeDeprecationTimeCondition(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "imageNameSet")) {
                    result.image_names = try deserializeImageNameList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "imageProviderSet")) {
                    result.image_providers = try deserializeImageProviderList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "marketplaceProductCodeSet")) {
                    result.marketplace_product_codes = try deserializeMarketplaceProductCodeList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageMetadata(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageMetadata {
    var result: ImageMetadata = undefined;
    result.creation_date = null;
    result.deprecation_time = null;
    result.image_allowed = null;
    result.image_id = null;
    result.image_owner_alias = null;
    result.is_public = null;
    result.name = null;
    result.owner_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationDate")) {
                    result.creation_date = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deprecationTime")) {
                    result.deprecation_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageAllowed")) {
                    result.image_allowed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerAlias")) {
                    result.image_owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPublic")) {
                    result.is_public = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageOwnerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageState")) {
                    result.state = std.meta.stringToEnum(ImageState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageRecycleBinInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageRecycleBinInfo {
    var result: ImageRecycleBinInfo = undefined;
    result.description = null;
    result.image_id = null;
    result.name = null;
    result.recycle_bin_enter_time = null;
    result.recycle_bin_exit_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recycleBinEnterTime")) {
                    result.recycle_bin_enter_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recycleBinExitTime")) {
                    result.recycle_bin_exit_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageReference(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageReference {
    var result: ImageReference = undefined;
    result.arn = null;
    result.image_id = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(ImageReferenceResourceType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageReport(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageUsageReport {
    var result: ImageUsageReport = undefined;
    result.account_ids = null;
    result.creation_time = null;
    result.expiration_time = null;
    result.image_id = null;
    result.report_id = null;
    result.resource_types = null;
    result.state = null;
    result.state_reason = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountIdSet")) {
                    result.account_ids = try deserializeUserIdList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "expirationTime")) {
                    result.expiration_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reportId")) {
                    result.report_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTypeSet")) {
                    result.resource_types = try deserializeImageUsageResourceTypeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageReportEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageUsageReportEntry {
    var result: ImageUsageReportEntry = undefined;
    result.account_id = null;
    result.image_id = null;
    result.report_creation_time = null;
    result.report_id = null;
    result.resource_type = null;
    result.usage_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reportCreationTime")) {
                    result.report_creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reportId")) {
                    result.report_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageCount")) {
                    result.usage_count = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageResourceType(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageUsageResourceType {
    var result: ImageUsageResourceType = undefined;
    result.resource_type = null;
    result.resource_type_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTypeOptionSet")) {
                    result.resource_type_options = try deserializeImageUsageResourceTypeOptionList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImageUsageResourceTypeOption(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImageUsageResourceTypeOption {
    var result: ImageUsageResourceTypeOption = undefined;
    result.option_name = null;
    result.option_values = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "optionName")) {
                    result.option_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "optionValueSet")) {
                    result.option_values = try deserializeImageUsageResourceTypeOptionValuesList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportImageLicenseConfigurationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImportImageLicenseConfigurationResponse {
    var result: ImportImageLicenseConfigurationResponse = undefined;
    result.license_configuration_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "licenseConfigurationArn")) {
                    result.license_configuration_arn = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportImageTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImportImageTask {
    var result: ImportImageTask = undefined;
    result.architecture = null;
    result.boot_mode = null;
    result.description = null;
    result.encrypted = null;
    result.hypervisor = null;
    result.image_id = null;
    result.import_task_id = null;
    result.kms_key_id = null;
    result.license_specifications = null;
    result.license_type = null;
    result.platform = null;
    result.progress = null;
    result.snapshot_details = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    result.usage_operation = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "architecture")) {
                    result.architecture = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bootMode")) {
                    result.boot_mode = std.meta.stringToEnum(BootModeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importTaskId")) {
                    result.import_task_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "licenseSpecifications")) {
                    result.license_specifications = try deserializeImportImageLicenseSpecificationListResponse(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "licenseType")) {
                    result.license_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotDetailSet")) {
                    result.snapshot_details = try deserializeSnapshotDetailList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "usageOperation")) {
                    result.usage_operation = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportInstanceTaskDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImportInstanceTaskDetails {
    var result: ImportInstanceTaskDetails = undefined;
    result.description = null;
    result.instance_id = null;
    result.platform = null;
    result.volumes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = std.meta.stringToEnum(PlatformValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumes")) {
                    result.volumes = try deserializeImportInstanceVolumeDetailSet(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportInstanceVolumeDetailItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImportInstanceVolumeDetailItem {
    var result: ImportInstanceVolumeDetailItem = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.bytes_converted = null;
    result.description = null;
    result.image = null;
    result.status = null;
    result.status_message = null;
    result.volume = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bytesConverted")) {
                    result.bytes_converted = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "image")) {
                    result.image = try deserializeDiskImageDescription(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volume")) {
                    result.volume = try deserializeDiskImageVolumeDescription(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportSnapshotTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImportSnapshotTask {
    var result: ImportSnapshotTask = undefined;
    result.description = null;
    result.import_task_id = null;
    result.snapshot_task_detail = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "importTaskId")) {
                    result.import_task_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotTaskDetail")) {
                    result.snapshot_task_detail = try deserializeSnapshotTaskDetail(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeImportVolumeTaskDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ImportVolumeTaskDetails {
    var result: ImportVolumeTaskDetails = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.bytes_converted = null;
    result.description = null;
    result.image = null;
    result.volume = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bytesConverted")) {
                    result.bytes_converted = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "image")) {
                    result.image = try deserializeDiskImageDescription(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "volume")) {
                    result.volume = try deserializeDiskImageVolumeDescription(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInferenceAcceleratorInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InferenceAcceleratorInfo {
    var result: InferenceAcceleratorInfo = undefined;
    result.accelerators = null;
    result.total_inference_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accelerators")) {
                    result.accelerators = try deserializeInferenceDeviceInfoList(reader, alloc, "member");
                } else if (std.mem.eql(u8, e.local, "totalInferenceMemoryInMiB")) {
                    result.total_inference_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInferenceDeviceInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InferenceDeviceInfo {
    var result: InferenceDeviceInfo = undefined;
    result.count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeInferenceDeviceMemoryInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInferenceDeviceMemoryInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InferenceDeviceMemoryInfo {
    _ = alloc;
    var result: InferenceDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInitializationStatusDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InitializationStatusDetails {
    _ = alloc;
    var result: InitializationStatusDetails = undefined;
    result.estimated_time_to_complete_in_seconds = null;
    result.initialization_type = null;
    result.progress = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "estimatedTimeToCompleteInSeconds")) {
                    result.estimated_time_to_complete_in_seconds = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "initializationType")) {
                    result.initialization_type = std.meta.stringToEnum(InitializationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Instance {
    var result: Instance = undefined;
    result.ami_launch_index = null;
    result.architecture = null;
    result.block_device_mappings = null;
    result.boot_mode = null;
    result.capacity_block_id = null;
    result.capacity_reservation_id = null;
    result.capacity_reservation_specification = null;
    result.client_token = null;
    result.cpu_options = null;
    result.current_instance_boot_mode = null;
    result.ebs_optimized = null;
    result.elastic_gpu_associations = null;
    result.elastic_inference_accelerator_associations = null;
    result.ena_support = null;
    result.enclave_options = null;
    result.hibernation_options = null;
    result.hypervisor = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_id = null;
    result.instance_lifecycle = null;
    result.instance_type = null;
    result.ipv_6_address = null;
    result.kernel_id = null;
    result.key_name = null;
    result.launch_time = null;
    result.licenses = null;
    result.maintenance_options = null;
    result.metadata_options = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.network_performance_options = null;
    result.operator = null;
    result.outpost_arn = null;
    result.placement = null;
    result.platform = null;
    result.platform_details = null;
    result.private_dns_name = null;
    result.private_dns_name_options = null;
    result.private_ip_address = null;
    result.product_codes = null;
    result.public_dns_name = null;
    result.public_ip_address = null;
    result.ramdisk_id = null;
    result.root_device_name = null;
    result.root_device_type = null;
    result.security_groups = null;
    result.source_dest_check = null;
    result.spot_instance_request_id = null;
    result.sriov_net_support = null;
    result.state = null;
    result.state_reason = null;
    result.state_transition_reason = null;
    result.subnet_id = null;
    result.tags = null;
    result.tpm_support = null;
    result.usage_operation = null;
    result.usage_operation_update_time = null;
    result.virtualization_type = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amiLaunchIndex")) {
                    result.ami_launch_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "architecture")) {
                    result.architecture = std.meta.stringToEnum(ArchitectureValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeInstanceBlockDeviceMappingList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "bootMode")) {
                    result.boot_mode = std.meta.stringToEnum(BootModeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationId")) {
                    result.capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationSpecification")) {
                    result.capacity_reservation_specification = try deserializeCapacityReservationSpecificationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cpuOptions")) {
                    result.cpu_options = try deserializeCpuOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "currentInstanceBootMode")) {
                    result.current_instance_boot_mode = std.meta.stringToEnum(InstanceBootModeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "elasticGpuAssociationSet")) {
                    result.elastic_gpu_associations = try deserializeElasticGpuAssociationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorAssociationSet")) {
                    result.elastic_inference_accelerator_associations = try deserializeElasticInferenceAcceleratorAssociationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "enaSupport")) {
                    result.ena_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enclaveOptions")) {
                    result.enclave_options = try deserializeEnclaveOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "hibernationOptions")) {
                    result.hibernation_options = try deserializeHibernationOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = std.meta.stringToEnum(HypervisorType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfile(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceLifecycle")) {
                    result.instance_lifecycle = std.meta.stringToEnum(InstanceLifecycleType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTime")) {
                    result.launch_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "licenseSet")) {
                    result.licenses = try deserializeLicenseList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "maintenanceOptions")) {
                    result.maintenance_options = try deserializeInstanceMaintenanceOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "metadataOptions")) {
                    result.metadata_options = try deserializeInstanceMetadataOptionsResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeMonitoring(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeInstanceNetworkInterfaceList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkPerformanceOptions")) {
                    result.network_performance_options = try deserializeInstanceNetworkPerformanceOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializePlacement(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = std.meta.stringToEnum(PlatformValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platformDetails")) {
                    result.platform_details = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameOptions")) {
                    result.private_dns_name_options = try deserializePrivateDnsNameOptionsResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productCodes")) {
                    result.product_codes = try deserializeProductCodeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.public_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddress")) {
                    result.public_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceName")) {
                    result.root_device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rootDeviceType")) {
                    result.root_device_type = std.meta.stringToEnum(DeviceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.security_groups = try deserializeGroupIdentifierList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourceDestCheck")) {
                    result.source_dest_check = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sriovNetSupport")) {
                    result.sriov_net_support = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceState")) {
                    result.state = try deserializeInstanceState(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try deserializeStateReason(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "reason")) {
                    result.state_transition_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tpmSupport")) {
                    result.tpm_support = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageOperation")) {
                    result.usage_operation = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usageOperationUpdateTime")) {
                    result.usage_operation_update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "virtualizationType")) {
                    result.virtualization_type = std.meta.stringToEnum(VirtualizationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceAttachmentEnaSrdSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceAttachmentEnaSrdSpecification {
    var result: InstanceAttachmentEnaSrdSpecification = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeInstanceAttachmentEnaSrdUdpSpecification(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceAttachmentEnaSrdUdpSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceAttachmentEnaSrdUdpSpecification {
    _ = alloc;
    var result: InstanceAttachmentEnaSrdUdpSpecification = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceBlockDeviceMapping(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceBlockDeviceMapping {
    var result: InstanceBlockDeviceMapping = undefined;
    result.device_name = null;
    result.ebs = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeEbsInstanceBlockDevice(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceCapacity(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceCapacity {
    var result: InstanceCapacity = undefined;
    result.available_capacity = null;
    result.instance_type = null;
    result.total_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availableCapacity")) {
                    result.available_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalCapacity")) {
                    result.total_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceConnectEndpointDnsNames(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceConnectEndpointDnsNames {
    var result: InstanceConnectEndpointDnsNames = undefined;
    result.dns_name = null;
    result.fips_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsName")) {
                    result.dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fipsDnsName")) {
                    result.fips_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceConnectEndpointPublicDnsNames(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceConnectEndpointPublicDnsNames {
    var result: InstanceConnectEndpointPublicDnsNames = undefined;
    result.dualstack = null;
    result.ipv_4 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dualstack")) {
                    result.dualstack = try deserializeInstanceConnectEndpointDnsNames(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ipv4")) {
                    result.ipv_4 = try deserializeInstanceConnectEndpointDnsNames(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceCount(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceCount {
    _ = alloc;
    var result: InstanceCount = undefined;
    result.instance_count = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(ListingState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceCreditSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceCreditSpecification {
    var result: InstanceCreditSpecification = undefined;
    result.cpu_credits = null;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpuCredits")) {
                    result.cpu_credits = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindow(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceEventWindow {
    var result: InstanceEventWindow = undefined;
    result.association_target = null;
    result.cron_expression = null;
    result.instance_event_window_id = null;
    result.name = null;
    result.state = null;
    result.tags = null;
    result.time_ranges = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationTarget")) {
                    result.association_target = try deserializeInstanceEventWindowAssociationTarget(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "cronExpression")) {
                    result.cron_expression = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceEventWindowId")) {
                    result.instance_event_window_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(InstanceEventWindowState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "timeRangeSet")) {
                    result.time_ranges = try deserializeInstanceEventWindowTimeRangeList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindowAssociationTarget(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceEventWindowAssociationTarget {
    var result: InstanceEventWindowAssociationTarget = undefined;
    result.dedicated_host_ids = null;
    result.instance_ids = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dedicatedHostIdSet")) {
                    result.dedicated_host_ids = try deserializeDedicatedHostIdList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "instanceIdSet")) {
                    result.instance_ids = try deserializeInstanceIdList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindowStateChange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceEventWindowStateChange {
    var result: InstanceEventWindowStateChange = undefined;
    result.instance_event_window_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceEventWindowId")) {
                    result.instance_event_window_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(InstanceEventWindowState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceEventWindowTimeRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceEventWindowTimeRange {
    _ = alloc;
    var result: InstanceEventWindowTimeRange = undefined;
    result.end_hour = null;
    result.end_week_day = null;
    result.start_hour = null;
    result.start_week_day = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endHour")) {
                    result.end_hour = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "endWeekDay")) {
                    result.end_week_day = std.meta.stringToEnum(WeekDay, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startHour")) {
                    result.start_hour = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "startWeekDay")) {
                    result.start_week_day = std.meta.stringToEnum(WeekDay, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceExportDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceExportDetails {
    var result: InstanceExportDetails = undefined;
    result.instance_id = null;
    result.target_environment = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetEnvironment")) {
                    result.target_environment = std.meta.stringToEnum(ExportEnvironment, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceFamilyCreditSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceFamilyCreditSpecification {
    var result: InstanceFamilyCreditSpecification = undefined;
    result.cpu_credits = null;
    result.instance_family = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cpuCredits")) {
                    result.cpu_credits = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = std.meta.stringToEnum(UnlimitedSupportedInstanceFamily, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceImageMetadata(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceImageMetadata {
    var result: InstanceImageMetadata = undefined;
    result.availability_zone = null;
    result.image_metadata = null;
    result.instance_id = null;
    result.instance_type = null;
    result.launch_time = null;
    result.operator = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.zone_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "imageMetadata")) {
                    result.image_metadata = try deserializeImageMetadata(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTime")) {
                    result.launch_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceOwnerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceState")) {
                    result.state = try deserializeInstanceState(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "zoneId")) {
                    result.zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceIpv4Prefix(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceIpv4Prefix {
    var result: InstanceIpv4Prefix = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv4Prefix")) {
                    result.ipv_4_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceIpv6Address(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceIpv6Address {
    var result: InstanceIpv6Address = undefined;
    result.ipv_6_address = null;
    result.is_primary_ipv_6 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPrimaryIpv6")) {
                    result.is_primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceIpv6Prefix(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceIpv6Prefix {
    var result: InstanceIpv6Prefix = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Prefix")) {
                    result.ipv_6_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMaintenanceOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceMaintenanceOptions {
    _ = alloc;
    var result: InstanceMaintenanceOptions = undefined;
    result.auto_recovery = null;
    result.reboot_migration = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoRecovery")) {
                    result.auto_recovery = std.meta.stringToEnum(InstanceAutoRecoveryState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rebootMigration")) {
                    result.reboot_migration = std.meta.stringToEnum(InstanceRebootMigrationState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMetadataDefaultsResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceMetadataDefaultsResponse {
    var result: InstanceMetadataDefaultsResponse = undefined;
    result.http_endpoint = null;
    result.http_put_response_hop_limit = null;
    result.http_tokens = null;
    result.instance_metadata_tags = null;
    result.managed_by = null;
    result.managed_exception_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "httpEndpoint")) {
                    result.http_endpoint = std.meta.stringToEnum(InstanceMetadataEndpointState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpPutResponseHopLimit")) {
                    result.http_put_response_hop_limit = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "httpTokens")) {
                    result.http_tokens = std.meta.stringToEnum(HttpTokensState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMetadataTags")) {
                    result.instance_metadata_tags = std.meta.stringToEnum(InstanceMetadataTagsState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "managedBy")) {
                    result.managed_by = std.meta.stringToEnum(ManagedBy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "managedExceptionMessage")) {
                    result.managed_exception_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMetadataOptionsResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceMetadataOptionsResponse {
    _ = alloc;
    var result: InstanceMetadataOptionsResponse = undefined;
    result.http_endpoint = null;
    result.http_protocol_ipv_6 = null;
    result.http_put_response_hop_limit = null;
    result.http_tokens = null;
    result.instance_metadata_tags = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "httpEndpoint")) {
                    result.http_endpoint = std.meta.stringToEnum(InstanceMetadataEndpointState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpProtocolIpv6")) {
                    result.http_protocol_ipv_6 = std.meta.stringToEnum(InstanceMetadataProtocolState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpPutResponseHopLimit")) {
                    result.http_put_response_hop_limit = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "httpTokens")) {
                    result.http_tokens = std.meta.stringToEnum(HttpTokensState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMetadataTags")) {
                    result.instance_metadata_tags = std.meta.stringToEnum(InstanceMetadataTagsState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(InstanceMetadataOptionsState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceMonitoring(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceMonitoring {
    var result: InstanceMonitoring = undefined;
    result.instance_id = null;
    result.monitoring = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeMonitoring(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterface(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceNetworkInterface {
    var result: InstanceNetworkInterface = undefined;
    result.association = null;
    result.attachment = null;
    result.connection_tracking_configuration = null;
    result.description = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_addresses = null;
    result.ipv_6_prefixes = null;
    result.mac_address = null;
    result.network_interface_id = null;
    result.operator = null;
    result.owner_id = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.source_dest_check = null;
    result.status = null;
    result.subnet_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeInstanceNetworkInterfaceAssociation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "attachment")) {
                    result.attachment = try deserializeInstanceNetworkInterfaceAttachment(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "connectionTrackingConfiguration")) {
                    result.connection_tracking_configuration = try deserializeConnectionTrackingSpecificationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "interfaceType")) {
                    result.interface_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeInstanceIpv4PrefixList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeInstanceIpv6AddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeInstanceIpv6PrefixList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "macAddress")) {
                    result.mac_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializeInstancePrivateIpAddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourceDestCheck")) {
                    result.source_dest_check = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(NetworkInterfaceStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterfaceAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceNetworkInterfaceAssociation {
    var result: InstanceNetworkInterfaceAssociation = undefined;
    result.carrier_ip = null;
    result.customer_owned_ip = null;
    result.ip_owner_id = null;
    result.public_dns_name = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierIp")) {
                    result.carrier_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIp")) {
                    result.customer_owned_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipOwnerId")) {
                    result.ip_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDnsName")) {
                    result.public_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterfaceAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceNetworkInterfaceAttachment {
    var result: InstanceNetworkInterfaceAttachment = undefined;
    result.attachment_id = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.network_card_index = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentId")) {
                    result.attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeInstanceAttachmentEnaSrdSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(AttachmentStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkInterfaceSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceNetworkInterfaceSpecification {
    var result: InstanceNetworkInterfaceSpecification = undefined;
    result.associate_carrier_ip_address = null;
    result.associate_public_ip_address = null;
    result.connection_tracking_specification = null;
    result.delete_on_termination = null;
    result.description = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefix_count = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_address_count = null;
    result.ipv_6_addresses = null;
    result.ipv_6_prefix_count = null;
    result.ipv_6_prefixes = null;
    result.network_card_index = null;
    result.network_interface_id = null;
    result.primary_ipv_6 = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.secondary_private_ip_address_count = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "AssociateCarrierIpAddress")) {
                    result.associate_carrier_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "associatePublicIpAddress")) {
                    result.associate_public_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ConnectionTrackingSpecification")) {
                    result.connection_tracking_specification = try deserializeConnectionTrackingSpecificationRequest(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "EnaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "EnaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeEnaSrdSpecificationRequest(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "SecurityGroupId")) {
                    result.groups = try deserializeSecurityGroupIdStringList(reader, alloc, "SecurityGroupId");
                } else if (std.mem.eql(u8, e.local, "InterfaceType")) {
                    result.interface_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "Ipv4PrefixCount")) {
                    result.ipv_4_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "Ipv4Prefix")) {
                    result.ipv_4_prefixes = try deserializeIpv4PrefixList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6AddressCount")) {
                    result.ipv_6_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeInstanceIpv6AddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "Ipv6PrefixCount")) {
                    result.ipv_6_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "Ipv6Prefix")) {
                    result.ipv_6_prefixes = try deserializeIpv6PrefixList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "NetworkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "PrimaryIpv6")) {
                    result.primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializePrivateIpAddressSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "secondaryPrivateIpAddressCount")) {
                    result.secondary_private_ip_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceNetworkPerformanceOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceNetworkPerformanceOptions {
    _ = alloc;
    var result: InstanceNetworkPerformanceOptions = undefined;
    result.bandwidth_weighting = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bandwidthWeighting")) {
                    result.bandwidth_weighting = std.meta.stringToEnum(InstanceBandwidthWeighting, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstancePrivateIpAddress(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstancePrivateIpAddress {
    var result: InstancePrivateIpAddress = undefined;
    result.association = null;
    result.primary = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeInstanceNetworkInterfaceAssociation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "primary")) {
                    result.primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceRequirements(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceRequirements {
    var result: InstanceRequirements = undefined;
    result.accelerator_count = null;
    result.accelerator_manufacturers = null;
    result.accelerator_names = null;
    result.accelerator_total_memory_mi_b = null;
    result.accelerator_types = null;
    result.allowed_instance_types = null;
    result.bare_metal = null;
    result.baseline_ebs_bandwidth_mbps = null;
    result.baseline_performance_factors = null;
    result.burstable_performance = null;
    result.cpu_manufacturers = null;
    result.excluded_instance_types = null;
    result.instance_generations = null;
    result.local_storage = null;
    result.local_storage_types = null;
    result.max_spot_price_as_percentage_of_optimal_on_demand_price = null;
    result.memory_gi_b_per_v_cpu = null;
    result.memory_mi_b = null;
    result.network_bandwidth_gbps = null;
    result.network_interface_count = null;
    result.on_demand_max_price_percentage_over_lowest_price = null;
    result.require_encryption_in_transit = null;
    result.require_hibernate_support = null;
    result.spot_max_price_percentage_over_lowest_price = null;
    result.total_local_storage_gb = null;
    result.v_cpu_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceleratorCount")) {
                    result.accelerator_count = try deserializeAcceleratorCount(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "acceleratorManufacturerSet")) {
                    result.accelerator_manufacturers = try deserializeAcceleratorManufacturerSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "acceleratorNameSet")) {
                    result.accelerator_names = try deserializeAcceleratorNameSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "acceleratorTotalMemoryMiB")) {
                    result.accelerator_total_memory_mi_b = try deserializeAcceleratorTotalMemoryMiB(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "acceleratorTypeSet")) {
                    result.accelerator_types = try deserializeAcceleratorTypeSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "allowedInstanceTypeSet")) {
                    result.allowed_instance_types = try deserializeAllowedInstanceTypeSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "bareMetal")) {
                    result.bare_metal = std.meta.stringToEnum(BareMetal, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "baselineEbsBandwidthMbps")) {
                    result.baseline_ebs_bandwidth_mbps = try deserializeBaselineEbsBandwidthMbps(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "baselinePerformanceFactors")) {
                    result.baseline_performance_factors = try deserializeBaselinePerformanceFactors(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "burstablePerformance")) {
                    result.burstable_performance = std.meta.stringToEnum(BurstablePerformance, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cpuManufacturerSet")) {
                    result.cpu_manufacturers = try deserializeCpuManufacturerSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "excludedInstanceTypeSet")) {
                    result.excluded_instance_types = try deserializeExcludedInstanceTypeSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "instanceGenerationSet")) {
                    result.instance_generations = try deserializeInstanceGenerationSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "localStorage")) {
                    result.local_storage = std.meta.stringToEnum(LocalStorage, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localStorageTypeSet")) {
                    result.local_storage_types = try deserializeLocalStorageTypeSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "maxSpotPriceAsPercentageOfOptimalOnDemandPrice")) {
                    result.max_spot_price_as_percentage_of_optimal_on_demand_price = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "memoryGiBPerVCpu")) {
                    result.memory_gi_b_per_v_cpu = try deserializeMemoryGiBPerVCpu(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "memoryMiB")) {
                    result.memory_mi_b = try deserializeMemoryMiB(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkBandwidthGbps")) {
                    result.network_bandwidth_gbps = try deserializeNetworkBandwidthGbps(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceCount")) {
                    result.network_interface_count = try deserializeNetworkInterfaceCount(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "onDemandMaxPricePercentageOverLowestPrice")) {
                    result.on_demand_max_price_percentage_over_lowest_price = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "requireEncryptionInTransit")) {
                    result.require_encryption_in_transit = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "requireHibernateSupport")) {
                    result.require_hibernate_support = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotMaxPricePercentageOverLowestPrice")) {
                    result.spot_max_price_percentage_over_lowest_price = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalLocalStorageGB")) {
                    result.total_local_storage_gb = try deserializeTotalLocalStorageGB(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vCpuCount")) {
                    result.v_cpu_count = try deserializeVCpuCountRange(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceState(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceState {
    _ = alloc;
    var result: InstanceState = undefined;
    result.code = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = std.meta.stringToEnum(InstanceStateName, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStateChange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceStateChange {
    var result: InstanceStateChange = undefined;
    result.current_state = null;
    result.instance_id = null;
    result.previous_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currentState")) {
                    result.current_state = try deserializeInstanceState(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousState")) {
                    result.previous_state = try deserializeInstanceState(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceStatus {
    var result: InstanceStatus = undefined;
    result.attached_ebs_status = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.events = null;
    result.instance_id = null;
    result.instance_state = null;
    result.instance_status = null;
    result.operator = null;
    result.outpost_arn = null;
    result.system_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachedEbsStatus")) {
                    result.attached_ebs_status = try deserializeEbsStatusSummary(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventsSet")) {
                    result.events = try deserializeInstanceStatusEventList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceState")) {
                    result.instance_state = try deserializeInstanceState(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceStatus")) {
                    result.instance_status = try deserializeInstanceStatusSummary(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "systemStatus")) {
                    result.system_status = try deserializeInstanceStatusSummary(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatusDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceStatusDetails {
    _ = alloc;
    var result: InstanceStatusDetails = undefined;
    result.impaired_since = null;
    result.name = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "impairedSince")) {
                    result.impaired_since = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = std.meta.stringToEnum(StatusName, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(StatusType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatusEvent(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceStatusEvent {
    var result: InstanceStatusEvent = undefined;
    result.code = null;
    result.description = null;
    result.instance_event_id = null;
    result.not_after = null;
    result.not_before = null;
    result.not_before_deadline = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(EventCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceEventId")) {
                    result.instance_event_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "notAfter")) {
                    result.not_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "notBefore")) {
                    result.not_before = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "notBeforeDeadline")) {
                    result.not_before_deadline = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStatusSummary(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceStatusSummary {
    var result: InstanceStatusSummary = undefined;
    result.details = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "details")) {
                    result.details = try deserializeInstanceStatusDetailsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(SummaryStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceStorageInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceStorageInfo {
    var result: InstanceStorageInfo = undefined;
    result.disks = null;
    result.encryption_support = null;
    result.nvme_support = null;
    result.total_size_in_gb = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "disks")) {
                    result.disks = try deserializeDiskInfoList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "encryptionSupport")) {
                    result.encryption_support = std.meta.stringToEnum(InstanceStorageEncryptionSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nvmeSupport")) {
                    result.nvme_support = std.meta.stringToEnum(EphemeralNvmeSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalSizeInGB")) {
                    result.total_size_in_gb = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTagNotificationAttribute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceTagNotificationAttribute {
    var result: InstanceTagNotificationAttribute = undefined;
    result.include_all_tags_of_instance = null;
    result.instance_tag_keys = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "includeAllTagsOfInstance")) {
                    result.include_all_tags_of_instance = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "instanceTagKeySet")) {
                    result.instance_tag_keys = try deserializeInstanceTagKeySet(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTopology(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceTopology {
    var result: InstanceTopology = undefined;
    result.availability_zone = null;
    result.capacity_block_id = null;
    result.group_name = null;
    result.instance_id = null;
    result.instance_type = null;
    result.network_nodes = null;
    result.zone_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityBlockId")) {
                    result.capacity_block_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkNodeSet")) {
                    result.network_nodes = try deserializeNetworkNodesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "zoneId")) {
                    result.zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTypeInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceTypeInfo {
    var result: InstanceTypeInfo = undefined;
    result.auto_recovery_supported = null;
    result.bare_metal = null;
    result.burstable_performance_supported = null;
    result.current_generation = null;
    result.dedicated_hosts_supported = null;
    result.ebs_info = null;
    result.fpga_info = null;
    result.free_tier_eligible = null;
    result.gpu_info = null;
    result.hibernation_supported = null;
    result.hypervisor = null;
    result.inference_accelerator_info = null;
    result.instance_storage_info = null;
    result.instance_storage_supported = null;
    result.instance_type = null;
    result.media_accelerator_info = null;
    result.memory_info = null;
    result.network_info = null;
    result.neuron_info = null;
    result.nitro_enclaves_support = null;
    result.nitro_tpm_info = null;
    result.nitro_tpm_support = null;
    result.phc_support = null;
    result.placement_group_info = null;
    result.processor_info = null;
    result.reboot_migration_support = null;
    result.supported_boot_modes = null;
    result.supported_root_device_types = null;
    result.supported_usage_classes = null;
    result.supported_virtualization_types = null;
    result.v_cpu_info = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoRecoverySupported")) {
                    result.auto_recovery_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "bareMetal")) {
                    result.bare_metal = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "burstablePerformanceSupported")) {
                    result.burstable_performance_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "currentGeneration")) {
                    result.current_generation = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "dedicatedHostsSupported")) {
                    result.dedicated_hosts_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsInfo")) {
                    result.ebs_info = try deserializeEbsInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "fpgaInfo")) {
                    result.fpga_info = try deserializeFpgaInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "freeTierEligible")) {
                    result.free_tier_eligible = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "gpuInfo")) {
                    result.gpu_info = try deserializeGpuInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "hibernationSupported")) {
                    result.hibernation_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hypervisor")) {
                    result.hypervisor = std.meta.stringToEnum(InstanceTypeHypervisor, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "inferenceAcceleratorInfo")) {
                    result.inference_accelerator_info = try deserializeInferenceAcceleratorInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceStorageInfo")) {
                    result.instance_storage_info = try deserializeInstanceStorageInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceStorageSupported")) {
                    result.instance_storage_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "mediaAcceleratorInfo")) {
                    result.media_accelerator_info = try deserializeMediaAcceleratorInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeMemoryInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInfo")) {
                    result.network_info = try deserializeNetworkInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "neuronInfo")) {
                    result.neuron_info = try deserializeNeuronInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "nitroEnclavesSupport")) {
                    result.nitro_enclaves_support = std.meta.stringToEnum(NitroEnclavesSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nitroTpmInfo")) {
                    result.nitro_tpm_info = try deserializeNitroTpmInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "nitroTpmSupport")) {
                    result.nitro_tpm_support = std.meta.stringToEnum(NitroTpmSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phcSupport")) {
                    result.phc_support = std.meta.stringToEnum(PhcSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "placementGroupInfo")) {
                    result.placement_group_info = try deserializePlacementGroupInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "processorInfo")) {
                    result.processor_info = try deserializeProcessorInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "rebootMigrationSupport")) {
                    result.reboot_migration_support = std.meta.stringToEnum(RebootMigrationSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "supportedBootModes")) {
                    result.supported_boot_modes = try deserializeBootModeTypeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "supportedRootDeviceTypes")) {
                    result.supported_root_device_types = try deserializeRootDeviceTypeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "supportedUsageClasses")) {
                    result.supported_usage_classes = try deserializeUsageClassTypeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "supportedVirtualizationTypes")) {
                    result.supported_virtualization_types = try deserializeVirtualizationTypeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vCpuInfo")) {
                    result.v_cpu_info = try deserializeVCpuInfo(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTypeInfoFromInstanceRequirements(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceTypeInfoFromInstanceRequirements {
    var result: InstanceTypeInfoFromInstanceRequirements = undefined;
    result.instance_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceTypeOffering(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceTypeOffering {
    var result: InstanceTypeOffering = undefined;
    result.instance_type = null;
    result.location = null;
    result.location_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "location")) {
                    result.location = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "locationType")) {
                    result.location_type = std.meta.stringToEnum(LocationType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInstanceUsage(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InstanceUsage {
    var result: InstanceUsage = undefined;
    result.account_id = null;
    result.used_instance_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usedInstanceCount")) {
                    result.used_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInternetGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InternetGateway {
    var result: InternetGateway = undefined;
    result.attachments = null;
    result.internet_gateway_id = null;
    result.owner_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentSet")) {
                    result.attachments = try deserializeInternetGatewayAttachmentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "internetGatewayId")) {
                    result.internet_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInternetGatewayAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InternetGatewayAttachment {
    var result: InternetGatewayAttachment = undefined;
    result.state = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(AttachmentStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInterruptibleCapacityAllocation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InterruptibleCapacityAllocation {
    var result: InterruptibleCapacityAllocation = undefined;
    result.instance_count = null;
    result.interruptible_capacity_reservation_id = null;
    result.interruption_type = null;
    result.status = null;
    result.target_instance_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "interruptibleCapacityReservationId")) {
                    result.interruptible_capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "interruptionType")) {
                    result.interruption_type = std.meta.stringToEnum(InterruptionType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(InterruptibleCapacityReservationAllocationStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetInstanceCount")) {
                    result.target_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeInterruptionInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !InterruptionInfo {
    var result: InterruptionInfo = undefined;
    result.interruption_type = null;
    result.source_capacity_reservation_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "interruptionType")) {
                    result.interruption_type = std.meta.stringToEnum(InterruptionType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceCapacityReservationId")) {
                    result.source_capacity_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpPermission(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpPermission {
    var result: IpPermission = undefined;
    result.from_port = null;
    result.ip_protocol = null;
    result.ip_ranges = null;
    result.ipv_6_ranges = null;
    result.prefix_list_ids = null;
    result.to_port = null;
    result.user_id_group_pairs = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipRanges")) {
                    result.ip_ranges = try deserializeIpRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Ranges")) {
                    result.ipv_6_ranges = try deserializeIpv6RangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "prefixListIds")) {
                    result.prefix_list_ids = try deserializePrefixListIdList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groups")) {
                    result.user_id_group_pairs = try deserializeUserIdGroupPairList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpRange {
    var result: IpRange = undefined;
    result.cidr_ip = null;
    result.description = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIp")) {
                    result.cidr_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpam(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipam {
    var result: Ipam = undefined;
    result.default_resource_discovery_association_id = null;
    result.default_resource_discovery_id = null;
    result.description = null;
    result.enable_private_gua = null;
    result.ipam_arn = null;
    result.ipam_id = null;
    result.ipam_region = null;
    result.metered_account = null;
    result.operating_regions = null;
    result.owner_id = null;
    result.private_default_scope_id = null;
    result.public_default_scope_id = null;
    result.resource_discovery_association_count = null;
    result.scope_count = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.tier = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "defaultResourceDiscoveryAssociationId")) {
                    result.default_resource_discovery_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultResourceDiscoveryId")) {
                    result.default_resource_discovery_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enablePrivateGua")) {
                    result.enable_private_gua = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "meteredAccount")) {
                    result.metered_account = std.meta.stringToEnum(IpamMeteredAccount, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operatingRegionSet")) {
                    result.operating_regions = try deserializeIpamOperatingRegionSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDefaultScopeId")) {
                    result.private_default_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDefaultScopeId")) {
                    result.public_default_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceDiscoveryAssociationCount")) {
                    result.resource_discovery_association_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "scopeCount")) {
                    result.scope_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tier")) {
                    result.tier = std.meta.stringToEnum(IpamTier, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamAddressHistoryRecord(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamAddressHistoryRecord {
    var result: IpamAddressHistoryRecord = undefined;
    result.resource_cidr = null;
    result.resource_compliance_status = null;
    result.resource_id = null;
    result.resource_name = null;
    result.resource_overlap_status = null;
    result.resource_owner_id = null;
    result.resource_region = null;
    result.resource_type = null;
    result.sampled_end_time = null;
    result.sampled_start_time = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceCidr")) {
                    result.resource_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceComplianceStatus")) {
                    result.resource_compliance_status = std.meta.stringToEnum(IpamComplianceStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceName")) {
                    result.resource_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOverlapStatus")) {
                    result.resource_overlap_status = std.meta.stringToEnum(IpamOverlapStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(IpamAddressHistoryResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sampledEndTime")) {
                    result.sampled_end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "sampledStartTime")) {
                    result.sampled_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveredAccount(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamDiscoveredAccount {
    var result: IpamDiscoveredAccount = undefined;
    result.account_id = null;
    result.discovery_region = null;
    result.failure_reason = null;
    result.last_attempted_discovery_time = null;
    result.last_successful_discovery_time = null;
    result.organizational_unit_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accountId")) {
                    result.account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "discoveryRegion")) {
                    result.discovery_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try deserializeIpamDiscoveryFailureReason(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "lastAttemptedDiscoveryTime")) {
                    result.last_attempted_discovery_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lastSuccessfulDiscoveryTime")) {
                    result.last_successful_discovery_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "organizationalUnitId")) {
                    result.organizational_unit_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveredPublicAddress(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamDiscoveredPublicAddress {
    var result: IpamDiscoveredPublicAddress = undefined;
    result.address = null;
    result.address_allocation_id = null;
    result.address_owner_id = null;
    result.address_region = null;
    result.address_type = null;
    result.association_status = null;
    result.instance_id = null;
    result.ipam_resource_discovery_id = null;
    result.network_border_group = null;
    result.network_interface_description = null;
    result.network_interface_id = null;
    result.public_ipv_4_pool_id = null;
    result.sample_time = null;
    result.security_groups = null;
    result.service = null;
    result.service_resource = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "address")) {
                    result.address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressAllocationId")) {
                    result.address_allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressOwnerId")) {
                    result.address_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressRegion")) {
                    result.address_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "addressType")) {
                    result.address_type = std.meta.stringToEnum(IpamPublicAddressType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationStatus")) {
                    result.association_status = std.meta.stringToEnum(IpamPublicAddressAssociationStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceDescription")) {
                    result.network_interface_description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv4PoolId")) {
                    result.public_ipv_4_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sampleTime")) {
                    result.sample_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "securityGroupSet")) {
                    result.security_groups = try deserializeIpamPublicAddressSecurityGroupList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "service")) {
                    result.service = std.meta.stringToEnum(IpamPublicAddressAwsService, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceResource")) {
                    result.service_resource = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tags")) {
                    result.tags = try deserializeIpamPublicAddressTags(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveredResourceCidr(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamDiscoveredResourceCidr {
    var result: IpamDiscoveredResourceCidr = undefined;
    result.availability_zone_id = null;
    result.ipam_resource_discovery_id = null;
    result.ip_source = null;
    result.ip_usage = null;
    result.network_interface_attachment_status = null;
    result.resource_cidr = null;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_region = null;
    result.resource_tags = null;
    result.resource_type = null;
    result.sample_time = null;
    result.subnet_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipSource")) {
                    result.ip_source = std.meta.stringToEnum(IpamResourceCidrIpSource, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipUsage")) {
                    result.ip_usage = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInterfaceAttachmentStatus")) {
                    result.network_interface_attachment_status = std.meta.stringToEnum(IpamNetworkInterfaceAttachmentStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceCidr")) {
                    result.resource_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTagSet")) {
                    result.resource_tags = try deserializeIpamResourceTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(IpamResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sampleTime")) {
                    result.sample_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamDiscoveryFailureReason(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamDiscoveryFailureReason {
    var result: IpamDiscoveryFailureReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(IpamDiscoveryFailureCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamExternalResourceVerificationToken(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamExternalResourceVerificationToken {
    var result: IpamExternalResourceVerificationToken = undefined;
    result.ipam_arn = null;
    result.ipam_external_resource_verification_token_arn = null;
    result.ipam_external_resource_verification_token_id = null;
    result.ipam_id = null;
    result.ipam_region = null;
    result.not_after = null;
    result.state = null;
    result.status = null;
    result.tags = null;
    result.token_name = null;
    result.token_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamExternalResourceVerificationTokenArn")) {
                    result.ipam_external_resource_verification_token_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamExternalResourceVerificationTokenId")) {
                    result.ipam_external_resource_verification_token_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "notAfter")) {
                    result.not_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamExternalResourceVerificationTokenState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(TokenState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tokenName")) {
                    result.token_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenValue")) {
                    result.token_value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamOperatingRegion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamOperatingRegion {
    var result: IpamOperatingRegion = undefined;
    result.region_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamOrganizationalUnitExclusion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamOrganizationalUnitExclusion {
    var result: IpamOrganizationalUnitExclusion = undefined;
    result.organizations_entity_path = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "organizationsEntityPath")) {
                    result.organizations_entity_path = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicy(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPolicy {
    var result: IpamPolicy = undefined;
    result.ipam_id = null;
    result.ipam_policy_arn = null;
    result.ipam_policy_id = null;
    result.ipam_policy_region = null;
    result.owner_id = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPolicyArn")) {
                    result.ipam_policy_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPolicyId")) {
                    result.ipam_policy_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPolicyRegion")) {
                    result.ipam_policy_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamPolicyState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicyAllocationRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPolicyAllocationRule {
    var result: IpamPolicyAllocationRule = undefined;
    result.source_ipam_pool_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sourceIpamPoolId")) {
                    result.source_ipam_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicyDocument(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPolicyDocument {
    var result: IpamPolicyDocument = undefined;
    result.allocation_rules = null;
    result.ipam_policy_id = null;
    result.locale = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationRuleSet")) {
                    result.allocation_rules = try deserializeIpamPolicyAllocationRuleList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipamPolicyId")) {
                    result.ipam_policy_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "locale")) {
                    result.locale = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(IpamPolicyResourceType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPolicyOrganizationTarget(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPolicyOrganizationTarget {
    var result: IpamPolicyOrganizationTarget = undefined;
    result.organization_target_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "organizationTargetId")) {
                    result.organization_target_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPool(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPool {
    var result: IpamPool = undefined;
    result.address_family = null;
    result.allocation_default_netmask_length = null;
    result.allocation_max_netmask_length = null;
    result.allocation_min_netmask_length = null;
    result.allocation_resource_tags = null;
    result.auto_import = null;
    result.aws_service = null;
    result.description = null;
    result.ipam_arn = null;
    result.ipam_pool_arn = null;
    result.ipam_pool_id = null;
    result.ipam_region = null;
    result.ipam_scope_arn = null;
    result.ipam_scope_type = null;
    result.locale = null;
    result.owner_id = null;
    result.pool_depth = null;
    result.public_ip_source = null;
    result.publicly_advertisable = null;
    result.source_ipam_pool_id = null;
    result.source_resource = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressFamily")) {
                    result.address_family = std.meta.stringToEnum(AddressFamily, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "allocationDefaultNetmaskLength")) {
                    result.allocation_default_netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "allocationMaxNetmaskLength")) {
                    result.allocation_max_netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "allocationMinNetmaskLength")) {
                    result.allocation_min_netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "allocationResourceTagSet")) {
                    result.allocation_resource_tags = try deserializeIpamResourceTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "autoImport")) {
                    result.auto_import = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "awsService")) {
                    result.aws_service = std.meta.stringToEnum(IpamPoolAwsService, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolArn")) {
                    result.ipam_pool_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolId")) {
                    result.ipam_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeArn")) {
                    result.ipam_scope_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeType")) {
                    result.ipam_scope_type = std.meta.stringToEnum(IpamScopeType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "locale")) {
                    result.locale = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolDepth")) {
                    result.pool_depth = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "publicIpSource")) {
                    result.public_ip_source = std.meta.stringToEnum(IpamPoolPublicIpSource, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publiclyAdvertisable")) {
                    result.publicly_advertisable = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "sourceIpamPoolId")) {
                    result.source_ipam_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceResource")) {
                    result.source_resource = try deserializeIpamPoolSourceResource(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamPoolState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolAllocation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPoolAllocation {
    var result: IpamPoolAllocation = undefined;
    result.cidr = null;
    result.description = null;
    result.ipam_pool_allocation_id = null;
    result.resource_id = null;
    result.resource_owner = null;
    result.resource_region = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolAllocationId")) {
                    result.ipam_pool_allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(IpamPoolAllocationResourceType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolCidr(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPoolCidr {
    var result: IpamPoolCidr = undefined;
    result.cidr = null;
    result.failure_reason = null;
    result.ipam_pool_cidr_id = null;
    result.netmask_length = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try deserializeIpamPoolCidrFailureReason(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ipamPoolCidrId")) {
                    result.ipam_pool_cidr_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "netmaskLength")) {
                    result.netmask_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamPoolCidrState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolCidrFailureReason(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPoolCidrFailureReason {
    var result: IpamPoolCidrFailureReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(IpamPoolCidrFailureCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPoolSourceResource(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPoolSourceResource {
    var result: IpamPoolSourceResource = undefined;
    result.resource_id = null;
    result.resource_owner = null;
    result.resource_region = null;
    result.resource_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(IpamPoolSourceResourceType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolver(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPrefixListResolver {
    var result: IpamPrefixListResolver = undefined;
    result.address_family = null;
    result.description = null;
    result.ipam_arn = null;
    result.ipam_prefix_list_resolver_arn = null;
    result.ipam_prefix_list_resolver_id = null;
    result.ipam_region = null;
    result.last_version_creation_status = null;
    result.last_version_creation_status_message = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressFamily")) {
                    result.address_family = std.meta.stringToEnum(AddressFamily, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverArn")) {
                    result.ipam_prefix_list_resolver_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverId")) {
                    result.ipam_prefix_list_resolver_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastVersionCreationStatus")) {
                    result.last_version_creation_status = std.meta.stringToEnum(IpamPrefixListResolverVersionCreationStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastVersionCreationStatusMessage")) {
                    result.last_version_creation_status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamPrefixListResolverState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPrefixListResolverRule {
    var result: IpamPrefixListResolverRule = undefined;
    result.conditions = null;
    result.ipam_scope_id = null;
    result.resource_type = null;
    result.rule_type = null;
    result.static_cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "conditionSet")) {
                    result.conditions = try deserializeIpamPrefixListResolverRuleConditionSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipamScopeId")) {
                    result.ipam_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(IpamResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleType")) {
                    result.rule_type = std.meta.stringToEnum(IpamPrefixListResolverRuleType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staticCidr")) {
                    result.static_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverRuleCondition(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPrefixListResolverRuleCondition {
    var result: IpamPrefixListResolverRuleCondition = undefined;
    result.cidr = null;
    result.ipam_pool_id = null;
    result.operation = null;
    result.resource_id = null;
    result.resource_owner = null;
    result.resource_region = null;
    result.resource_tag = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolId")) {
                    result.ipam_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operation")) {
                    result.operation = std.meta.stringToEnum(IpamPrefixListResolverRuleConditionOperation, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTag")) {
                    result.resource_tag = try deserializeIpamResourceTag(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverTarget(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPrefixListResolverTarget {
    var result: IpamPrefixListResolverTarget = undefined;
    result.desired_version = null;
    result.ipam_prefix_list_resolver_id = null;
    result.ipam_prefix_list_resolver_target_arn = null;
    result.ipam_prefix_list_resolver_target_id = null;
    result.last_synced_version = null;
    result.owner_id = null;
    result.prefix_list_id = null;
    result.prefix_list_region = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.track_latest_version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "desiredVersion")) {
                    result.desired_version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverId")) {
                    result.ipam_prefix_list_resolver_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverTargetArn")) {
                    result.ipam_prefix_list_resolver_target_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverTargetId")) {
                    result.ipam_prefix_list_resolver_target_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastSyncedVersion")) {
                    result.last_synced_version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListRegion")) {
                    result.prefix_list_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamPrefixListResolverTargetState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trackLatestVersion")) {
                    result.track_latest_version = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverVersion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPrefixListResolverVersion {
    _ = alloc;
    var result: IpamPrefixListResolverVersion = undefined;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "version")) {
                    result.version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPrefixListResolverVersionEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPrefixListResolverVersionEntry {
    var result: IpamPrefixListResolverVersionEntry = undefined;
    result.cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPublicAddressSecurityGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPublicAddressSecurityGroup {
    var result: IpamPublicAddressSecurityGroup = undefined;
    result.group_id = null;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPublicAddressTag(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPublicAddressTag {
    var result: IpamPublicAddressTag = undefined;
    result.key = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamPublicAddressTags(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamPublicAddressTags {
    var result: IpamPublicAddressTags = undefined;
    result.eip_tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eipTagSet")) {
                    result.eip_tags = try deserializeIpamPublicAddressTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceCidr(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamResourceCidr {
    var result: IpamResourceCidr = undefined;
    result.availability_zone_id = null;
    result.compliance_status = null;
    result.ipam_id = null;
    result.ipam_pool_id = null;
    result.ipam_scope_id = null;
    result.ip_usage = null;
    result.management_state = null;
    result.overlap_status = null;
    result.resource_cidr = null;
    result.resource_id = null;
    result.resource_name = null;
    result.resource_owner_id = null;
    result.resource_region = null;
    result.resource_tags = null;
    result.resource_type = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "complianceStatus")) {
                    result.compliance_status = std.meta.stringToEnum(IpamComplianceStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPoolId")) {
                    result.ipam_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeId")) {
                    result.ipam_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipUsage")) {
                    result.ip_usage = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "managementState")) {
                    result.management_state = std.meta.stringToEnum(IpamManagementState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "overlapStatus")) {
                    result.overlap_status = std.meta.stringToEnum(IpamOverlapStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceCidr")) {
                    result.resource_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceName")) {
                    result.resource_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceRegion")) {
                    result.resource_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceTagSet")) {
                    result.resource_tags = try deserializeIpamResourceTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(IpamResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceDiscovery(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamResourceDiscovery {
    var result: IpamResourceDiscovery = undefined;
    result.description = null;
    result.ipam_resource_discovery_arn = null;
    result.ipam_resource_discovery_id = null;
    result.ipam_resource_discovery_region = null;
    result.is_default = null;
    result.operating_regions = null;
    result.organizational_unit_exclusions = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryArn")) {
                    result.ipam_resource_discovery_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryRegion")) {
                    result.ipam_resource_discovery_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "operatingRegionSet")) {
                    result.operating_regions = try deserializeIpamOperatingRegionSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "organizationalUnitExclusionSet")) {
                    result.organizational_unit_exclusions = try deserializeIpamOrganizationalUnitExclusionSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamResourceDiscoveryState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceDiscoveryAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamResourceDiscoveryAssociation {
    var result: IpamResourceDiscoveryAssociation = undefined;
    result.ipam_arn = null;
    result.ipam_id = null;
    result.ipam_region = null;
    result.ipam_resource_discovery_association_arn = null;
    result.ipam_resource_discovery_association_id = null;
    result.ipam_resource_discovery_id = null;
    result.is_default = null;
    result.owner_id = null;
    result.resource_discovery_status = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamId")) {
                    result.ipam_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryAssociationArn")) {
                    result.ipam_resource_discovery_association_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryAssociationId")) {
                    result.ipam_resource_discovery_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamResourceDiscoveryId")) {
                    result.ipam_resource_discovery_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceDiscoveryStatus")) {
                    result.resource_discovery_status = std.meta.stringToEnum(IpamAssociatedResourceDiscoveryStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamResourceDiscoveryAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamResourceTag(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamResourceTag {
    var result: IpamResourceTag = undefined;
    result.key = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamScope(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamScope {
    var result: IpamScope = undefined;
    result.description = null;
    result.external_authority_configuration = null;
    result.ipam_arn = null;
    result.ipam_region = null;
    result.ipam_scope_arn = null;
    result.ipam_scope_id = null;
    result.ipam_scope_type = null;
    result.is_default = null;
    result.owner_id = null;
    result.pool_count = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "externalAuthorityConfiguration")) {
                    result.external_authority_configuration = try deserializeIpamScopeExternalAuthorityConfiguration(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ipamArn")) {
                    result.ipam_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamRegion")) {
                    result.ipam_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeArn")) {
                    result.ipam_scope_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeId")) {
                    result.ipam_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamScopeType")) {
                    result.ipam_scope_type = std.meta.stringToEnum(IpamScopeType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolCount")) {
                    result.pool_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(IpamScopeState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpamScopeExternalAuthorityConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !IpamScopeExternalAuthorityConfiguration {
    var result: IpamScopeExternalAuthorityConfiguration = undefined;
    result.external_resource_identifier = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "externalResourceIdentifier")) {
                    result.external_resource_identifier = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(IpamScopeExternalAuthorityType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv4PrefixSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv4PrefixSpecification {
    var result: Ipv4PrefixSpecification = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv4Prefix")) {
                    result.ipv_4_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv4PrefixSpecificationRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv4PrefixSpecificationRequest {
    var result: Ipv4PrefixSpecificationRequest = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "Ipv4Prefix")) {
                    result.ipv_4_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv4PrefixSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv4PrefixSpecificationResponse {
    var result: Ipv4PrefixSpecificationResponse = undefined;
    result.ipv_4_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv4Prefix")) {
                    result.ipv_4_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6CidrAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv6CidrAssociation {
    var result: Ipv6CidrAssociation = undefined;
    result.associated_resource = null;
    result.ipv_6_cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResource")) {
                    result.associated_resource = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6Cidr")) {
                    result.ipv_6_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6CidrBlock(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv6CidrBlock {
    var result: Ipv6CidrBlock = undefined;
    result.ipv_6_cidr_block = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6Pool(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv6Pool {
    var result: Ipv6Pool = undefined;
    result.description = null;
    result.pool_cidr_blocks = null;
    result.pool_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolCidrBlockSet")) {
                    result.pool_cidr_blocks = try deserializePoolCidrBlocksSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "poolId")) {
                    result.pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6PrefixSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv6PrefixSpecification {
    var result: Ipv6PrefixSpecification = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Prefix")) {
                    result.ipv_6_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6PrefixSpecificationRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv6PrefixSpecificationRequest {
    var result: Ipv6PrefixSpecificationRequest = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "Ipv6Prefix")) {
                    result.ipv_6_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6PrefixSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv6PrefixSpecificationResponse {
    var result: Ipv6PrefixSpecificationResponse = undefined;
    result.ipv_6_prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Prefix")) {
                    result.ipv_6_prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeIpv6Range(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Ipv6Range {
    var result: Ipv6Range = undefined;
    result.cidr_ipv_6 = null;
    result.description = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIpv6")) {
                    result.cidr_ipv_6 = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeKeyPairInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !KeyPairInfo {
    var result: KeyPairInfo = undefined;
    result.create_time = null;
    result.key_fingerprint = null;
    result.key_name = null;
    result.key_pair_id = null;
    result.key_type = null;
    result.public_key = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "keyFingerprint")) {
                    result.key_fingerprint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyPairId")) {
                    result.key_pair_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyType")) {
                    result.key_type = std.meta.stringToEnum(KeyType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicKey")) {
                    result.public_key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLastError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LastError {
    var result: LastError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchPermission(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchPermission {
    var result: LaunchPermission = undefined;
    result.group = null;
    result.organizational_unit_arn = null;
    result.organization_arn = null;
    result.user_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "group")) {
                    result.group = std.meta.stringToEnum(PermissionGroup, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "organizationalUnitArn")) {
                    result.organizational_unit_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "organizationArn")) {
                    result.organization_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchSpecification {
    var result: LaunchSpecification = undefined;
    result.addressing_type = null;
    result.block_device_mappings = null;
    result.ebs_optimized = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_type = null;
    result.kernel_id = null;
    result.key_name = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.placement = null;
    result.ramdisk_id = null;
    result.security_groups = null;
    result.subnet_id = null;
    result.user_data = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressingType")) {
                    result.addressing_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfileSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeRunInstancesMonitoringEnabled(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeInstanceNetworkInterfaceSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializeSpotPlacement(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.security_groups = try deserializeGroupIdentifierList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userData")) {
                    result.user_data = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplate(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplate {
    var result: LaunchTemplate = undefined;
    result.created_by = null;
    result.create_time = null;
    result.default_version_number = null;
    result.latest_version_number = null;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.operator = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdBy")) {
                    result.created_by = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultVersionNumber")) {
                    result.default_version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "latestVersionNumber")) {
                    result.latest_version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateAndOverridesResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateAndOverridesResponse {
    var result: LaunchTemplateAndOverridesResponse = undefined;
    result.launch_template_specification = null;
    result.overrides = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateSpecification")) {
                    result.launch_template_specification = try deserializeFleetLaunchTemplateSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "overrides")) {
                    result.overrides = try deserializeFleetLaunchTemplateOverrides(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateBlockDeviceMapping(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateBlockDeviceMapping {
    var result: LaunchTemplateBlockDeviceMapping = undefined;
    result.device_name = null;
    result.ebs = null;
    result.no_device = null;
    result.virtual_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebs")) {
                    result.ebs = try deserializeLaunchTemplateEbsBlockDevice(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "noDevice")) {
                    result.no_device = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualName")) {
                    result.virtual_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateCapacityReservationSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateCapacityReservationSpecificationResponse {
    var result: LaunchTemplateCapacityReservationSpecificationResponse = undefined;
    result.capacity_reservation_preference = null;
    result.capacity_reservation_target = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityReservationPreference")) {
                    result.capacity_reservation_preference = std.meta.stringToEnum(CapacityReservationPreference, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationTarget")) {
                    result.capacity_reservation_target = try deserializeCapacityReservationTargetResponse(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateConfig(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateConfig {
    var result: LaunchTemplateConfig = undefined;
    result.launch_template_specification = null;
    result.overrides = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "launchTemplateSpecification")) {
                    result.launch_template_specification = try deserializeFleetLaunchTemplateSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "overrides")) {
                    result.overrides = try deserializeLaunchTemplateOverridesList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateCpuOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateCpuOptions {
    _ = alloc;
    var result: LaunchTemplateCpuOptions = undefined;
    result.amd_sev_snp = null;
    result.core_count = null;
    result.threads_per_core = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amdSevSnp")) {
                    result.amd_sev_snp = std.meta.stringToEnum(AmdSevSnpSpecification, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreCount")) {
                    result.core_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "threadsPerCore")) {
                    result.threads_per_core = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEbsBlockDevice(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateEbsBlockDevice {
    var result: LaunchTemplateEbsBlockDevice = undefined;
    result.delete_on_termination = null;
    result.ebs_card_index = null;
    result.encrypted = null;
    result.iops = null;
    result.kms_key_id = null;
    result.snapshot_id = null;
    result.throughput = null;
    result.volume_initialization_rate = null;
    result.volume_size = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeInitializationRate")) {
                    result.volume_initialization_rate = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = std.meta.stringToEnum(VolumeType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateElasticInferenceAcceleratorResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateElasticInferenceAcceleratorResponse {
    var result: LaunchTemplateElasticInferenceAcceleratorResponse = undefined;
    result.count = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEnaSrdSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateEnaSrdSpecification {
    var result: LaunchTemplateEnaSrdSpecification = undefined;
    result.ena_srd_enabled = null;
    result.ena_srd_udp_specification = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdEnabled")) {
                    result.ena_srd_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdUdpSpecification")) {
                    result.ena_srd_udp_specification = try deserializeLaunchTemplateEnaSrdUdpSpecification(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEnaSrdUdpSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateEnaSrdUdpSpecification {
    _ = alloc;
    var result: LaunchTemplateEnaSrdUdpSpecification = undefined;
    result.ena_srd_udp_enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enaSrdUdpEnabled")) {
                    result.ena_srd_udp_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateEnclaveOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateEnclaveOptions {
    _ = alloc;
    var result: LaunchTemplateEnclaveOptions = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateHibernationOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateHibernationOptions {
    _ = alloc;
    var result: LaunchTemplateHibernationOptions = undefined;
    result.configured = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configured")) {
                    result.configured = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateIamInstanceProfileSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateIamInstanceProfileSpecification {
    var result: LaunchTemplateIamInstanceProfileSpecification = undefined;
    result.arn = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceMaintenanceOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateInstanceMaintenanceOptions {
    _ = alloc;
    var result: LaunchTemplateInstanceMaintenanceOptions = undefined;
    result.auto_recovery = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoRecovery")) {
                    result.auto_recovery = std.meta.stringToEnum(LaunchTemplateAutoRecoveryState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceMarketOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateInstanceMarketOptions {
    var result: LaunchTemplateInstanceMarketOptions = undefined;
    result.market_type = null;
    result.spot_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "marketType")) {
                    result.market_type = std.meta.stringToEnum(MarketType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotOptions")) {
                    result.spot_options = try deserializeLaunchTemplateSpotMarketOptions(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceMetadataOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateInstanceMetadataOptions {
    _ = alloc;
    var result: LaunchTemplateInstanceMetadataOptions = undefined;
    result.http_endpoint = null;
    result.http_protocol_ipv_6 = null;
    result.http_put_response_hop_limit = null;
    result.http_tokens = null;
    result.instance_metadata_tags = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "httpEndpoint")) {
                    result.http_endpoint = std.meta.stringToEnum(LaunchTemplateInstanceMetadataEndpointState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpProtocolIpv6")) {
                    result.http_protocol_ipv_6 = std.meta.stringToEnum(LaunchTemplateInstanceMetadataProtocolIpv6, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "httpPutResponseHopLimit")) {
                    result.http_put_response_hop_limit = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "httpTokens")) {
                    result.http_tokens = std.meta.stringToEnum(LaunchTemplateHttpTokensState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMetadataTags")) {
                    result.instance_metadata_tags = std.meta.stringToEnum(LaunchTemplateInstanceMetadataTagsState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(LaunchTemplateInstanceMetadataOptionsState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateInstanceNetworkInterfaceSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateInstanceNetworkInterfaceSpecification {
    var result: LaunchTemplateInstanceNetworkInterfaceSpecification = undefined;
    result.associate_carrier_ip_address = null;
    result.associate_public_ip_address = null;
    result.connection_tracking_specification = null;
    result.delete_on_termination = null;
    result.description = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefix_count = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_address_count = null;
    result.ipv_6_addresses = null;
    result.ipv_6_prefix_count = null;
    result.ipv_6_prefixes = null;
    result.network_card_index = null;
    result.network_interface_id = null;
    result.primary_ipv_6 = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.secondary_private_ip_address_count = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associateCarrierIpAddress")) {
                    result.associate_carrier_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "associatePublicIpAddress")) {
                    result.associate_public_ip_address = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "connectionTrackingSpecification")) {
                    result.connection_tracking_specification = try deserializeConnectionTrackingSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeLaunchTemplateEnaSrdSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdStringList(reader, alloc, "groupId");
                } else if (std.mem.eql(u8, e.local, "interfaceType")) {
                    result.interface_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixCount")) {
                    result.ipv_4_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeIpv4PrefixListResponse(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6AddressCount")) {
                    result.ipv_6_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeInstanceIpv6AddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixCount")) {
                    result.ipv_6_prefix_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeIpv6PrefixListResponse(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "primaryIpv6")) {
                    result.primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializePrivateIpAddressSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "secondaryPrivateIpAddressCount")) {
                    result.secondary_private_ip_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateLicenseConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateLicenseConfiguration {
    var result: LaunchTemplateLicenseConfiguration = undefined;
    result.license_configuration_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "licenseConfigurationArn")) {
                    result.license_configuration_arn = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateNetworkPerformanceOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateNetworkPerformanceOptions {
    _ = alloc;
    var result: LaunchTemplateNetworkPerformanceOptions = undefined;
    result.bandwidth_weighting = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bandwidthWeighting")) {
                    result.bandwidth_weighting = std.meta.stringToEnum(InstanceBandwidthWeighting, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateOverrides(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateOverrides {
    var result: LaunchTemplateOverrides = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.priority = null;
    result.spot_price = null;
    result.subnet_id = null;
    result.weighted_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "priority")) {
                    result.priority = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "weightedCapacity")) {
                    result.weighted_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplatePlacement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplatePlacement {
    var result: LaunchTemplatePlacement = undefined;
    result.affinity = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.group_id = null;
    result.group_name = null;
    result.host_id = null;
    result.host_resource_group_arn = null;
    result.partition_number = null;
    result.spread_domain = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "affinity")) {
                    result.affinity = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostResourceGroupArn")) {
                    result.host_resource_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "partitionNumber")) {
                    result.partition_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spreadDomain")) {
                    result.spread_domain = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(Tenancy, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplatePrivateDnsNameOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplatePrivateDnsNameOptions {
    _ = alloc;
    var result: LaunchTemplatePrivateDnsNameOptions = undefined;
    result.enable_resource_name_dns_aaaa_record = null;
    result.enable_resource_name_dns_a_record = null;
    result.hostname_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableResourceNameDnsAAAARecord")) {
                    result.enable_resource_name_dns_aaaa_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableResourceNameDnsARecord")) {
                    result.enable_resource_name_dns_a_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hostnameType")) {
                    result.hostname_type = std.meta.stringToEnum(HostnameType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateSpotMarketOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateSpotMarketOptions {
    var result: LaunchTemplateSpotMarketOptions = undefined;
    result.block_duration_minutes = null;
    result.instance_interruption_behavior = null;
    result.max_price = null;
    result.spot_instance_type = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blockDurationMinutes")) {
                    result.block_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = std.meta.stringToEnum(InstanceInterruptionBehavior, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxPrice")) {
                    result.max_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotInstanceType")) {
                    result.spot_instance_type = std.meta.stringToEnum(SpotInstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateTagSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateTagSpecification {
    var result: LaunchTemplateTagSpecification = undefined;
    result.resource_type = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(ResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplateVersion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplateVersion {
    var result: LaunchTemplateVersion = undefined;
    result.created_by = null;
    result.create_time = null;
    result.default_version = null;
    result.launch_template_data = null;
    result.launch_template_id = null;
    result.launch_template_name = null;
    result.operator = null;
    result.version_description = null;
    result.version_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdBy")) {
                    result.created_by = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultVersion")) {
                    result.default_version = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "launchTemplateData")) {
                    result.launch_template_data = try deserializeResponseLaunchTemplateData(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "launchTemplateId")) {
                    result.launch_template_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchTemplateName")) {
                    result.launch_template_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "versionDescription")) {
                    result.version_description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "versionNumber")) {
                    result.version_number = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLaunchTemplatesMonitoring(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LaunchTemplatesMonitoring {
    _ = alloc;
    var result: LaunchTemplatesMonitoring = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLicenseConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LicenseConfiguration {
    var result: LicenseConfiguration = undefined;
    result.license_configuration_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "licenseConfigurationArn")) {
                    result.license_configuration_arn = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLoadBalancersConfig(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LoadBalancersConfig {
    var result: LoadBalancersConfig = undefined;
    result.classic_load_balancers_config = null;
    result.target_groups_config = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLoadBalancersConfig")) {
                    result.classic_load_balancers_config = try deserializeClassicLoadBalancersConfig(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "targetGroupsConfig")) {
                    result.target_groups_config = try deserializeTargetGroupsConfig(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLoadPermission(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LoadPermission {
    var result: LoadPermission = undefined;
    result.group = null;
    result.user_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "group")) {
                    result.group = std.meta.stringToEnum(PermissionGroup, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LocalGateway {
    var result: LocalGateway = undefined;
    result.local_gateway_id = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LocalGatewayRoute {
    var result: LocalGatewayRoute = undefined;
    result.coip_pool_id = null;
    result.destination_cidr_block = null;
    result.destination_prefix_list_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.network_interface_id = null;
    result.owner_id = null;
    result.state = null;
    result.subnet_id = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coipPoolId")) {
                    result.coip_pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListId")) {
                    result.destination_prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(LocalGatewayRouteState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(LocalGatewayRouteType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRouteTable(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LocalGatewayRouteTable {
    var result: LocalGatewayRouteTable = undefined;
    result.local_gateway_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.mode = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.state = null;
    result.state_reason = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "mode")) {
                    result.mode = std.meta.stringToEnum(LocalGatewayRouteTableMode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try deserializeStateReason(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRouteTableVirtualInterfaceGroupAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LocalGatewayRouteTableVirtualInterfaceGroupAssociation {
    var result: LocalGatewayRouteTableVirtualInterfaceGroupAssociation = undefined;
    result.local_gateway_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.local_gateway_route_table_virtual_interface_group_association_id = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableVirtualInterfaceGroupAssociationId")) {
                    result.local_gateway_route_table_virtual_interface_group_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayRouteTableVpcAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LocalGatewayRouteTableVpcAssociation {
    var result: LocalGatewayRouteTableVpcAssociation = undefined;
    result.local_gateway_id = null;
    result.local_gateway_route_table_arn = null;
    result.local_gateway_route_table_id = null;
    result.local_gateway_route_table_vpc_association_id = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableArn")) {
                    result.local_gateway_route_table_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableId")) {
                    result.local_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayRouteTableVpcAssociationId")) {
                    result.local_gateway_route_table_vpc_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayVirtualInterface(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LocalGatewayVirtualInterface {
    var result: LocalGatewayVirtualInterface = undefined;
    result.configuration_state = null;
    result.local_address = null;
    result.local_bgp_asn = null;
    result.local_gateway_id = null;
    result.local_gateway_virtual_interface_arn = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.local_gateway_virtual_interface_id = null;
    result.outpost_lag_id = null;
    result.owner_id = null;
    result.peer_address = null;
    result.peer_bgp_asn = null;
    result.peer_bgp_asn_extended = null;
    result.tags = null;
    result.vlan = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configurationState")) {
                    result.configuration_state = std.meta.stringToEnum(LocalGatewayVirtualInterfaceConfigurationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localAddress")) {
                    result.local_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localBgpAsn")) {
                    result.local_bgp_asn = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceArn")) {
                    result.local_gateway_virtual_interface_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceId")) {
                    result.local_gateway_virtual_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostLagId")) {
                    result.outpost_lag_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerBgpAsn")) {
                    result.peer_bgp_asn = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "peerBgpAsnExtended")) {
                    result.peer_bgp_asn_extended = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vlan")) {
                    result.vlan = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLocalGatewayVirtualInterfaceGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LocalGatewayVirtualInterfaceGroup {
    var result: LocalGatewayVirtualInterfaceGroup = undefined;
    result.configuration_state = null;
    result.local_bgp_asn = null;
    result.local_bgp_asn_extended = null;
    result.local_gateway_id = null;
    result.local_gateway_virtual_interface_group_arn = null;
    result.local_gateway_virtual_interface_group_id = null;
    result.local_gateway_virtual_interface_ids = null;
    result.owner_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configurationState")) {
                    result.configuration_state = std.meta.stringToEnum(LocalGatewayVirtualInterfaceGroupConfigurationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localBgpAsn")) {
                    result.local_bgp_asn = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "localBgpAsnExtended")) {
                    result.local_bgp_asn_extended = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupArn")) {
                    result.local_gateway_virtual_interface_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceGroupId")) {
                    result.local_gateway_virtual_interface_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceIdSet")) {
                    result.local_gateway_virtual_interface_ids = try deserializeLocalGatewayVirtualInterfaceIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeLockedSnapshotsInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !LockedSnapshotsInfo {
    var result: LockedSnapshotsInfo = undefined;
    result.cool_off_period = null;
    result.cool_off_period_expires_on = null;
    result.lock_created_on = null;
    result.lock_duration = null;
    result.lock_duration_start_time = null;
    result.lock_expires_on = null;
    result.lock_state = null;
    result.owner_id = null;
    result.snapshot_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coolOffPeriod")) {
                    result.cool_off_period = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "coolOffPeriodExpiresOn")) {
                    result.cool_off_period_expires_on = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockCreatedOn")) {
                    result.lock_created_on = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockDuration")) {
                    result.lock_duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "lockDurationStartTime")) {
                    result.lock_duration_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockExpiresOn")) {
                    result.lock_expires_on = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lockState")) {
                    result.lock_state = std.meta.stringToEnum(LockState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMacHost(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MacHost {
    var result: MacHost = undefined;
    result.host_id = null;
    result.mac_os_latest_supported_versions = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "macOSLatestSupportedVersionSet")) {
                    result.mac_os_latest_supported_versions = try deserializeMacOSVersionStringList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMacModificationTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MacModificationTask {
    var result: MacModificationTask = undefined;
    result.instance_id = null;
    result.mac_modification_task_id = null;
    result.mac_system_integrity_protection_config = null;
    result.start_time = null;
    result.tags = null;
    result.task_state = null;
    result.task_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "macModificationTaskId")) {
                    result.mac_modification_task_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "macSystemIntegrityProtectionConfig")) {
                    result.mac_system_integrity_protection_config = try deserializeMacSystemIntegrityProtectionConfiguration(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "taskState")) {
                    result.task_state = std.meta.stringToEnum(MacModificationTaskState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "taskType")) {
                    result.task_type = std.meta.stringToEnum(MacModificationTaskType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMacSystemIntegrityProtectionConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MacSystemIntegrityProtectionConfiguration {
    _ = alloc;
    var result: MacSystemIntegrityProtectionConfiguration = undefined;
    result.apple_internal = null;
    result.base_system = null;
    result.debugging_restrictions = null;
    result.d_trace_restrictions = null;
    result.filesystem_protections = null;
    result.kext_signing = null;
    result.nvram_protections = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "appleInternal")) {
                    result.apple_internal = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "baseSystem")) {
                    result.base_system = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "debuggingRestrictions")) {
                    result.debugging_restrictions = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dTraceRestrictions")) {
                    result.d_trace_restrictions = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "filesystemProtections")) {
                    result.filesystem_protections = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kextSigning")) {
                    result.kext_signing = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nvramProtections")) {
                    result.nvram_protections = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(MacSystemIntegrityProtectionSettingStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMaintenanceDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MaintenanceDetails {
    var result: MaintenanceDetails = undefined;
    result.last_maintenance_applied = null;
    result.maintenance_auto_applied_after = null;
    result.pending_maintenance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "lastMaintenanceApplied")) {
                    result.last_maintenance_applied = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "maintenanceAutoAppliedAfter")) {
                    result.maintenance_auto_applied_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "pendingMaintenance")) {
                    result.pending_maintenance = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeManagedPrefixList(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ManagedPrefixList {
    var result: ManagedPrefixList = undefined;
    result.address_family = null;
    result.ipam_prefix_list_resolver_sync_enabled = null;
    result.ipam_prefix_list_resolver_target_id = null;
    result.max_entries = null;
    result.owner_id = null;
    result.prefix_list_arn = null;
    result.prefix_list_id = null;
    result.prefix_list_name = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressFamily")) {
                    result.address_family = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverSyncEnabled")) {
                    result.ipam_prefix_list_resolver_sync_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ipamPrefixListResolverTargetId")) {
                    result.ipam_prefix_list_resolver_target_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxEntries")) {
                    result.max_entries = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListArn")) {
                    result.prefix_list_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListName")) {
                    result.prefix_list_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(PrefixListState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMediaAcceleratorInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MediaAcceleratorInfo {
    var result: MediaAcceleratorInfo = undefined;
    result.accelerators = null;
    result.total_media_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accelerators")) {
                    result.accelerators = try deserializeMediaDeviceInfoList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "totalMediaMemoryInMiB")) {
                    result.total_media_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMediaDeviceInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MediaDeviceInfo {
    var result: MediaDeviceInfo = undefined;
    result.count = null;
    result.manufacturer = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeMediaDeviceMemoryInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMediaDeviceMemoryInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MediaDeviceMemoryInfo {
    _ = alloc;
    var result: MediaDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMemoryGiBPerVCpu(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MemoryGiBPerVCpu {
    _ = alloc;
    var result: MemoryGiBPerVCpu = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMemoryInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MemoryInfo {
    _ = alloc;
    var result: MemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMemoryMiB(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MemoryMiB {
    _ = alloc;
    var result: MemoryMiB = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMetricDataResult(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MetricDataResult {
    var result: MetricDataResult = undefined;
    result.dimension = null;
    result.metric_values = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dimension")) {
                    result.dimension = try deserializeCapacityManagerDimension(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "metricValueSet")) {
                    result.metric_values = try deserializeMetricValueSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMetricPoint(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MetricPoint {
    var result: MetricPoint = undefined;
    result.end_date = null;
    result.start_date = null;
    result.status = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMetricValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MetricValue {
    _ = alloc;
    var result: MetricValue = undefined;
    result.metric = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "metric")) {
                    result.metric = std.meta.stringToEnum(Metric, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMonitoring(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Monitoring {
    _ = alloc;
    var result: Monitoring = undefined;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(MonitoringState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeMovingAddressStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !MovingAddressStatus {
    var result: MovingAddressStatus = undefined;
    result.move_status = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "moveStatus")) {
                    result.move_status = std.meta.stringToEnum(MoveStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNatGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NatGateway {
    var result: NatGateway = undefined;
    result.attached_appliances = null;
    result.auto_provision_zones = null;
    result.auto_scaling_ips = null;
    result.availability_mode = null;
    result.connectivity_type = null;
    result.create_time = null;
    result.delete_time = null;
    result.failure_code = null;
    result.failure_message = null;
    result.nat_gateway_addresses = null;
    result.nat_gateway_id = null;
    result.provisioned_bandwidth = null;
    result.route_table_id = null;
    result.state = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachedApplianceSet")) {
                    result.attached_appliances = try deserializeNatGatewayAttachedApplianceList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "autoProvisionZones")) {
                    result.auto_provision_zones = std.meta.stringToEnum(AutoProvisionZonesState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "autoScalingIps")) {
                    result.auto_scaling_ips = std.meta.stringToEnum(AutoScalingIpsState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityMode")) {
                    result.availability_mode = std.meta.stringToEnum(AvailabilityMode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectivityType")) {
                    result.connectivity_type = std.meta.stringToEnum(ConnectivityType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteTime")) {
                    result.delete_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "failureCode")) {
                    result.failure_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureMessage")) {
                    result.failure_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGatewayAddressSet")) {
                    result.nat_gateway_addresses = try deserializeNatGatewayAddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "natGatewayId")) {
                    result.nat_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisionedBandwidth")) {
                    result.provisioned_bandwidth = try deserializeProvisionedBandwidth(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(NatGatewayState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNatGatewayAddress(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NatGatewayAddress {
    var result: NatGatewayAddress = undefined;
    result.allocation_id = null;
    result.association_id = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.failure_message = null;
    result.is_primary = null;
    result.network_interface_id = null;
    result.private_ip = null;
    result.public_ip = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureMessage")) {
                    result.failure_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPrimary")) {
                    result.is_primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIp")) {
                    result.private_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(NatGatewayAddressStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNatGatewayAttachedAppliance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NatGatewayAttachedAppliance {
    var result: NatGatewayAttachedAppliance = undefined;
    result.appliance_arn = null;
    result.attachment_state = null;
    result.failure_code = null;
    result.failure_message = null;
    result.modification_state = null;
    result.@"type" = null;
    result.vpc_endpoint_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "applianceArn")) {
                    result.appliance_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachmentState")) {
                    result.attachment_state = std.meta.stringToEnum(NatGatewayApplianceState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureCode")) {
                    result.failure_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureMessage")) {
                    result.failure_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "modificationState")) {
                    result.modification_state = std.meta.stringToEnum(NatGatewayApplianceModifyState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(NatGatewayApplianceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNativeApplicationOidcOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NativeApplicationOidcOptions {
    var result: NativeApplicationOidcOptions = undefined;
    result.authorization_endpoint = null;
    result.client_id = null;
    result.issuer = null;
    result.public_signing_key_endpoint = null;
    result.scope = null;
    result.token_endpoint = null;
    result.user_info_endpoint = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "authorizationEndpoint")) {
                    result.authorization_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientId")) {
                    result.client_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "issuer")) {
                    result.issuer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicSigningKeyEndpoint")) {
                    result.public_signing_key_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenEndpoint")) {
                    result.token_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userInfoEndpoint")) {
                    result.user_info_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkAcl(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkAcl {
    var result: NetworkAcl = undefined;
    result.associations = null;
    result.entries = null;
    result.is_default = null;
    result.network_acl_id = null;
    result.owner_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationSet")) {
                    result.associations = try deserializeNetworkAclAssociationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "entrySet")) {
                    result.entries = try deserializeNetworkAclEntryList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "default")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "networkAclId")) {
                    result.network_acl_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkAclAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkAclAssociation {
    var result: NetworkAclAssociation = undefined;
    result.network_acl_association_id = null;
    result.network_acl_id = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "networkAclAssociationId")) {
                    result.network_acl_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkAclId")) {
                    result.network_acl_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkAclEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkAclEntry {
    var result: NetworkAclEntry = undefined;
    result.cidr_block = null;
    result.egress = null;
    result.icmp_type_code = null;
    result.ipv_6_cidr_block = null;
    result.port_range = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_number = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egress")) {
                    result.egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "icmpTypeCode")) {
                    result.icmp_type_code = try deserializeIcmpTypeCode(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "portRange")) {
                    result.port_range = try deserializePortRange(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = std.meta.stringToEnum(RuleAction, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleNumber")) {
                    result.rule_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkBandwidthGbps(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkBandwidthGbps {
    _ = alloc;
    var result: NetworkBandwidthGbps = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkCardInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkCardInfo {
    var result: NetworkCardInfo = undefined;
    result.additional_flexible_network_interfaces = null;
    result.baseline_bandwidth_in_gbps = null;
    result.default_ena_queue_count_per_interface = null;
    result.maximum_ena_queue_count = null;
    result.maximum_ena_queue_count_per_interface = null;
    result.maximum_network_interfaces = null;
    result.network_card_index = null;
    result.network_performance = null;
    result.peak_bandwidth_in_gbps = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "additionalFlexibleNetworkInterfaces")) {
                    result.additional_flexible_network_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "baselineBandwidthInGbps")) {
                    result.baseline_bandwidth_in_gbps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultEnaQueueCountPerInterface")) {
                    result.default_ena_queue_count_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumEnaQueueCount")) {
                    result.maximum_ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumEnaQueueCountPerInterface")) {
                    result.maximum_ena_queue_count_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumNetworkInterfaces")) {
                    result.maximum_network_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkPerformance")) {
                    result.network_performance = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peakBandwidthInGbps")) {
                    result.peak_bandwidth_in_gbps = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInfo {
    var result: NetworkInfo = undefined;
    result.bandwidth_weightings = null;
    result.default_network_card_index = null;
    result.efa_info = null;
    result.efa_supported = null;
    result.ena_srd_supported = null;
    result.ena_support = null;
    result.encryption_in_transit_supported = null;
    result.flexible_ena_queues_support = null;
    result.ipv_4_addresses_per_interface = null;
    result.ipv_6_addresses_per_interface = null;
    result.ipv_6_supported = null;
    result.maximum_network_cards = null;
    result.maximum_network_interfaces = null;
    result.network_cards = null;
    result.network_performance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bandwidthWeightings")) {
                    result.bandwidth_weightings = try deserializeBandwidthWeightingTypeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "defaultNetworkCardIndex")) {
                    result.default_network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "efaInfo")) {
                    result.efa_info = try deserializeEfaInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "efaSupported")) {
                    result.efa_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSrdSupported")) {
                    result.ena_srd_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enaSupport")) {
                    result.ena_support = std.meta.stringToEnum(EnaSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionInTransitSupported")) {
                    result.encryption_in_transit_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "flexibleEnaQueuesSupport")) {
                    result.flexible_ena_queues_support = std.meta.stringToEnum(FlexibleEnaQueuesSupport, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4AddressesPerInterface")) {
                    result.ipv_4_addresses_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesPerInterface")) {
                    result.ipv_6_addresses_per_interface = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6Supported")) {
                    result.ipv_6_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "maximumNetworkCards")) {
                    result.maximum_network_cards = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maximumNetworkInterfaces")) {
                    result.maximum_network_interfaces = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkCards")) {
                    result.network_cards = try deserializeNetworkCardInfoList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkPerformance")) {
                    result.network_performance = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAccessScope(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInsightsAccessScope {
    var result: NetworkInsightsAccessScope = undefined;
    result.created_date = null;
    result.network_insights_access_scope_arn = null;
    result.network_insights_access_scope_id = null;
    result.tags = null;
    result.updated_date = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdDate")) {
                    result.created_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeArn")) {
                    result.network_insights_access_scope_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "updatedDate")) {
                    result.updated_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAccessScopeAnalysis(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInsightsAccessScopeAnalysis {
    var result: NetworkInsightsAccessScopeAnalysis = undefined;
    result.analyzed_eni_count = null;
    result.end_date = null;
    result.findings_found = null;
    result.network_insights_access_scope_analysis_arn = null;
    result.network_insights_access_scope_analysis_id = null;
    result.network_insights_access_scope_id = null;
    result.start_date = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    result.warning_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "analyzedEniCount")) {
                    result.analyzed_eni_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "endDate")) {
                    result.end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "findingsFound")) {
                    result.findings_found = std.meta.stringToEnum(FindingsFound, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeAnalysisArn")) {
                    result.network_insights_access_scope_analysis_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeAnalysisId")) {
                    result.network_insights_access_scope_analysis_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(AnalysisStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "warningMessage")) {
                    result.warning_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAccessScopeContent(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInsightsAccessScopeContent {
    var result: NetworkInsightsAccessScopeContent = undefined;
    result.exclude_paths = null;
    result.match_paths = null;
    result.network_insights_access_scope_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "excludePathSet")) {
                    result.exclude_paths = try deserializeAccessScopePathList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "matchPathSet")) {
                    result.match_paths = try deserializeAccessScopePathList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkInsightsAccessScopeId")) {
                    result.network_insights_access_scope_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsAnalysis(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInsightsAnalysis {
    var result: NetworkInsightsAnalysis = undefined;
    result.additional_accounts = null;
    result.alternate_path_hints = null;
    result.explanations = null;
    result.filter_in_arns = null;
    result.filter_out_arns = null;
    result.forward_path_components = null;
    result.network_insights_analysis_arn = null;
    result.network_insights_analysis_id = null;
    result.network_insights_path_id = null;
    result.network_path_found = null;
    result.return_path_components = null;
    result.start_date = null;
    result.status = null;
    result.status_message = null;
    result.suggested_accounts = null;
    result.tags = null;
    result.warning_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "additionalAccountSet")) {
                    result.additional_accounts = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "alternatePathHintSet")) {
                    result.alternate_path_hints = try deserializeAlternatePathHintList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "explanationSet")) {
                    result.explanations = try deserializeExplanationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "filterInArnSet")) {
                    result.filter_in_arns = try deserializeArnList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "filterOutArnSet")) {
                    result.filter_out_arns = try deserializeArnList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "forwardPathComponentSet")) {
                    result.forward_path_components = try deserializePathComponentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkInsightsAnalysisArn")) {
                    result.network_insights_analysis_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsAnalysisId")) {
                    result.network_insights_analysis_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsPathId")) {
                    result.network_insights_path_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkPathFound")) {
                    result.network_path_found = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "returnPathComponentSet")) {
                    result.return_path_components = try deserializePathComponentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "startDate")) {
                    result.start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(AnalysisStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "suggestedAccountSet")) {
                    result.suggested_accounts = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "warningMessage")) {
                    result.warning_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInsightsPath(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInsightsPath {
    var result: NetworkInsightsPath = undefined;
    result.created_date = null;
    result.destination = null;
    result.destination_arn = null;
    result.destination_ip = null;
    result.destination_port = null;
    result.filter_at_destination = null;
    result.filter_at_source = null;
    result.network_insights_path_arn = null;
    result.network_insights_path_id = null;
    result.protocol = null;
    result.source = null;
    result.source_arn = null;
    result.source_ip = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "createdDate")) {
                    result.created_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationArn")) {
                    result.destination_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationIp")) {
                    result.destination_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPort")) {
                    result.destination_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "filterAtDestination")) {
                    result.filter_at_destination = try deserializePathFilter(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "filterAtSource")) {
                    result.filter_at_source = try deserializePathFilter(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInsightsPathArn")) {
                    result.network_insights_path_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInsightsPathId")) {
                    result.network_insights_path_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.meta.stringToEnum(Protocol, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceArn")) {
                    result.source_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceIp")) {
                    result.source_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterface(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterface {
    var result: NetworkInterface = undefined;
    result.associated_subnets = null;
    result.association = null;
    result.attachment = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.connection_tracking_configuration = null;
    result.deny_all_igw_traffic = null;
    result.description = null;
    result.groups = null;
    result.interface_type = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_address = null;
    result.ipv_6_addresses = null;
    result.ipv_6_native = null;
    result.ipv_6_prefixes = null;
    result.mac_address = null;
    result.network_interface_id = null;
    result.operator = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    result.private_ip_addresses = null;
    result.public_dns_name = null;
    result.public_ip_dns_name_options = null;
    result.requester_id = null;
    result.requester_managed = null;
    result.source_dest_check = null;
    result.status = null;
    result.subnet_id = null;
    result.tag_set = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedSubnetSet")) {
                    result.associated_subnets = try deserializeAssociatedSubnetList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeNetworkInterfaceAssociation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "attachment")) {
                    result.attachment = try deserializeNetworkInterfaceAttachment(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "connectionTrackingConfiguration")) {
                    result.connection_tracking_configuration = try deserializeConnectionTrackingConfiguration(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "denyAllIgwTraffic")) {
                    result.deny_all_igw_traffic = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "interfaceType")) {
                    result.interface_type = std.meta.stringToEnum(NetworkInterfaceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeIpv4PrefixesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6AddressesSet")) {
                    result.ipv_6_addresses = try deserializeNetworkInterfaceIpv6AddressesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Native")) {
                    result.ipv_6_native = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeIpv6PrefixesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "macAddress")) {
                    result.mac_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddressesSet")) {
                    result.private_ip_addresses = try deserializeNetworkInterfacePrivateIpAddressList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "publicDnsName")) {
                    result.public_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpDnsNameOptions")) {
                    result.public_ip_dns_name_options = try deserializePublicIpDnsNameOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "requesterId")) {
                    result.requester_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "requesterManaged")) {
                    result.requester_managed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "sourceDestCheck")) {
                    result.source_dest_check = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(NetworkInterfaceStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tag_set = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterfaceAssociation {
    var result: NetworkInterfaceAssociation = undefined;
    result.allocation_id = null;
    result.association_id = null;
    result.carrier_ip = null;
    result.customer_owned_ip = null;
    result.ip_owner_id = null;
    result.public_dns_name = null;
    result.public_ip = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationId")) {
                    result.allocation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "carrierIp")) {
                    result.carrier_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIp")) {
                    result.customer_owned_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipOwnerId")) {
                    result.ip_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDnsName")) {
                    result.public_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIp")) {
                    result.public_ip = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterfaceAttachment {
    var result: NetworkInterfaceAttachment = undefined;
    result.attachment_id = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.device_index = null;
    result.ena_queue_count = null;
    result.ena_srd_specification = null;
    result.instance_id = null;
    result.instance_owner_id = null;
    result.network_card_index = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentId")) {
                    result.attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "deviceIndex")) {
                    result.device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaQueueCount")) {
                    result.ena_queue_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enaSrdSpecification")) {
                    result.ena_srd_specification = try deserializeAttachmentEnaSrdSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceOwnerId")) {
                    result.instance_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkCardIndex")) {
                    result.network_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(AttachmentStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceCount(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterfaceCount {
    _ = alloc;
    var result: NetworkInterfaceCount = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfaceIpv6Address(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterfaceIpv6Address {
    var result: NetworkInterfaceIpv6Address = undefined;
    result.ipv_6_address = null;
    result.is_primary_ipv_6 = null;
    result.public_ipv_6_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipv6Address")) {
                    result.ipv_6_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isPrimaryIpv6")) {
                    result.is_primary_ipv_6 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicIpv6DnsName")) {
                    result.public_ipv_6_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfacePermission(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterfacePermission {
    var result: NetworkInterfacePermission = undefined;
    result.aws_account_id = null;
    result.aws_service = null;
    result.network_interface_id = null;
    result.network_interface_permission_id = null;
    result.permission = null;
    result.permission_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "awsAccountId")) {
                    result.aws_account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsService")) {
                    result.aws_service = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfacePermissionId")) {
                    result.network_interface_permission_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "permission")) {
                    result.permission = std.meta.stringToEnum(InterfacePermissionType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "permissionState")) {
                    result.permission_state = try deserializeNetworkInterfacePermissionState(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfacePermissionState(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterfacePermissionState {
    var result: NetworkInterfacePermissionState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(NetworkInterfacePermissionStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNetworkInterfacePrivateIpAddress(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NetworkInterfacePrivateIpAddress {
    var result: NetworkInterfacePrivateIpAddress = undefined;
    result.association = null;
    result.primary = null;
    result.private_dns_name = null;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeNetworkInterfaceAssociation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "primary")) {
                    result.primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronDeviceCoreInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NeuronDeviceCoreInfo {
    _ = alloc;
    var result: NeuronDeviceCoreInfo = undefined;
    result.count = null;
    result.version = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "version")) {
                    result.version = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronDeviceInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NeuronDeviceInfo {
    var result: NeuronDeviceInfo = undefined;
    result.core_info = null;
    result.count = null;
    result.memory_info = null;
    result.name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coreInfo")) {
                    result.core_info = try deserializeNeuronDeviceCoreInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "memoryInfo")) {
                    result.memory_info = try deserializeNeuronDeviceMemoryInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronDeviceMemoryInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NeuronDeviceMemoryInfo {
    _ = alloc;
    var result: NeuronDeviceMemoryInfo = undefined;
    result.size_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "sizeInMiB")) {
                    result.size_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNeuronInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NeuronInfo {
    var result: NeuronInfo = undefined;
    result.neuron_devices = null;
    result.total_neuron_device_memory_in_mi_b = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "neuronDevices")) {
                    result.neuron_devices = try deserializeNeuronDeviceInfoList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "totalNeuronDeviceMemoryInMiB")) {
                    result.total_neuron_device_memory_in_mi_b = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeNitroTpmInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !NitroTpmInfo {
    var result: NitroTpmInfo = undefined;
    result.supported_versions = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "supportedVersions")) {
                    result.supported_versions = try deserializeNitroTpmSupportedVersionsList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOidcOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !OidcOptions {
    var result: OidcOptions = undefined;
    result.authorization_endpoint = null;
    result.client_id = null;
    result.client_secret = null;
    result.issuer = null;
    result.scope = null;
    result.token_endpoint = null;
    result.user_info_endpoint = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "authorizationEndpoint")) {
                    result.authorization_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientId")) {
                    result.client_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientSecret")) {
                    result.client_secret = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "issuer")) {
                    result.issuer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenEndpoint")) {
                    result.token_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userInfoEndpoint")) {
                    result.user_info_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOnDemandOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !OnDemandOptions {
    var result: OnDemandOptions = undefined;
    result.allocation_strategy = null;
    result.capacity_reservation_options = null;
    result.max_total_price = null;
    result.min_target_capacity = null;
    result.single_availability_zone = null;
    result.single_instance_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = std.meta.stringToEnum(FleetOnDemandAllocationStrategy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "capacityReservationOptions")) {
                    result.capacity_reservation_options = try deserializeCapacityReservationOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "maxTotalPrice")) {
                    result.max_total_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "minTargetCapacity")) {
                    result.min_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "singleAvailabilityZone")) {
                    result.single_availability_zone = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "singleInstanceType")) {
                    result.single_instance_type = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOperatorResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !OperatorResponse {
    var result: OperatorResponse = undefined;
    result.managed = null;
    result.principal = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "managed")) {
                    result.managed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "principal")) {
                    result.principal = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeOutpostLag(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !OutpostLag {
    var result: OutpostLag = undefined;
    result.local_gateway_virtual_interface_ids = null;
    result.outpost_arn = null;
    result.outpost_lag_id = null;
    result.owner_id = null;
    result.service_link_virtual_interface_ids = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "localGatewayVirtualInterfaceIdSet")) {
                    result.local_gateway_virtual_interface_ids = try deserializeLocalGatewayVirtualInterfaceIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostLagId")) {
                    result.outpost_lag_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceLinkVirtualInterfaceIdSet")) {
                    result.service_link_virtual_interface_ids = try deserializeServiceLinkVirtualInterfaceIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePacketHeaderStatement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PacketHeaderStatement {
    var result: PacketHeaderStatement = undefined;
    result.destination_addresses = null;
    result.destination_ports = null;
    result.destination_prefix_lists = null;
    result.protocols = null;
    result.source_addresses = null;
    result.source_ports = null;
    result.source_prefix_lists = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationAddressSet")) {
                    result.destination_addresses = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "destinationPortSet")) {
                    result.destination_ports = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListSet")) {
                    result.destination_prefix_lists = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "protocolSet")) {
                    result.protocols = try deserializeProtocolList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourceAddressSet")) {
                    result.source_addresses = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourcePortSet")) {
                    result.source_ports = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sourcePrefixListSet")) {
                    result.source_prefix_lists = try deserializeValueStringList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePathComponent(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PathComponent {
    var result: PathComponent = undefined;
    result.acl_rule = null;
    result.additional_details = null;
    result.attached_to = null;
    result.component = null;
    result.destination_vpc = null;
    result.elastic_load_balancer_listener = null;
    result.explanations = null;
    result.firewall_stateful_rule = null;
    result.firewall_stateless_rule = null;
    result.inbound_header = null;
    result.outbound_header = null;
    result.route_table_route = null;
    result.security_group_rule = null;
    result.sequence_number = null;
    result.service_name = null;
    result.source_vpc = null;
    result.subnet = null;
    result.transit_gateway = null;
    result.transit_gateway_route_table_route = null;
    result.vpc = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "aclRule")) {
                    result.acl_rule = try deserializeAnalysisAclRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "additionalDetailSet")) {
                    result.additional_details = try deserializeAdditionalDetailList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "attachedTo")) {
                    result.attached_to = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "component")) {
                    result.component = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "destinationVpc")) {
                    result.destination_vpc = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "elasticLoadBalancerListener")) {
                    result.elastic_load_balancer_listener = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "explanationSet")) {
                    result.explanations = try deserializeExplanationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "firewallStatefulRule")) {
                    result.firewall_stateful_rule = try deserializeFirewallStatefulRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "firewallStatelessRule")) {
                    result.firewall_stateless_rule = try deserializeFirewallStatelessRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "inboundHeader")) {
                    result.inbound_header = try deserializeAnalysisPacketHeader(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outboundHeader")) {
                    result.outbound_header = try deserializeAnalysisPacketHeader(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "routeTableRoute")) {
                    result.route_table_route = try deserializeAnalysisRouteTableRoute(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "securityGroupRule")) {
                    result.security_group_rule = try deserializeAnalysisSecurityGroupRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "sequenceNumber")) {
                    result.sequence_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceVpc")) {
                    result.source_vpc = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "subnet")) {
                    result.subnet = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGateway")) {
                    result.transit_gateway = try deserializeAnalysisComponent(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableRoute")) {
                    result.transit_gateway_route_table_route = try deserializeTransitGatewayRouteTableRoute(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpc")) {
                    result.vpc = try deserializeAnalysisComponent(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePathFilter(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PathFilter {
    var result: PathFilter = undefined;
    result.destination_address = null;
    result.destination_port_range = null;
    result.source_address = null;
    result.source_port_range = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationAddress")) {
                    result.destination_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try deserializeFilterPortRange(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "sourceAddress")) {
                    result.source_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try deserializeFilterPortRange(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePathStatement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PathStatement {
    var result: PathStatement = undefined;
    result.packet_header_statement = null;
    result.resource_statement = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "packetHeaderStatement")) {
                    result.packet_header_statement = try deserializePacketHeaderStatement(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "resourceStatement")) {
                    result.resource_statement = try deserializeResourceStatement(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePciId(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PciId {
    var result: PciId = undefined;
    result.device_id = null;
    result.subsystem_id = null;
    result.subsystem_vendor_id = null;
    result.vendor_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "DeviceId")) {
                    result.device_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "SubsystemId")) {
                    result.subsystem_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "SubsystemVendorId")) {
                    result.subsystem_vendor_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "VendorId")) {
                    result.vendor_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePeeringAttachmentStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PeeringAttachmentStatus {
    var result: PeeringAttachmentStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePeeringConnectionOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PeeringConnectionOptions {
    _ = alloc;
    var result: PeeringConnectionOptions = undefined;
    result.allow_dns_resolution_from_remote_vpc = null;
    result.allow_egress_from_local_classic_link_to_remote_vpc = null;
    result.allow_egress_from_local_vpc_to_remote_classic_link = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allowDnsResolutionFromRemoteVpc")) {
                    result.allow_dns_resolution_from_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalClassicLinkToRemoteVpc")) {
                    result.allow_egress_from_local_classic_link_to_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalVpcToRemoteClassicLink")) {
                    result.allow_egress_from_local_vpc_to_remote_classic_link = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePeeringTgwInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PeeringTgwInfo {
    var result: PeeringTgwInfo = undefined;
    result.core_network_id = null;
    result.owner_id = null;
    result.region = null;
    result.transit_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "coreNetworkId")) {
                    result.core_network_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePerformanceFactorReference(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PerformanceFactorReference {
    var result: PerformanceFactorReference = undefined;
    result.instance_family = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase1DHGroupNumbersListValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Phase1DHGroupNumbersListValue {
    _ = alloc;
    var result: Phase1DHGroupNumbersListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase1EncryptionAlgorithmsListValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Phase1EncryptionAlgorithmsListValue {
    var result: Phase1EncryptionAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase1IntegrityAlgorithmsListValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Phase1IntegrityAlgorithmsListValue {
    var result: Phase1IntegrityAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase2DHGroupNumbersListValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Phase2DHGroupNumbersListValue {
    _ = alloc;
    var result: Phase2DHGroupNumbersListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase2EncryptionAlgorithmsListValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Phase2EncryptionAlgorithmsListValue {
    var result: Phase2EncryptionAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePhase2IntegrityAlgorithmsListValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Phase2IntegrityAlgorithmsListValue {
    var result: Phase2IntegrityAlgorithmsListValue = undefined;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Placement {
    var result: Placement = undefined;
    result.affinity = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.group_id = null;
    result.group_name = null;
    result.host_id = null;
    result.host_resource_group_arn = null;
    result.partition_number = null;
    result.spread_domain = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "affinity")) {
                    result.affinity = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostId")) {
                    result.host_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hostResourceGroupArn")) {
                    result.host_resource_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "partitionNumber")) {
                    result.partition_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spreadDomain")) {
                    result.spread_domain = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(Tenancy, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacementGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PlacementGroup {
    var result: PlacementGroup = undefined;
    result.group_arn = null;
    result.group_id = null;
    result.group_name = null;
    result.linked_group_id = null;
    result.partition_count = null;
    result.spread_level = null;
    result.state = null;
    result.strategy = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupArn")) {
                    result.group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "linkedGroupId")) {
                    result.linked_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "partitionCount")) {
                    result.partition_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spreadLevel")) {
                    result.spread_level = std.meta.stringToEnum(SpreadLevel, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(PlacementGroupState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "strategy")) {
                    result.strategy = std.meta.stringToEnum(PlacementStrategy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacementGroupInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PlacementGroupInfo {
    var result: PlacementGroupInfo = undefined;
    result.supported_strategies = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "supportedStrategies")) {
                    result.supported_strategies = try deserializePlacementGroupStrategyList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePlacementResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PlacementResponse {
    var result: PlacementResponse = undefined;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePoolCidrBlock(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PoolCidrBlock {
    var result: PoolCidrBlock = undefined;
    result.cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "poolCidrBlock")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePortRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PortRange {
    _ = alloc;
    var result: PortRange = undefined;
    result.from = null;
    result.to = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "from")) {
                    result.from = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "to")) {
                    result.to = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixList(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrefixList {
    var result: PrefixList = undefined;
    result.cidrs = null;
    result.prefix_list_id = null;
    result.prefix_list_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrSet")) {
                    result.cidrs = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListName")) {
                    result.prefix_list_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixListAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrefixListAssociation {
    var result: PrefixListAssociation = undefined;
    result.resource_id = null;
    result.resource_owner = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwner")) {
                    result.resource_owner = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixListEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrefixListEntry {
    var result: PrefixListEntry = undefined;
    result.cidr = null;
    result.description = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrefixListId(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrefixListId {
    var result: PrefixListId = undefined;
    result.description = null;
    result.prefix_list_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePriceSchedule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PriceSchedule {
    _ = alloc;
    var result: PriceSchedule = undefined;
    result.active = null;
    result.currency_code = null;
    result.price = null;
    result.term = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "active")) {
                    result.active = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = std.meta.stringToEnum(CurrencyCodeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "price")) {
                    result.price = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "term")) {
                    result.term = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePricingDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PricingDetail {
    _ = alloc;
    var result: PricingDetail = undefined;
    result.count = null;
    result.price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "count")) {
                    result.count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "price")) {
                    result.price = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrincipalIdFormat(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrincipalIdFormat {
    var result: PrincipalIdFormat = undefined;
    result.arn = null;
    result.statuses = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusSet")) {
                    result.statuses = try deserializeIdFormatList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrivateDnsDetails {
    var result: PrivateDnsDetails = undefined;
    result.private_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsNameConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrivateDnsNameConfiguration {
    var result: PrivateDnsNameConfiguration = undefined;
    result.name = null;
    result.state = null;
    result.@"type" = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "name")) {
                    result.name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(DnsNameState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsNameOptionsOnLaunch(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrivateDnsNameOptionsOnLaunch {
    _ = alloc;
    var result: PrivateDnsNameOptionsOnLaunch = undefined;
    result.enable_resource_name_dns_aaaa_record = null;
    result.enable_resource_name_dns_a_record = null;
    result.hostname_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableResourceNameDnsAAAARecord")) {
                    result.enable_resource_name_dns_aaaa_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableResourceNameDnsARecord")) {
                    result.enable_resource_name_dns_a_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hostnameType")) {
                    result.hostname_type = std.meta.stringToEnum(HostnameType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateDnsNameOptionsResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrivateDnsNameOptionsResponse {
    _ = alloc;
    var result: PrivateDnsNameOptionsResponse = undefined;
    result.enable_resource_name_dns_aaaa_record = null;
    result.enable_resource_name_dns_a_record = null;
    result.hostname_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableResourceNameDnsAAAARecord")) {
                    result.enable_resource_name_dns_aaaa_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableResourceNameDnsARecord")) {
                    result.enable_resource_name_dns_a_record = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "hostnameType")) {
                    result.hostname_type = std.meta.stringToEnum(HostnameType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePrivateIpAddressSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PrivateIpAddressSpecification {
    var result: PrivateIpAddressSpecification = undefined;
    result.primary = null;
    result.private_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "primary")) {
                    result.primary = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "privateIpAddress")) {
                    result.private_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeProcessorInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ProcessorInfo {
    var result: ProcessorInfo = undefined;
    result.manufacturer = null;
    result.supported_architectures = null;
    result.supported_features = null;
    result.sustained_clock_speed_in_ghz = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "manufacturer")) {
                    result.manufacturer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "supportedArchitectures")) {
                    result.supported_architectures = try deserializeArchitectureTypeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "supportedFeatures")) {
                    result.supported_features = try deserializeSupportedAdditionalProcessorFeatureList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sustainedClockSpeedInGhz")) {
                    result.sustained_clock_speed_in_ghz = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeProductCode(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ProductCode {
    var result: ProductCode = undefined;
    result.product_code_id = null;
    result.product_code_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "productCode")) {
                    result.product_code_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.product_code_type = std.meta.stringToEnum(ProductCodeValues, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePropagatingVgw(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PropagatingVgw {
    var result: PropagatingVgw = undefined;
    result.gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeProvisionedBandwidth(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ProvisionedBandwidth {
    var result: ProvisionedBandwidth = undefined;
    result.provisioned = null;
    result.provision_time = null;
    result.requested = null;
    result.request_time = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "provisioned")) {
                    result.provisioned = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "provisionTime")) {
                    result.provision_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "requested")) {
                    result.requested = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "requestTime")) {
                    result.request_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePtrUpdateStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PtrUpdateStatus {
    var result: PtrUpdateStatus = undefined;
    result.reason = null;
    result.status = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reason")) {
                    result.reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePublicIpDnsNameOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PublicIpDnsNameOptions {
    var result: PublicIpDnsNameOptions = undefined;
    result.dns_hostname_type = null;
    result.public_dual_stack_dns_name = null;
    result.public_ipv_4_dns_name = null;
    result.public_ipv_6_dns_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dnsHostnameType")) {
                    result.dns_hostname_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicDualStackDnsName")) {
                    result.public_dual_stack_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv4DnsName")) {
                    result.public_ipv_4_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "publicIpv6DnsName")) {
                    result.public_ipv_6_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePublicIpv4Pool(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PublicIpv4Pool {
    var result: PublicIpv4Pool = undefined;
    result.description = null;
    result.network_border_group = null;
    result.pool_address_ranges = null;
    result.pool_id = null;
    result.tags = null;
    result.total_address_count = null;
    result.total_available_address_count = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "poolAddressRangeSet")) {
                    result.pool_address_ranges = try deserializePublicIpv4PoolRangeSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "poolId")) {
                    result.pool_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "totalAddressCount")) {
                    result.total_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalAvailableAddressCount")) {
                    result.total_available_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePublicIpv4PoolRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !PublicIpv4PoolRange {
    var result: PublicIpv4PoolRange = undefined;
    result.address_count = null;
    result.available_address_count = null;
    result.first_address = null;
    result.last_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressCount")) {
                    result.address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "availableAddressCount")) {
                    result.available_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "firstAddress")) {
                    result.first_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastAddress")) {
                    result.last_address = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializePurchase(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Purchase {
    var result: Purchase = undefined;
    result.currency_code = null;
    result.duration = null;
    result.host_id_set = null;
    result.host_reservation_id = null;
    result.hourly_price = null;
    result.instance_family = null;
    result.payment_option = null;
    result.upfront_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = std.meta.stringToEnum(CurrencyCodeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "hostIdSet")) {
                    result.host_id_set = try deserializeResponseHostIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "hostReservationId")) {
                    result.host_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceFamily")) {
                    result.instance_family = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "paymentOption")) {
                    result.payment_option = std.meta.stringToEnum(PaymentOption, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "upfrontPrice")) {
                    result.upfront_price = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRecurringCharge(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RecurringCharge {
    _ = alloc;
    var result: RecurringCharge = undefined;
    result.amount = null;
    result.frequency = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amount")) {
                    result.amount = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "frequency")) {
                    result.frequency = std.meta.stringToEnum(RecurringChargeFrequency, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReferencedSecurityGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReferencedSecurityGroup {
    var result: ReferencedSecurityGroup = undefined;
    result.group_id = null;
    result.peering_status = null;
    result.user_id = null;
    result.vpc_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringStatus")) {
                    result.peering_status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRegion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Region {
    var result: Region = undefined;
    result.endpoint = null;
    result.opt_in_status = null;
    result.region_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "regionEndpoint")) {
                    result.endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "optInStatus")) {
                    result.opt_in_status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRegionalSummary(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RegionalSummary {
    var result: RegionalSummary = undefined;
    result.number_of_matched_accounts = null;
    result.number_of_unmatched_accounts = null;
    result.region_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "numberOfMatchedAccounts")) {
                    result.number_of_matched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "numberOfUnmatchedAccounts")) {
                    result.number_of_unmatched_accounts = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "regionName")) {
                    result.region_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRegisteredInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RegisteredInstance {
    var result: RegisteredInstance = undefined;
    result.ha_status = null;
    result.instance_id = null;
    result.last_updated_time = null;
    result.processing_status = null;
    result.sql_server_credentials = null;
    result.sql_server_license_usage = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "haStatus")) {
                    result.ha_status = std.meta.stringToEnum(HaStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "processingStatus")) {
                    result.processing_status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sqlServerCredentials")) {
                    result.sql_server_credentials = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sqlServerLicenseUsage")) {
                    result.sql_server_license_usage = std.meta.stringToEnum(SqlServerLicenseUsage, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReplaceRootVolumeTask(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReplaceRootVolumeTask {
    var result: ReplaceRootVolumeTask = undefined;
    result.complete_time = null;
    result.delete_replaced_root_volume = null;
    result.image_id = null;
    result.instance_id = null;
    result.replace_root_volume_task_id = null;
    result.snapshot_id = null;
    result.start_time = null;
    result.tags = null;
    result.task_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "completeTime")) {
                    result.complete_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deleteReplacedRootVolume")) {
                    result.delete_replaced_root_volume = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "replaceRootVolumeTaskId")) {
                    result.replace_root_volume_task_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "taskState")) {
                    result.task_state = std.meta.stringToEnum(ReplaceRootVolumeTaskState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Reservation {
    var result: Reservation = undefined;
    result.groups = null;
    result.instances = null;
    result.owner_id = null;
    result.requester_id = null;
    result.reservation_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "instancesSet")) {
                    result.instances = try deserializeInstanceList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "requesterId")) {
                    result.requester_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationId")) {
                    result.reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservationValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservationValue {
    var result: ReservationValue = undefined;
    result.hourly_price = null;
    result.remaining_total_value = null;
    result.remaining_upfront_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remainingTotalValue")) {
                    result.remaining_total_value = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remainingUpfrontValue")) {
                    result.remaining_upfront_value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstanceReservationValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstanceReservationValue {
    var result: ReservedInstanceReservationValue = undefined;
    result.reservation_value = null;
    result.reserved_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservationValue")) {
                    result.reservation_value = try deserializeReservationValue(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "reservedInstanceId")) {
                    result.reserved_instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstances(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstances {
    var result: ReservedInstances = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.currency_code = null;
    result.duration = null;
    result.end = null;
    result.fixed_price = null;
    result.instance_count = null;
    result.instance_tenancy = null;
    result.instance_type = null;
    result.offering_class = null;
    result.offering_type = null;
    result.product_description = null;
    result.recurring_charges = null;
    result.reserved_instances_id = null;
    result.scope = null;
    result.start = null;
    result.state = null;
    result.tags = null;
    result.usage_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = std.meta.stringToEnum(CurrencyCodeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "end")) {
                    result.end = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fixedPrice")) {
                    result.fixed_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceTenancy")) {
                    result.instance_tenancy = std.meta.stringToEnum(Tenancy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringClass")) {
                    result.offering_class = std.meta.stringToEnum(OfferingClassType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringType")) {
                    result.offering_type = std.meta.stringToEnum(OfferingTypeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = std.meta.stringToEnum(RIProductDescription, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recurringCharges")) {
                    result.recurring_charges = try deserializeRecurringChargesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = std.meta.stringToEnum(scope, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "start")) {
                    result.start = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(ReservedInstanceState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "usagePrice")) {
                    result.usage_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstancesConfiguration {
    var result: ReservedInstancesConfiguration = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_count = null;
    result.instance_type = null;
    result.platform = null;
    result.scope = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = std.meta.stringToEnum(scope, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesId(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstancesId {
    var result: ReservedInstancesId = undefined;
    result.reserved_instances_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesListing(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstancesListing {
    var result: ReservedInstancesListing = undefined;
    result.client_token = null;
    result.create_date = null;
    result.instance_counts = null;
    result.price_schedules = null;
    result.reserved_instances_id = null;
    result.reserved_instances_listing_id = null;
    result.status = null;
    result.status_message = null;
    result.tags = null;
    result.update_date = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceCounts")) {
                    result.instance_counts = try deserializeInstanceCountList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "priceSchedules")) {
                    result.price_schedules = try deserializePriceScheduleList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservedInstancesListingId")) {
                    result.reserved_instances_listing_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(ListingStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "updateDate")) {
                    result.update_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesModification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstancesModification {
    var result: ReservedInstancesModification = undefined;
    result.client_token = null;
    result.create_date = null;
    result.effective_date = null;
    result.modification_results = null;
    result.reserved_instances_ids = null;
    result.reserved_instances_modification_id = null;
    result.status = null;
    result.status_message = null;
    result.update_date = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "effectiveDate")) {
                    result.effective_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "modificationResultSet")) {
                    result.modification_results = try deserializeReservedInstancesModificationResultList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesSet")) {
                    result.reserved_instances_ids = try deserializeReservedIntancesIds(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesModificationId")) {
                    result.reserved_instances_modification_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updateDate")) {
                    result.update_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesModificationResult(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstancesModificationResult {
    var result: ReservedInstancesModificationResult = undefined;
    result.reserved_instances_id = null;
    result.target_configuration = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetConfiguration")) {
                    result.target_configuration = try deserializeReservedInstancesConfiguration(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeReservedInstancesOffering(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ReservedInstancesOffering {
    var result: ReservedInstancesOffering = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.currency_code = null;
    result.duration = null;
    result.fixed_price = null;
    result.instance_tenancy = null;
    result.instance_type = null;
    result.marketplace = null;
    result.offering_class = null;
    result.offering_type = null;
    result.pricing_details = null;
    result.product_description = null;
    result.recurring_charges = null;
    result.reserved_instances_offering_id = null;
    result.scope = null;
    result.usage_price = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currencyCode")) {
                    result.currency_code = std.meta.stringToEnum(CurrencyCodeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "duration")) {
                    result.duration = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "fixedPrice")) {
                    result.fixed_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceTenancy")) {
                    result.instance_tenancy = std.meta.stringToEnum(Tenancy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "marketplace")) {
                    result.marketplace = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "offeringClass")) {
                    result.offering_class = std.meta.stringToEnum(OfferingClassType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "offeringType")) {
                    result.offering_type = std.meta.stringToEnum(OfferingTypeValues, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "pricingDetailsSet")) {
                    result.pricing_details = try deserializePricingDetailsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = std.meta.stringToEnum(RIProductDescription, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recurringCharges")) {
                    result.recurring_charges = try deserializeRecurringChargesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "reservedInstancesOfferingId")) {
                    result.reserved_instances_offering_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scope")) {
                    result.scope = std.meta.stringToEnum(scope, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "usagePrice")) {
                    result.usage_price = std.fmt.parseFloat(f32, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeResourceStatement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ResourceStatement {
    var result: ResourceStatement = undefined;
    result.resources = null;
    result.resource_types = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceSet")) {
                    result.resources = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "resourceTypeSet")) {
                    result.resource_types = try deserializeValueStringList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeResponseError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ResponseError {
    var result: ResponseError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(LaunchTemplateErrorCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeResponseLaunchTemplateData(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ResponseLaunchTemplateData {
    var result: ResponseLaunchTemplateData = undefined;
    result.block_device_mappings = null;
    result.capacity_reservation_specification = null;
    result.cpu_options = null;
    result.credit_specification = null;
    result.disable_api_stop = null;
    result.disable_api_termination = null;
    result.ebs_optimized = null;
    result.elastic_gpu_specifications = null;
    result.elastic_inference_accelerators = null;
    result.enclave_options = null;
    result.hibernation_options = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_initiated_shutdown_behavior = null;
    result.instance_market_options = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.kernel_id = null;
    result.key_name = null;
    result.license_specifications = null;
    result.maintenance_options = null;
    result.metadata_options = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.network_performance_options = null;
    result.operator = null;
    result.placement = null;
    result.private_dns_name_options = null;
    result.ram_disk_id = null;
    result.security_group_ids = null;
    result.security_groups = null;
    result.tag_specifications = null;
    result.user_data = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blockDeviceMappingSet")) {
                    result.block_device_mappings = try deserializeLaunchTemplateBlockDeviceMappingList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "capacityReservationSpecification")) {
                    result.capacity_reservation_specification = try deserializeLaunchTemplateCapacityReservationSpecificationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "cpuOptions")) {
                    result.cpu_options = try deserializeLaunchTemplateCpuOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "creditSpecification")) {
                    result.credit_specification = try deserializeCreditSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "disableApiStop")) {
                    result.disable_api_stop = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "disableApiTermination")) {
                    result.disable_api_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "elasticGpuSpecificationSet")) {
                    result.elastic_gpu_specifications = try deserializeElasticGpuSpecificationResponseList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "elasticInferenceAcceleratorSet")) {
                    result.elastic_inference_accelerators = try deserializeLaunchTemplateElasticInferenceAcceleratorResponseList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "enclaveOptions")) {
                    result.enclave_options = try deserializeLaunchTemplateEnclaveOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "hibernationOptions")) {
                    result.hibernation_options = try deserializeLaunchTemplateHibernationOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeLaunchTemplateIamInstanceProfileSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInitiatedShutdownBehavior")) {
                    result.instance_initiated_shutdown_behavior = std.meta.stringToEnum(ShutdownBehavior, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceMarketOptions")) {
                    result.instance_market_options = try deserializeLaunchTemplateInstanceMarketOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "licenseSet")) {
                    result.license_specifications = try deserializeLaunchTemplateLicenseList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "maintenanceOptions")) {
                    result.maintenance_options = try deserializeLaunchTemplateInstanceMaintenanceOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "metadataOptions")) {
                    result.metadata_options = try deserializeLaunchTemplateInstanceMetadataOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeLaunchTemplatesMonitoring(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeLaunchTemplateInstanceNetworkInterfaceSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkPerformanceOptions")) {
                    result.network_performance_options = try deserializeLaunchTemplateNetworkPerformanceOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializeLaunchTemplatePlacement(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "privateDnsNameOptions")) {
                    result.private_dns_name_options = try deserializeLaunchTemplatePrivateDnsNameOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ramDiskId")) {
                    result.ram_disk_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "securityGroupSet")) {
                    result.security_groups = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSpecificationSet")) {
                    result.tag_specifications = try deserializeLaunchTemplateTagSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "userData")) {
                    result.user_data = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRevokedSecurityGroupRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RevokedSecurityGroupRule {
    var result: RevokedSecurityGroupRule = undefined;
    result.cidr_ipv_4 = null;
    result.cidr_ipv_6 = null;
    result.description = null;
    result.from_port = null;
    result.group_id = null;
    result.ip_protocol = null;
    result.is_egress = null;
    result.prefix_list_id = null;
    result.referenced_group_id = null;
    result.security_group_rule_id = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIpv4")) {
                    result.cidr_ipv_4 = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrIpv6")) {
                    result.cidr_ipv_6 = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isEgress")) {
                    result.is_egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "referencedGroupId")) {
                    result.referenced_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupRuleId")) {
                    result.security_group_rule_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Route {
    var result: Route = undefined;
    result.carrier_gateway_id = null;
    result.core_network_arn = null;
    result.destination_cidr_block = null;
    result.destination_ipv_6_cidr_block = null;
    result.destination_prefix_list_id = null;
    result.egress_only_internet_gateway_id = null;
    result.gateway_id = null;
    result.instance_id = null;
    result.instance_owner_id = null;
    result.ip_address = null;
    result.local_gateway_id = null;
    result.nat_gateway_id = null;
    result.network_interface_id = null;
    result.odb_network_arn = null;
    result.origin = null;
    result.state = null;
    result.transit_gateway_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "carrierGatewayId")) {
                    result.carrier_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkArn")) {
                    result.core_network_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationIpv6CidrBlock")) {
                    result.destination_ipv_6_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPrefixListId")) {
                    result.destination_prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressOnlyInternetGatewayId")) {
                    result.egress_only_internet_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceOwnerId")) {
                    result.instance_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipAddress")) {
                    result.ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localGatewayId")) {
                    result.local_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "natGatewayId")) {
                    result.nat_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "odbNetworkArn")) {
                    result.odb_network_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "origin")) {
                    result.origin = std.meta.stringToEnum(RouteOrigin, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(RouteState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServer(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServer {
    var result: RouteServer = undefined;
    result.amazon_side_asn = null;
    result.persist_routes_duration = null;
    result.persist_routes_state = null;
    result.route_server_id = null;
    result.sns_notifications_enabled = null;
    result.sns_topic_arn = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amazonSideAsn")) {
                    result.amazon_side_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "persistRoutesDuration")) {
                    result.persist_routes_duration = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "persistRoutesState")) {
                    result.persist_routes_state = std.meta.stringToEnum(RouteServerPersistRoutesState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snsNotificationsEnabled")) {
                    result.sns_notifications_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "snsTopicArn")) {
                    result.sns_topic_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(RouteServerState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerAssociation {
    var result: RouteServerAssociation = undefined;
    result.route_server_id = null;
    result.state = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(RouteServerAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerBfdStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerBfdStatus {
    _ = alloc;
    var result: RouteServerBfdStatus = undefined;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(RouteServerBfdState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerBgpOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerBgpOptions {
    _ = alloc;
    var result: RouteServerBgpOptions = undefined;
    result.peer_asn = null;
    result.peer_liveness_detection = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "peerAsn")) {
                    result.peer_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "peerLivenessDetection")) {
                    result.peer_liveness_detection = std.meta.stringToEnum(RouteServerPeerLivenessMode, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerBgpStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerBgpStatus {
    _ = alloc;
    var result: RouteServerBgpStatus = undefined;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(RouteServerBgpState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerEndpoint(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerEndpoint {
    var result: RouteServerEndpoint = undefined;
    result.eni_address = null;
    result.eni_id = null;
    result.failure_reason = null;
    result.route_server_endpoint_id = null;
    result.route_server_id = null;
    result.state = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "eniAddress")) {
                    result.eni_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eniId")) {
                    result.eni_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerEndpointId")) {
                    result.route_server_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(RouteServerEndpointState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerPeer(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerPeer {
    var result: RouteServerPeer = undefined;
    result.bfd_status = null;
    result.bgp_options = null;
    result.bgp_status = null;
    result.endpoint_eni_address = null;
    result.endpoint_eni_id = null;
    result.failure_reason = null;
    result.peer_address = null;
    result.route_server_endpoint_id = null;
    result.route_server_id = null;
    result.route_server_peer_id = null;
    result.state = null;
    result.subnet_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bfdStatus")) {
                    result.bfd_status = try deserializeRouteServerBfdStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "bgpOptions")) {
                    result.bgp_options = try deserializeRouteServerBgpOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "bgpStatus")) {
                    result.bgp_status = try deserializeRouteServerBgpStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "endpointEniAddress")) {
                    result.endpoint_eni_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endpointEniId")) {
                    result.endpoint_eni_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerEndpointId")) {
                    result.route_server_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerPeerId")) {
                    result.route_server_peer_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(RouteServerPeerState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerPropagation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerPropagation {
    var result: RouteServerPropagation = undefined;
    result.route_server_id = null;
    result.route_table_id = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "routeServerId")) {
                    result.route_server_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(RouteServerPropagationState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerRoute {
    var result: RouteServerRoute = undefined;
    result.as_paths = null;
    result.med = null;
    result.next_hop_ip = null;
    result.prefix = null;
    result.route_installation_details = null;
    result.route_server_endpoint_id = null;
    result.route_server_peer_id = null;
    result.route_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "asPathSet")) {
                    result.as_paths = try deserializeAsPath(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "med")) {
                    result.med = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "nextHopIp")) {
                    result.next_hop_ip = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeInstallationDetailSet")) {
                    result.route_installation_details = try deserializeRouteServerRouteInstallationDetails(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "routeServerEndpointId")) {
                    result.route_server_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeServerPeerId")) {
                    result.route_server_peer_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeStatus")) {
                    result.route_status = std.meta.stringToEnum(RouteServerRouteStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteServerRouteInstallationDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteServerRouteInstallationDetail {
    var result: RouteServerRouteInstallationDetail = undefined;
    result.route_installation_status = null;
    result.route_installation_status_reason = null;
    result.route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "routeInstallationStatus")) {
                    result.route_installation_status = std.meta.stringToEnum(RouteServerRouteInstallationStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeInstallationStatusReason")) {
                    result.route_installation_status_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteTable(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteTable {
    var result: RouteTable = undefined;
    result.associations = null;
    result.owner_id = null;
    result.propagating_vgws = null;
    result.routes = null;
    result.route_table_id = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationSet")) {
                    result.associations = try deserializeRouteTableAssociationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "propagatingVgwSet")) {
                    result.propagating_vgws = try deserializePropagatingVgwList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "routeSet")) {
                    result.routes = try deserializeRouteList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteTableAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteTableAssociation {
    var result: RouteTableAssociation = undefined;
    result.association_state = null;
    result.gateway_id = null;
    result.main = null;
    result.public_ipv_4_pool = null;
    result.route_table_association_id = null;
    result.route_table_id = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationState")) {
                    result.association_state = try deserializeRouteTableAssociationState(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "gatewayId")) {
                    result.gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "main")) {
                    result.main = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicIpv4Pool")) {
                    result.public_ipv_4_pool = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableAssociationId")) {
                    result.route_table_association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableId")) {
                    result.route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRouteTableAssociationState(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RouteTableAssociationState {
    var result: RouteTableAssociationState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(RouteTableAssociationStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRuleGroupRuleOptionsPair(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RuleGroupRuleOptionsPair {
    var result: RuleGroupRuleOptionsPair = undefined;
    result.rule_group_arn = null;
    result.rule_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleOptionSet")) {
                    result.rule_options = try deserializeRuleOptionList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRuleGroupTypePair(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RuleGroupTypePair {
    var result: RuleGroupTypePair = undefined;
    result.rule_group_arn = null;
    result.rule_group_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ruleGroupArn")) {
                    result.rule_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleGroupType")) {
                    result.rule_group_type = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRuleOption(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RuleOption {
    var result: RuleOption = undefined;
    result.keyword = null;
    result.settings = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "keyword")) {
                    result.keyword = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "settingSet")) {
                    result.settings = try deserializeStringList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeRunInstancesMonitoringEnabled(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !RunInstancesMonitoringEnabled {
    _ = alloc;
    var result: RunInstancesMonitoringEnabled = undefined;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeS3Storage(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !S3Storage {
    var result: S3Storage = undefined;
    result.aws_access_key_id = null;
    result.bucket = null;
    result.prefix = null;
    result.upload_policy = null;
    result.upload_policy_signature = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "AWSAccessKeyId")) {
                    result.aws_access_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bucket")) {
                    result.bucket = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "uploadPolicy")) {
                    result.upload_policy = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "uploadPolicySignature")) {
                    result.upload_policy_signature = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeScheduledInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ScheduledInstance {
    var result: ScheduledInstance = undefined;
    result.availability_zone = null;
    result.create_date = null;
    result.hourly_price = null;
    result.instance_count = null;
    result.instance_type = null;
    result.network_platform = null;
    result.next_slot_start_time = null;
    result.platform = null;
    result.previous_slot_end_time = null;
    result.recurrence = null;
    result.scheduled_instance_id = null;
    result.slot_duration_in_hours = null;
    result.term_end_date = null;
    result.term_start_date = null;
    result.total_scheduled_instance_hours = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createDate")) {
                    result.create_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkPlatform")) {
                    result.network_platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nextSlotStartTime")) {
                    result.next_slot_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "previousSlotEndTime")) {
                    result.previous_slot_end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recurrence")) {
                    result.recurrence = try deserializeScheduledInstanceRecurrence(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "scheduledInstanceId")) {
                    result.scheduled_instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "slotDurationInHours")) {
                    result.slot_duration_in_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "termEndDate")) {
                    result.term_end_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "termStartDate")) {
                    result.term_start_date = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "totalScheduledInstanceHours")) {
                    result.total_scheduled_instance_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeScheduledInstanceAvailability(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ScheduledInstanceAvailability {
    var result: ScheduledInstanceAvailability = undefined;
    result.availability_zone = null;
    result.available_instance_count = null;
    result.first_slot_start_time = null;
    result.hourly_price = null;
    result.instance_type = null;
    result.max_term_duration_in_days = null;
    result.min_term_duration_in_days = null;
    result.network_platform = null;
    result.platform = null;
    result.purchase_token = null;
    result.recurrence = null;
    result.slot_duration_in_hours = null;
    result.total_scheduled_instance_hours = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableInstanceCount")) {
                    result.available_instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "firstSlotStartTime")) {
                    result.first_slot_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "hourlyPrice")) {
                    result.hourly_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "maxTermDurationInDays")) {
                    result.max_term_duration_in_days = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "minTermDurationInDays")) {
                    result.min_term_duration_in_days = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "networkPlatform")) {
                    result.network_platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "purchaseToken")) {
                    result.purchase_token = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recurrence")) {
                    result.recurrence = try deserializeScheduledInstanceRecurrence(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "slotDurationInHours")) {
                    result.slot_duration_in_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "totalScheduledInstanceHours")) {
                    result.total_scheduled_instance_hours = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeScheduledInstanceRecurrence(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ScheduledInstanceRecurrence {
    var result: ScheduledInstanceRecurrence = undefined;
    result.frequency = null;
    result.interval = null;
    result.occurrence_day_set = null;
    result.occurrence_relative_to_end = null;
    result.occurrence_unit = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "frequency")) {
                    result.frequency = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "interval")) {
                    result.interval = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "occurrenceDaySet")) {
                    result.occurrence_day_set = try deserializeOccurrenceDaySet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "occurrenceRelativeToEnd")) {
                    result.occurrence_relative_to_end = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "occurrenceUnit")) {
                    result.occurrence_unit = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SecurityGroup {
    var result: SecurityGroup = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.ip_permissions = null;
    result.ip_permissions_egress = null;
    result.owner_id = null;
    result.security_group_arn = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupDescription")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipPermissions")) {
                    result.ip_permissions = try deserializeIpPermissionList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipPermissionsEgress")) {
                    result.ip_permissions_egress = try deserializeIpPermissionList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupArn")) {
                    result.security_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupForVpc(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SecurityGroupForVpc {
    var result: SecurityGroupForVpc = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.owner_id = null;
    result.primary_vpc_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "primaryVpcId")) {
                    result.primary_vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupIdentifier(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SecurityGroupIdentifier {
    var result: SecurityGroupIdentifier = undefined;
    result.group_id = null;
    result.group_name = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupReference(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SecurityGroupReference {
    var result: SecurityGroupReference = undefined;
    result.group_id = null;
    result.referencing_vpc_id = null;
    result.transit_gateway_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "referencingVpcId")) {
                    result.referencing_vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SecurityGroupRule {
    var result: SecurityGroupRule = undefined;
    result.cidr_ipv_4 = null;
    result.cidr_ipv_6 = null;
    result.description = null;
    result.from_port = null;
    result.group_id = null;
    result.group_owner_id = null;
    result.ip_protocol = null;
    result.is_egress = null;
    result.prefix_list_id = null;
    result.referenced_group_info = null;
    result.security_group_rule_arn = null;
    result.security_group_rule_id = null;
    result.tags = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrIpv4")) {
                    result.cidr_ipv_4 = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrIpv6")) {
                    result.cidr_ipv_6 = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupOwnerId")) {
                    result.group_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isEgress")) {
                    result.is_egress = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "referencedGroupInfo")) {
                    result.referenced_group_info = try deserializeReferencedSecurityGroup(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "securityGroupRuleArn")) {
                    result.security_group_rule_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupRuleId")) {
                    result.security_group_rule_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSecurityGroupVpcAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SecurityGroupVpcAssociation {
    var result: SecurityGroupVpcAssociation = undefined;
    result.group_id = null;
    result.group_owner_id = null;
    result.state = null;
    result.state_reason = null;
    result.vpc_id = null;
    result.vpc_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupOwnerId")) {
                    result.group_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(SecurityGroupVpcAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateReason")) {
                    result.state_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcOwnerId")) {
                    result.vpc_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ServiceConfiguration {
    var result: ServiceConfiguration = undefined;
    result.acceptance_required = null;
    result.availability_zone_ids = null;
    result.availability_zones = null;
    result.base_endpoint_dns_names = null;
    result.gateway_load_balancer_arns = null;
    result.manages_vpc_endpoints = null;
    result.network_load_balancer_arns = null;
    result.payer_responsibility = null;
    result.private_dns_name = null;
    result.private_dns_name_configuration = null;
    result.remote_access_enabled = null;
    result.service_id = null;
    result.service_name = null;
    result.service_state = null;
    result.service_type = null;
    result.supported_ip_address_types = null;
    result.supported_regions = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceptanceRequired")) {
                    result.acceptance_required = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneIdSet")) {
                    result.availability_zone_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneSet")) {
                    result.availability_zones = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "baseEndpointDnsNameSet")) {
                    result.base_endpoint_dns_names = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "gatewayLoadBalancerArnSet")) {
                    result.gateway_load_balancer_arns = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "managesVpcEndpoints")) {
                    result.manages_vpc_endpoints = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "networkLoadBalancerArnSet")) {
                    result.network_load_balancer_arns = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "payerResponsibility")) {
                    result.payer_responsibility = std.meta.stringToEnum(PayerResponsibility, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameConfiguration")) {
                    result.private_dns_name_configuration = try deserializePrivateDnsNameConfiguration(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "remoteAccessEnabled")) {
                    result.remote_access_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceState")) {
                    result.service_state = std.meta.stringToEnum(ServiceState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceType")) {
                    result.service_type = try deserializeServiceTypeDetailSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "supportedIpAddressTypeSet")) {
                    result.supported_ip_address_types = try deserializeSupportedIpAddressTypes(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "supportedRegionSet")) {
                    result.supported_regions = try deserializeSupportedRegionSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ServiceDetail {
    var result: ServiceDetail = undefined;
    result.acceptance_required = null;
    result.availability_zone_ids = null;
    result.availability_zones = null;
    result.base_endpoint_dns_names = null;
    result.manages_vpc_endpoints = null;
    result.owner = null;
    result.payer_responsibility = null;
    result.private_dns_name = null;
    result.private_dns_names = null;
    result.private_dns_name_verification_state = null;
    result.service_id = null;
    result.service_name = null;
    result.service_region = null;
    result.service_type = null;
    result.supported_ip_address_types = null;
    result.tags = null;
    result.vpc_endpoint_policy_supported = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceptanceRequired")) {
                    result.acceptance_required = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneIdSet")) {
                    result.availability_zone_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZoneSet")) {
                    result.availability_zones = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "baseEndpointDnsNameSet")) {
                    result.base_endpoint_dns_names = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "managesVpcEndpoints")) {
                    result.manages_vpc_endpoints = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "owner")) {
                    result.owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "payerResponsibility")) {
                    result.payer_responsibility = std.meta.stringToEnum(PayerResponsibility, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsName")) {
                    result.private_dns_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameSet")) {
                    result.private_dns_names = try deserializePrivateDnsDetailsSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "privateDnsNameVerificationState")) {
                    result.private_dns_name_verification_state = std.meta.stringToEnum(DnsNameState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceRegion")) {
                    result.service_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceType")) {
                    result.service_type = try deserializeServiceTypeDetailSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "supportedIpAddressTypeSet")) {
                    result.supported_ip_address_types = try deserializeSupportedIpAddressTypes(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointPolicySupported")) {
                    result.vpc_endpoint_policy_supported = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceLinkVirtualInterface(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ServiceLinkVirtualInterface {
    var result: ServiceLinkVirtualInterface = undefined;
    result.configuration_state = null;
    result.local_address = null;
    result.outpost_arn = null;
    result.outpost_id = null;
    result.outpost_lag_id = null;
    result.owner_id = null;
    result.peer_address = null;
    result.peer_bgp_asn = null;
    result.service_link_virtual_interface_arn = null;
    result.service_link_virtual_interface_id = null;
    result.tags = null;
    result.vlan = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "configurationState")) {
                    result.configuration_state = std.meta.stringToEnum(ServiceLinkVirtualInterfaceConfigurationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localAddress")) {
                    result.local_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostId")) {
                    result.outpost_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostLagId")) {
                    result.outpost_lag_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerBgpAsn")) {
                    result.peer_bgp_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "serviceLinkVirtualInterfaceArn")) {
                    result.service_link_virtual_interface_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceLinkVirtualInterfaceId")) {
                    result.service_link_virtual_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vlan")) {
                    result.vlan = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeServiceTypeDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ServiceTypeDetail {
    _ = alloc;
    var result: ServiceTypeDetail = undefined;
    result.service_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "serviceType")) {
                    result.service_type = std.meta.stringToEnum(ServiceType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshot(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Snapshot {
    var result: Snapshot = undefined;
    result.availability_zone = null;
    result.completion_duration_minutes = null;
    result.completion_time = null;
    result.data_encryption_key_id = null;
    result.description = null;
    result.encrypted = null;
    result.full_snapshot_size_in_bytes = null;
    result.kms_key_id = null;
    result.outpost_arn = null;
    result.owner_alias = null;
    result.owner_id = null;
    result.progress = null;
    result.restore_expiry_time = null;
    result.snapshot_id = null;
    result.sse_type = null;
    result.start_time = null;
    result.state = null;
    result.state_message = null;
    result.storage_tier = null;
    result.tags = null;
    result.transfer_type = null;
    result.volume_id = null;
    result.volume_size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "completionDurationMinutes")) {
                    result.completion_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "completionTime")) {
                    result.completion_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dataEncryptionKeyId")) {
                    result.data_encryption_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "fullSnapshotSizeInBytes")) {
                    result.full_snapshot_size_in_bytes = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerAlias")) {
                    result.owner_alias = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "restoreExpiryTime")) {
                    result.restore_expiry_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseType")) {
                    result.sse_type = std.meta.stringToEnum(SSEType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.state = std.meta.stringToEnum(SnapshotState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storageTier")) {
                    result.storage_tier = std.meta.stringToEnum(StorageTier, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transferType")) {
                    result.transfer_type = std.meta.stringToEnum(TransferType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SnapshotDetail {
    var result: SnapshotDetail = undefined;
    result.description = null;
    result.device_name = null;
    result.disk_image_size = null;
    result.format = null;
    result.progress = null;
    result.snapshot_id = null;
    result.status = null;
    result.status_message = null;
    result.url = null;
    result.user_bucket = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceName")) {
                    result.device_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "diskImageSize")) {
                    result.disk_image_size = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "format")) {
                    result.format = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "url")) {
                    result.url = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userBucket")) {
                    result.user_bucket = try deserializeUserBucketDetails(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SnapshotInfo {
    var result: SnapshotInfo = undefined;
    result.availability_zone = null;
    result.description = null;
    result.encrypted = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.progress = null;
    result.snapshot_id = null;
    result.sse_type = null;
    result.start_time = null;
    result.state = null;
    result.tags = null;
    result.volume_id = null;
    result.volume_size = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseType")) {
                    result.sse_type = std.meta.stringToEnum(SSEType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(SnapshotState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeSize")) {
                    result.volume_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotRecycleBinInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SnapshotRecycleBinInfo {
    var result: SnapshotRecycleBinInfo = undefined;
    result.description = null;
    result.recycle_bin_enter_time = null;
    result.recycle_bin_exit_time = null;
    result.snapshot_id = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recycleBinEnterTime")) {
                    result.recycle_bin_enter_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recycleBinExitTime")) {
                    result.recycle_bin_exit_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotTaskDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SnapshotTaskDetail {
    var result: SnapshotTaskDetail = undefined;
    result.description = null;
    result.disk_image_size = null;
    result.encrypted = null;
    result.format = null;
    result.kms_key_id = null;
    result.progress = null;
    result.snapshot_id = null;
    result.status = null;
    result.status_message = null;
    result.url = null;
    result.user_bucket = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "diskImageSize")) {
                    result.disk_image_size = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "format")) {
                    result.format = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "url")) {
                    result.url = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userBucket")) {
                    result.user_bucket = try deserializeUserBucketDetails(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSnapshotTierStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SnapshotTierStatus {
    var result: SnapshotTierStatus = undefined;
    result.archival_complete_time = null;
    result.last_tiering_operation_status = null;
    result.last_tiering_operation_status_detail = null;
    result.last_tiering_progress = null;
    result.last_tiering_start_time = null;
    result.owner_id = null;
    result.restore_expiry_time = null;
    result.snapshot_id = null;
    result.status = null;
    result.storage_tier = null;
    result.tags = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "archivalCompleteTime")) {
                    result.archival_complete_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "lastTieringOperationStatus")) {
                    result.last_tiering_operation_status = std.meta.stringToEnum(TieringOperationStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastTieringOperationStatusDetail")) {
                    result.last_tiering_operation_status_detail = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastTieringProgress")) {
                    result.last_tiering_progress = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "lastTieringStartTime")) {
                    result.last_tiering_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "restoreExpiryTime")) {
                    result.restore_expiry_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(SnapshotState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storageTier")) {
                    result.storage_tier = std.meta.stringToEnum(StorageTier, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotCapacityRebalance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotCapacityRebalance {
    _ = alloc;
    var result: SpotCapacityRebalance = undefined;
    result.replacement_strategy = null;
    result.termination_delay = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "replacementStrategy")) {
                    result.replacement_strategy = std.meta.stringToEnum(ReplacementStrategy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "terminationDelay")) {
                    result.termination_delay = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotDatafeedSubscription(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotDatafeedSubscription {
    var result: SpotDatafeedSubscription = undefined;
    result.bucket = null;
    result.fault = null;
    result.owner_id = null;
    result.prefix = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bucket")) {
                    result.bucket = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fault")) {
                    result.fault = try deserializeSpotInstanceStateFault(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(DatafeedSubscriptionState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetLaunchSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotFleetLaunchSpecification {
    var result: SpotFleetLaunchSpecification = undefined;
    result.addressing_type = null;
    result.block_device_mappings = null;
    result.ebs_optimized = null;
    result.iam_instance_profile = null;
    result.image_id = null;
    result.instance_requirements = null;
    result.instance_type = null;
    result.kernel_id = null;
    result.key_name = null;
    result.monitoring = null;
    result.network_interfaces = null;
    result.placement = null;
    result.ramdisk_id = null;
    result.security_groups = null;
    result.spot_price = null;
    result.subnet_id = null;
    result.tag_specifications = null;
    result.user_data = null;
    result.weighted_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "addressingType")) {
                    result.addressing_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDeviceMapping")) {
                    result.block_device_mappings = try deserializeBlockDeviceMappingList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ebsOptimized")) {
                    result.ebs_optimized = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iamInstanceProfile")) {
                    result.iam_instance_profile = try deserializeIamInstanceProfileSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "imageId")) {
                    result.image_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceRequirements")) {
                    result.instance_requirements = try deserializeInstanceRequirements(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "kernelId")) {
                    result.kernel_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "keyName")) {
                    result.key_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "monitoring")) {
                    result.monitoring = try deserializeSpotFleetMonitoring(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceSet")) {
                    result.network_interfaces = try deserializeInstanceNetworkInterfaceSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "placement")) {
                    result.placement = try deserializeSpotPlacement(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ramdiskId")) {
                    result.ramdisk_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.security_groups = try deserializeGroupIdentifierList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSpecificationSet")) {
                    result.tag_specifications = try deserializeSpotFleetTagSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "userData")) {
                    result.user_data = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "weightedCapacity")) {
                    result.weighted_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetMonitoring(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotFleetMonitoring {
    _ = alloc;
    var result: SpotFleetMonitoring = undefined;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetRequestConfig(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotFleetRequestConfig {
    var result: SpotFleetRequestConfig = undefined;
    result.activity_status = null;
    result.create_time = null;
    result.spot_fleet_request_config = null;
    result.spot_fleet_request_id = null;
    result.spot_fleet_request_state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "activityStatus")) {
                    result.activity_status = std.meta.stringToEnum(ActivityStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestConfig")) {
                    result.spot_fleet_request_config = try deserializeSpotFleetRequestConfigData(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestId")) {
                    result.spot_fleet_request_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotFleetRequestState")) {
                    result.spot_fleet_request_state = std.meta.stringToEnum(BatchState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetRequestConfigData(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotFleetRequestConfigData {
    var result: SpotFleetRequestConfigData = undefined;
    result.allocation_strategy = null;
    result.client_token = null;
    result.context = null;
    result.excess_capacity_termination_policy = null;
    result.fulfilled_capacity = null;
    result.instance_interruption_behavior = null;
    result.instance_pools_to_use_count = null;
    result.launch_specifications = null;
    result.launch_template_configs = null;
    result.load_balancers_config = null;
    result.on_demand_allocation_strategy = null;
    result.on_demand_fulfilled_capacity = null;
    result.on_demand_max_total_price = null;
    result.on_demand_target_capacity = null;
    result.replace_unhealthy_instances = null;
    result.spot_maintenance_strategies = null;
    result.spot_max_total_price = null;
    result.spot_price = null;
    result.tag_specifications = null;
    result.target_capacity_unit_type = null;
    result.terminate_instances_with_expiration = null;
    result.@"type" = null;
    result.valid_from = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = std.meta.stringToEnum(AllocationStrategy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientToken")) {
                    result.client_token = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "context")) {
                    result.context = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "excessCapacityTerminationPolicy")) {
                    result.excess_capacity_termination_policy = std.meta.stringToEnum(ExcessCapacityTerminationPolicy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fulfilledCapacity")) {
                    result.fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "iamFleetRole")) {
                    result.iam_fleet_role = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = std.meta.stringToEnum(InstanceInterruptionBehavior, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePoolsToUseCount")) {
                    result.instance_pools_to_use_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "launchSpecifications")) {
                    result.launch_specifications = try deserializeLaunchSpecsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "launchTemplateConfigs")) {
                    result.launch_template_configs = try deserializeLaunchTemplateConfigList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "loadBalancersConfig")) {
                    result.load_balancers_config = try deserializeLoadBalancersConfig(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "onDemandAllocationStrategy")) {
                    result.on_demand_allocation_strategy = std.meta.stringToEnum(OnDemandAllocationStrategy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "onDemandFulfilledCapacity")) {
                    result.on_demand_fulfilled_capacity = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "onDemandMaxTotalPrice")) {
                    result.on_demand_max_total_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "onDemandTargetCapacity")) {
                    result.on_demand_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "replaceUnhealthyInstances")) {
                    result.replace_unhealthy_instances = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "spotMaintenanceStrategies")) {
                    result.spot_maintenance_strategies = try deserializeSpotMaintenanceStrategies(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "spotMaxTotalPrice")) {
                    result.spot_max_total_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "TagSpecification")) {
                    result.tag_specifications = try deserializeTagSpecificationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "targetCapacity")) {
                    result.target_capacity = try std.fmt.parseInt(i32, try reader.readElementText(), 10);
                } else if (std.mem.eql(u8, e.local, "targetCapacityUnitType")) {
                    result.target_capacity_unit_type = std.meta.stringToEnum(TargetCapacityUnitType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "terminateInstancesWithExpiration")) {
                    result.terminate_instances_with_expiration = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(FleetType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validFrom")) {
                    result.valid_from = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotFleetTagSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotFleetTagSpecification {
    var result: SpotFleetTagSpecification = undefined;
    result.resource_type = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(ResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tag")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotInstanceRequest(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotInstanceRequest {
    var result: SpotInstanceRequest = undefined;
    result.actual_block_hourly_price = null;
    result.availability_zone_group = null;
    result.block_duration_minutes = null;
    result.create_time = null;
    result.fault = null;
    result.instance_id = null;
    result.instance_interruption_behavior = null;
    result.launched_availability_zone = null;
    result.launched_availability_zone_id = null;
    result.launch_group = null;
    result.launch_specification = null;
    result.product_description = null;
    result.spot_instance_request_id = null;
    result.spot_price = null;
    result.state = null;
    result.status = null;
    result.tags = null;
    result.@"type" = null;
    result.valid_from = null;
    result.valid_until = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "actualBlockHourlyPrice")) {
                    result.actual_block_hourly_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneGroup")) {
                    result.availability_zone_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "blockDurationMinutes")) {
                    result.block_duration_minutes = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "fault")) {
                    result.fault = try deserializeSpotInstanceStateFault(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = std.meta.stringToEnum(InstanceInterruptionBehavior, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchedAvailabilityZone")) {
                    result.launched_availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchedAvailabilityZoneId")) {
                    result.launched_availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchGroup")) {
                    result.launch_group = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "launchSpecification")) {
                    result.launch_specification = try deserializeLaunchSpecification(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = std.meta.stringToEnum(RIProductDescription, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotInstanceRequestId")) {
                    result.spot_instance_request_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(SpotInstanceState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeSpotInstanceStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(SpotInstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "validFrom")) {
                    result.valid_from = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "validUntil")) {
                    result.valid_until = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotInstanceStateFault(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotInstanceStateFault {
    var result: SpotInstanceStateFault = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotInstanceStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotInstanceStatus {
    var result: SpotInstanceStatus = undefined;
    result.code = null;
    result.message = null;
    result.update_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updateTime")) {
                    result.update_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotMaintenanceStrategies(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotMaintenanceStrategies {
    var result: SpotMaintenanceStrategies = undefined;
    result.capacity_rebalance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "capacityRebalance")) {
                    result.capacity_rebalance = try deserializeSpotCapacityRebalance(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotOptions {
    var result: SpotOptions = undefined;
    result.allocation_strategy = null;
    result.instance_interruption_behavior = null;
    result.instance_pools_to_use_count = null;
    result.maintenance_strategies = null;
    result.max_total_price = null;
    result.min_target_capacity = null;
    result.single_availability_zone = null;
    result.single_instance_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allocationStrategy")) {
                    result.allocation_strategy = std.meta.stringToEnum(SpotAllocationStrategy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceInterruptionBehavior")) {
                    result.instance_interruption_behavior = std.meta.stringToEnum(SpotInstanceInterruptionBehavior, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instancePoolsToUseCount")) {
                    result.instance_pools_to_use_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "maintenanceStrategies")) {
                    result.maintenance_strategies = try deserializeFleetSpotMaintenanceStrategies(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "maxTotalPrice")) {
                    result.max_total_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "minTargetCapacity")) {
                    result.min_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "singleAvailabilityZone")) {
                    result.single_availability_zone = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "singleInstanceType")) {
                    result.single_instance_type = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotPlacement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotPlacement {
    var result: SpotPlacement = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.group_name = null;
    result.tenancy = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tenancy")) {
                    result.tenancy = std.meta.stringToEnum(Tenancy, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotPlacementScore(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotPlacementScore {
    var result: SpotPlacementScore = undefined;
    result.availability_zone_id = null;
    result.region = null;
    result.score = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "score")) {
                    result.score = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSpotPrice(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SpotPrice {
    var result: SpotPrice = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.instance_type = null;
    result.product_description = null;
    result.spot_price = null;
    result.timestamp = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceType")) {
                    result.instance_type = std.meta.stringToEnum(InstanceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "productDescription")) {
                    result.product_description = std.meta.stringToEnum(RIProductDescription, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "spotPrice")) {
                    result.spot_price = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "timestamp")) {
                    result.timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStaleIpPermission(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !StaleIpPermission {
    var result: StaleIpPermission = undefined;
    result.from_port = null;
    result.ip_protocol = null;
    result.ip_ranges = null;
    result.prefix_list_ids = null;
    result.to_port = null;
    result.user_id_group_pairs = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipProtocol")) {
                    result.ip_protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipRanges")) {
                    result.ip_ranges = try deserializeIpRanges(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "prefixListIds")) {
                    result.prefix_list_ids = try deserializePrefixListIdSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "groups")) {
                    result.user_id_group_pairs = try deserializeUserIdGroupPairSet(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStaleSecurityGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !StaleSecurityGroup {
    var result: StaleSecurityGroup = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.stale_ip_permissions = null;
    result.stale_ip_permissions_egress = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staleIpPermissions")) {
                    result.stale_ip_permissions = try deserializeStaleIpPermissionSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "staleIpPermissionsEgress")) {
                    result.stale_ip_permissions_egress = try deserializeStaleIpPermissionSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStateReason(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !StateReason {
    var result: StateReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStorage(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Storage {
    var result: Storage = undefined;
    result.s3 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "S3")) {
                    result.s3 = try deserializeS3Storage(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeStoreImageTaskResult(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !StoreImageTaskResult {
    var result: StoreImageTaskResult = undefined;
    result.ami_id = null;
    result.bucket = null;
    result.progress_percentage = null;
    result.s3_object_key = null;
    result.store_task_failure_reason = null;
    result.store_task_state = null;
    result.task_start_time = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amiId")) {
                    result.ami_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bucket")) {
                    result.bucket = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progressPercentage")) {
                    result.progress_percentage = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "s3objectKey")) {
                    result.s3_object_key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storeTaskFailureReason")) {
                    result.store_task_failure_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "storeTaskState")) {
                    result.store_task_state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "taskStartTime")) {
                    result.task_start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnet(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Subnet {
    var result: Subnet = undefined;
    result.assign_ipv_6_address_on_creation = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.available_ip_address_count = null;
    result.block_public_access_states = null;
    result.cidr_block = null;
    result.customer_owned_ipv_4_pool = null;
    result.default_for_az = null;
    result.enable_dns_64 = null;
    result.enable_lni_at_device_index = null;
    result.ipv_6_cidr_block_association_set = null;
    result.ipv_6_native = null;
    result.map_customer_owned_ip_on_launch = null;
    result.map_public_ip_on_launch = null;
    result.outpost_arn = null;
    result.owner_id = null;
    result.private_dns_name_options_on_launch = null;
    result.state = null;
    result.subnet_arn = null;
    result.subnet_id = null;
    result.tags = null;
    result.@"type" = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "assignIpv6AddressOnCreation")) {
                    result.assign_ipv_6_address_on_creation = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availableIpAddressCount")) {
                    result.available_ip_address_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "blockPublicAccessStates")) {
                    result.block_public_access_states = try deserializeBlockPublicAccessStates(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerOwnedIpv4Pool")) {
                    result.customer_owned_ipv_4_pool = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultForAz")) {
                    result.default_for_az = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableDns64")) {
                    result.enable_dns_64 = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "enableLniAtDeviceIndex")) {
                    result.enable_lni_at_device_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockAssociationSet")) {
                    result.ipv_6_cidr_block_association_set = try deserializeSubnetIpv6CidrBlockAssociationSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6Native")) {
                    result.ipv_6_native = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "mapCustomerOwnedIpOnLaunch")) {
                    result.map_customer_owned_ip_on_launch = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "mapPublicIpOnLaunch")) {
                    result.map_public_ip_on_launch = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsNameOptionsOnLaunch")) {
                    result.private_dns_name_options_on_launch = try deserializePrivateDnsNameOptionsOnLaunch(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(SubnetState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetArn")) {
                    result.subnet_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SubnetAssociation {
    var result: SubnetAssociation = undefined;
    result.state = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayMulitcastDomainAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetCidrBlockState(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SubnetCidrBlockState {
    var result: SubnetCidrBlockState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(SubnetCidrBlockStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetCidrReservation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SubnetCidrReservation {
    var result: SubnetCidrReservation = undefined;
    result.cidr = null;
    result.description = null;
    result.owner_id = null;
    result.reservation_type = null;
    result.subnet_cidr_reservation_id = null;
    result.subnet_id = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reservationType")) {
                    result.reservation_type = std.meta.stringToEnum(SubnetCidrReservationType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetCidrReservationId")) {
                    result.subnet_cidr_reservation_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetIpPrefixes(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SubnetIpPrefixes {
    var result: SubnetIpPrefixes = undefined;
    result.ip_prefixes = null;
    result.subnet_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "ipPrefixSet")) {
                    result.ip_prefixes = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubnetIpv6CidrBlockAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SubnetIpv6CidrBlockAssociation {
    var result: SubnetIpv6CidrBlockAssociation = undefined;
    result.association_id = null;
    result.ip_source = null;
    result.ipv_6_address_attribute = null;
    result.ipv_6_cidr_block = null;
    result.ipv_6_cidr_block_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipSource")) {
                    result.ip_source = std.meta.stringToEnum(IpSource, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6AddressAttribute")) {
                    result.ipv_6_address_attribute = std.meta.stringToEnum(Ipv6AddressAttribute, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockState")) {
                    result.ipv_6_cidr_block_state = try deserializeSubnetCidrBlockState(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSubscription(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Subscription {
    var result: Subscription = undefined;
    result.destination = null;
    result.metric = null;
    result.period = null;
    result.source = null;
    result.statistic = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destination")) {
                    result.destination = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metric")) {
                    result.metric = std.meta.stringToEnum(MetricType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "period")) {
                    result.period = std.meta.stringToEnum(PeriodType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statistic")) {
                    result.statistic = std.meta.stringToEnum(StatisticType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSuccessfulInstanceCreditSpecificationItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SuccessfulInstanceCreditSpecificationItem {
    var result: SuccessfulInstanceCreditSpecificationItem = undefined;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSuccessfulQueuedPurchaseDeletion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SuccessfulQueuedPurchaseDeletion {
    var result: SuccessfulQueuedPurchaseDeletion = undefined;
    result.reserved_instances_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservedInstancesId")) {
                    result.reserved_instances_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeSupportedRegionDetail(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !SupportedRegionDetail {
    var result: SupportedRegionDetail = undefined;
    result.region = null;
    result.service_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceState")) {
                    result.service_state = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTag(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Tag {
    var result: Tag = undefined;
    result.key = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTagDescription(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TagDescription {
    var result: TagDescription = undefined;
    result.key = null;
    result.resource_id = null;
    result.resource_type = null;
    result.value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "key")) {
                    result.key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(ResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "value")) {
                    result.value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTagSpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TagSpecification {
    var result: TagSpecification = undefined;
    result.resource_type = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(ResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "Tag")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetCapacitySpecification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TargetCapacitySpecification {
    _ = alloc;
    var result: TargetCapacitySpecification = undefined;
    result.default_target_capacity_type = null;
    result.on_demand_target_capacity = null;
    result.spot_target_capacity = null;
    result.target_capacity_unit_type = null;
    result.total_target_capacity = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "defaultTargetCapacityType")) {
                    result.default_target_capacity_type = std.meta.stringToEnum(DefaultTargetCapacityType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "onDemandTargetCapacity")) {
                    result.on_demand_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "spotTargetCapacity")) {
                    result.spot_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetCapacityUnitType")) {
                    result.target_capacity_unit_type = std.meta.stringToEnum(TargetCapacityUnitType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "totalTargetCapacity")) {
                    result.total_target_capacity = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TargetConfiguration {
    var result: TargetConfiguration = undefined;
    result.instance_count = null;
    result.offering_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceCount")) {
                    result.instance_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "offeringId")) {
                    result.offering_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TargetGroup {
    var result: TargetGroup = undefined;
    result.arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "arn")) {
                    result.arn = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetGroupsConfig(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TargetGroupsConfig {
    var result: TargetGroupsConfig = undefined;
    result.target_groups = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "targetGroups")) {
                    result.target_groups = try deserializeTargetGroups(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetNetwork(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TargetNetwork {
    var result: TargetNetwork = undefined;
    result.association_id = null;
    result.client_vpn_endpoint_id = null;
    result.security_groups = null;
    result.status = null;
    result.target_network_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientVpnEndpointId")) {
                    result.client_vpn_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroups")) {
                    result.security_groups = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeAssociationStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "targetNetworkId")) {
                    result.target_network_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTargetReservationValue(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TargetReservationValue {
    var result: TargetReservationValue = undefined;
    result.reservation_value = null;
    result.target_configuration = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "reservationValue")) {
                    result.reservation_value = try deserializeReservationValue(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "targetConfiguration")) {
                    result.target_configuration = try deserializeTargetConfiguration(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTerminateConnectionStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TerminateConnectionStatus {
    var result: TerminateConnectionStatus = undefined;
    result.connection_id = null;
    result.current_status = null;
    result.previous_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "connectionId")) {
                    result.connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "currentStatus")) {
                    result.current_status = try deserializeClientVpnConnectionStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "previousStatus")) {
                    result.previous_status = try deserializeClientVpnConnectionStatus(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeThroughResourcesStatement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ThroughResourcesStatement {
    var result: ThroughResourcesStatement = undefined;
    result.resource_statement = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceStatement")) {
                    result.resource_statement = try deserializeResourceStatement(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTotalLocalStorageGB(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TotalLocalStorageGB {
    _ = alloc;
    var result: TotalLocalStorageGB = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseFloat(f64, try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorFilter(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TrafficMirrorFilter {
    var result: TrafficMirrorFilter = undefined;
    result.description = null;
    result.egress_filter_rules = null;
    result.ingress_filter_rules = null;
    result.network_services = null;
    result.tags = null;
    result.traffic_mirror_filter_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "egressFilterRuleSet")) {
                    result.egress_filter_rules = try deserializeTrafficMirrorFilterRuleList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ingressFilterRuleSet")) {
                    result.ingress_filter_rules = try deserializeTrafficMirrorFilterRuleList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "networkServiceSet")) {
                    result.network_services = try deserializeTrafficMirrorNetworkServiceList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterId")) {
                    result.traffic_mirror_filter_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorFilterRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TrafficMirrorFilterRule {
    var result: TrafficMirrorFilterRule = undefined;
    result.description = null;
    result.destination_cidr_block = null;
    result.destination_port_range = null;
    result.protocol = null;
    result.rule_action = null;
    result.rule_number = null;
    result.source_cidr_block = null;
    result.source_port_range = null;
    result.tags = null;
    result.traffic_direction = null;
    result.traffic_mirror_filter_id = null;
    result.traffic_mirror_filter_rule_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try deserializeTrafficMirrorPortRange(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "ruleAction")) {
                    result.rule_action = std.meta.stringToEnum(TrafficMirrorRuleAction, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ruleNumber")) {
                    result.rule_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "sourceCidrBlock")) {
                    result.source_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try deserializeTrafficMirrorPortRange(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trafficDirection")) {
                    result.traffic_direction = std.meta.stringToEnum(TrafficDirection, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterId")) {
                    result.traffic_mirror_filter_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterRuleId")) {
                    result.traffic_mirror_filter_rule_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorPortRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TrafficMirrorPortRange {
    _ = alloc;
    var result: TrafficMirrorPortRange = undefined;
    result.from_port = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorSession(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TrafficMirrorSession {
    var result: TrafficMirrorSession = undefined;
    result.description = null;
    result.network_interface_id = null;
    result.owner_id = null;
    result.packet_length = null;
    result.session_number = null;
    result.tags = null;
    result.traffic_mirror_filter_id = null;
    result.traffic_mirror_session_id = null;
    result.traffic_mirror_target_id = null;
    result.virtual_network_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "packetLength")) {
                    result.packet_length = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "sessionNumber")) {
                    result.session_number = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trafficMirrorFilterId")) {
                    result.traffic_mirror_filter_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorSessionId")) {
                    result.traffic_mirror_session_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trafficMirrorTargetId")) {
                    result.traffic_mirror_target_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "virtualNetworkId")) {
                    result.virtual_network_id = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrafficMirrorTarget(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TrafficMirrorTarget {
    var result: TrafficMirrorTarget = undefined;
    result.description = null;
    result.gateway_load_balancer_endpoint_id = null;
    result.network_interface_id = null;
    result.network_load_balancer_arn = null;
    result.owner_id = null;
    result.tags = null;
    result.traffic_mirror_target_id = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayLoadBalancerEndpointId")) {
                    result.gateway_load_balancer_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkLoadBalancerArn")) {
                    result.network_load_balancer_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trafficMirrorTargetId")) {
                    result.traffic_mirror_target_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(TrafficMirrorTargetType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGateway {
    var result: TransitGateway = undefined;
    result.creation_time = null;
    result.description = null;
    result.options = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_arn = null;
    result.transit_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayArn")) {
                    result.transit_gateway_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayAssociation {
    var result: TransitGatewayAssociation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayAttachment {
    var result: TransitGatewayAttachment = undefined;
    result.association = null;
    result.creation_time = null;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.transit_gateway_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "association")) {
                    result.association = try deserializeTransitGatewayAttachmentAssociation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAttachmentState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayOwnerId")) {
                    result.transit_gateway_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachmentAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayAttachmentAssociation {
    var result: TransitGatewayAttachmentAssociation = undefined;
    result.state = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachmentBgpConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayAttachmentBgpConfiguration {
    var result: TransitGatewayAttachmentBgpConfiguration = undefined;
    result.bgp_status = null;
    result.peer_address = null;
    result.peer_asn = null;
    result.transit_gateway_address = null;
    result.transit_gateway_asn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpStatus")) {
                    result.bgp_status = std.meta.stringToEnum(BgpStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerAsn")) {
                    result.peer_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "transitGatewayAddress")) {
                    result.transit_gateway_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAsn")) {
                    result.transit_gateway_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayAttachmentPropagation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayAttachmentPropagation {
    var result: TransitGatewayAttachmentPropagation = undefined;
    result.state = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayPropagationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnect(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayConnect {
    var result: TransitGatewayConnect = undefined;
    result.creation_time = null;
    result.options = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.transport_transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayConnectOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAttachmentState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transportTransitGatewayAttachmentId")) {
                    result.transport_transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnectOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayConnectOptions {
    _ = alloc;
    var result: TransitGatewayConnectOptions = undefined;
    result.protocol = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.meta.stringToEnum(ProtocolValue, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnectPeer(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayConnectPeer {
    var result: TransitGatewayConnectPeer = undefined;
    result.connect_peer_configuration = null;
    result.creation_time = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_connect_peer_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "connectPeerConfiguration")) {
                    result.connect_peer_configuration = try deserializeTransitGatewayConnectPeerConfiguration(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayConnectPeerState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayConnectPeerId")) {
                    result.transit_gateway_connect_peer_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayConnectPeerConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayConnectPeerConfiguration {
    var result: TransitGatewayConnectPeerConfiguration = undefined;
    result.bgp_configurations = null;
    result.inside_cidr_blocks = null;
    result.peer_address = null;
    result.protocol = null;
    result.transit_gateway_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bgpConfigurations")) {
                    result.bgp_configurations = try deserializeTransitGatewayAttachmentBgpConfigurationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "insideCidrBlocks")) {
                    result.inside_cidr_blocks = try deserializeInsideCidrBlocksStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "peerAddress")) {
                    result.peer_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.meta.stringToEnum(ProtocolValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAddress")) {
                    result.transit_gateway_address = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMeteringPolicy(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMeteringPolicy {
    var result: TransitGatewayMeteringPolicy = undefined;
    result.middlebox_attachment_ids = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_metering_policy_id = null;
    result.update_effective_at = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "middleboxAttachmentIdSet")) {
                    result.middlebox_attachment_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayMeteringPolicyState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMeteringPolicyId")) {
                    result.transit_gateway_metering_policy_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updateEffectiveAt")) {
                    result.update_effective_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMeteringPolicyEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMeteringPolicyEntry {
    var result: TransitGatewayMeteringPolicyEntry = undefined;
    result.metered_account = null;
    result.metering_policy_rule = null;
    result.policy_rule_number = null;
    result.state = null;
    result.updated_at = null;
    result.update_effective_at = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "meteredAccount")) {
                    result.metered_account = std.meta.stringToEnum(TransitGatewayMeteringPayerType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "meteringPolicyRule")) {
                    result.metering_policy_rule = try deserializeTransitGatewayMeteringPolicyRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "policyRuleNumber")) {
                    result.policy_rule_number = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayMeteringPolicyEntryState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "updatedAt")) {
                    result.updated_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "updateEffectiveAt")) {
                    result.update_effective_at = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMeteringPolicyRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMeteringPolicyRule {
    var result: TransitGatewayMeteringPolicyRule = undefined;
    result.destination_cidr_block = null;
    result.destination_port_range = null;
    result.destination_transit_gateway_attachment_id = null;
    result.destination_transit_gateway_attachment_type = null;
    result.protocol = null;
    result.source_cidr_block = null;
    result.source_port_range = null;
    result.source_transit_gateway_attachment_id = null;
    result.source_transit_gateway_attachment_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationTransitGatewayAttachmentId")) {
                    result.destination_transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationTransitGatewayAttachmentType")) {
                    result.destination_transit_gateway_attachment_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceCidrBlock")) {
                    result.source_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceTransitGatewayAttachmentId")) {
                    result.source_transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceTransitGatewayAttachmentType")) {
                    result.source_transit_gateway_attachment_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDeregisteredGroupMembers(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastDeregisteredGroupMembers {
    var result: TransitGatewayMulticastDeregisteredGroupMembers = undefined;
    result.deregistered_network_interface_ids = null;
    result.group_ip_address = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deregisteredNetworkInterfaceIds")) {
                    result.deregistered_network_interface_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDeregisteredGroupSources(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastDeregisteredGroupSources {
    var result: TransitGatewayMulticastDeregisteredGroupSources = undefined;
    result.deregistered_network_interface_ids = null;
    result.group_ip_address = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deregisteredNetworkInterfaceIds")) {
                    result.deregistered_network_interface_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomain(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastDomain {
    var result: TransitGatewayMulticastDomain = undefined;
    result.creation_time = null;
    result.options = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_multicast_domain_arn = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayMulticastDomainOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayMulticastDomainState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainArn")) {
                    result.transit_gateway_multicast_domain_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomainAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastDomainAssociation {
    var result: TransitGatewayMulticastDomainAssociation = undefined;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.subnet = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnet")) {
                    result.subnet = try deserializeSubnetAssociation(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomainAssociations(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastDomainAssociations {
    var result: TransitGatewayMulticastDomainAssociations = undefined;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.subnets = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnets")) {
                    result.subnets = try deserializeSubnetAssociationList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastDomainOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastDomainOptions {
    _ = alloc;
    var result: TransitGatewayMulticastDomainOptions = undefined;
    result.auto_accept_shared_associations = null;
    result.igmpv_2_support = null;
    result.static_sources_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "autoAcceptSharedAssociations")) {
                    result.auto_accept_shared_associations = std.meta.stringToEnum(AutoAcceptSharedAssociationsValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "igmpv2Support")) {
                    result.igmpv_2_support = std.meta.stringToEnum(Igmpv2SupportValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staticSourcesSupport")) {
                    result.static_sources_support = std.meta.stringToEnum(StaticSourcesSupportValue, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastGroup {
    var result: TransitGatewayMulticastGroup = undefined;
    result.group_ip_address = null;
    result.group_member = null;
    result.group_source = null;
    result.member_type = null;
    result.network_interface_id = null;
    result.resource_id = null;
    result.resource_owner_id = null;
    result.resource_type = null;
    result.source_type = null;
    result.subnet_id = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupMember")) {
                    result.group_member = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "groupSource")) {
                    result.group_source = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "memberType")) {
                    result.member_type = std.meta.stringToEnum(MembershipType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceOwnerId")) {
                    result.resource_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceType")) {
                    result.source_type = std.meta.stringToEnum(MembershipType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetId")) {
                    result.subnet_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastRegisteredGroupMembers(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastRegisteredGroupMembers {
    var result: TransitGatewayMulticastRegisteredGroupMembers = undefined;
    result.group_ip_address = null;
    result.registered_network_interface_ids = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "registeredNetworkInterfaceIds")) {
                    result.registered_network_interface_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayMulticastRegisteredGroupSources(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayMulticastRegisteredGroupSources {
    var result: TransitGatewayMulticastRegisteredGroupSources = undefined;
    result.group_ip_address = null;
    result.registered_network_interface_ids = null;
    result.transit_gateway_multicast_domain_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "groupIpAddress")) {
                    result.group_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "registeredNetworkInterfaceIds")) {
                    result.registered_network_interface_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayMulticastDomainId")) {
                    result.transit_gateway_multicast_domain_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayOptions {
    var result: TransitGatewayOptions = undefined;
    result.amazon_side_asn = null;
    result.association_default_route_table_id = null;
    result.auto_accept_shared_attachments = null;
    result.default_route_table_association = null;
    result.default_route_table_propagation = null;
    result.dns_support = null;
    result.encryption_support = null;
    result.multicast_support = null;
    result.propagation_default_route_table_id = null;
    result.security_group_referencing_support = null;
    result.transit_gateway_cidr_blocks = null;
    result.vpn_ecmp_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amazonSideAsn")) {
                    result.amazon_side_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "associationDefaultRouteTableId")) {
                    result.association_default_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "autoAcceptSharedAttachments")) {
                    result.auto_accept_shared_attachments = std.meta.stringToEnum(AutoAcceptSharedAttachmentsValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultRouteTableAssociation")) {
                    result.default_route_table_association = std.meta.stringToEnum(DefaultRouteTableAssociationValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "defaultRouteTablePropagation")) {
                    result.default_route_table_propagation = std.meta.stringToEnum(DefaultRouteTablePropagationValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsSupport")) {
                    result.dns_support = std.meta.stringToEnum(DnsSupportValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionSupport")) {
                    result.encryption_support = try deserializeEncryptionSupport(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "multicastSupport")) {
                    result.multicast_support = std.meta.stringToEnum(MulticastSupportValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "propagationDefaultRouteTableId")) {
                    result.propagation_default_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupReferencingSupport")) {
                    result.security_group_referencing_support = std.meta.stringToEnum(SecurityGroupReferencingSupportValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayCidrBlocks")) {
                    result.transit_gateway_cidr_blocks = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpnEcmpSupport")) {
                    result.vpn_ecmp_support = std.meta.stringToEnum(VpnEcmpSupportValue, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPeeringAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPeeringAttachment {
    var result: TransitGatewayPeeringAttachment = undefined;
    result.accepter_tgw_info = null;
    result.accepter_transit_gateway_attachment_id = null;
    result.creation_time = null;
    result.options = null;
    result.requester_tgw_info = null;
    result.state = null;
    result.status = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accepterTgwInfo")) {
                    result.accepter_tgw_info = try deserializePeeringTgwInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "accepterTransitGatewayAttachmentId")) {
                    result.accepter_transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayPeeringAttachmentOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "requesterTgwInfo")) {
                    result.requester_tgw_info = try deserializePeeringTgwInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAttachmentState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializePeeringAttachmentStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPeeringAttachmentOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPeeringAttachmentOptions {
    _ = alloc;
    var result: TransitGatewayPeeringAttachmentOptions = undefined;
    result.dynamic_routing = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dynamicRouting")) {
                    result.dynamic_routing = std.meta.stringToEnum(DynamicRoutingValue, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyRule(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPolicyRule {
    var result: TransitGatewayPolicyRule = undefined;
    result.destination_cidr_block = null;
    result.destination_port_range = null;
    result.meta_data = null;
    result.protocol = null;
    result.source_cidr_block = null;
    result.source_port_range = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationPortRange")) {
                    result.destination_port_range = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metaData")) {
                    result.meta_data = try deserializeTransitGatewayPolicyRuleMetaData(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceCidrBlock")) {
                    result.source_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourcePortRange")) {
                    result.source_port_range = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyRuleMetaData(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPolicyRuleMetaData {
    var result: TransitGatewayPolicyRuleMetaData = undefined;
    result.meta_data_key = null;
    result.meta_data_value = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "metaDataKey")) {
                    result.meta_data_key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "metaDataValue")) {
                    result.meta_data_value = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyTable(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPolicyTable {
    var result: TransitGatewayPolicyTable = undefined;
    result.creation_time = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_policy_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayPolicyTableState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayPolicyTableId")) {
                    result.transit_gateway_policy_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyTableAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPolicyTableAssociation {
    var result: TransitGatewayPolicyTableAssociation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_policy_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayPolicyTableId")) {
                    result.transit_gateway_policy_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPolicyTableEntry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPolicyTableEntry {
    var result: TransitGatewayPolicyTableEntry = undefined;
    result.policy_rule = null;
    result.policy_rule_number = null;
    result.target_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "policyRule")) {
                    result.policy_rule = try deserializeTransitGatewayPolicyRule(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "policyRuleNumber")) {
                    result.policy_rule_number = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetRouteTableId")) {
                    result.target_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPrefixListAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPrefixListAttachment {
    var result: TransitGatewayPrefixListAttachment = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPrefixListReference(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPrefixListReference {
    var result: TransitGatewayPrefixListReference = undefined;
    result.blackhole = null;
    result.prefix_list_id = null;
    result.prefix_list_owner_id = null;
    result.state = null;
    result.transit_gateway_attachment = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blackhole")) {
                    result.blackhole = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListOwnerId")) {
                    result.prefix_list_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayPrefixListReferenceState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachment")) {
                    result.transit_gateway_attachment = try deserializeTransitGatewayPrefixListAttachment(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayPropagation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayPropagation {
    var result: TransitGatewayPropagation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_route_table_announcement_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayPropagationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayRoute {
    var result: TransitGatewayRoute = undefined;
    result.destination_cidr_block = null;
    result.prefix_list_id = null;
    result.state = null;
    result.transit_gateway_attachments = null;
    result.transit_gateway_route_table_announcement_id = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayRouteState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachments")) {
                    result.transit_gateway_attachments = try deserializeTransitGatewayRouteAttachmentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(TransitGatewayRouteType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayRouteAttachment {
    var result: TransitGatewayRouteAttachment = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTable(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayRouteTable {
    var result: TransitGatewayRouteTable = undefined;
    result.creation_time = null;
    result.default_association_route_table = null;
    result.default_propagation_route_table = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultAssociationRouteTable")) {
                    result.default_association_route_table = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "defaultPropagationRouteTable")) {
                    result.default_propagation_route_table = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayRouteTableState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTableAnnouncement(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayRouteTableAnnouncement {
    var result: TransitGatewayRouteTableAnnouncement = undefined;
    result.announcement_direction = null;
    result.core_network_id = null;
    result.creation_time = null;
    result.peer_core_network_id = null;
    result.peering_attachment_id = null;
    result.peer_transit_gateway_id = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.transit_gateway_route_table_announcement_id = null;
    result.transit_gateway_route_table_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "announcementDirection")) {
                    result.announcement_direction = std.meta.stringToEnum(TransitGatewayRouteTableAnnouncementDirection, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkId")) {
                    result.core_network_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "peerCoreNetworkId")) {
                    result.peer_core_network_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringAttachmentId")) {
                    result.peering_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peerTransitGatewayId")) {
                    result.peer_transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayRouteTableAnnouncementState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableId")) {
                    result.transit_gateway_route_table_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTableAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayRouteTableAssociation {
    var result: TransitGatewayRouteTableAssociation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTablePropagation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayRouteTablePropagation {
    var result: TransitGatewayRouteTablePropagation = undefined;
    result.resource_id = null;
    result.resource_type = null;
    result.state = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_route_table_announcement_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = std.meta.stringToEnum(TransitGatewayAttachmentResourceType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayPropagationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayRouteTableAnnouncementId")) {
                    result.transit_gateway_route_table_announcement_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayRouteTableRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayRouteTableRoute {
    var result: TransitGatewayRouteTableRoute = undefined;
    result.attachment_id = null;
    result.destination_cidr = null;
    result.prefix_list_id = null;
    result.resource_id = null;
    result.resource_type = null;
    result.route_origin = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentId")) {
                    result.attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "destinationCidr")) {
                    result.destination_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "prefixListId")) {
                    result.prefix_list_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceType")) {
                    result.resource_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeOrigin")) {
                    result.route_origin = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayVpcAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayVpcAttachment {
    var result: TransitGatewayVpcAttachment = undefined;
    result.creation_time = null;
    result.options = null;
    result.state = null;
    result.subnet_ids = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.vpc_id = null;
    result.vpc_owner_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeTransitGatewayVpcAttachmentOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(TransitGatewayAttachmentState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIds")) {
                    result.subnet_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcOwnerId")) {
                    result.vpc_owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTransitGatewayVpcAttachmentOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TransitGatewayVpcAttachmentOptions {
    _ = alloc;
    var result: TransitGatewayVpcAttachmentOptions = undefined;
    result.appliance_mode_support = null;
    result.dns_support = null;
    result.ipv_6_support = null;
    result.security_group_referencing_support = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "applianceModeSupport")) {
                    result.appliance_mode_support = std.meta.stringToEnum(ApplianceModeSupportValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsSupport")) {
                    result.dns_support = std.meta.stringToEnum(DnsSupportValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6Support")) {
                    result.ipv_6_support = std.meta.stringToEnum(Ipv6SupportValue, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "securityGroupReferencingSupport")) {
                    result.security_group_referencing_support = std.meta.stringToEnum(SecurityGroupReferencingSupportValue, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTrunkInterfaceAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TrunkInterfaceAssociation {
    var result: TrunkInterfaceAssociation = undefined;
    result.association_id = null;
    result.branch_interface_id = null;
    result.gre_key = null;
    result.interface_protocol = null;
    result.tags = null;
    result.trunk_interface_id = null;
    result.vlan_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "branchInterfaceId")) {
                    result.branch_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "greKey")) {
                    result.gre_key = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "interfaceProtocol")) {
                    result.interface_protocol = std.meta.stringToEnum(InterfaceProtocolType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trunkInterfaceId")) {
                    result.trunk_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vlanId")) {
                    result.vlan_id = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeTunnelOption(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !TunnelOption {
    var result: TunnelOption = undefined;
    result.dpd_timeout_action = null;
    result.dpd_timeout_seconds = null;
    result.enable_tunnel_lifecycle_control = null;
    result.ike_versions = null;
    result.log_options = null;
    result.outside_ip_address = null;
    result.phase_1_dh_group_numbers = null;
    result.phase_1_encryption_algorithms = null;
    result.phase_1_integrity_algorithms = null;
    result.phase_1_lifetime_seconds = null;
    result.phase_2_dh_group_numbers = null;
    result.phase_2_encryption_algorithms = null;
    result.phase_2_integrity_algorithms = null;
    result.phase_2_lifetime_seconds = null;
    result.pre_shared_key = null;
    result.rekey_fuzz_percentage = null;
    result.rekey_margin_time_seconds = null;
    result.replay_window_size = null;
    result.startup_action = null;
    result.tunnel_inside_cidr = null;
    result.tunnel_inside_ipv_6_cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "dpdTimeoutAction")) {
                    result.dpd_timeout_action = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dpdTimeoutSeconds")) {
                    result.dpd_timeout_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "enableTunnelLifecycleControl")) {
                    result.enable_tunnel_lifecycle_control = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ikeVersionSet")) {
                    result.ike_versions = try deserializeIKEVersionsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "logOptions")) {
                    result.log_options = try deserializeVpnTunnelLogOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outsideIpAddress")) {
                    result.outside_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "phase1DHGroupNumberSet")) {
                    result.phase_1_dh_group_numbers = try deserializePhase1DHGroupNumbersList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "phase1EncryptionAlgorithmSet")) {
                    result.phase_1_encryption_algorithms = try deserializePhase1EncryptionAlgorithmsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "phase1IntegrityAlgorithmSet")) {
                    result.phase_1_integrity_algorithms = try deserializePhase1IntegrityAlgorithmsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "phase1LifetimeSeconds")) {
                    result.phase_1_lifetime_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "phase2DHGroupNumberSet")) {
                    result.phase_2_dh_group_numbers = try deserializePhase2DHGroupNumbersList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "phase2EncryptionAlgorithmSet")) {
                    result.phase_2_encryption_algorithms = try deserializePhase2EncryptionAlgorithmsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "phase2IntegrityAlgorithmSet")) {
                    result.phase_2_integrity_algorithms = try deserializePhase2IntegrityAlgorithmsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "phase2LifetimeSeconds")) {
                    result.phase_2_lifetime_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "preSharedKey")) {
                    result.pre_shared_key = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rekeyFuzzPercentage")) {
                    result.rekey_fuzz_percentage = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "rekeyMarginTimeSeconds")) {
                    result.rekey_margin_time_seconds = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "replayWindowSize")) {
                    result.replay_window_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "startupAction")) {
                    result.startup_action = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelInsideCidr")) {
                    result.tunnel_inside_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelInsideIpv6Cidr")) {
                    result.tunnel_inside_ipv_6_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulInstanceCreditSpecificationItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !UnsuccessfulInstanceCreditSpecificationItem {
    var result: UnsuccessfulInstanceCreditSpecificationItem = undefined;
    result.@"error" = null;
    result.instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeUnsuccessfulInstanceCreditSpecificationItemError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulInstanceCreditSpecificationItemError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !UnsuccessfulInstanceCreditSpecificationItemError {
    var result: UnsuccessfulInstanceCreditSpecificationItemError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(UnsuccessfulInstanceCreditSpecificationErrorCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !UnsuccessfulItem {
    var result: UnsuccessfulItem = undefined;
    result.@"error" = null;
    result.resource_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "error")) {
                    result.@"error" = try deserializeUnsuccessfulItemError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "resourceId")) {
                    result.resource_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUnsuccessfulItemError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !UnsuccessfulItemError {
    var result: UnsuccessfulItemError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUserBucketDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !UserBucketDetails {
    var result: UserBucketDetails = undefined;
    result.s3_bucket = null;
    result.s3_key = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "s3Bucket")) {
                    result.s3_bucket = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3Key")) {
                    result.s3_key = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeUserIdGroupPair(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !UserIdGroupPair {
    var result: UserIdGroupPair = undefined;
    result.description = null;
    result.group_id = null;
    result.group_name = null;
    result.peering_status = null;
    result.user_id = null;
    result.vpc_id = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupId")) {
                    result.group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupName")) {
                    result.group_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringStatus")) {
                    result.peering_status = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userId")) {
                    result.user_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVCpuCountRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VCpuCountRange {
    _ = alloc;
    var result: VCpuCountRange = undefined;
    result.max = null;
    result.min = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "max")) {
                    result.max = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "min")) {
                    result.min = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVCpuInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VCpuInfo {
    var result: VCpuInfo = undefined;
    result.default_cores = null;
    result.default_threads_per_core = null;
    result.default_v_cpus = null;
    result.valid_cores = null;
    result.valid_threads_per_core = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "defaultCores")) {
                    result.default_cores = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultThreadsPerCore")) {
                    result.default_threads_per_core = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "defaultVCpus")) {
                    result.default_v_cpus = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "validCores")) {
                    result.valid_cores = try deserializeCoreCountList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "validThreadsPerCore")) {
                    result.valid_threads_per_core = try deserializeThreadsPerCoreList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeValidationError(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ValidationError {
    var result: ValidationError = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeValidationWarning(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !ValidationWarning {
    var result: ValidationWarning = undefined;
    result.errors = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "errorSet")) {
                    result.errors = try deserializeErrorSet(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpoint(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpoint {
    var result: VerifiedAccessEndpoint = undefined;
    result.application_domain = null;
    result.attachment_type = null;
    result.cidr_options = null;
    result.creation_time = null;
    result.deletion_time = null;
    result.description = null;
    result.device_validation_domain = null;
    result.domain_certificate_arn = null;
    result.endpoint_domain = null;
    result.endpoint_type = null;
    result.last_updated_time = null;
    result.load_balancer_options = null;
    result.network_interface_options = null;
    result.rds_options = null;
    result.security_group_ids = null;
    result.sse_specification = null;
    result.status = null;
    result.tags = null;
    result.verified_access_endpoint_id = null;
    result.verified_access_group_id = null;
    result.verified_access_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "applicationDomain")) {
                    result.application_domain = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachmentType")) {
                    result.attachment_type = std.meta.stringToEnum(VerifiedAccessEndpointAttachmentType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrOptions")) {
                    result.cidr_options = try deserializeVerifiedAccessEndpointCidrOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deletionTime")) {
                    result.deletion_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceValidationDomain")) {
                    result.device_validation_domain = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "domainCertificateArn")) {
                    result.domain_certificate_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endpointDomain")) {
                    result.endpoint_domain = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "endpointType")) {
                    result.endpoint_type = std.meta.stringToEnum(VerifiedAccessEndpointType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "loadBalancerOptions")) {
                    result.load_balancer_options = try deserializeVerifiedAccessEndpointLoadBalancerOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceOptions")) {
                    result.network_interface_options = try deserializeVerifiedAccessEndpointEniOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "rdsOptions")) {
                    result.rds_options = try deserializeVerifiedAccessEndpointRdsOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "securityGroupIdSet")) {
                    result.security_group_ids = try deserializeSecurityGroupIdList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "sseSpecification")) {
                    result.sse_specification = try deserializeVerifiedAccessSseSpecificationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeVerifiedAccessEndpointStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "verifiedAccessEndpointId")) {
                    result.verified_access_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessGroupId")) {
                    result.verified_access_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointCidrOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpointCidrOptions {
    var result: VerifiedAccessEndpointCidrOptions = undefined;
    result.cidr = null;
    result.port_ranges = null;
    result.protocol = null;
    result.subnet_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializeVerifiedAccessEndpointPortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.meta.stringToEnum(VerifiedAccessEndpointProtocol, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeVerifiedAccessEndpointSubnetIdList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointEniOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpointEniOptions {
    var result: VerifiedAccessEndpointEniOptions = undefined;
    result.network_interface_id = null;
    result.port = null;
    result.port_ranges = null;
    result.protocol = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "networkInterfaceId")) {
                    result.network_interface_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializeVerifiedAccessEndpointPortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.meta.stringToEnum(VerifiedAccessEndpointProtocol, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointLoadBalancerOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpointLoadBalancerOptions {
    var result: VerifiedAccessEndpointLoadBalancerOptions = undefined;
    result.load_balancer_arn = null;
    result.port = null;
    result.port_ranges = null;
    result.protocol = null;
    result.subnet_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "loadBalancerArn")) {
                    result.load_balancer_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "portRangeSet")) {
                    result.port_ranges = try deserializeVerifiedAccessEndpointPortRangeList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.meta.stringToEnum(VerifiedAccessEndpointProtocol, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeVerifiedAccessEndpointSubnetIdList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointPortRange(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpointPortRange {
    _ = alloc;
    var result: VerifiedAccessEndpointPortRange = undefined;
    result.from_port = null;
    result.to_port = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "fromPort")) {
                    result.from_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "toPort")) {
                    result.to_port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointRdsOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpointRdsOptions {
    var result: VerifiedAccessEndpointRdsOptions = undefined;
    result.port = null;
    result.protocol = null;
    result.rds_db_cluster_arn = null;
    result.rds_db_instance_arn = null;
    result.rds_db_proxy_arn = null;
    result.rds_endpoint = null;
    result.subnet_ids = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "port")) {
                    result.port = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "protocol")) {
                    result.protocol = std.meta.stringToEnum(VerifiedAccessEndpointProtocol, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsDbClusterArn")) {
                    result.rds_db_cluster_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsDbInstanceArn")) {
                    result.rds_db_instance_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsDbProxyArn")) {
                    result.rds_db_proxy_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "rdsEndpoint")) {
                    result.rds_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeVerifiedAccessEndpointSubnetIdList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpointStatus {
    var result: VerifiedAccessEndpointStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(VerifiedAccessEndpointStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessEndpointTarget(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessEndpointTarget {
    var result: VerifiedAccessEndpointTarget = undefined;
    result.verified_access_endpoint_id = null;
    result.verified_access_endpoint_target_dns = null;
    result.verified_access_endpoint_target_ip_address = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "verifiedAccessEndpointId")) {
                    result.verified_access_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessEndpointTargetDns")) {
                    result.verified_access_endpoint_target_dns = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessEndpointTargetIpAddress")) {
                    result.verified_access_endpoint_target_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessGroup(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessGroup {
    var result: VerifiedAccessGroup = undefined;
    result.creation_time = null;
    result.deletion_time = null;
    result.description = null;
    result.last_updated_time = null;
    result.owner = null;
    result.sse_specification = null;
    result.tags = null;
    result.verified_access_group_arn = null;
    result.verified_access_group_id = null;
    result.verified_access_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deletionTime")) {
                    result.deletion_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "owner")) {
                    result.owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseSpecification")) {
                    result.sse_specification = try deserializeVerifiedAccessSseSpecificationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "verifiedAccessGroupArn")) {
                    result.verified_access_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessGroupId")) {
                    result.verified_access_group_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstance(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessInstance {
    var result: VerifiedAccessInstance = undefined;
    result.cidr_endpoints_custom_sub_domain = null;
    result.creation_time = null;
    result.description = null;
    result.fips_enabled = null;
    result.last_updated_time = null;
    result.tags = null;
    result.verified_access_instance_id = null;
    result.verified_access_trust_providers = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrEndpointsCustomSubDomain")) {
                    result.cidr_endpoints_custom_sub_domain = try deserializeVerifiedAccessInstanceCustomSubDomain(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "fipsEnabled")) {
                    result.fips_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessTrustProviderSet")) {
                    result.verified_access_trust_providers = try deserializeVerifiedAccessTrustProviderCondensedList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceCustomSubDomain(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessInstanceCustomSubDomain {
    var result: VerifiedAccessInstanceCustomSubDomain = undefined;
    result.nameservers = null;
    result.sub_domain = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "nameserverSet")) {
                    result.nameservers = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "subDomain")) {
                    result.sub_domain = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceLoggingConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessInstanceLoggingConfiguration {
    var result: VerifiedAccessInstanceLoggingConfiguration = undefined;
    result.access_logs = null;
    result.verified_access_instance_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accessLogs")) {
                    result.access_logs = try deserializeVerifiedAccessLogs(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "verifiedAccessInstanceId")) {
                    result.verified_access_instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessInstanceOpenVpnClientConfiguration {
    var result: VerifiedAccessInstanceOpenVpnClientConfiguration = undefined;
    result.config = null;
    result.routes = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "config")) {
                    result.config = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeSet")) {
                    result.routes = try deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRouteList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceOpenVpnClientConfigurationRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessInstanceOpenVpnClientConfigurationRoute {
    var result: VerifiedAccessInstanceOpenVpnClientConfigurationRoute = undefined;
    result.cidr = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidr")) {
                    result.cidr = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessInstanceUserTrustProviderClientConfiguration(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessInstanceUserTrustProviderClientConfiguration {
    var result: VerifiedAccessInstanceUserTrustProviderClientConfiguration = undefined;
    result.authorization_endpoint = null;
    result.client_id = null;
    result.client_secret = null;
    result.issuer = null;
    result.pkce_enabled = null;
    result.public_signing_key_endpoint = null;
    result.scopes = null;
    result.token_endpoint = null;
    result.@"type" = null;
    result.user_info_endpoint = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "authorizationEndpoint")) {
                    result.authorization_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientId")) {
                    result.client_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "clientSecret")) {
                    result.client_secret = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "issuer")) {
                    result.issuer = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "pkceEnabled")) {
                    result.pkce_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "publicSigningKeyEndpoint")) {
                    result.public_signing_key_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "scopes")) {
                    result.scopes = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tokenEndpoint")) {
                    result.token_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(UserTrustProviderType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userInfoEndpoint")) {
                    result.user_info_endpoint = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogCloudWatchLogsDestination(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessLogCloudWatchLogsDestination {
    var result: VerifiedAccessLogCloudWatchLogsDestination = undefined;
    result.delivery_status = null;
    result.enabled = null;
    result.log_group = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deliveryStatus")) {
                    result.delivery_status = try deserializeVerifiedAccessLogDeliveryStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "logGroup")) {
                    result.log_group = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogDeliveryStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessLogDeliveryStatus {
    var result: VerifiedAccessLogDeliveryStatus = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(VerifiedAccessLogDeliveryStatusCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogKinesisDataFirehoseDestination(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessLogKinesisDataFirehoseDestination {
    var result: VerifiedAccessLogKinesisDataFirehoseDestination = undefined;
    result.delivery_status = null;
    result.delivery_stream = null;
    result.enabled = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "deliveryStatus")) {
                    result.delivery_status = try deserializeVerifiedAccessLogDeliveryStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "deliveryStream")) {
                    result.delivery_stream = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogS3Destination(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessLogS3Destination {
    var result: VerifiedAccessLogS3Destination = undefined;
    result.bucket_name = null;
    result.bucket_owner = null;
    result.delivery_status = null;
    result.enabled = null;
    result.prefix = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "bucketName")) {
                    result.bucket_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "bucketOwner")) {
                    result.bucket_owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deliveryStatus")) {
                    result.delivery_status = try deserializeVerifiedAccessLogDeliveryStatus(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "enabled")) {
                    result.enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "prefix")) {
                    result.prefix = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessLogs(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessLogs {
    var result: VerifiedAccessLogs = undefined;
    result.cloud_watch_logs = null;
    result.include_trust_context = null;
    result.kinesis_data_firehose = null;
    result.log_version = null;
    result.s3 = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cloudWatchLogs")) {
                    result.cloud_watch_logs = try deserializeVerifiedAccessLogCloudWatchLogsDestination(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "includeTrustContext")) {
                    result.include_trust_context = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "kinesisDataFirehose")) {
                    result.kinesis_data_firehose = try deserializeVerifiedAccessLogKinesisDataFirehoseDestination(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "logVersion")) {
                    result.log_version = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "s3")) {
                    result.s3 = try deserializeVerifiedAccessLogS3Destination(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessSseSpecificationResponse(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessSseSpecificationResponse {
    var result: VerifiedAccessSseSpecificationResponse = undefined;
    result.customer_managed_key_enabled = null;
    result.kms_key_arn = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "customerManagedKeyEnabled")) {
                    result.customer_managed_key_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "kmsKeyArn")) {
                    result.kms_key_arn = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessTrustProvider(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessTrustProvider {
    var result: VerifiedAccessTrustProvider = undefined;
    result.creation_time = null;
    result.description = null;
    result.device_options = null;
    result.device_trust_provider_type = null;
    result.last_updated_time = null;
    result.native_application_oidc_options = null;
    result.oidc_options = null;
    result.policy_reference_name = null;
    result.sse_specification = null;
    result.tags = null;
    result.trust_provider_type = null;
    result.user_trust_provider_type = null;
    result.verified_access_trust_provider_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTime")) {
                    result.creation_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceOptions")) {
                    result.device_options = try deserializeDeviceOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "deviceTrustProviderType")) {
                    result.device_trust_provider_type = std.meta.stringToEnum(DeviceTrustProviderType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdatedTime")) {
                    result.last_updated_time = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "nativeApplicationOidcOptions")) {
                    result.native_application_oidc_options = try deserializeNativeApplicationOidcOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "oidcOptions")) {
                    result.oidc_options = try deserializeOidcOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "policyReferenceName")) {
                    result.policy_reference_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseSpecification")) {
                    result.sse_specification = try deserializeVerifiedAccessSseSpecificationResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "trustProviderType")) {
                    result.trust_provider_type = std.meta.stringToEnum(TrustProviderType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userTrustProviderType")) {
                    result.user_trust_provider_type = std.meta.stringToEnum(UserTrustProviderType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessTrustProviderId")) {
                    result.verified_access_trust_provider_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVerifiedAccessTrustProviderCondensed(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VerifiedAccessTrustProviderCondensed {
    var result: VerifiedAccessTrustProviderCondensed = undefined;
    result.description = null;
    result.device_trust_provider_type = null;
    result.trust_provider_type = null;
    result.user_trust_provider_type = null;
    result.verified_access_trust_provider_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "deviceTrustProviderType")) {
                    result.device_trust_provider_type = std.meta.stringToEnum(DeviceTrustProviderType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "trustProviderType")) {
                    result.trust_provider_type = std.meta.stringToEnum(TrustProviderType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "userTrustProviderType")) {
                    result.user_trust_provider_type = std.meta.stringToEnum(UserTrustProviderType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "verifiedAccessTrustProviderId")) {
                    result.verified_access_trust_provider_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVgwTelemetry(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VgwTelemetry {
    var result: VgwTelemetry = undefined;
    result.accepted_route_count = null;
    result.certificate_arn = null;
    result.last_status_change = null;
    result.outside_ip_address = null;
    result.status = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "acceptedRouteCount")) {
                    result.accepted_route_count = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "certificateArn")) {
                    result.certificate_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastStatusChange")) {
                    result.last_status_change = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "outsideIpAddress")) {
                    result.outside_ip_address = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(TelemetryStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolume(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Volume {
    var result: Volume = undefined;
    result.attachments = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.create_time = null;
    result.encrypted = null;
    result.fast_restored = null;
    result.iops = null;
    result.kms_key_id = null;
    result.multi_attach_enabled = null;
    result.operator = null;
    result.outpost_arn = null;
    result.size = null;
    result.snapshot_id = null;
    result.source_volume_id = null;
    result.sse_type = null;
    result.state = null;
    result.tags = null;
    result.throughput = null;
    result.volume_id = null;
    result.volume_initialization_rate = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "attachmentSet")) {
                    result.attachments = try deserializeVolumeAttachmentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "encrypted")) {
                    result.encrypted = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "fastRestored")) {
                    result.fast_restored = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "kmsKeyId")) {
                    result.kms_key_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "multiAttachEnabled")) {
                    result.multi_attach_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceVolumeId")) {
                    result.source_volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sseType")) {
                    result.sse_type = std.meta.stringToEnum(SSEType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.state = std.meta.stringToEnum(VolumeState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeInitializationRate")) {
                    result.volume_initialization_rate = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = std.meta.stringToEnum(VolumeType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeAttachment {
    var result: VolumeAttachment = undefined;
    result.associated_resource = null;
    result.attach_time = null;
    result.delete_on_termination = null;
    result.device = null;
    result.ebs_card_index = null;
    result.instance_id = null;
    result.instance_owning_service = null;
    result.state = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResource")) {
                    result.associated_resource = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachTime")) {
                    result.attach_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deleteOnTermination")) {
                    result.delete_on_termination = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "device")) {
                    result.device = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ebsCardIndex")) {
                    result.ebs_card_index = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceOwningService")) {
                    result.instance_owning_service = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.state = std.meta.stringToEnum(VolumeAttachmentState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeModification(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeModification {
    var result: VolumeModification = undefined;
    result.end_time = null;
    result.modification_state = null;
    result.original_iops = null;
    result.original_multi_attach_enabled = null;
    result.original_size = null;
    result.original_throughput = null;
    result.original_volume_type = null;
    result.progress = null;
    result.start_time = null;
    result.status_message = null;
    result.target_iops = null;
    result.target_multi_attach_enabled = null;
    result.target_size = null;
    result.target_throughput = null;
    result.target_volume_type = null;
    result.volume_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "endTime")) {
                    result.end_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "modificationState")) {
                    result.modification_state = std.meta.stringToEnum(VolumeModificationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "originalIops")) {
                    result.original_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "originalMultiAttachEnabled")) {
                    result.original_multi_attach_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "originalSize")) {
                    result.original_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "originalThroughput")) {
                    result.original_throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "originalVolumeType")) {
                    result.original_volume_type = std.meta.stringToEnum(VolumeType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "progress")) {
                    result.progress = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "startTime")) {
                    result.start_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "targetIops")) {
                    result.target_iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetMultiAttachEnabled")) {
                    result.target_multi_attach_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "targetSize")) {
                    result.target_size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetThroughput")) {
                    result.target_throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "targetVolumeType")) {
                    result.target_volume_type = std.meta.stringToEnum(VolumeType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeRecycleBinInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeRecycleBinInfo {
    var result: VolumeRecycleBinInfo = undefined;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.create_time = null;
    result.iops = null;
    result.operator = null;
    result.outpost_arn = null;
    result.recycle_bin_enter_time = null;
    result.recycle_bin_exit_time = null;
    result.size = null;
    result.snapshot_id = null;
    result.source_volume_id = null;
    result.state = null;
    result.throughput = null;
    result.volume_id = null;
    result.volume_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "createTime")) {
                    result.create_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "iops")) {
                    result.iops = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "operator")) {
                    result.operator = try deserializeOperatorResponse(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "recycleBinEnterTime")) {
                    result.recycle_bin_enter_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "recycleBinExitTime")) {
                    result.recycle_bin_exit_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "size")) {
                    result.size = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "snapshotId")) {
                    result.snapshot_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "sourceVolumeId")) {
                    result.source_volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VolumeState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "throughput")) {
                    result.throughput = std.fmt.parseInt(i32, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeType")) {
                    result.volume_type = std.meta.stringToEnum(VolumeType, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusAction(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeStatusAction {
    var result: VolumeStatusAction = undefined;
    result.code = null;
    result.description = null;
    result.event_id = null;
    result.event_type = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventId")) {
                    result.event_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusAttachmentStatus(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeStatusAttachmentStatus {
    var result: VolumeStatusAttachmentStatus = undefined;
    result.instance_id = null;
    result.io_performance = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ioPerformance")) {
                    result.io_performance = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusDetails(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeStatusDetails {
    var result: VolumeStatusDetails = undefined;
    result.name = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "name")) {
                    result.name = std.meta.stringToEnum(VolumeStatusName, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusEvent(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeStatusEvent {
    var result: VolumeStatusEvent = undefined;
    result.description = null;
    result.event_id = null;
    result.event_type = null;
    result.instance_id = null;
    result.not_after = null;
    result.not_before = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventId")) {
                    result.event_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventType")) {
                    result.event_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "instanceId")) {
                    result.instance_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "notAfter")) {
                    result.not_after = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "notBefore")) {
                    result.not_before = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeStatusInfo {
    var result: VolumeStatusInfo = undefined;
    result.details = null;
    result.status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "details")) {
                    result.details = try deserializeVolumeStatusDetailsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = std.meta.stringToEnum(VolumeStatusInfoStatus, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVolumeStatusItem(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VolumeStatusItem {
    var result: VolumeStatusItem = undefined;
    result.actions = null;
    result.attachment_statuses = null;
    result.availability_zone = null;
    result.availability_zone_id = null;
    result.events = null;
    result.initialization_status_details = null;
    result.outpost_arn = null;
    result.volume_id = null;
    result.volume_status = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "actionsSet")) {
                    result.actions = try deserializeVolumeStatusActionsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "attachmentStatuses")) {
                    result.attachment_statuses = try deserializeVolumeStatusAttachmentStatusList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "availabilityZoneId")) {
                    result.availability_zone_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "eventsSet")) {
                    result.events = try deserializeVolumeStatusEventsList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "initializationStatusDetails")) {
                    result.initialization_status_details = try deserializeInitializationStatusDetails(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "outpostArn")) {
                    result.outpost_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeId")) {
                    result.volume_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "volumeStatus")) {
                    result.volume_status = try deserializeVolumeStatusInfo(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpc(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !Vpc {
    var result: Vpc = undefined;
    result.block_public_access_states = null;
    result.cidr_block = null;
    result.cidr_block_association_set = null;
    result.dhcp_options_id = null;
    result.encryption_control = null;
    result.instance_tenancy = null;
    result.ipv_6_cidr_block_association_set = null;
    result.is_default = null;
    result.owner_id = null;
    result.state = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "blockPublicAccessStates")) {
                    result.block_public_access_states = try deserializeBlockPublicAccessStates(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlockAssociationSet")) {
                    result.cidr_block_association_set = try deserializeVpcCidrBlockAssociationSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "dhcpOptionsId")) {
                    result.dhcp_options_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "encryptionControl")) {
                    result.encryption_control = try deserializeVpcEncryptionControl(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "instanceTenancy")) {
                    result.instance_tenancy = std.meta.stringToEnum(Tenancy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockAssociationSet")) {
                    result.ipv_6_cidr_block_association_set = try deserializeVpcIpv6CidrBlockAssociationSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "isDefault")) {
                    result.is_default = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpcState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcAttachment(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcAttachment {
    var result: VpcAttachment = undefined;
    result.state = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(AttachmentStatus, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcBlockPublicAccessExclusion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcBlockPublicAccessExclusion {
    var result: VpcBlockPublicAccessExclusion = undefined;
    result.creation_timestamp = null;
    result.deletion_timestamp = null;
    result.exclusion_id = null;
    result.internet_gateway_exclusion_mode = null;
    result.last_update_timestamp = null;
    result.reason = null;
    result.resource_arn = null;
    result.state = null;
    result.tags = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTimestamp")) {
                    result.creation_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "deletionTimestamp")) {
                    result.deletion_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "exclusionId")) {
                    result.exclusion_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "internetGatewayExclusionMode")) {
                    result.internet_gateway_exclusion_mode = std.meta.stringToEnum(InternetGatewayExclusionMode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdateTimestamp")) {
                    result.last_update_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "reason")) {
                    result.reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceArn")) {
                    result.resource_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpcBlockPublicAccessExclusionState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcBlockPublicAccessOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcBlockPublicAccessOptions {
    var result: VpcBlockPublicAccessOptions = undefined;
    result.aws_account_id = null;
    result.aws_region = null;
    result.exclusions_allowed = null;
    result.internet_gateway_block_mode = null;
    result.last_update_timestamp = null;
    result.managed_by = null;
    result.reason = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "awsAccountId")) {
                    result.aws_account_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "awsRegion")) {
                    result.aws_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "exclusionsAllowed")) {
                    result.exclusions_allowed = std.meta.stringToEnum(VpcBlockPublicAccessExclusionsAllowed, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "internetGatewayBlockMode")) {
                    result.internet_gateway_block_mode = std.meta.stringToEnum(InternetGatewayBlockMode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "lastUpdateTimestamp")) {
                    result.last_update_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "managedBy")) {
                    result.managed_by = std.meta.stringToEnum(ManagedBy, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "reason")) {
                    result.reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpcBlockPublicAccessState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcCidrBlockAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcCidrBlockAssociation {
    var result: VpcCidrBlockAssociation = undefined;
    result.association_id = null;
    result.cidr_block = null;
    result.cidr_block_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlockState")) {
                    result.cidr_block_state = try deserializeVpcCidrBlockState(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcCidrBlockState(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcCidrBlockState {
    var result: VpcCidrBlockState = undefined;
    result.state = null;
    result.status_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpcCidrBlockStateCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "statusMessage")) {
                    result.status_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcClassicLink(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcClassicLink {
    var result: VpcClassicLink = undefined;
    result.classic_link_enabled = null;
    result.tags = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "classicLinkEnabled")) {
                    result.classic_link_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionControl(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcEncryptionControl {
    var result: VpcEncryptionControl = undefined;
    result.mode = null;
    result.resource_exclusions = null;
    result.state = null;
    result.state_message = null;
    result.tags = null;
    result.vpc_encryption_control_id = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "mode")) {
                    result.mode = std.meta.stringToEnum(VpcEncryptionControlMode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "resourceExclusions")) {
                    result.resource_exclusions = try deserializeVpcEncryptionControlExclusions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpcEncryptionControlState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEncryptionControlId")) {
                    result.vpc_encryption_control_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionControlExclusion(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcEncryptionControlExclusion {
    var result: VpcEncryptionControlExclusion = undefined;
    result.state = null;
    result.state_message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpcEncryptionControlExclusionState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "stateMessage")) {
                    result.state_message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionControlExclusions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcEncryptionControlExclusions {
    var result: VpcEncryptionControlExclusions = undefined;
    result.egress_only_internet_gateway = null;
    result.elastic_file_system = null;
    result.internet_gateway = null;
    result.lambda = null;
    result.nat_gateway = null;
    result.virtual_private_gateway = null;
    result.vpc_lattice = null;
    result.vpc_peering = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "egressOnlyInternetGateway")) {
                    result.egress_only_internet_gateway = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "elasticFileSystem")) {
                    result.elastic_file_system = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "internetGateway")) {
                    result.internet_gateway = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "lambda")) {
                    result.lambda = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "natGateway")) {
                    result.nat_gateway = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "virtualPrivateGateway")) {
                    result.virtual_private_gateway = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpcLattice")) {
                    result.vpc_lattice = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "vpcPeering")) {
                    result.vpc_peering = try deserializeVpcEncryptionControlExclusion(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEncryptionNonCompliantResource(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcEncryptionNonCompliantResource {
    var result: VpcEncryptionNonCompliantResource = undefined;
    result.description = null;
    result.id = null;
    result.is_excludable = null;
    result.@"type" = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "description")) {
                    result.description = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "isExcludable")) {
                    result.is_excludable = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEndpoint(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcEndpoint {
    var result: VpcEndpoint = undefined;
    result.creation_timestamp = null;
    result.dns_entries = null;
    result.dns_options = null;
    result.failure_reason = null;
    result.groups = null;
    result.ip_address_type = null;
    result.ipv_4_prefixes = null;
    result.ipv_6_prefixes = null;
    result.last_error = null;
    result.network_interface_ids = null;
    result.owner_id = null;
    result.policy_document = null;
    result.private_dns_enabled = null;
    result.requester_managed = null;
    result.resource_configuration_arn = null;
    result.route_table_ids = null;
    result.service_name = null;
    result.service_network_arn = null;
    result.service_region = null;
    result.state = null;
    result.subnet_ids = null;
    result.tags = null;
    result.vpc_endpoint_id = null;
    result.vpc_endpoint_type = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTimestamp")) {
                    result.creation_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dnsEntrySet")) {
                    result.dns_entries = try deserializeDnsEntrySet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "dnsOptions")) {
                    result.dns_options = try deserializeDnsOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "groupSet")) {
                    result.groups = try deserializeGroupIdentifierSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipAddressType")) {
                    result.ip_address_type = std.meta.stringToEnum(IpAddressType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv4PrefixSet")) {
                    result.ipv_4_prefixes = try deserializeSubnetIpPrefixesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6PrefixSet")) {
                    result.ipv_6_prefixes = try deserializeSubnetIpPrefixesList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "lastError")) {
                    result.last_error = try deserializeLastError(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "networkInterfaceIdSet")) {
                    result.network_interface_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "policyDocument")) {
                    result.policy_document = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsEnabled")) {
                    result.private_dns_enabled = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "requesterManaged")) {
                    result.requester_managed = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "resourceConfigurationArn")) {
                    result.resource_configuration_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routeTableIdSet")) {
                    result.route_table_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "serviceName")) {
                    result.service_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceNetworkArn")) {
                    result.service_network_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceRegion")) {
                    result.service_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(State, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "subnetIdSet")) {
                    result.subnet_ids = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointType")) {
                    result.vpc_endpoint_type = std.meta.stringToEnum(VpcEndpointType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEndpointAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcEndpointAssociation {
    var result: VpcEndpointAssociation = undefined;
    result.associated_resource_accessibility = null;
    result.associated_resource_arn = null;
    result.dns_entry = null;
    result.failure_code = null;
    result.failure_reason = null;
    result.id = null;
    result.private_dns_entry = null;
    result.resource_configuration_group_arn = null;
    result.service_network_arn = null;
    result.service_network_name = null;
    result.tags = null;
    result.vpc_endpoint_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associatedResourceAccessibility")) {
                    result.associated_resource_accessibility = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "associatedResourceArn")) {
                    result.associated_resource_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "dnsEntry")) {
                    result.dns_entry = try deserializeDnsEntry(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "failureCode")) {
                    result.failure_code = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "failureReason")) {
                    result.failure_reason = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "id")) {
                    result.id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "privateDnsEntry")) {
                    result.private_dns_entry = try deserializeDnsEntry(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "resourceConfigurationGroupArn")) {
                    result.resource_configuration_group_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceNetworkArn")) {
                    result.service_network_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "serviceNetworkName")) {
                    result.service_network_name = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcEndpointConnection(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcEndpointConnection {
    var result: VpcEndpointConnection = undefined;
    result.creation_timestamp = null;
    result.dns_entries = null;
    result.gateway_load_balancer_arns = null;
    result.ip_address_type = null;
    result.network_load_balancer_arns = null;
    result.service_id = null;
    result.tags = null;
    result.vpc_endpoint_connection_id = null;
    result.vpc_endpoint_id = null;
    result.vpc_endpoint_owner = null;
    result.vpc_endpoint_region = null;
    result.vpc_endpoint_state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "creationTimestamp")) {
                    result.creation_timestamp = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "dnsEntrySet")) {
                    result.dns_entries = try deserializeDnsEntrySet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "gatewayLoadBalancerArnSet")) {
                    result.gateway_load_balancer_arns = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipAddressType")) {
                    result.ip_address_type = std.meta.stringToEnum(IpAddressType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkLoadBalancerArnSet")) {
                    result.network_load_balancer_arns = try deserializeValueStringList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "serviceId")) {
                    result.service_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcEndpointConnectionId")) {
                    result.vpc_endpoint_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointId")) {
                    result.vpc_endpoint_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointOwner")) {
                    result.vpc_endpoint_owner = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointRegion")) {
                    result.vpc_endpoint_region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcEndpointState")) {
                    result.vpc_endpoint_state = std.meta.stringToEnum(State, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcIpv6CidrBlockAssociation(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcIpv6CidrBlockAssociation {
    var result: VpcIpv6CidrBlockAssociation = undefined;
    result.association_id = null;
    result.ip_source = null;
    result.ipv_6_address_attribute = null;
    result.ipv_6_cidr_block = null;
    result.ipv_6_cidr_block_state = null;
    result.ipv_6_pool = null;
    result.network_border_group = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "associationId")) {
                    result.association_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipSource")) {
                    result.ip_source = std.meta.stringToEnum(IpSource, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6AddressAttribute")) {
                    result.ipv_6_address_attribute = std.meta.stringToEnum(Ipv6AddressAttribute, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlock")) {
                    result.ipv_6_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockState")) {
                    result.ipv_6_cidr_block_state = try deserializeVpcCidrBlockState(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "ipv6Pool")) {
                    result.ipv_6_pool = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "networkBorderGroup")) {
                    result.network_border_group = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnection(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcPeeringConnection {
    var result: VpcPeeringConnection = undefined;
    result.accepter_vpc_info = null;
    result.expiration_time = null;
    result.requester_vpc_info = null;
    result.status = null;
    result.tags = null;
    result.vpc_peering_connection_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "accepterVpcInfo")) {
                    result.accepter_vpc_info = try deserializeVpcPeeringConnectionVpcInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "expirationTime")) {
                    result.expiration_time = aws.date.parseIso8601(try reader.readElementText()) catch null;
                } else if (std.mem.eql(u8, e.local, "requesterVpcInfo")) {
                    result.requester_vpc_info = try deserializeVpcPeeringConnectionVpcInfo(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "status")) {
                    result.status = try deserializeVpcPeeringConnectionStateReason(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpcPeeringConnectionId")) {
                    result.vpc_peering_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnectionOptionsDescription(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcPeeringConnectionOptionsDescription {
    _ = alloc;
    var result: VpcPeeringConnectionOptionsDescription = undefined;
    result.allow_dns_resolution_from_remote_vpc = null;
    result.allow_egress_from_local_classic_link_to_remote_vpc = null;
    result.allow_egress_from_local_vpc_to_remote_classic_link = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "allowDnsResolutionFromRemoteVpc")) {
                    result.allow_dns_resolution_from_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalClassicLinkToRemoteVpc")) {
                    result.allow_egress_from_local_classic_link_to_remote_vpc = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "allowEgressFromLocalVpcToRemoteClassicLink")) {
                    result.allow_egress_from_local_vpc_to_remote_classic_link = std.mem.eql(u8, try reader.readElementText(), "true");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnectionStateReason(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcPeeringConnectionStateReason {
    var result: VpcPeeringConnectionStateReason = undefined;
    result.code = null;
    result.message = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "code")) {
                    result.code = std.meta.stringToEnum(VpcPeeringConnectionStateReasonCode, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "message")) {
                    result.message = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpcPeeringConnectionVpcInfo(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpcPeeringConnectionVpcInfo {
    var result: VpcPeeringConnectionVpcInfo = undefined;
    result.cidr_block = null;
    result.cidr_block_set = null;
    result.ipv_6_cidr_block_set = null;
    result.owner_id = null;
    result.peering_options = null;
    result.region = null;
    result.vpc_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cidrBlock")) {
                    result.cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "cidrBlockSet")) {
                    result.cidr_block_set = try deserializeCidrBlockSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ipv6CidrBlockSet")) {
                    result.ipv_6_cidr_block_set = try deserializeIpv6CidrBlockSet(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "ownerId")) {
                    result.owner_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "peeringOptions")) {
                    result.peering_options = try deserializeVpcPeeringConnectionOptionsDescription(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "region")) {
                    result.region = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpcId")) {
                    result.vpc_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConcentrator(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpnConcentrator {
    var result: VpnConcentrator = undefined;
    result.state = null;
    result.tags = null;
    result.transit_gateway_attachment_id = null;
    result.transit_gateway_id = null;
    result.@"type" = null;
    result.vpn_concentrator_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "state")) {
                    result.state = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayAttachmentId")) {
                    result.transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnConcentratorId")) {
                    result.vpn_concentrator_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConnection(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpnConnection {
    var result: VpnConnection = undefined;
    result.category = null;
    result.core_network_arn = null;
    result.core_network_attachment_arn = null;
    result.customer_gateway_configuration = null;
    result.customer_gateway_id = null;
    result.gateway_association_state = null;
    result.options = null;
    result.pre_shared_key_arn = null;
    result.routes = null;
    result.state = null;
    result.tags = null;
    result.transit_gateway_id = null;
    result.@"type" = null;
    result.vgw_telemetry = null;
    result.vpn_concentrator_id = null;
    result.vpn_connection_id = null;
    result.vpn_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "category")) {
                    result.category = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkArn")) {
                    result.core_network_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "coreNetworkAttachmentArn")) {
                    result.core_network_attachment_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGatewayConfiguration")) {
                    result.customer_gateway_configuration = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "customerGatewayId")) {
                    result.customer_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "gatewayAssociationState")) {
                    result.gateway_association_state = std.meta.stringToEnum(GatewayAssociationState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "options")) {
                    result.options = try deserializeVpnConnectionOptions(reader, alloc);
                } else if (std.mem.eql(u8, e.local, "preSharedKeyArn")) {
                    result.pre_shared_key_arn = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "routes")) {
                    result.routes = try deserializeVpnStaticRouteList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpnState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "transitGatewayId")) {
                    result.transit_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(GatewayType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vgwTelemetry")) {
                    result.vgw_telemetry = try deserializeVgwTelemetryList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpnConcentratorId")) {
                    result.vpn_concentrator_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnConnectionId")) {
                    result.vpn_connection_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnGatewayId")) {
                    result.vpn_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConnectionDeviceType(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpnConnectionDeviceType {
    var result: VpnConnectionDeviceType = undefined;
    result.platform = null;
    result.software = null;
    result.vendor = null;
    result.vpn_connection_device_type_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "platform")) {
                    result.platform = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "software")) {
                    result.software = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vendor")) {
                    result.vendor = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "vpnConnectionDeviceTypeId")) {
                    result.vpn_connection_device_type_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnConnectionOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpnConnectionOptions {
    var result: VpnConnectionOptions = undefined;
    result.enable_acceleration = null;
    result.local_ipv_4_network_cidr = null;
    result.local_ipv_6_network_cidr = null;
    result.outside_ip_address_type = null;
    result.remote_ipv_4_network_cidr = null;
    result.remote_ipv_6_network_cidr = null;
    result.static_routes_only = null;
    result.transport_transit_gateway_attachment_id = null;
    result.tunnel_bandwidth = null;
    result.tunnel_inside_ip_version = null;
    result.tunnel_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "enableAcceleration")) {
                    result.enable_acceleration = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "localIpv4NetworkCidr")) {
                    result.local_ipv_4_network_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "localIpv6NetworkCidr")) {
                    result.local_ipv_6_network_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "outsideIpAddressType")) {
                    result.outside_ip_address_type = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remoteIpv4NetworkCidr")) {
                    result.remote_ipv_4_network_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "remoteIpv6NetworkCidr")) {
                    result.remote_ipv_6_network_cidr = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "staticRoutesOnly")) {
                    result.static_routes_only = std.mem.eql(u8, try reader.readElementText(), "true");
                } else if (std.mem.eql(u8, e.local, "transportTransitGatewayAttachmentId")) {
                    result.transport_transit_gateway_attachment_id = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelBandwidth")) {
                    result.tunnel_bandwidth = std.meta.stringToEnum(VpnTunnelBandwidth, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelInsideIpVersion")) {
                    result.tunnel_inside_ip_version = std.meta.stringToEnum(TunnelInsideIpVersion, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tunnelOptionSet")) {
                    result.tunnel_options = try deserializeTunnelOptionsList(reader, alloc, "item");
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnGateway(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpnGateway {
    var result: VpnGateway = undefined;
    result.amazon_side_asn = null;
    result.availability_zone = null;
    result.state = null;
    result.tags = null;
    result.@"type" = null;
    result.vpc_attachments = null;
    result.vpn_gateway_id = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "amazonSideAsn")) {
                    result.amazon_side_asn = std.fmt.parseInt(i64, try reader.readElementText(), 10) catch null;
                } else if (std.mem.eql(u8, e.local, "availabilityZone")) {
                    result.availability_zone = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpnState, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "tagSet")) {
                    result.tags = try deserializeTagList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "type")) {
                    result.@"type" = std.meta.stringToEnum(GatewayType, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "attachments")) {
                    result.vpc_attachments = try deserializeVpcAttachmentList(reader, alloc, "item");
                } else if (std.mem.eql(u8, e.local, "vpnGatewayId")) {
                    result.vpn_gateway_id = try alloc.dupe(u8, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnStaticRoute(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpnStaticRoute {
    var result: VpnStaticRoute = undefined;
    result.destination_cidr_block = null;
    result.source = null;
    result.state = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "destinationCidrBlock")) {
                    result.destination_cidr_block = try alloc.dupe(u8, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "source")) {
                    result.source = std.meta.stringToEnum(VpnStaticRouteSource, try reader.readElementText());
                } else if (std.mem.eql(u8, e.local, "state")) {
                    result.state = std.meta.stringToEnum(VpnState, try reader.readElementText());
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn deserializeVpnTunnelLogOptions(reader: *aws.xml.Reader, alloc: std.mem.Allocator) !VpnTunnelLogOptions {
    var result: VpnTunnelLogOptions = undefined;
    result.cloud_watch_log_options = null;
    while (try reader.next()) |event| {
        switch (event) {
            .element_start => |e| {
                if (std.mem.eql(u8, e.local, "cloudWatchLogOptions")) {
                    result.cloud_watch_log_options = try deserializeCloudWatchLogOptions(reader, alloc);
                } else {
                    try reader.skipElement();
                }
            },
            .element_end => break,
            else => {},
        }
    }
    return result;
}

pub fn serializeAcceleratorManufacturerSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AcceleratorManufacturer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAcceleratorNameSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AcceleratorName, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAcceleratorTypeSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AcceleratorType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAccessScopePathListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AccessScopePathRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeAccessScopePathRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAccountAttributeNameStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AccountAttributeName, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAddIpamOperatingRegionSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AddIpamOperatingRegion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeAddIpamOperatingRegion(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAddIpamOrganizationalUnitExclusionSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AddIpamOrganizationalUnitExclusion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeAddIpamOrganizationalUnitExclusion(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAddPrefixListEntries(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AddPrefixListEntry, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeAddPrefixListEntry(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAllocationIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAllocationIds(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAllowedInstanceTypeSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeArchitectureTypeSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ArchitectureType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeArnList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAssetIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAssociationIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAthenaIntegrationsSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AthenaIntegration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeAthenaIntegration(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAvailabilityZoneAddresses(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const AvailabilityZoneAddress, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeAvailabilityZoneAddress(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAvailabilityZoneIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAvailabilityZoneStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeBillingProductList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeBlockDeviceMappingList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const BlockDeviceMapping, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeBlockDeviceMapping(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeBlockDeviceMappingRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const BlockDeviceMapping, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeBlockDeviceMapping(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeBundleIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCapacityBlockIds(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCapacityManagerConditionSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CapacityManagerCondition, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeCapacityManagerCondition(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCapacityManagerDataExportIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCapacityReservationFleetIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCapacityReservationIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCarrierGatewayIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeClassicLoadBalancers(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ClassicLoadBalancer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeClassicLoadBalancer(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeClientVpnAuthenticationRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ClientVpnAuthenticationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeClientVpnAuthenticationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeClientVpnEndpointIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeClientVpnSecurityGroupIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCoipPoolIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeConditionValueList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeConnectionNotificationIdsList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeConversionIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCpuManufacturerSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CpuManufacturer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCreateVerifiedAccessEndpointPortRangeList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CreateVerifiedAccessEndpointPortRange, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeCreateVerifiedAccessEndpointPortRange(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCreateVerifiedAccessEndpointSubnetIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCreateVolumePermissionList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const CreateVolumePermission, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeCreateVolumePermission(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeCustomerGatewayIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDataQueries(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const DataQuery, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeDataQuery(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDedicatedHostIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDeleteQueuedReservedInstancesIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDescribeImageReferencesImageIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDescribeImageUsageReportsImageIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDescribeInstanceTopologyGroupNameSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDescribeInstanceTopologyInstanceIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDhcpOptionsIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeDiskImageList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const DiskImage, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeDiskImage(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeEgressOnlyInternetGatewayIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeEipAssociationIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeElasticGpuIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeElasticGpuSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ElasticGpuSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeElasticGpuSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeElasticGpuSpecifications(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ElasticGpuSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeElasticGpuSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeElasticInferenceAccelerators(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ElasticInferenceAccelerator, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeElasticInferenceAccelerator(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeExcludedInstanceTypeSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeExecutableByStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeExportImageTaskIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeExportTaskIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFastLaunchImageIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFilterList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Filter, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeFilter(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFleetBlockDeviceMappingRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const FleetBlockDeviceMappingRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeFleetBlockDeviceMappingRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFleetIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFleetLaunchTemplateConfigListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const FleetLaunchTemplateConfigRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeFleetLaunchTemplateConfigRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFleetLaunchTemplateOverridesListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const FleetLaunchTemplateOverridesRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeFleetLaunchTemplateOverridesRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFlowLogIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFlowLogResourceIds(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeFpgaImageIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeGroupBySet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const GroupBy, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeGroupIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeGroupIdentifierList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const GroupIdentifier, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeGroupIdentifier(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeGroupIds(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeGroupNameStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeHostReservationIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIKEVersionsRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IKEVersionsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIKEVersionsRequestListValue(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageCriterionRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageCriterionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeImageCriterionRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageDiskContainerList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageDiskContainer, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeImageDiskContainer(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageNameRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageProviderRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageUsageReportIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageUsageReportUserIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageUsageResourceTypeOptionRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageUsageResourceTypeOptionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeImageUsageResourceTypeOptionRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageUsageResourceTypeOptionValuesList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImageUsageResourceTypeRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImageUsageResourceTypeRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeImageUsageResourceTypeRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImportImageLicenseSpecificationListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ImportImageLicenseConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeImportImageLicenseConfigurationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImportSnapshotTaskIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeImportTaskIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInsideCidrBlocksStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceBlockDeviceMappingSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceBlockDeviceMappingSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeInstanceBlockDeviceMappingSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceCreditSpecificationListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceCreditSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeInstanceCreditSpecificationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceEventWindowIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceEventWindowTimeRangeRequestSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceEventWindowTimeRangeRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeInstanceEventWindowTimeRangeRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceGenerationSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceGeneration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceIdUpdateStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceIpv6AddressList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceIpv6Address, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeInstanceIpv6Address(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceIpv6AddressListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceIpv6AddressRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeInstanceIpv6AddressRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceNetworkInterfaceSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceNetworkInterfaceSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeInstanceNetworkInterfaceSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceTagKeySet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceTypeList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInstanceTypes(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeInternetGatewayIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpPermissionList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpPermission, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpPermission(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpPrefixList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpRangeList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpRange, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpRange(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpamPolicyAllocationRuleListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpamPolicyAllocationRuleRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpamPolicyAllocationRuleRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpamPoolAllocationAllowedCidrs(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpamPoolAllocationDisallowedCidrs(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpamPrefixListResolverRuleConditionRequestSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpamPrefixListResolverRuleConditionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpamPrefixListResolverRuleConditionRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpamPrefixListResolverRuleRequestSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const IpamPrefixListResolverRuleRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpamPrefixListResolverRuleRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpamPrefixListResolverVersionNumberSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const i64, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{item}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpv4PrefixList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Ipv4PrefixSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpv4PrefixSpecificationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpv6AddressList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpv6PoolIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpv6PrefixList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Ipv6PrefixSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpv6PrefixSpecificationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeIpv6RangeList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Ipv6Range, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeIpv6Range(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeKeyNameStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeKeyPairIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchPermissionList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchPermission, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchPermission(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchSpecsList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SpotFleetLaunchSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeSpotFleetLaunchSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateBlockDeviceMappingRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateBlockDeviceMappingRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchTemplateBlockDeviceMappingRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateConfigList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateConfig, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchTemplateConfig(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateElasticInferenceAcceleratorList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateElasticInferenceAccelerator, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchTemplateElasticInferenceAccelerator(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateInstanceNetworkInterfaceSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateLicenseSpecificationListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateLicenseConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchTemplateLicenseConfigurationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateNameStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateOverridesList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateOverrides, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchTemplateOverrides(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLaunchTemplateTagSpecificationRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LaunchTemplateTagSpecificationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLaunchTemplateTagSpecificationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLicenseSpecificationListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LicenseConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLicenseConfigurationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLoadPermissionListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LoadPermissionRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeLoadPermissionRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLocalGatewayIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLocalGatewayRouteTableIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLocalGatewayRouteTableVirtualInterfaceGroupAssociationIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLocalGatewayRouteTableVpcAssociationIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLocalGatewayVirtualInterfaceGroupIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLocalGatewayVirtualInterfaceIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeLocalStorageTypeSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const LocalStorageType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeMacModificationTaskIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeMarketplaceProductCodeRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeMetricSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Metric, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeModifyVerifiedAccessEndpointPortRangeList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ModifyVerifiedAccessEndpointPortRange, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeModifyVerifiedAccessEndpointPortRange(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeModifyVerifiedAccessEndpointSubnetIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNatGatewayIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNetworkAclIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNetworkInsightsAccessScopeAnalysisIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNetworkInsightsAccessScopeIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNetworkInsightsAnalysisIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNetworkInsightsPathIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNetworkInterfaceIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNetworkInterfacePermissionIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeNewDhcpConfigurationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const NewDhcpConfiguration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeNewDhcpConfiguration(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeOccurrenceDayRequestSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const i32, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{item}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeOrganizationArnStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeOrganizationalUnitArnStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeOutpostLagIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeOwnerStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePerformanceFactorReferenceSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PerformanceFactorReference, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePerformanceFactorReference(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePerformanceFactorReferenceSetRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PerformanceFactorReferenceRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePerformanceFactorReferenceRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePhase1DHGroupNumbersRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase1DHGroupNumbersRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePhase1DHGroupNumbersRequestListValue(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePhase1EncryptionAlgorithmsRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase1EncryptionAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePhase1EncryptionAlgorithmsRequestListValue(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePhase1IntegrityAlgorithmsRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase1IntegrityAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePhase1IntegrityAlgorithmsRequestListValue(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePhase2DHGroupNumbersRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase2DHGroupNumbersRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePhase2DHGroupNumbersRequestListValue(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePhase2EncryptionAlgorithmsRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase2EncryptionAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePhase2EncryptionAlgorithmsRequestListValue(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePhase2IntegrityAlgorithmsRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Phase2IntegrityAlgorithmsRequestListValue, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePhase2IntegrityAlgorithmsRequestListValue(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePlacementGroupIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePlacementGroupStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePrefixListIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PrefixListId, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePrefixListId(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePrefixListResourceIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePriceScheduleSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PriceScheduleSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePriceScheduleSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePrivateDnsSpecifiedDomainSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePrivateIpAddressConfigSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesPrivateIpAddressConfig, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeScheduledInstancesPrivateIpAddressConfig(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePrivateIpAddressSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PrivateIpAddressSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePrivateIpAddressSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePrivateIpAddressStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeProductCodeStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeProductDescriptionList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeProtocolList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Protocol, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePublicIpStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePublicIpv4PoolIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializePurchaseRequestSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const PurchaseRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializePurchaseRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReasonCodesList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ReportInstanceReasonCodes, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRegionNameStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRegionNames(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRemoveIpamOperatingRegionSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RemoveIpamOperatingRegion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeRemoveIpamOperatingRegion(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRemoveIpamOrganizationalUnitExclusionSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RemoveIpamOrganizationalUnitExclusion, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeRemoveIpamOrganizationalUnitExclusion(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRemovePrefixListEntries(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RemovePrefixListEntry, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeRemovePrefixListEntry(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReplaceRootVolumeTaskIds(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRequestHostIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRequestHostIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRequestInstanceTypeList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const InstanceType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRequestIpamResourceTagList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const RequestIpamResourceTag, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeRequestIpamResourceTag(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRequestSpotLaunchSpecificationSecurityGroupIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRequestSpotLaunchSpecificationSecurityGroupList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReservationFleetInstanceSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ReservationFleetInstanceSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeReservationFleetInstanceSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReservedInstanceIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReservedInstancesConfigurationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ReservedInstancesConfiguration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeReservedInstancesConfiguration(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReservedInstancesIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReservedInstancesModificationIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeReservedInstancesOfferingIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeResourceIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeResourceList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeResourceTypeOptionList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ResourceTypeOption, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeResourceTypeOption(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeResourceTypeOptionValuesList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeResourceTypeRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ResourceTypeRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeResourceTypeRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRestorableByStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRouteServerEndpointIdsList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRouteServerIdsList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRouteServerPeerIdsList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeRouteTableIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeS3ObjectTagList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const S3ObjectTag, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeS3ObjectTag(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeScheduledInstanceIdRequestSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeScheduledInstancesBlockDeviceMappingSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesBlockDeviceMapping, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeScheduledInstancesBlockDeviceMapping(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeScheduledInstancesIpv6AddressList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesIpv6Address, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeScheduledInstancesIpv6Address(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeScheduledInstancesNetworkInterfaceSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ScheduledInstancesNetworkInterface, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeScheduledInstancesNetworkInterface(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeScheduledInstancesSecurityGroupIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSecurityGroupIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSecurityGroupIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSecurityGroupIdStringListRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSecurityGroupRuleDescriptionList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SecurityGroupRuleDescription, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeSecurityGroupRuleDescription(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSecurityGroupRuleIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSecurityGroupRuleUpdateList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SecurityGroupRuleUpdate, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeSecurityGroupRuleUpdate(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSecurityGroupStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeServiceLinkVirtualInterfaceIdSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSnapshotIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSpotFleetRequestIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSpotFleetTagSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SpotFleetTagSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeSpotFleetTagSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSpotInstanceRequestIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSubnetConfigurationsList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const SubnetConfiguration, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeSubnetConfiguration(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSubnetIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeSubnetIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTagList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const Tag, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeTag(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTagSpecificationList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TagSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeTagSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTargetConfigurationRequestSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TargetConfigurationRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeTargetConfigurationRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTargetGroups(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TargetGroup, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeTargetGroup(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeThroughResourcesStatementRequestList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const ThroughResourcesStatementRequest, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeThroughResourcesStatementRequest(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrafficMirrorFilterIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrafficMirrorFilterRuleFieldList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TrafficMirrorFilterRuleField, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrafficMirrorFilterRuleIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrafficMirrorNetworkServiceList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TrafficMirrorNetworkService, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrafficMirrorSessionFieldList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const TrafficMirrorSessionField, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrafficMirrorSessionIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrafficMirrorTargetIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayAttachmentIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayCidrBlockStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayConnectPeerIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayMeteringPolicyIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayMulticastDomainIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayNetworkInterfaceIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayPolicyTableIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayRouteTableAnnouncementIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewayRouteTableIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTransitGatewaySubnetIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeTrunkInterfaceAssociationIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeUserGroupStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeUserIdGroupPairList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const UserIdGroupPair, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeUserIdGroupPair(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeUserIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeValueStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVerifiedAccessEndpointIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVerifiedAccessGroupIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVerifiedAccessInstanceIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVerifiedAccessTrustProviderIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVersionStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVirtualizationTypeSet(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const VirtualizationType, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try buf.appendSlice(alloc, @tagName(item));
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVolumeIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcBlockPublicAccessExclusionIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcClassicLinkIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcEncryptionControlIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcEndpointIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcEndpointRouteTableIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcEndpointSecurityGroupIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcEndpointServiceIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcEndpointSubnetIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpcPeeringConnectionIdList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpnConcentratorIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpnConnectionIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpnGatewayIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeVpnTunnelOptionsSpecificationsList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const VpnTunnelOptionsSpecification, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try serializeVpnTunnelOptionsSpecification(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeZoneIdStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeZoneNameStringList(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: []const []const u8, comptime item_tag: []const u8) !void {
    for (value) |item| {
        try buf.appendSlice(alloc, "<");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
        try aws.xml.appendXmlEscaped(alloc, buf, item);
        try buf.appendSlice(alloc, "</");
        try buf.appendSlice(alloc, item_tag);
        try buf.appendSlice(alloc, ">");
    }
}

pub fn serializeAcceleratorCount(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorCount) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeAcceleratorCountRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorCountRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<Min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Min>");
    }
}

pub fn serializeAcceleratorTotalMemoryMiB(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorTotalMemoryMiB) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeAcceleratorTotalMemoryMiBRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AcceleratorTotalMemoryMiBRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<Min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Min>");
    }
}

pub fn serializeAccessScopePathRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AccessScopePathRequest) !void {
    if (value.destination) |v| {
        try buf.appendSlice(alloc, "<Destination>");
        try serializePathStatementRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Destination>");
    }
    if (value.source) |v| {
        try buf.appendSlice(alloc, "<Source>");
        try serializePathStatementRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Source>");
    }
    if (value.through_resources) |v| {
        try buf.appendSlice(alloc, "<ThroughResource>");
        try serializeThroughResourcesStatementRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ThroughResource>");
    }
}

pub fn serializeAddIpamOperatingRegion(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AddIpamOperatingRegion) !void {
    if (value.region_name) |v| {
        try buf.appendSlice(alloc, "<RegionName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RegionName>");
    }
}

pub fn serializeAddIpamOrganizationalUnitExclusion(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AddIpamOrganizationalUnitExclusion) !void {
    if (value.organizations_entity_path) |v| {
        try buf.appendSlice(alloc, "<OrganizationsEntityPath>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</OrganizationsEntityPath>");
    }
}

pub fn serializeAddPrefixListEntry(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AddPrefixListEntry) !void {
    try buf.appendSlice(alloc, "<Cidr>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.cidr);
    try buf.appendSlice(alloc, "</Cidr>");
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
}

pub fn serializeAsnAuthorizationContext(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AsnAuthorizationContext) !void {
    try buf.appendSlice(alloc, "<Message>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.message);
    try buf.appendSlice(alloc, "</Message>");
    try buf.appendSlice(alloc, "<Signature>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.signature);
    try buf.appendSlice(alloc, "</Signature>");
}

pub fn serializeAthenaIntegration(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AthenaIntegration) !void {
    try buf.appendSlice(alloc, "<IntegrationResultS3DestinationArn>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.integration_result_s3_destination_arn);
    try buf.appendSlice(alloc, "</IntegrationResultS3DestinationArn>");
    if (value.partition_end_date) |v| {
        try buf.appendSlice(alloc, "<PartitionEndDate>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</PartitionEndDate>");
    }
    try buf.appendSlice(alloc, "<PartitionLoadFrequency>");
    try buf.appendSlice(alloc, @tagName(value.partition_load_frequency));
    try buf.appendSlice(alloc, "</PartitionLoadFrequency>");
    if (value.partition_start_date) |v| {
        try buf.appendSlice(alloc, "<PartitionStartDate>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</PartitionStartDate>");
    }
}

pub fn serializeAttributeBooleanValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AttributeBooleanValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<value>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</value>");
    }
}

pub fn serializeAttributeValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AttributeValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</value>");
    }
}

pub fn serializeAvailabilityZoneAddress(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: AvailabilityZoneAddress) !void {
    if (value.allocation_ids) |v| {
        try buf.appendSlice(alloc, "<AllocationId>");
        try serializeAllocationIdList(alloc, buf, v, "AllocationId");
        try buf.appendSlice(alloc, "</AllocationId>");
    }
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZoneId>");
    }
}

pub fn serializeBaselineEbsBandwidthMbps(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselineEbsBandwidthMbps) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeBaselineEbsBandwidthMbpsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselineEbsBandwidthMbpsRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<Min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Min>");
    }
}

pub fn serializeBaselinePerformanceFactors(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselinePerformanceFactors) !void {
    if (value.cpu) |v| {
        try buf.appendSlice(alloc, "<cpu>");
        try serializeCpuPerformanceFactor(alloc, buf, v);
        try buf.appendSlice(alloc, "</cpu>");
    }
}

pub fn serializeBaselinePerformanceFactorsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: BaselinePerformanceFactorsRequest) !void {
    if (value.cpu) |v| {
        try buf.appendSlice(alloc, "<Cpu>");
        try serializeCpuPerformanceFactorRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Cpu>");
    }
}

pub fn serializeBlobAttributeValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: BlobAttributeValue) !void {
    _ = alloc;
    _ = buf;
    _ = value;
}

pub fn serializeBlockDeviceMapping(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: BlockDeviceMapping) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(alloc, "<deviceName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</deviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(alloc, "<ebs>");
        try serializeEbsBlockDevice(alloc, buf, v);
        try buf.appendSlice(alloc, "</ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(alloc, "<noDevice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</noDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(alloc, "<virtualName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</virtualName>");
    }
}

pub fn serializeCapacityManagerCondition(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityManagerCondition) !void {
    if (value.dimension_condition) |v| {
        try buf.appendSlice(alloc, "<DimensionCondition>");
        try serializeDimensionCondition(alloc, buf, v);
        try buf.appendSlice(alloc, "</DimensionCondition>");
    }
}

pub fn serializeCapacityReservationOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityReservationOptionsRequest) !void {
    if (value.usage_strategy) |v| {
        try buf.appendSlice(alloc, "<UsageStrategy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</UsageStrategy>");
    }
}

pub fn serializeCapacityReservationSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityReservationSpecification) !void {
    if (value.capacity_reservation_preference) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationPreference>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</CapacityReservationPreference>");
    }
    if (value.capacity_reservation_target) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationTarget>");
        try serializeCapacityReservationTarget(alloc, buf, v);
        try buf.appendSlice(alloc, "</CapacityReservationTarget>");
    }
}

pub fn serializeCapacityReservationTarget(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CapacityReservationTarget) !void {
    if (value.capacity_reservation_id) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</CapacityReservationId>");
    }
    if (value.capacity_reservation_resource_group_arn) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationResourceGroupArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</CapacityReservationResourceGroupArn>");
    }
}

pub fn serializeCertificateAuthenticationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CertificateAuthenticationRequest) !void {
    if (value.client_root_certificate_chain_arn) |v| {
        try buf.appendSlice(alloc, "<ClientRootCertificateChainArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientRootCertificateChainArn>");
    }
}

pub fn serializeCidrAuthorizationContext(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CidrAuthorizationContext) !void {
    try buf.appendSlice(alloc, "<Message>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.message);
    try buf.appendSlice(alloc, "</Message>");
    try buf.appendSlice(alloc, "<Signature>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.signature);
    try buf.appendSlice(alloc, "</Signature>");
}

pub fn serializeClassicLoadBalancer(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClassicLoadBalancer) !void {
    if (value.name) |v| {
        try buf.appendSlice(alloc, "<name>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</name>");
    }
}

pub fn serializeClassicLoadBalancersConfig(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClassicLoadBalancersConfig) !void {
    if (value.classic_load_balancers) |v| {
        try buf.appendSlice(alloc, "<classicLoadBalancers>");
        try serializeClassicLoadBalancers(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</classicLoadBalancers>");
    }
}

pub fn serializeClientConnectOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientConnectOptions) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
    if (value.lambda_function_arn) |v| {
        try buf.appendSlice(alloc, "<LambdaFunctionArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LambdaFunctionArn>");
    }
}

pub fn serializeClientData(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientData) !void {
    if (value.comment) |v| {
        try buf.appendSlice(alloc, "<Comment>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Comment>");
    }
    if (value.upload_end) |v| {
        try buf.appendSlice(alloc, "<UploadEnd>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</UploadEnd>");
    }
    if (value.upload_size) |v| {
        try buf.appendSlice(alloc, "<UploadSize>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</UploadSize>");
    }
    if (value.upload_start) |v| {
        try buf.appendSlice(alloc, "<UploadStart>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</UploadStart>");
    }
}

pub fn serializeClientLoginBannerOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientLoginBannerOptions) !void {
    if (value.banner_text) |v| {
        try buf.appendSlice(alloc, "<BannerText>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</BannerText>");
    }
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
}

pub fn serializeClientRouteEnforcementOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientRouteEnforcementOptions) !void {
    if (value.enforced) |v| {
        try buf.appendSlice(alloc, "<Enforced>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enforced>");
    }
}

pub fn serializeClientVpnAuthenticationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ClientVpnAuthenticationRequest) !void {
    if (value.active_directory) |v| {
        try buf.appendSlice(alloc, "<ActiveDirectory>");
        try serializeDirectoryServiceAuthenticationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</ActiveDirectory>");
    }
    if (value.federated_authentication) |v| {
        try buf.appendSlice(alloc, "<FederatedAuthentication>");
        try serializeFederatedAuthenticationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</FederatedAuthentication>");
    }
    if (value.mutual_authentication) |v| {
        try buf.appendSlice(alloc, "<MutualAuthentication>");
        try serializeCertificateAuthenticationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</MutualAuthentication>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(alloc, "<Type>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Type>");
    }
}

pub fn serializeCloudWatchLogOptionsSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CloudWatchLogOptionsSpecification) !void {
    if (value.bgp_log_enabled) |v| {
        try buf.appendSlice(alloc, "<BgpLogEnabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</BgpLogEnabled>");
    }
    if (value.bgp_log_group_arn) |v| {
        try buf.appendSlice(alloc, "<BgpLogGroupArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</BgpLogGroupArn>");
    }
    if (value.bgp_log_output_format) |v| {
        try buf.appendSlice(alloc, "<BgpLogOutputFormat>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</BgpLogOutputFormat>");
    }
    if (value.log_enabled) |v| {
        try buf.appendSlice(alloc, "<LogEnabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</LogEnabled>");
    }
    if (value.log_group_arn) |v| {
        try buf.appendSlice(alloc, "<LogGroupArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LogGroupArn>");
    }
    if (value.log_output_format) |v| {
        try buf.appendSlice(alloc, "<LogOutputFormat>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LogOutputFormat>");
    }
}

pub fn serializeConnectionLogOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ConnectionLogOptions) !void {
    if (value.cloudwatch_log_group) |v| {
        try buf.appendSlice(alloc, "<CloudwatchLogGroup>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</CloudwatchLogGroup>");
    }
    if (value.cloudwatch_log_stream) |v| {
        try buf.appendSlice(alloc, "<CloudwatchLogStream>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</CloudwatchLogStream>");
    }
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
}

pub fn serializeConnectionTrackingSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ConnectionTrackingSpecificationRequest) !void {
    if (value.tcp_established_timeout) |v| {
        try buf.appendSlice(alloc, "<TcpEstablishedTimeout>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</TcpEstablishedTimeout>");
    }
    if (value.udp_stream_timeout) |v| {
        try buf.appendSlice(alloc, "<UdpStreamTimeout>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</UdpStreamTimeout>");
    }
    if (value.udp_timeout) |v| {
        try buf.appendSlice(alloc, "<UdpTimeout>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</UdpTimeout>");
    }
}

pub fn serializeCpuOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CpuOptionsRequest) !void {
    if (value.amd_sev_snp) |v| {
        try buf.appendSlice(alloc, "<AmdSevSnp>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AmdSevSnp>");
    }
    if (value.core_count) |v| {
        try buf.appendSlice(alloc, "<CoreCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</CoreCount>");
    }
    if (value.threads_per_core) |v| {
        try buf.appendSlice(alloc, "<ThreadsPerCore>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ThreadsPerCore>");
    }
}

pub fn serializeCpuPerformanceFactor(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CpuPerformanceFactor) !void {
    if (value.references) |v| {
        try buf.appendSlice(alloc, "<referenceSet>");
        try serializePerformanceFactorReferenceSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</referenceSet>");
    }
}

pub fn serializeCpuPerformanceFactorRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CpuPerformanceFactorRequest) !void {
    if (value.references) |v| {
        try buf.appendSlice(alloc, "<Reference>");
        try serializePerformanceFactorReferenceSetRequest(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Reference>");
    }
}

pub fn serializeCreateTransitGatewayConnectRequestOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayConnectRequestOptions) !void {
    try buf.appendSlice(alloc, "<Protocol>");
    try buf.appendSlice(alloc, @tagName(value.protocol));
    try buf.appendSlice(alloc, "</Protocol>");
}

pub fn serializeCreateTransitGatewayMulticastDomainRequestOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayMulticastDomainRequestOptions) !void {
    if (value.auto_accept_shared_associations) |v| {
        try buf.appendSlice(alloc, "<AutoAcceptSharedAssociations>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AutoAcceptSharedAssociations>");
    }
    if (value.igmpv_2_support) |v| {
        try buf.appendSlice(alloc, "<Igmpv2Support>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Igmpv2Support>");
    }
    if (value.static_sources_support) |v| {
        try buf.appendSlice(alloc, "<StaticSourcesSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</StaticSourcesSupport>");
    }
}

pub fn serializeCreateTransitGatewayPeeringAttachmentRequestOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayPeeringAttachmentRequestOptions) !void {
    if (value.dynamic_routing) |v| {
        try buf.appendSlice(alloc, "<DynamicRouting>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DynamicRouting>");
    }
}

pub fn serializeCreateTransitGatewayVpcAttachmentRequestOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateTransitGatewayVpcAttachmentRequestOptions) !void {
    if (value.appliance_mode_support) |v| {
        try buf.appendSlice(alloc, "<ApplianceModeSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ApplianceModeSupport>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(alloc, "<DnsSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DnsSupport>");
    }
    if (value.ipv_6_support) |v| {
        try buf.appendSlice(alloc, "<Ipv6Support>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Ipv6Support>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</SecurityGroupReferencingSupport>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointCidrOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointCidrOptions) !void {
    if (value.cidr) |v| {
        try buf.appendSlice(alloc, "<Cidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Cidr>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(alloc, "<PortRange>");
        try serializeCreateVerifiedAccessEndpointPortRangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(alloc, "<Protocol>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Protocol>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try serializeCreateVerifiedAccessEndpointSubnetIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointEniOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointEniOptions) !void {
    if (value.network_interface_id) |v| {
        try buf.appendSlice(alloc, "<NetworkInterfaceId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</NetworkInterfaceId>");
    }
    if (value.port) |v| {
        try buf.appendSlice(alloc, "<Port>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(alloc, "<PortRange>");
        try serializeCreateVerifiedAccessEndpointPortRangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(alloc, "<Protocol>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Protocol>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointLoadBalancerOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointLoadBalancerOptions) !void {
    if (value.load_balancer_arn) |v| {
        try buf.appendSlice(alloc, "<LoadBalancerArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LoadBalancerArn>");
    }
    if (value.port) |v| {
        try buf.appendSlice(alloc, "<Port>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(alloc, "<PortRange>");
        try serializeCreateVerifiedAccessEndpointPortRangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(alloc, "<Protocol>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Protocol>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try serializeCreateVerifiedAccessEndpointSubnetIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointPortRange(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointPortRange) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(alloc, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(alloc, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ToPort>");
    }
}

pub fn serializeCreateVerifiedAccessEndpointRdsOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessEndpointRdsOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(alloc, "<Port>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Port>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(alloc, "<Protocol>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Protocol>");
    }
    if (value.rds_db_cluster_arn) |v| {
        try buf.appendSlice(alloc, "<RdsDbClusterArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RdsDbClusterArn>");
    }
    if (value.rds_db_instance_arn) |v| {
        try buf.appendSlice(alloc, "<RdsDbInstanceArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RdsDbInstanceArn>");
    }
    if (value.rds_db_proxy_arn) |v| {
        try buf.appendSlice(alloc, "<RdsDbProxyArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RdsDbProxyArn>");
    }
    if (value.rds_endpoint) |v| {
        try buf.appendSlice(alloc, "<RdsEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RdsEndpoint>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try serializeCreateVerifiedAccessEndpointSubnetIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeCreateVerifiedAccessNativeApplicationOidcOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessNativeApplicationOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(alloc, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(alloc, "<ClientId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(alloc, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(alloc, "<Issuer>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Issuer>");
    }
    if (value.public_signing_key_endpoint) |v| {
        try buf.appendSlice(alloc, "<PublicSigningKeyEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PublicSigningKeyEndpoint>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(alloc, "<Scope>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(alloc, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(alloc, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserInfoEndpoint>");
    }
}

pub fn serializeCreateVerifiedAccessTrustProviderDeviceOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessTrustProviderDeviceOptions) !void {
    if (value.public_signing_key_url) |v| {
        try buf.appendSlice(alloc, "<PublicSigningKeyUrl>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PublicSigningKeyUrl>");
    }
    if (value.tenant_id) |v| {
        try buf.appendSlice(alloc, "<TenantId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TenantId>");
    }
}

pub fn serializeCreateVerifiedAccessTrustProviderOidcOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVerifiedAccessTrustProviderOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(alloc, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(alloc, "<ClientId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(alloc, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(alloc, "<Issuer>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Issuer>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(alloc, "<Scope>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(alloc, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(alloc, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserInfoEndpoint>");
    }
}

pub fn serializeCreateVolumePermission(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVolumePermission) !void {
    if (value.group) |v| {
        try buf.appendSlice(alloc, "<group>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</group>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(alloc, "<userId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</userId>");
    }
}

pub fn serializeCreateVolumePermissionModifications(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreateVolumePermissionModifications) !void {
    if (value.add) |v| {
        try buf.appendSlice(alloc, "<Add>");
        try serializeCreateVolumePermissionList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Add>");
    }
    if (value.remove) |v| {
        try buf.appendSlice(alloc, "<Remove>");
        try serializeCreateVolumePermissionList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Remove>");
    }
}

pub fn serializeCreationDateConditionRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreationDateConditionRequest) !void {
    if (value.maximum_days_since_created) |v| {
        try buf.appendSlice(alloc, "<MaximumDaysSinceCreated>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</MaximumDaysSinceCreated>");
    }
}

pub fn serializeCreditSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: CreditSpecificationRequest) !void {
    try buf.appendSlice(alloc, "<CpuCredits>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.cpu_credits);
    try buf.appendSlice(alloc, "</CpuCredits>");
}

pub fn serializeDataQuery(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DataQuery) !void {
    if (value.destination) |v| {
        try buf.appendSlice(alloc, "<Destination>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Destination>");
    }
    if (value.id) |v| {
        try buf.appendSlice(alloc, "<Id>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Id>");
    }
    if (value.metric) |v| {
        try buf.appendSlice(alloc, "<Metric>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Metric>");
    }
    if (value.period) |v| {
        try buf.appendSlice(alloc, "<Period>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Period>");
    }
    if (value.source) |v| {
        try buf.appendSlice(alloc, "<Source>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Source>");
    }
    if (value.statistic) |v| {
        try buf.appendSlice(alloc, "<Statistic>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Statistic>");
    }
}

pub fn serializeDeprecationTimeConditionRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DeprecationTimeConditionRequest) !void {
    if (value.maximum_days_since_deprecated) |v| {
        try buf.appendSlice(alloc, "<MaximumDaysSinceDeprecated>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</MaximumDaysSinceDeprecated>");
    }
}

pub fn serializeDeregisterInstanceTagAttributeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DeregisterInstanceTagAttributeRequest) !void {
    if (value.include_all_tags_of_instance) |v| {
        try buf.appendSlice(alloc, "<IncludeAllTagsOfInstance>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</IncludeAllTagsOfInstance>");
    }
    if (value.instance_tag_keys) |v| {
        try buf.appendSlice(alloc, "<InstanceTagKey>");
        try serializeInstanceTagKeySet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</InstanceTagKey>");
    }
}

pub fn serializeDestinationOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DestinationOptionsRequest) !void {
    if (value.file_format) |v| {
        try buf.appendSlice(alloc, "<FileFormat>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</FileFormat>");
    }
    if (value.hive_compatible_partitions) |v| {
        try buf.appendSlice(alloc, "<HiveCompatiblePartitions>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</HiveCompatiblePartitions>");
    }
    if (value.per_hour_partition) |v| {
        try buf.appendSlice(alloc, "<PerHourPartition>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</PerHourPartition>");
    }
}

pub fn serializeDimensionCondition(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DimensionCondition) !void {
    if (value.comparison) |v| {
        try buf.appendSlice(alloc, "<Comparison>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Comparison>");
    }
    if (value.dimension) |v| {
        try buf.appendSlice(alloc, "<Dimension>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Dimension>");
    }
    if (value.values) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try serializeConditionValueList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializeDirectoryServiceAuthenticationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DirectoryServiceAuthenticationRequest) !void {
    if (value.directory_id) |v| {
        try buf.appendSlice(alloc, "<DirectoryId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DirectoryId>");
    }
}

pub fn serializeDiskImage(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DiskImage) !void {
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
    if (value.image) |v| {
        try buf.appendSlice(alloc, "<Image>");
        try serializeDiskImageDetail(alloc, buf, v);
        try buf.appendSlice(alloc, "</Image>");
    }
    if (value.volume) |v| {
        try buf.appendSlice(alloc, "<Volume>");
        try serializeVolumeDetail(alloc, buf, v);
        try buf.appendSlice(alloc, "</Volume>");
    }
}

pub fn serializeDiskImageDetail(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DiskImageDetail) !void {
    try buf.appendSlice(alloc, "<bytes>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.bytes}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</bytes>");
    try buf.appendSlice(alloc, "<format>");
    try buf.appendSlice(alloc, @tagName(value.format));
    try buf.appendSlice(alloc, "</format>");
    try buf.appendSlice(alloc, "<importManifestUrl>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.import_manifest_url);
    try buf.appendSlice(alloc, "</importManifestUrl>");
}

pub fn serializeDnsOptionsSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DnsOptionsSpecification) !void {
    if (value.dns_record_ip_type) |v| {
        try buf.appendSlice(alloc, "<DnsRecordIpType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DnsRecordIpType>");
    }
    if (value.private_dns_only_for_inbound_resolver_endpoint) |v| {
        try buf.appendSlice(alloc, "<PrivateDnsOnlyForInboundResolverEndpoint>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</PrivateDnsOnlyForInboundResolverEndpoint>");
    }
    if (value.private_dns_preference) |v| {
        try buf.appendSlice(alloc, "<PrivateDnsPreference>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PrivateDnsPreference>");
    }
    if (value.private_dns_specified_domains) |v| {
        try buf.appendSlice(alloc, "<PrivateDnsSpecifiedDomain>");
        try serializePrivateDnsSpecifiedDomainSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PrivateDnsSpecifiedDomain>");
    }
}

pub fn serializeDnsServersOptionsModifyStructure(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: DnsServersOptionsModifyStructure) !void {
    if (value.custom_dns_servers) |v| {
        try buf.appendSlice(alloc, "<CustomDnsServers>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</CustomDnsServers>");
    }
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
}

pub fn serializeEbsBlockDevice(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: EbsBlockDevice) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZoneId>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<deleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</deleteOnTermination>");
    }
    if (value.ebs_card_index) |v| {
        try buf.appendSlice(alloc, "<EbsCardIndex>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</EbsCardIndex>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(alloc, "<encrypted>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(alloc, "<iops>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</iops>");
    }
    if (value.kms_key_id) |v| {
        try buf.appendSlice(alloc, "<kmsKeyId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</kmsKeyId>");
    }
    if (value.outpost_arn) |v| {
        try buf.appendSlice(alloc, "<outpostArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</outpostArn>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(alloc, "<snapshotId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</snapshotId>");
    }
    if (value.throughput) |v| {
        try buf.appendSlice(alloc, "<throughput>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</throughput>");
    }
    if (value.volume_initialization_rate) |v| {
        try buf.appendSlice(alloc, "<VolumeInitializationRate>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</VolumeInitializationRate>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(alloc, "<volumeSize>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</volumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(alloc, "<volumeType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</volumeType>");
    }
}

pub fn serializeEbsInstanceBlockDeviceSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: EbsInstanceBlockDeviceSpecification) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<deleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</deleteOnTermination>");
    }
    if (value.volume_id) |v| {
        try buf.appendSlice(alloc, "<volumeId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</volumeId>");
    }
}

pub fn serializeElasticGpuSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ElasticGpuSpecification) !void {
    try buf.appendSlice(alloc, "<Type>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.@"type");
    try buf.appendSlice(alloc, "</Type>");
}

pub fn serializeElasticInferenceAccelerator(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ElasticInferenceAccelerator) !void {
    if (value.count) |v| {
        try buf.appendSlice(alloc, "<Count>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Count>");
    }
    try buf.appendSlice(alloc, "<Type>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.@"type");
    try buf.appendSlice(alloc, "</Type>");
}

pub fn serializeEnaSrdSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdSpecification) !void {
    if (value.ena_srd_enabled) |v| {
        try buf.appendSlice(alloc, "<EnaSrdEnabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnaSrdEnabled>");
    }
    if (value.ena_srd_udp_specification) |v| {
        try buf.appendSlice(alloc, "<EnaSrdUdpSpecification>");
        try serializeEnaSrdUdpSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</EnaSrdUdpSpecification>");
    }
}

pub fn serializeEnaSrdSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdSpecificationRequest) !void {
    if (value.ena_srd_enabled) |v| {
        try buf.appendSlice(alloc, "<EnaSrdEnabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnaSrdEnabled>");
    }
    if (value.ena_srd_udp_specification) |v| {
        try buf.appendSlice(alloc, "<EnaSrdUdpSpecification>");
        try serializeEnaSrdUdpSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</EnaSrdUdpSpecification>");
    }
}

pub fn serializeEnaSrdUdpSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdUdpSpecification) !void {
    if (value.ena_srd_udp_enabled) |v| {
        try buf.appendSlice(alloc, "<EnaSrdUdpEnabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnaSrdUdpEnabled>");
    }
}

pub fn serializeEnaSrdUdpSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnaSrdUdpSpecificationRequest) !void {
    if (value.ena_srd_udp_enabled) |v| {
        try buf.appendSlice(alloc, "<EnaSrdUdpEnabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnaSrdUdpEnabled>");
    }
}

pub fn serializeEnclaveOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: EnclaveOptionsRequest) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
}

pub fn serializeExportTaskS3LocationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ExportTaskS3LocationRequest) !void {
    try buf.appendSlice(alloc, "<S3Bucket>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.s3_bucket);
    try buf.appendSlice(alloc, "</S3Bucket>");
    if (value.s3_prefix) |v| {
        try buf.appendSlice(alloc, "<S3Prefix>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</S3Prefix>");
    }
}

pub fn serializeExportToS3TaskSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ExportToS3TaskSpecification) !void {
    if (value.container_format) |v| {
        try buf.appendSlice(alloc, "<containerFormat>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</containerFormat>");
    }
    if (value.disk_image_format) |v| {
        try buf.appendSlice(alloc, "<diskImageFormat>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</diskImageFormat>");
    }
    if (value.s3_bucket) |v| {
        try buf.appendSlice(alloc, "<s3Bucket>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</s3Bucket>");
    }
    if (value.s3_prefix) |v| {
        try buf.appendSlice(alloc, "<s3Prefix>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</s3Prefix>");
    }
}

pub fn serializeExternalAuthorityConfiguration(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ExternalAuthorityConfiguration) !void {
    if (value.external_resource_identifier) |v| {
        try buf.appendSlice(alloc, "<ExternalResourceIdentifier>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ExternalResourceIdentifier>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(alloc, "<Type>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Type>");
    }
}

pub fn serializeFastLaunchLaunchTemplateSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FastLaunchLaunchTemplateSpecificationRequest) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(alloc, "<LaunchTemplateId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LaunchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(alloc, "<LaunchTemplateName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LaunchTemplateName>");
    }
    try buf.appendSlice(alloc, "<Version>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.version);
    try buf.appendSlice(alloc, "</Version>");
}

pub fn serializeFastLaunchSnapshotConfigurationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FastLaunchSnapshotConfigurationRequest) !void {
    if (value.target_resource_count) |v| {
        try buf.appendSlice(alloc, "<TargetResourceCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</TargetResourceCount>");
    }
}

pub fn serializeFederatedAuthenticationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FederatedAuthenticationRequest) !void {
    if (value.saml_provider_arn) |v| {
        try buf.appendSlice(alloc, "<SAMLProviderArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SAMLProviderArn>");
    }
    if (value.self_service_saml_provider_arn) |v| {
        try buf.appendSlice(alloc, "<SelfServiceSAMLProviderArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SelfServiceSAMLProviderArn>");
    }
}

pub fn serializeFilter(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Filter) !void {
    if (value.name) |v| {
        try buf.appendSlice(alloc, "<Name>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Name>");
    }
    if (value.values) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializeFleetBlockDeviceMappingRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetBlockDeviceMappingRequest) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(alloc, "<DeviceName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DeviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(alloc, "<Ebs>");
        try serializeFleetEbsBlockDeviceRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(alloc, "<NoDevice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</NoDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(alloc, "<VirtualName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</VirtualName>");
    }
}

pub fn serializeFleetEbsBlockDeviceRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetEbsBlockDeviceRequest) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<DeleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DeleteOnTermination>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(alloc, "<Encrypted>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(alloc, "<Iops>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Iops>");
    }
    if (value.kms_key_id) |v| {
        try buf.appendSlice(alloc, "<KmsKeyId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</KmsKeyId>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(alloc, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SnapshotId>");
    }
    if (value.throughput) |v| {
        try buf.appendSlice(alloc, "<Throughput>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Throughput>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(alloc, "<VolumeSize>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</VolumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(alloc, "<VolumeType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</VolumeType>");
    }
}

pub fn serializeFleetLaunchTemplateConfigRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateConfigRequest) !void {
    if (value.launch_template_specification) |v| {
        try buf.appendSlice(alloc, "<LaunchTemplateSpecification>");
        try serializeFleetLaunchTemplateSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</LaunchTemplateSpecification>");
    }
    if (value.overrides) |v| {
        try buf.appendSlice(alloc, "<Overrides>");
        try serializeFleetLaunchTemplateOverridesListRequest(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Overrides>");
    }
}

pub fn serializeFleetLaunchTemplateOverridesRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateOverridesRequest) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZoneId>");
    }
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(alloc, "<BlockDeviceMapping>");
        try serializeFleetBlockDeviceMappingRequestList(alloc, buf, v, "BlockDeviceMapping");
        try buf.appendSlice(alloc, "</BlockDeviceMapping>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(alloc, "<ImageId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ImageId>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(alloc, "<InstanceRequirements>");
        try serializeInstanceRequirementsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</InstanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<InstanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceType>");
    }
    if (value.max_price) |v| {
        try buf.appendSlice(alloc, "<MaxPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</MaxPrice>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(alloc, "<Placement>");
        try serializePlacement(alloc, buf, v);
        try buf.appendSlice(alloc, "</Placement>");
    }
    if (value.priority) |v| {
        try buf.appendSlice(alloc, "<Priority>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Priority>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SubnetId>");
    }
    if (value.weighted_capacity) |v| {
        try buf.appendSlice(alloc, "<WeightedCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</WeightedCapacity>");
    }
}

pub fn serializeFleetLaunchTemplateSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateSpecification) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(alloc, "<launchTemplateId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</launchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(alloc, "<launchTemplateName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</launchTemplateName>");
    }
    if (value.version) |v| {
        try buf.appendSlice(alloc, "<version>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</version>");
    }
}

pub fn serializeFleetLaunchTemplateSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetLaunchTemplateSpecificationRequest) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(alloc, "<LaunchTemplateId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LaunchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(alloc, "<LaunchTemplateName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LaunchTemplateName>");
    }
    if (value.version) |v| {
        try buf.appendSlice(alloc, "<Version>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Version>");
    }
}

pub fn serializeFleetSpotCapacityRebalanceRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetSpotCapacityRebalanceRequest) !void {
    if (value.replacement_strategy) |v| {
        try buf.appendSlice(alloc, "<ReplacementStrategy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ReplacementStrategy>");
    }
    if (value.termination_delay) |v| {
        try buf.appendSlice(alloc, "<TerminationDelay>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</TerminationDelay>");
    }
}

pub fn serializeFleetSpotMaintenanceStrategiesRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: FleetSpotMaintenanceStrategiesRequest) !void {
    if (value.capacity_rebalance) |v| {
        try buf.appendSlice(alloc, "<CapacityRebalance>");
        try serializeFleetSpotCapacityRebalanceRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</CapacityRebalance>");
    }
}

pub fn serializeGroupIdentifier(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: GroupIdentifier) !void {
    if (value.group_id) |v| {
        try buf.appendSlice(alloc, "<groupId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</groupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(alloc, "<groupName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</groupName>");
    }
}

pub fn serializeHibernationOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: HibernationOptionsRequest) !void {
    if (value.configured) |v| {
        try buf.appendSlice(alloc, "<Configured>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Configured>");
    }
}

pub fn serializeIKEVersionsRequestListValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IKEVersionsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializeIamInstanceProfileSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IamInstanceProfileSpecification) !void {
    if (value.arn) |v| {
        try buf.appendSlice(alloc, "<arn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</arn>");
    }
    if (value.name) |v| {
        try buf.appendSlice(alloc, "<name>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</name>");
    }
}

pub fn serializeIcmpTypeCode(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IcmpTypeCode) !void {
    if (value.code) |v| {
        try buf.appendSlice(alloc, "<code>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</code>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(alloc, "<type>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</type>");
    }
}

pub fn serializeImageCriterionRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageCriterionRequest) !void {
    if (value.creation_date_condition) |v| {
        try buf.appendSlice(alloc, "<CreationDateCondition>");
        try serializeCreationDateConditionRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</CreationDateCondition>");
    }
    if (value.deprecation_time_condition) |v| {
        try buf.appendSlice(alloc, "<DeprecationTimeCondition>");
        try serializeDeprecationTimeConditionRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</DeprecationTimeCondition>");
    }
    if (value.image_names) |v| {
        try buf.appendSlice(alloc, "<ImageName>");
        try serializeImageNameRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ImageName>");
    }
    if (value.image_providers) |v| {
        try buf.appendSlice(alloc, "<ImageProvider>");
        try serializeImageProviderRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ImageProvider>");
    }
    if (value.marketplace_product_codes) |v| {
        try buf.appendSlice(alloc, "<MarketplaceProductCode>");
        try serializeMarketplaceProductCodeRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</MarketplaceProductCode>");
    }
}

pub fn serializeImageDiskContainer(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageDiskContainer) !void {
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
    if (value.device_name) |v| {
        try buf.appendSlice(alloc, "<DeviceName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DeviceName>");
    }
    if (value.format) |v| {
        try buf.appendSlice(alloc, "<Format>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Format>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(alloc, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SnapshotId>");
    }
    if (value.url) |v| {
        try buf.appendSlice(alloc, "<Url>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Url>");
    }
    if (value.user_bucket) |v| {
        try buf.appendSlice(alloc, "<UserBucket>");
        try serializeUserBucket(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserBucket>");
    }
}

pub fn serializeImageUsageResourceTypeOptionRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageUsageResourceTypeOptionRequest) !void {
    if (value.option_name) |v| {
        try buf.appendSlice(alloc, "<OptionName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</OptionName>");
    }
    if (value.option_values) |v| {
        try buf.appendSlice(alloc, "<OptionValue>");
        try serializeImageUsageResourceTypeOptionValuesList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</OptionValue>");
    }
}

pub fn serializeImageUsageResourceTypeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImageUsageResourceTypeRequest) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(alloc, "<ResourceType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceType>");
    }
    if (value.resource_type_options) |v| {
        try buf.appendSlice(alloc, "<ResourceTypeOption>");
        try serializeImageUsageResourceTypeOptionRequestList(alloc, buf, v, "member");
        try buf.appendSlice(alloc, "</ResourceTypeOption>");
    }
}

pub fn serializeImportImageLicenseConfigurationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImportImageLicenseConfigurationRequest) !void {
    if (value.license_configuration_arn) |v| {
        try buf.appendSlice(alloc, "<LicenseConfigurationArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LicenseConfigurationArn>");
    }
}

pub fn serializeImportInstanceLaunchSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ImportInstanceLaunchSpecification) !void {
    if (value.additional_info) |v| {
        try buf.appendSlice(alloc, "<additionalInfo>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</additionalInfo>");
    }
    if (value.architecture) |v| {
        try buf.appendSlice(alloc, "<architecture>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</architecture>");
    }
    if (value.group_ids) |v| {
        try buf.appendSlice(alloc, "<GroupId>");
        try serializeSecurityGroupIdStringList(alloc, buf, v, "SecurityGroupId");
        try buf.appendSlice(alloc, "</GroupId>");
    }
    if (value.group_names) |v| {
        try buf.appendSlice(alloc, "<GroupName>");
        try serializeSecurityGroupStringList(alloc, buf, v, "SecurityGroup");
        try buf.appendSlice(alloc, "</GroupName>");
    }
    if (value.instance_initiated_shutdown_behavior) |v| {
        try buf.appendSlice(alloc, "<instanceInitiatedShutdownBehavior>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</instanceInitiatedShutdownBehavior>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<instanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</instanceType>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(alloc, "<monitoring>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</monitoring>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(alloc, "<placement>");
        try serializePlacement(alloc, buf, v);
        try buf.appendSlice(alloc, "</placement>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(alloc, "<privateIpAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</privateIpAddress>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<subnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</subnetId>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(alloc, "<userData>");
        try serializeUserData(alloc, buf, v);
        try buf.appendSlice(alloc, "</userData>");
    }
}

pub fn serializeInstanceBlockDeviceMappingSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceBlockDeviceMappingSpecification) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(alloc, "<deviceName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</deviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(alloc, "<ebs>");
        try serializeEbsInstanceBlockDeviceSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(alloc, "<noDevice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</noDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(alloc, "<virtualName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</virtualName>");
    }
}

pub fn serializeInstanceCreditSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceCreditSpecificationRequest) !void {
    if (value.cpu_credits) |v| {
        try buf.appendSlice(alloc, "<CpuCredits>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</CpuCredits>");
    }
    try buf.appendSlice(alloc, "<InstanceId>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.instance_id);
    try buf.appendSlice(alloc, "</InstanceId>");
}

pub fn serializeInstanceEventWindowAssociationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceEventWindowAssociationRequest) !void {
    if (value.dedicated_host_ids) |v| {
        try buf.appendSlice(alloc, "<DedicatedHostId>");
        try serializeDedicatedHostIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</DedicatedHostId>");
    }
    if (value.instance_ids) |v| {
        try buf.appendSlice(alloc, "<InstanceId>");
        try serializeInstanceIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</InstanceId>");
    }
    if (value.instance_tags) |v| {
        try buf.appendSlice(alloc, "<InstanceTag>");
        try serializeTagList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</InstanceTag>");
    }
}

pub fn serializeInstanceEventWindowDisassociationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceEventWindowDisassociationRequest) !void {
    if (value.dedicated_host_ids) |v| {
        try buf.appendSlice(alloc, "<DedicatedHostId>");
        try serializeDedicatedHostIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</DedicatedHostId>");
    }
    if (value.instance_ids) |v| {
        try buf.appendSlice(alloc, "<InstanceId>");
        try serializeInstanceIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</InstanceId>");
    }
    if (value.instance_tags) |v| {
        try buf.appendSlice(alloc, "<InstanceTag>");
        try serializeTagList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</InstanceTag>");
    }
}

pub fn serializeInstanceEventWindowTimeRangeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceEventWindowTimeRangeRequest) !void {
    if (value.end_hour) |v| {
        try buf.appendSlice(alloc, "<EndHour>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</EndHour>");
    }
    if (value.end_week_day) |v| {
        try buf.appendSlice(alloc, "<EndWeekDay>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</EndWeekDay>");
    }
    if (value.start_hour) |v| {
        try buf.appendSlice(alloc, "<StartHour>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</StartHour>");
    }
    if (value.start_week_day) |v| {
        try buf.appendSlice(alloc, "<StartWeekDay>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</StartWeekDay>");
    }
}

pub fn serializeInstanceIpv6Address(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceIpv6Address) !void {
    if (value.ipv_6_address) |v| {
        try buf.appendSlice(alloc, "<ipv6Address>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ipv6Address>");
    }
    if (value.is_primary_ipv_6) |v| {
        try buf.appendSlice(alloc, "<isPrimaryIpv6>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</isPrimaryIpv6>");
    }
}

pub fn serializeInstanceIpv6AddressRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceIpv6AddressRequest) !void {
    if (value.ipv_6_address) |v| {
        try buf.appendSlice(alloc, "<Ipv6Address>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ipv6Address>");
    }
}

pub fn serializeInstanceMaintenanceOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceMaintenanceOptionsRequest) !void {
    if (value.auto_recovery) |v| {
        try buf.appendSlice(alloc, "<AutoRecovery>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AutoRecovery>");
    }
}

pub fn serializeInstanceMarketOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceMarketOptionsRequest) !void {
    if (value.market_type) |v| {
        try buf.appendSlice(alloc, "<MarketType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</MarketType>");
    }
    if (value.spot_options) |v| {
        try buf.appendSlice(alloc, "<SpotOptions>");
        try serializeSpotMarketOptions(alloc, buf, v);
        try buf.appendSlice(alloc, "</SpotOptions>");
    }
}

pub fn serializeInstanceMetadataOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceMetadataOptionsRequest) !void {
    if (value.http_endpoint) |v| {
        try buf.appendSlice(alloc, "<HttpEndpoint>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HttpEndpoint>");
    }
    if (value.http_protocol_ipv_6) |v| {
        try buf.appendSlice(alloc, "<HttpProtocolIpv6>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HttpProtocolIpv6>");
    }
    if (value.http_put_response_hop_limit) |v| {
        try buf.appendSlice(alloc, "<HttpPutResponseHopLimit>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</HttpPutResponseHopLimit>");
    }
    if (value.http_tokens) |v| {
        try buf.appendSlice(alloc, "<HttpTokens>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HttpTokens>");
    }
    if (value.instance_metadata_tags) |v| {
        try buf.appendSlice(alloc, "<InstanceMetadataTags>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceMetadataTags>");
    }
}

pub fn serializeInstanceNetworkInterfaceSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceNetworkInterfaceSpecification) !void {
    if (value.associate_carrier_ip_address) |v| {
        try buf.appendSlice(alloc, "<AssociateCarrierIpAddress>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</AssociateCarrierIpAddress>");
    }
    if (value.associate_public_ip_address) |v| {
        try buf.appendSlice(alloc, "<associatePublicIpAddress>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</associatePublicIpAddress>");
    }
    if (value.connection_tracking_specification) |v| {
        try buf.appendSlice(alloc, "<ConnectionTrackingSpecification>");
        try serializeConnectionTrackingSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</ConnectionTrackingSpecification>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<deleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</deleteOnTermination>");
    }
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</description>");
    }
    if (value.device_index) |v| {
        try buf.appendSlice(alloc, "<deviceIndex>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</deviceIndex>");
    }
    if (value.ena_queue_count) |v| {
        try buf.appendSlice(alloc, "<EnaQueueCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</EnaQueueCount>");
    }
    if (value.ena_srd_specification) |v| {
        try buf.appendSlice(alloc, "<EnaSrdSpecification>");
        try serializeEnaSrdSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</EnaSrdSpecification>");
    }
    if (value.groups) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupId>");
        try serializeSecurityGroupIdStringList(alloc, buf, v, "SecurityGroupId");
        try buf.appendSlice(alloc, "</SecurityGroupId>");
    }
    if (value.interface_type) |v| {
        try buf.appendSlice(alloc, "<InterfaceType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</InterfaceType>");
    }
    if (value.ipv_4_prefix_count) |v| {
        try buf.appendSlice(alloc, "<Ipv4PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Ipv4PrefixCount>");
    }
    if (value.ipv_4_prefixes) |v| {
        try buf.appendSlice(alloc, "<Ipv4Prefix>");
        try serializeIpv4PrefixList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Ipv4Prefix>");
    }
    if (value.ipv_6_address_count) |v| {
        try buf.appendSlice(alloc, "<ipv6AddressCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ipv6AddressCount>");
    }
    if (value.ipv_6_addresses) |v| {
        try buf.appendSlice(alloc, "<ipv6AddressesSet>");
        try serializeInstanceIpv6AddressList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ipv6AddressesSet>");
    }
    if (value.ipv_6_prefix_count) |v| {
        try buf.appendSlice(alloc, "<Ipv6PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Ipv6PrefixCount>");
    }
    if (value.ipv_6_prefixes) |v| {
        try buf.appendSlice(alloc, "<Ipv6Prefix>");
        try serializeIpv6PrefixList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Ipv6Prefix>");
    }
    if (value.network_card_index) |v| {
        try buf.appendSlice(alloc, "<NetworkCardIndex>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</NetworkCardIndex>");
    }
    if (value.network_interface_id) |v| {
        try buf.appendSlice(alloc, "<networkInterfaceId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</networkInterfaceId>");
    }
    if (value.primary_ipv_6) |v| {
        try buf.appendSlice(alloc, "<PrimaryIpv6>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</PrimaryIpv6>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(alloc, "<privateIpAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</privateIpAddress>");
    }
    if (value.private_ip_addresses) |v| {
        try buf.appendSlice(alloc, "<privateIpAddressesSet>");
        try serializePrivateIpAddressSpecificationList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</privateIpAddressesSet>");
    }
    if (value.secondary_private_ip_address_count) |v| {
        try buf.appendSlice(alloc, "<secondaryPrivateIpAddressCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</secondaryPrivateIpAddressCount>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<subnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</subnetId>");
    }
}

pub fn serializeInstanceNetworkPerformanceOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceNetworkPerformanceOptionsRequest) !void {
    if (value.bandwidth_weighting) |v| {
        try buf.appendSlice(alloc, "<BandwidthWeighting>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</BandwidthWeighting>");
    }
}

pub fn serializeInstanceRequirements(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceRequirements) !void {
    if (value.accelerator_count) |v| {
        try buf.appendSlice(alloc, "<acceleratorCount>");
        try serializeAcceleratorCount(alloc, buf, v);
        try buf.appendSlice(alloc, "</acceleratorCount>");
    }
    if (value.accelerator_manufacturers) |v| {
        try buf.appendSlice(alloc, "<acceleratorManufacturerSet>");
        try serializeAcceleratorManufacturerSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</acceleratorManufacturerSet>");
    }
    if (value.accelerator_names) |v| {
        try buf.appendSlice(alloc, "<acceleratorNameSet>");
        try serializeAcceleratorNameSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</acceleratorNameSet>");
    }
    if (value.accelerator_total_memory_mi_b) |v| {
        try buf.appendSlice(alloc, "<acceleratorTotalMemoryMiB>");
        try serializeAcceleratorTotalMemoryMiB(alloc, buf, v);
        try buf.appendSlice(alloc, "</acceleratorTotalMemoryMiB>");
    }
    if (value.accelerator_types) |v| {
        try buf.appendSlice(alloc, "<acceleratorTypeSet>");
        try serializeAcceleratorTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</acceleratorTypeSet>");
    }
    if (value.allowed_instance_types) |v| {
        try buf.appendSlice(alloc, "<allowedInstanceTypeSet>");
        try serializeAllowedInstanceTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</allowedInstanceTypeSet>");
    }
    if (value.bare_metal) |v| {
        try buf.appendSlice(alloc, "<bareMetal>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</bareMetal>");
    }
    if (value.baseline_ebs_bandwidth_mbps) |v| {
        try buf.appendSlice(alloc, "<baselineEbsBandwidthMbps>");
        try serializeBaselineEbsBandwidthMbps(alloc, buf, v);
        try buf.appendSlice(alloc, "</baselineEbsBandwidthMbps>");
    }
    if (value.baseline_performance_factors) |v| {
        try buf.appendSlice(alloc, "<baselinePerformanceFactors>");
        try serializeBaselinePerformanceFactors(alloc, buf, v);
        try buf.appendSlice(alloc, "</baselinePerformanceFactors>");
    }
    if (value.burstable_performance) |v| {
        try buf.appendSlice(alloc, "<burstablePerformance>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</burstablePerformance>");
    }
    if (value.cpu_manufacturers) |v| {
        try buf.appendSlice(alloc, "<cpuManufacturerSet>");
        try serializeCpuManufacturerSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</cpuManufacturerSet>");
    }
    if (value.excluded_instance_types) |v| {
        try buf.appendSlice(alloc, "<excludedInstanceTypeSet>");
        try serializeExcludedInstanceTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</excludedInstanceTypeSet>");
    }
    if (value.instance_generations) |v| {
        try buf.appendSlice(alloc, "<instanceGenerationSet>");
        try serializeInstanceGenerationSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</instanceGenerationSet>");
    }
    if (value.local_storage) |v| {
        try buf.appendSlice(alloc, "<localStorage>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</localStorage>");
    }
    if (value.local_storage_types) |v| {
        try buf.appendSlice(alloc, "<localStorageTypeSet>");
        try serializeLocalStorageTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</localStorageTypeSet>");
    }
    if (value.max_spot_price_as_percentage_of_optimal_on_demand_price) |v| {
        try buf.appendSlice(alloc, "<maxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</maxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
    }
    if (value.memory_gi_b_per_v_cpu) |v| {
        try buf.appendSlice(alloc, "<memoryGiBPerVCpu>");
        try serializeMemoryGiBPerVCpu(alloc, buf, v);
        try buf.appendSlice(alloc, "</memoryGiBPerVCpu>");
    }
    if (value.memory_mi_b) |v| {
        try buf.appendSlice(alloc, "<memoryMiB>");
        try serializeMemoryMiB(alloc, buf, v);
        try buf.appendSlice(alloc, "</memoryMiB>");
    }
    if (value.network_bandwidth_gbps) |v| {
        try buf.appendSlice(alloc, "<networkBandwidthGbps>");
        try serializeNetworkBandwidthGbps(alloc, buf, v);
        try buf.appendSlice(alloc, "</networkBandwidthGbps>");
    }
    if (value.network_interface_count) |v| {
        try buf.appendSlice(alloc, "<networkInterfaceCount>");
        try serializeNetworkInterfaceCount(alloc, buf, v);
        try buf.appendSlice(alloc, "</networkInterfaceCount>");
    }
    if (value.on_demand_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(alloc, "<onDemandMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</onDemandMaxPricePercentageOverLowestPrice>");
    }
    if (value.require_encryption_in_transit) |v| {
        try buf.appendSlice(alloc, "<requireEncryptionInTransit>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</requireEncryptionInTransit>");
    }
    if (value.require_hibernate_support) |v| {
        try buf.appendSlice(alloc, "<requireHibernateSupport>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</requireHibernateSupport>");
    }
    if (value.spot_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(alloc, "<spotMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</spotMaxPricePercentageOverLowestPrice>");
    }
    if (value.total_local_storage_gb) |v| {
        try buf.appendSlice(alloc, "<totalLocalStorageGB>");
        try serializeTotalLocalStorageGB(alloc, buf, v);
        try buf.appendSlice(alloc, "</totalLocalStorageGB>");
    }
    if (value.v_cpu_count) |v| {
        try buf.appendSlice(alloc, "<vCpuCount>");
        try serializeVCpuCountRange(alloc, buf, v);
        try buf.appendSlice(alloc, "</vCpuCount>");
    }
}

pub fn serializeInstanceRequirementsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceRequirementsRequest) !void {
    if (value.accelerator_count) |v| {
        try buf.appendSlice(alloc, "<AcceleratorCount>");
        try serializeAcceleratorCountRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</AcceleratorCount>");
    }
    if (value.accelerator_manufacturers) |v| {
        try buf.appendSlice(alloc, "<AcceleratorManufacturer>");
        try serializeAcceleratorManufacturerSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</AcceleratorManufacturer>");
    }
    if (value.accelerator_names) |v| {
        try buf.appendSlice(alloc, "<AcceleratorName>");
        try serializeAcceleratorNameSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</AcceleratorName>");
    }
    if (value.accelerator_total_memory_mi_b) |v| {
        try buf.appendSlice(alloc, "<AcceleratorTotalMemoryMiB>");
        try serializeAcceleratorTotalMemoryMiBRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</AcceleratorTotalMemoryMiB>");
    }
    if (value.accelerator_types) |v| {
        try buf.appendSlice(alloc, "<AcceleratorType>");
        try serializeAcceleratorTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</AcceleratorType>");
    }
    if (value.allowed_instance_types) |v| {
        try buf.appendSlice(alloc, "<AllowedInstanceType>");
        try serializeAllowedInstanceTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</AllowedInstanceType>");
    }
    if (value.bare_metal) |v| {
        try buf.appendSlice(alloc, "<BareMetal>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</BareMetal>");
    }
    if (value.baseline_ebs_bandwidth_mbps) |v| {
        try buf.appendSlice(alloc, "<BaselineEbsBandwidthMbps>");
        try serializeBaselineEbsBandwidthMbpsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</BaselineEbsBandwidthMbps>");
    }
    if (value.baseline_performance_factors) |v| {
        try buf.appendSlice(alloc, "<BaselinePerformanceFactors>");
        try serializeBaselinePerformanceFactorsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</BaselinePerformanceFactors>");
    }
    if (value.burstable_performance) |v| {
        try buf.appendSlice(alloc, "<BurstablePerformance>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</BurstablePerformance>");
    }
    if (value.cpu_manufacturers) |v| {
        try buf.appendSlice(alloc, "<CpuManufacturer>");
        try serializeCpuManufacturerSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</CpuManufacturer>");
    }
    if (value.excluded_instance_types) |v| {
        try buf.appendSlice(alloc, "<ExcludedInstanceType>");
        try serializeExcludedInstanceTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ExcludedInstanceType>");
    }
    if (value.instance_generations) |v| {
        try buf.appendSlice(alloc, "<InstanceGeneration>");
        try serializeInstanceGenerationSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</InstanceGeneration>");
    }
    if (value.local_storage) |v| {
        try buf.appendSlice(alloc, "<LocalStorage>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</LocalStorage>");
    }
    if (value.local_storage_types) |v| {
        try buf.appendSlice(alloc, "<LocalStorageType>");
        try serializeLocalStorageTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</LocalStorageType>");
    }
    if (value.max_spot_price_as_percentage_of_optimal_on_demand_price) |v| {
        try buf.appendSlice(alloc, "<MaxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</MaxSpotPriceAsPercentageOfOptimalOnDemandPrice>");
    }
    if (value.memory_gi_b_per_v_cpu) |v| {
        try buf.appendSlice(alloc, "<MemoryGiBPerVCpu>");
        try serializeMemoryGiBPerVCpuRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</MemoryGiBPerVCpu>");
    }
    try buf.appendSlice(alloc, "<MemoryMiB>");
    try serializeMemoryMiBRequest(alloc, buf, value.memory_mi_b);
    try buf.appendSlice(alloc, "</MemoryMiB>");
    if (value.network_bandwidth_gbps) |v| {
        try buf.appendSlice(alloc, "<NetworkBandwidthGbps>");
        try serializeNetworkBandwidthGbpsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</NetworkBandwidthGbps>");
    }
    if (value.network_interface_count) |v| {
        try buf.appendSlice(alloc, "<NetworkInterfaceCount>");
        try serializeNetworkInterfaceCountRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</NetworkInterfaceCount>");
    }
    if (value.on_demand_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(alloc, "<OnDemandMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</OnDemandMaxPricePercentageOverLowestPrice>");
    }
    if (value.require_encryption_in_transit) |v| {
        try buf.appendSlice(alloc, "<RequireEncryptionInTransit>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</RequireEncryptionInTransit>");
    }
    if (value.require_hibernate_support) |v| {
        try buf.appendSlice(alloc, "<RequireHibernateSupport>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</RequireHibernateSupport>");
    }
    if (value.spot_max_price_percentage_over_lowest_price) |v| {
        try buf.appendSlice(alloc, "<SpotMaxPricePercentageOverLowestPrice>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</SpotMaxPricePercentageOverLowestPrice>");
    }
    if (value.total_local_storage_gb) |v| {
        try buf.appendSlice(alloc, "<TotalLocalStorageGB>");
        try serializeTotalLocalStorageGBRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</TotalLocalStorageGB>");
    }
    try buf.appendSlice(alloc, "<VCpuCount>");
    try serializeVCpuCountRangeRequest(alloc, buf, value.v_cpu_count);
    try buf.appendSlice(alloc, "</VCpuCount>");
}

pub fn serializeInstanceRequirementsWithMetadataRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceRequirementsWithMetadataRequest) !void {
    if (value.architecture_types) |v| {
        try buf.appendSlice(alloc, "<ArchitectureType>");
        try serializeArchitectureTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ArchitectureType>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(alloc, "<InstanceRequirements>");
        try serializeInstanceRequirementsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</InstanceRequirements>");
    }
    if (value.virtualization_types) |v| {
        try buf.appendSlice(alloc, "<VirtualizationType>");
        try serializeVirtualizationTypeSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</VirtualizationType>");
    }
}

pub fn serializeInstanceSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: InstanceSpecification) !void {
    if (value.exclude_boot_volume) |v| {
        try buf.appendSlice(alloc, "<ExcludeBootVolume>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</ExcludeBootVolume>");
    }
    if (value.exclude_data_volume_ids) |v| {
        try buf.appendSlice(alloc, "<ExcludeDataVolumeId>");
        try serializeVolumeIdStringList(alloc, buf, v, "VolumeId");
        try buf.appendSlice(alloc, "</ExcludeDataVolumeId>");
    }
    try buf.appendSlice(alloc, "<InstanceId>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.instance_id);
    try buf.appendSlice(alloc, "</InstanceId>");
}

pub fn serializeIntegrateServices(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IntegrateServices) !void {
    if (value.athena_integrations) |v| {
        try buf.appendSlice(alloc, "<AthenaIntegration>");
        try serializeAthenaIntegrationsSet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</AthenaIntegration>");
    }
}

pub fn serializeIpPermission(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpPermission) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(alloc, "<fromPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</fromPort>");
    }
    if (value.ip_protocol) |v| {
        try buf.appendSlice(alloc, "<ipProtocol>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ipProtocol>");
    }
    if (value.ip_ranges) |v| {
        try buf.appendSlice(alloc, "<ipRanges>");
        try serializeIpRangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ipRanges>");
    }
    if (value.ipv_6_ranges) |v| {
        try buf.appendSlice(alloc, "<ipv6Ranges>");
        try serializeIpv6RangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ipv6Ranges>");
    }
    if (value.prefix_list_ids) |v| {
        try buf.appendSlice(alloc, "<prefixListIds>");
        try serializePrefixListIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</prefixListIds>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(alloc, "<toPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</toPort>");
    }
    if (value.user_id_group_pairs) |v| {
        try buf.appendSlice(alloc, "<groups>");
        try serializeUserIdGroupPairList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</groups>");
    }
}

pub fn serializeIpRange(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpRange) !void {
    if (value.cidr_ip) |v| {
        try buf.appendSlice(alloc, "<cidrIp>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</cidrIp>");
    }
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</description>");
    }
}

pub fn serializeIpamCidrAuthorizationContext(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamCidrAuthorizationContext) !void {
    if (value.message) |v| {
        try buf.appendSlice(alloc, "<Message>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Message>");
    }
    if (value.signature) |v| {
        try buf.appendSlice(alloc, "<Signature>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Signature>");
    }
}

pub fn serializeIpamPolicyAllocationRuleRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPolicyAllocationRuleRequest) !void {
    if (value.source_ipam_pool_id) |v| {
        try buf.appendSlice(alloc, "<SourceIpamPoolId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SourceIpamPoolId>");
    }
}

pub fn serializeIpamPoolSourceResourceRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPoolSourceResourceRequest) !void {
    if (value.resource_id) |v| {
        try buf.appendSlice(alloc, "<ResourceId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceId>");
    }
    if (value.resource_owner) |v| {
        try buf.appendSlice(alloc, "<ResourceOwner>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceOwner>");
    }
    if (value.resource_region) |v| {
        try buf.appendSlice(alloc, "<ResourceRegion>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceRegion>");
    }
    if (value.resource_type) |v| {
        try buf.appendSlice(alloc, "<ResourceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ResourceType>");
    }
}

pub fn serializeIpamPrefixListResolverRuleConditionRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPrefixListResolverRuleConditionRequest) !void {
    if (value.cidr) |v| {
        try buf.appendSlice(alloc, "<Cidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Cidr>");
    }
    if (value.ipam_pool_id) |v| {
        try buf.appendSlice(alloc, "<IpamPoolId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</IpamPoolId>");
    }
    try buf.appendSlice(alloc, "<Operation>");
    try buf.appendSlice(alloc, @tagName(value.operation));
    try buf.appendSlice(alloc, "</Operation>");
    if (value.resource_id) |v| {
        try buf.appendSlice(alloc, "<ResourceId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceId>");
    }
    if (value.resource_owner) |v| {
        try buf.appendSlice(alloc, "<ResourceOwner>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceOwner>");
    }
    if (value.resource_region) |v| {
        try buf.appendSlice(alloc, "<ResourceRegion>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceRegion>");
    }
    if (value.resource_tag) |v| {
        try buf.appendSlice(alloc, "<ResourceTag>");
        try serializeRequestIpamResourceTag(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceTag>");
    }
}

pub fn serializeIpamPrefixListResolverRuleRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: IpamPrefixListResolverRuleRequest) !void {
    if (value.conditions) |v| {
        try buf.appendSlice(alloc, "<Condition>");
        try serializeIpamPrefixListResolverRuleConditionRequestSet(alloc, buf, v, "Condition");
        try buf.appendSlice(alloc, "</Condition>");
    }
    if (value.ipam_scope_id) |v| {
        try buf.appendSlice(alloc, "<IpamScopeId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</IpamScopeId>");
    }
    if (value.resource_type) |v| {
        try buf.appendSlice(alloc, "<ResourceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ResourceType>");
    }
    try buf.appendSlice(alloc, "<RuleType>");
    try buf.appendSlice(alloc, @tagName(value.rule_type));
    try buf.appendSlice(alloc, "</RuleType>");
    if (value.static_cidr) |v| {
        try buf.appendSlice(alloc, "<StaticCidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</StaticCidr>");
    }
}

pub fn serializeIpv4PrefixSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Ipv4PrefixSpecificationRequest) !void {
    if (value.ipv_4_prefix) |v| {
        try buf.appendSlice(alloc, "<Ipv4Prefix>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ipv4Prefix>");
    }
}

pub fn serializeIpv6PrefixSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Ipv6PrefixSpecificationRequest) !void {
    if (value.ipv_6_prefix) |v| {
        try buf.appendSlice(alloc, "<Ipv6Prefix>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ipv6Prefix>");
    }
}

pub fn serializeIpv6Range(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Ipv6Range) !void {
    if (value.cidr_ipv_6) |v| {
        try buf.appendSlice(alloc, "<cidrIpv6>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</cidrIpv6>");
    }
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</description>");
    }
}

pub fn serializeLaunchPermission(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchPermission) !void {
    if (value.group) |v| {
        try buf.appendSlice(alloc, "<group>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</group>");
    }
    if (value.organizational_unit_arn) |v| {
        try buf.appendSlice(alloc, "<organizationalUnitArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</organizationalUnitArn>");
    }
    if (value.organization_arn) |v| {
        try buf.appendSlice(alloc, "<organizationArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</organizationArn>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(alloc, "<userId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</userId>");
    }
}

pub fn serializeLaunchPermissionModifications(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchPermissionModifications) !void {
    if (value.add) |v| {
        try buf.appendSlice(alloc, "<Add>");
        try serializeLaunchPermissionList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Add>");
    }
    if (value.remove) |v| {
        try buf.appendSlice(alloc, "<Remove>");
        try serializeLaunchPermissionList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Remove>");
    }
}

pub fn serializeLaunchTemplateBlockDeviceMappingRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateBlockDeviceMappingRequest) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(alloc, "<DeviceName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DeviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(alloc, "<Ebs>");
        try serializeLaunchTemplateEbsBlockDeviceRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(alloc, "<NoDevice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</NoDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(alloc, "<VirtualName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</VirtualName>");
    }
}

pub fn serializeLaunchTemplateCapacityReservationSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateCapacityReservationSpecificationRequest) !void {
    if (value.capacity_reservation_preference) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationPreference>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</CapacityReservationPreference>");
    }
    if (value.capacity_reservation_target) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationTarget>");
        try serializeCapacityReservationTarget(alloc, buf, v);
        try buf.appendSlice(alloc, "</CapacityReservationTarget>");
    }
}

pub fn serializeLaunchTemplateConfig(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateConfig) !void {
    if (value.launch_template_specification) |v| {
        try buf.appendSlice(alloc, "<launchTemplateSpecification>");
        try serializeFleetLaunchTemplateSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</launchTemplateSpecification>");
    }
    if (value.overrides) |v| {
        try buf.appendSlice(alloc, "<overrides>");
        try serializeLaunchTemplateOverridesList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</overrides>");
    }
}

pub fn serializeLaunchTemplateCpuOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateCpuOptionsRequest) !void {
    if (value.amd_sev_snp) |v| {
        try buf.appendSlice(alloc, "<AmdSevSnp>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AmdSevSnp>");
    }
    if (value.core_count) |v| {
        try buf.appendSlice(alloc, "<CoreCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</CoreCount>");
    }
    if (value.threads_per_core) |v| {
        try buf.appendSlice(alloc, "<ThreadsPerCore>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ThreadsPerCore>");
    }
}

pub fn serializeLaunchTemplateEbsBlockDeviceRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateEbsBlockDeviceRequest) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<DeleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DeleteOnTermination>");
    }
    if (value.ebs_card_index) |v| {
        try buf.appendSlice(alloc, "<EbsCardIndex>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</EbsCardIndex>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(alloc, "<Encrypted>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(alloc, "<Iops>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Iops>");
    }
    if (value.kms_key_id) |v| {
        try buf.appendSlice(alloc, "<KmsKeyId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</KmsKeyId>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(alloc, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SnapshotId>");
    }
    if (value.throughput) |v| {
        try buf.appendSlice(alloc, "<Throughput>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Throughput>");
    }
    if (value.volume_initialization_rate) |v| {
        try buf.appendSlice(alloc, "<VolumeInitializationRate>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</VolumeInitializationRate>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(alloc, "<VolumeSize>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</VolumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(alloc, "<VolumeType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</VolumeType>");
    }
}

pub fn serializeLaunchTemplateElasticInferenceAccelerator(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateElasticInferenceAccelerator) !void {
    if (value.count) |v| {
        try buf.appendSlice(alloc, "<Count>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Count>");
    }
    try buf.appendSlice(alloc, "<Type>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.@"type");
    try buf.appendSlice(alloc, "</Type>");
}

pub fn serializeLaunchTemplateEnclaveOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateEnclaveOptionsRequest) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
}

pub fn serializeLaunchTemplateHibernationOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateHibernationOptionsRequest) !void {
    if (value.configured) |v| {
        try buf.appendSlice(alloc, "<Configured>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Configured>");
    }
}

pub fn serializeLaunchTemplateIamInstanceProfileSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateIamInstanceProfileSpecificationRequest) !void {
    if (value.arn) |v| {
        try buf.appendSlice(alloc, "<Arn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Arn>");
    }
    if (value.name) |v| {
        try buf.appendSlice(alloc, "<Name>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Name>");
    }
}

pub fn serializeLaunchTemplateInstanceMaintenanceOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceMaintenanceOptionsRequest) !void {
    if (value.auto_recovery) |v| {
        try buf.appendSlice(alloc, "<AutoRecovery>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AutoRecovery>");
    }
}

pub fn serializeLaunchTemplateInstanceMarketOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceMarketOptionsRequest) !void {
    if (value.market_type) |v| {
        try buf.appendSlice(alloc, "<MarketType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</MarketType>");
    }
    if (value.spot_options) |v| {
        try buf.appendSlice(alloc, "<SpotOptions>");
        try serializeLaunchTemplateSpotMarketOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</SpotOptions>");
    }
}

pub fn serializeLaunchTemplateInstanceMetadataOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceMetadataOptionsRequest) !void {
    if (value.http_endpoint) |v| {
        try buf.appendSlice(alloc, "<HttpEndpoint>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HttpEndpoint>");
    }
    if (value.http_protocol_ipv_6) |v| {
        try buf.appendSlice(alloc, "<HttpProtocolIpv6>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HttpProtocolIpv6>");
    }
    if (value.http_put_response_hop_limit) |v| {
        try buf.appendSlice(alloc, "<HttpPutResponseHopLimit>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</HttpPutResponseHopLimit>");
    }
    if (value.http_tokens) |v| {
        try buf.appendSlice(alloc, "<HttpTokens>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HttpTokens>");
    }
    if (value.instance_metadata_tags) |v| {
        try buf.appendSlice(alloc, "<InstanceMetadataTags>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceMetadataTags>");
    }
}

pub fn serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateInstanceNetworkInterfaceSpecificationRequest) !void {
    if (value.associate_carrier_ip_address) |v| {
        try buf.appendSlice(alloc, "<AssociateCarrierIpAddress>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</AssociateCarrierIpAddress>");
    }
    if (value.associate_public_ip_address) |v| {
        try buf.appendSlice(alloc, "<AssociatePublicIpAddress>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</AssociatePublicIpAddress>");
    }
    if (value.connection_tracking_specification) |v| {
        try buf.appendSlice(alloc, "<ConnectionTrackingSpecification>");
        try serializeConnectionTrackingSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</ConnectionTrackingSpecification>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<DeleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DeleteOnTermination>");
    }
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
    if (value.device_index) |v| {
        try buf.appendSlice(alloc, "<DeviceIndex>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</DeviceIndex>");
    }
    if (value.ena_queue_count) |v| {
        try buf.appendSlice(alloc, "<EnaQueueCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</EnaQueueCount>");
    }
    if (value.ena_srd_specification) |v| {
        try buf.appendSlice(alloc, "<EnaSrdSpecification>");
        try serializeEnaSrdSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</EnaSrdSpecification>");
    }
    if (value.groups) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupId>");
        try serializeSecurityGroupIdStringList(alloc, buf, v, "SecurityGroupId");
        try buf.appendSlice(alloc, "</SecurityGroupId>");
    }
    if (value.interface_type) |v| {
        try buf.appendSlice(alloc, "<InterfaceType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</InterfaceType>");
    }
    if (value.ipv_4_prefix_count) |v| {
        try buf.appendSlice(alloc, "<Ipv4PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Ipv4PrefixCount>");
    }
    if (value.ipv_4_prefixes) |v| {
        try buf.appendSlice(alloc, "<Ipv4Prefix>");
        try serializeIpv4PrefixList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Ipv4Prefix>");
    }
    if (value.ipv_6_address_count) |v| {
        try buf.appendSlice(alloc, "<Ipv6AddressCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Ipv6AddressCount>");
    }
    if (value.ipv_6_addresses) |v| {
        try buf.appendSlice(alloc, "<Ipv6Addresses>");
        try serializeInstanceIpv6AddressListRequest(alloc, buf, v, "InstanceIpv6Address");
        try buf.appendSlice(alloc, "</Ipv6Addresses>");
    }
    if (value.ipv_6_prefix_count) |v| {
        try buf.appendSlice(alloc, "<Ipv6PrefixCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Ipv6PrefixCount>");
    }
    if (value.ipv_6_prefixes) |v| {
        try buf.appendSlice(alloc, "<Ipv6Prefix>");
        try serializeIpv6PrefixList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Ipv6Prefix>");
    }
    if (value.network_card_index) |v| {
        try buf.appendSlice(alloc, "<NetworkCardIndex>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</NetworkCardIndex>");
    }
    if (value.network_interface_id) |v| {
        try buf.appendSlice(alloc, "<NetworkInterfaceId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</NetworkInterfaceId>");
    }
    if (value.primary_ipv_6) |v| {
        try buf.appendSlice(alloc, "<PrimaryIpv6>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</PrimaryIpv6>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(alloc, "<PrivateIpAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PrivateIpAddress>");
    }
    if (value.private_ip_addresses) |v| {
        try buf.appendSlice(alloc, "<PrivateIpAddresses>");
        try serializePrivateIpAddressSpecificationList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PrivateIpAddresses>");
    }
    if (value.secondary_private_ip_address_count) |v| {
        try buf.appendSlice(alloc, "<SecondaryPrivateIpAddressCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</SecondaryPrivateIpAddressCount>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeLaunchTemplateLicenseConfigurationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateLicenseConfigurationRequest) !void {
    if (value.license_configuration_arn) |v| {
        try buf.appendSlice(alloc, "<LicenseConfigurationArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LicenseConfigurationArn>");
    }
}

pub fn serializeLaunchTemplateNetworkPerformanceOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateNetworkPerformanceOptionsRequest) !void {
    if (value.bandwidth_weighting) |v| {
        try buf.appendSlice(alloc, "<BandwidthWeighting>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</BandwidthWeighting>");
    }
}

pub fn serializeLaunchTemplateOverrides(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateOverrides) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZoneId>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(alloc, "<instanceRequirements>");
        try serializeInstanceRequirements(alloc, buf, v);
        try buf.appendSlice(alloc, "</instanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<instanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</instanceType>");
    }
    if (value.priority) |v| {
        try buf.appendSlice(alloc, "<priority>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</priority>");
    }
    if (value.spot_price) |v| {
        try buf.appendSlice(alloc, "<spotPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</spotPrice>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<subnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</subnetId>");
    }
    if (value.weighted_capacity) |v| {
        try buf.appendSlice(alloc, "<weightedCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</weightedCapacity>");
    }
}

pub fn serializeLaunchTemplatePlacementRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplatePlacementRequest) !void {
    if (value.affinity) |v| {
        try buf.appendSlice(alloc, "<Affinity>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Affinity>");
    }
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZoneId>");
    }
    if (value.group_id) |v| {
        try buf.appendSlice(alloc, "<GroupId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</GroupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(alloc, "<GroupName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</GroupName>");
    }
    if (value.host_id) |v| {
        try buf.appendSlice(alloc, "<HostId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</HostId>");
    }
    if (value.host_resource_group_arn) |v| {
        try buf.appendSlice(alloc, "<HostResourceGroupArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</HostResourceGroupArn>");
    }
    if (value.partition_number) |v| {
        try buf.appendSlice(alloc, "<PartitionNumber>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</PartitionNumber>");
    }
    if (value.spread_domain) |v| {
        try buf.appendSlice(alloc, "<SpreadDomain>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SpreadDomain>");
    }
    if (value.tenancy) |v| {
        try buf.appendSlice(alloc, "<Tenancy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Tenancy>");
    }
}

pub fn serializeLaunchTemplatePrivateDnsNameOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplatePrivateDnsNameOptionsRequest) !void {
    if (value.enable_resource_name_dns_aaaa_record) |v| {
        try buf.appendSlice(alloc, "<EnableResourceNameDnsAAAARecord>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnableResourceNameDnsAAAARecord>");
    }
    if (value.enable_resource_name_dns_a_record) |v| {
        try buf.appendSlice(alloc, "<EnableResourceNameDnsARecord>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnableResourceNameDnsARecord>");
    }
    if (value.hostname_type) |v| {
        try buf.appendSlice(alloc, "<HostnameType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HostnameType>");
    }
}

pub fn serializeLaunchTemplateSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateSpecification) !void {
    if (value.launch_template_id) |v| {
        try buf.appendSlice(alloc, "<LaunchTemplateId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LaunchTemplateId>");
    }
    if (value.launch_template_name) |v| {
        try buf.appendSlice(alloc, "<LaunchTemplateName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LaunchTemplateName>");
    }
    if (value.version) |v| {
        try buf.appendSlice(alloc, "<Version>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Version>");
    }
}

pub fn serializeLaunchTemplateSpotMarketOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateSpotMarketOptionsRequest) !void {
    if (value.block_duration_minutes) |v| {
        try buf.appendSlice(alloc, "<BlockDurationMinutes>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</BlockDurationMinutes>");
    }
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(alloc, "<InstanceInterruptionBehavior>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceInterruptionBehavior>");
    }
    if (value.max_price) |v| {
        try buf.appendSlice(alloc, "<MaxPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</MaxPrice>");
    }
    if (value.spot_instance_type) |v| {
        try buf.appendSlice(alloc, "<SpotInstanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</SpotInstanceType>");
    }
    if (value.valid_until) |v| {
        try buf.appendSlice(alloc, "<ValidUntil>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</ValidUntil>");
    }
}

pub fn serializeLaunchTemplateTagSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplateTagSpecificationRequest) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(alloc, "<ResourceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ResourceType>");
    }
    if (value.tags) |v| {
        try buf.appendSlice(alloc, "<Tag>");
        try serializeTagList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Tag>");
    }
}

pub fn serializeLaunchTemplatesMonitoringRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LaunchTemplatesMonitoringRequest) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
}

pub fn serializeLicenseConfigurationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LicenseConfigurationRequest) !void {
    if (value.license_configuration_arn) |v| {
        try buf.appendSlice(alloc, "<LicenseConfigurationArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LicenseConfigurationArn>");
    }
}

pub fn serializeLoadBalancersConfig(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LoadBalancersConfig) !void {
    if (value.classic_load_balancers_config) |v| {
        try buf.appendSlice(alloc, "<classicLoadBalancersConfig>");
        try serializeClassicLoadBalancersConfig(alloc, buf, v);
        try buf.appendSlice(alloc, "</classicLoadBalancersConfig>");
    }
    if (value.target_groups_config) |v| {
        try buf.appendSlice(alloc, "<targetGroupsConfig>");
        try serializeTargetGroupsConfig(alloc, buf, v);
        try buf.appendSlice(alloc, "</targetGroupsConfig>");
    }
}

pub fn serializeLoadPermissionModifications(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LoadPermissionModifications) !void {
    if (value.add) |v| {
        try buf.appendSlice(alloc, "<Add>");
        try serializeLoadPermissionListRequest(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Add>");
    }
    if (value.remove) |v| {
        try buf.appendSlice(alloc, "<Remove>");
        try serializeLoadPermissionListRequest(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Remove>");
    }
}

pub fn serializeLoadPermissionRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: LoadPermissionRequest) !void {
    if (value.group) |v| {
        try buf.appendSlice(alloc, "<Group>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Group>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(alloc, "<UserId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserId>");
    }
}

pub fn serializeMacSystemIntegrityProtectionConfigurationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: MacSystemIntegrityProtectionConfigurationRequest) !void {
    if (value.apple_internal) |v| {
        try buf.appendSlice(alloc, "<AppleInternal>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AppleInternal>");
    }
    if (value.base_system) |v| {
        try buf.appendSlice(alloc, "<BaseSystem>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</BaseSystem>");
    }
    if (value.debugging_restrictions) |v| {
        try buf.appendSlice(alloc, "<DebuggingRestrictions>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DebuggingRestrictions>");
    }
    if (value.d_trace_restrictions) |v| {
        try buf.appendSlice(alloc, "<DTraceRestrictions>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DTraceRestrictions>");
    }
    if (value.filesystem_protections) |v| {
        try buf.appendSlice(alloc, "<FilesystemProtections>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</FilesystemProtections>");
    }
    if (value.kext_signing) |v| {
        try buf.appendSlice(alloc, "<KextSigning>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</KextSigning>");
    }
    if (value.nvram_protections) |v| {
        try buf.appendSlice(alloc, "<NvramProtections>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</NvramProtections>");
    }
}

pub fn serializeMemoryGiBPerVCpu(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryGiBPerVCpu) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeMemoryGiBPerVCpuRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryGiBPerVCpuRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<Min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Min>");
    }
}

pub fn serializeMemoryMiB(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryMiB) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeMemoryMiBRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: MemoryMiBRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    try buf.appendSlice(alloc, "<Min>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.min}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</Min>");
}

pub fn serializeModifyTransitGatewayOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyTransitGatewayOptions) !void {
    if (value.add_transit_gateway_cidr_blocks) |v| {
        try buf.appendSlice(alloc, "<AddTransitGatewayCidrBlocks>");
        try serializeTransitGatewayCidrBlockStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</AddTransitGatewayCidrBlocks>");
    }
    if (value.amazon_side_asn) |v| {
        try buf.appendSlice(alloc, "<AmazonSideAsn>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</AmazonSideAsn>");
    }
    if (value.association_default_route_table_id) |v| {
        try buf.appendSlice(alloc, "<AssociationDefaultRouteTableId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AssociationDefaultRouteTableId>");
    }
    if (value.auto_accept_shared_attachments) |v| {
        try buf.appendSlice(alloc, "<AutoAcceptSharedAttachments>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AutoAcceptSharedAttachments>");
    }
    if (value.default_route_table_association) |v| {
        try buf.appendSlice(alloc, "<DefaultRouteTableAssociation>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DefaultRouteTableAssociation>");
    }
    if (value.default_route_table_propagation) |v| {
        try buf.appendSlice(alloc, "<DefaultRouteTablePropagation>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DefaultRouteTablePropagation>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(alloc, "<DnsSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DnsSupport>");
    }
    if (value.encryption_support) |v| {
        try buf.appendSlice(alloc, "<EncryptionSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</EncryptionSupport>");
    }
    if (value.propagation_default_route_table_id) |v| {
        try buf.appendSlice(alloc, "<PropagationDefaultRouteTableId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PropagationDefaultRouteTableId>");
    }
    if (value.remove_transit_gateway_cidr_blocks) |v| {
        try buf.appendSlice(alloc, "<RemoveTransitGatewayCidrBlocks>");
        try serializeTransitGatewayCidrBlockStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</RemoveTransitGatewayCidrBlocks>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</SecurityGroupReferencingSupport>");
    }
    if (value.vpn_ecmp_support) |v| {
        try buf.appendSlice(alloc, "<VpnEcmpSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</VpnEcmpSupport>");
    }
}

pub fn serializeModifyTransitGatewayVpcAttachmentRequestOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyTransitGatewayVpcAttachmentRequestOptions) !void {
    if (value.appliance_mode_support) |v| {
        try buf.appendSlice(alloc, "<ApplianceModeSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ApplianceModeSupport>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(alloc, "<DnsSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DnsSupport>");
    }
    if (value.ipv_6_support) |v| {
        try buf.appendSlice(alloc, "<Ipv6Support>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Ipv6Support>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</SecurityGroupReferencingSupport>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointCidrOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointCidrOptions) !void {
    if (value.port_ranges) |v| {
        try buf.appendSlice(alloc, "<PortRange>");
        try serializeModifyVerifiedAccessEndpointPortRangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PortRange>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointEniOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointEniOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(alloc, "<Port>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(alloc, "<PortRange>");
        try serializeModifyVerifiedAccessEndpointPortRangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(alloc, "<Protocol>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Protocol>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointLoadBalancerOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointLoadBalancerOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(alloc, "<Port>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Port>");
    }
    if (value.port_ranges) |v| {
        try buf.appendSlice(alloc, "<PortRange>");
        try serializeModifyVerifiedAccessEndpointPortRangeList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</PortRange>");
    }
    if (value.protocol) |v| {
        try buf.appendSlice(alloc, "<Protocol>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</Protocol>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try serializeModifyVerifiedAccessEndpointSubnetIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointPortRange(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointPortRange) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(alloc, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(alloc, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ToPort>");
    }
}

pub fn serializeModifyVerifiedAccessEndpointRdsOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessEndpointRdsOptions) !void {
    if (value.port) |v| {
        try buf.appendSlice(alloc, "<Port>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Port>");
    }
    if (value.rds_endpoint) |v| {
        try buf.appendSlice(alloc, "<RdsEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RdsEndpoint>");
    }
    if (value.subnet_ids) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try serializeModifyVerifiedAccessEndpointSubnetIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeModifyVerifiedAccessNativeApplicationOidcOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessNativeApplicationOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(alloc, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(alloc, "<ClientId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(alloc, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(alloc, "<Issuer>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Issuer>");
    }
    if (value.public_signing_key_endpoint) |v| {
        try buf.appendSlice(alloc, "<PublicSigningKeyEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PublicSigningKeyEndpoint>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(alloc, "<Scope>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(alloc, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(alloc, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserInfoEndpoint>");
    }
}

pub fn serializeModifyVerifiedAccessTrustProviderDeviceOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessTrustProviderDeviceOptions) !void {
    if (value.public_signing_key_url) |v| {
        try buf.appendSlice(alloc, "<PublicSigningKeyUrl>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PublicSigningKeyUrl>");
    }
}

pub fn serializeModifyVerifiedAccessTrustProviderOidcOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVerifiedAccessTrustProviderOidcOptions) !void {
    if (value.authorization_endpoint) |v| {
        try buf.appendSlice(alloc, "<AuthorizationEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AuthorizationEndpoint>");
    }
    if (value.client_id) |v| {
        try buf.appendSlice(alloc, "<ClientId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientId>");
    }
    if (value.client_secret) |v| {
        try buf.appendSlice(alloc, "<ClientSecret>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ClientSecret>");
    }
    if (value.issuer) |v| {
        try buf.appendSlice(alloc, "<Issuer>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Issuer>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(alloc, "<Scope>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Scope>");
    }
    if (value.token_endpoint) |v| {
        try buf.appendSlice(alloc, "<TokenEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TokenEndpoint>");
    }
    if (value.user_info_endpoint) |v| {
        try buf.appendSlice(alloc, "<UserInfoEndpoint>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserInfoEndpoint>");
    }
}

pub fn serializeModifyVpnTunnelOptionsSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ModifyVpnTunnelOptionsSpecification) !void {
    if (value.dpd_timeout_action) |v| {
        try buf.appendSlice(alloc, "<DPDTimeoutAction>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DPDTimeoutAction>");
    }
    if (value.dpd_timeout_seconds) |v| {
        try buf.appendSlice(alloc, "<DPDTimeoutSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</DPDTimeoutSeconds>");
    }
    if (value.enable_tunnel_lifecycle_control) |v| {
        try buf.appendSlice(alloc, "<EnableTunnelLifecycleControl>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnableTunnelLifecycleControl>");
    }
    if (value.ike_versions) |v| {
        try buf.appendSlice(alloc, "<IKEVersion>");
        try serializeIKEVersionsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</IKEVersion>");
    }
    if (value.log_options) |v| {
        try buf.appendSlice(alloc, "<LogOptions>");
        try serializeVpnTunnelLogOptionsSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</LogOptions>");
    }
    if (value.phase_1_dh_group_numbers) |v| {
        try buf.appendSlice(alloc, "<Phase1DHGroupNumber>");
        try serializePhase1DHGroupNumbersRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase1DHGroupNumber>");
    }
    if (value.phase_1_encryption_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase1EncryptionAlgorithm>");
        try serializePhase1EncryptionAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase1EncryptionAlgorithm>");
    }
    if (value.phase_1_integrity_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase1IntegrityAlgorithm>");
        try serializePhase1IntegrityAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase1IntegrityAlgorithm>");
    }
    if (value.phase_1_lifetime_seconds) |v| {
        try buf.appendSlice(alloc, "<Phase1LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Phase1LifetimeSeconds>");
    }
    if (value.phase_2_dh_group_numbers) |v| {
        try buf.appendSlice(alloc, "<Phase2DHGroupNumber>");
        try serializePhase2DHGroupNumbersRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase2DHGroupNumber>");
    }
    if (value.phase_2_encryption_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase2EncryptionAlgorithm>");
        try serializePhase2EncryptionAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase2EncryptionAlgorithm>");
    }
    if (value.phase_2_integrity_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase2IntegrityAlgorithm>");
        try serializePhase2IntegrityAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase2IntegrityAlgorithm>");
    }
    if (value.phase_2_lifetime_seconds) |v| {
        try buf.appendSlice(alloc, "<Phase2LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Phase2LifetimeSeconds>");
    }
    if (value.pre_shared_key) |v| {
        try buf.appendSlice(alloc, "<PreSharedKey>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PreSharedKey>");
    }
    if (value.rekey_fuzz_percentage) |v| {
        try buf.appendSlice(alloc, "<RekeyFuzzPercentage>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</RekeyFuzzPercentage>");
    }
    if (value.rekey_margin_time_seconds) |v| {
        try buf.appendSlice(alloc, "<RekeyMarginTimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</RekeyMarginTimeSeconds>");
    }
    if (value.replay_window_size) |v| {
        try buf.appendSlice(alloc, "<ReplayWindowSize>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ReplayWindowSize>");
    }
    if (value.startup_action) |v| {
        try buf.appendSlice(alloc, "<StartupAction>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</StartupAction>");
    }
    if (value.tunnel_inside_cidr) |v| {
        try buf.appendSlice(alloc, "<TunnelInsideCidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TunnelInsideCidr>");
    }
    if (value.tunnel_inside_ipv_6_cidr) |v| {
        try buf.appendSlice(alloc, "<TunnelInsideIpv6Cidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TunnelInsideIpv6Cidr>");
    }
}

pub fn serializeNetworkBandwidthGbps(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkBandwidthGbps) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeNetworkBandwidthGbpsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkBandwidthGbpsRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<Min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Min>");
    }
}

pub fn serializeNetworkInterfaceAttachmentChanges(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkInterfaceAttachmentChanges) !void {
    if (value.attachment_id) |v| {
        try buf.appendSlice(alloc, "<attachmentId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</attachmentId>");
    }
    if (value.default_ena_queue_count) |v| {
        try buf.appendSlice(alloc, "<DefaultEnaQueueCount>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DefaultEnaQueueCount>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<deleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</deleteOnTermination>");
    }
    if (value.ena_queue_count) |v| {
        try buf.appendSlice(alloc, "<EnaQueueCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</EnaQueueCount>");
    }
}

pub fn serializeNetworkInterfaceCount(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkInterfaceCount) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeNetworkInterfaceCountRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: NetworkInterfaceCountRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<Min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Min>");
    }
}

pub fn serializeNewDhcpConfiguration(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: NewDhcpConfiguration) !void {
    if (value.key) |v| {
        try buf.appendSlice(alloc, "<key>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</key>");
    }
    if (value.values) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializeOnDemandOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: OnDemandOptionsRequest) !void {
    if (value.allocation_strategy) |v| {
        try buf.appendSlice(alloc, "<AllocationStrategy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AllocationStrategy>");
    }
    if (value.capacity_reservation_options) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationOptions>");
        try serializeCapacityReservationOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</CapacityReservationOptions>");
    }
    if (value.max_total_price) |v| {
        try buf.appendSlice(alloc, "<MaxTotalPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</MaxTotalPrice>");
    }
    if (value.min_target_capacity) |v| {
        try buf.appendSlice(alloc, "<MinTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</MinTargetCapacity>");
    }
    if (value.single_availability_zone) |v| {
        try buf.appendSlice(alloc, "<SingleAvailabilityZone>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</SingleAvailabilityZone>");
    }
    if (value.single_instance_type) |v| {
        try buf.appendSlice(alloc, "<SingleInstanceType>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</SingleInstanceType>");
    }
}

pub fn serializeOperatorRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: OperatorRequest) !void {
    if (value.principal) |v| {
        try buf.appendSlice(alloc, "<Principal>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Principal>");
    }
}

pub fn serializePacketHeaderStatementRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PacketHeaderStatementRequest) !void {
    if (value.destination_addresses) |v| {
        try buf.appendSlice(alloc, "<DestinationAddress>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</DestinationAddress>");
    }
    if (value.destination_ports) |v| {
        try buf.appendSlice(alloc, "<DestinationPort>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</DestinationPort>");
    }
    if (value.destination_prefix_lists) |v| {
        try buf.appendSlice(alloc, "<DestinationPrefixList>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</DestinationPrefixList>");
    }
    if (value.protocols) |v| {
        try buf.appendSlice(alloc, "<Protocol>");
        try serializeProtocolList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Protocol>");
    }
    if (value.source_addresses) |v| {
        try buf.appendSlice(alloc, "<SourceAddress>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SourceAddress>");
    }
    if (value.source_ports) |v| {
        try buf.appendSlice(alloc, "<SourcePort>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SourcePort>");
    }
    if (value.source_prefix_lists) |v| {
        try buf.appendSlice(alloc, "<SourcePrefixList>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SourcePrefixList>");
    }
}

pub fn serializePathRequestFilter(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PathRequestFilter) !void {
    if (value.destination_address) |v| {
        try buf.appendSlice(alloc, "<DestinationAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DestinationAddress>");
    }
    if (value.destination_port_range) |v| {
        try buf.appendSlice(alloc, "<DestinationPortRange>");
        try serializeRequestFilterPortRange(alloc, buf, v);
        try buf.appendSlice(alloc, "</DestinationPortRange>");
    }
    if (value.source_address) |v| {
        try buf.appendSlice(alloc, "<SourceAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SourceAddress>");
    }
    if (value.source_port_range) |v| {
        try buf.appendSlice(alloc, "<SourcePortRange>");
        try serializeRequestFilterPortRange(alloc, buf, v);
        try buf.appendSlice(alloc, "</SourcePortRange>");
    }
}

pub fn serializePathStatementRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PathStatementRequest) !void {
    if (value.packet_header_statement) |v| {
        try buf.appendSlice(alloc, "<PacketHeaderStatement>");
        try serializePacketHeaderStatementRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</PacketHeaderStatement>");
    }
    if (value.resource_statement) |v| {
        try buf.appendSlice(alloc, "<ResourceStatement>");
        try serializeResourceStatementRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceStatement>");
    }
}

pub fn serializePeeringConnectionOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PeeringConnectionOptionsRequest) !void {
    if (value.allow_dns_resolution_from_remote_vpc) |v| {
        try buf.appendSlice(alloc, "<AllowDnsResolutionFromRemoteVpc>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</AllowDnsResolutionFromRemoteVpc>");
    }
    if (value.allow_egress_from_local_classic_link_to_remote_vpc) |v| {
        try buf.appendSlice(alloc, "<AllowEgressFromLocalClassicLinkToRemoteVpc>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</AllowEgressFromLocalClassicLinkToRemoteVpc>");
    }
    if (value.allow_egress_from_local_vpc_to_remote_classic_link) |v| {
        try buf.appendSlice(alloc, "<AllowEgressFromLocalVpcToRemoteClassicLink>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</AllowEgressFromLocalVpcToRemoteClassicLink>");
    }
}

pub fn serializePerformanceFactorReference(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PerformanceFactorReference) !void {
    if (value.instance_family) |v| {
        try buf.appendSlice(alloc, "<instanceFamily>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</instanceFamily>");
    }
}

pub fn serializePerformanceFactorReferenceRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PerformanceFactorReferenceRequest) !void {
    if (value.instance_family) |v| {
        try buf.appendSlice(alloc, "<InstanceFamily>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</InstanceFamily>");
    }
}

pub fn serializePhase1DHGroupNumbersRequestListValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase1DHGroupNumbersRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializePhase1EncryptionAlgorithmsRequestListValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase1EncryptionAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializePhase1IntegrityAlgorithmsRequestListValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase1IntegrityAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializePhase2DHGroupNumbersRequestListValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase2DHGroupNumbersRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializePhase2EncryptionAlgorithmsRequestListValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase2EncryptionAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializePhase2IntegrityAlgorithmsRequestListValue(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Phase2IntegrityAlgorithmsRequestListValue) !void {
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializePlacement(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Placement) !void {
    if (value.affinity) |v| {
        try buf.appendSlice(alloc, "<affinity>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</affinity>");
    }
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZoneId>");
    }
    if (value.group_id) |v| {
        try buf.appendSlice(alloc, "<groupId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</groupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(alloc, "<groupName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</groupName>");
    }
    if (value.host_id) |v| {
        try buf.appendSlice(alloc, "<hostId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</hostId>");
    }
    if (value.host_resource_group_arn) |v| {
        try buf.appendSlice(alloc, "<hostResourceGroupArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</hostResourceGroupArn>");
    }
    if (value.partition_number) |v| {
        try buf.appendSlice(alloc, "<partitionNumber>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</partitionNumber>");
    }
    if (value.spread_domain) |v| {
        try buf.appendSlice(alloc, "<spreadDomain>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</spreadDomain>");
    }
    if (value.tenancy) |v| {
        try buf.appendSlice(alloc, "<tenancy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</tenancy>");
    }
}

pub fn serializePortRange(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PortRange) !void {
    if (value.from) |v| {
        try buf.appendSlice(alloc, "<from>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</from>");
    }
    if (value.to) |v| {
        try buf.appendSlice(alloc, "<to>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</to>");
    }
}

pub fn serializePrefixListId(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PrefixListId) !void {
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</description>");
    }
    if (value.prefix_list_id) |v| {
        try buf.appendSlice(alloc, "<prefixListId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</prefixListId>");
    }
}

pub fn serializePriceScheduleSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PriceScheduleSpecification) !void {
    if (value.currency_code) |v| {
        try buf.appendSlice(alloc, "<currencyCode>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</currencyCode>");
    }
    if (value.price) |v| {
        try buf.appendSlice(alloc, "<price>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</price>");
    }
    if (value.term) |v| {
        try buf.appendSlice(alloc, "<term>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</term>");
    }
}

pub fn serializePrivateDnsNameOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PrivateDnsNameOptionsRequest) !void {
    if (value.enable_resource_name_dns_aaaa_record) |v| {
        try buf.appendSlice(alloc, "<EnableResourceNameDnsAAAARecord>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnableResourceNameDnsAAAARecord>");
    }
    if (value.enable_resource_name_dns_a_record) |v| {
        try buf.appendSlice(alloc, "<EnableResourceNameDnsARecord>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnableResourceNameDnsARecord>");
    }
    if (value.hostname_type) |v| {
        try buf.appendSlice(alloc, "<HostnameType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</HostnameType>");
    }
}

pub fn serializePrivateIpAddressSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PrivateIpAddressSpecification) !void {
    if (value.primary) |v| {
        try buf.appendSlice(alloc, "<primary>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</primary>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(alloc, "<privateIpAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</privateIpAddress>");
    }
}

pub fn serializePurchaseRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: PurchaseRequest) !void {
    try buf.appendSlice(alloc, "<InstanceCount>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.instance_count}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</InstanceCount>");
    try buf.appendSlice(alloc, "<PurchaseToken>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.purchase_token);
    try buf.appendSlice(alloc, "</PurchaseToken>");
}

pub fn serializeRegisterInstanceTagAttributeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RegisterInstanceTagAttributeRequest) !void {
    if (value.include_all_tags_of_instance) |v| {
        try buf.appendSlice(alloc, "<IncludeAllTagsOfInstance>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</IncludeAllTagsOfInstance>");
    }
    if (value.instance_tag_keys) |v| {
        try buf.appendSlice(alloc, "<InstanceTagKey>");
        try serializeInstanceTagKeySet(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</InstanceTagKey>");
    }
}

pub fn serializeRemoveIpamOperatingRegion(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RemoveIpamOperatingRegion) !void {
    if (value.region_name) |v| {
        try buf.appendSlice(alloc, "<RegionName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RegionName>");
    }
}

pub fn serializeRemoveIpamOrganizationalUnitExclusion(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RemoveIpamOrganizationalUnitExclusion) !void {
    if (value.organizations_entity_path) |v| {
        try buf.appendSlice(alloc, "<OrganizationsEntityPath>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</OrganizationsEntityPath>");
    }
}

pub fn serializeRemovePrefixListEntry(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RemovePrefixListEntry) !void {
    try buf.appendSlice(alloc, "<Cidr>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.cidr);
    try buf.appendSlice(alloc, "</Cidr>");
}

pub fn serializeRequestFilterPortRange(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestFilterPortRange) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(alloc, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(alloc, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ToPort>");
    }
}

pub fn serializeRequestIpamResourceTag(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestIpamResourceTag) !void {
    if (value.key) |v| {
        try buf.appendSlice(alloc, "<Key>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Key>");
    }
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializeRequestLaunchTemplateData(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestLaunchTemplateData) !void {
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(alloc, "<BlockDeviceMapping>");
        try serializeLaunchTemplateBlockDeviceMappingRequestList(alloc, buf, v, "BlockDeviceMapping");
        try buf.appendSlice(alloc, "</BlockDeviceMapping>");
    }
    if (value.capacity_reservation_specification) |v| {
        try buf.appendSlice(alloc, "<CapacityReservationSpecification>");
        try serializeLaunchTemplateCapacityReservationSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</CapacityReservationSpecification>");
    }
    if (value.cpu_options) |v| {
        try buf.appendSlice(alloc, "<CpuOptions>");
        try serializeLaunchTemplateCpuOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</CpuOptions>");
    }
    if (value.credit_specification) |v| {
        try buf.appendSlice(alloc, "<CreditSpecification>");
        try serializeCreditSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</CreditSpecification>");
    }
    if (value.disable_api_stop) |v| {
        try buf.appendSlice(alloc, "<DisableApiStop>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DisableApiStop>");
    }
    if (value.disable_api_termination) |v| {
        try buf.appendSlice(alloc, "<DisableApiTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DisableApiTermination>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(alloc, "<EbsOptimized>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EbsOptimized>");
    }
    if (value.elastic_gpu_specifications) |v| {
        try buf.appendSlice(alloc, "<ElasticGpuSpecification>");
        try serializeElasticGpuSpecificationList(alloc, buf, v, "ElasticGpuSpecification");
        try buf.appendSlice(alloc, "</ElasticGpuSpecification>");
    }
    if (value.elastic_inference_accelerators) |v| {
        try buf.appendSlice(alloc, "<ElasticInferenceAccelerator>");
        try serializeLaunchTemplateElasticInferenceAcceleratorList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ElasticInferenceAccelerator>");
    }
    if (value.enclave_options) |v| {
        try buf.appendSlice(alloc, "<EnclaveOptions>");
        try serializeLaunchTemplateEnclaveOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</EnclaveOptions>");
    }
    if (value.hibernation_options) |v| {
        try buf.appendSlice(alloc, "<HibernationOptions>");
        try serializeLaunchTemplateHibernationOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</HibernationOptions>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(alloc, "<IamInstanceProfile>");
        try serializeLaunchTemplateIamInstanceProfileSpecificationRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</IamInstanceProfile>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(alloc, "<ImageId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ImageId>");
    }
    if (value.instance_initiated_shutdown_behavior) |v| {
        try buf.appendSlice(alloc, "<InstanceInitiatedShutdownBehavior>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceInitiatedShutdownBehavior>");
    }
    if (value.instance_market_options) |v| {
        try buf.appendSlice(alloc, "<InstanceMarketOptions>");
        try serializeLaunchTemplateInstanceMarketOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</InstanceMarketOptions>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(alloc, "<InstanceRequirements>");
        try serializeInstanceRequirementsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</InstanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<InstanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(alloc, "<KernelId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</KernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(alloc, "<KeyName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</KeyName>");
    }
    if (value.license_specifications) |v| {
        try buf.appendSlice(alloc, "<LicenseSpecification>");
        try serializeLaunchTemplateLicenseSpecificationListRequest(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</LicenseSpecification>");
    }
    if (value.maintenance_options) |v| {
        try buf.appendSlice(alloc, "<MaintenanceOptions>");
        try serializeLaunchTemplateInstanceMaintenanceOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</MaintenanceOptions>");
    }
    if (value.metadata_options) |v| {
        try buf.appendSlice(alloc, "<MetadataOptions>");
        try serializeLaunchTemplateInstanceMetadataOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</MetadataOptions>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(alloc, "<Monitoring>");
        try serializeLaunchTemplatesMonitoringRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(alloc, "<NetworkInterface>");
        try serializeLaunchTemplateInstanceNetworkInterfaceSpecificationRequestList(alloc, buf, v, "InstanceNetworkInterfaceSpecification");
        try buf.appendSlice(alloc, "</NetworkInterface>");
    }
    if (value.network_performance_options) |v| {
        try buf.appendSlice(alloc, "<NetworkPerformanceOptions>");
        try serializeLaunchTemplateNetworkPerformanceOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</NetworkPerformanceOptions>");
    }
    if (value.operator) |v| {
        try buf.appendSlice(alloc, "<Operator>");
        try serializeOperatorRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Operator>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(alloc, "<Placement>");
        try serializeLaunchTemplatePlacementRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</Placement>");
    }
    if (value.private_dns_name_options) |v| {
        try buf.appendSlice(alloc, "<PrivateDnsNameOptions>");
        try serializeLaunchTemplatePrivateDnsNameOptionsRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</PrivateDnsNameOptions>");
    }
    if (value.ram_disk_id) |v| {
        try buf.appendSlice(alloc, "<RamDiskId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RamDiskId>");
    }
    if (value.security_group_ids) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupId>");
        try serializeSecurityGroupIdStringList(alloc, buf, v, "SecurityGroupId");
        try buf.appendSlice(alloc, "</SecurityGroupId>");
    }
    if (value.security_groups) |v| {
        try buf.appendSlice(alloc, "<SecurityGroup>");
        try serializeSecurityGroupStringList(alloc, buf, v, "SecurityGroup");
        try buf.appendSlice(alloc, "</SecurityGroup>");
    }
    if (value.tag_specifications) |v| {
        try buf.appendSlice(alloc, "<TagSpecification>");
        try serializeLaunchTemplateTagSpecificationRequestList(alloc, buf, v, "LaunchTemplateTagSpecificationRequest");
        try buf.appendSlice(alloc, "</TagSpecification>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(alloc, "<UserData>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserData>");
    }
}

pub fn serializeRequestSpotLaunchSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RequestSpotLaunchSpecification) !void {
    if (value.addressing_type) |v| {
        try buf.appendSlice(alloc, "<addressingType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</addressingType>");
    }
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(alloc, "<blockDeviceMapping>");
        try serializeBlockDeviceMappingList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</blockDeviceMapping>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(alloc, "<ebsOptimized>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</ebsOptimized>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(alloc, "<iamInstanceProfile>");
        try serializeIamInstanceProfileSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</iamInstanceProfile>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(alloc, "<imageId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</imageId>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<instanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</instanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(alloc, "<kernelId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</kernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(alloc, "<keyName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</keyName>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(alloc, "<monitoring>");
        try serializeRunInstancesMonitoringEnabled(alloc, buf, v);
        try buf.appendSlice(alloc, "</monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(alloc, "<NetworkInterface>");
        try serializeInstanceNetworkInterfaceSpecificationList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</NetworkInterface>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(alloc, "<placement>");
        try serializeSpotPlacement(alloc, buf, v);
        try buf.appendSlice(alloc, "</placement>");
    }
    if (value.ramdisk_id) |v| {
        try buf.appendSlice(alloc, "<ramdiskId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ramdiskId>");
    }
    if (value.security_group_ids) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupId>");
        try serializeRequestSpotLaunchSpecificationSecurityGroupIdList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SecurityGroupId>");
    }
    if (value.security_groups) |v| {
        try buf.appendSlice(alloc, "<SecurityGroup>");
        try serializeRequestSpotLaunchSpecificationSecurityGroupList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</SecurityGroup>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<subnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</subnetId>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(alloc, "<userData>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</userData>");
    }
}

pub fn serializeReservationFleetInstanceSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ReservationFleetInstanceSpecification) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZoneId>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(alloc, "<EbsOptimized>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EbsOptimized>");
    }
    if (value.instance_platform) |v| {
        try buf.appendSlice(alloc, "<InstancePlatform>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstancePlatform>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<InstanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceType>");
    }
    if (value.priority) |v| {
        try buf.appendSlice(alloc, "<Priority>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Priority>");
    }
    if (value.weight) |v| {
        try buf.appendSlice(alloc, "<Weight>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Weight>");
    }
}

pub fn serializeReservedInstanceLimitPrice(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ReservedInstanceLimitPrice) !void {
    if (value.amount) |v| {
        try buf.appendSlice(alloc, "<amount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</amount>");
    }
    if (value.currency_code) |v| {
        try buf.appendSlice(alloc, "<currencyCode>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</currencyCode>");
    }
}

pub fn serializeReservedInstancesConfiguration(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ReservedInstancesConfiguration) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZoneId>");
    }
    if (value.instance_count) |v| {
        try buf.appendSlice(alloc, "<instanceCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</instanceCount>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<instanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</instanceType>");
    }
    if (value.platform) |v| {
        try buf.appendSlice(alloc, "<platform>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</platform>");
    }
    if (value.scope) |v| {
        try buf.appendSlice(alloc, "<scope>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</scope>");
    }
}

pub fn serializeResourceStatementRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ResourceStatementRequest) !void {
    if (value.resources) |v| {
        try buf.appendSlice(alloc, "<Resource>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Resource>");
    }
    if (value.resource_types) |v| {
        try buf.appendSlice(alloc, "<ResourceType>");
        try serializeValueStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</ResourceType>");
    }
}

pub fn serializeResourceTypeOption(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ResourceTypeOption) !void {
    if (value.option_name) |v| {
        try buf.appendSlice(alloc, "<OptionName>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</OptionName>");
    }
    if (value.option_values) |v| {
        try buf.appendSlice(alloc, "<OptionValue>");
        try serializeResourceTypeOptionValuesList(alloc, buf, v, "member");
        try buf.appendSlice(alloc, "</OptionValue>");
    }
}

pub fn serializeResourceTypeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ResourceTypeRequest) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(alloc, "<ResourceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ResourceType>");
    }
    if (value.resource_type_options) |v| {
        try buf.appendSlice(alloc, "<ResourceTypeOption>");
        try serializeResourceTypeOptionList(alloc, buf, v, "member");
        try buf.appendSlice(alloc, "</ResourceTypeOption>");
    }
}

pub fn serializeRouteServerBgpOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RouteServerBgpOptionsRequest) !void {
    try buf.appendSlice(alloc, "<PeerAsn>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.peer_asn}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</PeerAsn>");
    if (value.peer_liveness_detection) |v| {
        try buf.appendSlice(alloc, "<PeerLivenessDetection>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</PeerLivenessDetection>");
    }
}

pub fn serializeRunInstancesMonitoringEnabled(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: RunInstancesMonitoringEnabled) !void {
    try buf.appendSlice(alloc, "<enabled>");
    try buf.appendSlice(alloc, if (value.enabled) "true" else "false");
    try buf.appendSlice(alloc, "</enabled>");
}

pub fn serializeS3ObjectTag(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: S3ObjectTag) !void {
    if (value.key) |v| {
        try buf.appendSlice(alloc, "<Key>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Key>");
    }
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<Value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Value>");
    }
}

pub fn serializeS3Storage(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: S3Storage) !void {
    if (value.aws_access_key_id) |v| {
        try buf.appendSlice(alloc, "<AWSAccessKeyId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AWSAccessKeyId>");
    }
    if (value.bucket) |v| {
        try buf.appendSlice(alloc, "<bucket>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</bucket>");
    }
    if (value.prefix) |v| {
        try buf.appendSlice(alloc, "<prefix>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</prefix>");
    }
    if (value.upload_policy_signature) |v| {
        try buf.appendSlice(alloc, "<uploadPolicySignature>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</uploadPolicySignature>");
    }
}

pub fn serializeScheduledInstanceRecurrenceRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstanceRecurrenceRequest) !void {
    if (value.frequency) |v| {
        try buf.appendSlice(alloc, "<Frequency>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Frequency>");
    }
    if (value.interval) |v| {
        try buf.appendSlice(alloc, "<Interval>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Interval>");
    }
    if (value.occurrence_days) |v| {
        try buf.appendSlice(alloc, "<OccurrenceDay>");
        try serializeOccurrenceDayRequestSet(alloc, buf, v, "OccurenceDay");
        try buf.appendSlice(alloc, "</OccurrenceDay>");
    }
    if (value.occurrence_relative_to_end) |v| {
        try buf.appendSlice(alloc, "<OccurrenceRelativeToEnd>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</OccurrenceRelativeToEnd>");
    }
    if (value.occurrence_unit) |v| {
        try buf.appendSlice(alloc, "<OccurrenceUnit>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</OccurrenceUnit>");
    }
}

pub fn serializeScheduledInstancesBlockDeviceMapping(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesBlockDeviceMapping) !void {
    if (value.device_name) |v| {
        try buf.appendSlice(alloc, "<DeviceName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DeviceName>");
    }
    if (value.ebs) |v| {
        try buf.appendSlice(alloc, "<Ebs>");
        try serializeScheduledInstancesEbs(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ebs>");
    }
    if (value.no_device) |v| {
        try buf.appendSlice(alloc, "<NoDevice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</NoDevice>");
    }
    if (value.virtual_name) |v| {
        try buf.appendSlice(alloc, "<VirtualName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</VirtualName>");
    }
}

pub fn serializeScheduledInstancesEbs(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesEbs) !void {
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<DeleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DeleteOnTermination>");
    }
    if (value.encrypted) |v| {
        try buf.appendSlice(alloc, "<Encrypted>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Encrypted>");
    }
    if (value.iops) |v| {
        try buf.appendSlice(alloc, "<Iops>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Iops>");
    }
    if (value.snapshot_id) |v| {
        try buf.appendSlice(alloc, "<SnapshotId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SnapshotId>");
    }
    if (value.volume_size) |v| {
        try buf.appendSlice(alloc, "<VolumeSize>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</VolumeSize>");
    }
    if (value.volume_type) |v| {
        try buf.appendSlice(alloc, "<VolumeType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</VolumeType>");
    }
}

pub fn serializeScheduledInstancesIamInstanceProfile(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesIamInstanceProfile) !void {
    if (value.arn) |v| {
        try buf.appendSlice(alloc, "<Arn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Arn>");
    }
    if (value.name) |v| {
        try buf.appendSlice(alloc, "<Name>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Name>");
    }
}

pub fn serializeScheduledInstancesIpv6Address(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesIpv6Address) !void {
    if (value.ipv_6_address) |v| {
        try buf.appendSlice(alloc, "<Ipv6Address>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ipv6Address>");
    }
}

pub fn serializeScheduledInstancesLaunchSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesLaunchSpecification) !void {
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(alloc, "<BlockDeviceMapping>");
        try serializeScheduledInstancesBlockDeviceMappingSet(alloc, buf, v, "BlockDeviceMapping");
        try buf.appendSlice(alloc, "</BlockDeviceMapping>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(alloc, "<EbsOptimized>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EbsOptimized>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(alloc, "<IamInstanceProfile>");
        try serializeScheduledInstancesIamInstanceProfile(alloc, buf, v);
        try buf.appendSlice(alloc, "</IamInstanceProfile>");
    }
    try buf.appendSlice(alloc, "<ImageId>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.image_id);
    try buf.appendSlice(alloc, "</ImageId>");
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<InstanceType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</InstanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(alloc, "<KernelId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</KernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(alloc, "<KeyName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</KeyName>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(alloc, "<Monitoring>");
        try serializeScheduledInstancesMonitoring(alloc, buf, v);
        try buf.appendSlice(alloc, "</Monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(alloc, "<NetworkInterface>");
        try serializeScheduledInstancesNetworkInterfaceSet(alloc, buf, v, "NetworkInterface");
        try buf.appendSlice(alloc, "</NetworkInterface>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(alloc, "<Placement>");
        try serializeScheduledInstancesPlacement(alloc, buf, v);
        try buf.appendSlice(alloc, "</Placement>");
    }
    if (value.ramdisk_id) |v| {
        try buf.appendSlice(alloc, "<RamdiskId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RamdiskId>");
    }
    if (value.security_group_ids) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupId>");
        try serializeScheduledInstancesSecurityGroupIdSet(alloc, buf, v, "SecurityGroupId");
        try buf.appendSlice(alloc, "</SecurityGroupId>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SubnetId>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(alloc, "<UserData>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserData>");
    }
}

pub fn serializeScheduledInstancesMonitoring(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesMonitoring) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<Enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Enabled>");
    }
}

pub fn serializeScheduledInstancesNetworkInterface(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesNetworkInterface) !void {
    if (value.associate_public_ip_address) |v| {
        try buf.appendSlice(alloc, "<AssociatePublicIpAddress>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</AssociatePublicIpAddress>");
    }
    if (value.delete_on_termination) |v| {
        try buf.appendSlice(alloc, "<DeleteOnTermination>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</DeleteOnTermination>");
    }
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
    if (value.device_index) |v| {
        try buf.appendSlice(alloc, "<DeviceIndex>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</DeviceIndex>");
    }
    if (value.groups) |v| {
        try buf.appendSlice(alloc, "<Group>");
        try serializeScheduledInstancesSecurityGroupIdSet(alloc, buf, v, "SecurityGroupId");
        try buf.appendSlice(alloc, "</Group>");
    }
    if (value.ipv_6_address_count) |v| {
        try buf.appendSlice(alloc, "<Ipv6AddressCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Ipv6AddressCount>");
    }
    if (value.ipv_6_addresses) |v| {
        try buf.appendSlice(alloc, "<Ipv6Address>");
        try serializeScheduledInstancesIpv6AddressList(alloc, buf, v, "Ipv6Address");
        try buf.appendSlice(alloc, "</Ipv6Address>");
    }
    if (value.network_interface_id) |v| {
        try buf.appendSlice(alloc, "<NetworkInterfaceId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</NetworkInterfaceId>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(alloc, "<PrivateIpAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PrivateIpAddress>");
    }
    if (value.private_ip_address_configs) |v| {
        try buf.appendSlice(alloc, "<PrivateIpAddressConfig>");
        try serializePrivateIpAddressConfigSet(alloc, buf, v, "PrivateIpAddressConfigSet");
        try buf.appendSlice(alloc, "</PrivateIpAddressConfig>");
    }
    if (value.secondary_private_ip_address_count) |v| {
        try buf.appendSlice(alloc, "<SecondaryPrivateIpAddressCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</SecondaryPrivateIpAddressCount>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeScheduledInstancesPlacement(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesPlacement) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<AvailabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</AvailabilityZone>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(alloc, "<GroupName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</GroupName>");
    }
}

pub fn serializeScheduledInstancesPrivateIpAddressConfig(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ScheduledInstancesPrivateIpAddressConfig) !void {
    if (value.primary) |v| {
        try buf.appendSlice(alloc, "<Primary>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</Primary>");
    }
    if (value.private_ip_address) |v| {
        try buf.appendSlice(alloc, "<PrivateIpAddress>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PrivateIpAddress>");
    }
}

pub fn serializeSecurityGroupRuleDescription(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SecurityGroupRuleDescription) !void {
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
    if (value.security_group_rule_id) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupRuleId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SecurityGroupRuleId>");
    }
}

pub fn serializeSecurityGroupRuleRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SecurityGroupRuleRequest) !void {
    if (value.cidr_ipv_4) |v| {
        try buf.appendSlice(alloc, "<CidrIpv4>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</CidrIpv4>");
    }
    if (value.cidr_ipv_6) |v| {
        try buf.appendSlice(alloc, "<CidrIpv6>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</CidrIpv6>");
    }
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
    if (value.from_port) |v| {
        try buf.appendSlice(alloc, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</FromPort>");
    }
    if (value.ip_protocol) |v| {
        try buf.appendSlice(alloc, "<IpProtocol>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</IpProtocol>");
    }
    if (value.prefix_list_id) |v| {
        try buf.appendSlice(alloc, "<PrefixListId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PrefixListId>");
    }
    if (value.referenced_group_id) |v| {
        try buf.appendSlice(alloc, "<ReferencedGroupId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ReferencedGroupId>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(alloc, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ToPort>");
    }
}

pub fn serializeSecurityGroupRuleUpdate(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SecurityGroupRuleUpdate) !void {
    if (value.security_group_rule) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupRule>");
        try serializeSecurityGroupRuleRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</SecurityGroupRule>");
    }
    try buf.appendSlice(alloc, "<SecurityGroupRuleId>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.security_group_rule_id);
    try buf.appendSlice(alloc, "</SecurityGroupRuleId>");
}

pub fn serializeSlotDateTimeRangeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SlotDateTimeRangeRequest) !void {
    try buf.appendSlice(alloc, "<EarliestTime>");
    {
        const ts_str = std.fmt.allocPrint(alloc, "{d}", .{value.earliest_time}) catch "";
        try buf.appendSlice(alloc, ts_str);
    }
    try buf.appendSlice(alloc, "</EarliestTime>");
    try buf.appendSlice(alloc, "<LatestTime>");
    {
        const ts_str = std.fmt.allocPrint(alloc, "{d}", .{value.latest_time}) catch "";
        try buf.appendSlice(alloc, ts_str);
    }
    try buf.appendSlice(alloc, "</LatestTime>");
}

pub fn serializeSlotStartTimeRangeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SlotStartTimeRangeRequest) !void {
    if (value.earliest_time) |v| {
        try buf.appendSlice(alloc, "<EarliestTime>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</EarliestTime>");
    }
    if (value.latest_time) |v| {
        try buf.appendSlice(alloc, "<LatestTime>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</LatestTime>");
    }
}

pub fn serializeSnapshotDiskContainer(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SnapshotDiskContainer) !void {
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<Description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Description>");
    }
    if (value.format) |v| {
        try buf.appendSlice(alloc, "<Format>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Format>");
    }
    if (value.url) |v| {
        try buf.appendSlice(alloc, "<Url>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Url>");
    }
    if (value.user_bucket) |v| {
        try buf.appendSlice(alloc, "<UserBucket>");
        try serializeUserBucket(alloc, buf, v);
        try buf.appendSlice(alloc, "</UserBucket>");
    }
}

pub fn serializeSpotCapacityRebalance(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotCapacityRebalance) !void {
    if (value.replacement_strategy) |v| {
        try buf.appendSlice(alloc, "<replacementStrategy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</replacementStrategy>");
    }
    if (value.termination_delay) |v| {
        try buf.appendSlice(alloc, "<terminationDelay>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</terminationDelay>");
    }
}

pub fn serializeSpotFleetLaunchSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetLaunchSpecification) !void {
    if (value.addressing_type) |v| {
        try buf.appendSlice(alloc, "<addressingType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</addressingType>");
    }
    if (value.block_device_mappings) |v| {
        try buf.appendSlice(alloc, "<blockDeviceMapping>");
        try serializeBlockDeviceMappingList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</blockDeviceMapping>");
    }
    if (value.ebs_optimized) |v| {
        try buf.appendSlice(alloc, "<ebsOptimized>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</ebsOptimized>");
    }
    if (value.iam_instance_profile) |v| {
        try buf.appendSlice(alloc, "<iamInstanceProfile>");
        try serializeIamInstanceProfileSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</iamInstanceProfile>");
    }
    if (value.image_id) |v| {
        try buf.appendSlice(alloc, "<imageId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</imageId>");
    }
    if (value.instance_requirements) |v| {
        try buf.appendSlice(alloc, "<instanceRequirements>");
        try serializeInstanceRequirements(alloc, buf, v);
        try buf.appendSlice(alloc, "</instanceRequirements>");
    }
    if (value.instance_type) |v| {
        try buf.appendSlice(alloc, "<instanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</instanceType>");
    }
    if (value.kernel_id) |v| {
        try buf.appendSlice(alloc, "<kernelId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</kernelId>");
    }
    if (value.key_name) |v| {
        try buf.appendSlice(alloc, "<keyName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</keyName>");
    }
    if (value.monitoring) |v| {
        try buf.appendSlice(alloc, "<monitoring>");
        try serializeSpotFleetMonitoring(alloc, buf, v);
        try buf.appendSlice(alloc, "</monitoring>");
    }
    if (value.network_interfaces) |v| {
        try buf.appendSlice(alloc, "<networkInterfaceSet>");
        try serializeInstanceNetworkInterfaceSpecificationList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</networkInterfaceSet>");
    }
    if (value.placement) |v| {
        try buf.appendSlice(alloc, "<placement>");
        try serializeSpotPlacement(alloc, buf, v);
        try buf.appendSlice(alloc, "</placement>");
    }
    if (value.ramdisk_id) |v| {
        try buf.appendSlice(alloc, "<ramdiskId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</ramdiskId>");
    }
    if (value.security_groups) |v| {
        try buf.appendSlice(alloc, "<groupSet>");
        try serializeGroupIdentifierList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</groupSet>");
    }
    if (value.spot_price) |v| {
        try buf.appendSlice(alloc, "<spotPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</spotPrice>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<subnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</subnetId>");
    }
    if (value.tag_specifications) |v| {
        try buf.appendSlice(alloc, "<tagSpecificationSet>");
        try serializeSpotFleetTagSpecificationList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</tagSpecificationSet>");
    }
    if (value.user_data) |v| {
        try buf.appendSlice(alloc, "<userData>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</userData>");
    }
    if (value.weighted_capacity) |v| {
        try buf.appendSlice(alloc, "<weightedCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</weightedCapacity>");
    }
}

pub fn serializeSpotFleetMonitoring(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetMonitoring) !void {
    if (value.enabled) |v| {
        try buf.appendSlice(alloc, "<enabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</enabled>");
    }
}

pub fn serializeSpotFleetRequestConfigData(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetRequestConfigData) !void {
    if (value.allocation_strategy) |v| {
        try buf.appendSlice(alloc, "<allocationStrategy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</allocationStrategy>");
    }
    if (value.client_token) |v| {
        try buf.appendSlice(alloc, "<clientToken>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</clientToken>");
    }
    if (value.context) |v| {
        try buf.appendSlice(alloc, "<context>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</context>");
    }
    if (value.excess_capacity_termination_policy) |v| {
        try buf.appendSlice(alloc, "<excessCapacityTerminationPolicy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</excessCapacityTerminationPolicy>");
    }
    if (value.fulfilled_capacity) |v| {
        try buf.appendSlice(alloc, "<fulfilledCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</fulfilledCapacity>");
    }
    try buf.appendSlice(alloc, "<iamFleetRole>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.iam_fleet_role);
    try buf.appendSlice(alloc, "</iamFleetRole>");
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(alloc, "<instanceInterruptionBehavior>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</instanceInterruptionBehavior>");
    }
    if (value.instance_pools_to_use_count) |v| {
        try buf.appendSlice(alloc, "<instancePoolsToUseCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</instancePoolsToUseCount>");
    }
    if (value.launch_specifications) |v| {
        try buf.appendSlice(alloc, "<launchSpecifications>");
        try serializeLaunchSpecsList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</launchSpecifications>");
    }
    if (value.launch_template_configs) |v| {
        try buf.appendSlice(alloc, "<launchTemplateConfigs>");
        try serializeLaunchTemplateConfigList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</launchTemplateConfigs>");
    }
    if (value.load_balancers_config) |v| {
        try buf.appendSlice(alloc, "<loadBalancersConfig>");
        try serializeLoadBalancersConfig(alloc, buf, v);
        try buf.appendSlice(alloc, "</loadBalancersConfig>");
    }
    if (value.on_demand_allocation_strategy) |v| {
        try buf.appendSlice(alloc, "<onDemandAllocationStrategy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</onDemandAllocationStrategy>");
    }
    if (value.on_demand_fulfilled_capacity) |v| {
        try buf.appendSlice(alloc, "<onDemandFulfilledCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</onDemandFulfilledCapacity>");
    }
    if (value.on_demand_max_total_price) |v| {
        try buf.appendSlice(alloc, "<onDemandMaxTotalPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</onDemandMaxTotalPrice>");
    }
    if (value.on_demand_target_capacity) |v| {
        try buf.appendSlice(alloc, "<onDemandTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</onDemandTargetCapacity>");
    }
    if (value.replace_unhealthy_instances) |v| {
        try buf.appendSlice(alloc, "<replaceUnhealthyInstances>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</replaceUnhealthyInstances>");
    }
    if (value.spot_maintenance_strategies) |v| {
        try buf.appendSlice(alloc, "<spotMaintenanceStrategies>");
        try serializeSpotMaintenanceStrategies(alloc, buf, v);
        try buf.appendSlice(alloc, "</spotMaintenanceStrategies>");
    }
    if (value.spot_max_total_price) |v| {
        try buf.appendSlice(alloc, "<spotMaxTotalPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</spotMaxTotalPrice>");
    }
    if (value.spot_price) |v| {
        try buf.appendSlice(alloc, "<spotPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</spotPrice>");
    }
    if (value.tag_specifications) |v| {
        try buf.appendSlice(alloc, "<TagSpecification>");
        try serializeTagSpecificationList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</TagSpecification>");
    }
    try buf.appendSlice(alloc, "<targetCapacity>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.target_capacity}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</targetCapacity>");
    if (value.target_capacity_unit_type) |v| {
        try buf.appendSlice(alloc, "<targetCapacityUnitType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</targetCapacityUnitType>");
    }
    if (value.terminate_instances_with_expiration) |v| {
        try buf.appendSlice(alloc, "<terminateInstancesWithExpiration>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</terminateInstancesWithExpiration>");
    }
    if (value.@"type") |v| {
        try buf.appendSlice(alloc, "<type>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</type>");
    }
    if (value.valid_from) |v| {
        try buf.appendSlice(alloc, "<validFrom>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</validFrom>");
    }
    if (value.valid_until) |v| {
        try buf.appendSlice(alloc, "<validUntil>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</validUntil>");
    }
}

pub fn serializeSpotFleetTagSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotFleetTagSpecification) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(alloc, "<resourceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</resourceType>");
    }
    if (value.tags) |v| {
        try buf.appendSlice(alloc, "<tag>");
        try serializeTagList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</tag>");
    }
}

pub fn serializeSpotMaintenanceStrategies(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotMaintenanceStrategies) !void {
    if (value.capacity_rebalance) |v| {
        try buf.appendSlice(alloc, "<capacityRebalance>");
        try serializeSpotCapacityRebalance(alloc, buf, v);
        try buf.appendSlice(alloc, "</capacityRebalance>");
    }
}

pub fn serializeSpotMarketOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotMarketOptions) !void {
    if (value.block_duration_minutes) |v| {
        try buf.appendSlice(alloc, "<BlockDurationMinutes>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</BlockDurationMinutes>");
    }
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(alloc, "<InstanceInterruptionBehavior>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceInterruptionBehavior>");
    }
    if (value.max_price) |v| {
        try buf.appendSlice(alloc, "<MaxPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</MaxPrice>");
    }
    if (value.spot_instance_type) |v| {
        try buf.appendSlice(alloc, "<SpotInstanceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</SpotInstanceType>");
    }
    if (value.valid_until) |v| {
        try buf.appendSlice(alloc, "<ValidUntil>");
        {
            const ts_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, ts_str);
        }
        try buf.appendSlice(alloc, "</ValidUntil>");
    }
}

pub fn serializeSpotOptionsRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotOptionsRequest) !void {
    if (value.allocation_strategy) |v| {
        try buf.appendSlice(alloc, "<AllocationStrategy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AllocationStrategy>");
    }
    if (value.instance_interruption_behavior) |v| {
        try buf.appendSlice(alloc, "<InstanceInterruptionBehavior>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InstanceInterruptionBehavior>");
    }
    if (value.instance_pools_to_use_count) |v| {
        try buf.appendSlice(alloc, "<InstancePoolsToUseCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</InstancePoolsToUseCount>");
    }
    if (value.maintenance_strategies) |v| {
        try buf.appendSlice(alloc, "<MaintenanceStrategies>");
        try serializeFleetSpotMaintenanceStrategiesRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</MaintenanceStrategies>");
    }
    if (value.max_total_price) |v| {
        try buf.appendSlice(alloc, "<MaxTotalPrice>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</MaxTotalPrice>");
    }
    if (value.min_target_capacity) |v| {
        try buf.appendSlice(alloc, "<MinTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</MinTargetCapacity>");
    }
    if (value.single_availability_zone) |v| {
        try buf.appendSlice(alloc, "<SingleAvailabilityZone>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</SingleAvailabilityZone>");
    }
    if (value.single_instance_type) |v| {
        try buf.appendSlice(alloc, "<SingleInstanceType>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</SingleInstanceType>");
    }
}

pub fn serializeSpotPlacement(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SpotPlacement) !void {
    if (value.availability_zone) |v| {
        try buf.appendSlice(alloc, "<availabilityZone>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZone>");
    }
    if (value.availability_zone_id) |v| {
        try buf.appendSlice(alloc, "<availabilityZoneId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</availabilityZoneId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(alloc, "<groupName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</groupName>");
    }
    if (value.tenancy) |v| {
        try buf.appendSlice(alloc, "<tenancy>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</tenancy>");
    }
}

pub fn serializeStorage(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Storage) !void {
    if (value.s3) |v| {
        try buf.appendSlice(alloc, "<S3>");
        try serializeS3Storage(alloc, buf, v);
        try buf.appendSlice(alloc, "</S3>");
    }
}

pub fn serializeStorageLocation(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: StorageLocation) !void {
    if (value.bucket) |v| {
        try buf.appendSlice(alloc, "<Bucket>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Bucket>");
    }
    if (value.key) |v| {
        try buf.appendSlice(alloc, "<Key>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Key>");
    }
}

pub fn serializeSubnetConfiguration(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: SubnetConfiguration) !void {
    if (value.ipv_4) |v| {
        try buf.appendSlice(alloc, "<Ipv4>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ipv4>");
    }
    if (value.ipv_6) |v| {
        try buf.appendSlice(alloc, "<Ipv6>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Ipv6>");
    }
    if (value.subnet_id) |v| {
        try buf.appendSlice(alloc, "<SubnetId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</SubnetId>");
    }
}

pub fn serializeTag(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: Tag) !void {
    if (value.key) |v| {
        try buf.appendSlice(alloc, "<key>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</key>");
    }
    if (value.value) |v| {
        try buf.appendSlice(alloc, "<value>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</value>");
    }
}

pub fn serializeTagSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TagSpecification) !void {
    if (value.resource_type) |v| {
        try buf.appendSlice(alloc, "<resourceType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</resourceType>");
    }
    if (value.tags) |v| {
        try buf.appendSlice(alloc, "<Tag>");
        try serializeTagList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Tag>");
    }
}

pub fn serializeTargetCapacitySpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetCapacitySpecificationRequest) !void {
    if (value.default_target_capacity_type) |v| {
        try buf.appendSlice(alloc, "<DefaultTargetCapacityType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DefaultTargetCapacityType>");
    }
    if (value.on_demand_target_capacity) |v| {
        try buf.appendSlice(alloc, "<OnDemandTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</OnDemandTargetCapacity>");
    }
    if (value.spot_target_capacity) |v| {
        try buf.appendSlice(alloc, "<SpotTargetCapacity>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</SpotTargetCapacity>");
    }
    if (value.target_capacity_unit_type) |v| {
        try buf.appendSlice(alloc, "<TargetCapacityUnitType>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</TargetCapacityUnitType>");
    }
    try buf.appendSlice(alloc, "<TotalTargetCapacity>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.total_target_capacity}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</TotalTargetCapacity>");
}

pub fn serializeTargetConfigurationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetConfigurationRequest) !void {
    if (value.instance_count) |v| {
        try buf.appendSlice(alloc, "<InstanceCount>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</InstanceCount>");
    }
    try buf.appendSlice(alloc, "<OfferingId>");
    try aws.xml.appendXmlEscaped(alloc, buf, value.offering_id);
    try buf.appendSlice(alloc, "</OfferingId>");
}

pub fn serializeTargetGroup(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetGroup) !void {
    if (value.arn) |v| {
        try buf.appendSlice(alloc, "<arn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</arn>");
    }
}

pub fn serializeTargetGroupsConfig(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TargetGroupsConfig) !void {
    if (value.target_groups) |v| {
        try buf.appendSlice(alloc, "<targetGroups>");
        try serializeTargetGroups(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</targetGroups>");
    }
}

pub fn serializeThroughResourcesStatementRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: ThroughResourcesStatementRequest) !void {
    if (value.resource_statement) |v| {
        try buf.appendSlice(alloc, "<ResourceStatement>");
        try serializeResourceStatementRequest(alloc, buf, v);
        try buf.appendSlice(alloc, "</ResourceStatement>");
    }
}

pub fn serializeTotalLocalStorageGB(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TotalLocalStorageGB) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeTotalLocalStorageGBRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TotalLocalStorageGBRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<Min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Min>");
    }
}

pub fn serializeTrafficMirrorPortRangeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TrafficMirrorPortRangeRequest) !void {
    if (value.from_port) |v| {
        try buf.appendSlice(alloc, "<FromPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</FromPort>");
    }
    if (value.to_port) |v| {
        try buf.appendSlice(alloc, "<ToPort>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ToPort>");
    }
}

pub fn serializeTransitGatewayConnectRequestBgpOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TransitGatewayConnectRequestBgpOptions) !void {
    if (value.peer_asn) |v| {
        try buf.appendSlice(alloc, "<PeerAsn>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</PeerAsn>");
    }
}

pub fn serializeTransitGatewayRequestOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: TransitGatewayRequestOptions) !void {
    if (value.amazon_side_asn) |v| {
        try buf.appendSlice(alloc, "<AmazonSideAsn>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</AmazonSideAsn>");
    }
    if (value.auto_accept_shared_attachments) |v| {
        try buf.appendSlice(alloc, "<AutoAcceptSharedAttachments>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</AutoAcceptSharedAttachments>");
    }
    if (value.default_route_table_association) |v| {
        try buf.appendSlice(alloc, "<DefaultRouteTableAssociation>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DefaultRouteTableAssociation>");
    }
    if (value.default_route_table_propagation) |v| {
        try buf.appendSlice(alloc, "<DefaultRouteTablePropagation>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DefaultRouteTablePropagation>");
    }
    if (value.dns_support) |v| {
        try buf.appendSlice(alloc, "<DnsSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</DnsSupport>");
    }
    if (value.multicast_support) |v| {
        try buf.appendSlice(alloc, "<MulticastSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</MulticastSupport>");
    }
    if (value.security_group_referencing_support) |v| {
        try buf.appendSlice(alloc, "<SecurityGroupReferencingSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</SecurityGroupReferencingSupport>");
    }
    if (value.transit_gateway_cidr_blocks) |v| {
        try buf.appendSlice(alloc, "<TransitGatewayCidrBlocks>");
        try serializeTransitGatewayCidrBlockStringList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</TransitGatewayCidrBlocks>");
    }
    if (value.vpn_ecmp_support) |v| {
        try buf.appendSlice(alloc, "<VpnEcmpSupport>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</VpnEcmpSupport>");
    }
}

pub fn serializeUserBucket(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: UserBucket) !void {
    if (value.s3_bucket) |v| {
        try buf.appendSlice(alloc, "<S3Bucket>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</S3Bucket>");
    }
    if (value.s3_key) |v| {
        try buf.appendSlice(alloc, "<S3Key>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</S3Key>");
    }
}

pub fn serializeUserData(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: UserData) !void {
    if (value.data) |v| {
        try buf.appendSlice(alloc, "<data>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</data>");
    }
}

pub fn serializeUserIdGroupPair(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: UserIdGroupPair) !void {
    if (value.description) |v| {
        try buf.appendSlice(alloc, "<description>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</description>");
    }
    if (value.group_id) |v| {
        try buf.appendSlice(alloc, "<groupId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</groupId>");
    }
    if (value.group_name) |v| {
        try buf.appendSlice(alloc, "<groupName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</groupName>");
    }
    if (value.peering_status) |v| {
        try buf.appendSlice(alloc, "<peeringStatus>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</peeringStatus>");
    }
    if (value.user_id) |v| {
        try buf.appendSlice(alloc, "<userId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</userId>");
    }
    if (value.vpc_id) |v| {
        try buf.appendSlice(alloc, "<vpcId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</vpcId>");
    }
    if (value.vpc_peering_connection_id) |v| {
        try buf.appendSlice(alloc, "<vpcPeeringConnectionId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</vpcPeeringConnectionId>");
    }
}

pub fn serializeVCpuCountRange(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VCpuCountRange) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</max>");
    }
    if (value.min) |v| {
        try buf.appendSlice(alloc, "<min>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</min>");
    }
}

pub fn serializeVCpuCountRangeRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VCpuCountRangeRequest) !void {
    if (value.max) |v| {
        try buf.appendSlice(alloc, "<Max>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Max>");
    }
    try buf.appendSlice(alloc, "<Min>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.min}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</Min>");
}

pub fn serializeVerifiedAccessLogCloudWatchLogsDestinationOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogCloudWatchLogsDestinationOptions) !void {
    try buf.appendSlice(alloc, "<Enabled>");
    try buf.appendSlice(alloc, if (value.enabled) "true" else "false");
    try buf.appendSlice(alloc, "</Enabled>");
    if (value.log_group) |v| {
        try buf.appendSlice(alloc, "<LogGroup>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LogGroup>");
    }
}

pub fn serializeVerifiedAccessLogKinesisDataFirehoseDestinationOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogKinesisDataFirehoseDestinationOptions) !void {
    if (value.delivery_stream) |v| {
        try buf.appendSlice(alloc, "<DeliveryStream>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DeliveryStream>");
    }
    try buf.appendSlice(alloc, "<Enabled>");
    try buf.appendSlice(alloc, if (value.enabled) "true" else "false");
    try buf.appendSlice(alloc, "</Enabled>");
}

pub fn serializeVerifiedAccessLogOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogOptions) !void {
    if (value.cloud_watch_logs) |v| {
        try buf.appendSlice(alloc, "<CloudWatchLogs>");
        try serializeVerifiedAccessLogCloudWatchLogsDestinationOptions(alloc, buf, v);
        try buf.appendSlice(alloc, "</CloudWatchLogs>");
    }
    if (value.include_trust_context) |v| {
        try buf.appendSlice(alloc, "<IncludeTrustContext>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</IncludeTrustContext>");
    }
    if (value.kinesis_data_firehose) |v| {
        try buf.appendSlice(alloc, "<KinesisDataFirehose>");
        try serializeVerifiedAccessLogKinesisDataFirehoseDestinationOptions(alloc, buf, v);
        try buf.appendSlice(alloc, "</KinesisDataFirehose>");
    }
    if (value.log_version) |v| {
        try buf.appendSlice(alloc, "<LogVersion>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LogVersion>");
    }
    if (value.s3) |v| {
        try buf.appendSlice(alloc, "<S3>");
        try serializeVerifiedAccessLogS3DestinationOptions(alloc, buf, v);
        try buf.appendSlice(alloc, "</S3>");
    }
}

pub fn serializeVerifiedAccessLogS3DestinationOptions(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessLogS3DestinationOptions) !void {
    if (value.bucket_name) |v| {
        try buf.appendSlice(alloc, "<BucketName>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</BucketName>");
    }
    if (value.bucket_owner) |v| {
        try buf.appendSlice(alloc, "<BucketOwner>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</BucketOwner>");
    }
    try buf.appendSlice(alloc, "<Enabled>");
    try buf.appendSlice(alloc, if (value.enabled) "true" else "false");
    try buf.appendSlice(alloc, "</Enabled>");
    if (value.prefix) |v| {
        try buf.appendSlice(alloc, "<Prefix>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</Prefix>");
    }
}

pub fn serializeVerifiedAccessSseSpecificationRequest(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VerifiedAccessSseSpecificationRequest) !void {
    if (value.customer_managed_key_enabled) |v| {
        try buf.appendSlice(alloc, "<CustomerManagedKeyEnabled>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</CustomerManagedKeyEnabled>");
    }
    if (value.kms_key_arn) |v| {
        try buf.appendSlice(alloc, "<KmsKeyArn>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</KmsKeyArn>");
    }
}

pub fn serializeVolumeDetail(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VolumeDetail) !void {
    try buf.appendSlice(alloc, "<size>");
    {
        const num_str = std.fmt.allocPrint(alloc, "{d}", .{value.size}) catch "";
        try buf.appendSlice(alloc, num_str);
    }
    try buf.appendSlice(alloc, "</size>");
}

pub fn serializeVpcEncryptionControlConfiguration(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpcEncryptionControlConfiguration) !void {
    if (value.egress_only_internet_gateway_exclusion) |v| {
        try buf.appendSlice(alloc, "<EgressOnlyInternetGatewayExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</EgressOnlyInternetGatewayExclusion>");
    }
    if (value.elastic_file_system_exclusion) |v| {
        try buf.appendSlice(alloc, "<ElasticFileSystemExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</ElasticFileSystemExclusion>");
    }
    if (value.internet_gateway_exclusion) |v| {
        try buf.appendSlice(alloc, "<InternetGatewayExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</InternetGatewayExclusion>");
    }
    if (value.lambda_exclusion) |v| {
        try buf.appendSlice(alloc, "<LambdaExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</LambdaExclusion>");
    }
    try buf.appendSlice(alloc, "<Mode>");
    try buf.appendSlice(alloc, @tagName(value.mode));
    try buf.appendSlice(alloc, "</Mode>");
    if (value.nat_gateway_exclusion) |v| {
        try buf.appendSlice(alloc, "<NatGatewayExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</NatGatewayExclusion>");
    }
    if (value.virtual_private_gateway_exclusion) |v| {
        try buf.appendSlice(alloc, "<VirtualPrivateGatewayExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</VirtualPrivateGatewayExclusion>");
    }
    if (value.vpc_lattice_exclusion) |v| {
        try buf.appendSlice(alloc, "<VpcLatticeExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</VpcLatticeExclusion>");
    }
    if (value.vpc_peering_exclusion) |v| {
        try buf.appendSlice(alloc, "<VpcPeeringExclusion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</VpcPeeringExclusion>");
    }
}

pub fn serializeVpnConnectionOptionsSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpnConnectionOptionsSpecification) !void {
    if (value.enable_acceleration) |v| {
        try buf.appendSlice(alloc, "<EnableAcceleration>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnableAcceleration>");
    }
    if (value.local_ipv_4_network_cidr) |v| {
        try buf.appendSlice(alloc, "<LocalIpv4NetworkCidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LocalIpv4NetworkCidr>");
    }
    if (value.local_ipv_6_network_cidr) |v| {
        try buf.appendSlice(alloc, "<LocalIpv6NetworkCidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</LocalIpv6NetworkCidr>");
    }
    if (value.outside_ip_address_type) |v| {
        try buf.appendSlice(alloc, "<OutsideIpAddressType>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</OutsideIpAddressType>");
    }
    if (value.remote_ipv_4_network_cidr) |v| {
        try buf.appendSlice(alloc, "<RemoteIpv4NetworkCidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RemoteIpv4NetworkCidr>");
    }
    if (value.remote_ipv_6_network_cidr) |v| {
        try buf.appendSlice(alloc, "<RemoteIpv6NetworkCidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</RemoteIpv6NetworkCidr>");
    }
    if (value.static_routes_only) |v| {
        try buf.appendSlice(alloc, "<staticRoutesOnly>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</staticRoutesOnly>");
    }
    if (value.transport_transit_gateway_attachment_id) |v| {
        try buf.appendSlice(alloc, "<TransportTransitGatewayAttachmentId>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TransportTransitGatewayAttachmentId>");
    }
    if (value.tunnel_bandwidth) |v| {
        try buf.appendSlice(alloc, "<TunnelBandwidth>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</TunnelBandwidth>");
    }
    if (value.tunnel_inside_ip_version) |v| {
        try buf.appendSlice(alloc, "<TunnelInsideIpVersion>");
        try buf.appendSlice(alloc, @tagName(v));
        try buf.appendSlice(alloc, "</TunnelInsideIpVersion>");
    }
    if (value.tunnel_options) |v| {
        try buf.appendSlice(alloc, "<TunnelOptions>");
        try serializeVpnTunnelOptionsSpecificationsList(alloc, buf, v, "member");
        try buf.appendSlice(alloc, "</TunnelOptions>");
    }
}

pub fn serializeVpnTunnelLogOptionsSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpnTunnelLogOptionsSpecification) !void {
    if (value.cloud_watch_log_options) |v| {
        try buf.appendSlice(alloc, "<CloudWatchLogOptions>");
        try serializeCloudWatchLogOptionsSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</CloudWatchLogOptions>");
    }
}

pub fn serializeVpnTunnelOptionsSpecification(alloc: std.mem.Allocator, buf: *std.ArrayList(u8), value: VpnTunnelOptionsSpecification) !void {
    if (value.dpd_timeout_action) |v| {
        try buf.appendSlice(alloc, "<DPDTimeoutAction>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</DPDTimeoutAction>");
    }
    if (value.dpd_timeout_seconds) |v| {
        try buf.appendSlice(alloc, "<DPDTimeoutSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</DPDTimeoutSeconds>");
    }
    if (value.enable_tunnel_lifecycle_control) |v| {
        try buf.appendSlice(alloc, "<EnableTunnelLifecycleControl>");
        try buf.appendSlice(alloc, if (v) "true" else "false");
        try buf.appendSlice(alloc, "</EnableTunnelLifecycleControl>");
    }
    if (value.ike_versions) |v| {
        try buf.appendSlice(alloc, "<IKEVersion>");
        try serializeIKEVersionsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</IKEVersion>");
    }
    if (value.log_options) |v| {
        try buf.appendSlice(alloc, "<LogOptions>");
        try serializeVpnTunnelLogOptionsSpecification(alloc, buf, v);
        try buf.appendSlice(alloc, "</LogOptions>");
    }
    if (value.phase_1_dh_group_numbers) |v| {
        try buf.appendSlice(alloc, "<Phase1DHGroupNumber>");
        try serializePhase1DHGroupNumbersRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase1DHGroupNumber>");
    }
    if (value.phase_1_encryption_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase1EncryptionAlgorithm>");
        try serializePhase1EncryptionAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase1EncryptionAlgorithm>");
    }
    if (value.phase_1_integrity_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase1IntegrityAlgorithm>");
        try serializePhase1IntegrityAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase1IntegrityAlgorithm>");
    }
    if (value.phase_1_lifetime_seconds) |v| {
        try buf.appendSlice(alloc, "<Phase1LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Phase1LifetimeSeconds>");
    }
    if (value.phase_2_dh_group_numbers) |v| {
        try buf.appendSlice(alloc, "<Phase2DHGroupNumber>");
        try serializePhase2DHGroupNumbersRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase2DHGroupNumber>");
    }
    if (value.phase_2_encryption_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase2EncryptionAlgorithm>");
        try serializePhase2EncryptionAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase2EncryptionAlgorithm>");
    }
    if (value.phase_2_integrity_algorithms) |v| {
        try buf.appendSlice(alloc, "<Phase2IntegrityAlgorithm>");
        try serializePhase2IntegrityAlgorithmsRequestList(alloc, buf, v, "item");
        try buf.appendSlice(alloc, "</Phase2IntegrityAlgorithm>");
    }
    if (value.phase_2_lifetime_seconds) |v| {
        try buf.appendSlice(alloc, "<Phase2LifetimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</Phase2LifetimeSeconds>");
    }
    if (value.pre_shared_key) |v| {
        try buf.appendSlice(alloc, "<PreSharedKey>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</PreSharedKey>");
    }
    if (value.rekey_fuzz_percentage) |v| {
        try buf.appendSlice(alloc, "<RekeyFuzzPercentage>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</RekeyFuzzPercentage>");
    }
    if (value.rekey_margin_time_seconds) |v| {
        try buf.appendSlice(alloc, "<RekeyMarginTimeSeconds>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</RekeyMarginTimeSeconds>");
    }
    if (value.replay_window_size) |v| {
        try buf.appendSlice(alloc, "<ReplayWindowSize>");
        {
            const num_str = std.fmt.allocPrint(alloc, "{d}", .{v}) catch "";
            try buf.appendSlice(alloc, num_str);
        }
        try buf.appendSlice(alloc, "</ReplayWindowSize>");
    }
    if (value.startup_action) |v| {
        try buf.appendSlice(alloc, "<StartupAction>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</StartupAction>");
    }
    if (value.tunnel_inside_cidr) |v| {
        try buf.appendSlice(alloc, "<TunnelInsideCidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TunnelInsideCidr>");
    }
    if (value.tunnel_inside_ipv_6_cidr) |v| {
        try buf.appendSlice(alloc, "<TunnelInsideIpv6Cidr>");
        try aws.xml.appendXmlEscaped(alloc, buf, v);
        try buf.appendSlice(alloc, "</TunnelInsideIpv6Cidr>");
    }
}

