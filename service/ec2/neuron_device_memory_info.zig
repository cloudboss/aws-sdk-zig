/// Describes the memory available to the neuron accelerator.
pub const NeuronDeviceMemoryInfo = struct {
    /// The size of the memory available to the neuron accelerator, in MiB.
    size_in_mi_b: ?i32 = null,
};
