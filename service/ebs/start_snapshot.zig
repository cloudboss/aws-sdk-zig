const aws = @import("aws");
const std = @import("std");

const Client = @import("client.zig").Client;
const CallOptions = @import("call_options.zig").CallOptions;
const ServiceError = @import("errors.zig").ServiceError;
const Tag = @import("tag.zig").Tag;
const SSEType = @import("sse_type.zig").SSEType;
const Status = @import("status.zig").Status;

pub const StartSnapshotInput = struct {
    /// A unique, case-sensitive identifier that you provide to ensure the
    /// idempotency of the
    /// request. Idempotency ensures that an API request completes only once. With
    /// an idempotent
    /// request, if the original request completes successfully. The subsequent
    /// retries with the same
    /// client token return the result from the original successful request and they
    /// have no additional
    /// effect.
    ///
    /// If you do not specify a client token, one is automatically generated by the
    /// Amazon Web Services SDK.
    ///
    /// For more information, see [
    /// Idempotency for StartSnapshot
    /// API](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebs-direct-api-idempotency.html) in the *Amazon Elastic Compute Cloud User Guide*.
    client_token: ?[]const u8 = null,

    /// A description for the snapshot.
    description: ?[]const u8 = null,

    /// Indicates whether to encrypt the snapshot.
    ///
    /// You can't specify **Encrypted** and **
    /// ParentSnapshotId** in the same request. If you specify both parameters, the
    /// request fails with `ValidationException`.
    ///
    /// The encryption status of the snapshot depends on the values that you specify
    /// for
    /// **Encrypted**, **KmsKeyArn**,
    /// and **ParentSnapshotId**, and whether your Amazon Web Services account
    /// is enabled for [
    /// encryption by
    /// default](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/EBSEncryption.html#encryption-by-default). For more information, see [
    /// Using
    /// encryption](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapis-using-encryption.html) in the *Amazon Elastic Compute Cloud User Guide*.
    ///
    /// To create an encrypted snapshot, you must have permission to use the KMS
    /// key. For
    /// more information, see [
    /// Permissions to use Key Management Service
    /// keys](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapi-permissions.html#ebsapi-kms-permissions) in the *Amazon Elastic Compute Cloud User
    /// Guide*.
    encrypted: ?bool = null,

    /// The Amazon Resource Name (ARN) of the Key Management Service (KMS) key to be
    /// used to encrypt the snapshot.
    ///
    /// The encryption status of the snapshot depends on the values that you specify
    /// for
    /// **Encrypted**, **KmsKeyArn**,
    /// and **ParentSnapshotId**, and whether your Amazon Web Services account
    /// is enabled for [
    /// encryption by
    /// default](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/EBSEncryption.html#encryption-by-default). For more information, see [
    /// Using
    /// encryption](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapis-using-encryption.html) in the *Amazon Elastic Compute Cloud User Guide*.
    ///
    /// To create an encrypted snapshot, you must have permission to use the KMS
    /// key. For
    /// more information, see [
    /// Permissions to use Key Management Service
    /// keys](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapi-permissions.html#ebsapi-kms-permissions) in the *Amazon Elastic Compute Cloud User
    /// Guide*.
    kms_key_arn: ?[]const u8 = null,

    /// The ID of the parent snapshot. If there is no parent snapshot, or if you are
    /// creating
    /// the first snapshot for an on-premises volume, omit this parameter.
    ///
    /// You can't specify **ParentSnapshotId** and
    /// **Encrypted** in the same request. If you specify both
    /// parameters, the request fails with `ValidationException`.
    ///
    /// The encryption status of the snapshot depends on the values that you specify
    /// for
    /// **Encrypted**, **KmsKeyArn**,
    /// and **ParentSnapshotId**, and whether your Amazon Web Services account
    /// is enabled for [
    /// encryption by
    /// default](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/EBSEncryption.html#encryption-by-default). For more information, see [
    /// Using
    /// encryption](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapis-using-encryption.html) in the *Amazon Elastic Compute Cloud User Guide*.
    ///
    /// If you specify an encrypted parent snapshot, you must have permission to use
    /// the
    /// KMS key that was used to encrypt the parent snapshot. For more information,
    /// see
    /// [
    /// Permissions to use Key Management Service
    /// keys](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapi-permissions.html#ebsapi-kms-permissions) in the *Amazon Elastic Compute Cloud User
    /// Guide*.
    parent_snapshot_id: ?[]const u8 = null,

    /// The tags to apply to the snapshot.
    tags: ?[]const Tag = null,

    /// The amount of time (in minutes) after which the snapshot is automatically
    /// cancelled
    /// if:
    ///
    /// * No blocks are written to the snapshot.
    ///
    /// * The snapshot is not completed after writing the last block of data.
    ///
    /// If no value is specified, the timeout defaults to `60` minutes.
    timeout: ?i32 = null,

    /// The size of the volume, in GiB. The maximum size is `65536` GiB (64
    /// TiB).
    volume_size: i64,

    pub const json_field_names = .{
        .client_token = "ClientToken",
        .description = "Description",
        .encrypted = "Encrypted",
        .kms_key_arn = "KmsKeyArn",
        .parent_snapshot_id = "ParentSnapshotId",
        .tags = "Tags",
        .timeout = "Timeout",
        .volume_size = "VolumeSize",
    };
};

pub const StartSnapshotOutput = struct {
    /// The size of the blocks in the snapshot, in bytes.
    block_size: ?i32 = null,

    /// The description of the snapshot.
    description: ?[]const u8 = null,

    /// The Amazon Resource Name (ARN) of the Key Management Service (KMS) key used
    /// to encrypt the snapshot.
    kms_key_arn: ?[]const u8 = null,

    /// The Amazon Web Services account ID of the snapshot owner.
    owner_id: ?[]const u8 = null,

    /// The ID of the parent snapshot.
    parent_snapshot_id: ?[]const u8 = null,

    /// The ID of the snapshot.
    snapshot_id: ?[]const u8 = null,

    /// Reserved for future use.
    sse_type: ?SSEType = null,

    /// The timestamp when the snapshot was created.
    start_time: ?i64 = null,

    /// The status of the snapshot.
    status: ?Status = null,

    /// The tags applied to the snapshot. You can specify up to 50 tags per
    /// snapshot. For more
    /// information, see [ Tagging your Amazon EC2
    /// resources](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/Using_Tags.html) in the *Amazon Elastic Compute Cloud User Guide*.
    tags: ?[]const Tag = null,

    /// The size of the volume, in GiB.
    volume_size: ?i64 = null,

    pub const json_field_names = .{
        .block_size = "BlockSize",
        .description = "Description",
        .kms_key_arn = "KmsKeyArn",
        .owner_id = "OwnerId",
        .parent_snapshot_id = "ParentSnapshotId",
        .snapshot_id = "SnapshotId",
        .sse_type = "SseType",
        .start_time = "StartTime",
        .status = "Status",
        .tags = "Tags",
        .volume_size = "VolumeSize",
    };
};

pub fn execute(client: *Client, allocator: std.mem.Allocator, input: StartSnapshotInput, options: CallOptions) !StartSnapshotOutput {
    var arena = std.heap.ArenaAllocator.init(client.allocator);
    defer arena.deinit();
    const alloc = arena.allocator();

    var request = try serializeRequest(alloc, input, client.config);
    defer request.deinit(alloc);

    const creds = try client.config.credentials.getCredentials(alloc);
    try aws.signing.signRequest(alloc, &request, creds, client.config.region, "ebs");

    var response = try client.http_client.sendRequest(&request);
    defer response.deinit();

    if (!response.isSuccess()) {
        if (options.diagnostic) |d| {
            d.* = parseErrorResponse(client.allocator, response.body, response.status) catch .{ .kind = .{ .unknown = .{ .http_status = @intCast(response.status) } } };
        }
        return error.ServiceError;
    }

    const result = try deserializeResponse(allocator, response.body, response.status, response.headers);
    return result;
}

fn serializeRequest(allocator: std.mem.Allocator, input: StartSnapshotInput, config: *aws.Config) !aws.http.Request {
    const endpoint = try config.getEndpointForService("ebs", "EBS", allocator);

    const host = aws.url.parseHost(endpoint);
    const tls = !std.mem.startsWith(u8, endpoint, "http://");
    const port = aws.url.parsePort(endpoint);

    const path = "/snapshots";

    var body_buf: std.ArrayList(u8) = .{};
    var has_prev = false;
    try body_buf.appendSlice(allocator, "{");

    if (input.client_token) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"ClientToken\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.description) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"Description\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.encrypted) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"Encrypted\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.kms_key_arn) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"KmsKeyArn\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.parent_snapshot_id) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"ParentSnapshotId\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.tags) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"Tags\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (input.timeout) |v| {
        if (has_prev) try body_buf.appendSlice(allocator, ",");
        try body_buf.appendSlice(allocator, "\"Timeout\":");
        try aws.json.writeValue(@TypeOf(v), v, allocator, &body_buf);
        has_prev = true;
    }
    if (has_prev) try body_buf.appendSlice(allocator, ",");
    try body_buf.appendSlice(allocator, "\"VolumeSize\":");
    try aws.json.writeValue(@TypeOf(input.volume_size), input.volume_size, allocator, &body_buf);
    has_prev = true;

    try body_buf.appendSlice(allocator, "}");
    const body = try body_buf.toOwnedSlice(allocator);

    var request = aws.http.Request.init(host);
    request.method = .POST;
    request.path = path;
    request.tls = tls;
    request.port = port;
    request.body = body;
    try request.headers.put(allocator, "Content-Type", "application/json");

    return request;
}

fn deserializeResponse(allocator: std.mem.Allocator, body: []const u8, status: u16, headers: anytype) !StartSnapshotOutput {
    var result: StartSnapshotOutput = .{};
    if (body.len > 0) {
        result = try aws.json.parseJsonObject(StartSnapshotOutput, body, allocator);
    }
    _ = status;
    _ = headers;

    return result;
}

fn parseErrorResponse(allocator: std.mem.Allocator, body: []const u8, status: u16) !ServiceError {
    const error_code = blk: {
        const type_str = aws.json.findJsonValue(body, "__type") orelse break :blk @as([]const u8, "Unknown");
        if (std.mem.lastIndexOfScalar(u8, type_str, '#')) |idx| {
            break :blk type_str[idx + 1 ..];
        }
        break :blk type_str;
    };
    const error_message = aws.json.findJsonValue(body, "message") orelse aws.json.findJsonValue(body, "Message") orelse "";
    var arena = std.heap.ArenaAllocator.init(allocator);
    errdefer arena.deinit();
    const arena_alloc = arena.allocator();
    const owned_message = try arena_alloc.dupe(u8, error_message);
    const owned_request_id = try arena_alloc.dupe(u8, "");

    if (std.mem.eql(u8, error_code, "AccessDeniedException")) {
        return .{ .arena = arena, .kind = .{ .access_denied_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ConcurrentLimitExceededException")) {
        return .{ .arena = arena, .kind = .{ .concurrent_limit_exceeded_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ConflictException")) {
        return .{ .arena = arena, .kind = .{ .conflict_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "InternalServerException")) {
        return .{ .arena = arena, .kind = .{ .internal_server_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "RequestThrottledException")) {
        return .{ .arena = arena, .kind = .{ .request_throttled_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ResourceNotFoundException")) {
        return .{ .arena = arena, .kind = .{ .resource_not_found_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ServiceQuotaExceededException")) {
        return .{ .arena = arena, .kind = .{ .service_quota_exceeded_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }
    if (std.mem.eql(u8, error_code, "ValidationException")) {
        return .{ .arena = arena, .kind = .{ .validation_exception = .{
            .message = owned_message,
            .request_id = owned_request_id,
        } } };
    }

    const owned_code = try arena_alloc.dupe(u8, error_code);
    return .{ .arena = arena, .kind = .{ .unknown = .{
        .code = owned_code,
        .message = owned_message,
        .request_id = owned_request_id,
        .http_status = status,
    } } };
}
