pub const AdditionalResultAttributeValueType = enum {
    text_with_highlights_value,

    pub const json_field_names = .{
        .text_with_highlights_value = "TEXT_WITH_HIGHLIGHTS_VALUE",
    };
};
